<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Smarter&#39;s blog</title>
  
  <subtitle>要学习、要快乐</subtitle>
  <link href="https://smarter.xin/atom.xml" rel="self"/>
  
  <link href="https://smarter.xin/"/>
  <updated>2026-04-04T19:11:49.729Z</updated>
  <id>https://smarter.xin/</id>
  
  <author>
    <name>Smarter</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>CUDA系统拆解-18-NCCL、多GPU与通信隐藏：规模扩展怎么不被通信拖垮</title>
    <link href="https://smarter.xin/posts/15f00dc3/"/>
    <id>https://smarter.xin/posts/15f00dc3/</id>
    <published>2026-04-06T09:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-18-nccl-duo-gpu-yu-tong-xin-yin-cang-gui-mo-kuo-zhan-zen-yao-bu-bei-tong-xin-tuo-kua" tabindex="-1">CUDA系统拆解-18-NCCL、多GPU与通信隐藏：规模扩展怎么不被通信拖垮</h1><blockquote><p>本文是「CUDA系统拆解」系列第 18 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/47afb486/">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇讲的是：当系统从单 GPU 走向多 GPU 之后，性能问题为什么会突然变成“通信问题”。</p><p>核心要回答 5 件事：</p><ul><li>多 GPU 新增的核心矛盾到底是什么。</li><li><code>NCCL</code> 到底处在什么位置，为什么它几乎是 NVIDIA 多 GPU 系统的默认基础设施。</li><li><code>Broadcast</code>、<code>AllReduce</code>、<code>AllGather</code>、<code>ReduceScatter</code> 这些 collective 为什么重要。</li><li>为什么 <code>ring</code>、拓扑感知和链路选择会直接影响扩展效率。</li><li>为什么 <code>overlap</code>、<code>pipeline</code>、通信隐藏常常决定大模型系统最终能跑多快。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>单卡优化主要在回答“数据怎么在一张卡里少搬”，多卡优化主要在回答“数据怎么在多张卡之间尽量少搬、尽量晚搬、尽量边搬边算”。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>多 GPU 引入的新核心矛盾通常不是“算力不够”，而是“跨卡通信太贵”。</li><li><code>NCCL</code> 是 NVIDIA 的高性能 GPU collective communication 库，重点不是只提供 API，而是把拓扑感知、通信算法和 CUDA 执行模型结合起来。</li><li><code>Broadcast</code>、<code>AllReduce</code>、<code>AllGather</code>、<code>ReduceScatter</code> 是多 GPU 系统里最常见的 collective，分别对应不同的数据分发、聚合和收集需求。</li><li><code>AllReduce</code> 是最经典的多 GPU 同步原语之一，因为它代表了“大家先规约，再都拿到最终结果”这类高频需求。</li><li><code>ring</code> 常见是因为它对大消息传输带宽利用率很好；但并不存在永远最优的单一算法，拓扑和消息规模会影响选择。</li><li><code>overlap</code> 的本质是让通信时间和计算时间重叠，尽量把等待藏进已有计算里，而不是让通信成本真的消失。</li><li><code>pipeline</code> 的本质是分阶段并行；<code>bubble</code> 是流水线没被填满带来的空闲。</li><li>在大模型训练和推理里，很多最终性能差距都来自通信组织质量，而不是单个 kernel 的峰值。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-duo-gpu-de-ben-zhi-mao-dun-shi-shi-yao" tabindex="-1" id="3-1-多-GPU-的本质矛盾是什么">3.1 多 GPU 的本质矛盾是什么</h3><p>一张 GPU 上，性能问题主要集中在：</p><ul><li>算子组织</li><li>访存层级</li><li>launch 开销</li><li>同步和调度</li></ul><p>到了多 GPU，问题突然多出一层：</p><p><strong>每张卡本地算得很快，但卡和卡之间交换数据很慢。</strong></p><p>这就是多 GPU 最核心的新矛盾。</p><p>从代价层级看，可以粗略理解成：</p><ul><li>registers / shared memory 最快</li><li>本地 HBM 也很快</li><li>同机其他 GPU 更慢</li><li>跨机 GPU 更慢</li></ul><p>所以多 GPU 优化和单 GPU 优化在逻辑上其实是一脉相承的：</p><ul><li>单 GPU：尽量少访问慢层级</li><li>多 GPU：尽量少做跨卡交换</li></ul><h3 id="3-2-nccl-dao-di-shi-shi-yao" tabindex="-1" id="3-2-NCCL-到底是什么">3.2 NCCL 到底是什么</h3><p><code>NCCL</code> 是 NVIDIA 提供的高性能 GPU collective communication 库。它要解决的不是“怎么写一个 kernel”，而是“多张 GPU 怎么高效交换数据”。</p><p>它最重要的价值不只是接口，而是三件事：</p><ul><li>提供常见 collective communication 原语</li><li>感知硬件拓扑并选择更合适的通信路径</li><li>和 CUDA stream、异步执行模型配合，方便做 overlap</li></ul><p>所以理解 <code>NCCL</code> 时，不要把它只看成“多卡 API 集合”。更准确地说，它是 GPU 世界里的通信执行基础设施。</p><h3 id="3-3-shi-yao-shi-collective-communication" tabindex="-1" id="3-3-什么是-collective-communication">3.3 什么是 collective communication</h3><p>所谓 collective，可以先理解成：</p><p><strong>一组 GPU 按某种固定规则一起交换和重组数据。</strong></p><p>最常见的几类是：</p><p><code>Broadcast</code></p><ul><li>一个 GPU 把数据发给所有 GPU</li><li>常见于参数分发、初始化、共享配置同步</li></ul><p><code>AllReduce</code></p><ul><li>每个 GPU 各有一份数据</li><li>先按元素规约，比如 <code>sum</code></li><li>再把最终结果发回所有 GPU</li><li>这是最经典的 collective 之一</li></ul><p><code>AllGather</code></p><ul><li>每个 GPU 提供自己的一块数据</li><li>所有 GPU 最终都拿到拼接后的完整结果</li><li>常见于张量并行后的结果收集</li></ul><p><code>ReduceScatter</code></p><ul><li>先做规约</li><li>再把规约后的结果分块散给不同 GPU</li><li>很适合那些不需要所有 GPU 都拿完整结果的场景</li></ul><p>这些 collective 之所以重要，是因为多 GPU 系统几乎总要在“本地算一部分”和“全局拼结果”之间来回切换。</p><h3 id="3-4-wei-shi-yao-all-reduce-zhe-yao-zhong-yao" tabindex="-1" id="3-4-为什么-AllReduce-这么重要">3.4 为什么 AllReduce 这么重要</h3><p>如果只记一个 collective，那通常先记 <code>AllReduce</code>。</p><p>原因很简单：它代表了多 GPU 协作里最经典的一类需求。</p><p>它同时做两件事：</p><ul><li>聚合</li><li>分发</li></ul><p>所以它经常很贵，也经常是扩展瓶颈。<br>训练里它典型对应梯度同步；推理里在某些张量并行和中间结果合并路径上，也会遇到类似问题。</p><p>面试里如果问“为什么多卡不线性加速”，一个高频原因就是：<br><strong>本地计算虽然分摊了，但每一层或每一步都可能新增 collective 成本。</strong></p><h3 id="3-5-wei-shi-yao-ring-zhe-yao-chang-jian" tabindex="-1" id="3-5-为什么-ring-这么常见">3.5 为什么 ring 这么常见</h3><p><code>ring</code> 是最常见的 collective 实现直觉之一，尤其适合大消息场景。</p><p>直观上可以理解成：</p><ul><li>GPU 组成一个环</li><li>每轮每张卡只和相邻卡交换一块数据</li><li>数据一边转、一边规约或收集</li></ul><p>它的优势主要是：</p><ul><li>链路利用比较均衡</li><li>更容易把所有链路都忙起来</li><li>对大消息时的带宽利用通常不错</li></ul><p>但不要把它神化成唯一正确答案。<br>真实系统里还会有：</p><ul><li>tree</li><li>hierarchical</li><li>拓扑感知的混合策略</li></ul><p>选择和这些因素有关：</p><ul><li>消息大小</li><li>GPU 数量</li><li>NVLink / PCIe / IB 拓扑</li><li>单机还是跨机</li></ul><p>所以 <code>NCCL</code> 的强点之一，就在于它不是死板地用一种算法，而是会结合拓扑和场景做更合理的选择。</p><h3 id="3-6-wei-shi-yao-tuo-bu-gan-zhi-fei-chang-guan-jian" tabindex="-1" id="3-6-为什么拓扑感知非常关键">3.6 为什么拓扑感知非常关键</h3><p>多 GPU 性能不只取决于“用了几张卡”，还取决于“这些卡之间到底怎么连”。</p><p>例如：</p><ul><li>有的 GPU 之间有 NVLink</li><li>有的只能走 PCIe</li><li>跨机要走 IB 或其他网络</li><li>某些链路可能共享交换资源</li></ul><p>如果通信路径选得不好，就会出现：</p><ul><li>热点链路</li><li>带宽利用不均</li><li>某些 GPU 长时间等待</li></ul><p>所以 <code>NCCL</code> 的价值很大一部分在于：<br><strong>它在做 topology-aware 的通信组织，而不是只在做“把字节发出去”。</strong></p><h3 id="3-7-overlap-pipeline-tong-xin-yin-cang-shi-shi-yao-yi-si" tabindex="-1" id="3-7-overlap、pipeline、通信隐藏是什么意思">3.7 overlap、pipeline、通信隐藏是什么意思</h3><p>这几个词经常一起出现，但不是一回事。</p><p><code>overlap</code></p><ul><li>指通信和计算重叠</li><li>理想目标是让总时间更接近 <code>max(计算时间, 通信时间)</code>，而不是两者简单相加</li></ul><p><code>pipeline</code></p><ul><li>指把系统拆成多个阶段，让不同阶段并行推进</li><li>常见于 pipeline parallel，也常见于更一般的数据流分阶段组织</li></ul><p><code>communication hiding</code></p><ul><li>指尽量把通信等待藏进已有的计算时间里</li><li>它依赖 overlap，但不等于“通信成本不存在”</li></ul><p>这三者的重要性在于：<br>在多 GPU 系统里，最怕的不是“有通信”，而是“通信裸露在关键路径上，所有卡都在等”。</p><h3 id="3-8-pipeline-bubble-shi-shi-yao" tabindex="-1" id="3-8-pipeline-bubble-是什么">3.8 pipeline bubble 是什么</h3><p>一旦谈 pipeline，就绕不开 <code>bubble</code>。</p><p><code>bubble</code> 可以理解成：</p><ul><li>流水线没有被填满时产生的空闲</li><li>或者不同 stage 不平衡导致的等待</li></ul><p>典型来源包括：</p><ul><li>stage 切分不均衡</li><li>micro-batch 太少</li><li>某些阶段明显更慢</li></ul><p>所以 pipeline 的关键不只是“分阶段”，还包括：</p><ul><li>阶段怎么切</li><li>每阶段负载是否均衡</li><li>是否有足够多的 micro-batch 来填满流水线</li></ul><h3 id="3-9-wei-shi-yao-duo-gpu-chang-chang-bu-xian-xing-jia-su" tabindex="-1" id="3-9-为什么多-GPU-常常不线性加速">3.9 为什么多 GPU 常常不线性加速</h3><p>这是最常见的系统问题之一。</p><p>多卡扩展不线性的典型原因包括：</p><ul><li>通信占比上升</li><li>通信无法被完全隐藏</li><li>切分过细，导致本地计算太碎</li><li>launch 和同步开销比例上升</li><li>拓扑不理想</li><li>负载不均衡</li></ul><p>所以多 GPU 扩展效率的本质问题是：</p><p><strong>卡虽然变多了，但“等待”的部分也变多了。</strong></p><h3 id="3-10-he-bing-xing-ce-lue-de-guan-xi" tabindex="-1" id="3-10-和并行策略的关系">3.10 和并行策略的关系</h3><p>通信模式不是独立存在的，它通常由并行切分方式决定。</p><p>常见直觉是：</p><ul><li><code>data parallel</code> 更容易出现梯度同步类通信</li><li><code>tensor parallel</code> 经常需要 <code>AllGather</code>、<code>ReduceScatter</code>、<code>AllReduce</code></li><li><code>pipeline parallel</code> 更强调相邻 stage 之间的传递和流水线组织</li><li><code>expert parallel / MoE</code> 会更敏感于 token 分发和跨设备交换</li></ul><p>这意味着真正分析一个多 GPU 系统时，不应该先问“用了什么 collective”，而应该先问：</p><ul><li>模型怎么切</li><li>数据怎么切</li><li>每一步的通信到底发生在哪里</li></ul><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>对大模型推理来说，这一篇非常关键，因为推理尤其容易暴露通信问题。</p><p>原因之一是 decode 阶段往往本地计算时间短：</p><ul><li>每步新增计算量小</li><li>kernel 更碎</li><li>launch 更频繁</li><li>于是通信更难被隐藏</li></ul><p>所以在大模型推理里，很多工程动作最终都在围绕同一个目标：</p><ul><li>减少每步必须做的跨卡同步</li><li>尽量让通信和计算重叠</li><li>让切分、调度、KV locality 和通信路径尽量一致</li></ul><p>这也是为什么 AI infra / 推理岗位会非常看重你对 <code>NCCL</code>、<code>collective</code>、<code>overlap</code>、<code>pipeline bubble</code> 的理解。它们直接决定多卡系统能不能真正跑出吞吐。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>NCCL</code> 不只是“多卡 API 库”，它的真正价值在于 topology-aware 通信和与 CUDA 执行模型的集成。</li><li><code>overlap</code> 不是让通信成本消失，而是尽量把通信等待藏进已有计算里。</li><li><code>ring</code> 不是永远最优，只是它在很多大消息场景下带宽利用很好。</li><li>多 GPU 不线性加速不一定是 kernel 太慢，很多时候是通信、同步或负载不均衡在拖后腿。</li><li>pipeline 不等于自动高效；如果 stage 切分差或 bubble 大，流水线本身也会浪费很多时间。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么说多 GPU 新增的核心矛盾通常是通信成本，而不是单纯算力不足？</li><li><code>NCCL</code> 解决的核心问题是什么？为什么不能只把它理解成一组 API？</li><li><code>Broadcast</code>、<code>AllReduce</code>、<code>AllGather</code>、<code>ReduceScatter</code> 各自更适合哪类数据流场景？</li><li>为什么 <code>AllReduce</code> 经常成为多 GPU 扩展瓶颈？</li><li><code>ring</code> 为什么常见？它的优势和局限分别是什么？</li><li>为什么拓扑感知会直接影响多 GPU 性能？</li><li><code>overlap</code>、<code>pipeline</code>、<code>communication hiding</code>、<code>bubble</code> 分别在讲什么？</li><li>为什么 decode 阶段的多 GPU 推理往往比 prefill 更容易暴露通信问题？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/47afb486/">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</a></li><li>延伸阅读：<a href="/posts/68e6bad5/">GPU系统拆解-13-多 GPU 与通信：并行策略、拓扑与扩展代价</a></li><li>延伸阅读：<a href="/posts/b599e19f/">CUDA实践-15-多卡通信与全归约</a></li><li>延伸阅读：<a href="/posts/9ce7b444/">vLLM系统拆解-10-分布式部署：TP、PP、DP、EP各解决什么问题</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-18-nccl-duo-gpu-yu-tong-xin-yin-cang-gui-mo-kuo-zhan-zen-yao-bu-bei-tong-xin-tuo-kua&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="Pipeline" scheme="https://smarter.xin/tags/pipeline/"/>
    
    <category term="NCCL" scheme="https://smarter.xin/tags/nccl/"/>
    
    <category term="NVLink" scheme="https://smarter.xin/tags/nvlink/"/>
    
    <category term="多GPU" scheme="https://smarter.xin/tags/multi-gpu/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</title>
    <link href="https://smarter.xin/posts/47afb486/"/>
    <id>https://smarter.xin/posts/47afb486/</id>
    <published>2026-04-06T08:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-17-v-llm-tensor-rt-llm-yu-continuous-batching-cuda-wei-shi-yao-zui-zhong-lian-dao-tui-li-xi-tong" tabindex="-1">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</h1><blockquote><p>本文是「CUDA系统拆解」系列第 17 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/7061c0d7/">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</a><br>下一篇：<a href="/posts/15f00dc3/">CUDA系统拆解-18-NCCL、多GPU与通信隐藏：规模扩展怎么不被通信拖垮</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇要讲清的不是“某个框架怎么用”，而是现代 LLM 在线推理到底在解决什么系统问题。核心有 6 件事：</p><ul><li>为什么在线 LLM serving 和离线前向不是同一个问题。</li><li>为什么 <code>prefill</code> 更偏 <code>compute-bound</code>，而 <code>decode</code> 更偏 <code>memory-bound</code>。</li><li>为什么传统静态 batching 不够用。</li><li><code>continuous batching / in-flight batching</code> 到底在解决什么调度问题。</li><li><code>paged KV cache / paged attention</code> 到底在解决什么显存和访存问题。</li><li>为什么调度、kernel 和显存管理必须一起看，而不能拆开看。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>现代 LLM 推理系统的核心竞争力，很多时候不在“模型会不会跑”，而在“动态请求怎么调度、KV cache 怎么管理、decode 阶段怎么把 GPU 尽量喂满”。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>在线 LLM serving 面对的是动态到达、变长、生命周期不同的请求，和离线固定 batch 前向完全不是一类问题。</li><li><code>prefill</code> 通常更偏 <code>compute-bound</code>，因为计算密集、更像大 GEMM；<code>decode</code> 通常更偏 <code>memory-bound</code>，因为每步计算小，但要频繁读取历史 KV cache。</li><li>静态 batching 假设请求整齐、同步、同生命周期，这和真实在线请求分布不匹配。</li><li><code>continuous batching / in-flight batching</code> 的目标是持续维护活跃请求集合，让新请求进入、已完成请求退出，从而减少 GPU 空转。</li><li><code>paged KV cache</code> 的意义不是“一个存储小技巧”，而是把 KV cache 从大块连续内存改成小块分页管理，降低显存浪费并支持动态调度。</li><li><code>paged attention</code> 不只是内存管理，还要求 attention kernel 适配“逻辑连续、物理分块”的 KV 访问方式。</li><li>vLLM 和 TensorRT-LLM 的共同点，是都在围绕调度、KV cache、decode 吞吐和低延迟做系统设计；差别更多在工程栈、优化深度和部署取向。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-xu-yao-zhuan-men-de-llm-tui-li-kuang-jia" tabindex="-1" id="3-1-为什么需要专门的-LLM-推理框架">3.1 为什么需要专门的 LLM 推理框架</h3><p>如果只是离线跑一批固定 shape 的 Transformer 前向，普通深度学习框架加标准库也能做。但在线 LLM serving 面对的不是这种条件。</p><p>在线请求有几个根本特点：</p><ul><li>请求是动态到达的，不会整齐地等你凑满一个 batch</li><li>prompt 长度不同，输出长度也不同</li><li>一部分请求还在 <code>prefill</code></li><li>一部分请求已经进入 <code>decode</code></li><li>每个请求的 KV cache 都在持续增长</li><li>有的请求很快结束，有的请求会跑很久</li></ul><p>所以问题不再只是“算子快不快”，而是：</p><ul><li>当前该让哪些请求上 GPU</li><li>这些请求的 KV cache 放在哪里</li><li>哪些 kernel 该在这一轮执行</li><li>如何在吞吐和延迟之间做折中</li></ul><p>这就是为什么 vLLM、TensorRT-LLM 这类框架存在。它们不是简单把模型封装起来，而是在做在线推理的系统组织。</p><h3 id="3-2-prefill-he-decode-wei-shi-yao-bi-xu-fen-kai-li-jie" tabindex="-1" id="3-2-Prefill-和-decode-为什么必须分开理解">3.2 Prefill 和 decode 为什么必须分开理解</h3><p>LLM 推理通常分成两个性质很不一样的阶段。</p><p><code>prefill</code> 阶段：</p><ul><li>一次性处理整个 prompt</li><li>attention 和线性层计算量都比较大</li><li>更接近大矩阵计算</li><li>通常更偏 <code>compute-bound</code></li><li>更容易利用 GEMM、Tensor Core 和高吞吐 kernel</li></ul><p><code>decode</code> 阶段：</p><ul><li>每次只生成一个新 token</li><li>新增计算量相对较小</li><li>但每一步都要读取历史所有 token 的 KV cache</li><li>很多时间花在读 KV 和组织访存上</li><li>通常更偏 <code>memory-bound</code></li></ul><p>这是 LLM 推理里最重要的系统直觉之一。<br>如果不把 prefill 和 decode 分开看，就很容易误判瓶颈。</p><h3 id="3-3-wei-shi-yao-jing-tai-batching-bu-gou-yong" tabindex="-1" id="3-3-为什么静态-batching-不够用">3.3 为什么静态 batching 不够用</h3><p>最朴素的 batching 思路是：</p><ul><li>等一批请求凑齐</li><li>pad 到相近长度</li><li>一起执行</li><li>整批结束后再处理下一批</li></ul><p>这在离线推理里还能工作，但在线 serving 问题很多：</p><ul><li>请求到达时间不同，不能一直等</li><li>请求长度差异大，padding 浪费明显</li><li>decode 是逐 token 推进，不同请求进度不同</li><li>有的请求已经完成，有的请求还要继续跑</li><li>KV cache 的增长和回收都不是同步的</li></ul><p>也就是说，静态 batching 假设的是“整齐、同步、同生命周期”的任务；而在线 LLM 请求恰恰是“异步、变长、生命周期不同”的任务。</p><h3 id="3-4-continuous-batching-in-flight-batching-shi-shi-yao" tabindex="-1" id="3-4-continuous-batching-in-flight-batching-是什么">3.4 continuous batching / in-flight batching 是什么</h3><p><code>continuous batching</code> 的直觉定义是：</p><p><strong>不再让一整批请求同生共死，而是在每个调度步里动态维护一个活跃请求集合。</strong></p><p>它的典型行为是：</p><ul><li>新请求可以持续加入</li><li>已完成请求可以立刻退出</li><li>GPU 每一轮都尽量被可运行请求填满</li><li>系统不必等整批结束再开启下一批</li></ul><p>TensorRT-LLM 常用 <code>in-flight batching</code> 这个说法，本质上和 continuous batching 是同类思想：请求在系统运行过程中动态进入、退出和重排。</p><p>它解决的核心问题是：</p><ul><li>提升 GPU 利用率</li><li>减少短请求被长请求拖住</li><li>减少“整批等待”带来的空转</li></ul><p>但它也会带来新复杂度：</p><ul><li>每一轮 batch shape 都可能变化</li><li>prefill 和 decode 可能要混合调度</li><li>KV cache 管理更复杂</li><li>kernel 输入组织更难</li><li>CUDA Graph 的适用范围也会受影响</li></ul><p>所以 continuous batching 不是一个“小技巧”，而是整个 serving 系统的调度骨架。</p><h3 id="3-5-wei-shi-yao-kv-cache-shi-zai-xian-tui-li-de-he-xin-nan-dian" tabindex="-1" id="3-5-为什么-KV-cache-是在线推理的核心难点">3.5 为什么 KV cache 是在线推理的核心难点</h3><p>在 decode 阶段，每生成一个 token，都要把新的 K/V 存下来，供后续 token 使用。因此每个请求的 KV cache 会随着生成过程不断增长。</p><p>如果用最朴素的方法管理 KV cache，通常会想到：</p><ul><li>给每个请求分一段连续的大显存</li><li>然后顺着往后写</li></ul><p>这在动态在线系统里问题很大：</p><ul><li>你不知道每个请求最后会生成多长</li><li>按最大长度预留会浪费很多显存</li><li>请求结束后空间回收不够灵活</li><li>如果空间不够，扩容和迁移代价很高</li></ul><p>这和操作系统里的动态内存管理问题很像。真正困难的不是“能不能存”，而是“能不能在动态请求场景下高效分配、增长、回收和复用”。</p><h3 id="3-6-paged-kv-cache-paged-attention-dao-di-shi-shi-yao" tabindex="-1" id="3-6-paged-KV-cache-paged-attention-到底是什么">3.6 paged KV cache / paged attention 到底是什么</h3><p><code>paged KV cache</code> 的核心思想是：</p><p><strong>不要求一个请求的 KV cache 必须物理连续，而是把它切成固定大小的 blocks/pages，再通过映射关系把逻辑位置对应到物理位置。</strong></p><p>这样做有几个直接好处：</p><ul><li>显存分配粒度更细，浪费更少</li><li>请求增长时只需要继续分配新块</li><li>请求结束后回收更灵活</li><li>更适合和 continuous batching 一起工作</li></ul><p>但这还只是内存管理的一半。<br>真正的难点在于：attention kernel 也得跟着变。</p><p>因为现在历史 KV 不再是一整段连续内存，而是分散在多个 block 里。于是 kernel 读取历史 K/V 时需要：</p><ul><li>先根据逻辑位置找到对应物理 block</li><li>再在 block 内定位偏移</li><li>按 block 组织访存和计算</li></ul><p>所以更准确地说：</p><ul><li><code>paged KV cache</code> 解决的是存储和生命周期管理问题</li><li><code>paged attention</code> 解决的是适配这种分页布局后的 attention 访存与计算问题</li></ul><p>这就是为什么它不只是“显存管理策略”，而是“显存布局 + kernel 访问模型”一起设计。</p><h3 id="3-7-wei-shi-yao-diao-du-kernel-he-xian-cun-guan-li-bi-xu-yi-qi-kan" tabindex="-1" id="3-7-为什么调度、kernel-和显存管理必须一起看">3.7 为什么调度、kernel 和显存管理必须一起看</h3><p>这一点是很多人最容易漏掉的。</p><p>如果你只看调度，会觉得 continuous batching 只是队列管理。<br>如果你只看显存，会觉得 paged KV cache 只是内存分配。<br>如果你只看 kernel，会觉得 attention 只是算子优化。</p><p>但在真实系统里，这三者强耦合：</p><ul><li>调度决定这一轮哪些请求进入活跃集合</li><li>活跃集合决定当前 batch shape 和要读哪些 KV blocks</li><li>KV 布局决定 attention kernel 的访存模式</li><li>kernel 的开销和 shape 特征反过来影响调度策略</li></ul><p>所以现代 LLM 推理系统本质上是在同时平衡：</p><ul><li>吞吐</li><li>延迟</li><li>显存利用率</li><li>launch overhead</li><li>decode 阶段的访存效率</li></ul><p>这也是为什么“框架层术语”最终都能翻译回 GPU 语言：</p><ul><li><code>continuous batching</code> 是调度层在减少 GPU 空转</li><li><code>paged KV cache</code> 是显存层在做细粒度动态管理</li><li><code>paged attention</code> 是 kernel 层在适配分块访存</li><li><code>chunked prefill</code> 是在平衡 compute-heavy prefill 和 memory-heavy decode</li></ul><h3 id="3-8-v-llm-he-tensor-rt-llm-de-ding-wei-chai-bie" tabindex="-1" id="3-8-vLLM-和-TensorRT-LLM-的定位差别">3.8 vLLM 和 TensorRT-LLM 的定位差别</h3><p>从学习角度看，可以把两者这样理解：</p><p><code>vLLM</code> 更像：</p><ul><li>围绕高吞吐 serving 组织的一套开源框架</li><li>强调 paged attention、continuous batching、KV cache 管理</li><li>很适合作为理解现代 LLM serving 机制的入口</li></ul><p><code>TensorRT-LLM</code> 更像：</p><ul><li>更深地绑定 NVIDIA 推理栈的高性能生产化路线</li><li>更强调 engine 化、量化、调度、并行策略和硬件协同</li><li>更适合看“如何把这些机制和 NVIDIA 优化栈压得更深”</li></ul><p>更好的面试回答不是“谁更强”，而是：</p><p><strong>两者都围绕同一组核心矛盾工作：动态请求调度、KV cache 管理、decode 吞吐和低延迟；差别主要在工程栈、优化深度和部署目标。</strong></p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这一篇其实就是 AI 推理系统本体。</p><p>前面的 CUDA、访存、Tensor Core、FlashAttention 这些知识，到这里才真正汇总成系统判断：</p><ul><li>为什么 decode 常常不由 Tensor Core 峰值决定，而由 KV 读取决定</li><li>为什么框架设计要围绕请求生命周期，而不是只围绕算子</li><li>为什么高 QPS、低延迟和高显存利用率往往互相牵制</li><li>为什么 serving 框架的竞争力很大一部分来自 scheduler、KV cache 和 kernel 组织，而不是单个 API</li></ul><p>对于 AI infra / 推理岗位，这一篇是典型高频主线，因为它能直接暴露你有没有“从单 kernel 视角切换到系统视角”。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>在线 LLM serving 不是“把离线前向多跑几次”，两者面对的请求分布和系统约束完全不同。</li><li><code>decode</code> 不是天然 compute-heavy，很多时候它更偏 <code>memory-bound</code>。</li><li><code>continuous batching</code> 不只是把 batch 变动态，它背后是完整的调度问题。</li><li><code>paged KV cache</code> 不只是节省显存，它还影响 attention kernel 的访问模式。</li><li>不是只要 kernel 很快，系统吞吐就一定高；如果调度和 KV 管理做不好，GPU 仍然会空转或被访存拖住。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么在线 LLM serving 和离线固定 batch 前向不是同一个问题？</li><li>为什么 <code>prefill</code> 更偏 <code>compute-bound</code>，而 <code>decode</code> 更偏 <code>memory-bound</code>？</li><li>静态 batching 的假设是什么？它为什么不适合真实在线请求？</li><li><code>continuous batching / in-flight batching</code> 的目标和代价分别是什么？</li><li><code>paged KV cache</code> 解决了哪些显存管理问题？为什么它天然适合动态请求？</li><li>为什么 <code>paged attention</code> 不能只理解成“内存分页”，而必须连同 kernel 访问方式一起理解？</li><li>如果一个系统 decode 吞吐很差，你应该只盯着单个 attention kernel，还是把调度、KV cache 和 kernel 一起查？为什么？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/7061c0d7/">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</a></li><li>下一篇：<a href="/posts/15f00dc3/">CUDA系统拆解-18-NCCL、多GPU与通信隐藏：规模扩展怎么不被通信拖垮</a></li><li>延伸阅读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>延伸阅读：<a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-17-v-llm-tensor-rt-llm-yu-continuous-batching-cuda-wei-shi-yao-zui-zhong-lian-dao-tui-li-xi-tong&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="PagedAttention" scheme="https://smarter.xin/tags/paged-attention/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="Continuous Batching" scheme="https://smarter.xin/tags/continuous-batching/"/>
    
    <category term="TensorRT-LLM" scheme="https://smarter.xin/tags/tensorrt-llm/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</title>
    <link href="https://smarter.xin/posts/7061c0d7/"/>
    <id>https://smarter.xin/posts/7061c0d7/</id>
    <published>2026-04-06T08:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-16-cutlass-triton-cu-blas-yu-flash-attention-gao-xing-neng-shi-xian-du-zai-zuo-shi-yao" tabindex="-1">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</h1><blockquote><p>本文是「CUDA系统拆解」系列第 16 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/bd9c352d/">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</a><br>下一篇：<a href="/posts/47afb486/">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇不是教你背 API，而是帮你建立一张高性能实现路线图。核心问题有 5 个：</p><ul><li><code>cuBLAS / cuBLASLt</code>、<code>CUTLASS</code>、<code>Triton</code>、<code>FlashAttention</code> 各自在解决什么问题。</li><li>为什么 GPU 上很多高性能实现都喜欢从 <code>GEMM</code> 思维出发。</li><li>为什么 <code>FlashAttention</code> 的核心是 IO-aware，而不只是“把几个算子 fusion 起来”。</li><li>什么时候应该优先调库，什么时候才值得自己定制 kernel。</li><li>这些路线在 AI 推理工程里分别处在什么位置，有什么边界和取舍。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>高性能实现不只是“算得快”，更关键的是“数据怎么分块、怎么复用、怎么少搬、怎么把硬件峰值真正喂满”。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>cuBLAS / cuBLASLt</code> 代表标准高性能库路线，适合标准 GEMM 和它的常见变体。</li><li><code>CUTLASS</code> 代表结构化构建高性能 GEMM / Tensor Core kernel 的路线，重点在分层组织和可定制性。</li><li><code>Triton</code> 代表更高生产力的 GPU kernel DSL 路线，特别适合 fusion、特殊 layout、快速迭代的定制算子。</li><li><code>FlashAttention</code> 代表 IO-aware 的专用算法与 kernel 共设计路线，核心是减少 attention 中间结果的显存读写。</li><li>很多高性能实现都从 <code>GEMM</code> 思维出发，因为它天然适合 tiling、数据复用、shared memory staging 和 Tensor Core。</li><li>真实工程里这几条路线通常不是互相替代，而是分工协作。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-si-tiao-gao-xing-neng-lu-xian-de-zong-tu" tabindex="-1" id="3-1-四条高性能路线的总图">3.1 四条高性能路线的总图</h3><p>可以先把这四条路线粗略分成四类：</p><ul><li><code>cuBLAS / cuBLASLt</code>：标准高性能库</li><li><code>CUTLASS</code>：高性能 kernel 的结构化积木</li><li><code>Triton</code>：高生产力的定制 kernel 语言和编译器</li><li><code>FlashAttention</code>：面向 attention 的 IO-aware 专用算法与实现</li></ul><p>它们不是同一层的东西，所以不要直接问“谁替代谁”。更合理的问题是：当前这个瓶颈属于哪一类问题，应该用哪条路线解决。</p><h3 id="3-2-wei-shi-yao-gao-xing-neng-shi-xian-chang-cong-gemm-si-wei-chu-fa" tabindex="-1" id="3-2-为什么高性能实现常从-GEMM-思维出发">3.2 为什么高性能实现常从 GEMM 思维出发</h3><p>在 GPU 上，<code>GEMM</code> 是最重要的高性能母问题之一。原因很简单：</p><ul><li>计算模式规则，容易分块</li><li>数据复用高，适合做 tiling</li><li>算术强度高，容易把 GPU 算力压出来</li><li>很适合走 shared memory + registers + Tensor Core 这条路径</li></ul><p>而 AI 推理里很多看起来不完全一样的计算，最后都能尽量改写成 GEMM 风格：</p><ul><li>线性层</li><li>MLP</li><li>attention 里的 <code>QK^T</code></li><li>attention 里的 <code>PV</code></li><li>一些卷积实现</li></ul><p>所以高性能实现经常围绕这些问题组织：</p><ul><li>一个 block 负责哪一块输出</li><li>输入 tile 怎么搬运</li><li>shared memory 怎么做 staging</li><li>registers 怎么保存局部累加</li><li>warp / Tensor Core 怎么参与计算</li><li>epilogue 是否顺手融合 bias、activation、scale</li></ul><p>一旦你接受“很多高性能 kernel 本质上是在做 GEMM 风格的数据流设计”，后面看 <code>cuBLAS</code>、<code>CUTLASS</code>、<code>Triton</code> 甚至 <code>FlashAttention</code> 都会顺很多。</p><h3 id="3-3-cu-blas-cu-blas-lt-wei-shi-yao-biao-zhun-wen-ti-you-xian-diao-ku" tabindex="-1" id="3-3-cuBLAS-cuBLASLt：为什么标准问题优先调库">3.3 cuBLAS / cuBLASLt：为什么标准问题优先调库</h3><p><code>cuBLAS</code> 的核心价值不是“能算矩阵乘法”，而是它把标准线代问题做成了成熟、稳定、跨架构持续优化的高性能方案。</p><p>标准 GEMM 很难随手超越，原因不在于公式复杂，而在于工程细节极多：</p><ul><li>tile 形状怎么选</li><li>layout 和对齐怎么处理</li><li>Tensor Core 路径怎么映射</li><li>shared memory 和 registers 怎么配合</li><li>prefetch、pipeline、epilogue 怎么组织</li><li>不同 shape、dtype、架构下怎么选更优 kernel</li></ul><p>所以工程上一个很实际的原则是：</p><p><strong>如果问题本质上还是标准 GEMM 或 batched GEMM，先看 <code>cuBLAS / cuBLASLt</code> 能不能解决。</strong></p><p>这里 <code>cuBLASLt</code> 特别重要，因为现代 AI workload 往往不只是“裸 GEMM”，还会关心：</p><ul><li>bias</li><li>activation</li><li>layout</li><li>quantized path</li><li>更灵活的 epilogue</li><li>heuristic 和 autotuning</li></ul><p>这也是为什么很多推理系统真正依赖的不是最老的 <code>cuBLAS</code> 接口，而是更现代的 <code>cuBLASLt</code>。</p><h3 id="3-4-cutlass-gao-xing-neng-kernel-bu-shi-yi-duan-dai-ma-er-shi-yi-tao-ceng-ci-jie-gou" tabindex="-1" id="3-4-CUTLASS：高性能-kernel-不是一段代码，而是一套层次结构">3.4 CUTLASS：高性能 kernel 不是一段代码，而是一套层次结构</h3><p><code>CUTLASS</code> 的价值不在于“又多了一个 GEMM 库”，而在于它把高性能 GEMM / Tensor Core kernel 的组织方式拆成了可复用的结构。</p><p>最重要的不是具体模板，而是这套思路：</p><ul><li>threadblock-level tile</li><li>warp-level tile</li><li>instruction-level tile</li><li>global memory -&gt; shared memory -&gt; registers 的数据路径</li><li>mainloop 与 epilogue 的拆分</li></ul><p>换句话说，<code>CUTLASS</code> 在教你一件事：</p><p><strong>高性能 kernel 不是“把 for-loop 搬到 GPU 上”，而是把计算层次、存储层次和执行层次一起对齐。</strong></p><p>所以 <code>CUTLASS</code> 很适合两类人：</p><ul><li>想看懂高性能 GEMM / Tensor Core kernel 是怎么搭出来的人</li><li>需要在接近库级性能的前提下做深度定制的人</li></ul><p>它在工程里的定位通常是：</p><ul><li>不想从零赤手写所有底层细节</li><li>但标准库表达力不够</li><li>需要更细地控制 tile、epilogue、pipeline、layout</li></ul><h3 id="3-5-triton-wei-shi-yao-ta-te-bie-gua-he-ding-zhi-he-fusion" tabindex="-1" id="3-5-Triton：为什么它特别适合定制和-fusion">3.5 Triton：为什么它特别适合定制和 fusion</h3><p><code>Triton</code> 的核心不是“比 CUDA 更底层”，而是“用更高效的方式表达 tile 级数据流和定制 kernel 结构”。</p><p>它特别受欢迎，是因为现实里有很多算子处在一个尴尬区间：</p><ul><li>直接调库不完全合适</li><li>手写 CUDA 成本高、调试慢、维护重</li><li>但性能又不能退化太多</li></ul><p>这正是 Triton 的甜点区：</p><ul><li>fused operator</li><li>pointwise + reduction 组合</li><li>特殊 layout 下的自定义 kernel</li><li>快速试验不同 tiling 和 autotune 配置</li></ul><p>它的一个关键优势，是很适合把多步逻辑收进一个 kernel，减少不必要的 global memory 往返。例如：</p><ul><li>fused softmax</li><li>fused layernorm</li><li>fused activation</li><li>一些 attention 周边算子</li></ul><p>也就是说，Triton 的价值常常不只是“写起来更短”，而是更容易把你真正想要的 tile 级数据流写对。</p><p>但不要神化它。Triton 仍然吃性能基本功：</p><ul><li>memory access pattern</li><li>register pressure</li><li>tile 设计</li><li>Tensor Core 映射</li><li>fusion 收益是否真的大于复杂度</li></ul><p>所以真正强的不是“会写 Triton”，而是既懂 CUDA 性能原理，又会用 Triton 更快把这些原理落成代码。</p><h3 id="3-6-flash-attention-he-xin-bu-shi-fusion-er-shi-io-aware" tabindex="-1" id="3-6-FlashAttention：核心不是-fusion，而是-IO-aware">3.6 FlashAttention：核心不是 fusion，而是 IO-aware</h3><p><code>FlashAttention</code> 和前面三者不完全同类，因为它首先是一条专门针对 attention 的算法与实现路线。</p><p>标准 attention 的直觉流程是：</p><ol><li>计算 <code>S = QK^T</code></li><li>对 <code>S</code> 做缩放、mask、softmax</li><li>再算 <code>O = softmax(S)V</code></li></ol><p>问题在于，<code>S</code> 往往是一个很大的中间矩阵。序列一长，这个中间结果的显存读写就会变得非常重。于是瓶颈很容易不在 FLOPs，而在 IO。</p><p><code>FlashAttention</code> 的核心思想是：</p><ul><li>按块处理 <code>Q / K / V</code></li><li>尽量把中间状态留在片上路径里</li><li>不去物化完整的 attention score 矩阵</li><li>用 online softmax 保证分块处理时仍能得到精确结果</li></ul><p>所以它真正优化的是：</p><ul><li>HBM / global memory traffic</li><li>attention 中间状态的物化成本</li><li>长序列下的 IO 压力</li></ul><p>这就是为什么说它是 <code>IO-aware</code>。<br>它当然也会用到 Tensor Core、shared memory、warp 协作，但这些只是手段。真正的一层思想是：<strong>attention 不能只从“算”看，还必须从“搬”看。</strong></p><h3 id="3-7-si-tiao-lu-xian-bu-shi-jing-zheng-guan-xi-er-shi-fen-gong-guan-xi" tabindex="-1" id="3-7-四条路线不是竞争关系，而是分工关系">3.7 四条路线不是竞争关系，而是分工关系</h3><p>更实用的理解方式是：</p><ul><li><code>cuBLAS / cuBLASLt</code>：标准问题的首选</li><li><code>CUTLASS</code>：需要理解和构建高性能 GEMM/Tensor Core 路线时的结构化基础</li><li><code>Triton</code>：需要高生产力地写定制 kernel、尤其是 fusion 时的强工具</li><li><code>FlashAttention</code>：当 attention 的核心矛盾在 IO，而不是普通 GEMM 本身时的专用路线</li></ul><p>所以真实推理系统通常是混用的。例如：</p><ul><li>线性层主干走 <code>cuBLASLt</code></li><li>一些 fused normalization / activation / special layout kernel 用 <code>Triton</code></li><li>某些深度定制矩阵核参考或基于 <code>CUTLASS</code></li><li>attention 主路径走 <code>FlashAttention</code> 或类似的 paged attention 变体</li></ul><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>站在推理工程角度，这四条路线的差别，本质上是四种 trade-off：</p><ul><li>标准化 vs 定制化</li><li>生产力 vs 极限控制力</li><li>通用性 vs 算子特化</li><li>算力峰值 vs IO 路径优化</li></ul><p>在推理系统里，真正重要的不是“我会不会某个工具”，而是你能不能判断：</p><ul><li>当前瓶颈更偏 <code>compute-bound</code> 还是 <code>memory-bound</code></li><li>是标准 GEMM 问题，还是 fusion 问题，还是 attention 的 IO 问题</li><li>是继续调库更划算，还是值得投入定制 kernel</li></ul><p>这也是 AI infra / 推理岗位真正看重的能力：不是工具崇拜，而是代价模型和工程判断。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>Triton</code> 不是“自动比 CUDA 更快”，它只是更容易把某些定制 kernel 写出来。</li><li><code>CUTLASS</code> 不是简单的“另一个调用库”，它更像高性能 GEMM/Tensor Core kernel 的结构化框架。</li><li><code>FlashAttention</code> 不只是把几个算子融合在一起，它的核心是重构 attention 的数据流，减少 IO。</li><li>不是所有问题都该自己写 kernel；标准 GEMM 问题优先调 <code>cuBLAS / cuBLASLt</code> 往往才是成熟工程选择。</li><li>不是所有 attention 优化都等价于 <code>FlashAttention</code>；只有当核心矛盾真在中间状态物化和 IO 时，这条路线才最有价值。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么说很多 GPU 高性能实现都能从 <code>GEMM</code> 思维出发？</li><li><code>cuBLAS / cuBLASLt</code>、<code>CUTLASS</code>、<code>Triton</code>、<code>FlashAttention</code> 分别适合解决哪类问题？</li><li>为什么标准 GEMM 通常优先调库，而不是自己写 CUDA kernel？</li><li><code>CUTLASS</code> 最值得学的到底是某个模板参数，还是那套分层结构？为什么？</li><li>Triton 为什么特别适合 fusion 和快速迭代？它的边界又在哪里？</li><li>为什么 <code>FlashAttention</code> 的核心是 IO-aware，而不是简单 fusion？</li><li>在一个真实推理系统里，什么时候该继续调库，什么时候才值得走 Triton 或更深的定制路线？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/bd9c352d/">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</a></li><li>下一篇：<a href="/posts/47afb486/">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</a></li><li>延伸阅读：<a href="/posts/eb522a31/">Triton-04-Matmul Autotune与PyTorch集成</a></li><li>延伸阅读：<a href="/posts/77cd2905/">Triton-05-推理热点算子与RMSNorm</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-16-cutlass-triton-cu-blas-yu-flash-attention-gao-xing-neng-shi-xian-du-zai-zuo-shi-yao&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="cuBLAS" scheme="https://smarter.xin/tags/cublas/"/>
    
    <category term="FlashAttention" scheme="https://smarter.xin/tags/flash-attention/"/>
    
    <category term="CUTLASS" scheme="https://smarter.xin/tags/cutlass/"/>
    
    <category term="Triton" scheme="https://smarter.xin/tags/triton/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</title>
    <link href="https://smarter.xin/posts/bd9c352d/"/>
    <id>https://smarter.xin/posts/bd9c352d/</id>
    <published>2026-04-06T07:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-15-tensor-core-wmma-yu-mma-ju-zhen-cheng-zhi-ling-lu-jing-zen-yao-da-tong" tabindex="-1">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</h1><blockquote><p>本文是「CUDA系统拆解」系列第 15 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/15f423b5/">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</a><br>下一篇：<a href="/posts/7061c0d7/">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇要讲清 5 件事：</p><ul><li>Tensor Core 到底是什么，它和普通 CUDA Core 的区别是什么。</li><li><code>Tensor Core</code>、<code>MMA</code>、<code>WMMA</code> 三者分别处在哪一层。</li><li>为什么 Tensor Core 对 AI 推理特别重要，但不是所有 kernel 都能自动吃满它。</li><li><code>fragment</code> 和 warp 级矩阵计算到底该怎么理解。</li><li>一个最小 <code>WMMA</code> 骨架在做什么，尤其是 <code>mma_sync</code> 的含义。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>Tensor Core 提供的是矩阵乘加的硬件峰值能力；真正性能能不能出来，还要看数据类型、tile 组织、内存 feeding 和整体数据流是否都围着它设计。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>Tensor Core</code> 是专门做矩阵乘加的硬件执行单元，不是“更强的 CUDA Core”。</li><li><code>MMA</code> 是更低层的矩阵乘加指令语义，表达的是 <code>D = A * B + C</code> 这类 warp 级 tile 计算。</li><li><code>WMMA</code> 是 CUDA C++ 提供的较高层 warp 级编程接口，用来更方便地走 Tensor Core 路径。</li><li>Tensor Core 的基本工作粒度不是单线程标量运算，而是 warp 协作下的小块矩阵计算。</li><li><code>fragment</code> 不是普通二维数组，而是一个逻辑 tile 在 warp 多个线程寄存器中的分布式表示。</li><li>只有算子模式、精度、shape、layout、对齐和 feeding 都比较合适时，Tensor Core 才能真正带来明显加速。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-tensor-core-dao-di-shi-shi-yao" tabindex="-1" id="3-1-Tensor-Core-到底是什么">3.1 Tensor Core 到底是什么</h3><p>Tensor Core 本质上是 GPU 上专门为矩阵乘加设计的硬件单元。它最擅长的不是通用标量计算，而是高度规则、重复很多、可批量执行的小块矩阵乘加。</p><p>可以把它抽象成：</p><p>[<br>D = A \times B + C<br>]</p><p>这里重点不是公式本身，而是它的计算粒度：</p><ul><li>不是一个线程做一次 <code>a * b + c</code></li><li>而是一组线程协作完成一个 tile 的矩阵乘加</li><li>输入和输出通常都是分块组织的</li><li>底层硬件和指令路径都围绕这种模式做了专门优化</li></ul><p>所以 Tensor Core 快，不是因为“单个线程更强”，而是因为它把深度学习里最常见、最规则的一类重计算单独拉出来，用专门硬件做高吞吐处理。</p><h3 id="3-2-wei-shi-yao-ai-tui-li-te-bie-xu-yao-tensor-core" tabindex="-1" id="3-2-为什么-AI-推理特别需要-Tensor-Core">3.2 为什么 AI 推理特别需要 Tensor Core</h3><p>AI 推理里最重的部分，很多都能映射成 GEMM 或近似 GEMM：</p><ul><li>线性层</li><li>attention 里的 <code>QK^T</code></li><li>attention 里的 <code>PV</code></li><li>很多卷积实现</li><li>MoE 中的专家 MLP</li></ul><p>这类计算有两个特点：</p><ul><li>乘加密度高</li><li>结构比较规则</li></ul><p>这正好适合 Tensor Core。再加上推理很依赖低精度计算，比如 <code>FP16</code>、<code>BF16</code>、<code>TF32</code>、<code>INT8</code>、<code>FP8</code>，而 Tensor Core 又正是这些低精度高吞吐路径的核心硬件支点，所以它和 AI 推理天然绑得很紧。</p><p>但要注意一个常见误区：推理系统里不是所有阶段都同样受益。比如很多在线 LLM decode 阶段常常更偏 <code>memory-bound</code>，这时候即使 Tensor Core 峰值很高，实际收益也可能被访存和调度开销吃掉。</p><h3 id="3-3-tensor-core-mma-wmma-san-zhe-de-guan-xi" tabindex="-1" id="3-3-Tensor-Core、MMA、WMMA-三者的关系">3.3 Tensor Core、MMA、WMMA 三者的关系</h3><p>这三者要分层理解：</p><ul><li><code>Tensor Core</code>：硬件层</li><li><code>MMA</code>：更低层的矩阵乘加指令语义</li><li><code>WMMA</code>：CUDA C++ 暴露出来的较高层编程接口</li></ul><p>一句话记忆：</p><p><strong>Tensor Core 是硬件，MMA 是低层运算语义，WMMA 是程序员更容易使用的接口。</strong></p><p><code>WMMA</code> 的价值是把 warp 级矩阵乘加这件事抽象成了几个高层操作：</p><ul><li>定义 fragment</li><li>加载 tile</li><li>调用 <code>mma_sync</code></li><li>存回结果</li></ul><p>但 <code>WMMA</code> 不是极致性能的终点。很多高性能库和定制 kernel 会进一步下探到更底层的 <code>mma</code> 指令路径，因为那里对 tile、layout、流水和寄存器分布的控制更细。</p><h3 id="3-4-wei-shi-yao-shi-warp-ji-ji-suan-er-bu-shi-thread-ji-ji-suan" tabindex="-1" id="3-4-为什么是-warp-级计算，而不是-thread-级计算">3.4 为什么是 warp 级计算，而不是 thread 级计算</h3><p>普通 CUDA 编程里，你经常把 thread 当成主要思考单位；到了 Tensor Core 路径，更合适的思考单位通常是 warp。</p><p>原因很直接：一个线程自己并不能高效完成一个矩阵 tile 的乘加。更典型的方式是：</p><ul><li>一个 warp 负责一个或多个输出 tile</li><li>warp 内线程分工装载输入块</li><li>矩阵乘加在 Tensor Core 路径上执行</li><li>warp 内线程共同维护累加结果</li></ul><p>这就是 <code>Warp Matrix Multiply and Accumulate</code> 这个名字的含义。它已经在名字里告诉你：这不是 thread-level API，而是 warp-level API。</p><h3 id="3-5-fragment-dao-di-shi-shi-yao" tabindex="-1" id="3-5-fragment-到底是什么">3.5 fragment 到底是什么</h3><p><code>fragment</code> 最容易被误解成“一个普通小矩阵”。这种理解不完全错，但不够准确。</p><p>更准确地说：</p><p><strong>fragment 是一个逻辑矩阵 tile 在 warp 多个线程寄存器中的分布式表示。</strong></p><p>这意味着：</p><ul><li>你逻辑上在操作一个 tile</li><li>物理上它并不是整块连续地放在某个地方</li><li>数据按照硬件定义的映射方式分散在 warp 内各线程寄存器里</li></ul><p>所以 <code>fragment</code> 不是你能随意按二维数组访问的数据结构。你通常需要通过专门接口来操作它：</p><ul><li><code>load_matrix_sync</code></li><li><code>mma_sync</code></li><li><code>store_matrix_sync</code></li></ul><p>这个抽象的目的，是把“适合 Tensor Core 的数据组织方式”隐藏在 API 和编译器后面，让程序员不用手工管理最底层的寄存器映射细节。</p><h3 id="3-6-yi-ge-zui-xiao-wmma-gu-jia" tabindex="-1" id="3-6-一个最小-WMMA-骨架">3.6 一个最小 WMMA 骨架</h3><p>下面这段代码不是完整工程代码，而是保留理解主线的最小骨架：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mma.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> nvcuda;</span><br><span class="line"></span><br><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">wmma_gemm</span><span class="params">(<span class="type">const</span> half* A, <span class="type">const</span> half* B, <span class="type">float</span>* C,</span></span></span><br><span class="line"><span class="params"><span class="function">                          <span class="type">int</span> M, <span class="type">int</span> N, <span class="type">int</span> K)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> warpM = blockIdx.y;</span><br><span class="line">    <span class="type">int</span> warpN = blockIdx.x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((warpM + <span class="number">1</span>) * <span class="number">16</span> &gt; M || (warpN + <span class="number">1</span>) * <span class="number">16</span> &gt; N) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    wmma::fragment&lt;wmma::matrix_a, <span class="number">16</span>, <span class="number">16</span>, <span class="number">16</span>, half, wmma::row_major&gt; a_frag;</span><br><span class="line">    wmma::fragment&lt;wmma::matrix_b, <span class="number">16</span>, <span class="number">16</span>, <span class="number">16</span>, half, wmma::col_major&gt; b_frag;</span><br><span class="line">    wmma::fragment&lt;wmma::accumulator, <span class="number">16</span>, <span class="number">16</span>, <span class="number">16</span>, <span class="type">float</span>&gt; c_frag;</span><br><span class="line"></span><br><span class="line">    wmma::<span class="built_in">fill_fragment</span>(c_frag, <span class="number">0.0f</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> k = <span class="number">0</span>; k &lt; K; k += <span class="number">16</span>) &#123;</span><br><span class="line">        <span class="type">const</span> half* a_ptr = A + warpM * <span class="number">16</span> * K + k;</span><br><span class="line">        <span class="type">const</span> half* b_ptr = B + k * N + warpN * <span class="number">16</span>;</span><br><span class="line"></span><br><span class="line">        wmma::<span class="built_in">load_matrix_sync</span>(a_frag, a_ptr, K);</span><br><span class="line">        wmma::<span class="built_in">load_matrix_sync</span>(b_frag, b_ptr, N);</span><br><span class="line">        wmma::<span class="built_in">mma_sync</span>(c_frag, a_frag, b_frag, c_frag);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span>* c_ptr = C + warpM * <span class="number">16</span> * N + warpN * <span class="number">16</span>;</span><br><span class="line">    wmma::<span class="built_in">store_matrix_sync</span>(c_ptr, c_frag, N, wmma::mem_row_major);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>看这段代码时，不要先陷进每个模板参数，而要先抓住主流程：</p><ol><li>定义 A、B、C 对应的 fragment</li><li>初始化 accumulator</li><li>按 <code>K</code> 维循环加载输入 tile</li><li>调用 <code>wmma::mma_sync</code> 做乘加</li><li>把结果 tile 写回全局内存</li></ol><p>这里最关键的是 <code>wmma::mma_sync</code>。它表达的不是普通标量乘加，而是：</p><p><strong>warp 协作地对一个 tile 执行 <code>C = A * B + C</code>。</strong></p><p>再注意一个常见设计：</p><ul><li>输入常是 <code>half</code> / <code>bf16</code></li><li>累加常是 <code>float</code></li></ul><p>这就是混合精度的核心直觉：输入用低精度提高吞吐，累加用更高精度维持数值稳定性。</p><h3 id="3-7-wei-shi-yao-bu-shi-suo-you-suan-zi-du-neng-zi-dong-chi-man-tensor-core" tabindex="-1" id="3-7-为什么不是所有算子都能自动吃满-Tensor-Core">3.7 为什么不是所有算子都能自动吃满 Tensor Core</h3><p>这是工程里最重要的现实问题之一。Tensor Core 很强，但它不是“打开就自动满速”的开关。</p><p>通常要同时满足几类条件：</p><ul><li>算子模式适合映射到矩阵乘加</li><li>数据类型落在支持范围内</li><li>tile shape 和维度比较友好</li><li>layout、对齐和加载路径比较合适</li><li>周围的数据搬运、变形、mask、归约、epilogue 开销不能太大</li></ul><p>所以以下情况常常会削弱收益：</p><ul><li>kernel 本身更偏 <code>memory-bound</code></li><li>shape 太小或太碎</li><li>动态形状过多</li><li>layout 变换成本高</li><li>访存 feeding 跟不上计算峰值</li></ul><p>这也是为什么真正高性能的 GEMM、attention、FlashAttention、TensorRT-LLM kernel，不是“加一条 Tensor Core 指令”就结束，而是会把多级 tiling、shared memory staging、寄存器累加、双缓冲和流水线一起设计好。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>理解 Tensor Core 之后，你会更容易看懂很多推理优化为什么长那样：</p><ul><li>为什么推理框架如此重视 <code>FP16</code>、<code>BF16</code>、<code>INT8</code>、<code>FP8</code></li><li>为什么高性能 GEMM 会反复强调 tile、layout、pipeline</li><li>为什么 FlashAttention 不只是一个“调用 Tensor Core 的 kernel”，而是同时在解决访存和 Tensor Core feeding</li><li>为什么量化之后还要继续做 fusion、dequant 优化和数据流重排</li></ul><p>从系统角度看，Tensor Core 给的是高计算峰值；推理工程真正要做的，是把这个峰值尽可能变成稳定的实际吞吐。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>Tensor Core</code> 不是“更强的 CUDA Core”，而是更专用的矩阵乘加硬件。</li><li>不是只要用了 <code>FP16</code>，程序就一定会自动走到 Tensor Core 的最佳路径。</li><li><code>WMMA</code> 不是 Tensor Core 的全部，它只是一个较高层的编程入口。</li><li>有了 Tensor Core，不代表访存、layout、shared memory、流水线这些问题就不重要了；通常反而更重要。</li><li>不是所有 GEMM 或 attention kernel 都能同样吃满 Tensor Core，shape、精度、对齐和 feeding 条件都很关键。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>Tensor Core、MMA、WMMA 分别处在哪一层，各自解决什么问题？</li><li>为什么 Tensor Core 的自然思考单位是 warp，而不是单线程？</li><li><code>fragment</code> 为什么不能当普通二维数组来理解？</li><li><code>wmma::mma_sync</code> 在逻辑上做了什么？为什么常配合低精度输入和高精度累加？</li><li>为什么 Tensor Core 峰值很高，但某些推理 kernel 实际收益却不明显？</li><li>如果一个 kernel 已经偏 <code>memory-bound</code>，你应该优先检查 Tensor Core 使用率，还是优先检查 feeding 和访存路径？为什么？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/15f423b5/">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</a></li><li>下一篇：<a href="/posts/7061c0d7/">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</a></li><li>延伸阅读：<a href="/posts/6577c292/">GPU系统拆解-08-Tensor Core、GEMM 与 FlashAttention：AI 计算热点为什么这样组织</a></li><li>延伸阅读：<a href="/posts/78e375e8/">CUDA实践-09-张量核心与混合精度</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-15-tensor-core-wmma-yu-mma-ju-zhen-cheng-zhi-ling-lu-jing-zen-yao-da-tong&quot; tabindex=&quot;-1&quot;&gt;CUDA系统拆解-15-Tensor</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="Tensor Core" scheme="https://smarter.xin/tags/tensor-core/"/>
    
    <category term="WMMA" scheme="https://smarter.xin/tags/wmma/"/>
    
    <category term="矩阵乘法" scheme="https://smarter.xin/tags/matrix-multiplication/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</title>
    <link href="https://smarter.xin/posts/15f423b5/"/>
    <id>https://smarter.xin/posts/15f423b5/</id>
    <published>2026-04-06T07:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-14-ptx-sass-yu-bian-yi-lian-cuda-dai-ma-ru-he-luo-dao-ji-qi-zhi-ling" tabindex="-1">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</h1><blockquote><p>本文是「CUDA系统拆解」系列第 14 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/939f5917/">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</a><br>下一篇：<a href="/posts/bd9c352d/">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇要讲清 5 件事：</p><ul><li>为什么 CUDA 体系里需要 <code>PTX</code> 这一层，而不是直接把 <code>.cu</code> 编成某张卡的最终机器码。</li><li><code>PTX</code> 和 <code>SASS</code> 分别是什么，它们的本质区别在哪里。</li><li>一个 CUDA 程序大致会经历怎样的编译和装载链路。</li><li><code>fatbin</code>、JIT、前向兼容这些词到底在解决什么工程问题。</li><li>在真实工程里，什么时候你需要关心这条链路，什么时候又不需要过度下钻。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong><code>PTX</code> 负责在高层 CUDA 代码和具体 GPU 架构之间提供一层可移植中间表示，<code>SASS</code> 才是最终真正落到某代芯片上的执行指令。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>PTX</code> 是 NVIDIA CUDA 路径中的虚拟 ISA / 中间表示，不等于最终机器码。</li><li><code>SASS</code> 是面向具体 GPU 架构的真实机器指令，更接近芯片最终执行内容。</li><li>之所以需要 <code>PTX</code>，核心是要在“高层编程抽象”和“具体硬件差异”之间加一层缓冲，兼顾编译灵活性和一定程度的前向兼容。</li><li><code>.cu</code> 文件不会直接变成一份单一产物，中间可能涉及主机代码编译、设备代码编译、<code>PTX</code>、<code>cubin</code>、<code>fatbin</code>、运行时装载和可能的 JIT。</li><li><code>fatbin</code> 的价值是把多种目标代码打包在一起，让同一个程序更容易覆盖多种 GPU。</li><li>JIT 的意义是：当没有完全匹配的预编译机器码时，可以基于 <code>PTX</code> 在运行时继续为目标设备生成更合适的代码。</li><li>工程里通常只在性能定位、架构兼容、部署打包或指令路径分析时，需要认真关心这条链路。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-xu-yao-ptx" tabindex="-1" id="3-1-为什么需要-PTX">3.1 为什么需要 PTX</h3><p>如果 CUDA 没有 <code>PTX</code> 这一层，最直接的方案就是：</p><ul><li>你写 <code>.cu</code></li><li>编译器直接为某个具体 GPU 架构吐出最终机器码</li></ul><p>这当然能工作，但问题很多：</p><ul><li>不同 GPU 架构的指令细节并不完全一样</li><li>同一份源码如果想兼容多代 GPU，打包和分发会更麻烦</li><li>编译器优化和后续目标设备适配空间会变小</li></ul><p>所以 <code>PTX</code> 的核心作用，就是在中间加一层“还没有完全绑死到某张卡”的表示。</p><p>你可以把它理解成：</p><ul><li>高层：程序员写的是 CUDA C++</li><li>中层：编译器先把设备端逻辑降到 <code>PTX</code></li><li>底层：再根据具体目标架构生成真正执行的 <code>SASS</code></li></ul><p>这样设计的好处，是能把“程序语义”和“具体架构落地”部分解耦一些。</p><h3 id="3-2-ptx-he-sass-ge-shi-shi-yao" tabindex="-1" id="3-2-PTX-和-SASS-各是什么">3.2 PTX 和 SASS 各是什么</h3><p>最稳的理解方式是：</p><ul><li><code>PTX</code>：虚拟指令集 / 中间表示</li><li><code>SASS</code>：具体 GPU 架构上的机器指令</li></ul><p>两者的差别，不只是“一个更早、一个更晚”，而是抽象层级不同。</p><p><code>PTX</code> 更像是在表达：</p><ul><li>这个 kernel 想做什么</li><li>数据和指令的大致组织方式</li><li>给后续编译 / JIT 留出进一步映射空间</li></ul><p><code>SASS</code> 更像是在表达：</p><ul><li>针对某代 SM、某套指令能力</li><li>最终到底发出了哪些真实机器指令</li><li>调度和执行最终更接近什么样子</li></ul><p>所以不能把 <code>PTX</code> 当成最终执行内容，也不能把看过高层 CUDA 代码就等同于看过真实指令路径。</p><h3 id="3-3-cong-cu-dao-zui-zhong-zhi-xing-da-zhi-jing-li-shi-yao" tabindex="-1" id="3-3-从-cu-到最终执行，大致经历什么">3.3 从 .cu 到最终执行，大致经历什么</h3><p>最粗粒度的编译链路可以这样理解：</p><ol><li>你写下 <code>.cu</code></li><li>编译器把主机代码和设备代码拆开处理</li><li>设备代码先被降成 <code>PTX</code>，也可能进一步生成某些目标架构的 <code>cubin</code></li><li>多种设备端产物可以一起打进 <code>fatbin</code></li><li>程序运行时，CUDA runtime / driver 为当前设备选择可用代码</li><li>如果已经有匹配架构的机器码，就直接装载</li><li>如果只有 <code>PTX</code> 或没有完全匹配的机器码，就可能触发 JIT，再生成适合当前设备的最终代码</li></ol><p>这条链路的重点不是背每个工具细节，而是理解：</p><ul><li>主机和设备代码不是一锅编完</li><li>设备代码可能同时以多种形式存在</li><li>最终执行内容和部署时打包内容不一定完全同一份</li></ul><h3 id="3-4-cubin-fatbin-ptx-sass-de-guan-xi" tabindex="-1" id="3-4-cubin、fatbin、PTX、SASS-的关系">3.4 cubin、fatbin、PTX、SASS 的关系</h3><p>这几个词经常一起出现，容易混。</p><p>可以这样记：</p><ul><li><code>PTX</code>：中间表示</li><li><code>SASS</code>：最终机器指令</li><li><code>cubin</code>：包含某个目标架构机器码的二进制产物</li><li><code>fatbin</code>：把多个设备端产物打包到一起的容器</li></ul><p>从工程视角看，<code>fatbin</code> 最实用的意义是：</p><ul><li>你可以同时放入多个架构的预编译代码</li><li>也可以保留 <code>PTX</code> 作为兜底路径</li><li>程序部署后，不必只服务单一型号 GPU</li></ul><p>所以 <code>fatbin</code> 解决的是“分发和兼容”的问题，<code>PTX</code> 解决的是“中间表示和后续映射”的问题，<code>SASS</code> 解决的是“具体设备最终执行”的问题。</p><h3 id="3-5-jit-he-qian-xiang-jian-rong-de-gong-cheng-yi-yi" tabindex="-1" id="3-5-JIT-和前向兼容的工程意义">3.5 JIT 和前向兼容的工程意义</h3><p>JIT 常常被说得很抽象，其实直觉并不复杂。</p><p>假设你发布程序时，没有为用户当前这张 GPU 预编译完全匹配的机器码，但你保留了 <code>PTX</code>。<br>那么运行时系统仍然有机会：</p><ul><li>读取 <code>PTX</code></li><li>针对当前设备继续编译</li><li>生成可执行的目标代码</li></ul><p>这就是 JIT 的意义。</p><p>它为什么重要？</p><ul><li>提高部署覆盖面</li><li>减少“每个设备都必须完全重新打包”的压力</li><li>给未来硬件留出一定前向兼容空间</li></ul><p>但也要看到代价：</p><ul><li>首次运行可能增加编译开销</li><li>最终生成结果受驱动和环境影响</li><li>如果你特别关心启动延迟或完全可控的部署表现，往往更希望提前准备好更匹配的目标代码</li></ul><p>所以工程上常见做法不是“只靠 JIT”，而是：</p><ul><li>对主流目标架构准备预编译代码</li><li>同时保留 <code>PTX</code> 作为兼容兜底</li></ul><h3 id="3-6-shi-yao-shi-hou-xu-yao-guan-xin-zhe-tiao-lian-lu" tabindex="-1" id="3-6-什么时候需要关心这条链路">3.6 什么时候需要关心这条链路</h3><p>不是每个 CUDA 开发者都需要天天盯着 <code>PTX</code> 和 <code>SASS</code>。<br>更实际的问题是：什么时候这条链路会变成重要信息？</p><p>典型场景有：</p><ul><li>你在做跨架构部署，想知道包该怎么打</li><li>某个 kernel 在新旧 GPU 上表现差异明显</li><li>你怀疑高层代码没有走到预期指令路径，例如没走 Tensor Core</li><li>你在分析启动延迟，怀疑有 JIT 成本</li><li>你在看更底层的性能问题，想确认编译器最终生成了什么</li></ul><p>反过来说，如果你只是正常调用成熟库，且没有性能或兼容性异常，那么没必要把所有精力都花在反汇编级细节上。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这一篇对推理工程的价值，主要在 3 个地方：</p><ul><li>你更容易理解为什么同一套推理程序在不同 GPU 上表现会不同</li><li>你更容易判断某个 kernel 到底有没有落到预期的 Tensor Core / 低精度路径</li><li>你更容易看懂部署、打包、运行时兼容和启动开销之间的取舍</li></ul><p>尤其在 AI infra 场景里，常见问题都会碰到这条链路：</p><ul><li>为什么某台机器第一次加载 engine 或 kernel 比较慢</li><li>为什么同样模型换一代 GPU 吞吐差很多</li><li>为什么某些构建产物要同时带多种架构目标</li><li>为什么性能定位时有时需要继续看 <code>PTX</code> 或 <code>SASS</code></li></ul><p>所以这不是“编译器内部知识点”，而是会直接影响推理部署和性能判断的一层背景。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>PTX</code> 不是最终机器码，它更像中间表示。</li><li><code>SASS</code> 才更接近真实执行指令，所以两者不能混为一谈。</li><li>有 <code>PTX</code> 不代表一定没有额外成本；运行时 JIT 可能带来首次启动开销。</li><li><code>fatbin</code> 不是一种指令，而是打包多种设备端产物的容器思路。</li><li>不是所有场景都要下钻到 <code>PTX / SASS</code>，但一旦涉及性能异常、兼容性或部署问题，这条链路就很关键。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么 CUDA 体系里需要 <code>PTX</code> 这一层？</li><li><code>PTX</code> 和 <code>SASS</code> 的本质区别是什么？</li><li>从 <code>.cu</code> 到最终执行，通常会经历哪些关键阶段？</li><li><code>cubin</code> 和 <code>fatbin</code> 分别在解决什么问题？</li><li>JIT 为什么能带来前向兼容能力？它的代价又是什么？</li><li>在 AI 推理工程里，哪些问题会让你必须开始关心 <code>PTX / SASS / fatbin / JIT</code> 这条链路？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/939f5917/">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</a></li><li>下一篇：<a href="/posts/bd9c352d/">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</a></li><li>延伸阅读：<a href="/posts/b8f3cfc3/">GPU系统拆解-07-PTX、SASS 与编译链：一段 CUDA 代码如何变成指令</a></li><li>延伸阅读：<a href="/posts/92dc1028/">CUDA C编程权威指南：导读</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-14-ptx-sass-yu-bian-yi-lian-cuda-dai-ma-ru-he-luo-dao-ji-qi-zhi-ling&quot; tabindex=&quot;-1&quot;&gt;CUDA系统拆解-14-PTX、SASS</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="执行模型" scheme="https://smarter.xin/tags/execution-model/"/>
    
    <category term="PTX" scheme="https://smarter.xin/tags/ptx/"/>
    
    <category term="SASS" scheme="https://smarter.xin/tags/sass/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</title>
    <link href="https://smarter.xin/posts/939f5917/"/>
    <id>https://smarter.xin/posts/939f5917/</id>
    <published>2026-04-06T06:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-13-mian-shi-qiang-hua-zhuan-ti-ptx-tensor-core-cutlass-yu-triton-zen-yao-jiang" tabindex="-1">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</h1><blockquote><p>本文是「CUDA系统拆解」系列第 13 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/18814acd/">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</a><br>下一篇：<a href="/posts/15f423b5/">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇不是分别讲 4 个零散名词，而是把它们放回同一张图里。核心要讲清 5 件事：</p><ul><li><code>CUDA C++</code>、<code>PTX</code>、<code>SASS</code> 分别处在哪一层。</li><li><code>Tensor Core</code> 为什么会成为现代 AI 推理里的关键硬件路径。</li><li><code>WMMA</code> 和 <code>MMA</code> 的定位差异是什么。</li><li><code>CUTLASS</code> 和 <code>Triton</code> 在工程里分别解决什么问题。</li><li>面试里遇到这些词时，应该怎么把它们回答成一条完整的推理内核主线。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>这几个专题本质上覆盖了高性能推理内核的四个层次：编程抽象、指令路径、专用硬件和工程实现方式。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>CUDA C++</code> 是你主要写代码的高层编程接口，<code>PTX</code> 是更靠近虚拟 ISA 的中间表示，<code>SASS</code> 是最终落到具体 GPU 架构上的机器指令。</li><li><code>Tensor Core</code> 是专门为矩阵乘加设计的硬件单元，它之所以对推理重要，是因为现代推理大量依赖低精度 GEMM、attention 和相关变体。</li><li><code>MMA</code> 更接近底层矩阵乘加指令语义，<code>WMMA</code> 是 CUDA C++ 暴露出来的较高层 warp 级接口。</li><li><code>CUTLASS</code> 更像高性能 GEMM / Tensor Core kernel 的结构化积木，强调分层组织和可定制性。</li><li><code>Triton</code> 更像高生产力的 GPU kernel DSL，特别适合做定制算子、fusion 和快速迭代。</li><li>面试里真正重要的不是会背定义，而是能讲清这些层次之间怎么衔接，以及它们为什么会影响推理吞吐和延迟。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-xian-ba-zong-tu-da-qi-lai" tabindex="-1" id="3-1-先把总图搭起来">3.1 先把总图搭起来</h3><p>可以把这一篇涉及的东西分成 4 层：</p><ul><li>编程层：<code>CUDA C++</code>、<code>Triton</code></li><li>中间 / 指令层：<code>PTX</code>、<code>MMA</code></li><li>硬件层：<code>Tensor Core</code></li><li>工程实现层：<code>CUTLASS</code>、各种高性能 kernel 模板</li></ul><p>如果只把这些词一个个背下来，很容易觉得它们彼此无关。<br>但从推理内核视角看，它们其实是在回答同一件事：</p><p><strong>怎样把一个高层算子，稳定地落成吃到硬件峰值的 GPU 执行路径。</strong></p><h3 id="3-2-cuda-c-ptx-sass-de-guan-xi" tabindex="-1" id="3-2-CUDA-C-、PTX、SASS-的关系">3.2 CUDA C++、PTX、SASS 的关系</h3><p>最稳的分层方式是：</p><ul><li><code>CUDA C++</code>：程序员主要接触的高层语言和编程模型</li><li><code>PTX</code>：面向 NVIDIA GPU 路径的虚拟指令表示</li><li><code>SASS</code>：具体架构上的真实机器指令</li></ul><p>这套关系很重要，因为它说明了一件事：</p><p>你写下的 CUDA 代码，并不会直接等于最终在芯片上执行的那串指令。<br>中间还会经过编译、优化、目标架构映射，最后才落到真正执行的机器码上。</p><p>这也是为什么做高性能分析时，有时你需要知道：</p><ul><li>高层代码有没有被编译成你预期的指令路径</li><li>某段代码为什么在不同架构上表现不同</li><li>某个 kernel 是不是走到了 Tensor Core / MMA 路径</li></ul><h3 id="3-3-tensor-core-wei-shi-yao-dui-tui-li-zhong-yao" tabindex="-1" id="3-3-Tensor-Core-为什么对推理重要">3.3 Tensor Core 为什么对推理重要</h3><p><code>Tensor Core</code> 的核心价值不是“GPU 上又多了一种算力”，而是：</p><p><strong>把深度学习里最常见、最规则的一类矩阵乘加计算，用专用硬件做成高吞吐路径。</strong></p><p>现代 AI 推理里很多重计算都能落到 GEMM 或近似 GEMM：</p><ul><li>线性层</li><li>MLP</li><li>attention 里的 <code>QK^T</code></li><li>attention 里的 <code>PV</code></li><li>一些卷积和变体算子</li></ul><p>再加上推理大量使用：</p><ul><li><code>FP16</code></li><li><code>BF16</code></li><li><code>TF32</code></li><li><code>INT8</code></li><li><code>FP8</code></li></ul><p>这就让 Tensor Core 和低精度推理天然绑定在一起。<br>所以你可以把它理解成：<strong>没有 Tensor Core，现代高吞吐推理的很多性能上限就很难成立。</strong></p><p>但要注意，Tensor Core 只解决“算”的峰值问题，不自动解决：</p><ul><li>feeding 是否跟得上</li><li>layout 是否合适</li><li>shared memory / registers 是否组织得好</li><li>整个 kernel 是不是其实更偏 <code>memory-bound</code></li></ul><h3 id="3-4-wmma-he-mma-de-ding-wei-chai-yi" tabindex="-1" id="3-4-WMMA-和-MMA-的定位差异">3.4 WMMA 和 MMA 的定位差异</h3><p>这两个词经常一起出现，但层级并不一样。</p><ul><li><code>MMA</code> 更接近底层矩阵乘加指令语义，表达的是 warp 级 tile 乘加</li><li><code>WMMA</code> 是 CUDA C++ 提供的较高层 warp 级编程接口</li></ul><p>一句话记：</p><p><strong><code>MMA</code> 更像底层能力，<code>WMMA</code> 更像程序员可直接使用的高层入口。</strong></p><p>为什么这个区分重要？</p><p>因为它对应着两类工程路径：</p><ul><li>如果你更关心学习和快速上手，<code>WMMA</code> 很适合建立直觉</li><li>如果你更关心极致性能和更细控制，通常要进一步理解更底层的 <code>mma</code> 指令路径和配套数据组织</li></ul><p>这也是为什么高性能库和深度定制 kernel 往往不会停留在“会用 <code>WMMA</code>”这一步。</p><h3 id="3-5-cutlass-de-gong-cheng-ding-wei" tabindex="-1" id="3-5-CUTLASS-的工程定位">3.5 CUTLASS 的工程定位</h3><p><code>CUTLASS</code> 经常被误解成“另一个调用库”，其实它更像：</p><p><strong>把高性能 GEMM / Tensor Core kernel 的组织方式做成了一套结构化积木。</strong></p><p>它最值得学的不是某个模板名字，而是这套思路：</p><ul><li>threadblock tile</li><li>warp tile</li><li>instruction tile</li><li>global memory -&gt; shared memory -&gt; registers 的分层数据流</li><li>mainloop 和 epilogue 的拆分</li></ul><p>所以 <code>CUTLASS</code> 的定位通常是：</p><ul><li>标准库不够表达你的定制需求</li><li>你又不想从零手写全部底层实现</li><li>你需要在接近库级性能的前提下做结构化改造</li></ul><p>对面试来说，回答重点不该是“CUTLASS 很复杂”，而是：</p><p><strong>它让你看到高性能 Tensor Core kernel 不是一段代码，而是一整套层次化数据流设计。</strong></p><h3 id="3-6-triton-de-gong-cheng-ding-wei" tabindex="-1" id="3-6-Triton-的工程定位">3.6 Triton 的工程定位</h3><p><code>Triton</code> 的价值不在于“比 CUDA 更低层”，而在于：</p><p><strong>它用更高生产力的方式，让你表达 tile 级 kernel 和 fusion 型算子。</strong></p><p>它特别适合的场景通常是：</p><ul><li>标准库不直接覆盖</li><li>手写 CUDA 成本高</li><li>需要快速实验不同 tile 和融合策略</li><li>算子比较定制，或者和上游框架耦合较深</li></ul><p>所以 Triton 常被用在：</p><ul><li>fused operator</li><li>特殊 layout kernel</li><li>一些 attention 周边算子</li><li>需要快速迭代和 autotune 的定制实现</li></ul><p>但 Triton 不是魔法。它能不能快，仍然取决于：</p><ul><li>访存模式</li><li>tile 组织</li><li>register pressure</li><li>Tensor Core 映射</li><li>fusion 是否真的减少了 global memory 往返</li></ul><p>换句话说，Triton 提高的是表达效率，不会替你绕开性能基本规律。</p><h3 id="3-7-zhe-ji-ge-zhuan-ti-zai-mian-shi-li-gai-zen-yao-chuan-qi-lai" tabindex="-1" id="3-7-这几个专题在面试里该怎么串起来">3.7 这几个专题在面试里该怎么串起来</h3><p>比较好的回答方式，不是把名词拆开背，而是按下面这条线讲：</p><ol><li>高层算子最终要映射到 GPU kernel</li><li>高层代码会经过 <code>CUDA C++ -&gt; PTX -&gt; SASS</code> 这条链路</li><li>如果算子能映射成低精度矩阵乘加，就有机会走 Tensor Core 路径</li><li><code>WMMA / MMA</code> 对应的是这条矩阵乘加路径的不同抽象层</li><li><code>CUTLASS</code> 和 <code>Triton</code> 则是两种常见的高性能工程实现路线</li></ol><p>这类回答的重点不是证明你“会多少术语”，而是证明你知道：</p><ul><li>哪一层决定抽象表达</li><li>哪一层决定最终指令路径</li><li>哪一层决定硬件峰值</li><li>哪一层决定工程落地效率和可维护性</li></ul><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这一篇其实是在补齐“推理内核工程图谱”。</p><p>前面你学过：</p><ul><li>thread / warp / SM</li><li>访存和 shared memory</li><li>occupancy</li><li>profiling</li></ul><p>但真正进入推理工程后，你还会不断遇到这些问题：</p><ul><li>这个 kernel 到底有没有走 Tensor Core？</li><li>为什么某个 shape 明明是 GEMM，却没打出应有吞吐？</li><li>是继续调 <code>cuBLASLt</code>，还是基于 <code>CUTLASS</code> 定制？</li><li>某个 fused kernel 更适合用 CUDA 写，还是 Triton 写？</li><li>编译器最终把高层代码落成了什么指令路径？</li></ul><p>所以这一篇的价值，在于把“性能原理”继续推到“实现路线选择”。<br>这正是 AI infra / 推理岗位很看重的一层能力。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>PTX</code> 不是最终机器码，<code>SASS</code> 才更接近真实执行指令。</li><li><code>Tensor Core</code> 不是“任何 CUDA kernel 都会自动吃满”的通用加速器。</li><li><code>WMMA</code> 和 <code>MMA</code> 不是同一层概念，前者更高层，后者更接近底层运算语义。</li><li><code>CUTLASS</code> 不是简单“又一个库”，它更像高性能 GEMM / Tensor Core kernel 的结构化框架。</li><li><code>Triton</code> 不是自动性能保证，它只是让定制 kernel 更容易表达和迭代。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li><code>CUDA C++</code>、<code>PTX</code>、<code>SASS</code> 分别处在哪一层？为什么这层次划分重要？</li><li>为什么 Tensor Core 会和现代低精度推理强绑定？</li><li><code>WMMA</code> 和 <code>MMA</code> 的定位差异是什么？</li><li><code>CUTLASS</code> 和 <code>Triton</code> 分别更适合解决哪类工程问题？</li><li>如果面试官问你“PTX、Tensor Core、CUTLASS、Triton 有什么关系”，你能不能按一条推理内核主线讲出来？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/18814acd/">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</a></li><li>下一篇：<a href="/posts/15f423b5/">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</a></li><li>延伸阅读：<a href="/posts/6577c292/">GPU系统拆解-08-Tensor Core、GEMM 与 FlashAttention：AI 计算热点为什么这样组织</a></li><li>延伸阅读：<a href="/posts/3162877d/">Triton-导读-从算子开发到模型服务</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-13-mian-shi-qiang-hua-zhuan-ti-ptx-tensor-core-cutlass-yu-triton-zen-yao-jiang&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="Tensor Core" scheme="https://smarter.xin/tags/tensor-core/"/>
    
    <category term="CUTLASS" scheme="https://smarter.xin/tags/cutlass/"/>
    
    <category term="PTX" scheme="https://smarter.xin/tags/ptx/"/>
    
    <category term="Triton" scheme="https://smarter.xin/tags/triton/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</title>
    <link href="https://smarter.xin/posts/18814acd/"/>
    <id>https://smarter.xin/posts/18814acd/</id>
    <published>2026-04-06T06:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-12-mian-shi-zong-fu-pan-zen-yao-ba-cuda-neng-li-lian-jie-dao-ai-tui-li" tabindex="-1">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</h1><blockquote><p>本文是「CUDA系统拆解」系列第 12 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/c824c712/">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</a><br>下一篇：<a href="/posts/939f5917/">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>学到这里，你到底应该具备哪些能力。</li><li>为什么 AI 推理岗位会反复问 CUDA。</li><li>怎样把前面学过的内容整理成 <code>算子层 / 执行层 / 系统层</code> 三层框架。</li><li>看到一个慢算子、一个 kernel、一个框架设计时，怎样把它还原成 CUDA 问题。</li><li>面试里怎样把 <code>occupancy</code>、<code>coalescing</code>、<code>fusion</code>、<code>graph</code>、<code>batching</code> 讲成一套完整的系统理解。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>AI 推理优化的本质，不只是“算得快”，而是“算和搬，以及调度和资源管理一起做好”。</li><li>CUDA 在推理岗位里重要，不是因为大家都要求你手写复杂 kernel，而是因为大量系统问题最后都能还原成 CUDA 的代价模型。</li><li>一套成熟的理解框架可以分成三层：<code>算子层</code>、<code>执行层</code>、<code>系统层</code>。</li><li>看到慢算子时，要能还原成：线程怎么分工、数据怎么流动、同步和 launch 开销在哪里、GPU 为什么没吃满。</li><li>面试真正想听的不是 API 名字，而是你能不能说清楚：为什么这样设计、快在哪里、代价在哪里、边界在哪里。</li><li>高水平回答一定带 trade-off，而不是只有结论。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-xue-dao-zhe-li-ying-gai-ju-bei-na-xie-neng-li" tabindex="-1" id="3-1-学到这里应该具备哪些能力">3.1 学到这里应该具备哪些能力</h3><p>如果前面 11 篇你真的吃进去了，现在应该不只是“知道一些 CUDA 名词”，而是至少具备下面几种能力。</p><p>第一，看到一个 kernel，能快速判断它在干什么。</p><p>也就是能回答：</p><ul><li>一个线程负责什么</li><li>一个 block 为什么这样切</li><li>它更像 <code>elementwise</code>、<code>reduction</code>、<code>reorder</code> 还是 <code>blocked compute</code></li><li>主要瓶颈更像带宽、同步、算力还是 launch</li></ul><p>第二，看到一个慢算子，能大致猜到问题可能在哪。</p><p>例如：</p><ul><li>reduction 慢，可能是同步和局部合并路径不好</li><li>transpose 慢，可能是访存模式和 <code>bank conflict</code></li><li>GEMM 慢，可能是 tile、复用或资源平衡不好</li><li>decode 慢，可能是 kernel 太碎、launch 太重、memory latency 太明显</li></ul><p>第三，看到一个推理框架设计，能把它拆成底层代价问题。</p><p>例如：</p><ul><li>continuous batching 在解决 GPU 吃不满</li><li>fusion 在减少 global memory traffic 和 launch overhead</li><li>paged attention 在做 KV cache 的内存组织和复用管理</li><li>CUDA Graph 在减少 CPU 反复 launch 的开销</li></ul><p>如果你已经能做到这三点，说明你学到的不是“概念列表”，而是分析框架。</p><h3 id="3-2-wei-shi-yao-ai-tui-li-gang-wei-hui-wen-cuda" tabindex="-1" id="3-2-为什么-AI-推理岗位会问-CUDA">3.2 为什么 AI 推理岗位会问 CUDA</h3><p>很多人会误以为：</p><blockquote><p>我做的是推理系统，不是专职内核工程，为什么还要学 CUDA？</p></blockquote><p>原因很简单：推理系统的大量核心问题，最后都能落到 CUDA。</p><p>推理系统关心的通常是：</p><ul><li>更低延迟</li><li>更高吞吐</li><li>更高 GPU 利用率</li><li>更低显存占用</li><li>更稳定的多请求并发</li></ul><p>而这些目标背后，本质上都对应一些 CUDA 问题：</p><ul><li>延迟为什么高：kernel 慢、launch 太碎、同步太多、调度有空泡</li><li>吞吐为什么上不去：GPU 没吃满、batch 不规整、copy / compute 没 overlap</li><li>显存为什么吃紧：activation、workspace、KV cache 和 allocator 策略</li><li>GPU 利用率为什么不高：occupancy、stream 编排、request 调度、kernel 粒度</li></ul><p>所以面试官问 CUDA，不是在确认你会不会 <code>cudaMalloc</code>，而是在确认：</p><blockquote><p>你能不能把推理系统问题还原成底层执行和资源问题。</p></blockquote><h3 id="3-3-yi-tao-cheng-shou-de-san-ceng-kuang-jia" tabindex="-1" id="3-3-一套成熟的三层框架">3.3 一套成熟的三层框架</h3><p>我建议把前面的 CUDA 知识收束成三层。</p><p><code>算子层</code></p><ul><li>关注单个算子怎么实现、为什么快、为什么慢</li><li>关键词通常是：线程映射、访存模式、shared memory、reduction、tiling、fusion</li></ul><p>典型问题：</p><ul><li>GEMM 为什么适合 tile 化</li><li>softmax 为什么是 reduction + elementwise</li><li>LayerNorm 为什么常常 <code>memory-bound</code></li><li>attention 为什么经常受 KV cache 访问和中间数据流影响</li></ul><p><code>执行层</code></p><ul><li>关注多个 kernel 和数据传输怎样组织</li><li>关键词通常是：stream、异步拷贝、pinned memory、overlap、launch overhead、CUDA Graph</li></ul><p>典型问题：</p><ul><li>为什么默认 stream 会把流水拉直</li><li>为什么 decode 场景更容易暴露 launch 开销</li><li>为什么 graph capture 对稳定重复路径有价值</li></ul><p><code>系统层</code></p><ul><li>关注请求调度、显存管理、并发策略、多 GPU 协作</li><li>关键词通常是：continuous batching、KV cache、paged attention、memory pool、通信重叠</li></ul><p>典型问题：</p><ul><li>为什么 continuous batching 能提高吞吐</li><li>为什么 KV cache 是推理系统里的核心状态</li><li>为什么 paged attention 的关键是管理灵活性和访问代价之间的平衡</li></ul><p>这三层最大的价值在于：</p><blockquote><p>你可以把系统层问题一路拆到执行层，再拆到算子层。</p></blockquote><h3 id="3-4-suan-zi-ceng-kan-dao-man-suan-zi-shi-ru-he-huan-yuan-cheng-cuda-wen-ti" tabindex="-1" id="3-4-算子层：看到慢算子时如何还原成-CUDA-问题">3.4 算子层：看到慢算子时如何还原成 CUDA 问题</h3><p>算子层的核心不是公式，而是代价模型。</p><p>以后看到一个算子，先问：</p><ul><li>它是 <code>elementwise</code>、<code>reduction</code>、<code>reorder</code> 还是 <code>blocked compute</code></li><li>它更像 <code>memory-bound</code> 还是 <code>compute-bound</code></li><li>中间结果主要留在寄存器、<code>shared memory</code> 还是频繁回写 <code>global memory</code></li></ul><p>几个最典型的推理映射：</p><p>Linear / MLP</p><ul><li>核心是 GEMM</li><li>真正关键的是 tile、复用、shared memory、寄存器和 tensor core 路径</li></ul><p>softmax / LayerNorm / RMSNorm</p><ul><li>本质是 reduction + elementwise</li><li>常见问题是中间结果回写多、同步多、launch 太碎</li></ul><p>attention</p><ul><li>本质是 GEMM-like 计算、reduction、reorder 和内存管理的组合</li><li>真正难点常在数据流和 KV cache 访问，而不只是公式</li></ul><p>所以当面试官问你一个算子为什么慢时，不要只说“复杂度高”，而要说：</p><ul><li>它在哪一层受限</li><li>是算得慢还是搬得慢</li><li>是复用不够还是 launch 太碎</li></ul><h3 id="3-5-zhi-xing-ceng-wei-shi-yao-hen-duo-wen-ti-bu-shi-kernel-ben-shen-man" tabindex="-1" id="3-5-执行层：为什么很多问题不是-kernel-本身慢">3.5 执行层：为什么很多问题不是 kernel 本身慢</h3><p>到了执行层，你要建立一个很重要的意识：</p><blockquote><p>很多推理性能问题，不是单个 kernel 本身差，而是整条执行流水组织得差。</p></blockquote><p>几个最典型的执行层因素：</p><p>stream</p><ul><li>本质是设备端的有序任务队列</li><li>它决定哪些工作必须排队，哪些工作有潜在并发机会</li></ul><p>异步拷贝和 pinned memory</p><ul><li>不是为了 API 好看</li><li>而是为了让 H2D / D2H 与计算更容易 overlap</li></ul><p>launch overhead</p><ul><li>在 prefill 这种大算子场景里往往不明显</li><li>在 decode、小 batch、小 kernel 场景里会被迅速放大</li></ul><p>CUDA Graph</p><ul><li>本质是在减少 CPU 反复提交稳定路径的成本</li><li>适合稳定重复的执行图，不适合无限动态的一切场景</li></ul><p>所以执行层最核心的提问方式是：</p><ul><li>GPU 是慢在算，还是慢在等</li><li>时间花在 compute、copy、sync，还是 host 调度</li><li>overlap 到底成立了没有</li></ul><h3 id="3-6-xi-tong-ceng-wei-shi-yao-tui-li-xi-tong-she-ji-ben-zhi-shang-shi-zi-yuan-guan-li" tabindex="-1" id="3-6-系统层：为什么推理系统设计本质上是资源管理">3.6 系统层：为什么推理系统设计本质上是资源管理</h3><p>到了系统层，很多话题表面看已经不像 CUDA，但底层逻辑还是一样。</p><p>最典型的是 KV cache。</p><p>KV cache 的本质是：</p><ul><li>用显存换计算</li><li>让 decode 不必重复算历史 token</li></ul><p>但这立刻会引出系统级问题：</p><ul><li>显存怎么管</li><li>请求进进出出时怎么避免碎片</li><li>长上下文下怎么让访问仍可接受</li></ul><p>paged attention 的重要性也在这里。<br>它真正解决的不是“分页”这个词，而是：</p><blockquote><p>在动态请求和长上下文场景下，让 KV cache 更可管理、更可复用、更可扩展。</p></blockquote><p>再比如 continuous batching。</p><p>它表面上是调度策略，底层本质却是：</p><ul><li>让 batch 不至于过稀</li><li>让 GPU 一直有比较规整的活可做</li><li>缓解 decode 阶段小步长、小并行度带来的资源浪费</li></ul><p>所以系统层真正关心的是：</p><ul><li>工作怎么持续喂给 GPU</li><li>显存和缓存怎么被长期管理</li><li>通信、计算、copy、调度如何重叠</li></ul><h3 id="3-7-zen-yang-ba-code-occupancy-code-code-coalescing-code-code-fusion-code-code-graph-code-code-batching-code-lian-dao-tui-li-xi-tong" tabindex="-1" id="3-7-怎样把-occupancy、coalescing、fusion、graph、batching-连到推理系统">3.7 怎样把 <code>occupancy</code>、<code>coalescing</code>、<code>fusion</code>、<code>graph</code>、<code>batching</code> 连到推理系统</h3><p>这一段是面试里最容易形成完整表达的地方。</p><p><code>occupancy</code></p><ul><li>不是性能分数</li><li>在推理里它更像“延迟隐藏能力”的一个窗口</li><li>decode、小 batch、小 kernel 时更容易显得不够</li></ul><p><code>coalescing</code></p><ul><li>不只是访存概念</li><li>它会直接影响 KV cache 访问、layout transform、attention 读取效率</li></ul><p><code>fusion</code></p><ul><li>不只是减少 kernel 数量</li><li>更重要的是减少中间结果写回 <code>global memory</code> 的次数</li><li>在 LayerNorm、softmax、bias、activation 一类链式算子里尤其关键</li></ul><p><code>graph</code></p><ul><li>不只是“优化 launch”</li><li>它在推理里真正有价值的场景是重复、稳定、可 capture 的路径</li></ul><p><code>batching</code></p><ul><li>不只是把请求凑一起</li><li>本质是在系统层维持更好的 GPU 利用率和执行规整性</li></ul><p>如果把它们串起来，你就会发现：</p><blockquote><p>推理优化本质上是在同时管理并行度、访存效率、执行流水和系统动态性。</p></blockquote><p>这就是面试里最值得讲出来的“整体观”。</p><h3 id="3-8-mian-shi-hui-da-wei-shi-yao-bu-neng-ting-liu-zai-api-ceng" tabindex="-1" id="3-8-面试回答为什么不能停留在-API-层">3.8 面试回答为什么不能停留在 API 层</h3><p>很多人的回答最大的问题是：</p><ul><li>会报术语</li><li>会背定义</li><li>但没有代价模型</li></ul><p>例如别人问：</p><ul><li>为什么 fusion 能快</li><li>为什么 decode 难优化</li><li>为什么 paged attention 重要</li></ul><p>如果你只回答：</p><ul><li>“因为减少 kernel 数量”</li><li>“因为 decode 更复杂”</li><li>“因为 paged attention 更适合长序列”</li></ul><p>这种回答信息密度太低。</p><p>更好的回答必须包含三件事：</p><p>第一，它在解决什么问题。</p><p>第二，底层为什么会快或更稳。</p><p>第三，它的代价和边界是什么。</p><p>这就是为什么我一直强调：</p><blockquote><p>面试回答重点应是代价模型和系统理解，而不是背 API。</p></blockquote><h3 id="3-9-yi-ge-tong-yong-de-hui-da-mo-ban" tabindex="-1" id="3-9-一个通用的回答模板">3.9 一个通用的回答模板</h3><p>以后你遇到任何 CUDA × 推理问题，都可以用下面这个模板组织：</p><p>第一步：先说它在解决什么问题。</p><p>例如：</p><ul><li>fusion 在解决中间结果频繁回写和 kernel 过碎</li><li>continuous batching 在解决动态请求下 GPU 吃不满</li><li>paged attention 在解决 KV cache 管理和碎片</li></ul><p>第二步：再说底层为什么有效。</p><p>例如：</p><ul><li>减少 global memory traffic</li><li>提高数据复用</li><li>减少 launch overhead</li><li>让 overlap 更容易成立</li></ul><p>第三步：说收益。</p><ul><li>更高吞吐</li><li>更低延迟</li><li>更稳的 GPU 利用率</li><li>更低显存压力</li></ul><p>第四步：说代价或边界。</p><ul><li>寄存器压力上升</li><li>动态 shape 更难做</li><li>实现复杂度更高</li><li>管理更复杂</li></ul><p>这套模板非常适合面试，因为它天然带工程感。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这一篇本身就是在把 CUDA 和 AI 推理直接连起来。</p><p>如果只用一句话概括：</p><blockquote><p>AI 推理中的很多高性能设计，表面上是在做系统、算子和调度，底层本质上都可以还原为 CUDA 的三个核心问题：线程如何组织、数据如何流动、延迟如何隐藏。</p></blockquote><p>更具体一点：</p><ul><li>prefill 更像大算子吞吐问题</li><li>decode 更像小步长、强动态、memory latency 和调度问题</li><li>attention 是最好的桥梁，因为它同时包含 GEMM、reduction、重排、KV cache 和系统调度代价</li><li>高水平推理优化，本质上是跨越算子层、执行层、系统层一起做 trade-off</li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>推理岗位不需要学 CUDA。不是，很多系统问题最后都能还原成 CUDA 代价模型。</li><li>学 CUDA 就是为了手写复杂 kernel。不是，更重要的是能读懂底层代价和系统边界。</li><li>面试只要会讲几个 API。不是，真正想听的是“为什么这样设计”。</li><li>fusion、graph、batching 是系统技巧，和 CUDA 没关系。不是，它们本质上都在管理执行和数据流。</li><li>occupancy 高、GPU 利用率高，就说明系统没问题。不是，QPS、首 token 延迟和 steady-state token latency 还取决于很多执行层和系统层因素。</li><li>paged attention 只是一个框架名词。不是，它背后是典型的显存管理与访问代价权衡。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>学到这里，你应该具备哪些能力？</li><li>为什么 AI 推理岗位会反复问 CUDA？</li><li>怎样用 <code>算子层 / 执行层 / 系统层</code> 三层框架去理解推理系统？</li><li>看到一个慢算子时，怎样把它还原成 CUDA 问题？</li><li>怎样把 <code>occupancy</code>、<code>coalescing</code>、<code>fusion</code>、<code>graph</code>、<code>batching</code> 连成一套系统理解？</li><li>为什么面试回答重点应该是代价模型和 trade-off，而不是背定义？</li><li>prefill 和 decode 的瓶颈差异应该怎样讲？</li><li>为什么说很多推理创新，本质上都是更懂 GPU 代价模型？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/c824c712/">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</a></li><li>下一篇：<a href="/posts/939f5917/">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</a></li><li>延伸阅读：<a href="/posts/821fc7ff/">GPU系统拆解-10-面试表达与系统思维：怎么把 GPU 理解讲成工程判断</a></li><li>延伸阅读：<a href="/posts/c3e99548/">vLLM系统拆解-11-面试题精讲：从概念到系统设计如何讲清楚为什么</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-12-mian-shi-zong-fu-pan-zen-yao-ba-cuda-neng-li-lian-jie-dao-ai-tui-li&quot; tabindex=&quot;-1&quot;&gt;CUDA系统拆解-12-面试总复盘：怎么把</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="面试" scheme="https://smarter.xin/tags/interview/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</title>
    <link href="https://smarter.xin/posts/c824c712/"/>
    <id>https://smarter.xin/posts/c824c712/</id>
    <published>2026-04-06T05:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-11-jing-dian-cuda-suan-zi-mo-shi-elementwise-reduction-reorder-yu-blocked-compute" tabindex="-1">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</h1><blockquote><p>本文是「CUDA系统拆解」系列第 11 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/55daa93f/">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</a><br>下一篇：<a href="/posts/18814acd/">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>经典 CUDA kernel 到底能分成哪几种模式。</li><li><code>elementwise</code>、<code>reduction</code>、<code>reorder</code>、<code>blocked compute</code> 各自怎么做线程映射。</li><li>reduction、transpose、GEMM 这些典型算子，瓶颈为什么完全不同。</li><li>看一个 kernel 时，怎样从 <code>work split</code>、访存和同步去读它。</li><li>怎样把这些模式直接映射到 AI 推理里的常见算子。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>经典 CUDA 算子大致可以分成五类：<code>elementwise</code>、<code>reduction</code>、<code>reorder</code>、<code>blocked compute</code>、<code>composite</code>。</li><li><code>elementwise</code> 通常最像“一线程一个元素”，常见瓶颈是 <code>memory-bound</code>。</li><li><code>reduction</code> 的关键不是公式，而是“如何把很多线程的中间结果安全、高效地合并起来”。</li><li><code>reorder</code> 的重点不在计算，而在访存模式和数据重排，transpose 是最典型案例。</li><li><code>blocked compute</code> 的核心是 tile 化和数据复用，GEMM 是最重要代表。</li><li>很多 AI 推理算子都不是全新物种，而是这些经典模式的组合。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-xian-jian-li-yi-zhang-suan-zi-mo-shi-di-tu" tabindex="-1" id="3-1-先建立一张算子模式地图">3.1 先建立一张算子模式地图</h3><p>看到一个 CUDA kernel，先别急着看每一行代码，先判断它属于哪种模式。</p><p><code>elementwise</code></p><ul><li>每个线程处理一个或几个独立元素</li><li>线程之间几乎没有依赖</li><li>常见于向量加法、激活函数、逐元素量化/反量化</li></ul><p><code>reduction</code></p><ul><li>多个线程共同算一个结果</li><li>必须合并中间结果</li><li>常见于 sum、max、norm、softmax 的分母计算</li></ul><p><code>reorder</code></p><ul><li>核心不是算，而是搬和重排</li><li>常见于 transpose、layout transform、某些 attention 前后的数据排布</li></ul><p><code>blocked compute</code></p><ul><li>一个 block 负责输出空间的一块 tile</li><li>输入数据通常会先进入 <code>shared memory</code></li><li>常见于 GEMM、卷积、attention 的核心矩阵块计算</li></ul><p><code>composite</code></p><ul><li>上面几种模式的组合</li><li>常见于 softmax、LayerNorm、RMSNorm、一些 fused kernel</li></ul><p>这一张图很重要，因为后面你看任何 kernel，都可以先把它放进某个框里，再谈瓶颈和优化。</p><h3 id="3-2-mo-shi-yi-code-elementwise-code" tabindex="-1" id="3-2-模式一：elementwise">3.2 模式一：<code>elementwise</code></h3><p>最基础的骨架长这样：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">vecAdd</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* A, <span class="type">const</span> <span class="type">float</span>* B, <span class="type">float</span>* C, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; N) &#123;</span><br><span class="line">        C[idx] = A[idx] + B[idx];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个模式最重要的特征是：</p><ul><li>问题空间通常是一维或容易摊平成一维</li><li>一个线程负责一个输出位置</li><li>线程之间几乎没有同步需求</li></ul><p>它的性能直觉也最简单：</p><ul><li>算得不多</li><li>读写不少</li><li>常常是典型的 <code>memory-bound</code></li></ul><p>所以 <code>elementwise</code> kernel 的关键通常不是复杂同步，而是：</p><ul><li>线程映射是否自然</li><li>访存是否连续</li><li>是否值得做 fusion，减少中间结果回写</li></ul><h3 id="3-3-mo-shi-er-code-reduction-code" tabindex="-1" id="3-3-模式二：reduction">3.3 模式二：<code>reduction</code></h3><p>最小代表性骨架可以写成：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">reduceSum</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* x, <span class="type">float</span>* out, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    __shared__ <span class="type">float</span> buf[<span class="number">256</span>];</span><br><span class="line">    <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + tid;</span><br><span class="line"></span><br><span class="line">    buf[tid] = (idx &lt; N) ? x[idx] : <span class="number">0.0f</span>;</span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> stride = blockDim.x / <span class="number">2</span>; stride &gt; <span class="number">0</span>; stride &gt;&gt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (tid &lt; stride) &#123;</span><br><span class="line">            buf[tid] += buf[tid + stride];</span><br><span class="line">        &#125;</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tid == <span class="number">0</span>) out[blockIdx.x] = buf[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个模式和 <code>elementwise</code> 最大的不同在于：</p><ul><li>一个输出不再对应一个线程</li><li>线程之间必须协作</li><li>中间结果要分阶段合并</li></ul><p>所以 reduction 的核心问题不再只是访存，而是：</p><ul><li>block 内怎样组织协作</li><li><code>shared memory</code> 是否合适</li><li>barrier 放得是否正确</li><li>后半程线程闲置是否严重</li></ul><p>你要特别记住 reduction 的性能直觉：</p><ul><li>它常常不是纯算力问题</li><li>它会同时受同步、访存、并行度下降影响</li></ul><p>这也是为什么 reduction 很适合用来考你是否真的理解 <code>shared memory</code> 和 <code>__syncthreads()</code>。</p><h3 id="3-4-mo-shi-san-code-reorder-code" tabindex="-1" id="3-4-模式三：reorder">3.4 模式三：<code>reorder</code></h3><p>transpose 是最经典代表。<br>它的难点不是公式，而是访存方向。</p><p>最小骨架可以这样理解：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">transposeNaive</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* in, <span class="type">float</span>* out, <span class="type">int</span> W, <span class="type">int</span> H)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">int</span> y = blockIdx.y * blockDim.y + threadIdx.y;</span><br><span class="line">    <span class="keyword">if</span> (x &lt; W &amp;&amp; y &lt; H) &#123;</span><br><span class="line">        out[x * H + y] = in[y * W + x];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个 kernel 逻辑完全正确，但经常不快。<br>因为它暴露了 <code>reorder</code> 模式的本质：</p><ul><li>算得很少</li><li>但读写方向往往无法同时都对 <code>global memory</code> 友好</li></ul><p>所以这类 kernel 的主问题通常是：</p><ul><li>coalescing 好不好</li><li>stride 大不大</li><li>是否需要用 <code>shared memory</code> 做 staging 和重排</li></ul><p>这就是为什么 transpose 常被当成“访存优化范例”，而不是“数学计算范例”。</p><h3 id="3-5-mo-shi-si-code-blocked-compute-code" tabindex="-1" id="3-5-模式四：blocked-compute">3.5 模式四：<code>blocked compute</code></h3><p>GEMM 是这个模式里最重要的代表。</p><p>一个最小 tiled 骨架可以写成：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TILE 16</span></span><br><span class="line"></span><br><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">gemmTiled</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* A, <span class="type">const</span> <span class="type">float</span>* B, <span class="type">float</span>* C,</span></span></span><br><span class="line"><span class="params"><span class="function">                          <span class="type">int</span> M, <span class="type">int</span> N, <span class="type">int</span> K)</span> </span>&#123;</span><br><span class="line">    __shared__ <span class="type">float</span> As[TILE][TILE];</span><br><span class="line">    __shared__ <span class="type">float</span> Bs[TILE][TILE];</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> row = blockIdx.y * TILE + threadIdx.y;</span><br><span class="line">    <span class="type">int</span> col = blockIdx.x * TILE + threadIdx.x;</span><br><span class="line">    <span class="type">float</span> sum = <span class="number">0.0f</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> t = <span class="number">0</span>; t &lt; (K + TILE - <span class="number">1</span>) / TILE; ++t) &#123;</span><br><span class="line">        <span class="type">int</span> aCol = t * TILE + threadIdx.x;</span><br><span class="line">        <span class="type">int</span> bRow = t * TILE + threadIdx.y;</span><br><span class="line"></span><br><span class="line">        As[threadIdx.y][threadIdx.x] =</span><br><span class="line">            (row &lt; M &amp;&amp; aCol &lt; K) ? A[row * K + aCol] : <span class="number">0.0f</span>;</span><br><span class="line">        Bs[threadIdx.y][threadIdx.x] =</span><br><span class="line">            (bRow &lt; K &amp;&amp; col &lt; N) ? B[bRow * N + col] : <span class="number">0.0f</span>;</span><br><span class="line"></span><br><span class="line">        __syncthreads();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> k = <span class="number">0</span>; k &lt; TILE; ++k) &#123;</span><br><span class="line">            sum += As[threadIdx.y][k] * Bs[k][threadIdx.x];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (row &lt; M &amp;&amp; col &lt; N) C[row * N + col] = sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个模式最关键的点不是“矩阵乘法公式”，而是：</p><ul><li>一个 block 负责输出矩阵的一块 tile</li><li>输入 tile 被 block 内线程协作搬到 <code>shared memory</code></li><li>每次 global memory 读进来的数据，会被尽可能多次复用</li></ul><p>所以 <code>blocked compute</code> 的核心是：</p><ul><li>tile 切得是否合理</li><li><code>shared memory</code> 是否真的提高了复用</li><li>寄存器和 <code>shared memory</code> 是否压得太重</li><li>block 内同步是否必要且不过多</li></ul><p>它和前几类模式最大的区别是：</p><blockquote><p>它不是在减少线程之间的关系，而是在有组织地利用线程协作和数据复用。</p></blockquote><h3 id="3-6-mo-shi-wu-code-composite-code" tabindex="-1" id="3-6-模式五：composite">3.6 模式五：<code>composite</code></h3><p>这类算子通常不是单一模式，而是多个模式拼起来。</p><p>最典型的例子是 softmax：</p><ul><li>先做 max reduction</li><li>再逐元素减 max 和 exp</li><li>再做 sum reduction</li><li>再逐元素归一化</li></ul><p>LayerNorm / RMSNorm 也很像：</p><ul><li>先 reduction</li><li>再 elementwise</li></ul><p>所以 <code>composite</code> 模式的关键不是“它更复杂”，而是：</p><ul><li>中间结果是否要回写 <code>global memory</code></li><li>哪些阶段值得 fusion</li><li>哪些阶段适合 block 内完成</li><li>哪些地方会把 launch overhead 放大</li></ul><p>这也是为什么很多推理 kernel 不追求“每一步都单独清晰”，而是追求“把多个阶段尽量留在同一个 kernel 里做完”。</p><h3 id="3-7-reduction-transpose-gemm-dao-di-chai-zai-na" tabindex="-1" id="3-7-reduction、transpose、GEMM-到底差在哪">3.7 reduction、transpose、GEMM 到底差在哪</h3><p>这三个是最适合对比着学的。</p><p>reduction</p><ul><li>重点是合并</li><li>主要矛盾是协作、同步、后半程并行度下降</li></ul><p>transpose</p><ul><li>重点是重排</li><li>主要矛盾是访存模式、coalescing、bank conflict</li></ul><p>GEMM</p><ul><li>重点是复用</li><li>主要矛盾是 tile 设计、数据流、<code>shared memory</code> 和寄存器的平衡</li></ul><p>这三个模式一旦分清楚，后面很多“看起来不同”的推理算子，其实都能归到这三条主线之一。</p><h3 id="3-8-zen-yang-tong-guo-code-work-split-code-fang-cun-he-tong-bu-qu-du-yi-ge-kernel" tabindex="-1" id="3-8-怎样通过-work-split、访存和同步去读一个-kernel">3.8 怎样通过 <code>work split</code>、访存和同步去读一个 kernel</h3><p>这是这篇最重要的实战部分。</p><p>以后你看到一个 kernel，先问四个问题：</p><p>第一，<code>work split</code> 是什么？</p><ul><li>一个线程负责什么</li><li>一个 block 为什么负责这块数据</li><li>grid 怎样覆盖全部输出空间</li></ul><p>第二，数据主要从哪来，往哪去？</p><ul><li>是直接从 <code>global memory</code> 读写</li><li>还是会先经过 <code>shared memory</code></li><li>中间结果是否主要留在寄存器</li></ul><p>第三，线程之间有没有协作？</p><ul><li>完全独立</li><li>只在 warp 内协作</li><li>还是 block 内要共享数据和同步</li></ul><p>第四，瓶颈更像哪一类？</p><ul><li><code>memory-bound</code></li><li><code>compute-bound</code></li><li>sync-heavy</li><li>launch-sensitive</li></ul><p>如果你能按这四步读 kernel，很多陌生代码都会变得可拆解。</p><h3 id="3-9-mei-lei-mo-shi-de-xing-neng-zhi-jue" tabindex="-1" id="3-9-每类模式的性能直觉">3.9 每类模式的性能直觉</h3><p><code>elementwise</code></p><ul><li>常见瓶颈：带宽</li><li>关键优化：coalescing、fusion、减少中间回写</li></ul><p><code>reduction</code></p><ul><li>常见瓶颈：同步、访存、后半程并行度浪费</li><li>关键优化：warp-level reduce、分层归约、减少 barrier</li></ul><p><code>reorder</code></p><ul><li>常见瓶颈：访存模式差</li><li>关键优化：<code>shared memory</code> staging、padding、layout 设计</li></ul><p><code>blocked compute</code></p><ul><li>常见瓶颈：数据复用不够、资源压力过高</li><li>关键优化：tiling、寄存器复用、<code>shared memory</code> 复用、pipeline 设计</li></ul><p><code>composite</code></p><ul><li>常见瓶颈：中间结果回写过多、launch 太碎</li><li>关键优化：fusion、阶段合并、就地消费中间值</li></ul><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常直接，因为推理里的大多数热点算子，都能拆成上面的模式。</p><p>几个典型映射：</p><ul><li>Linear / MLP：<code>blocked compute</code> 为主，本质接近 GEMM</li><li>softmax、LayerNorm、RMSNorm：<code>composite</code>，里面带有 reduction 和 elementwise</li><li>attention：通常是 GEMM-like 块计算、reduction、reorder 的组合</li><li>KV cache 读写、layout transform：常带有 <code>reorder</code> 特征</li><li>激活、bias、残差：很多是 <code>elementwise</code></li></ul><p>所以你后面看推理 kernel 时，真正该做的不是把它们当成“特殊算子名词”，而是先问：</p><ul><li>它属于哪种经典模式</li><li>它真正的瓶颈是协作、访存还是复用</li><li>它为什么会被做成 fused kernel</li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>经典算子实战就是刷几个教学样例。不是，真正要学的是模式和判断框架。</li><li><code>elementwise</code> 简单，所以没什么价值。不是，很多推理里的小算子恰恰最容易受带宽和 launch 影响。</li><li>reduction 就是“会写 shared memory 求和”。不是，重点是协作边界和合并路径设计。</li><li>transpose 只是数据搬一下。不是，它是访存重排和 <code>shared memory</code> 使用的经典范式。</li><li>GEMM 只是矩阵乘法。不是，它是 tile 化、复用、寄存器和 <code>shared memory</code> 平衡的代表。</li><li>softmax / LayerNorm 只要懂数学公式就行。不是，在 CUDA 上真正难的是阶段组织和中间结果数据流。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>经典 CUDA 算子大致能分成哪几类模式？</li><li><code>elementwise</code>、<code>reduction</code>、<code>reorder</code>、<code>blocked compute</code> 各自的线程映射方式有什么区别？</li><li>reduction、transpose、GEMM 的核心矛盾分别是什么？</li><li>看一个 kernel 时，怎样从 <code>work split</code>、访存和同步去读它？</li><li>为什么 <code>elementwise</code> 常常是 <code>memory-bound</code>？</li><li>为什么 transpose 常常要借助 <code>shared memory</code>？</li><li>为什么 GEMM 天然适合 tile 化和数据复用？</li><li>为什么很多 AI 推理算子本质上都是这些经典模式的组合？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/55daa93f/">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</a></li><li>下一篇：<a href="/posts/18814acd/">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</a></li><li>延伸阅读：<a href="/posts/44fe4eb3/">CUDA实践-02-归约与线程粗化</a></li><li>延伸阅读：<a href="/posts/1a09f6f/">CUDA实践-04-矩阵乘优化与寄存器分块</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-11-jing-dian-cuda-suan-zi-mo-shi-elementwise-reduction-reorder-yu-blocked-compute&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="核函数" scheme="https://smarter.xin/tags/kernel-function/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</title>
    <link href="https://smarter.xin/posts/55daa93f/"/>
    <id>https://smarter.xin/posts/55daa93f/</id>
    <published>2026-04-06T05:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-10-profiling-diao-shi-yu-ping-jing-ding-wei-xian-zhao-dao-gen-yin-zai-tan-you-hua" tabindex="-1">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</h1><blockquote><p>本文是「CUDA系统拆解」系列第 10 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/481fda56/">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</a><br>下一篇：<a href="/posts/c824c712/">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>Profiling 到底在看什么，为什么不能凭感觉猜性能。</li><li>正确性调试和性能分析为什么是两条线。</li><li><code>Nsight Systems</code>、<code>Nsight Compute</code>、<code>Compute Sanitizer</code> 分别解决什么问题。</li><li>分析 CUDA 程序时，正确的顺序应该是什么。</li><li>怎样把工具结果映射到算力、带宽、同步、launch、调度这些瓶颈类别。</li><li>怎样把这些分析方法直接用于推理延迟、QPS 和 token latency。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>先定位再优化，不能看代码猜性能。</li><li>正确性调试和性能分析是两条线：前者回答“有没有错”，后者回答“为什么慢”。</li><li><code>Nsight Systems</code> 看整条时间线和执行流水，<code>Nsight Compute</code> 看单个 kernel 的微观瓶颈，<code>Compute Sanitizer</code> 抓越界、竞争、未初始化访问和同步错误。</li><li>CUDA 瓶颈通常可以先分成几类：算力、带宽、同步、launch、调度。</li><li>正确的分析顺序通常是：先看全局 timeline，再锁定热点 kernel，再下钻微观指标；有不稳定结果时先排 correctness。</li><li>在 AI 推理里，真正重要的不只是单个 kernel 快不快，还包括 prefill / decode 的阶段行为、QPS、首 token 延迟和 steady-state token latency。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-profiling-bu-shi-ke-xuan-xiang" tabindex="-1" id="3-1-为什么-Profiling-不是可选项">3.1 为什么 Profiling 不是可选项</h3><p>学 CUDA 到这个阶段，最容易出现一种错觉：</p><ul><li>理论大概都懂</li><li>kernel 也能写</li><li>访存、同步、occupancy 这些名词都知道</li></ul><p>但一到真实工程里就会卡住：</p><ul><li>程序慢，不知道慢在哪</li><li>某个 batch 变慢，不知道是 kernel 问题还是流水问题</li><li>某轮结果异常，不知道是越界、竞争还是同步错了</li></ul><p>这就是“会写代码”和“会做工程”的分界线。</p><p>真正的工程能力不是背出多少概念，而是能回答：</p><ul><li>时间到底花在哪</li><li>为什么花在这里</li><li>该先改哪一层</li><li>改完之后是否真的变好了</li></ul><p>所以 profiling 的本质就是：</p><blockquote><p>用证据替代感觉。</p></blockquote><h3 id="3-2-profiling-dao-di-zai-kan-shi-yao" tabindex="-1" id="3-2-Profiling-到底在看什么">3.2 Profiling 到底在看什么</h3><p>从本质上说，profiling 在做两件事。</p><p>第一，看时间分布。<br>也就是回答：</p><ul><li>时间主要花在 kernel、memcpy、同步，还是 CPU 侧调度</li><li>是某个大 kernel 特别慢，还是一堆小 kernel 把时间切碎了</li><li>copy 和 compute 有没有 overlap</li><li>GPU 是真的忙，还是 timeline 上有很多空洞</li></ul><p>第二，看资源为什么没发挥出来。<br>也就是回答：</p><ul><li>是算力没吃满，还是带宽没吃满</li><li>是 active warps 不够，还是 stall 太多</li><li>是访存模式差，还是同步太重</li><li>是 launch 太碎，还是 host 端把 GPU 喂不饱</li></ul><p>所以 profiling 不是“看一个总时间”，而是：</p><blockquote><p>先看哪里慢，再看为什么慢。</p></blockquote><h3 id="3-3-zheng-que-xing-diao-shi-he-xing-neng-fen-xi-shi-liang-tiao-xian" tabindex="-1" id="3-3-正确性调试和性能分析是两条线">3.3 正确性调试和性能分析是两条线</h3><p>这一点必须刻意区分。</p><p>正确性调试关心的是：</p><ul><li>有没有越界</li><li>有没有未初始化访问</li><li>有没有 race condition</li><li>barrier 用法有没有问题</li><li>某些输入为什么偶发出错</li></ul><p>性能分析关心的是：</p><ul><li>是算力瓶颈还是带宽瓶颈</li><li>是 launch overhead 还是同步开销</li><li>是 CPU 调度问题还是 device 端问题</li><li>改 block size、访存、fusion 是否真的有收益</li></ul><p>这两条线经常有关联，但不能混着做。<br>尤其当现象是：</p><ul><li>偶发错</li><li>不稳定</li><li>某些输入才坏</li></ul><p>这类问题首先要怀疑 correctness，而不是先做性能优化。</p><h3 id="3-4-san-lei-he-xin-gong-ju-de-fen-gong" tabindex="-1" id="3-4-三类核心工具的分工">3.4 三类核心工具的分工</h3><p>这一篇最核心的工具分工只有三类。</p><p><code>Nsight Systems</code></p><ul><li>看系统级 timeline</li><li>看 CPU 和 GPU 在干什么</li><li>看 stream、memcpy、kernel 是否重叠</li><li>看有没有大量 idle 区间</li><li>看 launch、调度、流水是否有问题</li></ul><p>它最适合回答：</p><blockquote><p>整条执行流水到底怎么跑的。</p></blockquote><p><code>Nsight Compute</code></p><ul><li>看单个 kernel 的微观行为</li><li>看它更像 <code>memory-bound</code> 还是 <code>compute-bound</code></li><li>看 occupancy、warp stall、memory workload、cache 行为</li><li>看源码或指令层面的热点位置</li></ul><p>它最适合回答：</p><blockquote><p>这个 kernel 为什么慢。</p></blockquote><p><code>Compute Sanitizer</code></p><ul><li>抓越界和非法访存</li><li>抓 shared memory 数据竞争</li><li>抓 global memory 未初始化访问</li><li>抓同步原语使用错误</li></ul><p>它最适合回答：</p><blockquote><p>程序是不是有 correctness 问题。</p></blockquote><h3 id="3-5-zheng-que-de-fen-xi-shun-xu" tabindex="-1" id="3-5-正确的分析顺序">3.5 正确的分析顺序</h3><p>很多人一看到程序慢，就立刻去盯单个 kernel 指标。<br>这经常是顺序错了。</p><p>更稳的顺序通常是：</p><p>第一步，先用 <code>Nsight Systems</code> 看全局 timeline。</p><p>先回答：</p><ul><li>GPU 有没有长时间空闲</li><li>memcpy 和 kernel 有没有 overlap</li><li>多 stream 是否真的并发</li><li>CPU 是否喂不饱 GPU</li><li>timeline 上是不是挤满了短小 kernel</li></ul><p>第二步，锁定真正的热点。</p><p>也就是：</p><ul><li>总时间占比最高的几个 kernel</li><li>调用次数极高的短 kernel</li><li>某类输入下明显异常的阶段</li></ul><p>第三步，再用 <code>Nsight Compute</code> 下钻。</p><p>这时才去问：</p><ul><li>它是带宽瓶颈还是算力瓶颈</li><li>occupancy 是否被资源限制</li><li>stall 主要来自哪里</li><li>访存和同步结构是否健康</li></ul><p>第四步，如果行为不稳定或结果可疑，先跑 <code>Compute Sanitizer</code>。</p><p>因为很多看起来像性能问题的现象，最后其实是 correctness 问题。</p><h3 id="3-6-ru-he-li-jie-ji-lei-chang-jian-ping-jing" tabindex="-1" id="3-6-如何理解几类常见瓶颈">3.6 如何理解几类常见瓶颈</h3><p>这篇需要把“瓶颈分类”建立出来。</p><p>算力瓶颈</p><ul><li>SM 侧计算吞吐更接近上限</li><li>访存不是主因</li><li>更该关注指令吞吐、tensor core 利用、算法结构</li></ul><p>带宽瓶颈</p><ul><li>memory throughput 高或明显受限</li><li>warp 大量在等内存</li><li>更该关注 coalescing、reuse、tiling、fusion</li></ul><p>同步瓶颈</p><ul><li>barrier 多</li><li>block 内工作不均衡</li><li>大量时间花在等待别的线程</li></ul><p>launch 瓶颈</p><ul><li>kernel 很短</li><li>但调用次数很多</li><li>decode 或小 batch 场景特别容易出现</li></ul><p>调度瓶颈</p><ul><li>GPU timeline 有空洞</li><li>CPU 端提交、拼 batch、准备 metadata 太慢</li><li>stream 组织不合理</li></ul><p>这几类瓶颈不是互斥的，但先把问题粗分到这一层，后面分析会清楚很多。</p><h3 id="3-7-code-nsight-systems-code-zai-kan-shi-yao" tabindex="-1" id="3-7-Nsight-Systems-在看什么">3.7 <code>Nsight Systems</code> 在看什么</h3><p><code>Nsight Systems</code> 最适合看“整条流水”。</p><p>你打开 timeline 后，最应该先看的是：</p><ul><li>GPU 是否有明显 idle 空洞</li><li>H2D / D2H copy 是否和 kernel overlap</li><li>多 stream 任务是不是交错执行</li><li>API 调用是否很碎</li><li>host 线程是否经常在等</li></ul><p>很多问题在这一层就已经暴露了：</p><ul><li>GPU 不是慢，而是没活干</li><li>stream 看起来很多，但其实被同步点拉直了</li><li>memcpy 很重，真正瓶颈在数据流</li><li>decode 阶段 kernel 太碎，launch overhead 明显</li></ul><p>所以 <code>Nsight Systems</code> 的价值在于：</p><blockquote><p>先判断问题是单个 kernel 的问题，还是整条系统流水的问题。</p></blockquote><h3 id="3-8-code-nsight-compute-code-zai-kan-shi-yao" tabindex="-1" id="3-8-Nsight-Compute-在看什么">3.8 <code>Nsight Compute</code> 在看什么</h3><p>当你已经确定某个 kernel 值得下钻时，才轮到 <code>Nsight Compute</code>。</p><p>你不需要一开始就看上百个指标，先抓几类：</p><ul><li>这个 kernel 整体更像 <code>memory-bound</code> 还是 <code>compute-bound</code></li><li>occupancy 是否受寄存器或 <code>shared memory</code> 限制</li><li>warp stall 的主因是什么</li><li>memory workload 是否健康</li><li>源码哪一段真正是热点</li></ul><p>这里最容易犯的错是“盯某个单独指标看”。<br>例如：</p><ul><li>occupancy 低，不一定就是主因</li><li>某个 stall 高，不一定就最该先改</li><li>memory throughput 不高，也不代表访存没问题</li></ul><p>指标必须放回上下文里解释。</p><h3 id="3-9-code-compute-sanitizer-code-zai-zhua-shi-yao" tabindex="-1" id="3-9-Compute-Sanitizer-在抓什么">3.9 <code>Compute Sanitizer</code> 在抓什么</h3><p>当你怀疑程序有 correctness 问题时，<code>Compute Sanitizer</code> 是第一工具。</p><p>你至少要知道几类典型问题：</p><ul><li>越界、misaligned access、非法访存</li><li>shared memory race</li><li>global memory 未初始化访问</li><li>barrier 或同步原语使用错误</li></ul><p>这类问题特别容易出现在：</p><ul><li>边界条件没处理好</li><li>shared memory 协作写错了</li><li>只在某些输入规模下才会触发</li><li>某些分支没覆盖完整写入</li></ul><p>所以遇到“偶发错”“偶发 NaN”“某些 batch 才坏”时，优先跑 sanitizer，比盲改代码稳得多。</p><h3 id="3-10-ru-he-ba-gong-ju-jie-guo-ying-she-dao-tui-li-ping-jing" tabindex="-1" id="3-10-如何把工具结果映射到推理瓶颈">3.10 如何把工具结果映射到推理瓶颈</h3><p>这一篇和 AI 推理的连接点就在这里。</p><p>你不能只会看“kernel 快不快”，还要能把工具结果映射到用户真正关心的指标上：</p><ul><li>首 token 延迟</li><li>steady-state token latency</li><li>QPS</li><li>吞吐</li><li>尾延迟</li></ul><p>几个典型映射方式：</p><p>如果 <code>Nsight Systems</code> 显示 decode 阶段 timeline 被大量短 kernel 切碎：</p><ul><li>很可能是 launch overhead、调度开销、kernel 粒度问题</li><li>可以考虑 fusion、CUDA Graph、persistent kernel、continuous batching</li></ul><p>如果 <code>Nsight Compute</code> 显示某个 LayerNorm / softmax kernel 明显 <code>memory-bound</code>：</p><ul><li>重点就不是继续抠 occupancy</li><li>而是看访存模式、fusion、reuse、KV cache 访问结构</li></ul><p>如果 timeline 上 GPU 空洞很多：</p><ul><li>问题可能不在 kernel</li><li>而在 host 端 request packing、metadata 准备、stream 编排、buffer 管理</li></ul><p>如果 prefill 很快，但 decode token latency 很差：</p><ul><li>很可能不是大算子吞吐问题</li><li>而是小 batch、小 kernel、launch、memory latency 和流水线组织问题</li></ul><p>所以 profiling 的真正价值不是“读懂工具界面”，而是：</p><blockquote><p>把工具中的时间线和指标，翻译成推理系统里的真实瓶颈。</p></blockquote><h3 id="3-11-prefill-he-decode-wei-shi-yao-yao-fen-kai-kan" tabindex="-1" id="3-11-prefill-和-decode-为什么要分开看">3.11 prefill 和 decode 为什么要分开看</h3><p>这是推理 profiling 里必须建立的意识。</p><p><code>prefill</code></p><ul><li>通常并行度更高</li><li>大算子更多</li><li>更像吞吐问题</li><li>大 GEMM / attention kernel 常是主角</li></ul><p><code>decode</code></p><ul><li>batch 往往更小</li><li>每步工作量更碎</li><li>更容易暴露 launch overhead</li><li>更容易受 host 调度、stream 组织、memory latency 影响</li></ul><p>所以同一套工具，在两个阶段看到的重点经常完全不同。<br>这也是为什么做推理分析时，必须先把 workload 场景说清楚。</p><h3 id="3-12-yi-ge-shi-yong-de-pai-cha-mo-ban" tabindex="-1" id="3-12-一个实用的排查模板">3.12 一个实用的排查模板</h3><p>以后你拿到一个 CUDA / 推理问题，可以先按这套模板走。</p><p>第一问：问题是稳定的，还是偶发的？</p><ul><li>偶发或不稳定，优先怀疑 correctness</li></ul><p>第二问：时间主要花在哪？</p><ul><li>先看 <code>Nsight Systems</code></li></ul><p>第三问：是系统流水问题，还是热点 kernel 问题？</p><ul><li>流水问题就查 stream、copy、host 调度</li><li>kernel 问题再下钻 <code>Nsight Compute</code></li></ul><p>第四问：瓶颈更像哪一类？</p><ul><li>算力</li><li>带宽</li><li>同步</li><li>launch</li><li>调度</li></ul><p>第五问：这个问题如何映射回业务指标？</p><ul><li>QPS 下降</li><li>首 token 延迟升高</li><li>steady-state token latency 变差</li></ul><p>这套模板在面试里也很好用，因为它比单纯背指标更像工程思维。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常强，而且比前面几篇都更直接。</p><p>原因很简单：推理系统的瓶颈经常不只存在于单个 kernel，而是存在于整条执行链。</p><p>典型问题包括：</p><ul><li>prefill 是否真的吃满 GPU</li><li>decode 是否被小 kernel 和 launch overhead 拖慢</li><li>H2D / D2H copy 是否和计算 overlap</li><li>stream 编排是否成立</li><li>host 端 batch 维护和 metadata 准备是否成为瓶颈</li><li>某个热点 kernel 是否是 memory-bound</li><li>KV cache 访问模式是否拖住 token latency</li></ul><p>所以做 AI infra / 推理时，这篇的真正意义是：</p><blockquote><p>你要学会把 CUDA 工具的输出，翻译成系统吞吐和时延问题。</p></blockquote><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>看代码就能猜出瓶颈。不是，先看证据。</li><li>程序慢就先抠单个 kernel。不是，很多问题先要看全局 timeline。</li><li>correctness 和 performance 可以一起乱改。不是，偶发问题先排 correctness。</li><li>occupancy 高说明 kernel 没问题。不是，瓶颈可能在带宽、同步、launch 或调度。</li><li>多 stream 就一定 overlap。不是，要看 timeline 是否真的成立。</li><li>GPU 利用率不低就说明推理系统没问题。不是，token latency 和 QPS 还取决于流水线组织。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>Profiling 到底在看什么？</li><li>为什么正确性调试和性能分析要分成两条线？</li><li><code>Nsight Systems</code>、<code>Nsight Compute</code>、<code>Compute Sanitizer</code> 各自解决什么问题？</li><li>为什么通常先看全局 timeline，再看单个 kernel？</li><li>CUDA 瓶颈可以先粗分成哪几类？</li><li>如果一个 decode 路径很慢，你会先看哪些系统级现象？</li><li>如果一个热点 kernel 很慢，你会怎样判断它更像 <code>memory-bound</code> 还是 <code>compute-bound</code>？</li><li>如何把工具结果映射到推理的 QPS、首 token 延迟和 token latency？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/481fda56/">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</a></li><li>下一篇：<a href="/posts/c824c712/">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</a></li><li>延伸阅读：<a href="/posts/edff4eb0/">GPU系统拆解-09-Profiling 与性能定位：先找到瓶颈，再谈优化</a></li><li>延伸阅读：<a href="/posts/d53a5f5b/">PyTorch推理工程-07-Profiling与瓶颈分析</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-10-profiling-diao-shi-yu-ping-jing-ding-wei-xian-zhao-dao-gen-yin-zai-tan-you-hua&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="Nsight Compute" scheme="https://smarter.xin/tags/nsight-compute/"/>
    
    <category term="性能剖析" scheme="https://smarter.xin/tags/performance-profiling/"/>
    
    <category term="调试" scheme="https://smarter.xin/tags/debugging/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</title>
    <link href="https://smarter.xin/posts/481fda56/"/>
    <id>https://smarter.xin/posts/481fda56/</id>
    <published>2026-04-06T04:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-09-streams-yi-bu-kao-bei-yu-overlap-ru-he-ba-kao-bei-he-ji-suan-die-qi-lai" tabindex="-1">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</h1><blockquote><p>本文是「CUDA系统拆解」系列第 09 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/cae09a20/">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</a><br>下一篇：<a href="/posts/55daa93f/">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><p>这一篇要讲清 5 件事：</p><ul><li>为什么 CUDA 程序不能只盯着“一个 kernel 算得快不快”，还要关心整条执行流水。</li><li><code>stream</code> 到底是什么，它和“主机端异步返回”是什么关系。</li><li>为什么“主机端看起来异步”不等于“设备端真的并发执行”。</li><li><code>pinned memory</code> 为什么会影响异步拷贝和计算重叠。</li><li><code>overlap</code> 到底在什么条件下才成立，以及它为什么在小 batch 和 <code>decode</code> 场景更重要。</li></ul><p>如果这篇只记住一句话，那就是：</p><p><strong>异步执行的目标不是把 API 写成异步，而是把拷贝、launch 和计算组织成真正能重叠的设备端流水。</strong></p><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>stream</code> 可以理解成设备端的一条有序执行队列；同一个 stream 内通常按提交顺序执行，不同 stream 才有机会并发。</li><li>主机端函数“提前返回”只说明 CPU 没被阻塞，不说明 GPU 上的拷贝和 kernel 一定并发。</li><li>默认 <code>stream</code> 往往带来更强的顺序语义，如果所有操作都丢进默认 stream，就很难形成真正的 overlap。</li><li><code>pinned memory</code> 是页锁定内存。它更适合做高效 DMA 拷贝，也是很多异步 H2D / D2H 和重叠执行成立的前提。</li><li><code>overlap</code> 通常需要同时满足：不同 stream、异步 API、合适的硬件能力、合适的内存类型，以及没有额外同步把流水打断。</li><li>在大算子很重的场景里，launch 和流水组织问题可能被算力掩盖；在小 batch、短序列、<code>decode</code> 这类场景里，它们会更容易暴露出来。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-xu-yao-yi-bu-zhi-xing" tabindex="-1" id="3-1-为什么需要异步执行">3.1 为什么需要异步执行</h3><p>最朴素的 CUDA 执行方式是：</p><ol><li>把数据从主机拷到设备</li><li>launch 一个 kernel</li><li>等它跑完</li><li>再把结果拷回主机</li></ol><p>这种方式逻辑简单，但资源利用率通常不高。原因是：</p><ul><li>CPU 在很多时刻只是等待</li><li>PCIe / NVLink 拷贝和 GPU 计算没有尽量重叠</li><li>多个小任务之间容易出现空洞</li></ul><p>所以异步执行的目标不是“代码看起来高级一点”，而是尽量把：</p><ul><li>主机准备下一批工作</li><li>H2D / D2H 拷贝</li><li>kernel 执行</li></ul><p>组织成一条连续流水，减少设备空转和主机等待。</p><h3 id="3-2-stream-dao-di-shi-shi-yao" tabindex="-1" id="3-2-stream-到底是什么">3.2 stream 到底是什么</h3><p>理解 <code>stream</code> 时，最稳的口径是：</p><p><strong>stream 是设备端的一条有序工作队列。</strong></p><p>你往一个 stream 里提交的操作，例如：</p><ul><li>H2D 拷贝</li><li>kernel launch</li><li>D2H 拷贝</li></ul><p>通常会按顺序排队。于是：</p><ul><li>同一个 stream 更强调顺序性</li><li>不同 stream 才有机会被设备调度成并发</li></ul><p>这里要特别区分两件事：</p><ul><li>主机端 API 是否阻塞</li><li>设备端任务是否并发</li></ul><p>例如，一个异步 API 可能很快就返回给 CPU，但 GPU 端仍然只是把任务排进某条 stream，最后是否并发，还要看设备端队列关系和资源条件。</p><h3 id="3-3-mo-ren-stream-he-pu-tong-stream-de-qu-bie" tabindex="-1" id="3-3-默认-stream-和普通-stream-的区别">3.3 默认 stream 和普通 stream 的区别</h3><p>很多初学者以为“用了异步 API 就天然并发”，问题常常出在默认 <code>stream</code> 上。</p><p>默认 <code>stream</code> 的直觉可以理解成：它常常带着更强的顺序语义。<br>如果所有拷贝和 kernel 都放在同一条默认 stream 里，那么即使主机端调用是异步的，设备端通常也还是顺序推进。</p><p>所以判断有没有 overlap，不能只看代码里有没有：</p><ul><li><code>cudaMemcpyAsync</code></li><li>异步 launch</li></ul><p>还要看：</p><ul><li>是否真的分到了不同 stream</li><li>中间是否插入了同步</li><li>默认 stream 语义是否把事情重新串行化了</li></ul><h3 id="3-4-wei-shi-yao-zhu-ji-yi-bu-bu-deng-yu-she-bei-duan-bing-fa" tabindex="-1" id="3-4-为什么主机异步不等于设备端并发">3.4 为什么主机异步不等于设备端并发</h3><p>这是这一篇最容易踩坑的地方。</p><p>“主机异步”只说明：</p><ul><li>CPU 提交任务后不用原地等</li><li>CPU 可以继续做别的事</li></ul><p>但设备端是否并发，还要继续问：</p><ul><li>这些任务是否在不同 stream</li><li>GPU 是否有 copy engine 和计算资源可同时工作</li><li>数据拷贝是否真的是异步可执行的</li><li>是否有 <code>cudaDeviceSynchronize</code>、事件等待、默认 stream 依赖把队列卡住</li></ul><p>所以更准确的说法是：</p><p><strong>异步是主机视角的接口行为，并发是设备视角的执行结果。两者相关，但不等价。</strong></p><h3 id="3-5-pinned-memory-wei-shi-yao-zhong-yao" tabindex="-1" id="3-5-pinned-memory-为什么重要">3.5 pinned memory 为什么重要</h3><p><code>pinned memory</code> 指的是页锁定内存，也叫 page-locked memory。它和普通 pageable host memory 的关键区别在于：</p><ul><li>普通主机内存可能被操作系统换页</li><li><code>pinned memory</code> 不会被随意换出</li><li>设备做 DMA 传输时更容易直接、高效地访问它</li></ul><p>这对 CUDA 有两个直接意义：</p><ol><li>主机到设备、设备到主机的拷贝效率通常更好</li><li>很多真正想做异步拷贝和 overlap 的场景，会更依赖 <code>pinned memory</code></li></ol><p>可以把它理解成：<br>如果 host memory 本身不适合直接做高效异步搬运，那么所谓“异步拷贝”在设备端很可能达不到你预期的重叠效果。</p><p>所以 <code>pinned memory</code> 的意义不只是“快一点”，而是它经常是异步数据通路能否真正成立的一块地基。</p><h3 id="3-6-overlap-zai-shi-yao-tiao-jian-xia-cai-cheng-li" tabindex="-1" id="3-6-overlap-在什么条件下才成立">3.6 overlap 在什么条件下才成立</h3><p>想让拷贝和计算重叠，通常要同时满足几类条件：</p><ul><li>使用异步 API，而不是阻塞式拷贝</li><li>工作被放到合适的多个 stream，而不是全都塞进同一条队列</li><li>host memory 类型合适，常见就是使用 <code>pinned memory</code></li><li>GPU 具有相应的并发执行能力，例如 copy engine 与计算单元能协同</li><li>没有额外同步把流水切断</li></ul><p>也就是说，真正的 overlap 是一条链路条件同时满足后的结果。<br>只满足其中一个条件，通常不够。</p><p>这也是为什么很多人代码里已经写了 <code>cudaMemcpyAsync</code>，实际 profile 一看仍然几乎全串行。问题不是“异步 API 没用”，而是整条流水没有真的被搭起来。</p><h3 id="3-7-overlap-de-gong-cheng-yi-yi" tabindex="-1" id="3-7-overlap-的工程意义">3.7 overlap 的工程意义</h3><p><code>overlap</code> 的价值，本质上是在隐藏不能消掉的时间：</p><ul><li>H2D / D2H 拷贝时间</li><li>kernel launch 间隙</li><li>多阶段流水之间的等待</li></ul><p>如果一段计算本身非常重，例如一个大 GEMM 跑很久，那么这些开销有时会被淹没。<br>但如果任务变碎，问题就会变明显：</p><ul><li>batch 很小</li><li>请求很多但每个都短</li><li><code>decode</code> 每步计算量小</li><li>一轮里要频繁 launch 多个小 kernel</li></ul><p>这时瓶颈可能不再只是某个 kernel 的 FLOPs，而是：</p><ul><li>launch overhead</li><li>stream 组织</li><li>拷贝和计算有没有重叠</li><li>CPU 和 GPU 是否形成稳定流水</li></ul><p>所以在 AI 推理里，异步执行和 overlap 往往不是“高级优化项”，而是系统吞吐和尾延迟能不能做好的基础条件。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这一篇和推理系统的关系非常直接。</p><p>很多在线推理场景并不是“大 batch 大矩阵一路轰过去”，而是：</p><ul><li>动态请求持续到达</li><li>batch 经常变化</li><li>小 kernel 很多</li><li><code>decode</code> 单步计算偏小</li><li>主机端调度、数据准备和设备端执行要持续配合</li></ul><p>所以推理系统里常见的问题，最后都能翻译回这一篇：</p><ul><li>为什么小 batch 吞吐上不去</li><li>为什么 GPU 利用率不稳定</li><li>为什么 <code>decode</code> 阶段 launch 开销特别显眼</li><li>为什么有些系统要非常重视 stream、事件、异步 H2D、pinned buffer 和流水组织</li></ul><p>换句话说，大模型推理不只是“kernel 快”，还要“整机流水顺”。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>用了异步 API，不代表 GPU 端就一定并发。</li><li><code>stream</code> 不是 CPU 线程，也不是“随便分个标签”；它的本质是设备端队列关系。</li><li>默认 <code>stream</code> 往往会带来更强顺序语义，容易把你以为的并发重新串起来。</li><li><code>pinned memory</code> 不只是更快的 host memory，它经常决定异步拷贝和 overlap 能不能真正成立。</li><li>overlap 不是单点优化，而是拷贝、队列、硬件能力和同步策略共同作用的结果。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么说 <code>stream</code> 更适合理解成设备端的有序执行队列？</li><li>为什么“主机端异步返回”不等于“设备端并发执行”？</li><li>默认 <code>stream</code> 为什么容易让程序重新串行化？</li><li><code>pinned memory</code> 对异步拷贝和 overlap 的价值到底是什么？</li><li>overlap 通常需要满足哪些条件？</li><li>为什么小 batch 和 <code>decode</code> 场景更容易暴露 launch overhead 和流水组织问题？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/cae09a20/">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</a></li><li>下一篇：<a href="/posts/55daa93f/">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</a></li><li>延伸阅读：<a href="/posts/b1c0c6a3/">CUDA实践-08-多流图执行与扩展开发</a></li><li>延伸阅读：<a href="/posts/82dbd045/">PyTorch推理工程-04-CUDA与显存同步</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-09-streams-yi-bu-kao-bei-yu-overlap-ru-he-ba-kao-bei-he-ji-suan-die-qi-lai&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="Multi-Stream" scheme="https://smarter.xin/tags/multi-stream/"/>
    
    <category term="Async Copy" scheme="https://smarter.xin/tags/async-copy/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</title>
    <link href="https://smarter.xin/posts/cae09a20/"/>
    <id>https://smarter.xin/posts/cae09a20/</id>
    <published>2026-04-06T04:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-08-occupancy-ji-cun-qi-ya-li-yu-launch-can-shu-diao-you-bu-shi-ba-zhan-yong-lu-la-man" tabindex="-1">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</h1><blockquote><p>本文是「CUDA系统拆解」系列第 08 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/416851d9/">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</a><br>下一篇：<a href="/posts/481fda56/">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li><code>occupancy</code> 到底是什么，它为什么重要。</li><li>为什么 <code>occupancy</code> 高不等于最终性能高。</li><li>一个 SM 上到底是被哪些资源限制住的。</li><li><code>block size</code> 为什么会影响并发度、调度粒度和资源切分方式。</li><li>什么是 <code>register pressure</code>，为什么会引出 <code>spilling</code>。</li><li>Launch 参数调优为什么本质上是在平衡多个约束，而不是套固定公式。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>occupancy</code> 指的是一个 SM 上活跃 warp 数相对于该 SM 最大可支持活跃 warp 数的比例。</li><li><code>occupancy</code> 的意义主要是帮助隐藏延迟，而不是直接代表性能。</li><li>限制 <code>occupancy</code> 的常见资源包括：线程数、warp 数、block 数、寄存器总量、<code>shared memory</code> 总量。</li><li><code>block size</code> 不只是线程数设置，它还决定了资源分配粒度、warp 数量和调度灵活性。</li><li><code>register pressure</code> 高会压低并发，严重时会触发 <code>spilling</code>，让本该在寄存器里的数据掉到更慢的路径。</li><li>Launch 调优的本质是平衡：更多活跃 warp，还是更强的数据复用与单线程效率。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-xian-ba-he-xin-wen-ti-shuo-tou-yi-ge-sm-shang-dao-di-neng-gua-duo-shao-huo" tabindex="-1" id="3-1-先把核心问题说透：一个-SM-上到底能挂多少活">3.1 先把核心问题说透：一个 SM 上到底能挂多少活</h3><p>到了这一篇，你应该把关注点从“线程怎么写”转到“资源怎么被占满”。</p><p>同一个 kernel launch 之后：</p><ul><li>thread 组成 block</li><li>block 被分配到 SM</li><li>block 再拆成多个 warp</li></ul><p>所以这一篇真正要回答的问题是：</p><blockquote><p>一个 SM 上，在同一时刻到底能同时驻留多少 block、多少 warp、多少 thread？</p></blockquote><p>这个问题之所以重要，是因为它直接影响两件事：</p><ul><li>有没有足够多的 warp 可以轮换执行，用来隐藏延迟</li><li>每个线程和每个 block 是否还能拿到足够多的寄存器、<code>shared memory</code> 和局部工作空间</li></ul><p><code>occupancy</code> 就是在描述这个大背景。</p><h3 id="3-2-code-occupancy-code-de-han-yi-he-ju-xian" tabindex="-1" id="3-2-occupancy-的含义和局限">3.2 <code>occupancy</code> 的含义和局限</h3><p>最直白的定义是：</p><blockquote><p><code>occupancy = 活跃 warps 数 / SM 最大可支持 warps 数</code></p></blockquote><p>它本质上反映的是：</p><ul><li>一个 SM 上当前有多少 warp 处于可调度状态</li><li>GPU 有没有足够多的候选 warp 去隐藏等待</li></ul><p>为什么这件事有意义？<br>因为 GPU 的强项不是把单线程做到极致快，而是：</p><ul><li>某个 warp 在等 global memory</li><li>调度器马上切去跑另一个 ready warp</li><li>用足够多的活跃 warp 覆盖掉等待时间</li></ul><p>所以 <code>occupancy</code> 重要，是因为它和延迟隐藏能力直接相关。</p><p>但一定要立刻记住：</p><blockquote><p><code>occupancy</code> 不是性能分数。</p></blockquote><p>它的局限在于：</p><ul><li><code>occupancy</code> 高，不代表访存就一定高效</li><li><code>occupancy</code> 高，不代表寄存器复用就一定好</li><li><code>occupancy</code> 高，不代表指令吞吐、tensor core 利用率、数据复用就一定更强</li></ul><p>所以更准确地说：</p><blockquote><p><code>occupancy</code> 是“隐藏延迟的能力指标”，不是“最终性能指标”。</p></blockquote><h3 id="3-3-wei-shi-yao-code-occupancy-code-tai-di-tong-chang-bu-xing-dan-gao-ye-bu-yi-ding-geng-kuai" tabindex="-1" id="3-3-为什么-occupancy-太低通常不行，但高也不一定更快">3.3 为什么 <code>occupancy</code> 太低通常不行，但高也不一定更快</h3><p>如果 <code>occupancy</code> 很低，常见问题是：</p><ul><li>某些 warp 一旦 stall</li><li>SM 上没有足够多的其他 warp 可以补位</li><li>管线更容易空转</li></ul><p>这时提高 <code>occupancy</code> 往往有帮助。</p><p>但为什么它不是越高越好？<br>因为提高 <code>occupancy</code> 往往不是免费的。你通常要付出某些代价，例如：</p><ul><li>减少每线程寄存器</li><li>减少 <code>shared memory</code> 缓冲</li><li>让 tile 变小</li><li>降低数据复用</li><li>降低单线程或单 block 的工作效率</li></ul><p>于是就会出现一种典型情况：</p><ul><li><code>occupancy</code> 提高了</li><li>但每个线程“变瘦了”</li><li>访存更多了，复用更差了</li><li>总性能反而下降</li></ul><p>所以成熟的判断不是“能不能把 occupancy 拉满”，而是：</p><blockquote><p>现在的瓶颈到底是活跃 warp 不够，还是别的资源已经更关键。</p></blockquote><h3 id="3-4-sm-zi-yuan-ru-he-xian-zhi-bing-fa" tabindex="-1" id="3-4-SM-资源如何限制并发">3.4 SM 资源如何限制并发</h3><p>一个 SM 上能同时驻留多少 block / warp，不是你想开多少就能开多少，而是受多种资源共同限制。</p><p>最常见的限制项包括：</p><ul><li>每个 SM 支持的最大线程数</li><li>每个 SM 支持的最大 warp 数</li><li>每个 SM 支持的最大 block 数</li><li>每个 SM 的寄存器总量</li><li>每个 SM 的 <code>shared memory</code> 总量</li></ul><p>这意味着：</p><ul><li>即使线程上限还没满，只要寄存器不够，新 block 也上不去</li><li>即使寄存器还有剩余，只要 <code>shared memory</code> 不够，也挂不下更多 block</li><li>即使资源看似都还有一点，只要 block slot 用完，也会被卡住</li></ul><p>所以你可以把可驻留 block 数理解成一个“多重门槛共同取最小值”的结果。</p><h3 id="3-5-code-block-size-code-wei-shi-yao-ying-xiang-zi-yuan-li-yong-he-diao-du-li-du" tabindex="-1" id="3-5-block-size-为什么影响资源利用和调度粒度">3.5 <code>block size</code> 为什么影响资源利用和调度粒度</h3><p>很多人把 <code>block size</code> 理解成“每个 block 开多少线程”。这当然没错，但太浅了。</p><p>它更深一层的作用是：</p><ul><li>决定每个 block 有多少 warp</li><li>决定资源按多大粒度被切分给 block</li><li>决定一个 block 自己吃掉多少寄存器和 <code>shared memory</code></li></ul><p>比如：</p><ul><li><code>block size = 128</code>，意味着一个 block 有 4 个 warp</li><li><code>block size = 256</code>，意味着一个 block 有 8 个 warp</li></ul><p>这不只是线程翻倍，而是：</p><ul><li>block 的寄存器需求可能翻倍</li><li>block 的调度粒度变大</li><li>一个 SM 上可同时驻留的 block 数可能下降</li></ul><p>所以 <code>block size</code> 调优本质上是在权衡：</p><ul><li>并行度</li><li>资源切分粒度</li><li>block 内协作需求</li><li>调度灵活性</li></ul><p>这也是为什么 <code>block size</code> 不是越大越好，也不是越小越好。</p><h3 id="3-6-shi-yao-shi-code-register-pressure-code" tabindex="-1" id="3-6-什么是-register-pressure">3.6 什么是 <code>register pressure</code></h3><p>寄存器是按线程分配的。<br>如果一个 kernel 每线程需要很多寄存器，那么：</p><ul><li>单线程局部计算可能更舒服</li><li>但整个 block 的寄存器消耗会很高</li><li>一个 SM 上能并行驻留的线程和 block 就会减少</li></ul><p>这就是 <code>register pressure</code>。</p><p>它的本质可以理解成：</p><blockquote><p>这个 kernel 对寄存器的需求太高，已经开始明显挤压并发资源。</p></blockquote><p>常见来源包括：</p><ul><li>临时变量很多</li><li>live range 很长</li><li>展开过多</li><li>fusion 后中间值变多</li><li>一个线程承担了过多局部状态</li></ul><p>所以寄存器不是越多越好。<br>它和 <code>occupancy</code> 常常天然冲突。</p><h3 id="3-7-shi-yao-shi-code-spilling-code-ta-wei-shi-yao-hui-shang-xing-neng" tabindex="-1" id="3-7-什么是-spilling，它为什么会伤性能">3.7 什么是 <code>spilling</code>，它为什么会伤性能</h3><p>当寄存器压力继续升高，超过编译器或硬件能高效承受的范围时，就会发生 <code>spilling</code>。</p><p>也就是：</p><ul><li>本来想放在寄存器里的数据</li><li>被迫溢出到更慢的内存路径</li></ul><p>这时问题就变成了双重打击：</p><ul><li>并发下降，因为寄存器压力高</li><li>单线程也变慢，因为部分数据已经不在寄存器里了</li></ul><p>所以 <code>spilling</code> 的后果通常很明显：</p><ul><li>更多访问开销</li><li>更差的延迟隐藏</li><li>更差的整体吞吐</li></ul><p>这也是为什么很多“看起来没什么问题”的 kernel，只是因为寄存器数暴涨，就会突然慢很多。</p><h3 id="3-8-code-shared-memory-code-wei-shi-yao-ye-hui-ya-di-code-occupancy-code" tabindex="-1" id="3-8-shared-memory-为什么也会压低-occupancy">3.8 <code>shared memory</code> 为什么也会压低 <code>occupancy</code></h3><p>前面你已经学过，<code>shared memory</code> 能带来：</p><ul><li>数据复用</li><li>访存重排</li><li>block 内协作</li></ul><p>但它也是按 block 分配的。<br>这意味着一个 block 用得越多，一个 SM 上能同时挂的 block 数通常就越少。</p><p>所以 <code>shared memory</code> 的好处和代价永远是一起出现的：</p><p>好处：</p><ul><li>更少的 global memory 访问</li><li>更高的数据复用</li><li>更规则的数据流</li></ul><p>代价：</p><ul><li>可能降低并发 block 数</li><li>降低 <code>occupancy</code></li><li>增加同步和管理开销</li></ul><p>这再次说明：</p><blockquote><p>所有优化本质上都是 trade-off，不存在白拿的性能。</p></blockquote><h3 id="3-9-launch-can-shu-diao-you-de-ping-heng-luo-ji" tabindex="-1" id="3-9-Launch-参数调优的平衡逻辑">3.9 Launch 参数调优的平衡逻辑</h3><p>现在把前面几件事收束起来。</p><p>你调 launch 参数时，真正做的不是“找一个固定最优数字”，而是在平衡下面这些约束：</p><ul><li>是否有足够多的活跃 warp 去隐藏延迟</li><li>每线程寄存器够不够</li><li>block 的 <code>shared memory</code> 开销大不大</li><li>block 内协作是否需要更大粒度</li><li>tile 大小是否还能维持足够数据复用</li><li>问题规模是否足够大，能否填满设备</li></ul><p>所以 launch 调优的成熟理解应该是：</p><blockquote><p>不是单独追求更高 occupancy，而是在并发能力、数据复用、同步结构和资源消耗之间找到平衡点。</p></blockquote><h3 id="3-10-block-size-dao-di-zen-yao-xuan" tabindex="-1" id="3-10-block-size-到底怎么选">3.10 block size 到底怎么选</h3><p>没有万能答案，但有一套很实用的起点。</p><p>通常先从 warp 的整数倍开始试，比如：</p><ul><li><code>64</code></li><li><code>128</code></li><li><code>256</code></li><li><code>512</code></li></ul><p>为什么这样做？</p><ul><li>避免大量不完整 warp</li><li>更贴近硬件调度粒度</li><li>更容易得到合理的初始 <code>occupancy</code></li></ul><p>很多普通 kernel，默认会从 <code>128</code> 或 <code>256</code> 作为 baseline 开始。<br>但如果 kernel：</p><ul><li><code>shared memory</code> 很重</li><li>寄存器很多</li><li>需要复杂 block 内协作</li><li>tile 很大</li></ul><p>那 block size 往往要更保守。</p><p>所以真正的判断方式永远是：</p><ul><li>先拿一个合理 baseline</li><li>再用 profiler 看资源使用和瓶颈</li><li>再决定是该追更多并发，还是保更强复用</li></ul><h3 id="3-11-wei-shi-yao-gao-xing-neng-kernel-wang-wang-bu-zhui-zui-gao-code-occupancy-code" tabindex="-1" id="3-11-为什么高性能-kernel-往往不追最高-occupancy">3.11 为什么高性能 kernel 往往不追最高 <code>occupancy</code></h3><p>这点在 GEMM、attention、fused kernel 里特别常见。</p><p>这类 kernel 通常会刻意使用：</p><ul><li>更多寄存器</li><li>更多 <code>shared memory</code></li><li>更复杂的 tile</li><li>更重的 pipeline</li></ul><p>原因是它们追求的不是“挂更多线程”，而是：</p><blockquote><p>让每个 block、每个 warp 干得更值。</p></blockquote><p>这时即使 <code>occupancy</code> 不是最高，性能也可能非常好，因为：</p><ul><li>数据复用更强</li><li>global memory 流量更少</li><li>单个 warp 的有效工作量更大</li></ul><p>所以以后看到高性能 kernel 的 <code>occupancy</code> 没到 100%，不要本能地认为它有问题。先看瓶颈是不是本来就不在这里。</p><h3 id="3-12-yi-ge-shi-yong-de-diao-you-liu-cheng" tabindex="-1" id="3-12-一个实用的调优流程">3.12 一个实用的调优流程</h3><p>这篇最实用的部分，是把调优思路固定下来。</p><p>第一步，先拿 baseline：</p><ul><li>当前 block size</li><li><code>registers / thread</code></li><li><code>shared memory / block</code></li><li>理论和实际 <code>occupancy</code></li><li>kernel 时间</li></ul><p>第二步，判断瓶颈：</p><ul><li>如果明显是带宽瓶颈，不要只盯着 <code>occupancy</code></li><li>如果 stall 很重且活跃 warp 不够，再重点看并发问题</li></ul><p>第三步，扫几个 block size：</p><ul><li><code>64</code></li><li><code>128</code></li><li><code>256</code></li><li><code>512</code></li></ul><p>观察：</p><ul><li>时间怎么变</li><li><code>occupancy</code> 怎么变</li><li>寄存器和 <code>shared memory</code> 开销怎么变</li></ul><p>第四步，看寄存器压力和 spilling：</p><ul><li>是否临时变量太多</li><li>是否 live range 太长</li><li>是否某些展开或 fusion 把寄存器压爆了</li></ul><p>第五步，再看 <code>shared memory</code> 是否值得：</p><ul><li>它换来了多少复用</li><li>降低了多少 global memory traffic</li><li>是否值得为此付出更低并发</li></ul><p>这套流程比“拍脑袋改 256”稳得多。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常强，因为推理 kernel 经常在“更高并发”和“更强复用”之间拉扯。</p><p>几个典型场景：</p><ul><li>fused kernel：常减少 global memory traffic，但也会显著增加寄存器压力</li><li>GEMM / attention：经常依赖更大的 tile、更多 <code>shared memory</code> 和寄存器片段，不一定追求最高 <code>occupancy</code></li><li>decode 小 batch 场景：工作量本来就偏小，更容易出现设备填不满的问题</li><li>FlashAttention 一类 IO-aware 实现：经常是在主动接受某些资源压力，以换取更好的数据流</li></ul><p>所以推理优化里常见的真实问题不是：</p><ul><li>“怎样把 occupancy 拉到最高”</li></ul><p>而是：</p><ul><li>现在更缺的是活跃 warp，还是更缺数据复用</li><li>fusion 带来的寄存器成本是否值得</li><li>当前 kernel 更像 <code>memory-bound</code> 还是 <code>compute-bound</code></li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>occupancy</code> 越高越好。不是，它只是延迟隐藏能力指标。</li><li>看到 <code>occupancy</code> 低就一定要降寄存器。不是，可能会破坏复用和单线程效率。</li><li><code>block size</code> 固定写 <code>256</code> 就够了。不是，不同 kernel 的最优配置可能完全不同。</li><li>只要线程数很多就一定快。不是，还要看寄存器、<code>shared memory</code>、访存和同步结构。</li><li><code>spilling</code> 只是编译器小问题。不是，它经常直接带来明显性能损失。</li><li>launch 调优就是试几个数字。不是，真正要调的是整个资源平衡结构。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li><code>occupancy</code> 的定义是什么，它为什么重要？</li><li>为什么 <code>occupancy</code> 不等于最终性能？</li><li>哪些资源会限制一个 SM 上的并发？</li><li><code>block size</code> 为什么会影响资源利用和调度粒度？</li><li>什么是 <code>register pressure</code>，它为什么会压低性能？</li><li><code>spilling</code> 到底意味着什么，为什么会伤性能？</li><li>为什么很多高性能 GEMM / attention kernel 不追求最高 <code>occupancy</code>？</li><li>Launch 参数调优为什么本质上是在平衡多个约束？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/416851d9/">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</a></li><li>下一篇：<a href="/posts/481fda56/">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</a></li><li>延伸阅读：<a href="/posts/edff4eb0/">GPU系统拆解-09-Profiling 与性能定位：先找到瓶颈，再谈优化</a></li><li>延伸阅读：<a href="/posts/803b94d6/">CUDA实践-13-性能分析屋顶线与占用率</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-08-occupancy-ji-cun-qi-ya-li-yu-launch-can-shu-diao-you-bu-shi-ba-zhan-yong-lu-la-man&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="Occupancy" scheme="https://smarter.xin/tags/occupancy-en/"/>
    
    <category term="占用率" scheme="https://smarter.xin/tags/occupancy/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</title>
    <link href="https://smarter.xin/posts/416851d9/"/>
    <id>https://smarter.xin/posts/416851d9/</id>
    <published>2026-04-06T03:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-07-tong-bu-yuan-zi-cao-zuo-yu-nei-cun-yi-zhi-xing-bing-fa-zheng-que-xing-zen-yao-bao-zheng" tabindex="-1">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</h1><blockquote><p>本文是「CUDA系统拆解」系列第 07 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/b8b486f5/">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</a><br>下一篇：<a href="/posts/cae09a20/">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>为什么并行程序比串行程序更容易出错。</li><li>同步在 CUDA 里到底在保护什么。</li><li><code>__syncthreads()</code> 真正的作用是什么，为什么它只在 block 内有效。</li><li>原子操作解决了什么问题，为什么它又常常带来性能代价。</li><li>什么叫可见性和顺序性，为什么这些问题不能只靠“感觉”判断。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>并行程序更难，不是因为线程多，而是因为多个线程会同时读写共享状态，导致协调、冲突、可见性问题。</li><li><code>__syncthreads()</code> 是 block 级 barrier，它的意义不只是“等一下”，而是建立 block 内的阶段边界。</li><li><code>__syncthreads()</code> 只在 block 内有效，不能拿它做跨 block 的同步。</li><li>原子操作解决的是共享地址上的并发写冲突，但通常会把热点更新串行化，所以正确不等于高性能。</li><li>block 独立是 CUDA 的重要设计，这让调度和扩展更灵活，但也意味着很多全局协作要拆成多阶段。</li><li>内存一致性和顺序性要单独理解，因为“某线程已经写了”不等于“其他线程现在一定能按你期望的顺序看到”。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-bing-xing-cheng-xu-geng-nan" tabindex="-1" id="3-1-为什么并行程序更难">3.1 为什么并行程序更难</h3><p>串行程序里，你通常有一种很强的默认直觉：</p><ul><li>先执行 A</li><li>再执行 B</li><li>再执行 C</li></ul><p>所以只要代码顺序写出来了，你就会自然相信后面的语句能看到前面的结果。</p><p>并行程序不是这样。到了 CUDA 里：</p><ul><li>很多线程同时执行</li><li>不同线程推进速度未必一样</li><li>不同 warp 之间没有天然顺序</li><li>不同 block 之间更没有默认同步</li></ul><p>于是问题就变成了：</p><blockquote><p>当多个线程同时读写同一份数据时，谁先写、谁后读、谁能看到什么，不再是天然确定的。</p></blockquote><p>所以并行程序真正难的地方，不是把工作分出去，而是把共享状态管对。</p><h3 id="3-2-bing-xing-zheng-que-xing-li-zui-he-xin-de-san-lei-wen-ti" tabindex="-1" id="3-2-并行正确性里最核心的三类问题">3.2 并行正确性里最核心的三类问题</h3><p>这一篇最重要的总图可以压成三类。</p><p>第一类，协调问题。<br>也就是线程之间要不要在某个阶段对齐。<br>典型情况是：</p><ul><li>有的线程还没写完</li><li>另一些线程已经准备开始读</li></ul><p>第二类，冲突问题。<br>也就是多个线程同时更新同一个地址，导致结果丢失或不确定。</p><p>第三类，可见性和顺序问题。<br>也就是一个线程已经写了数据，但另外的线程什么时候、以什么顺序能可靠看到这次写入。</p><p>同步、atomic 和一致性，本质上都在处理这三类问题。</p><h3 id="3-3-shi-yao-shi-race-condition" tabindex="-1" id="3-3-什么是-race-condition">3.3 什么是 race condition</h3><p><code>race condition</code> 不是说“大家都在竞争 GPU 资源”，而是：</p><blockquote><p>程序结果依赖于多个线程的执行先后顺序，而这个顺序本身又不受控制。</p></blockquote><p>最经典的例子就是很多线程一起做：</p><ul><li>读一个共享变量</li><li>改它</li><li>再写回去</li></ul><p>如果两个线程都先读到旧值，再分别写回，就会发生“丢更新”。</p><p>所以你要建立一个很稳的判断：</p><blockquote><p>只要多个线程可能同时写同一个地址，就要立刻警惕 race condition。</p></blockquote><h3 id="3-4-tong-bu-dao-di-zai-jie-jue-shi-yao-wen-ti" tabindex="-1" id="3-4-同步到底在解决什么问题">3.4 同步到底在解决什么问题</h3><p>很多人把同步理解成“让线程停一下，等别人”。<br>这只是表面。</p><p>同步更本质的作用有两层：</p><ul><li>让一组线程在时间上对齐，形成阶段边界</li><li>让这个阶段边界之前的写入，在边界之后对相关线程可可靠使用</li></ul><p>也就是说，同步不只是控制时间，更是在建立数据依赖。</p><p>这就是为什么同步通常出现在：</p><ul><li>shared memory 协作</li><li>分阶段归约</li><li>tile 搬运之后</li><li>多轮迭代中间</li></ul><h3 id="3-5-code-syncthreads-code-zhen-zheng-shi-shi-yao-yi-si" tabindex="-1" id="3-5-syncthreads-真正是什么意思">3.5 <code>__syncthreads()</code> 真正是什么意思</h3><p><code>__syncthreads()</code> 是 CUDA 里最重要的 block 级 barrier。</p><p>它的直觉可以理解成：</p><blockquote><p>同一个 block 内的线程，都必须先走到这里，之后才能继续往后执行。</p></blockquote><p>但更重要的是它的语义含义：</p><ul><li>它建立了 block 内的阶段边界</li><li>让 barrier 前的 shared memory 写入，可以在 barrier 后被同 block 线程可靠消费</li></ul><p>所以 <code>__syncthreads()</code> 最典型的使用场景，不是“为了好看加一个同步”，而是：</p><ul><li>先把一块数据搬进 shared memory</li><li>同步</li><li>再让别的线程读取和复用</li></ul><h3 id="3-6-wei-shi-yao-code-syncthreads-code-zhi-zai-block-nei-you-xiao" tabindex="-1" id="3-6-为什么-syncthreads-只在-block-内有效">3.6 为什么 <code>__syncthreads()</code> 只在 block 内有效</h3><p>这是这篇必须吃透的点。</p><p><code>__syncthreads()</code> 只能同步：</p><ul><li>同一个 block 内的线程</li></ul><p>它不能同步：</p><ul><li>不同 block</li><li>整个 grid</li><li>跨 kernel 的阶段</li></ul><p>原因不是 API 做得不够强，而是 CUDA 的设计就是这样：</p><blockquote><p>block 是独立调度单位。</p></blockquote><p>不同 block：</p><ul><li>可能在不同 SM 上</li><li>可能不是同时运行</li><li>甚至可能前后分批被调度</li></ul><p>如果 CUDA 默认支持轻松的跨 block barrier，调度成本和硬件复杂度都会高很多。</p><p>所以 block 独立不是“限制”，而是 CUDA 可扩展和可调度的基础。</p><h3 id="3-7-wei-shi-yao-block-du-li-shi-zhong-yao-she-ji" tabindex="-1" id="3-7-为什么-block-独立是重要设计">3.7 为什么 block 独立是重要设计</h3><p>block 独立至少带来三件好事：</p><ul><li>调度更灵活，SM 可以自由接活</li><li>同一 kernel 更容易适配不同规模 GPU</li><li>编程模型更清晰，天然鼓励“局部协作、全局分阶段”</li></ul><p>这也是为什么很多 CUDA 算法的结构都长这样：</p><ol><li>block 内先做局部处理</li><li>block 内同步</li><li>产出局部结果</li><li>用下一阶段 kernel 再做更高层合并</li></ol><p>所以你以后看到 reduction、prefix sum、softmax 分阶段实现时，要知道这不是“写得麻烦”，而是在适配 CUDA 的协作边界。</p><h3 id="3-8-code-syncthreads-code-de-yi-ge-dian-xing-xian-jing" tabindex="-1" id="3-8-syncthreads-的一个典型陷阱">3.8 <code>__syncthreads()</code> 的一个典型陷阱</h3><p><code>__syncthreads()</code> 不是随便塞进分支里就行。</p><p>最危险的情况是：</p><ul><li>block 内只有一部分线程会执行到 barrier</li><li>另一部分线程永远到不了</li></ul><p>这样就会产生死锁或未定义行为。</p><p>所以非常重要的一条规则是：</p><blockquote><p>对同一个 block 来说，凡是要到达 <code>__syncthreads()</code> 的控制流，必须保证相关线程一致地到达。</p></blockquote><p>这一点在写条件分支、边界处理和循环时尤其容易踩坑。</p><h3 id="3-9-yuan-zi-cao-zuo-dao-di-jie-jue-shi-yao-wen-ti" tabindex="-1" id="3-9-原子操作到底解决什么问题">3.9 原子操作到底解决什么问题</h3><p>同步解决的是“何时协作”。<br>原子操作解决的是“同时更新同一个地址怎么办”。</p><p>例如一个共享计数器、一个全局和、一个直方图桶，如果很多线程都对同一位置做读改写，那么普通写法会丢更新。</p><p>原子操作的含义就是：</p><blockquote><p>对某个共享地址上的读改写过程，提供不可分割的并发保护。</p></blockquote><p>所以 <code>atomicAdd</code>、<code>atomicMax</code> 这类原语的本质，不是“更高级的加法”，而是：</p><ul><li>正确处理共享热点更新</li><li>防止多个线程互相覆盖结果</li></ul><h3 id="3-10-wei-shi-yao-yuan-zi-cao-zuo-dui-dan-chang-chang-man" tabindex="-1" id="3-10-为什么原子操作对，但常常慢">3.10 为什么原子操作对，但常常慢</h3><p>原子操作能保证正确，但它常常会引入一个新的现实：</p><blockquote><p>热点地址上的更新会变得高度串行化。</p></blockquote><p>如果几千个线程都在抢同一个地址做 <code>atomicAdd</code>，那就意味着：</p><ul><li>逻辑上结果是对的</li><li>但硬件上大家都在争同一个共享点</li></ul><p>于是吞吐会明显下降。</p><p>所以原子操作的成熟理解应该是：</p><ul><li>它是正确性工具</li><li>不是默认高性能方案</li></ul><p>高性能实现通常会想办法减少 atomic 的次数和冲突范围。</p><h3 id="3-11-wei-shi-yao-chang-jian-you-hua-shi-xian-ju-bu-zai-quan-ju" tabindex="-1" id="3-11-为什么常见优化是“先局部，再全局”">3.11 为什么常见优化是“先局部，再全局”</h3><p>这条思路你以后会反复看到。</p><p>最差的做法通常是：</p><ul><li>每个线程直接对同一个全局地址做 atomic</li></ul><p>更好的做法通常是：</p><ol><li>warp 内先聚合</li><li>block 内再聚合</li><li>最后只让少量线程做全局 atomic</li></ol><p>这样做的意义非常直接：</p><ul><li>降低全局热点</li><li>减少 atomic 次数</li><li>让大部分协作先在更近、更便宜的层级完成</li></ul><p>所以很多高性能 reduction、softmax、统计类 kernel 的主线，本质上都是：</p><blockquote><p>先局部合并，再少量全局提交。</p></blockquote><h3 id="3-12-nei-cun-yi-zhi-xing-he-shun-xu-xing-dao-di-zai-shuo-shi-yao" tabindex="-1" id="3-12-内存一致性和顺序性到底在说什么">3.12 内存一致性和顺序性到底在说什么</h3><p>这个词听起来抽象，但你可以先把它翻译成一个非常具体的问题：</p><blockquote><p>一个线程写了数据，别的线程什么时候能看到？看到时顺序是不是你想要的那个顺序？</p></blockquote><p>这里要特别警惕两个误解：</p><ul><li>写了，不等于别的线程立刻可靠可见</li><li>看到了，不等于顺序就是你以为的顺序</li></ul><p>所以一致性和顺序性要单独理解。<br>它们不是“补充知识”，而是并行正确性的基础。</p><p>对当前阶段来说，你至少要建立下面这个直觉：</p><ul><li>shared memory 协作通常要靠 barrier 建立“先写后读”的阶段边界</li><li>跨 block 的复杂协作，通常不该在一个 kernel 内硬做</li><li>kernel 边界本身就是很强的全局阶段边界</li></ul><h3 id="3-13-wei-shi-yao-hen-duo-kua-block-xie-zuo-yao-chai-cheng-duo-ge-kernel" tabindex="-1" id="3-13-为什么很多跨-block-协作要拆成多个-kernel">3.13 为什么很多跨 block 协作要拆成多个 kernel</h3><p>这是 CUDA 很重要的工程哲学。</p><p>因为 block 间没有天然 barrier，所以很多算法会故意拆成多阶段：</p><ol><li>第一个 kernel 做局部工作</li><li>kernel 结束，形成全局阶段边界</li><li>第二个 kernel 再读取前一阶段结果</li></ol><p>这种写法的好处是：</p><ul><li>正确性更清晰</li><li>调度更稳定</li><li>更符合 CUDA 的 block 独立模型</li></ul><p>所以以后看到“多 kernel 分阶段实现”，不要第一反应觉得它笨。很多时候，这恰恰是更对的设计。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常直接，因为推理里的很多 kernel 都有局部协作和共享状态问题。</p><p>典型场景包括：</p><ul><li>softmax、LayerNorm、RMSNorm：要做 block 或 warp 内归约</li><li>attention tile 计算：先搬数据、同步，再进行阶段性计算</li><li>统计、计数、buffer slot 分配：经常涉及 atomic</li><li>推理框架里的共享元数据更新：本质上也是并发读写协调问题</li></ul><p>所以做 AI infra / 推理时，你不能只把这些当成“CUDA 基础题”，而要知道：</p><ul><li>barrier 放错了会直接错</li><li>atomic 用多了会直接拖性能</li><li>block 边界理解错了，算法结构就会从根上出问题</li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>加了 <code>__syncthreads()</code> 就一定安全。不是，它只解决 block 内 barrier 问题。</li><li><code>__syncthreads()</code> 可以拿来同步整个 kernel。不是，它不跨 block。</li><li>atomic 能解决所有并发问题。不是，它只解决特定共享更新的正确性问题。</li><li>为了保险，多加同步总没错。不是，过多 barrier 会拖慢执行，而且不一定真解决逻辑问题。</li><li>结果偶尔对，说明写法大体没问题。不是，并发 bug 最危险的就是不稳定和偶发。</li><li>多 kernel 分阶段实现说明实现不高级。不是，这往往正是在适配 CUDA 的同步边界。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么并行程序比串行程序更难？</li><li>同步在 CUDA 里到底在保护什么？</li><li><code>__syncthreads()</code> 的真实作用是什么，为什么它只在 block 内有效？</li><li>为什么 block 独立是 CUDA 的重要设计？</li><li>原子操作到底解决什么问题，为什么它常常会带来性能代价？</li><li>为什么高性能实现通常更喜欢“先局部聚合，再少量全局提交”？</li><li>内存一致性和顺序性为什么要单独理解？</li><li>为什么很多跨 block 协作更适合拆成多个 kernel 分阶段完成？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/b8b486f5/">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</a></li><li>下一篇：<a href="/posts/cae09a20/">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</a></li><li>延伸阅读：<a href="/posts/92dc1028/">CUDA C编程权威指南：导读</a></li><li>延伸阅读：<a href="/posts/a1e20e80/">CUDA实践-12-标准库与工程实践</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-07-tong-bu-yuan-zi-cao-zuo-yu-nei-cun-yi-zhi-xing-bing-fa-zheng-que-xing-zen-yao-bao-zheng&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="原子操作" scheme="https://smarter.xin/tags/atomic-operations/"/>
    
    <category term="并发" scheme="https://smarter.xin/tags/concurrency/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</title>
    <link href="https://smarter.xin/posts/b8b486f5/"/>
    <id>https://smarter.xin/posts/b8b486f5/</id>
    <published>2026-04-06T03:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-06-gong-xiang-nei-cun-yu-coalescing-fang-cun-you-hua-xian-zhua-na-ji-ge-zhua-shou" tabindex="-1">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</h1><blockquote><p>本文是「CUDA系统拆解」系列第 06 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/8f25342e/">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</a><br>下一篇：<a href="/posts/416851d9/">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>为什么很多 CUDA kernel 明明计算不复杂，却仍然跑不快。</li><li>什么是 <code>coalescing</code>，坏的访问模式为什么会明显变慢。</li><li><code>shared memory</code> 真正解决的到底是什么问题。</li><li>为什么 <code>shared memory</code> 不只是“更快读”，而是“更好组织数据流”。</li><li>什么是 <code>bank conflict</code>，为什么 <code>shared memory</code> 自己也会出问题。</li><li>为什么转置、tiling、GEMM、attention 这类高性能实现都绕不开这些概念。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>很多 kernel 的主要瓶颈不是算术运算，而是 <code>global memory</code> 访问方式不够高效。</li><li><code>coalescing</code> 的本质，是让一个 warp 的线程访问尽量落在规则、连续、容易合并的地址上。</li><li>差的访问模式通常不是“读错了”，而是让一次内存事务的有效利用率变低了。</li><li><code>shared memory</code> 的核心价值有两个：数据复用，以及访存重排。</li><li><code>shared memory</code> 不是自动加速器；如果没有复用价值、没有重排价值，反而可能增加搬运和同步成本。</li><li><code>bank conflict</code> 说明 <code>shared memory</code> 也有自己的硬件访问约束。</li><li>转置和 tiling 是最典型的访存优化案例，因为它们把“差的全局访问”改造成“更好的数据流”。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-code-global-memory-code-chang-chang-hui-cheng-wei-ping-jing" tabindex="-1" id="3-1-为什么-global-memory-常常会成为瓶颈">3.1 为什么 <code>global memory</code> 常常会成为瓶颈</h3><p>前一篇已经讲过，很多 CUDA 程序首先是 <code>memory-bound</code>。<br>到了这一篇，要把这件事再往前推进一步：</p><blockquote><p>真正的问题通常不是“用了 global memory”，而是“怎么用 global memory”。</p></blockquote><p><code>global memory</code> 的现实地位很尴尬：</p><ul><li>它容量大，离不开</li><li>但它远、慢、带宽宝贵</li></ul><p>所以高性能 CUDA 的核心不是逃离 <code>global memory</code>，而是：</p><ul><li>尽量减少不必要的访问</li><li>访问时尽量让硬件更容易高效服务</li><li>能复用的数据不要反复从远处重拿</li></ul><p>很多 kernel 慢，并不是因为 ALU 没能力，而是因为线程在等数据。</p><h3 id="3-2-shi-yao-shi-code-coalescing-code" tabindex="-1" id="3-2-什么是-coalescing">3.2 什么是 <code>coalescing</code></h3><p><code>coalescing</code> 可以先这样理解：</p><blockquote><p>同一个 warp 内线程访问 <code>global memory</code> 时，如果地址分布足够规则、连续、对齐，硬件就更容易把这些访问合并成更少、更高效的内存事务。</p></blockquote><p>这件事的关键不是“看起来连续很美观”，而是：</p><ul><li>一次内存请求搬来的数据，有多少真的被当前 warp 用到了</li><li>硬件要为这次访问付出多少事务开销</li></ul><p>所以 <code>coalescing</code> 的本质是：</p><blockquote><p>提高一个 warp 访问 <code>global memory</code> 时的有效利用率。</p></blockquote><h3 id="3-3-hao-fang-wen-mo-shi-he-pi-fang-wen-mo-shi-dao-di-chai-zai-na" tabindex="-1" id="3-3-好访问模式和坏访问模式到底差在哪">3.3 好访问模式和坏访问模式到底差在哪</h3><p>最典型的好模式是：</p><ul><li>相邻线程访问相邻元素</li><li>访问方向和数据在内存中的主存储方向一致</li></ul><p>例如一个 warp 里：</p><ul><li>thread 0 访问第 0 个元素</li><li>thread 1 访问第 1 个元素</li><li>thread 2 访问第 2 个元素</li></ul><p>这通常对硬件很友好。</p><p>典型的坏模式则包括：</p><ul><li>stride 很大的访问</li><li>相邻线程访问彼此很远的数据</li><li>行主序数据却按列方向稀疏读取</li><li>地址乱跳，导致一次事务搬来的很多字节没人用</li></ul><p>所以坏模式为什么慢，不是因为“逻辑不正确”，而是因为：</p><blockquote><p>线程访问太散，硬件需要做更多事务，而每次事务的有效载荷又不高。</p></blockquote><h3 id="3-4-wei-shi-yao-zhuan-zhi-lie-fang-wen-stride-fang-wen-zong-rong-yi-chu-wen-ti" tabindex="-1" id="3-4-为什么转置、列访问、stride-访问总容易出问题">3.4 为什么转置、列访问、stride 访问总容易出问题</h3><p>很多二维数据默认是按行连续存的。<br>这意味着：</p><ul><li>按行读，往往更自然</li><li>按列读，往往更容易出现稀疏访问</li></ul><p>这类问题的共同点是：</p><ul><li>算法逻辑本身没问题</li><li>但线程和数据布局之间的方向不匹配</li></ul><p>于是就会出现一种典型现象：</p><ul><li>读可能是好的</li><li>写很差</li></ul><p>或者反过来：</p><ul><li>写比较顺</li><li>读却很散</li></ul><p>这就是为什么转置会成为 CUDA 教学里的经典案例。它几乎把“算法很简单，但访存模式很差”这件事浓缩到了极致。</p><h3 id="3-5-code-shared-memory-code-wei-shi-yao-cun-zai" tabindex="-1" id="3-5-shared-memory-为什么存在">3.5 <code>shared memory</code> 为什么存在</h3><p>如果只看表面，<code>shared memory</code> 只是片上更快的一块内存。<br>但真正重要的理解是：</p><blockquote><p><code>shared memory</code> 是 block 内线程共同使用的可控 scratchpad，用来主动组织数据流。</p></blockquote><p>它最核心的两个价值是：</p><p>第一，数据复用。<br>如果一块数据会被 block 内多个线程多次用到，那么先从 <code>global memory</code> 读一次到 <code>shared memory</code>，再在片上反复消费，通常比让每个线程都去远处重读更划算。</p><p>第二，访存重排。<br>很多时候，算法需要的访问顺序，对 <code>global memory</code> 并不友好。这时就可以：</p><ol><li>先以更好的方式把数据读到 <code>shared memory</code></li><li>在 <code>shared memory</code> 中改变布局或访问方向</li><li>再以更好的方式继续算，或者写回 <code>global memory</code></li></ol><p>这就是为什么 <code>shared memory</code> 不只是“更快读”，而是“更好组织数据流”。</p><h3 id="3-6-code-shared-memory-code-bu-shi-shi-yao" tabindex="-1" id="3-6-shared-memory-不是什么">3.6 <code>shared memory</code> 不是什么</h3><p>这点也很重要。<code>shared memory</code> 不是：</p><ul><li>自动提速按钮</li><li>默认一定要上的优化</li><li>单纯替代 cache 的东西</li></ul><p>如果一个 kernel：</p><ul><li>没有明显数据复用</li><li>不需要访存重排</li><li>用了 <code>shared memory</code> 还得多搬一次数据、多做一次同步</li></ul><p>那它反而可能更慢。</p><p>所以成熟的判断方式不是“能不能用 shared”，而是：</p><ul><li>这里有没有复用关系</li><li>这里有没有重排收益</li><li>这里的同步和共享开销值不值得</li></ul><h3 id="3-7-code-bank-conflict-code-de-ji-ben-zhi-jue" tabindex="-1" id="3-7-bank-conflict-的基本直觉">3.7 <code>bank conflict</code> 的基本直觉</h3><p>很多人以为解决了 <code>global memory</code> 问题，事情就结束了。<br>还没结束，因为 <code>shared memory</code> 自己也有访问结构。</p><p>可以先把 <code>bank conflict</code> 理解成：</p><blockquote><p>一个 warp 内多个线程访问 <code>shared memory</code> 时，如果它们不幸挤到了同一条内部通路上，原本能并行完成的访问就会部分串行化。</p></blockquote><p>这就是 <code>bank conflict</code>。</p><p>最直观的比喻是：</p><ul><li><code>shared memory</code> 像有很多服务窗口</li><li>如果线程分散去不同窗口，整体吞吐很好</li><li>如果很多线程同时挤同一个窗口，就要排队</li></ul><p>所以 <code>shared memory</code> 虽然快，但也不是“无脑快”。<br>它同样要求访问模式对硬件友好。</p><h3 id="3-8-wei-shi-yao-er-wei-tile-jing-chang-hui-yin-chu-code-bank-conflict-code" tabindex="-1" id="3-8-为什么二维-tile-经常会引出-bank-conflict">3.8 为什么二维 tile 经常会引出 <code>bank conflict</code></h3><p>二维 tile 是最容易暴露这个问题的地方。</p><p>原因很简单：</p><ul><li>按一个方向访问时，地址排布可能很好</li><li>换一个方向访问时，步长突然变得很整齐</li><li>这种“整齐”有时恰好会让很多线程撞到相同 bank 上</li></ul><p>这也是为什么在矩阵转置里，经常会看到一个非常经典的修复方式：</p><ul><li>原本是 <code>32 x 32</code></li><li>改成 <code>32 x 33</code></li></ul><p>这个 <code>+1</code> 的本质不是魔法，而是：</p><blockquote><p>改变步长，打散原来不理想的 bank 映射。</p></blockquote><p>所以你真正该记住的不是“答案是加 1”，而是：</p><ul><li>数据布局的 stride 会影响 bank 映射</li><li>bank 映射又会影响 warp 访问 <code>shared memory</code> 的吞吐</li></ul><h3 id="3-9-wei-shi-yao-zhuan-zhi-shi-zui-dian-xing-de-fang-cun-you-hua-an-li" tabindex="-1" id="3-9-为什么转置是最典型的访存优化案例">3.9 为什么转置是最典型的访存优化案例</h3><p>转置之所以经典，是因为它同时暴露了两类问题：</p><ul><li><code>global memory</code> 的 coalescing</li><li><code>shared memory</code> 的 bank conflict</li></ul><p>一个常见的优化思路是：</p><ol><li>先按对 <code>global memory</code> 友好的方向，把一个 tile 读进 <code>shared memory</code></li><li>在 <code>shared memory</code> 中完成局部重排</li><li>再按对 <code>global memory</code> 友好的方向写回</li></ol><p>如果只做到这里，你解决的是全局访存问题。<br>如果你还处理了 padding，避免了 <code>shared memory</code> 内部冲突，那这条数据流才算真正顺了。</p><p>所以转置不是一个孤立技巧，而是一个完整范式：</p><blockquote><p>用片上 staging，把坏的全局访问模式改造成更好的全局访问模式。</p></blockquote><h3 id="3-10-tiling-de-ben-zhi-dao-di-shi-shi-yao" tabindex="-1" id="3-10-tiling-的本质到底是什么">3.10 tiling 的本质到底是什么</h3><p>以后你会频繁听到：</p><ul><li>tile</li><li>blocking</li><li>tiled GEMM</li><li>block tiling</li></ul><p>这些词本质都在说同一件事：</p><blockquote><p>不直接对整块大数据做生硬操作，而是切出一小块一小块，在更近的地方反复使用。</p></blockquote><p>为什么这样做？</p><ul><li>大数据放在 <code>global memory</code>，远且慢</li><li>把当前 block 需要的一块搬到 <code>shared memory</code></li><li>再让线程把最热点的数据留在 <code>register</code></li></ul><p>于是数据流就变成了：</p><p><code>global memory -&gt; shared memory -&gt; register -&gt; 计算 -&gt; global memory</code></p><p>这条链几乎就是 CUDA 高性能 kernel 的标准骨架。</p><h3 id="3-11-wei-shi-yao-shuo-code-shared-memory-code-de-jie-zhi-shi-fu-yong-zhong-pai" tabindex="-1" id="3-11-为什么说-shared-memory-的价值是“复用-重排”">3.11 为什么说 <code>shared memory</code> 的价值是“复用 + 重排”</h3><p>把前面内容收束一下，<code>shared memory</code> 的核心作用其实就是两种：</p><p>复用</p><ul><li>一次从 <code>global memory</code> 读进来</li><li>多个线程、多轮计算反复使用</li></ul><p>重排</p><ul><li><code>global memory</code> 上不好读/写</li><li>先按更友好的方式搬到 <code>shared memory</code></li><li>再在片上改变布局和访问方向</li></ul><p>这两件事共同指向一个目标：</p><blockquote><p>减少慢速路径上的无效流量，让每次远距离访问都更值。</p></blockquote><h3 id="3-12-wei-shi-yao-zhe-xie-wen-ti-hui-zhi-jie-jue-ding-hou-xu-suan-zi-xing-neng" tabindex="-1" id="3-12-为什么这些问题会直接决定后续算子性能">3.12 为什么这些问题会直接决定后续算子性能</h3><p>到了这里，你应该能把很多后续话题接起来了：</p><ul><li>GEMM 为什么依赖 tiling：因为它有强数据复用</li><li>transpose 为什么总讲 <code>shared memory</code>：因为它天然需要访存重排</li><li>fusion 为什么常能提速：因为减少了中间结果写回 <code>global memory</code> 再读回的次数</li><li>FlashAttention 为什么强调 IO-aware：因为它本质上是在重写数据流，减少慢速内存往返</li></ul><p>所以这一篇不是“访存技巧合集”，而是在建立一种更通用的判断框架：</p><ul><li>访问顺不顺</li><li>数据复不复用</li><li>重排值不值</li><li>慢速路径上的流量是不是太多</li></ul><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常直接，因为推理里的大量算子，本质上都在和数据流打交道。</p><p>几个最典型的例子：</p><ul><li>GEMM：核心是 tile 化和复用，把数据尽可能留在更近的层级</li><li>attention：既要考虑访存模式，又要考虑中间结果是否必须落回 <code>global memory</code></li><li>LayerNorm、RMSNorm、softmax：常常更接近 <code>memory-bound</code>，非常依赖访存组织</li><li>layout transform、transpose、KV cache 访问：本质上都在问访问方向和局部性是否友好</li></ul><p>所以做推理优化时，真正关键的问题往往不是“这个公式怎么写”，而是：</p><ul><li>一个 warp 在读什么</li><li>这些读写是否 coalesced</li><li>block 内是否值得 staging 到 <code>shared memory</code></li><li>中间结果能不能不落回更慢的层级</li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>coalescing</code> 就是“连续访问”四个字。不是，核心是 warp 级内存事务的有效利用率。</li><li><code>shared memory</code> 只是更快的缓存。不是，它更重要的作用是复用和重排数据流。</li><li>只要用了 <code>shared memory</code> 就会更快。不是，没有复用或重排收益时，反而可能更慢。</li><li>解决了 <code>global memory</code> 访问就够了。不是，<code>shared memory</code> 自己也可能有 <code>bank conflict</code>。</li><li>转置只是一个小例子。不是，它几乎把 coalescing、重排、padding、bank conflict 都串起来了。</li><li>访存优化只对 CUDA 教学样例有用。不是，GEMM、attention、FlashAttention、KV cache 都在用同一套思路。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么 <code>global memory</code> 会成为很多 kernel 的主要瓶颈？</li><li><code>coalescing</code> 的本质是什么，坏访问模式为什么慢？</li><li><code>shared memory</code> 的核心价值为什么是“复用 + 重排”？</li><li>为什么说 <code>shared memory</code> 不只是“更快读”，而是“更好组织数据流”？</li><li><code>bank conflict</code> 的基本直觉是什么？</li><li>为什么转置是理解访存优化的经典案例？</li><li>tiling 到底在解决什么问题？</li><li>如果你在看一个 GEMM 或 attention kernel，你会先从哪些访存问题开始分析？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/8f25342e/">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</a></li><li>下一篇：<a href="/posts/416851d9/">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</a></li><li>延伸阅读：<a href="/posts/63789fbe/">GPU系统拆解-06-内存层次与访存主线：GPU 性能为什么常常卡在数据</a></li><li>延伸阅读：<a href="/posts/5b6f891d/">CUDA实践-10-访存优化与共享内存冲突</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-06-gong-xiang-nei-cun-yu-coalescing-fang-cun-you-hua-xian-zhua-na-ji-ge-zhua-shou&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="共享内存" scheme="https://smarter.xin/tags/shared-memory/"/>
    
    <category term="内存合并" scheme="https://smarter.xin/tags/memory-coalescing/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</title>
    <link href="https://smarter.xin/posts/8f25342e/"/>
    <id>https://smarter.xin/posts/8f25342e/</id>
    <published>2026-04-06T02:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-05-nei-cun-ceng-ji-yu-fang-cun-ben-zhi-xing-neng-ping-jing-wei-shi-yao-chang-qia-zai-shu-ju" tabindex="-1">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</h1><blockquote><p>本文是「CUDA系统拆解」系列第 05 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/fc8443b4/">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</a><br>下一篇：<a href="/posts/b8b486f5/">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>为什么很多 CUDA 程序明明计算不复杂，却还是很慢。</li><li>为什么 CUDA 必须设计成分层内存，而不是“所有内存都一样”。</li><li><code>register</code>、<code>local memory</code>、<code>shared memory</code>、<code>global memory</code> 各自解决什么问题。</li><li>什么叫 <code>register spill</code>，为什么 <code>local memory</code> 往往不是你想象中的“近”。</li><li>为什么理解内存层级，是后面分析 coalescing、shared memory、fusion、GEMM、attention 的前提。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>很多 CUDA kernel 首先是 <code>memory-bound</code>，也就是先受内存访问和数据搬运限制，而不是先受算术能力限制。</li><li>CUDA 内存层级的核心矛盾是：越靠近计算单元，通常越快，但容量越小、成本越高。</li><li><code>register</code> 最快，但线程私有、数量有限；<code>shared memory</code> 适合 block 内协作和数据复用；<code>global memory</code> 容量大，但常常是主要瓶颈。</li><li><code>local memory</code> 的“local”说的是可见性，不代表物理上更近；它经常意味着寄存器不够或线程私有对象太大。</li><li><code>register spill</code> 的本质是本来想放在寄存器里的数据，被迫落到更慢的内存路径。</li><li>真正该建立的不是“哪种内存快”的静态记忆，而是“数据应该怎样在这些层级之间流动”的思维。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-wei-shi-yao-fang-cun-chang-chang-bi-ji-suan-geng-guan-jian" tabindex="-1" id="3-1-为什么访存常常比计算更关键">3.1 为什么访存常常比计算更关键</h3><p>很多人一想到 GPU，会先想到：</p><ul><li>核很多</li><li>并行强</li><li>FLOPS 高</li></ul><p>这些都对，但不够。<br>对 CUDA 来说，更关键的现实是：</p><blockquote><p>算术运算本身未必贵，昂贵的往往是把数据拿过来、等它回来、再把结果写回去。</p></blockquote><p>所以很多 kernel 的真实性能瓶颈并不是“乘加太慢”，而是：</p><ul><li>从 <code>global memory</code> 读得太多</li><li>访问模式不好</li><li>本该复用的数据被反复读取</li><li>中间结果过早写回，再被重新读出</li></ul><p>这就是为什么很多 CUDA 程序首先是 <code>memory-bound</code>，而不是 <code>compute-bound</code>。<br>前者可以先理解成“受数据搬运和带宽限制”，后者则是“受计算单元本身限制”。</p><h3 id="3-2-wei-shi-yao-yi-ding-yao-you-fen-ceng-nei-cun" tabindex="-1" id="3-2-为什么一定要有分层内存">3.2 为什么一定要有分层内存</h3><p>如果理想一点，我们当然希望：</p><ul><li>所有线程都访问同一块大内存</li><li>它像寄存器一样快</li><li>容量还无限大</li></ul><p>现实做不到，因为这里有天然冲突：</p><ul><li>速度</li><li>容量</li><li>带宽</li><li>功耗</li><li>距离计算单元的远近</li></ul><p>通常规律是：</p><ul><li>越靠近计算单元，延迟越低、带宽越高</li><li>但容量越小、成本越高</li></ul><p>所以 CUDA 的内存层级，本质上是在不同层之间做取舍。<br>它不是为了让你背术语，而是为了给你一个更清晰的数据流组织方式。</p><h3 id="3-3-xian-ba-cuda-nei-cun-ceng-ji-zong-tu-jian-li-qi-lai" tabindex="-1" id="3-3-先把-CUDA-内存层级总图建立起来">3.3 先把 CUDA 内存层级总图建立起来</h3><p>初学阶段你最该抓住的是四层：</p><ul><li><code>register</code></li><li><code>local memory</code></li><li><code>shared memory</code></li><li><code>global memory</code></li></ul><p>可以先这样理解：</p><p><code>register</code></p><ul><li>每个线程私有</li><li>最快</li><li>最稀缺</li></ul><p><code>local memory</code></p><ul><li>也是线程私有可见</li><li>但通常不是片上快存储</li><li>常常意味着寄存器放不下了</li></ul><p><code>shared memory</code></p><ul><li>一个 block 内线程共享</li><li>片上</li><li>适合协作和数据复用</li></ul><p><code>global memory</code></p><ul><li>所有线程都能访问</li><li>容量大</li><li>延迟高，带宽宝贵</li></ul><p>除此之外还有 <code>constant memory</code>、<code>texture memory</code> 这类更特殊的路径，但对你当前这条学习线来说，优先级不如前四者高。</p><h3 id="3-4-code-register-code-zui-kuai-dan-zui-xi-que" tabindex="-1" id="3-4-register：最快，但最稀缺">3.4 <code>register</code>：最快，但最稀缺</h3><p><code>register</code> 可以理解成线程手边的临时工作区。</p><p>它最适合放：</p><ul><li>当前线程频繁使用的小变量</li><li>中间结果</li><li>累加值</li></ul><p>为什么它这么重要？因为它体现了一个很核心的优化原则：</p><blockquote><p>能不去更远的内存拿数据，就不要去。</p></blockquote><p>如果数据已经在寄存器里：</p><ul><li>不需要额外同步</li><li>不需要走更慢的内存路径</li><li>单线程局部计算会更顺畅</li></ul><p>但寄存器有两个硬约束：</p><ul><li>它只能线程私有，不能共享</li><li>数量有限，会影响一个 SM 上能同时挂多少线程和多少 warp</li></ul><p>所以寄存器不是越多越好。<br>每线程寄存器占用一旦太高，就会：</p><ul><li>压低并发</li><li>影响 occupancy</li><li>甚至触发 <code>register spill</code></li></ul><h3 id="3-5-code-local-memory-code-he-code-register-spill-code-dao-di-shi-shi-yao" tabindex="-1" id="3-5-local-memory-和-register-spill-到底是什么">3.5 <code>local memory</code> 和 <code>register spill</code> 到底是什么</h3><p>这是最容易被名字误导的地方。</p><p><code>local memory</code> 的 <code>local</code>，说的是：</p><ul><li>数据对线程私有可见</li></ul><p>它不代表：</p><ul><li>物理上就在这个线程附近</li><li>访问很快</li></ul><p>很多时候，<code>local memory</code> 出现的原因是：</p><ul><li>寄存器不够</li><li>线程私有数组太大</li><li>编译器无法把某些对象稳定留在寄存器里</li></ul><p>这时就会发生 <code>register spill</code>。<br>也就是本来想待在寄存器里的数据，被挤到了更慢的路径上。</p><p>所以你要牢牢记住一句话：</p><blockquote><p><code>local memory</code> 是“可见性局部”，不是“性能局部”。</p></blockquote><p>理解这一点很重要，因为后面很多 kernel 变慢，不是因为算法逻辑变复杂了，而是因为寄存器压力太大，变量开始 spill。</p><h3 id="3-6-code-shared-memory-code-wei-shi-yao-ta-shi-cuda-zui-you-dai-biao-xing-de-she-ji-zhi-yi" tabindex="-1" id="3-6-shared-memory：为什么它是-CUDA-最有代表性的设计之一">3.6 <code>shared memory</code>：为什么它是 CUDA 最有代表性的设计之一</h3><p>如果说寄存器是线程自己的口袋，那 <code>shared memory</code> 更像是 block 共用的一张工作台。</p><p>它的关键特性是：</p><ul><li>block 内线程共享</li><li>片上，延迟低、带宽高</li><li>需要程序员显式管理</li></ul><p>它最核心的价值有两个。</p><p>第一，做数据复用。<br>如果一块数据会被一个 block 内很多线程反复用到，那么：</p><ul><li>每个线程都去 <code>global memory</code> 读一遍，会很浪费</li><li>先搬到 <code>shared memory</code>，再复用，通常更划算</li></ul><p>第二，重组访问模式。<br>有时原始数据布局对 <code>global memory</code> 不友好，这时常见做法是：</p><ol><li>先用较好的方式从 <code>global memory</code> 读进来</li><li>放到 <code>shared memory</code></li><li>在 block 内按更合适的方式重排和消费</li></ol><p>所以 <code>shared memory</code> 的本质不是“它更快，所以总要用”，而是：</p><blockquote><p>当存在明显复用或访存重组需求时，它特别有价值。</p></blockquote><h3 id="3-7-wei-shi-yao-code-global-memory-code-chang-chang-shi-ping-jing" tabindex="-1" id="3-7-为什么-global-memory-常常是瓶颈">3.7 为什么 <code>global memory</code> 常常是瓶颈</h3><p><code>global memory</code> 是大部分真实数据的主战场：</p><ul><li>大张量在这里</li><li>权重在这里</li><li>activation 在这里</li><li>KV cache 也在这里</li></ul><p>你不可能绕开它。<br>真正的问题从来不是“不要用 <code>global memory</code>”，而是：</p><blockquote><p>必须用时，能不能少用一点、用得更整齐一点、让每次访问更值一点。</p></blockquote><p>这也是为什么很多推理算子虽然数学不复杂，却依然很慢。<br>例如：</p><ul><li>LayerNorm</li><li>RMSNorm</li><li>softmax</li><li>一些 elementwise 算子</li></ul><p>它们常常不是算不动，而是读写太多，典型地属于 <code>memory-bound</code>。</p><h3 id="3-8-su-du-rong-liang-ju-chi-zhi-jian-dao-di-zen-yao-qu-she" tabindex="-1" id="3-8-速度、容量、距离之间到底怎么取舍">3.8 速度、容量、距离之间到底怎么取舍</h3><p>把内存层级真正串起来看，核心就是三件事：</p><ul><li>快的，通常小</li><li>大的，通常远</li><li>远的，通常贵</li></ul><p>所以一个成熟的 CUDA 视角不是“哪种内存最快”，而是：</p><ul><li>哪些数据必须长期放在 <code>global memory</code></li><li>哪些数据值得临时搬到 <code>shared memory</code></li><li>哪些中间值应该尽量留在 <code>register</code></li><li>哪些对象大到可能导致 <code>local memory</code> 或 spill</li></ul><p>一旦你开始这样想，内存层级就不再是静态定义，而变成了动态的数据流设计问题。</p><h3 id="3-9-yong-shu-ju-zen-yao-liu-dong-lai-li-jie-kernel" tabindex="-1" id="3-9-用“数据怎么流动”来理解-kernel">3.9 用“数据怎么流动”来理解 kernel</h3><p>这篇最重要的思维切换是：</p><blockquote><p>不只看线程在算什么，还要看数据在哪一层、接下来会往哪一层走。</p></blockquote><p>很多高性能 kernel 背后的典型路径都是：</p><p><code>global memory -&gt; shared memory -&gt; register -&gt; global memory</code></p><p>这条路径背后的含义是：</p><ul><li>大块原始数据先在 <code>global memory</code></li><li>当前 block 需要的一小块搬进 <code>shared memory</code></li><li>每个线程再把自己最频繁用的值放进 <code>register</code></li><li>算完后结果再写回 <code>global memory</code></li></ul><p>这几乎就是后面 GEMM、卷积、attention、fused kernel 的基本骨架。</p><h3 id="3-10-wei-shi-yao-li-jie-nei-cun-ceng-ji-shi-hou-xu-you-hua-de-qian-ti" tabindex="-1" id="3-10-为什么理解内存层级是后续优化的前提">3.10 为什么理解内存层级是后续优化的前提</h3><p>从这一篇开始，你后面很多知识点都会自动串起来：</p><ul><li>为什么 coalescing 重要：因为 <code>global memory</code> 访问方式直接决定带宽利用率</li><li>为什么 bank conflict 会拖慢 <code>shared memory</code>：因为片上共享存储也有自己的硬件约束</li><li>为什么 fusion 常能提速：因为减少了中间结果写回 <code>global memory</code> 再读回的次数</li><li>为什么 occupancy 不能脱离寄存器和 shared memory 看：因为这些资源会限制一个 SM 上能挂多少活跃 warp</li></ul><p>所以“内存层级”不是一个独立章节，而是后面几乎所有优化章节的地基。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇和 AI 推理的关系非常直接，因为很多推理优化，本质上都在减少慢速数据流量。</p><p>例如：</p><ul><li><code>fusion</code>：减少中间结果写回 <code>global memory</code> 的次数</li><li>tiled GEMM：把可复用数据先搬到 <code>shared memory</code></li><li>FlashAttention：尽量避免把过大的中间结果落回慢速内存</li><li>decode 阶段的很多 kernel：常常更受访存和带宽限制，而不是受乘加能力限制</li></ul><p>所以你后面看推理系统时，真正该问的是：</p><ul><li>数据主要卡在哪一层</li><li>哪些数据可以复用</li><li>哪些搬运是不必要的</li><li>现在这个 kernel 更像 <code>memory-bound</code> 还是 <code>compute-bound</code></li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li>GPU 算力很强，所以性能问题主要来自计算。不是，很多 kernel 首先卡在数据搬运。</li><li><code>local memory</code> 听起来很近，所以应该很快。不是，它常常意味着寄存器放不下了。</li><li>寄存器越多越好。不是，寄存器太多会压并发，甚至 spill。</li><li><code>shared memory</code> 一定值得手动用。不是，只有在复用和重组访问明显值得时才划算。</li><li><code>global memory</code> 很慢，所以应该完全避免。不是，大数据离不开它，关键是减少不必要访问并优化访问方式。</li><li>内存层级就是背几个名词。不是，真正要学的是数据怎样在不同层级之间流动。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么很多 CUDA 程序首先受访存限制，而不是先受计算限制？</li><li>CUDA 为什么必须设计成分层内存？</li><li><code>register</code>、<code>local memory</code>、<code>shared memory</code>、<code>global memory</code> 各自解决什么问题？</li><li>为什么 <code>local memory</code> 不一定快？</li><li>什么是 <code>register spill</code>，它为什么会拖慢程序？</li><li><code>shared memory</code> 最核心的两个价值是什么？</li><li>为什么 <code>global memory</code> 常常是推理算子的主要瓶颈？</li><li>为什么说真正重要的不是“哪种内存快”，而是“数据应该怎样流动”？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/fc8443b4/">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</a></li><li>下一篇：<a href="/posts/b8b486f5/">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</a></li><li>延伸阅读：<a href="/posts/63789fbe/">GPU系统拆解-06-内存层次与访存主线：GPU 性能为什么常常卡在数据</a></li><li>延伸阅读：<a href="/posts/92dc1028/">CUDA C编程权威指南：导读</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-05-nei-cun-ceng-ji-yu-fang-cun-ben-zhi-xing-neng-ping-jing-wei-shi-yao-chang-qia-zai-shu-ju&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="内存管理" scheme="https://smarter.xin/tags/memory-management/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="全局内存" scheme="https://smarter.xin/tags/global-memory/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</title>
    <link href="https://smarter.xin/posts/fc8443b4/"/>
    <id>https://smarter.xin/posts/fc8443b4/</id>
    <published>2026-04-06T02:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-04-warp-simt-yu-sm-zhen-shi-zhi-xing-bu-shi-xian-cheng-ge-pao-ge-de" tabindex="-1">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</h1><blockquote><p>本文是「CUDA系统拆解」系列第 04 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/8f94f6de/">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</a><br>下一篇：<a href="/posts/8f25342e/">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>为什么写 CUDA 时看到的是 <code>thread</code>，但真正影响执行效率的常常是 <code>warp</code>。</li><li><code>warp</code>、<code>SIMT</code>、<code>SM</code> 这些概念分别在说什么。</li><li><code>thread</code>、<code>warp</code>、<code>block</code>、<code>SM</code> 到底是什么关系。</li><li>为什么分支发散会拖慢执行。</li><li>GPU 为什么能靠很多并发 warp 去隐藏延迟。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li><code>thread</code> 是编程模型里的基本单位，<code>warp</code> 是执行和性能分析里的关键粒度。</li><li>在 CUDA 语境里，一个 <code>warp</code> 通常是 <code>32</code> 个线程一起被推进执行。</li><li>CUDA 更准确的描述是 <code>SIMT</code>，不是简单的 <code>SIMD</code>。</li><li><code>block</code> 是协作单位，<code>SM</code> 是执行 <code>block</code> 和 <code>warp</code> 的核心硬件单元。</li><li>同一个 warp 内线程行为越一致，通常执行效率越高；控制流一旦分裂，就可能发生 <code>warp divergence</code>。</li><li>GPU 隐藏延迟的主要方式不是把单线程做得很强，而是让一个 <code>SM</code> 上挂很多 warp，谁在等内存就先切去跑别的 warp。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-cong-xian-cheng-zu-zhi-zou-dao-zhen-shi-zhi-xing" tabindex="-1" id="3-1-从线程组织走到真实执行">3.1 从线程组织走到真实执行</h3><p>上一部分你已经知道：</p><ul><li><code>thread / block / grid</code> 是怎么组织任务的</li><li>一个线程如何通过索引找到自己的数据</li><li><code>block</code> 为什么是协作边界</li></ul><p>但这还只是编程视角。<br>到了执行视角，最关键的变化是：</p><blockquote><p>你写的是 thread，GPU 高效推进时主要看的却是 warp。</p></blockquote><p>所以后面很多性能现象，最后都要从 warp 角度解释：</p><ul><li>为什么分支会慢</li><li>为什么访存模式重要</li><li>为什么 <code>block size</code> 常取 <code>32</code> 的倍数</li><li>为什么活跃 warp 不够时，延迟就会暴露出来</li></ul><h3 id="3-2-warp-dao-di-shi-shi-yao" tabindex="-1" id="3-2-warp-到底是什么">3.2 warp 到底是什么</h3><p>可以先把 warp 理解成：</p><blockquote><p>一组一起被调度、一起向前推进的线程。</p></blockquote><p>你写 kernel 时，代码看起来像每个线程都在独立执行；但在底层，GPU 并不会把每个线程都当成一个完全独立的小任务去调度。更常见的做法是：</p><ul><li>先把 block 内线程分组</li><li>每组形成一个 warp</li><li>再以 warp 为关键粒度发射和推进指令</li></ul><p>这就是为什么后面分析性能时，常见问题都不是“某个线程发生了什么”，而是：</p><ul><li>一个 warp 内线程是不是走了相同路径</li><li>一个 warp 内线程访存是不是规则</li><li>一个 SM 上是不是有足够多 warp 可切换</li></ul><p>所以 warp 的意义不只是“32 个线程一组”这个定义，而是：</p><blockquote><p>它是把编程层面的很多线程，映射成硬件可高吞吐执行的基本观察窗口。</p></blockquote><h3 id="3-3-shi-yao-shi-simt-wei-shi-yao-ta-bu-xiang-jian-dan-de-simd" tabindex="-1" id="3-3-什么是-SIMT，为什么它不像简单的-SIMD">3.3 什么是 SIMT，为什么它不像简单的 SIMD</h3><p>这是最容易混淆的一组概念。</p><p><code>SIMD</code></p><ul><li>更像“一条向量指令处理多个数据 lane”</li><li>常见于 CPU 的向量指令，比如 AVX、SSE</li><li>程序员看到的是更显式的向量化模型</li></ul><p><code>SIMT</code></p><ul><li>你写的是很多线程各自的标量代码</li><li>每个线程逻辑上有自己的寄存器、线程 ID、控制流</li><li>但硬件会尽量让同一个 warp 内线程按相似的指令流一起推进</li></ul><p>所以二者的共同点是都在追求多数据并行。<br>关键区别是：</p><ul><li><code>SIMD</code> 更强调“显式向量指令”</li><li><code>SIMT</code> 更强调“线程级编程抽象 + 底层成组推进执行”</li></ul><p>这也是 CUDA 好学但又不能只停留在语法层的原因：</p><blockquote><p>表面上你写的是多个 thread，底层效率却依赖这些 thread 在 warp 内是否足够整齐。</p></blockquote><h3 id="3-4-code-thread-code-code-warp-code-code-block-code-code-sm-code-de-guan-xi" tabindex="-1" id="3-4-thread、warp、block、SM-的关系">3.4 <code>thread</code>、<code>warp</code>、<code>block</code>、<code>SM</code> 的关系</h3><p>这四个概念最好一次压清楚：</p><p><code>thread</code></p><ul><li>最小编程单位</li><li>负责一份具体计算</li></ul><p><code>warp</code></p><ul><li>关键执行粒度</li><li>block 内线程会被拆成多个 warp</li></ul><p><code>block</code></p><ul><li>线程协作单位</li><li>block 内可以同步，可以共享 <code>shared memory</code></li><li>一个 block 会被放到某个 SM 上执行</li></ul><p><code>SM</code></p><ul><li>GPU 上负责执行 block 和 warp 的核心硬件单元</li><li>强调高吞吐和快速切换，不强调单线程极致低延迟</li></ul><p>可以把关系记成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">grid</span><br><span class="line">  -&gt; many blocks</span><br><span class="line">     -&gt; each block runs on one SM</span><br><span class="line">        -&gt; each block is split into multiple warps</span><br><span class="line">           -&gt; each warp contains many threads</span><br></pre></td></tr></table></figure><p>这里最关键的两句话是：</p><ul><li><code>thread</code> 是你写代码时直接面对的单位</li><li><code>warp</code> 和 <code>SM</code> 才是解释执行效率时更关键的单位</li></ul><h3 id="3-5-wei-shi-yao-warp-divergence-hui-dai-lai-xing-neng-sun-shi" tabindex="-1" id="3-5-为什么-warp-divergence-会带来性能损失">3.5 为什么 warp divergence 会带来性能损失</h3><p>理想情况下，同一个 warp 内线程会沿着相同控制流一起前进。<br>一旦同一个 warp 内线程走了不同分支，就会出现 <code>warp divergence</code>。</p><p>例如一种很典型的坏模式是：</p><ul><li>同一个 warp 里，部分线程走 <code>if</code></li><li>另一部分线程走 <code>else</code></li></ul><p>这时硬件通常不能让同一个 warp 在同一时刻同时执行两条完全不同的路径，只能变成：</p><ol><li>先推进其中一条路径，让另一部分线程暂时不活跃</li><li>再推进另一条路径，让前一部分线程暂时不活跃</li><li>最后再汇合</li></ol><p>这意味着什么？</p><ul><li>原本可以一起推进的线程组，被部分串行化了</li><li>warp 的有效并行度下降</li><li>吞吐下降</li></ul><p>所以 <code>warp divergence</code> 的本质不是“分支一定错”，而是：</p><blockquote><p>同一个 warp 内控制流不一致，会让原本适合成组推进的执行变得破碎。</p></blockquote><h3 id="3-6-na-xie-fen-zhi-geng-rong-yi-chu-wen-ti-na-xie-mei-na-yao-ke-pa" tabindex="-1" id="3-6-哪些分支更容易出问题，哪些没那么可怕">3.6 哪些分支更容易出问题，哪些没那么可怕</h3><p>更危险的分支通常有两类：</p><ul><li>按线程细粒度交错分裂的分支，比如奇偶线程走不同路径</li><li>随机数据驱动、同一个 warp 内很容易混杂的分支</li></ul><p>相对没那么可怕的情况：</p><ul><li>同一个 warp 内大部分线程都走同一路径</li><li>只在边界附近少量线程发生分裂</li></ul><p>例如最常见的边界判断：</p><ul><li><code>if (idx &lt; N)</code></li></ul><p>它当然也可能让最后一个 warp 有部分线程空转，但影响通常只集中在尾部少量 warp 上。相比正确性，这个代价完全合理。</p><p>所以正确理解不是“CUDA 里不要写 if”，而是：</p><blockquote><p>尽量避免把同一个 warp 切得很碎，必须分支时要知道代价在哪里。</p></blockquote><h3 id="3-7-gpu-wei-shi-yao-neng-yin-cang-yan-chi" tabindex="-1" id="3-7-GPU-为什么能隐藏延迟">3.7 GPU 为什么能隐藏延迟</h3><p>CPU 的典型思路是：</p><ul><li>尽量把单线程做快</li><li>靠缓存、乱序执行、分支预测去减少等待</li></ul><p>GPU 的典型思路不是这样。GPU 更常见的做法是：</p><ul><li>一个 SM 上同时挂很多 warp</li><li>某个 warp 因为 global memory 访问而等待时</li><li>不傻等，先切去执行别的 ready warp</li></ul><p>这就是 GPU 的 <code>latency hiding</code>。</p><p>它的核心不是“让等待消失”，而是：</p><blockquote><p>当一部分 warp 在等时，尽量保证还有别的 warp 可以继续让 SM 忙起来。</p></blockquote><p>这也是为什么活跃 warp 数量很重要。<br>如果一个 SM 上可切换的 warp 太少，就会出现：</p><ul><li>这个 warp 在等</li><li>那个 warp 也在等</li><li>没有足够的 ready warp 可以补位</li></ul><p>最后结果就是 SM 更容易空转，吞吐下降。</p><h3 id="3-8-wei-shi-yao-zhe-xie-gai-nian-hui-zhi-jie-ying-xiang-hou-xu-you-hua" tabindex="-1" id="3-8-为什么这些概念会直接影响后续优化">3.8 为什么这些概念会直接影响后续优化</h3><p>到了这里，你应该能理解几件事：</p><ul><li>为什么 <code>block size</code> 常取 <code>32</code> 的倍数，因为 block 最终会被拆成 warp</li><li>为什么访存优化常按 warp 看，因为同一个 warp 的地址模式会影响 memory transaction</li><li>为什么 occupancy 有意义，因为更多活跃 warp 往往更有利于隐藏延迟</li><li>为什么很多 GPU 不喜欢复杂控制流，因为 warp 内控制流一碎，执行效率就会掉</li></ul><p>所以这一篇不是在学“几个硬件名词”，而是在建立后面所有性能分析的第一层直觉。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这些概念和 AI 推理的关系非常直接。</p><p>GEMM、attention、LayerNorm、softmax 这类 kernel 之所以适合 GPU，本质上都是因为它们更容易形成：</p><ul><li>大量规则并行</li><li>warp 内较整齐的行为</li><li>可重组的访存模式</li><li>足够多的并发工作去喂满 SM</li></ul><p>反过来，如果一个推理 kernel：</p><ul><li>分支很多</li><li>数据访问很乱</li><li>每个 warp 干的事差异很大</li></ul><p>那即使线程数很多，也未必高效。</p><p>所以你后面看 FlashAttention、paged attention、decode 优化时，真正要问的是：</p><ul><li>warp 内线程在做什么</li><li>block 如何协作</li><li>SM 上有没有足够多 warp 隐藏等待</li></ul><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>thread</code> 就是执行效率分析里最关键的单位。不是，很多性能问题要落到 <code>warp</code> 上看。</li><li><code>SIMT</code> 就等于 <code>SIMD</code>。不是，它们都追求并行，但编程抽象和执行方式不同。</li><li><code>warp divergence</code> 说明代码写错了。不是，问题不在“有分支”，而在同一个 warp 被切得太碎。</li><li>GPU 隐藏延迟靠的是单线程很强。不是，主要靠很多并发 warp 和快速切换。</li><li>线程够多就一定快。不是，还要看 warp 一致性、访存模式、资源占用和 SM 上的活跃 warp 数量。</li><li><code>SM</code> 就是 CPU core。不能这么机械类比，SM 更像高吞吐执行单元，不是豪华单核。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么说 <code>thread</code> 是编程单位，<code>warp</code> 是关键执行粒度？</li><li><code>warp</code> 的定义是什么，它为什么重要？</li><li><code>SIMT</code> 和 <code>SIMD</code> 的共同点与区别是什么？</li><li><code>thread</code>、<code>warp</code>、<code>block</code>、<code>SM</code> 分别处在什么层次，它们之间怎么对应？</li><li>为什么同一个 warp 内线程走不同分支会导致性能下降？</li><li>为什么边界判断通常不是最可怕的分支？</li><li>GPU 是怎样依靠很多并发 warp 去隐藏延迟的？</li><li>为什么后面的 coalescing、occupancy、GEMM、attention 都要回到 warp / SM 视角来理解？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/8f94f6de/">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</a></li><li>下一篇：<a href="/posts/8f25342e/">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</a></li><li>延伸阅读：<a href="/posts/85cd1013/">GPU系统拆解-03-从 Ampere 到 Hopper：现代 CUDA 写法为什么变了</a></li><li>延伸阅读：<a href="/posts/fec051fc/">CUDA实践-06-线程束原语与寄存器通信</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-04-warp-simt-yu-sm-zhen-shi-zhi-xing-bu-shi-xian-cheng-ge-pao-ge-de&quot; tabindex=&quot;-1&quot;&gt;CUDA系统拆解-04-warp、SIMT 与</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="执行模型" scheme="https://smarter.xin/tags/execution-model/"/>
    
    <category term="Warp" scheme="https://smarter.xin/tags/warp/"/>
    
    <category term="GPU架构" scheme="https://smarter.xin/tags/gpu-architecture/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</title>
    <link href="https://smarter.xin/posts/8f94f6de/"/>
    <id>https://smarter.xin/posts/8f94f6de/</id>
    <published>2026-04-06T01:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-03-xian-cheng-zu-zhi-mo-xing-grid-block-thread-dao-di-zai-biao-da-shi-yao" tabindex="-1">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</h1><blockquote><p>本文是「CUDA系统拆解」系列第 03 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/ab45c244/">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</a><br>下一篇：<a href="/posts/fc8443b4/">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>CUDA 为什么不是“直接启动很多线程”，而是要分成 <code>thread / block / grid</code> 三层。</li><li><code>thread</code>、<code>block</code>、<code>grid</code> 各自到底解决什么问题。</li><li>为什么几乎所有基础 kernel 都要先算全局索引，再做边界判断。</li><li>什么时候适合用 1D、2D、3D 线程映射。</li><li>这种线程组织方式为什么会直接影响后面的 shared memory、同步、GEMM、attention 和推理算子设计。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>CUDA 的并行不是“你写很多函数”，而是“你定义一个线程做什么，然后复制成很多线程并行执行”。</li><li><code>thread</code> 是最小计算单位，<code>block</code> 是局部协作单位，<code>grid</code> 是整体任务覆盖范围。</li><li><code>block</code> 的关键价值不只是分组，而是提供同步和 shared memory 的作用边界。</li><li>不同 <code>block</code> 默认独立，这个约束既限制了算法写法，也换来了更好的调度和可扩展性。</li><li>一维索引公式和边界判断不是模板技巧，而是线程空间映射到数据空间的直接结果。</li><li>后面几乎所有高性能 kernel，本质上都要先回答：一个线程处理什么、一个 block 为什么这样协作、grid 如何覆盖整个问题空间。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-cuda-de-bing-xing-si-wei-shi-shi-yao" tabindex="-1" id="3-1-CUDA-的并行思维是什么">3.1 CUDA 的并行思维是什么</h3><p>在 CPU 上，你更常见的是这种思路：</p><ul><li>写一个 <code>for</code> 循环</li><li>每次迭代处理一个元素</li><li>让同一个线程把全部数据做完</li></ul><p>在 CUDA 上，思路变成了：</p><ul><li>写一个 <code>kernel</code></li><li>定义“一个线程该做什么”</li><li>启动大量线程</li><li>让每个线程通过自己的线程 ID 去处理不同的数据位置</li></ul><p>所以 CUDA 的并行不是“手工写很多份逻辑”，而是：</p><blockquote><p>写一份线程逻辑，然后让大量线程并行执行这份逻辑。</p></blockquote><p>这就是为什么你会不断看到这类代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"><span class="keyword">if</span> (idx &lt; N) &#123;</span><br><span class="line">    C[idx] = A[idx] + B[idx];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里真正重要的不是公式本身，而是背后的思维：</p><ul><li>先定义线程怎么找到自己的数据</li><li>再让很多线程并行处理这些数据</li></ul><h3 id="3-2-wei-shi-yao-bu-shi-zhi-you-xian-cheng-er-shi-code-thread-block-grid-code" tabindex="-1" id="3-2-为什么不是只有线程，而是-thread-block-grid">3.2 为什么不是只有线程，而是 <code>thread / block / grid</code></h3><p>如果 CUDA 只有“线程”这一层，看起来会更简单，但很多关键问题就没法解决。</p><p>三层结构各自承担不同职责。</p><p><code>thread</code></p><ul><li>最小计算单位</li><li>负责一份局部工作</li><li>例如处理一个向量元素、一个矩阵元素、一个像素或一个 tile 内的一小部分数据</li></ul><p><code>block</code></p><ul><li>一组线程组成的局部协作单位</li><li>block 内线程可以同步</li><li>block 内线程可以共享 shared memory</li><li>block 也是资源分配和调度中的重要粒度</li></ul><p><code>grid</code></p><ul><li>一次 kernel launch 的整体工作空间</li><li>负责用很多个 block 去覆盖整个问题规模</li></ul><p>如果只保留线程，没有 block 这一层，会遇到两个大问题：</p><ul><li>没有清晰的局部协作边界</li><li>shared memory 和同步语义会变得很混乱</li></ul><p>如果只有 block，没有 grid，又很难把任务扩展到更大规模。</p><p>所以这三层不是语法拆分，而是把“计算粒度”“局部协作”“整体覆盖”分开表达。</p><h3 id="3-3-code-thread-code-code-block-code-code-grid-code-fen-bie-jie-jue-shi-yao-wen-ti" tabindex="-1" id="3-3-thread、block、grid-分别解决什么问题">3.3 <code>thread</code>、<code>block</code>、<code>grid</code> 分别解决什么问题</h3><p>可以把这三层压缩成一张表：</p><table><thead><tr><th>层级</th><th>本质角色</th><th>主要解决的问题</th></tr></thead><tbody><tr><td><code>thread</code></td><td>计算单位</td><td>单个线程做什么</td></tr><tr><td><code>block</code></td><td>协作单位</td><td>哪些线程放在一起同步和共享数据</td></tr><tr><td><code>grid</code></td><td>覆盖单位</td><td>如何把全部数据空间都覆盖到</td></tr></tbody></table><p>最重要的一句是：</p><blockquote><p><code>block</code> 内可以协作，不同 <code>block</code> 默认独立。</p></blockquote><p>这句话后面会不断出现，因为它决定了：</p><ul><li>shared memory 的作用范围</li><li><code>__syncthreads()</code> 的有效范围</li><li>为什么很多全局问题要拆成多个 kernel</li></ul><h3 id="3-4-zui-ji-chu-de-1-d-xian-cheng-ying-she" tabindex="-1" id="3-4-最基础的-1D-线程映射">3.4 最基础的 1D 线程映射</h3><p>最常见的启动方式是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> blockSize = <span class="number">256</span>;</span><br><span class="line"><span class="type">int</span> gridSize = (N + blockSize - <span class="number">1</span>) / blockSize;</span><br><span class="line">kernel&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(...);</span><br></pre></td></tr></table></figure><p>这里：</p><ul><li><code>blockSize</code> 是每个 block 的线程数</li><li><code>gridSize</code> 是 block 的数量</li></ul><p>总线程数通常是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gridSize * blockSize</span><br></pre></td></tr></table></figure><p>这通常会大于等于 <code>N</code>，而不是严格等于 <code>N</code>。</p><p>原因很简单：按 block 对齐去组织线程，更符合 GPU 的调度方式，也更方便统一 launch 配置。</p><p>这里要额外记住一层：<code>&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;</code> 的本质不是“把两个数字传给 kernel”，而是在声明怎么把数据空间切成很多块去并行覆盖。也就是说，launch 参数本质上是在做数据分解。</p><p>线程如何找到自己的全局编号？最经典的 1D 公式就是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>threadIdx.x</code>：线程在当前 block 内的局部位置</li><li><code>blockIdx.x</code>：当前 block 在整个 grid 中的位置</li><li><code>blockDim.x</code>：每个 block 的线程数</li></ul><p>这行代码的意义就是：</p><blockquote><p>把线程空间映射到一维数据空间。</p></blockquote><h3 id="3-5-wei-shi-yao-ji-hu-zong-yao-xie-bian-jie-pan-duan" tabindex="-1" id="3-5-为什么几乎总要写边界判断">3.5 为什么几乎总要写边界判断</h3><p>边界判断是和上面的 launch 方式配套出现的：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (idx &lt; N) &#123; ... &#125;</span><br></pre></td></tr></table></figure><p>原因是总线程数经常会向上取整。</p><p>例如：</p><ul><li><code>N = 1000</code></li><li><code>blockSize = 256</code></li><li><code>gridSize = (1000 + 255) / 256 = 4</code></li><li>总线程数 = <code>1024</code></li></ul><p>这意味着最后会多出 24 个线程。<br>如果这些线程继续访问 <code>A[idx]</code>、<code>B[idx]</code>、<code>C[idx]</code>，就会越界。</p><p>所以边界判断的本质不是“语法习惯”，而是：</p><ul><li>允许线程组织按硬件友好的方式对齐</li><li>用很小的代价裁掉尾部多出来的线程</li></ul><p>这体现了 CUDA 很典型的工程思路：</p><blockquote><p>接受少量冗余线程，换取更统一、更可扩展的线程组织方式。</p></blockquote><h3 id="3-6-yi-ge-zui-xiao-1-d-gu-jia" tabindex="-1" id="3-6-一个最小-1D-骨架">3.6 一个最小 1D 骨架</h3><p>下面这段代码已经足够体现 1D 线程组织的核心：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">vecAdd</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* A, <span class="type">const</span> <span class="type">float</span>* B, <span class="type">float</span>* C, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; N) &#123;</span><br><span class="line">        C[idx] = A[idx] + B[idx];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从线程组织的角度看，这段代码只回答了两件事：</p><ol><li>当前线程处理哪个元素</li><li>如果线程越界，该如何安全退出</li></ol><p>而真正的并行规模，是由 launch 时的 <code>grid</code> 和 <code>block</code> 决定的。</p><h3 id="3-7-wei-shi-yao-hen-duo-wen-ti-geng-gua-he-2-d-huo-3-d-ying-she" tabindex="-1" id="3-7-为什么很多问题更适合-2D-或-3D-映射">3.7 为什么很多问题更适合 2D 或 3D 映射</h3><p>不是所有问题都天然是一维的。</p><p>很多问题本身就是二维甚至三维结构，例如：</p><ul><li>图像：<code>height × width</code></li><li>矩阵：<code>rows × cols</code></li><li>feature map：<code>H × W</code></li><li>部分张量计算：<code>batch × seq × hidden</code></li></ul><p>这时继续硬用 1D 当然也能写，但代码结构会不自然，后续优化也会更绕。</p><p>2D 映射的典型写法是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> x = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"><span class="type">int</span> y = blockIdx.y * blockDim.y + threadIdx.y;</span><br></pre></td></tr></table></figure><p>例如做矩阵加法时，线程坐标可以直接对应到 <code>(row, col)</code>。</p><p>它的好处是：</p><ul><li>代码和数据结构更一致</li><li>更容易构造 tile</li><li>更方便后面配合 shared memory 做局部优化</li></ul><p>3D 映射进一步扩展到 <code>z</code> 维，常见于体数据、三维网格，或者把 batch / head / spatial 某一维塞进 <code>z</code> 方向。</p><p>不过要记住一点：</p><blockquote><p>2D / 3D launch 的意义主要是更自然地表达问题结构，而不是硬件真的按“二维”或“三维”特殊加速。</p></blockquote><h3 id="3-8-code-block-code-wei-shi-yao-shi-xie-zuo-bian-jie" tabindex="-1" id="3-8-block-为什么是协作边界">3.8 <code>block</code> 为什么是协作边界</h3><p><code>block</code> 最重要的价值，不是把线程分组，而是让局部线程协作有了明确边界。</p><p><code>block</code> 内线程通常可以做两件事：</p><ul><li>用 <code>__syncthreads()</code> 做同步</li><li>共享 <code>shared memory</code></li></ul><p>这意味着一个 <code>block</code> 常常对应“一个局部工作块”。</p><p>例如在矩阵乘法里：</p><ul><li>一个 <code>block</code> 可能负责输出矩阵中的一个 tile</li><li>block 内线程一起把输入 tile 搬进 shared memory</li><li>然后再在 block 内协作完成计算</li></ul><p>如果没有 <code>block</code> 这一层，这种局部协作会非常难组织。</p><h3 id="3-9-wei-shi-yao-bu-tong-code-block-code-mo-ren-du-li" tabindex="-1" id="3-9-为什么不同-block-默认独立">3.9 为什么不同 <code>block</code> 默认独立</h3><p>这也是 CUDA 非常关键的设计取舍。</p><p>不同 <code>block</code> 默认独立，有两个明显好处：</p><p>第一，调度更灵活。<br>GPU 可以把不同 <code>block</code> 分发到不同 SM 上执行，不需要让它们彼此强依赖。</p><p>第二，可扩展性更强。<br>同一个 kernel 可以在不同 GPU 上运行，只要 block 之间独立即可，不需要高度绑定具体硬件规模。</p><p>代价也很清楚：</p><ul><li>block 间默认不能直接做像 block 内那样的同步</li><li>很多全局问题不能在一个 kernel 里一次性完成</li></ul><p>例如对一个大数组做全局归约，常见做法往往是：</p><ol><li>第一个 kernel：每个 block 先做局部归约</li><li>第二个 kernel：再把各 block 的部分结果继续归约</li></ol><p>这不是写法笨，而是线程组织模型决定的。</p><h3 id="3-10-code-block-size-code-wei-shi-yao-bu-neng-sui-bian-qu" tabindex="-1" id="3-10-block-size-为什么不能随便取">3.10 <code>block size</code> 为什么不能随便取</h3><p><code>block size</code> 既不能太小，也不能太大。</p><p>太小的问题：</p><ul><li>并行度不够</li><li>很难把 warp 级执行吃满</li><li>调度粒度不划算</li></ul><p>太大的问题：</p><ul><li>会吃掉更多寄存器和 shared memory</li><li>可能降低一个 SM 上能同时驻留的 block 数</li><li>影响 occupancy 和调度灵活性</li></ul><p>所以 <code>block size</code> 本质上是在平衡：</p><ul><li>并行度</li><li>协作粒度</li><li>资源占用</li><li>调度灵活性</li></ul><p>为什么常见值经常是 <code>128 / 256 / 512</code>？</p><ul><li>它们通常是 <code>32</code> 的倍数，便于和 warp 粒度对齐</li><li>对很多 kernel 来说是比较实用的经验起点</li></ul><p>但要记住：</p><blockquote><p>block size 不是越大越好，也不是固定答案。它最终要和 kernel 的寄存器、shared memory、访存模式一起看。</p></blockquote><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>线程组织模型和 AI 推理的关系非常直接，因为后面你看到的几乎所有推理 kernel，都先要回答这个问题：</p><ul><li>一份数据由哪个线程处理</li><li>一个 block 为什么这样分工</li><li>哪些线程需要局部协作</li><li>grid 如何覆盖完整问题空间</li></ul><p>例如：</p><p>GEMM</p><ul><li>grid 覆盖输出矩阵的所有 tile</li><li>block 负责一个 tile</li><li>thread 负责 tile 内的一部分结果</li></ul><p>attention</p><ul><li>某些维度会映射到 batch、head、query block、key block</li><li>block 内线程共同处理一个 attention tile</li></ul><p>decode 阶段的部分 kernel</p><ul><li>可能按 token、head、hidden slice 或 cache block 做映射</li></ul><p>所以这篇不是在学“基础语法”，而是在学：</p><blockquote><p>以后所有 CUDA 算子和推理 kernel 的最底层骨架。</p></blockquote><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>thread / block / grid</code> 只是语法层次。不是，它们分别对应计算粒度、协作边界和整体覆盖范围。</li><li><code>block</code> 只是为了把线程分组。不是，block 最关键的是同步和 shared memory 的作用边界。</li><li><code>if (idx &lt; N)</code> 只是模板。不是，它是向上取整 launch 配置的自然结果。</li><li>只会 1D 索引就够了。不是，矩阵、图像、张量问题经常更适合 2D 或 3D 映射。</li><li>不同 block 之间也能像 block 内一样直接协作。默认不行，这正是很多算法需要拆成多个 kernel 的原因。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么 CUDA 不是只有线程，而是要分成 <code>thread / block / grid</code> 三层？</li><li><code>thread</code>、<code>block</code>、<code>grid</code> 各自解决什么问题？</li><li>一维索引公式为什么写成 <code>blockIdx.x * blockDim.x + threadIdx.x</code>？</li><li>为什么边界判断几乎总会出现？</li><li>为什么 <code>block</code> 既是组织单位，又是协作单位？</li><li>为什么不同 <code>block</code> 默认独立，这种设计带来了什么好处和限制？</li><li>什么时候 2D 映射会比 1D 更自然？</li><li>如果把一个 GEMM 或 attention kernel 拿出来，你会先从哪些线程组织问题开始分析？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/ab45c244/">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</a></li><li>下一篇：<a href="/posts/fc8443b4/">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</a></li><li>延伸阅读：<a href="/posts/492fe17c/">GPU系统拆解-05-CUDA 执行映射：抽象模型如何落到真实硬件</a></li><li>延伸阅读：<a href="/posts/92dc1028/">CUDA C编程权威指南：导读</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-03-xian-cheng-zu-zhi-mo-xing-grid-block-thread-dao-di-zai-biao-da-shi-yao&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="编程模型" scheme="https://smarter.xin/tags/programming-model/"/>
    
    <category term="执行模型" scheme="https://smarter.xin/tags/execution-model/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</title>
    <link href="https://smarter.xin/posts/ab45c244/"/>
    <id>https://smarter.xin/posts/ab45c244/</id>
    <published>2026-04-06T01:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-02-di-yi-ge-cuda-cheng-xu-zui-xiao-bi-huan-yu-dai-ma-zhi-xing-lu-jing" tabindex="-1">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</h1><blockquote><p>本文是「CUDA系统拆解」系列第 02 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>上一篇：<a href="/posts/cf7051aa/">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</a><br>下一篇：<a href="/posts/8f94f6de/">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>CUDA 程序为什么天然分成 <code>host</code> 和 <code>device</code> 两部分。</li><li><code>kernel</code> 和普通函数到底有什么区别。</li><li><code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 这套 launch 语法在表达什么。</li><li><code>cudaMalloc</code>、<code>cudaMemcpy</code>、<code>cudaDeviceSynchronize</code>、<code>cudaFree</code> 分别在做什么。</li><li>一个最小 CUDA 程序从 CPU 发起，到 GPU 执行，再把结果取回，完整链路到底怎么走。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>CUDA 程序不是“整个程序都跑在 GPU 上”，而是 CPU 控制、GPU 计算。</li><li><code>host</code> 负责准备数据、申请显存、提交任务、同步和取回结果；<code>device</code> 负责并行执行 <code>kernel</code>。</li><li><code>kernel</code> 不是普通函数，它是 GPU 上的大规模并行执行入口。</li><li><code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 不是语法装饰，而是在定义线程组织方式。</li><li>最小闭环就是：准备数据 -&gt; <code>cudaMalloc</code> -&gt; <code>cudaMemcpy(H2D)</code> -&gt; launch kernel -&gt; 同步 -&gt; <code>cudaMemcpy(D2H)</code> -&gt; 释放资源。</li><li>理解这条最小执行链，后面学 <code>warp</code>、内存层级、stream、profiling、推理引擎才不会断层。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-cuda-cheng-xu-bu-shi-pao-zai-gpu-shang-er-shi-cpu-kong-zhi-gpu-gan-huo" tabindex="-1" id="3-1-CUDA-程序不是“跑在-GPU-上”，而是“CPU-控制-GPU-干活”">3.1 CUDA 程序不是“跑在 GPU 上”，而是“CPU 控制 GPU 干活”</h3><p>初学 CUDA 最容易犯的错，就是把 <code>.cu</code> 文件理解成“整段程序都在 GPU 上运行”。</p><p>更准确的理解是：</p><ul><li>主控逻辑仍然在 CPU 上</li><li>GPU 是被 CPU 调用的加速器</li><li>CUDA 程序本质上是 <code>host code + device code</code></li></ul><p>其中：</p><ul><li><code>host code</code> 运行在 CPU 上，负责准备数据、申请 GPU 显存、数据拷贝、启动 kernel、同步和结果校验</li><li><code>device code</code> 运行在 GPU 上，主要就是 <code>kernel</code></li></ul><p>你应该先建立下面这张脑图：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">CPU(host)</span><br><span class="line">  ├─ 准备数据</span><br><span class="line">  ├─ 申请 GPU 内存</span><br><span class="line">  ├─ 把输入拷到 GPU</span><br><span class="line">  ├─ 启动 kernel</span><br><span class="line">  ├─ 等待或同步</span><br><span class="line">  └─ 把结果取回</span><br><span class="line"></span><br><span class="line">GPU(device)</span><br><span class="line">  └─ 并行执行 kernel</span><br></pre></td></tr></table></figure><p>这就是 CUDA 最小闭环的骨架。</p><h3 id="3-2-wei-shi-yao-bi-xu-fen-host-he-device" tabindex="-1" id="3-2-为什么必须分-host-和-device">3.2 为什么必须分 host 和 device</h3><p>这不是 CUDA 故意把编程搞复杂，而是硬件现实决定的。</p><p>CPU 和 GPU 是两类不同处理器：</p><ul><li>执行模型不同</li><li>擅长的问题不同</li><li>内存空间通常不同</li><li>调度方式不同</li></ul><p>CPU 更适合：</p><ul><li>复杂控制流</li><li>分支和逻辑调度</li><li>系统调用、文件、网络、IO</li></ul><p>GPU 更适合：</p><ul><li>大规模规则并行</li><li>高吞吐张量计算</li><li>很多线程做相似操作</li></ul><p>所以 CUDA 的设计很明确：</p><blockquote><p>CPU 负责控制和调度，GPU 负责高吞吐并行计算。</p></blockquote><p>这也是为什么推理框架看起来在 CPU 侧运行，但底层真正重计算的地方会落到 CUDA kernel 或 CUDA 库。</p><h3 id="3-3-kernel-shi-shi-yao-he-pu-tong-han-shu-you-shi-yao-qu-bie" tabindex="-1" id="3-3-kernel-是什么，和普通函数有什么区别">3.3 kernel 是什么，和普通函数有什么区别</h3><p>普通 C++ 函数的调用语义很简单：CPU 调，CPU 执行。</p><p>CUDA <code>kernel</code> 不是这样。</p><p>例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">vecAdd</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* A, <span class="type">const</span> <span class="type">float</span>* B, <span class="type">float</span>* C, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; N) &#123;</span><br><span class="line">        C[idx] = A[idx] + B[idx];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>__global__</code> 表示：</p><ul><li>这是一个 <code>kernel</code></li><li>从 <code>host</code> 侧发起</li><li>在 <code>device</code> 上执行</li></ul><p>它和普通函数最大的区别，不在“写法长得不一样”，而在执行语义不一样：</p><ul><li>普通函数通常执行一次</li><li>kernel 会被 GPU 扩展成大量线程并行执行</li></ul><p>所以你写 kernel 时，写的不是“整个任务如何串行做完”，而是：</p><blockquote><p>单个线程该做什么</p></blockquote><p>这就是 CUDA 思维和普通 C++ 思维的第一个分界线。</p><h3 id="3-4-zui-xiao-dai-ma-gu-jia-chang-shi-yao-yang" tabindex="-1" id="3-4-最小代码骨架长什么样">3.4 最小代码骨架长什么样</h3><p>下面是一个足够小、但已经完整体现执行链的例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">__global__ <span class="type">void</span> <span class="title">vecAdd</span><span class="params">(<span class="type">const</span> <span class="type">float</span>* A, <span class="type">const</span> <span class="type">float</span>* B, <span class="type">float</span>* C, <span class="type">int</span> N)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; N) &#123;</span><br><span class="line">        C[idx] = A[idx] + B[idx];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> N = <span class="number">1024</span>;</span><br><span class="line">    <span class="type">size_t</span> bytes = N * <span class="built_in">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *h_A, *h_B, *h_C;   <span class="comment">// host memory</span></span><br><span class="line">    <span class="type">float</span> *d_A, *d_B, *d_C;   <span class="comment">// device memory</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">cudaMalloc</span>(&amp;d_A, bytes);</span><br><span class="line">    <span class="built_in">cudaMalloc</span>(&amp;d_B, bytes);</span><br><span class="line">    <span class="built_in">cudaMalloc</span>(&amp;d_C, bytes);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">cudaMemcpy</span>(d_A, h_A, bytes, cudaMemcpyHostToDevice);</span><br><span class="line">    <span class="built_in">cudaMemcpy</span>(d_B, h_B, bytes, cudaMemcpyHostToDevice);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> blockSize = <span class="number">256</span>;</span><br><span class="line">    <span class="type">int</span> gridSize = (N + blockSize - <span class="number">1</span>) / blockSize;</span><br><span class="line">    vecAdd&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_A, d_B, d_C, N);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">cudaDeviceSynchronize</span>();</span><br><span class="line">    <span class="built_in">cudaMemcpy</span>(h_C, d_C, bytes, cudaMemcpyDeviceToHost);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">cudaFree</span>(d_A);</span><br><span class="line">    <span class="built_in">cudaFree</span>(d_B);</span><br><span class="line">    <span class="built_in">cudaFree</span>(d_C);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码虽然简单，但已经包含了最核心的动作：</p><ol><li>分配 device memory</li><li>把输入从 host 拷到 device</li><li>用 <code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 启动 kernel</li><li>等待 GPU 完成</li><li>把结果从 device 拷回 host</li><li>释放 device memory</li></ol><p>你后面看到的大多数 CUDA 程序，骨架都还是这套流程，只是更复杂、更并发、或者做了更多优化。</p><h3 id="3-5-code-lt-lt-lt-grid-block-gt-gt-gt-code-dao-di-zai-biao-da-shi-yao" tabindex="-1" id="3-5-grid-block-到底在表达什么">3.5 <code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 到底在表达什么</h3><p>这一行是 CUDA 最有辨识度的语法：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vecAdd&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_A, d_B, d_C, N);</span><br></pre></td></tr></table></figure><p>它表示：</p><blockquote><p>用某种线程组织方式去启动这个 kernel</p></blockquote><p>其中：</p><ul><li><code>blockSize</code>：每个 block 有多少线程</li><li><code>gridSize</code>：总共有多少个 block</li></ul><p>所以总线程数大致是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gridSize * blockSize</span><br></pre></td></tr></table></figure><p>为什么 CUDA 不直接让你说“启动 N 个线程”，而是要分 <code>grid</code> 和 <code>block</code>？</p><p>因为 <code>block</code> 不是单纯分组，它还是：</p><ul><li>调度单位</li><li>协作单位</li><li>后面 shared memory 和同步的基本作用范围</li></ul><p>所以 <code>grid / block</code> 是执行模型的一部分，不是装饰语法。</p><h3 id="3-6-wei-shi-yao-yao-xian-suan-code-idx-code-huan-yao-xie-bian-jie-pan-duan" tabindex="-1" id="3-6-为什么要先算-idx，还要写边界判断">3.6 为什么要先算 <code>idx</code>，还要写边界判断</h3><p>这两行在 CUDA 代码里极其常见：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"><span class="keyword">if</span> (idx &lt; N) &#123; ... &#125;</span><br></pre></td></tr></table></figure><p>第一行的作用是算全局索引，也就是“当前线程该处理哪一份数据”。</p><p>这里用到的几个量分别表示：</p><ul><li><code>threadIdx.x</code>：线程在 block 内的局部编号</li><li><code>blockIdx.x</code>：block 在 grid 内的编号</li><li><code>blockDim.x</code>：每个 block 的线程数</li></ul><p>第二行边界判断也几乎总是需要。</p><p>原因很简单：线程组织通常按 block 对齐，而数据规模未必正好整除 block size。</p><p>例如：</p><ul><li><code>N = 1000</code></li><li><code>blockSize = 256</code></li><li><code>gridSize = (1000 + 255) / 256 = 4</code></li><li>总线程数 = <code>4 * 256 = 1024</code></li></ul><p>这时会多出 24 个线程。如果不写 <code>if (idx &lt; N)</code>，这些线程就会访问越界内存。</p><p>所以这套写法不是模板习惯，而是 CUDA 执行模型的自然结果：</p><ul><li>先按硬件友好的方式组织线程</li><li>再用边界判断裁掉多出来的尾巴</li></ul><h3 id="3-7-code-cuda-malloc-code-code-cuda-memcpy-code-code-cuda-device-synchronize-code-fen-bie-zai-zuo-shi-yao" tabindex="-1" id="3-7-cudaMalloc、cudaMemcpy、cudaDeviceSynchronize-分别在做什么">3.7 <code>cudaMalloc</code>、<code>cudaMemcpy</code>、<code>cudaDeviceSynchronize</code> 分别在做什么</h3><p>这几个 API 是最小闭环里的关键节点。</p><p><code>cudaMalloc</code></p><ul><li>在 GPU 的 global memory 上申请空间</li><li>对应的是 <code>device memory</code></li></ul><p>这里必须明确：</p><ul><li><code>h_A / h_B / h_C</code> 在 host memory</li><li><code>d_A / d_B / d_C</code> 在 device memory</li></ul><p>这两边默认不是同一块空间。</p><p><code>cudaMemcpy</code></p><ul><li>负责在 host 和 device 之间搬数据</li><li>常见方向是 <code>HostToDevice</code> 和 <code>DeviceToHost</code></li></ul><p>这一步非常重要，因为在很多真实系统里，搬数据本身就是性能成本。</p><p><code>cudaDeviceSynchronize</code></p><ul><li>让 CPU 等待，直到 GPU 上之前提交的任务完成</li></ul><p>这也说明一个关键事实：</p><blockquote><p>CPU 提交 kernel，不等于 CPU 会自动等 GPU 算完</p></blockquote><p>理解这点，后面才能继续理解 <code>stream</code>、异步执行和 overlap。</p><h3 id="3-8-cong-code-cu-code-dao-shi-ji-zhi-xing-dao-di-fa-sheng-liao-shi-yao" tabindex="-1" id="3-8-从-cu-到实际执行，到底发生了什么">3.8 从 <code>.cu</code> 到实际执行，到底发生了什么</h3><p>这一层先建立高层直觉，不展开到编译细节。</p><p>一个最小 CUDA 程序的大致路径是：</p><ol><li>你写 <code>.cu</code> 文件，里面同时包含 host 代码和 device 代码。</li><li>编译阶段会把 host 部分处理成 CPU 可执行部分，把 device 部分处理成给 GPU 使用的代码。</li><li>运行时由 CPU 执行主程序。</li><li>CPU 通过 Runtime API 申请 device memory、搬数据、提交 kernel。</li><li>GPU 接到 launch 请求后，按 <code>grid / block / thread</code> 组织并行执行。</li><li>结果写回 device memory。</li><li>CPU 同步后，再把结果取回 host。</li></ol><p>这就是“从 <code>.cu</code> 到实际执行”的最小直觉版路径。后面讲 PTX / SASS / 编译链路时，会把这一层再展开。</p><h3 id="3-9-yong-yi-tiao-zhi-xing-shi-jian-xian-ba-zhe-pian-chuan-qi-lai" tabindex="-1" id="3-9-用一条执行时间线把这篇串起来">3.9 用一条执行时间线把这篇串起来</h3><p>把上面的内容连成时间线，会更清楚：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">CPU(host)</span><br><span class="line">  1. 准备输入数据</span><br><span class="line">  2. cudaMalloc 申请 d_A, d_B, d_C</span><br><span class="line">  3. cudaMemcpy: h_A -&gt; d_A</span><br><span class="line">  4. cudaMemcpy: h_B -&gt; d_B</span><br><span class="line">  5. launch vecAdd&lt;&lt;&lt;grid, block&gt;&gt;&gt;</span><br><span class="line">  6. cudaDeviceSynchronize</span><br><span class="line">  7. cudaMemcpy: d_C -&gt; h_C</span><br><span class="line">  8. 检查结果并释放资源</span><br><span class="line"></span><br><span class="line">GPU(device)</span><br><span class="line">  1. 接收输入数据</span><br><span class="line">  2. 接收 kernel launch 请求</span><br><span class="line">  3. 创建 grid / block / thread</span><br><span class="line">  4. 每个线程计算自己的 idx</span><br><span class="line">  5. 满足 idx &lt; N 的线程执行计算</span><br><span class="line">  6. 结果写回 d_C</span><br></pre></td></tr></table></figure><p>这条时间线很重要。因为后面所有复杂 CUDA 程序，本质上都是把这条链：</p><ul><li>拉长</li><li>并发</li><li>重叠</li><li>做更精细的资源管理</li></ul><p>但骨架不会变。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>这篇虽然很基础，但它和 AI 推理的关系很直接。</p><p>推理框架表面上可能在做：</p><ul><li>请求处理</li><li>batching</li><li>cache 管理</li><li>调度</li></ul><p>但底层仍然要重复执行同一套最小闭环：</p><ul><li>准备输入 tensor</li><li>管理显存</li><li>启动 CUDA kernel 或 CUDA 库</li><li>同步或继续推进下一步</li></ul><p>所以后面你会看到很多推理优化，其实都在压这条链上的某个成本：</p><ul><li>减少 <code>cudaMemcpy</code></li><li>减少 kernel 次数</li><li>降低 launch overhead</li><li>让多个阶段重叠执行</li><li>让数据尽量长时间留在 GPU 上</li></ul><p>也就是说，这篇讲的不是“教学玩具”，而是所有推理执行链的最小骨架。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>kernel 就是普通函数</code>。不对，普通函数通常是 CPU 调 CPU 执行；kernel 是 host 发起、device 并行执行。</li><li><code>写了 CUDA 代码，数据就自动在 GPU 上</code>。不对，host memory 和 device memory 通常需要显式管理和拷贝。</li><li><code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 只是语法装饰。不对，它决定了线程组织方式，也是后续性能分析的入口。</li><li><code>launch 之后 CPU 一定会等 GPU 算完</code>。不对，提交和完成通常不是同一件事。</li><li><code>会写一个向量加法就等于懂 CUDA</code>。不对，真正重要的是能解释这条执行链为什么这样设计、代价在哪、以后怎么优化。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>为什么 CUDA 程序天然要分成 <code>host</code> 和 <code>device</code>？</li><li><code>kernel</code> 和普通函数最本质的区别是什么？</li><li><code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 到底在表达什么？</li><li>为什么几乎总要写 <code>idx</code> 计算和 <code>if (idx &lt; N)</code>？</li><li><code>cudaMalloc</code>、<code>cudaMemcpy</code>、<code>cudaDeviceSynchronize</code> 分别在解决什么问题？</li><li>一个最小 CUDA 程序从 CPU 发起到 GPU 执行再返回结果，完整路径是什么？</li><li>为什么说推理系统的很多优化，本质上都是在压这条最小执行链上的某个成本？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>上一篇：<a href="/posts/cf7051aa/">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</a></li><li>下一篇：<a href="/posts/8f94f6de/">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</a></li><li>延伸阅读：<a href="/posts/492fe17c/">GPU系统拆解-05-CUDA 执行映射：抽象模型如何落到真实硬件</a></li><li>延伸阅读：<a href="/posts/7608f1b0/">CUDA实践-01-基础概念与分块</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-02-di-yi-ge-cuda-cheng-xu-zui-xiao-bi-huan-yu-dai-ma-zhi-xing-lu-jing&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="编程模型" scheme="https://smarter.xin/tags/programming-model/"/>
    
    <category term="执行模型" scheme="https://smarter.xin/tags/execution-model/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</title>
    <link href="https://smarter.xin/posts/cf7051aa/"/>
    <id>https://smarter.xin/posts/cf7051aa/</id>
    <published>2026-04-06T00:30:00.000Z</published>
    <updated>2026-04-04T19:11:49.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-01-cuda-wei-shi-yao-cun-zai-cong-ji-suan-fu-zai-dao-bian-cheng-mo-xing" tabindex="-1">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</h1><blockquote><p>本文是「CUDA系统拆解」系列第 01 篇。<br>系列导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a><br>下一篇：<a href="/posts/ab45c244/">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</a></p></blockquote><h2 id="1-zhe-pian-jie-jue-shi-yao-wen-ti" tabindex="-1" id="1-这篇解决什么问题">1. 这篇解决什么问题</h2><ul><li>CUDA 到底是什么，它和 GPU、深度学习框架、高层加速库分别是什么关系。</li><li>为什么很多 AI 计算更适合 GPU，而不是 CPU。</li><li>CUDA 为什么会出现，它想解决的核心问题是什么。</li><li>为什么 CUDA 的编程模型强调“很多线程做相似的事”。</li><li>为什么做 AI Infra / 推理，最终很难绕开 CUDA。</li></ul><h2 id="2-xian-ji-zhu-de-he-xin-jie-lun" tabindex="-1" id="2-先记住的核心结论">2. 先记住的核心结论</h2><ul><li>CUDA 不只是 <code>.cu</code> 语法，而是 NVIDIA 提供的并行计算平台和编程模型。</li><li>GPU 的核心优势不是单线程更强，而是更擅长高吞吐并行计算。</li><li>CUDA 适合处理数据量大、并行度高、计算模式相对规则的问题。</li><li>CUDA 采用 <code>thread / block / grid</code>，本质上是在表达如何把大任务拆成很多相似小任务。</li><li>AI 推理系统虽然表面上在做调度、缓存和框架封装，但底层仍然要落到 CUDA 的执行和内存现实上。</li><li>学 CUDA 不能只背 API，关键是理解任务如何拆、数据如何流、性能为什么快或慢。</li></ul><h2 id="3-zheng-wen-jiang-jie" tabindex="-1" id="3-正文讲解">3. 正文讲解</h2><h3 id="3-1-cuda-dao-di-shi-shi-yao" tabindex="-1" id="3-1-CUDA-到底是什么">3.1 CUDA 到底是什么</h3><p>很多初学者容易把下面几层东西混在一起：</p><ul><li>GPU：硬件</li><li>CUDA：并行计算平台和编程模型</li><li>cuBLAS / cuDNN / NCCL / TensorRT：建立在 CUDA 之上的高层库</li><li>PyTorch / TensorFlow：更上层的框架</li></ul><p>先把关系理顺：</p><ul><li>GPU 负责提供并行算力。</li><li>CUDA 负责把通用计算映射到 GPU 上执行。</li><li>高层库负责把常见计算封装成高性能实现。</li><li>深度学习框架负责进一步屏蔽底层细节，提供更高层接口。</li></ul><p>所以，CUDA 不是单纯“写 kernel 的语法”，而是包含编译工具链、运行时、驱动接口、分析工具和底层加速库的一整套生态。</p><p>换句话说，框架可以屏蔽 CUDA，但不会消灭 CUDA。你在框架里写一句矩阵乘法，底层仍然可能在调 cuBLAS、Triton 或自定义 CUDA kernel。</p><h3 id="3-2-cuda-wei-shi-yao-hui-chu-xian" tabindex="-1" id="3-2-CUDA-为什么会出现">3.2 CUDA 为什么会出现</h3><p>CUDA 出现的背景很直接：很多计算问题虽然不是图形任务，但和图形渲染有相似特征。</p><p>这些问题通常有几个共同点：</p><ul><li>数据规模大</li><li>操作重复多</li><li>并行度高</li><li>每个元素做的事情相对相似</li></ul><p>例如：</p><ul><li>向量计算</li><li>矩阵乘法</li><li>卷积</li><li>attention</li><li>各类 reduction</li></ul><p>传统 CPU 当然也能算这些问题，但 CPU 的设计重点不是这里。CPU 更强调：</p><ul><li>低延迟</li><li>单线程性能</li><li>复杂控制流</li><li>更强的分支和缓存体系</li></ul><p>而这类 AI / HPC 问题更需要的是：让大量相似计算同时推进，把整体吞吐拉高。</p><p>GPU 最早为图形渲染而生，本来就擅长大规模并行处理。CUDA 的价值，就是把这种硬件能力开放给通用计算。</p><h3 id="3-3-wei-shi-yao-gpu-geng-gua-he-zhe-lei-ji-suan" tabindex="-1" id="3-3-为什么-GPU-更适合这类计算">3.3 为什么 GPU 更适合这类计算</h3><p>很多人会说 GPU 快是因为“核心多”，这只说对了一半。</p><p>更准确的说法是：</p><blockquote><p>GPU 用更少的复杂控制能力，换取了更强的并行吞吐能力。</p></blockquote><p>CPU 和 GPU 的设计目标不一样：</p><ul><li>CPU 更像“精英小队”，适合复杂、变化多、决策密集的任务。</li><li>GPU 更像“大规模流水线”，适合大量重复、规则统一的任务。</li></ul><p>因此 GPU 更擅长：</p><ul><li>矩阵和张量运算</li><li>规则的批量计算</li><li>高吞吐场景</li></ul><p>但 GPU 不一定擅长：</p><ul><li>强串行依赖</li><li>分支极多的逻辑</li><li>非常随机的访存</li><li>任务规模很小的工作负载</li></ul><p>所以“能并行”还不够，一个问题是否适合 CUDA，还要看任务规模、访存模式、控制流复杂度，以及数据搬运成本能不能摊薄。</p><h3 id="3-4-cuda-de-she-ji-mu-biao-shi-shi-yao" tabindex="-1" id="3-4-CUDA-的设计目标是什么">3.4 CUDA 的设计目标是什么</h3><p>CUDA 的目标，不是让你“把 CPU 程序原样搬到 GPU 上”，而是让你用一种适合 GPU 的方式表达并行任务。</p><p>它主要在做三件事：</p><p>第一，把大任务拆成很多相似的小任务。<br>这就是为什么 CUDA 的核心抽象不是普通函数调用，而是 <code>thread / block / grid</code>。</p><p>第二，在抽象和控制之间取平衡。<br>CUDA 没有把硬件细节完全隐藏掉。它给了你足够高层的并行抽象，但同时也保留了很多硬件相关控制能力，比如线程块大小、访存方式、同步、shared memory、stream 等。</p><p>第三，最大化吞吐，而不是只优化单个任务的延迟。<br>后面你会学到的 <code>warp</code>、<code>occupancy</code>、<code>latency hiding</code>、<code>stream overlap</code>，本质上都在服务这个目标。</p><h3 id="3-5-wei-shi-yao-cuda-de-bian-cheng-mo-xing-shi-hen-duo-xian-cheng-zuo-xiang-si-de-shi" tabindex="-1" id="3-5-为什么-CUDA-的编程模型是“很多线程做相似的事”">3.5 为什么 CUDA 的编程模型是“很多线程做相似的事”</h3><p>CPU 上常见的思路是：一个线程遍历全部数据。</p><p>CUDA 的思路是：让很多线程同时工作，每个线程处理一个或少量元素。</p><p>这样设计的原因很明确：</p><ul><li>更符合 GPU 的硬件执行方式</li><li>更容易把数据并行展开</li><li>更容易用并发线程隐藏访存延迟</li><li>更容易把任务结构映射到硬件层级</li></ul><p>它的代价也很明确：</p><ul><li>你要自己考虑线程划分</li><li>要考虑边界判断</li><li>要考虑同步和访存效率</li><li>复杂控制逻辑会更难写</li></ul><p>所以 CUDA 的难点和价值其实是同一件事：它更贴近硬件。</p><h3 id="3-6-shi-yao-yang-de-wen-ti-gua-he-cuda" tabindex="-1" id="3-6-什么样的问题适合-CUDA">3.6 什么样的问题适合 CUDA</h3><p>这是一个很重要的工程判断。</p><p>通常更适合 CUDA 的问题，往往同时满足下面几个条件：</p><ul><li>数据量足够大</li><li>并行度足够高</li><li>很多元素执行相同或相似操作</li><li>计算量足以覆盖数据搬运和 kernel launch 开销</li><li>访存模式可以被较好地组织</li></ul><p>典型例子包括：</p><ul><li>向量计算</li><li>矩阵乘法</li><li>卷积</li><li>softmax</li><li>attention</li><li>各类 reduction</li></ul><p>不太适合 CUDA 的问题，通常有这些特征：</p><ul><li>任务规模太小</li><li>串行依赖很强</li><li>分支极其复杂</li><li>随机访存非常重</li><li>控制流主导，而不是数值计算主导</li></ul><p>所以不能把“适合并行”和“适合 CUDA”简单画等号。真正的判断标准，是 GPU 的吞吐优势能不能覆盖掉调度、数据搬运和访存组织的代价。</p><h3 id="3-7-xian-jian-li-yi-tiao-wan-zheng-yin-guo-lian" tabindex="-1" id="3-7-先建立一条完整因果链">3.7 先建立一条完整因果链</h3><p>这篇最重要的不是记住几个定义，而是先建立下面这条因果链：</p><ol><li>AI / HPC 中存在大量大规模、重复性强、并行度高的计算。</li><li>GPU 的架构更适合处理这类高吞吐任务。</li><li>CUDA 提供了把这些任务映射到 GPU 的编程模型和执行环境。</li><li>为了表达并行性，CUDA 采用 <code>thread / block / grid</code>。</li><li>为了真正跑快，后续还要继续处理 <code>warp</code>、内存层级、同步、occupancy、stream 等问题。</li><li>推理框架和推理引擎的上层优化，最终也都要落回这些底层现实。</li></ol><p>只要这条链建立起来，后面你学到的每个概念都会有位置，不会变成孤立名词。</p><h2 id="4-he-ai-tui-li-de-guan-xi" tabindex="-1" id="4-和-AI-推理的关系">4. 和 AI 推理的关系</h2><p>AI 推理系统表面上在谈：</p><ul><li>batching</li><li>调度</li><li>KV cache</li><li>graph capture</li><li>并发请求管理</li></ul><p>但真正吃算力和带宽的主体，仍然是张量计算：</p><ul><li>GEMM</li><li>attention</li><li>softmax</li><li>layernorm / rmsnorm</li><li>activation</li></ul><p>这些算子的性能，最终都要落到几个底层问题上：</p><ul><li>kernel 怎么组织</li><li>数据怎么布局</li><li>显存怎么用</li><li>global memory 往返能否减少</li><li>stream 能否并发</li><li>launch overhead 能否压低</li></ul><p>这就是为什么做 AI Infra / 推理，不能只停在框架接口层。你最终还是要理解 CUDA：否则很难真正解释一个系统为什么快、慢在哪里、还能怎么优化。</p><p>这也是为什么很多看起来是“系统层”的优化，最后都会落到 CUDA 视角：</p><ul><li>fusion 要看能否减少 global memory 往返</li><li>graph capture 要看能否减少反复 launch 的 CPU 开销</li><li>KV cache 要看显存组织和读取代价</li><li>attention 优化要看数据布局、计算顺序和片上资源利用</li></ul><p>推理系统的上层设计，最终都要服从底层执行现实。</p><h2 id="5-chang-jian-wu-qu" tabindex="-1" id="5-常见误区">5. 常见误区</h2><ul><li><code>CUDA 就是把 CPU 代码改成并行 for</code>。不对，CUDA 有独立的执行模型、内存层级和性能瓶颈。</li><li><code>GPU 快只是因为核心多</code>。不对，更核心的是架构取舍和吞吐导向。</li><li><code>只要任务能并行，就适合 CUDA</code>。不对，还要看规模、访存模式、分支复杂度和数据搬运成本。</li><li><code>学会 API 就等于学会 CUDA</code>。不对，真正关键的是理解为什么这样分工、为什么这样访存、为什么这样会快。</li><li><code>做 AI 推理只要会框架，不用懂 CUDA</code>。不对，推理系统的关键优化最终都要落到底层执行和内存现实上。</li></ul><h2 id="6-fu-xi-zi-ce" tabindex="-1" id="6-复习自测">6. 复习自测</h2><ul><li>CUDA 和 GPU、cuBLAS、TensorRT、PyTorch 分别是什么关系？</li><li>为什么很多 AI 计算更适合 GPU，而不是 CPU？</li><li>CUDA 为什么采用 <code>thread / block / grid</code> 这种组织方式？</li><li>什么样的问题通常适合 CUDA，什么样的问题通常不适合？</li><li>为什么说 CUDA 的重点不是“功能多”，而是“并行、吞吐、数据流”？</li><li>为什么 AI 推理系统的上层优化最终要落回 CUDA 的底层执行现实？</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/d134963b/">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</a></li><li>下一篇：<a href="/posts/ab45c244/">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</a></li><li>延伸阅读：<a href="/posts/3f7ace4/">GPU系统拆解-00-导读：从架构认知到推理系统的学习路线</a></li><li>延伸阅读：<a href="/posts/d6fa227a/">CUDA实践-00-系列导读与学习路线</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-01-cuda-wei-shi-yao-cun-zai-cong-ji-suan-fu-zai-dao-bian-cheng-mo-xing&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
  </entry>
  
  <entry>
    <title>CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</title>
    <link href="https://smarter.xin/posts/d134963b/"/>
    <id>https://smarter.xin/posts/d134963b/</id>
    <published>2026-04-06T00:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.728Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-xi-tong-chai-jie-00-dao-du-cong-bian-cheng-mo-xing-dao-ai-tui-li-xi-tong-de-xue-xi-lu-xian" tabindex="-1">CUDA系统拆解-00-导读：从编程模型到 AI 推理系统的学习路线</h1><h2 id="yue-du-zhe-pian-hou-ying-huo-de-shi-yao" tabindex="-1" id="阅读这篇后应获得什么">阅读这篇后应获得什么</h2><ul><li>知道这套文章不是 CUDA API 手册，而是一条从编程模型、执行机制、访存、调优、面试表达一路连到 AI 推理系统的学习路径。</li><li>能分清它和站内 <code>GPU系统拆解</code>、<code>CUDA实践</code>、<code>Triton</code>、<code>vLLM</code> 这些系列的边界和连接关系。</li><li>能按目标选阅读顺序：偏基础原理、偏调优、偏高性能实现、偏推理系统或偏面试表达都能找到入口。</li></ul><h2 id="zhe-ge-xi-lie-jie-jue-shi-yao-wen-ti" tabindex="-1" id="这个系列解决什么问题">这个系列解决什么问题</h2><p>一句话概括：<strong>先把 CUDA 当作一套“让并行硬件可被工程化地利用起来”的系统抽象来理解，再把执行模型、memory hierarchy、同步、profiling、高性能实现和推理系统连接成一套稳定的工程判断。</strong></p><p>它不重复解释所有 GPU 架构背景，也不直接进入一堆 kernel 练习题，而是解决这些更关键的问题：CUDA 为什么这样设计；代码是怎么被组织和执行的；为什么性能问题大多落在访存、资源占用和通信上；为什么真正高价值的 CUDA 能力最后会连接到 Triton、CUTLASS、vLLM、TensorRT-LLM 这类系统和框架上。</p><h2 id="yu-zhan-nei-qi-ta-wen-zhang-de-guan-xi" tabindex="-1" id="与站内其他文章的关系">与站内其他文章的关系</h2><table><thead><tr><th>文章</th><th>作用</th></tr></thead><tbody><tr><td><a href="/posts/3f7ace4/">GPU系统拆解-00-导读：从架构认知到推理系统的学习路线</a></td><td>先补 GPU 硬件认知、数据中心卡和推理系统背景，再回来看 CUDA 为什么这样抽象会更顺。</td></tr><tr><td><a href="/posts/d6fa227a/">CUDA实践-00-系列导读与学习路线</a></td><td>这套更偏手写 kernel 与实战模式，适合读完本系列后把概念落到代码优化。</td></tr><tr><td><a href="/posts/3162877d/">Triton-导读-从算子开发到模型服务</a></td><td>适合在读到 Tensor Core、CUTLASS、FlashAttention 和高性能实现思路后继续深入。</td></tr><tr><td><a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></td><td>适合在读到 Continuous Batching、PagedAttention、多 GPU 扩展和面试表达后继续连接推理系统。</td></tr><tr><td><a href="/posts/846fbbaf/">PyTorch推理工程-00-系列导读与学习路线</a></td><td>适合把 CUDA 视角继续接到框架执行、导出、服务化与推理工程。</td></tr><tr><td><a href="/posts/92dc1028/">CUDA C编程权威指南：导读</a></td><td>更偏官方编程模型和内存、并发等底层知识的系统性整理。</td></tr></tbody></table><h2 id="xi-lie-jie-gou" tabindex="-1" id="系列结构">系列结构</h2><ul><li><code>01</code> 到 <code>04</code>：先建编程模型。理解 CUDA 的设计动机、最小程序闭环、线程组织和真实执行机制。</li><li><code>05</code> 到 <code>10</code>：进入性能主干。把 memory hierarchy、共享内存、同步、occupancy、stream overlap 和 profiling 串起来。</li><li><code>11</code> 到 <code>16</code>：从模式到高性能实现。先看经典算子模式，再进入 PTX、Tensor Core、CUTLASS、Triton、cuBLAS 与 FlashAttention。</li><li><code>12</code>、<code>13</code>、<code>17</code>、<code>18</code>：把 CUDA 能力连接到面试表达、AI 推理系统和多 GPU 扩展能力。</li></ul><h2 id="lu-xian-tu" tabindex="-1" id="路线图">路线图</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">00 导读</span><br><span class="line">  -&gt; 01 设计动机 -&gt; 02 最小闭环 -&gt; 03 线程组织模型 -&gt; 04 warp / SIMT / SM</span><br><span class="line">  -&gt; 05 内存层级 -&gt; 06 共享内存 / Coalescing -&gt; 07 同步 / 原子操作</span><br><span class="line">  -&gt; 08 Occupancy / Launch 参数 -&gt; 09 Streams / Overlap -&gt; 10 Profiling / 调试</span><br><span class="line">  -&gt; 11 经典算子模式 -&gt; 12 面试总复盘 -&gt; 13 面试强化专题</span><br><span class="line">  -&gt; 14 PTX / SASS / 编译链 -&gt; 15 Tensor Core / WMMA / MMA</span><br><span class="line">  -&gt; 16 CUTLASS / Triton / cuBLAS / FlashAttention</span><br><span class="line">  -&gt; 17 vLLM / TensorRT-LLM / Continuous Batching</span><br><span class="line">  -&gt; 18 NCCL / 多 GPU / 通信隐藏</span><br></pre></td></tr></table></figure><h2 id="pian-mu-suo-yin" tabindex="-1" id="篇目索引">篇目索引</h2><table><thead><tr><th style="text-align:center">编号</th><th style="text-align:left">文章</th><th style="text-align:left">关键词</th></tr></thead><tbody><tr><td style="text-align:center">01</td><td style="text-align:left"><a href="/posts/cf7051aa/">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</a></td><td style="text-align:left">CUDA、GPU编程</td></tr><tr><td style="text-align:center">02</td><td style="text-align:left"><a href="/posts/ab45c244/">CUDA系统拆解-02-第一个CUDA程序：最小闭环与代码执行路径</a></td><td style="text-align:left">CUDA、CUDA入门</td></tr><tr><td style="text-align:center">03</td><td style="text-align:left"><a href="/posts/8f94f6de/">CUDA系统拆解-03-线程组织模型：grid、block、thread 到底在表达什么</a></td><td style="text-align:left">CUDA、编程模型</td></tr><tr><td style="text-align:center">04</td><td style="text-align:left"><a href="/posts/fc8443b4/">CUDA系统拆解-04-warp、SIMT 与 SM：真实执行不是“线程各跑各的”</a></td><td style="text-align:left">CUDA、Warp</td></tr><tr><td style="text-align:center">05</td><td style="text-align:left"><a href="/posts/8f25342e/">CUDA系统拆解-05-内存层级与访存本质：性能瓶颈为什么常卡在数据</a></td><td style="text-align:left">CUDA、全局内存</td></tr><tr><td style="text-align:center">06</td><td style="text-align:left"><a href="/posts/b8b486f5/">CUDA系统拆解-06-共享内存与Coalescing：访存优化先抓哪几个抓手</a></td><td style="text-align:left">CUDA、共享内存</td></tr><tr><td style="text-align:center">07</td><td style="text-align:left"><a href="/posts/416851d9/">CUDA系统拆解-07-同步、原子操作与内存一致性：并发正确性怎么保证</a></td><td style="text-align:left">CUDA、原子操作</td></tr><tr><td style="text-align:center">08</td><td style="text-align:left"><a href="/posts/cae09a20/">CUDA系统拆解-08-Occupancy、寄存器压力与Launch参数：调优不是把占用率拉满</a></td><td style="text-align:left">CUDA、Occupancy</td></tr><tr><td style="text-align:center">09</td><td style="text-align:left"><a href="/posts/481fda56/">CUDA系统拆解-09-Streams、异步拷贝与Overlap：如何把拷贝和计算叠起来</a></td><td style="text-align:left">CUDA、Multi-Stream</td></tr><tr><td style="text-align:center">10</td><td style="text-align:left"><a href="/posts/55daa93f/">CUDA系统拆解-10-Profiling、调试与瓶颈定位：先找到根因再谈优化</a></td><td style="text-align:left">CUDA、性能剖析</td></tr><tr><td style="text-align:center">11</td><td style="text-align:left"><a href="/posts/c824c712/">CUDA系统拆解-11-经典CUDA算子模式：elementwise、reduction、reorder 与 blocked compute</a></td><td style="text-align:left">CUDA、核函数</td></tr><tr><td style="text-align:center">12</td><td style="text-align:left"><a href="/posts/18814acd/">CUDA系统拆解-12-面试总复盘：怎么把 CUDA 能力连接到 AI 推理</a></td><td style="text-align:left">CUDA、面试</td></tr><tr><td style="text-align:center">13</td><td style="text-align:left"><a href="/posts/939f5917/">CUDA系统拆解-13-面试强化专题：PTX、Tensor Core、CUTLASS 与 Triton 怎么讲</a></td><td style="text-align:left">CUDA、PTX</td></tr><tr><td style="text-align:center">14</td><td style="text-align:left"><a href="/posts/15f423b5/">CUDA系统拆解-14-PTX、SASS 与编译链：CUDA代码如何落到机器指令</a></td><td style="text-align:left">CUDA、PTX</td></tr><tr><td style="text-align:center">15</td><td style="text-align:left"><a href="/posts/bd9c352d/">CUDA系统拆解-15-Tensor Core、WMMA 与 MMA：矩阵乘指令路径怎么打通</a></td><td style="text-align:left">CUDA、Tensor Core</td></tr><tr><td style="text-align:center">16</td><td style="text-align:left"><a href="/posts/7061c0d7/">CUDA系统拆解-16-CUTLASS、Triton、cuBLAS 与 FlashAttention：高性能实现都在做什么</a></td><td style="text-align:left">CUDA、CUTLASS</td></tr><tr><td style="text-align:center">17</td><td style="text-align:left"><a href="/posts/47afb486/">CUDA系统拆解-17-vLLM、TensorRT-LLM 与 Continuous Batching：CUDA 为什么最终连到推理系统</a></td><td style="text-align:left">CUDA、vLLM</td></tr><tr><td style="text-align:center">18</td><td style="text-align:left"><a href="/posts/15f00dc3/">CUDA系统拆解-18-NCCL、多GPU与通信隐藏：规模扩展怎么不被通信拖垮</a></td><td style="text-align:left">CUDA、NCCL</td></tr></tbody></table><h2 id="yue-du-shun-xu-jian-yi" tabindex="-1" id="阅读顺序建议">阅读顺序建议</h2><ul><li>想先把 CUDA 原理讲明白：重点读 <code>01</code>、<code>02</code>、<code>03</code>、<code>04</code>、<code>05</code>、<code>07</code>。</li><li>想尽快进入调优主线：重点读 <code>05</code>、<code>06</code>、<code>08</code>、<code>09</code>、<code>10</code>、<code>11</code>。</li><li>想理解高性能实现与框架关系：重点读 <code>13</code>、<code>14</code>、<code>15</code>、<code>16</code>。</li><li>想把 CUDA 连接到 AI 推理系统与面试表达：重点读 <code>12</code>、<code>13</code>、<code>17</code>、<code>18</code>。</li></ul><h2 id="qi-shi-pian" tabindex="-1" id="起始篇">起始篇</h2><p>建议从 <a href="/posts/cf7051aa/">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</a> 开始。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>下一篇：<a href="/posts/cf7051aa/">CUDA系统拆解-01-CUDA为什么存在：从计算负载到编程模型</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-xi-tong-chai-jie-00-dao-du-cong-bian-cheng-mo-xing-dao-ai-tui-li-xi-tong-de-xue-xi-lu-xian&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/categories/ai-infra/cuda/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
  </entry>
  
  <entry>
    <title>GPU系统拆解-14-从 PyTorch 到 CUDA Extension：把算子真正接入工程</title>
    <link href="https://smarter.xin/posts/b47bc812/"/>
    <id>https://smarter.xin/posts/b47bc812/</id>
    <published>2026-04-05T07:00:00.000Z</published>
    <updated>2026-04-04T19:11:49.731Z</updated>
    
    <content type="html"><![CDATA[<h1 id="gpu-xi-tong-chai-jie-14-cong-py-torch-dao-cuda-extension-ba-suan-zi-zhen-zheng-jie-ru-gong-cheng" tabindex="-1">GPU系统拆解-14-从 PyTorch 到 CUDA Extension：把算子真正接入工程</h1><blockquote><p>本文是「GPU系统拆解」系列第 14 篇。<br>系列导读：<a href="/posts/3f7ace4/">GPU系统拆解-00-导读：从架构认知到推理系统的学习路线</a><br>上一篇：<a href="/posts/68e6bad5/">GPU系统拆解-13-多 GPU 与通信：并行策略、拓扑与扩展代价</a></p></blockquote><blockquote><p>这一篇的目标不是再讲 CUDA 语法，而是把前面学过的 GPU、kernel、profiling 和推理系统知识，真正落到“我怎么把一个想法做成可运行、可 benchmark、可 profile、可面试讲清楚的算子工程”上。学完这一篇之后，你应该知道什么样的算子值得自己写、最小工程链路是什么、为什么很多手写 kernel 反而更慢，以及怎样把一个自定义算子讲成完整工程闭环。</p></blockquote><h2 id="1-xian-gei-jie-lun" tabindex="-1" id="1-先给结论">1. 先给结论</h2><ul><li>学 <code>PyTorch -&gt; CUDA extension</code> 的意义，不是学会写一个 demo，而是学会把性能想法真正接进框架和系统。</li><li>自定义算子的价值通常不在“比 PyTorch 更低级”，而在“更贴近你的 shape、数据布局和系统关键路径”。</li><li>一个最小 extension 工程，至少要有：Python 调用层、C++ binding 层、launcher 层、CUDA kernel 层。</li><li>很多手写 kernel 比 PyTorch 慢，不是因为想法错了，而是因为你在和高度优化库对比，或者 benchmark / 访存 / 瓶颈判断出了问题。</li><li>真正值得自己写的，通常是高频、小而热、可融合、shape 较固定、默认实现没有照顾好的算子。</li><li>一个成熟的自定义算子工程，不只是 kernel 本身，还包括正确性验证、稳定 benchmark、profile 和收益判断。</li></ul><h2 id="2-wei-shi-yao-yao-xue-cong-py-torch-dao-cuda-extension" tabindex="-1" id="2-为什么要学从-PyTorch-到-CUDA-extension">2. 为什么要学从 PyTorch 到 CUDA extension</h2><p>前面学的很多东西都偏“硬件视角”：</p><ul><li>warp 和 block 怎么执行</li><li><code>shared memory</code> 为什么有用</li><li><code>coalescing</code> 和 <code>occupancy</code> 怎么看</li><li>Tensor Core 为什么适合矩阵乘</li><li>decode 为什么更像 memory problem</li></ul><p>但在真实 <code>AI infra / 推理</code> 岗位里，常见问题不是：</p><blockquote><p>你会不会解释一个 kernel。</p></blockquote><p>而是：</p><blockquote><p>你能不能把一个优化想法真的落到框架里，并证明它值得做。</p></blockquote><p>这就是 extension 的意义。它是：</p><blockquote><p>框架层和 CUDA 层之间的桥。</p></blockquote><h2 id="3-xian-jian-li-yi-ge-zui-xiao-diao-yong-lian-nao-tu" tabindex="-1" id="3-先建立一个最小调用链脑图">3. 先建立一个最小调用链脑图</h2><p>可以先把整个路径记成这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Python / PyTorch</span><br><span class="line">  -&gt; Python wrapper / torch op</span><br><span class="line">  -&gt; C++ binding</span><br><span class="line">  -&gt; launcher</span><br><span class="line">  -&gt; CUDA kernel</span><br><span class="line">  -&gt; GPU 执行</span><br></pre></td></tr></table></figure><p>真正重要的是理解每一层在干什么。</p><h3 id="3-1-python-ceng" tabindex="-1" id="3-1-Python-层">3.1 Python 层</h3><p>这一层通常负责：</p><ul><li>提供用户接口</li><li>做 shape / dtype 检查</li><li>组织 benchmark 和测试</li><li>方便和模型代码集成</li></ul><h3 id="3-2-c-binding-ceng" tabindex="-1" id="3-2-C-binding-层">3.2 C++ binding 层</h3><p>这一层负责把 Python 调用接到 C++ / CUDA。</p><p>它通常会做：</p><ul><li>接收 <code>torch::Tensor</code></li><li>检查设备、dtype、contiguous</li><li>准备输出 tensor</li><li>调用 launcher</li></ul><h3 id="3-3-launcher-ceng" tabindex="-1" id="3-3-Launcher-层">3.3 Launcher 层</h3><p>这一层通常负责：</p><ul><li>计算 <code>grid / block</code></li><li>取指针</li><li>绑定 stream</li><li>调 CUDA kernel</li></ul><h3 id="3-4-kernel-ceng" tabindex="-1" id="3-4-Kernel-层">3.4 Kernel 层</h3><p>这一层才是你真正优化的地方：</p><ul><li>并行映射</li><li>访存模式</li><li><code>shared memory</code></li><li>warp primitive</li><li>向量化</li><li>Tensor Core 路径</li></ul><p>所以一个很实用的理解是：</p><ul><li>Python / C++ 层决定“这个算子怎么接进系统”</li><li>kernel 层决定“它到底快不快”</li></ul><h2 id="4-wei-shi-yao-hen-duo-shi-hou-yao-zi-ji-xie-suan-zi" tabindex="-1" id="4-为什么很多时候要自己写算子">4. 为什么很多时候要自己写算子</h2><h3 id="4-1-tong-yong-suan-zi-bu-yi-ding-gua-he-ni-de-gong-zuo-fu-zai" tabindex="-1" id="4-1-通用算子不一定适合你的工作负载">4.1 通用算子不一定适合你的工作负载</h3><p>PyTorch 内置算子很强，但它必须兼容很多不同情况：</p><ul><li>各种 shape</li><li>各种 dtype</li><li>各种边界条件</li><li>各种数据布局</li></ul><p>所以它未必对你的固定场景最优。</p><p>特别在推理里，很多常见需求都很像：</p><ul><li>fused bias + activation</li><li>fused add + norm</li><li>特定 KV cache gather / scatter</li><li>特定 layout 的 attention 前后处理</li></ul><p>这些场景的共同点是：</p><blockquote><p>单个操作不大，但调用频繁；如果不融合，就会被 launch 和中间读写拖慢。</p></blockquote><h3 id="4-2-tui-li-xi-tong-zhui-qiu-de-shi-zheng-ti-lu-jing-er-bu-shi-dan-dian-piao-liang" tabindex="-1" id="4-2-推理系统追求的是整体路径，而不是单点漂亮">4.2 推理系统追求的是整体路径，而不是单点漂亮</h3><p>推理里经常关心的是：</p><ul><li><code>TTFT</code></li><li><code>ITL</code></li><li><code>token/s</code></li><li>P99 延迟</li><li>显存占用</li></ul><p>所以写自定义算子往往不是为了炫技，而是为了：</p><ul><li>少一次 kernel launch</li><li>少一次中间 tensor 写回</li><li>少一次无意义的数据搬运</li><li>让系统关键路径更短</li></ul><h2 id="5-yi-ge-zui-xiao-gong-cheng-jie-gou-ying-gai-chang-shi-yao-yang" tabindex="-1" id="5-一个最小工程结构应该长什么样">5. 一个最小工程结构应该长什么样</h2><p>最常见的结构大致是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">my_extension/</span><br><span class="line">  setup.py</span><br><span class="line">  bindings.cpp</span><br><span class="line">  kernel.cu</span><br><span class="line">  wrapper.py</span><br><span class="line">  test_op.py</span><br></pre></td></tr></table></figure><p>这些文件的职责很清楚：</p><ul><li><code>setup.py</code>：构建 extension</li><li><code>bindings.cpp</code>：暴露给 Python 调用的接口</li><li><code>kernel.cu</code>：launcher 和 CUDA kernel</li><li><code>wrapper.py</code>：Python 端封装</li><li><code>test_op.py</code>：正确性和 benchmark</li></ul><p>这里最重要的工程习惯是：</p><blockquote><p>test 和 benchmark 不是可选项，而是算子工程的一部分。</p></blockquote><h2 id="6-zui-xiao-shi-li-zhen-zheng-ying-gai-xue-shi-yao" tabindex="-1" id="6-最小示例真正应该学什么">6. 最小示例真正应该学什么</h2><p>很多教程会用向量加法作为最小示例。</p><p>这个例子本身不重要，重要的是你要看懂它背后的三个点。</p><h3 id="6-1-ni-xie-de-bu-shi-pu-tong-c-han-shu-er-shi-kuang-jia-ke-diao-yong-suan-zi" tabindex="-1" id="6-1-你写的不是普通-C-函数，而是框架可调用算子">6.1 你写的不是普通 C++ 函数，而是框架可调用算子</h3><p>这和“写一个 CUDA 作业”完全不同。</p><p>作业里你只关心 kernel；<br>工程里你要关心：</p><ul><li>外部怎么调用</li><li>输入怎么检查</li><li>输出怎么组织</li><li>怎么方便测试和集成</li></ul><h3 id="6-2-suan-zi-she-ji-yi-ding-yao-dai-shape-si-wei" tabindex="-1" id="6-2-算子设计一定要带-shape-思维">6.2 算子设计一定要带 shape 思维</h3><p>很多 GPU 优化不是“算法更复杂”，而是“shape 特征很清楚”。</p><p>例如：</p><ul><li>hidden size 固定</li><li>head dim 固定</li><li>top-k 固定</li><li>page size 固定</li></ul><p>当 shape 很固定时，自定义 kernel 往往更有价值。</p><h3 id="6-3-yao-xi-guan-launcher-kernel-de-er-ceng-fen-gong" tabindex="-1" id="6-3-要习惯-launcher-kernel-的二层分工">6.3 要习惯 launcher + kernel 的二层分工</h3><p>不要把所有逻辑全堆进 kernel。更好的分工是：</p><ul><li>launcher 负责参数准备和 launch 配置</li><li>kernel 只专注执行</li></ul><p>这样更清晰，也更容易调优。</p><h2 id="7-wei-shi-yao-hen-duo-shou-xie-kernel-fan-er-bi-py-torch-man" tabindex="-1" id="7-为什么很多手写-kernel-反而比-PyTorch-慢">7. 为什么很多手写 kernel 反而比 PyTorch 慢</h2><p>这是面试很爱问的点。</p><h3 id="7-1-ni-zai-he-gao-du-you-hua-ku-bi" tabindex="-1" id="7-1-你在和高度优化库比">7.1 你在和高度优化库比</h3><p>很多时候你以为在和“一个 Python 算子”比，实际上你是在和下面这些比：</p><ul><li><code>cuBLAS</code></li><li><code>cuDNN</code></li><li>vendor-tuned fused kernel</li><li>更成熟的 Triton / CUTLASS / TensorRT 路径</li></ul><p>所以如果你的 kernel 只是“正确但朴素”，跑不过它们很正常。</p><h3 id="7-2-ni-de-fang-cun-mo-shi-ke-neng-hen-chai" tabindex="-1" id="7-2-你的访存模式可能很差">7.2 你的访存模式可能很差</h3><p>高频问题通常不是数学逻辑，而是：</p><ul><li>没有 coalescing</li><li><code>shared memory</code> 用得不对</li><li>寄存器压力太高</li><li>block 配置不合理</li><li>launch 太碎</li></ul><h3 id="7-3-ni-ke-neng-benchmark-cuo-liao" tabindex="-1" id="7-3-你可能-benchmark-错了">7.3 你可能 benchmark 错了</h3><p>常见错误包括：</p><ul><li>没 warmup</li><li>没 <code>synchronize</code></li><li>把 host 开销混进来</li><li>shape 太小，结果主要测到 launch overhead</li></ul><h3 id="7-4-ni-you-hua-liao-ju-bu-que-mei-you-hua-xi-tong-lu-jing" tabindex="-1" id="7-4-你优化了局部，却没优化系统路径">7.4 你优化了局部，却没优化系统路径</h3><p>例如某个小操作快了 2 倍，但它只占总时间 3%，那系统整体几乎不会变。</p><p>这说明你没有先做 profile，没有先找真正热点。</p><h2 id="8-shi-yao-yang-de-suan-zi-zhi-de-zi-ji-xie" tabindex="-1" id="8-什么样的算子值得自己写">8. 什么样的算子值得自己写</h2><p>这题非常重要，因为它体现的是工程判断力。</p><h3 id="8-1-gao-pin-xiao-er-re-rong-yi-rong-he-de-suan-zi" tabindex="-1" id="8-1-高频、小而热、容易融合的算子">8.1 高频、小而热、容易融合的算子</h3><p>例如：</p><ul><li>add + norm</li><li>bias + activation</li><li>某些小型后处理 kernel</li></ul><p>这类算子的问题常常不是 FLOPs 不够，而是：</p><ul><li>中间结果来回写</li><li>launch 次数太多</li></ul><h3 id="8-2-shape-hen-gu-ding-de-suan-zi" tabindex="-1" id="8-2-shape-很固定的算子">8.2 shape 很固定的算子</h3><p>如果你的部署场景非常固定，例如：</p><ul><li>hidden size 固定</li><li>head dim 固定</li><li>top-k 固定</li></ul><p>那通用实现为了兼容各种情况付出的代价，你也许可以绕开。</p><h3 id="8-3-mo-ren-shi-xian-mei-you-zhao-gu-ni-de-shu-ju-bu-ju" tabindex="-1" id="8-3-默认实现没有照顾你的数据布局">8.3 默认实现没有照顾你的数据布局</h3><p>尤其在 attention、KV cache、paged memory 场景，layout 对性能影响非常大。</p><h3 id="8-4-xi-tong-guan-jian-lu-jing-shang-de-re-dian-suan-zi" tabindex="-1" id="8-4-系统关键路径上的热点算子">8.4 系统关键路径上的热点算子</h3><p>例如：</p><ul><li>attention 相关</li><li>norm 相关</li><li>sampling / top-k</li><li>KV cache gather / scatter</li></ul><h2 id="9-yi-ge-hen-dian-xing-de-tui-li-suan-zi-si-lu-fused-add-norm" tabindex="-1" id="9-一个很典型的推理算子思路：fused-add-norm">9. 一个很典型的推理算子思路：fused add + norm</h2><p>这类算子很有代表性，因为它体现了推理优化的核心逻辑：</p><blockquote><p>不是做更复杂的数学，而是减少中间结果回写和重复读写。</p></blockquote><p>如果原始路径是：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">x = x + residual</span><br><span class="line">x = layer_norm(x)</span><br></pre></td></tr></table></figure><p>拆成两个 kernel 时，可能会发生：</p><ol><li>add 写回一次</li><li>norm 再读一次</li><li>norm 输出再写一次</li></ol><p>而 fused kernel 的思路是：</p><ul><li>一次读入需要的数据</li><li>在寄存器或更近层级中继续处理</li><li>最后尽量只写一次结果</li></ul><p>这类融合的收益通常不在“数学更少”，而在：</p><ul><li>launch 更少</li><li>global round-trip 更少</li><li>locality 更好</li></ul><h2 id="10-xie-zi-ding-yi-suan-zi-shi-kernel-she-ji-gai-zen-yao-xiang" tabindex="-1" id="10-写自定义算子时，kernel-设计该怎么想">10. 写自定义算子时，kernel 设计该怎么想</h2><p>这里给一个非常实用的五步法。</p><h3 id="10-1-di-yi-bu-xian-pan-duan-ping-jing" tabindex="-1" id="10-1-第一步：先判断瓶颈">10.1 第一步：先判断瓶颈</h3><p>先问：</p><ul><li>是 <code>compute-bound</code></li><li>还是 <code>memory-bound</code></li><li>还是 <code>launch-bound</code></li></ul><p>不同瓶颈，对应完全不同的优化方向。</p><h3 id="10-2-di-er-bu-que-ding-bing-xing-ying-she" tabindex="-1" id="10-2-第二步：确定并行映射">10.2 第二步：确定并行映射</h3><p>例如：</p><ul><li>一个 thread 一个元素</li><li>一个 warp 一行</li><li>一个 block 一个 token / 一个 row / 一个 tile</li></ul><p>并行映射决定：</p><ul><li>coalescing 是否自然</li><li>reduction 是否方便</li><li><code>shared memory</code> 值不值得用</li><li>occupancy 是否容易受限</li></ul><h3 id="10-3-di-san-bu-que-ding-shu-ju-ban-yun-lu-jing" tabindex="-1" id="10-3-第三步：确定数据搬运路径">10.3 第三步：确定数据搬运路径</h3><p>明确：</p><ul><li>哪些数据进寄存器</li><li>哪些进 <code>shared memory</code></li><li>哪些必须从 global memory 读</li><li>有没有重复读机会可以缓存</li></ul><h3 id="10-4-di-si-bu-que-ding-tong-bu-fan-wei" tabindex="-1" id="10-4-第四步：确定同步范围">10.4 第四步：确定同步范围</h3><ul><li>thread 内：寄存器</li><li>warp 内：优先 warp primitive</li><li>block 内：<code>shared memory</code> + <code>__syncthreads()</code></li><li>block 间：通常拆成多 kernel 或借助更高层机制</li></ul><h3 id="10-5-di-wu-bu-xian-correctness-zai-benchmark-zai-profile" tabindex="-1" id="10-5-第五步：先-correctness，再-benchmark，再-profile">10.5 第五步：先 correctness，再 benchmark，再 profile</h3><p>顺序不要反。</p><p>正确顺序通常是：</p><ol><li>正确性</li><li>稳定 benchmark</li><li>profile 定位瓶颈</li><li>再优化</li></ol><h2 id="11-benchmark-he-zheng-que-xing-yan-zheng-wei-shi-yao-bi-xu-du-li-kan" tabindex="-1" id="11-benchmark-和正确性验证为什么必须独立看">11. benchmark 和正确性验证为什么必须独立看</h2><h3 id="11-1-benchmark-zhi-shao-yao-zuo-dao-shi-yao" tabindex="-1" id="11-1-benchmark-至少要做到什么">11.1 benchmark 至少要做到什么</h3><p>至少要有：</p><ul><li>warmup</li><li><code>torch.cuda.synchronize()</code></li><li>多次迭代平均</li></ul><p>否则测出来的往往只是异步提交时间或初始化噪声。</p><h3 id="11-2-zheng-que-xing-yan-zheng-zhi-shao-yao-fu-gai-shi-yao" tabindex="-1" id="11-2-正确性验证至少要覆盖什么">11.2 正确性验证至少要覆盖什么</h3><p>至少要做：</p><ul><li>和 baseline 对比</li><li>多 shape 测试</li><li>多 dtype 测试</li><li>边界条件测试</li></ul><p>真正能上线的算子，正确性和性能同等重要。</p><h2 id="12-shi-yao-shi-hou-bu-gai-zi-ji-xie" tabindex="-1" id="12-什么时候不该自己写">12. 什么时候不该自己写</h2><p>这同样重要。</p><h3 id="12-1-yi-you-cheng-shou-ku-yi-jing-fei-chang-hao" tabindex="-1" id="12-1-已有成熟库已经非常好">12.1 已有成熟库已经非常好</h3><p>如果热点就是标准 GEMM、标准卷积，而且成熟库已经很强，自己写通常性价比不高。</p><h3 id="12-2-re-dian-gen-ben-bu-zai-zhe-li" tabindex="-1" id="12-2-热点根本不在这里">12.2 热点根本不在这里</h3><p>如果系统瓶颈不在这个算子，写了也很难有整体收益。</p><h3 id="12-3-shape-bu-gu-ding-wei-hu-cheng-ben-tai-gao" tabindex="-1" id="12-3-shape-不固定，维护成本太高">12.3 shape 不固定，维护成本太高</h3><p>如果输入变化太大，你可能要为很多边界情况付出大量维护成本。</p><h3 id="12-4-zheng-que-xing-he-ke-wei-hu-xing-feng-xian-da-yu-shou-yi" tabindex="-1" id="12-4-正确性和可维护性风险大于收益">12.4 正确性和可维护性风险大于收益</h3><p>如果收益很小，但验证和维护代价很大，工程上就不值得。</p><p>所以一个成熟判断是：</p><blockquote><p>不是所有能写 custom op 的地方，都值得写 custom op。</p></blockquote><h2 id="13-he-tensor-rt-triton-cutlass-de-guan-xi-zen-yao-li-jie" tabindex="-1" id="13-和-TensorRT、Triton、CUTLASS-的关系怎么理解">13. 和 TensorRT、Triton、CUTLASS 的关系怎么理解</h2><p>这一题经常被追问。</p><h3 id="13-1-tensor-rt" tabindex="-1" id="13-1-TensorRT">13.1 TensorRT</h3><p>更偏推理引擎和图级优化，它不只是写 kernel，而是在做更完整的推理执行优化。</p><h3 id="13-2-triton" tabindex="-1" id="13-2-Triton">13.2 Triton</h3><p>更像一种更高层的 GPU kernel 编写方式，适合很多算子级优化场景。</p><h3 id="13-3-cutlass" tabindex="-1" id="13-3-CUTLASS">13.3 CUTLASS</h3><p>更偏高性能矩阵和 Tensor Core 路径的模板化组件库。</p><h3 id="13-4-shou-xie-py-torch-cuda-extension" tabindex="-1" id="13-4-手写-PyTorch-CUDA-extension">13.4 手写 PyTorch CUDA extension</h3><p>更适合做：</p><ul><li>特定场景定制</li><li>框架集成</li><li>小而热的定制 kernel</li></ul><p>这几者不是简单替代关系，更像不同抽象层。</p><h2 id="14-zhe-yi-pian-bi-xu-ji-zhu-de-ji-ju-hua" tabindex="-1" id="14-这一篇必须记住的几句话">14. 这一篇必须记住的几句话</h2><ul><li>自定义算子的价值，通常不在“更低级”，而在“更贴近你的 shape、layout 和系统关键路径”。</li><li>一个最小 extension 工程，至少包括 Python 层、C++ binding 层、launcher 和 kernel 层。</li><li>很多手写 kernel 比 PyTorch 慢，不是因为 custom op 没价值，而是因为你在和高度优化库比，或者 benchmark / 访存 / 瓶颈判断出了问题。</li><li>值得自己写的，通常是高频、小而热、可融合、shape 较固定的算子。</li><li>一个成熟的算子工程，不只是 kernel 本身，还包括 correctness、benchmark、profile 和收益判断。</li><li>不是所有能写 custom op 的地方，都值得写。</li></ul><h2 id="15-jing-jian-ban-mian-shi-biao-da" tabindex="-1" id="15-精简版面试表达">15. 精简版面试表达</h2><p>如果面试官问你怎么理解 PyTorch 到 CUDA extension 的工程落地，可以这样答：</p><blockquote><p>我会先看这个算子是不是系统关键路径上的热点，shape 是否比较固定，默认实现是否没有照顾我的数据布局或融合需求。如果值得做，我会把它拆成 Python 接口、C++ binding、launcher 和 CUDA kernel 四层，先做正确性，再做稳定 benchmark，再用 profile 看它到底是 compute-bound、memory-bound 还是 launch-bound。很多时候自定义算子的真正收益不在单个 kernel 的绝对速度，而在于减少 launch、减少中间结果回写、让整个推理路径更贴近 GPU 的执行方式。</p></blockquote><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/3f7ace4/">GPU系统拆解-00-导读：从架构认知到推理系统的学习路线</a></li><li>上一篇：<a href="/posts/68e6bad5/">GPU系统拆解-13-多 GPU 与通信：并行策略、拓扑与扩展代价</a></li><li>延伸阅读：<a href="/posts/846fbbaf/">PyTorch推理工程-00-系列导读与学习路线</a></li><li>延伸阅读：<a href="/posts/eb522a31/">Triton-04-Matmul Autotune与PyTorch集成</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;gpu-xi-tong-chai-jie-14-cong-py-torch-dao-cuda-extension-ba-suan-zi-zhen-zheng-jie-ru-gong-cheng&quot; tabindex=&quot;-1&quot;&gt;GPU系统拆解-14-从</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="GPU" scheme="https://smarter.xin/categories/ai-infra/gpu/"/>
    
    
    <category term="GPU" scheme="https://smarter.xin/tags/gpu/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="PyTorch Extension" scheme="https://smarter.xin/tags/pytorch-extension/"/>
    
    <category term="PyTorch" scheme="https://smarter.xin/tags/pytorch/"/>
    
    <category term="Kernel 开发" scheme="https://smarter.xin/tags/kernel-development/"/>
    
  </entry>
  
</feed>
