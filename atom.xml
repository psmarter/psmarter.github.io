<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Smarter&#39;s blog</title>
  
  <subtitle>要学习、要快乐</subtitle>
  <link href="https://smarter.xin/atom.xml" rel="self"/>
  
  <link href="https://smarter.xin/"/>
  <updated>2026-04-03T11:44:50.292Z</updated>
  <id>https://smarter.xin/</id>
  
  <author>
    <name>Smarter</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>vLLM系统拆解-19-AI Infra与推理岗面试手册：vLLM学到什么程度才算够用</title>
    <link href="https://smarter.xin/posts/cc221b1e/"/>
    <id>https://smarter.xin/posts/cc221b1e/</id>
    <published>2026-04-02T09:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.292Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-19-ai-infra-yu-tui-li-gang-mian-shi-shou-ce-v-llm-xue-dao-shi-yao-cheng-du-cai-suan-gou-yong" tabindex="-1">vLLM系统拆解-19-AI Infra与推理岗面试手册：vLLM学到什么程度才算够用</h1><p>这篇是整个系列的收尾，不追加新知识点，只做一件事：把前 18 篇压缩成面试实战手册。</p><p>目标是清楚回答三个问题：你该会讲什么、该怎么组织答案、该如何把 vLLM 讲成系统能力而不是背概念。</p><hr><h2 id="san-ceng-xue-xi-mu-biao" tabindex="-1" id="三层学习目标">三层学习目标</h2><p>&quot;学会 vLLM&quot;不等于会用 <code>vllm serve</code>，也不等于能背出 PagedAttention 的名字。对 AI Infra / 推理岗来说，竞争力来自下面三层，缺一不可：</p><table><thead><tr><th>层级</th><th>代表能力</th><th>面试价值</th></tr></thead><tbody><tr><td><strong>第一层：会解释</strong></td><td>能清楚说明 vLLM 是什么、为什么快、解决了什么问题、和 HF <code>generate()</code> 的差距在哪</td><td>基本门槛</td></tr><tr><td><strong>第二层：会拆解</strong></td><td>能把 vLLM 分成几个核心子系统，说清每个子系统的职责、为什么这样设计、不这样会怎样、子系统如何配合</td><td>有基础</td></tr><tr><td><strong>第三层：会迁移</strong></td><td>能区分哪些是 vLLM 特有实现、哪些是通用 LLM 推理系统都绕不开的共性；如果不用 vLLM 自己设计，大致知道该走哪些路</td><td>最加分</td></tr></tbody></table><p>第三层是 AI Infra 面试真正拉开差距的地方。</p><hr><h2 id="wu-ge-he-xin-xin-zhi-mo-xing" tabindex="-1" id="五个核心心智模型">五个核心心智模型</h2><p>这五个模型是理解 vLLM 所有机制的基础。缺少其中任何一个，讲具体机制时都容易讲虚。</p><h3 id="1-llm-tui-li-prefill-decode-liang-zhe-ping-jing-bu-tong" tabindex="-1" id="1-LLM-推理-Prefill-Decode，两者瓶颈不同">1. LLM 推理 = Prefill + Decode，两者瓶颈不同</h3><p>Prefill 把整段 prompt 喂给模型，建立初始上下文并把对应的 K/V 写入 KV Cache，<strong>计算密集</strong>；Decode 每步只生成少量新 token 但必须读取所有历史 KV Cache，<strong>显存/带宽密集</strong>。</p><p>这个差异直接决定了 vLLM 的核心设计取舍：</p><ul><li>长 prefill 会长时间阻塞 decode → 需要 chunked prefill</li><li>Prefill 和 decode 竞争同一轮算力预算 → 需要统一 token budget 调度</li><li>TTFT 和 ITL 对应不同阶段，优化方向不同 → 不可能同时极致</li></ul><p>讲调度器前没有讲清 prefill/decode 差异，后面的内容就容易站不住脚。</p><h3 id="2-zai-xian-fu-wu-de-ping-jing-jing-chang-bu-shi-flo-ps-er-shi-kv-cache" tabindex="-1" id="2-在线服务的瓶颈经常不是-FLOPs，而是-KV-Cache">2. 在线服务的瓶颈经常不是 FLOPs，而是 KV Cache</h3><p>模型权重相对固定，但 KV Cache 随请求数和上下文长度动态增长，波动范围大。一个模型能放进显存，不等于它能服务得好——高并发、长请求下，KV Cache 的增长速度可能会把显存挤爆。</p><p>vLLM 的设计起点就在这里：<strong>把 KV Cache 当成核心系统资源来管理，而不是 forward 的附属品</strong>。</p><h3 id="3-v-llm-de-he-xin-bu-shi-yi-ge-kernel-er-shi-yi-tao-zi-yuan-bian-pai-xi-tong" tabindex="-1" id="3-vLLM-的核心不是一个-kernel，而是一套资源编排系统">3. vLLM 的核心不是一个 kernel，而是一套资源编排系统</h3><p>PagedAttention 是 vLLM 最著名的机制，但把 vLLM 等同于 PagedAttention 是不准确的。真正让系统有优势的是这条完整链条：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">请求进入</span><br><span class="line">  → 调度器决定谁先执行，token budget 如何分配</span><br><span class="line">  → KV blocks 分配 / 回收 / prefix cache 是否命中</span><br><span class="line">  → worker 执行本轮 forward</span><br><span class="line">  → sampler 产出 token</span><br><span class="line">  → 输出流回客户端</span><br></pre></td></tr></table></figure><p>面试里最好把 vLLM 描述为&quot;一个围绕请求调度、KV Cache 管理和 GPU 执行链组织起来的推理服务系统&quot;，而不是&quot;一个用了 PagedAttention 的框架&quot;。</p><h3 id="4-tun-tu-he-yan-chi-shi-quan-heng-bu-shi-tong-shi-zui-da-hua" tabindex="-1" id="4-吞吐和延迟是权衡，不是同时最大化">4. 吞吐和延迟是权衡，不是同时最大化</h3><p>以 <code>max_num_batched_tokens</code> 为例：</p><table><thead><tr><th>方向</th><th>吞吐影响</th><th>延迟影响</th></tr></thead><tbody><tr><td>调大</td><td>更容易组成大 batch，吞吐通常更高</td><td>decode 可能被大 prefill 拖住，ITL 恶化</td></tr><tr><td>调小</td><td>GPU 利用率可能下降，吞吐可能损失</td><td>decode 响应更敏捷，ITL 往往更好</td></tr></tbody></table><p>优化不是&quot;总能更快&quot;，而是&quot;在不同 workload 下追求不同指标的平衡&quot;。这句话要真正内化，不能只停在字面上。</p><h3 id="5-hao-de-tui-li-xi-tong-bi-xu-dui-workload-min-gan" tabindex="-1" id="5-好的推理系统必须对-workload-敏感">5. 好的推理系统必须对 workload 敏感</h3><p>不存在对所有场景都最优的静态配置：</p><ul><li>Prefix caching 的收益取决于前缀复用率</li><li>Speculative decoding 是否有效取决于负载与模型特性</li><li>Chunked prefill 的价值取决于长 prompt 和 decode 混跑情况</li><li>TP / PP / DP / EP 的选择取决于模型大小和通信代价</li></ul><p>真正的 AI Infra 思维，是知道特性对什么 workload 有效，而不是会背特性列表。</p><hr><h2 id="mian-shi-li-bi-xu-hui-jiang-de-8-ge-zhu-ti" tabindex="-1" id="面试里必须会讲的-8-个主题">面试里必须会讲的 8 个主题</h2><p>这 8 个主题覆盖了 AI Infra / 推理岗面试中关于 vLLM 的绝大多数提问。每个主题都给出&quot;应该怎么讲&quot;的要点。</p><h3 id="zhu-ti-1-v-llm-shi-shi-yao" tabindex="-1" id="主题-1：vLLM-是什么">主题 1：vLLM 是什么</h3><blockquote><p>vLLM 不是单纯的模型调用库，而是面向在线大模型服务的推理引擎。它的重点不是&quot;单次生成能不能跑&quot;，而是&quot;面对大量不同长度请求时，怎样更高效地利用显存和 GPU 计算资源，在吞吐、TTFT 和 ITL 之间做更好的权衡&quot;。</p></blockquote><p>这段话解决定位问题。一句话里包含四个关键词：<strong>在线服务、高吞吐、显存高效、很多不同长度请求同时生成</strong>。</p><h3 id="zhu-ti-2-wei-shi-yao-v-llm-kuai" tabindex="-1" id="主题-2：为什么-vLLM-快">主题 2：为什么 vLLM 快</h3><p>不要只答&quot;因为 PagedAttention&quot;。完整答案需要覆盖多个层次：</p><ol><li>KV Cache 分页化管理，减少碎片，提高显存利用率</li><li>Continuous batching / unified scheduling，让动态请求更容易持续组成有效 batch</li><li>Chunked prefill，减少长 prompt 对 decode 的阻塞</li><li>Prefix caching，在高重复前缀场景下降低重复 prefill 成本</li><li>CUDA graph、优化 kernel、量化等进一步减少执行开销</li></ol><p>快不是因为某一个点神奇，而是多个层次同时优化。</p><h3 id="zhu-ti-3-paged-attention-jie-jue-de-ben-zhi-wen-ti" tabindex="-1" id="主题-3：PagedAttention-解决的本质问题">主题 3：PagedAttention 解决的本质问题</h3><blockquote><p>它解决的是在线服务场景下，KV Cache 随请求动态增长时的内存分配与碎片化问题。传统连续分配在高并发、不同长度请求混合时会造成严重浪费和管理困难；PagedAttention 把 KV Cache 切成固定 block，以逻辑连续、物理非连续的方式管理，降低碎片、提高可扩展性，让系统更容易支持大 batch 和请求动态进出。</p></blockquote><p>关键词：动态增长、不同长度混合、逻辑连续/物理非连续、支持在线调度。</p><h3 id="zhu-ti-4-diao-du-qi-wei-shi-yao-shi-he-xin" tabindex="-1" id="主题-4：调度器为什么是核心">主题 4：调度器为什么是核心</h3><p>在线请求不是静态 batch——新请求随时进入、老请求随时结束、有的在 prefill 有的在 decode、token budget 有限、KV Cache 空间有限。</p><p>调度器要做的是：决定本轮哪些请求执行、优先保证哪些 decode、给 prefill 分配多少预算、是否对 prefill 做 chunking、KV Cache 不够时如何 preempt，在吞吐和延迟之间维持合理平衡。</p><p>一句话：<strong>把有限 GPU 时间片和显存预算分配给动态请求集合的中枢。</strong></p><h3 id="zhu-ti-5-prefix-caching-zhen-zheng-de-jie-zhi-zai-na" tabindex="-1" id="主题-5：Prefix-Caching-真正的价值在哪">主题 5：Prefix Caching 真正的价值在哪</h3><p>它真正值钱的场景：相同 system prompt、相同工具描述、相同 few-shot 模板、大量相似前缀请求——这在企业服务、Agent 编排、固定模板业务中非常常见。</p><blockquote><p>Prefix caching 把原本&quot;每个请求都重复做一遍的前缀 prefill&quot;，变成&quot;跨请求复用的公共计算结果&quot;。</p></blockquote><p>从业务场景讲，比从技术名词讲更有说服力。也要知道它的边界：前缀复用率低时收益不明显。</p><h3 id="zhu-ti-6-chunked-prefill-wei-shi-yao-zhong-yao" tabindex="-1" id="主题-6：Chunked-Prefill-为什么重要">主题 6：Chunked Prefill 为什么重要</h3><p>不要讲成&quot;prefill 切小一点更灵活&quot;这种太表面的话。</p><blockquote><p>在在线服务里，长 prompt prefill 容易占满本轮 token budget，拖住 decode 请求。Chunked prefill 的价值在于把大 prefill 拆开后，与 decode 请求更细粒度地交织执行——让系统既能继续推进长 prompt，又不至于让已经在生成的请求长时间等不到下一 token。</p></blockquote><p>关键词：减小阻塞、更细粒度交织、服务体验更平滑。</p><h3 id="zhu-ti-7-xian-cun-man-bu-deng-yu-xi-tong-hao-xian-cun-mei-man-ye-bu-deng-yu-xi-tong-chai" tabindex="-1" id="主题-7：显存满不等于系统好，显存没满也不等于系统差">主题 7：显存满不等于系统好，显存没满也不等于系统差</h3><p>显存高利用率通常说明资源吃得比较充分，但系统&quot;好不好&quot;需要综合看：</p><ul><li>吞吐</li><li>TTFT 和 ITL</li><li>preemption 是否频繁</li><li>GPU 利用率是否稳定</li><li>CPU 是否饱和</li><li>不同长度请求是否互相拖累</li></ul><p>有时为了更好 ITL 会故意不把 token budget 撑到最大；有时显存还有富余，系统却被 CPU、调度或采样环节拖住。&quot;显存使用率&quot;只是一个信号，不是结论。</p><h3 id="zhu-ti-8-v-llm-de-si-xiang-neng-fou-qian-yi-dao-bie-de-xi-tong" tabindex="-1" id="主题-8：vLLM-的思想能否迁移到别的系统">主题 8：vLLM 的思想能否迁移到别的系统</h3><p>可以，而且这是最应该体现出的地方。可迁移的核心思想：</p><ul><li>在线推理必须显式管理 KV Cache</li><li>动态请求场景需要 continuous batching / unified scheduling</li><li>大模型服务是&quot;资源编排问题&quot;，不是只靠单个 kernel</li><li>吞吐 / TTFT / ITL 必然存在权衡</li><li>Workload-aware optimization 比静态最佳实践更重要</li></ul><p>能讲到这一层，面试官会觉得你学到的是推理系统设计思想，而不是某个框架的说明书。</p><hr><h2 id="san-chong-da-an-ban-ben" tabindex="-1" id="三种答案版本">三种答案版本</h2><p>面对不同的面试场景，答案的深度和展开方式应当不同。</p><p><strong>30 秒版</strong>（适合自我介绍、“你了解 vLLM 吗”）：</p><blockquote><p>vLLM 是面向在线大模型服务的推理引擎。我理解它不是停留在会用 API 层，而是关注它为什么快：通过分页式 KV Cache 管理减少碎片、提升显存利用率，通过持续批处理和统一调度更好处理动态请求，通过 prefix caching 和 chunked prefill 在吞吐和延迟之间做更合理的权衡。我更关注它背后的推理系统设计思想。</p></blockquote><p><strong>3 分钟版</strong>（适合一般技术面、“展开讲讲 vLLM”）：</p><blockquote><p>我把 vLLM 看成一个推理服务系统，而不是普通 inference library。在线 LLM 服务的难点不只是单次 forward，而是很多不同长度请求并发进入系统，每个请求的 KV Cache 还会动态增长，所以真正关键的是请求调度和显存管理。</p><p>vLLM 的核心创新之一是 PagedAttention，把 KV Cache 切成固定 block，以逻辑连续、物理非连续的方式管理，缓解了连续分配带来的碎片问题，让系统更容易支撑高并发和更大 batch。</p><p>但它真正形成系统优势，不只是因为这个 attention 机制，而是配合了 continuous batching、unified scheduler、prefix caching、chunked prefill 等设计。例如长 prompt 容易拖慢 decode，所以 chunked prefill 会把大 prefill 切开，与 decode 更细粒度混排；如果很多请求共享相同 system prompt，prefix caching 又可以避免重复 prefill。</p><p>所以我对 vLLM 的理解重点是：它把在线推理变成了一个围绕 token budget、KV Cache 和动态请求集合的资源编排问题。</p></blockquote><p><strong>10 分钟版</strong>（适合核心岗位深度面）——建议按以下顺序展开，不要跳步：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">在线推理场景的本质问题</span><br><span class="line">  → prefill / decode 差异及瓶颈</span><br><span class="line">  → KV Cache 动态增长与显存压力</span><br><span class="line">  → PagedAttention（block 化 KV 管理）</span><br><span class="line">  → unified scheduler（token budget 分配，动态调度）</span><br><span class="line">  → prefix caching / chunked prefill</span><br><span class="line">  → 吞吐 / TTFT / ITL trade-off</span><br><span class="line">  → 背后的通用设计思想（可迁移部分）</span><br></pre></td></tr></table></figure><p>目标是练成&quot;自然展开&quot;，不是背稿。</p><hr><h2 id="di-shui-ping-biao-shu-dui-zhao" tabindex="-1" id="低水平表述对照">低水平表述对照</h2><p>这四类说法在面试里会暴露理解深度不足，附上更准确的替代表述：</p><table><thead><tr><th>低水平表述</th><th>问题所在</th><th>更好的说法</th></tr></thead><tbody><tr><td>“vLLM 快是因为它用了 PagedAttention”</td><td>过于单点化，忽略系统层组合</td><td>“PagedAttention 是显存高效的关键基础，但系统级速度来自 KV Cache 管理、调度、continuous batching、chunked prefill、prefix caching 多层配合”</td></tr><tr><td>“Prefix caching 就是缓存 prompt”</td><td>太粗糙，丢失了 KV block 复用的本质</td><td>“Prefix caching 是对共享前缀对应的 KV blocks 做跨请求复用，把重复 prefill 结果缓存下来，减少重复计算”</td></tr><tr><td>“Scheduler 就是把请求拼成 batch”</td><td>太浅，没有体现动态性和资源视角</td><td>“Scheduler 的核心是把有限 token budget 和 KV Cache 空间分配给动态请求集合，决定哪些请求本轮执行、如何平衡 decode 和 prefill、何时 chunk、何时 preempt”</td></tr><tr><td>“显存越满越好”</td><td>典型误区</td><td>“显存高利用率通常说明资源吃得比较充分，但还要结合 TTFT、ITL、吞吐、preemption 频率和 CPU/GPU 协调效率一起判断”</td></tr></tbody></table><hr><h2 id="san-lei-chang-jian-zhui-wen-ji-ying-dui" tabindex="-1" id="三类常见追问及应对">三类常见追问及应对</h2><p><strong>追问方向 1：你真的理解&quot;为什么这样设计&quot;吗？</strong></p><p>这类问题的形式通常是：“为什么要做分页而不是连续分配”、“为什么 prefix caching 不是天然总能收益”、“为什么 decode 优先通常更有利于用户体验”。</p><p>回答框架：约束条件是什么 → 原方案的问题是什么 → 新方案解决了什么 → 新方案带来了什么代价。只说&quot;更高效&quot;是不够的。</p><p><strong>追问方向 2：你能解释 trade-off 吗？</strong></p><p>这类问题的形式通常是：“为什么调大 batch 不一定更好”、“为什么 speculative decoding 不是所有场景都提速”、“为什么 TP 不一定越大越快”。</p><p>回答时必须点出：不同 workload，不同瓶颈，不同指标，取舍不同。workload 没变化，这类问题没有固定答案。</p><p><strong>追问方向 3：你能把现象和系统模块对应起来吗？</strong></p><p>这类问题的形式通常是：“TTFT 变高先怀疑哪里”、“ITL 变差可能是什么原因”、“明明显存没爆为什么还是慢”。</p><p>建议的排查顺序：</p><ol><li>workload 形态（请求长度分布、并发）</li><li>调度策略（token budget 设置、preemption 频率）</li><li>KV Cache 压力（cache hit rate、block 使用率）</li><li>CPU 资源（engine core 是否饱和）</li><li>GPU 利用率（是否在做有效计算）</li><li>通信/并行开销（多卡场景）</li><li>额外功能负担（sampler、logprobs 等）</li><li>模型结构因素（GQA、MoE 路由等）</li></ol><hr><h2 id="ru-guo-bei-wen-quot-ni-mei-gai-guo-yuan-ma-ping-shi-yao-shuo-li-jie-v-llm-quot" tabindex="-1" id="如果被问-你没改过源码，凭什么说理解-vLLM">如果被问&quot;你没改过源码，凭什么说理解 vLLM&quot;</h2><p>这个问题很现实，可以这样回答：</p><blockquote><p>我没有把自己包装成 vLLM 核心贡献者，但我对它的理解不停留在 API 使用层。我重点学习了请求主流程、V1 架构、调度逻辑、KV Cache 管理、prefix caching、chunked prefill、性能权衡和分布式扩展，并且把这些机制放回通用 LLM 推理系统设计里去理解。</p><p>所以我不强调&quot;改过多少源码&quot;，而强调&quot;知道它为什么这样设计、这个设计解决了什么问题、在什么 workload 下有效、又会带来什么代价&quot;。</p></blockquote><p>不装、不虚，也不把自己说低了。</p><hr><h2 id="v-llm-zhi-wai-huan-yao-bu-shi-yao" tabindex="-1" id="vLLM-之外还要补什么">vLLM 之外还要补什么</h2><p>vLLM 是很好的切入点，但不是推理方向的全部。后续建议补充四类基础：</p><table><thead><tr><th>方向</th><th>关键内容</th></tr></thead><tbody><tr><td><strong>LLM 推理基础</strong></td><td>Transformer 推理流程、KV Cache 原理、RoPE/GQA/MQA/MLA 结构差异、sampling 基础、logprobs/beam/speculative 等解码机制</td></tr><tr><td><strong>GPU 与系统基础</strong></td><td>CUDA 基本执行模型、显存层次与带宽瓶颈、kernel launch 开销、CUDA graph 意义、NCCL/多卡通信基本常识</td></tr><tr><td><strong>推理生态横向认知</strong></td><td>HF Transformers（模型库层）、TGI、TensorRT-LLM、SGLang、Triton Inference Server（服务化）、FlashAttention/FlashInfer（算子生态）</td></tr><tr><td><strong>部署与调优</strong></td><td>吞吐/TTFT/ITL 指标体系、压测方法、日志与 profiling、CPU/GPU/通信/KV Cache 瓶颈定位、不同 workload 下的参数调优</td></tr></tbody></table><p>把 vLLM 当主线，但不要把视野局限在 vLLM 上。</p><hr><h2 id="5-ge-zui-zhong-mu-biao" tabindex="-1" id="5-个最终目标">5 个最终目标</h2><p>学完这 19 篇，不是&quot;把文档都看完了&quot;。衡量是否真正学到的标准是做到这 5 件事：</p><ol><li>能用自己的话把 vLLM 讲清楚，不是照稿念</li><li>能解释它为什么这样设计，以及设计的收益和代价</li><li>能把调度、KV Cache、prefix caching、chunked prefill 串成一张系统图</li><li>能把性能现象映射到可能的系统原因</li><li>能把 vLLM 上升到通用 LLM 推理系统设计思想层面</li></ol><hr><h2 id="10-ju-hua-de-zui-zhong-kuang-jia" tabindex="-1" id="10-句话的最终框架">10 句话的最终框架</h2><p>这 10 句是这整个系列可以压缩成的最精简形式：</p><ol><li>vLLM 是在线大模型服务引擎，不只是 inference library</li><li>在线推理的难点是动态请求集合，不是单次 forward</li><li>LLM 推理分成 Prefill 和 Decode，两者瓶颈不同</li><li>KV Cache 是在线服务里的核心资源之一</li><li>PagedAttention 用 block/page 化方式管理 KV Cache，缓解碎片问题</li><li>vLLM 的核心优势来自系统层组合优化，而不是单一算子</li><li>Scheduler 负责把 token budget 和显存预算分配给动态请求集合</li><li>Prefix caching 适合高前缀复用场景，本质是跨请求复用 KV 结果</li><li>Chunked prefill 的价值在于减少长 prefill 对 decode 的阻塞</li><li>真正的优化要结合 workload，看吞吐、TTFT、ITL 的平衡</li></ol><hr><h2 id="jie-xia-lai-san-jian-shi" tabindex="-1" id="接下来三件事">接下来三件事</h2><p>把这 19 篇读完是起点，不是终点。接下来最值得做的三件事：</p><p><strong>第一：把三种答案版本练熟。</strong> 不是背，是练成自然展开。能在不同时间压力下稳定讲清楚，比能背出所有细节更有用。</p><p><strong>第二：按系统主线把前 18 篇重连一遍。</strong> 只抓这条链：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">在线服务问题</span><br><span class="line">  → prefill/decode 差异</span><br><span class="line">  → KV Cache 动态增长</span><br><span class="line">  → PagedAttention</span><br><span class="line">  → unified scheduler</span><br><span class="line">  → prefix caching</span><br><span class="line">  → chunked prefill</span><br><span class="line">  → 性能 trade-off</span><br><span class="line">  → 分布式扩展</span><br></pre></td></tr></table></figure><p><strong>第三：开始做口头表达训练。</strong> 真正缺的往往不是知识，而是组织能力、层次感、追问时不乱。这只能靠实际开口练。</p><hr><p>自检标准：</p><blockquote><p>如果你能从在线服务场景出发，说清楚 vLLM 为什么要引入分页式 KV Cache、统一调度、持续批处理、前缀复用和分块 prefill，也能说明这些设计各自解决了什么问题、彼此如何协同，以及在不同 workload 下有哪些收益和代价——那这个系列就学到位了。</p></blockquote><hr><h2 id="guan-fang-can-kao" tabindex="-1" id="官方参考">官方参考</h2><ul><li><a href="https://docs.vllm.ai/en/latest/design/arch_overview/">vLLM Architecture Overview</a></li><li><a href="https://docs.vllm.ai/en/latest/usage/v1_guide/">vLLM V1 Guide</a></li><li><a href="https://docs.vllm.ai/en/stable/configuration/optimization/">Optimization and Tuning</a></li><li><a href="https://docs.vllm.ai/en/stable/serving/parallelism_scaling/">Parallelism and Scaling</a></li><li><a href="https://github.com/vllm-project/vllm">vLLM GitHub</a></li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/6f0af891/">vLLM系统拆解-18-通用LLM推理系统设计：从vLLM出发建立框架</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-19-ai-infra-yu-tui-li-gang-mian-shi-shou-ce-v-llm-xue-dao-shi-yao-cheng-du-cai-suan-gou-yong&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="推理系统" scheme="https://smarter.xin/tags/inference-system/"/>
    
    <category term="面试" scheme="https://smarter.xin/tags/interview/"/>
    
    <category term="系统设计" scheme="https://smarter.xin/tags/system-design/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-18-通用LLM推理系统设计：从vLLM出发建立框架</title>
    <link href="https://smarter.xin/posts/6f0af891/"/>
    <id>https://smarter.xin/posts/6f0af891/</id>
    <published>2026-04-02T09:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.292Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-18-tong-yong-llm-tui-li-xi-tong-she-ji-cong-v-llm-chu-fa-jian-li-kuang-jia" tabindex="-1">vLLM系统拆解-18-通用LLM推理系统设计：从vLLM出发建立框架</h1><p>前面十几篇把 vLLM 的核心机制都拆解过了：PagedAttention、prefix caching、unified scheduler、chunked prefill、speculative decoding。但有一个问题没正面回答：这些设计里，哪些是 vLLM 独有的，哪些其实是任何成熟推理系统都绕不开的？</p><p>这篇文章换一个视角——不再盯着 vLLM 的实现细节，而是把它当作一个具体案例，用来拼出一套通用 LLM 推理系统的设计框架。</p><p>读完这篇，你应该能做到：</p><ul><li>把 vLLM 的各个机制放进更大的系统分层里定位</li><li>区分 LLM 推理系统的共性设计问题与 vLLM 的特定选择</li><li>在面试里把&quot;会讲 vLLM&quot;升级为&quot;能讲清通用推理系统设计&quot;</li></ul><hr><h2 id="tui-li-xi-tong-jie-jue-shi-yao-wen-ti" tabindex="-1" id="推理系统解决什么问题">推理系统解决什么问题</h2><p>用 Transformers 的 <code>model.generate()</code> 跑一个 prompt，这是&quot;单请求、单次调用&quot;视角——模型加载、forward、输出 token，完事。</p><p>真正的线上推理系统面对的是另一个问题空间：</p><ul><li>请求持续流入，长度各不相同</li><li>显存有限，KV Cache 会随着生成持续增长</li><li>系统必须同时照顾吞吐、延迟、成本和稳定性</li><li>不是一批请求来了处理完再等下一批，而是新请求随时加入、已有请求随时结束</li></ul><p><strong>推理系统的本质不是&quot;让模型能生成&quot;，而是&quot;在资源受限、请求动态变化的条件下，让模型稳定且高效地持续生成&quot;。</strong></p><p>把这个问题展开，任何推理系统都必须同时做好五件事：</p><table><thead><tr><th>目标</th><th>含义</th></tr></thead><tbody><tr><td><strong>算得动</strong></td><td>模型能在目标硬件上运行，权重加载、精度、量化、并行都要支持</td></tr><tr><td><strong>装得下</strong></td><td>权重和 KV Cache 共存于有限显存，不 OOM，不频繁抢占</td></tr><tr><td><strong>调得好</strong></td><td>动态到来的请求能被高效组织，prefill/decode 混排，资源分配合理</td></tr><tr><td><strong>回得快</strong></td><td>首 token 延迟（TTFT）和后续 token 间隔（ITL）都在 SLA 以内</td></tr><tr><td><strong>扩得开</strong></td><td>能从单卡走向多卡、多机，满足规模增长</td></tr></tbody></table><hr><h2 id="liu-ceng-jie-gou" tabindex="-1" id="六层结构">六层结构</h2><p>把一个成熟的 LLM 推理系统分成六层，有助于定位问题、划分职责、做系统设计。</p><pre><code class="highlight mermaid">flowchart TB    L6[&quot;第 6 层：控制与运维层\n监控 / 日志 / Autoscaling / 热更新&quot;]    L5[&quot;第 5 层：服务层\nHTTP/gRPC API / Streaming / 多租户&quot;]    L4[&quot;第 4 层：调度层\nContinuous Batching / Token Budget / 抢占&quot;]    L3[&quot;第 3 层：状态管理层\nKV Cache 分配 / 回收 / 跨请求复用&quot;]    L2[&quot;第 2 层：执行层\nKernel / CUDA Graph / FlashAttention / Sampler&quot;]    L1[&quot;第 1 层：模型表示层\n架构 / 权重加载 / 量化 / 图优化&quot;]    L6 --&gt; L5 --&gt; L4 --&gt; L3 --&gt; L2 --&gt; L1</code></pre><p><strong>第 1 层：模型表示层</strong>。关心模型架构是什么（Decoder-only、MoE、多模态等）、权重如何加载和存储、是否需要量化和图融合。没有这一层，系统连 forward 都无法执行。大多数研究代码只到这一层，关心&quot;单次 forward 的正确性&quot;，不关心服务场景。</p><p><strong>第 2 层：执行层</strong>。每轮 forward 如何在 GPU 上执行——attention、MLP、norm、sampler 的具体算子，kernel 调度，CUDA Graph、FlashAttention 等优化手段。这一层解决&quot;每一轮算得够不够快&quot;。</p><p><strong>第 3 层：状态管理层</strong>。这是 LLM 推理系统和普通深度学习推理最不一样的地方。自回归生成要求系统跨轮维护每个请求的 KV Cache——它从哪里分配、什么时候回收、能不能跨请求复用、哪些前缀可以共享。vLLM 的 PagedAttention、block 化 KV Cache、prefix caching，本质都在这一层。</p><p><strong>第 4 层：调度层</strong>。这是系统大脑。本轮哪些请求进 batch、token budget 如何分配、prefill 和 decode 如何混排、资源不够时谁被抢占、谁被延后。没有调度层，系统只能做静态批处理；真实线上流量不是静态批次。</p><p><strong>第 5 层：服务层</strong>。OpenAI-compatible API、tokenization、流式返回、请求超时取消、多租户隔离。很多人会低估这一层，但线上系统最终靠它接住业务。</p><p><strong>第 6 层：控制与运维层</strong>。监控指标、日志与 tracing、autoscaling、故障恢复、模型热更新。前五层解决&quot;如何推理&quot;，这一层解决&quot;如何长期可靠运行&quot;。</p><hr><h2 id="v-llm-dai-lai-de-si-wei-zhuan-bian" tabindex="-1" id="vLLM-带来的思维转变">vLLM 带来的思维转变</h2><p>现在把 vLLM 放进这个框架里看，它贡献的不只是某个 kernel，而是四件事组合成的系统思路：</p><ol><li><p><strong>把 KV Cache 当成核心系统资源，而不是附属张量</strong>——分 block 管理、可回收、可复用、可调度，而不是单请求 forward 过程中的内部状态。</p></li><li><p><strong>把调度看成动态持续过程，而不是静态 batch 组织</strong>——每个调度周期都重新决定哪些请求推进、以多少 token budget 推进，而不是&quot;一批请求来了处理完再等下一批&quot;。</p></li><li><p><strong>把请求复用和显存管理统一到同一套 block 语义里</strong>——prefix caching 不是独立特性，而是 block 化 KV Cache 的自然延伸。</p></li><li><p><strong>把服务、调度、worker 执行拆成清晰的多进程架构</strong>——职责分离，便于独立演进和调试。</p></li></ol><p>这四点里，第 1、2、3 点的系统意义最重要。它背后是一次关键的思维转变：</p><blockquote><p>传统深度学习推理框架：模型 forward 是中心，batch 是输入的组织方式，中间状态是 forward 的附属产物。</p><p>LLM 在线推理：真正的中心对象变成了<strong>请求、KV 状态、调度周期、token budget</strong>——模型 forward 从&quot;唯一主角&quot;变成&quot;被调度系统反复调用的执行单元&quot;。</p></blockquote><p>以后看任何推理系统，可以先问一句：这个系统是把&quot;模型&quot;放在中心，还是把&quot;请求与状态调度&quot;放在中心？如果是后者，它通常更接近服务型推理引擎。</p><hr><h2 id="san-zu-he-xin-mao-dun" tabindex="-1" id="三组核心矛盾">三组核心矛盾</h2><p>任何 LLM 推理系统设计，都要在三组核心矛盾之间做取舍。这不是某个框架的特殊问题，而是这个问题域的固有张力。</p><table><thead><tr><th>矛盾</th><th>两端是什么</th><th>怎么取舍</th></tr></thead><tbody><tr><td><strong>吞吐 vs 延迟</strong></td><td>大 batch 有利于 GPU 利用率（吞吐好），但等待更多请求会让当前用户排队（延迟差）</td><td>离线批处理偏吞吐；在线聊天偏 TTFT 和 ITL；不同场景最优设计不同</td></tr><tr><td><strong>计算资源 vs 状态资源</strong></td><td>传统推理只看 kernel 速度；但 LLM 自回归生成要一边算、一边积累 KV Cache，状态是长期负担</td><td>系统必须同时优化算子性能和 KV Cache 管理，只做其一都不够</td></tr><tr><td><strong>通用框架 vs 专项极致</strong></td><td>通用框架模型覆盖广、易维护、快速支持新模型；专项系统针对特定硬件和模型打磨到极致</td><td>模型经常迭代 → 通用框架；模型固定且量大 → 专项优化；没有绝对高下</td></tr></tbody></table><p>吞吐和延迟的矛盾值得多说一点。GPU 喜欢大工作量——batch 越大，kernel 启动开销摊薄越充分，SM 利用率越高，单 token 平均成本越低。但如果为了追求大 batch 而总是等待更多请求，当前用户 TTFT 就会上升，已经在 decode 的请求也可能被长 prefill 拖慢。真正成熟的设计不是消灭这组矛盾，而是根据目标 workload 决定在哪里取舍。</p><hr><h2 id="cong-ling-she-ji-tong-yong-tui-li-xi-tong-10-ge-bi-xu-xian-xiang-qing-de-wen-ti" tabindex="-1" id="从零设计通用推理系统：10-个必须先想清的问题">从零设计通用推理系统：10 个必须先想清的问题</h2><p>这 10 个问题可以直接当系统设计题的骨架用：</p><table><thead><tr><th>#</th><th>问题</th><th>为什么重要</th></tr></thead><tbody><tr><td>1</td><td><strong>目标 workload 是什么？</strong></td><td>离线批处理 / 在线聊天 / agent 多轮 / 高共享前缀 RAG，后续所有设计都依赖这个前提</td></tr><tr><td>2</td><td><strong>主要优化目标是什么？</strong></td><td>最大吞吐 / 最低 TTFT / 最低 ITL / 最低成本 / 最大并发——不先定目标无从谈设计</td></tr><tr><td>3</td><td><strong>模型和硬件约束是什么？</strong></td><td>模型大小、上下文长度、GPU 显存、单卡/多卡、是否允许量化，直接决定并行策略和 batch 上限</td></tr><tr><td>4</td><td><strong>状态怎么存？</strong></td><td>KV Cache 连续分配还是 block 化？分配粒度多大？如何回收？是否支持跨请求复用？</td></tr><tr><td>5</td><td><strong>请求怎么调度？</strong></td><td>静态 batch 还是 continuous batching？prefill/decode 如何混合？token budget 如何定义？</td></tr><tr><td>6</td><td><strong>执行器怎么设计？</strong></td><td>每轮 forward 走 eager 还是 graph capture？attention 用什么实现？sampler 在 CPU 还是 GPU？</td></tr><tr><td>7</td><td><strong>并行怎么做？</strong></td><td>单卡不够时先 TP 还是 PP 还是 DP？MoE expert 路由与通信如何处理？通信和计算能否 overlap？</td></tr><tr><td>8</td><td><strong>服务接口怎么做？</strong></td><td>OpenAI-compatible API？是否支持 streaming？如何做取消、超时？多租户怎么隔离？</td></tr><tr><td>9</td><td><strong>监控与调优怎么做？</strong></td><td>暴露哪些指标？如何看 TTFT / ITL / throughput / cache hit rate？如何定位各类瓶颈？</td></tr><tr><td>10</td><td><strong>如何持续演进？</strong></td><td>模型升级、新量化格式、新硬件后端、新加速特性——推理系统不是一次性工程</td></tr></tbody></table><hr><h2 id="v-llm-de-te-se-xuan-ze-vs-tong-yong-gong-xing" tabindex="-1" id="vLLM-的特色选择-vs-通用共性">vLLM 的特色选择 vs 通用共性</h2><p>学完 vLLM 后很容易把它的所有设计当成独创，实际上可以明确区分两类。</p><p><strong>任何成熟推理系统都绕不开的共性问题：</strong></p><ul><li>Prefill / Decode 的阶段差异及其对调度的影响</li><li>吞吐与延迟的权衡</li><li>动态请求调度（而不是等全批凑齐再处理）</li><li>KV Cache 的存储、增长与回收</li><li>多卡并行与通信代价</li><li>服务接口与流式输出</li><li>监控、调优与故障定位</li></ul><p>面试时不要把这些说成&quot;vLLM 发明了这些问题&quot;——这是 LLM 推理系统的共性挑战。</p><p><strong>vLLM 对上述共性问题给出的特定工程答案：</strong></p><table><thead><tr><th>vLLM 特色</th><th>对应哪个共性问题</th></tr></thead><tbody><tr><td>Block/Page 化 KV Cache 管理</td><td>KV Cache 的存储与回收</td></tr><tr><td>PagedAttention（attention 访问适配 paged KV）</td><td>执行层对 block 化状态的适配</td></tr><tr><td>Prefix Caching（block 级 hash 复用）</td><td>跨请求前缀共享</td></tr><tr><td>Unified Scheduler + token budget 抽象</td><td>Prefill/decode 混排调度</td></tr><tr><td>强吞吐/显存效率平衡（通用服务场景）</td><td>吞吐与延迟的综合权衡</td></tr></tbody></table><p>vLLM 的价值在于：这些共性问题是通用的，但它对这些问题给出了一套很系统、很工程化、很适合通用服务场景的答案。</p><hr><h2 id="mian-shi-hui-da-kuang-jia-jie-v-llm-jiang-xi-tong-she-ji" tabindex="-1" id="面试回答框架：借-vLLM-讲系统设计">面试回答框架：借 vLLM 讲系统设计</h2><p>当面试官问&quot;你学过 vLLM，那你觉得一个通用 LLM 推理系统应该怎么设计？&quot;时，以下是一种比较成熟的答法主线：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">第一步：先定 workload</span><br><span class="line">  → LLM 推理没有统一最优解，离线高吞吐和在线低延迟的设计重点不同</span><br><span class="line">  → 问清楚：什么模型？什么并发？什么 prompt 长度分布？什么延迟 SLA？</span><br><span class="line"></span><br><span class="line">第二步：重点设计四件事</span><br><span class="line"></span><br><span class="line">  1. 状态管理（以 KV Cache 为核心）</span><br><span class="line">     → 自回归生成不是一次性计算，而是长期维护状态</span><br><span class="line">     → 如何表示、分配、回收、跨请求复用</span><br><span class="line"></span><br><span class="line">  2. 动态调度</span><br><span class="line">     → Prefill 和 decode 如何混排</span><br><span class="line">     → Token budget 如何分配，不同长度请求如何共存</span><br><span class="line"></span><br><span class="line">  3. 执行与并行</span><br><span class="line">     → Attention/kernel 选择，graph capture</span><br><span class="line">     → TP/PP/DP/EP 等扩展策略</span><br><span class="line"></span><br><span class="line">  4. 服务与可观测性</span><br><span class="line">     → Streaming API、请求取消</span><br><span class="line">     → 监控指标、性能瓶颈定位</span><br><span class="line"></span><br><span class="line">第三步：用 vLLM 举例</span><br><span class="line">  → 它给的最大启发：高性能 LLM 推理系统，核心不只是把某一轮 forward 做快</span><br><span class="line">  → 而是把&quot;请求调度 + KV 状态管理 + 复用机制 + 执行器&quot;组织成一个协同系统</span><br></pre></td></tr></table></figure><p>这个框架的好处是：没有被框死在某个框架特性里，把问题提升到了系统设计层，同时又能用 vLLM 作为具体例子支撑判断。</p><hr><h2 id="cheng-shou-tui-li-xi-tong-gong-cheng-shi-de-wu-ge-si-wei-xi-guan" tabindex="-1" id="成熟推理系统工程师的五个思维习惯">成熟推理系统工程师的五个思维习惯</h2><p>这五个习惯是从 vLLM 学习中可以提炼出的可迁移认知：</p><p><strong>习惯 1：先问 workload，再谈优化。</strong> &quot;某系统更快&quot;在没有上下文时没有意义。先问：什么模型、什么硬件、什么并发、什么 prompt 长度分布、什么延迟 SLA——否则&quot;更快&quot;只是一句空话。</p><p><strong>习惯 2：把状态当一等公民。</strong> LLM 推理里，KV Cache 不是 forward 的副产物，而是持续增长的核心资源。谁把状态管理得好，谁更可能做出真正成熟的系统。</p><p><strong>习惯 3：把系统拆层，而不是把特性堆在一起。</strong> 遇到复杂系统时，先问：这是调度问题？状态问题？执行器问题？服务层问题？运维问题？能拆层，才能定位问题和设计改进，特性列表叠再多也没用。</p><p><strong>习惯 4：任何优化都要讲代价和边界。</strong> Speculative decoding 不是永远有效；更大 batch 不一定对延迟友好；更激进量化可能伤精度；更复杂调度可能增加 CPU 开销。成熟的工程理解永远是&quot;收益 + 代价 + 适用边界&quot;三件套。</p><p><strong>习惯 5：把实现细节上升为设计原则。</strong> 学完 vLLM，不应该只记住&quot;它有 PagedAttention、有 prefix caching、有 unified scheduler&quot;，而应该上升到：</p><ul><li>长生命周期状态要按系统资源来管理</li><li>动态流量要用持续调度而不是静态批处理</li><li>复用机制最好建立在统一的数据抽象之上</li></ul><p>这才是遇到新系统也能迁移理解的基础。</p><hr><h2 id="12-ge-he-xin-ming-ti" tabindex="-1" id="12-个核心命题">12 个核心命题</h2><p>把这一篇的核心结论压缩成 12 句话，每一句都值得单独思考：</p><ol><li>LLM 推理系统不只是计算系统，还是状态管理系统。</li><li>线上推理的核心对象不是单次 forward，而是持续到来的请求。</li><li>任何成熟推理系统都必须同时处理吞吐、延迟、显存和稳定性。</li><li>Prefill / Decode 的差异会直接决定调度和优化策略。</li><li>KV Cache 是 LLM 推理系统最重要的长期资源之一。</li><li>调度的任务不是把 batch 拼起来，而是在动态工作负载下分配推进机会。</li><li>好的推理系统，不只要算得快，还要装得下、调得稳、扩得开。</li><li>通用框架和专项极致优化没有绝对高下，关键看业务场景。</li><li>vLLM 的强项不只是一个 kernel，而是一套围绕请求与状态组织起来的系统设计。</li><li>问题往往是通用的，系统之间真正拉开差距的是对这些问题的工程答案。</li><li>面试里讲 vLLM 时，最好能上升到通用 LLM 推理系统设计层面。</li><li>真正学会一个推理系统，不是会背特性，而是能提炼出可迁移的设计原则。</li></ol><hr><p>到这里，这个系列从单机 serving 原理、KV Cache 管理、调度机制、分布式部署、性能调优，到横向框架对比，走到了最后一步——把 vLLM 当成一个具体案例，拼出了通用 LLM 推理系统的设计框架。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/32c30b06/">vLLM系统拆解-17-面试深挖：面试官继续追问为什么时怎么回答</a></li><li>下一篇：<a href="/posts/cc221b1e/">vLLM系统拆解-19-AI Infra与推理岗面试手册：vLLM学到什么程度才算够用</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-18-tong-yong-llm-tui-li-xi-tong-she-ji-cong-v-llm-chu-fa-jian-li-kuang-jia&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="推理系统" scheme="https://smarter.xin/tags/inference-system/"/>
    
    <category term="系统设计" scheme="https://smarter.xin/tags/system-design/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-17-面试深挖：面试官继续追问为什么时怎么回答</title>
    <link href="https://smarter.xin/posts/32c30b06/"/>
    <id>https://smarter.xin/posts/32c30b06/</id>
    <published>2026-04-02T08:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.292Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-17-mian-shi-shen-wa-mian-shi-guan-ji-xu-zhui-wen-wei-shi-yao-shi-zen-yao-hui-da" tabindex="-1">vLLM系统拆解-17-面试深挖：面试官继续追问为什么时怎么回答</h1><p>面试里有两种完全不同的问题。第一种是&quot;vLLM 是什么、PagedAttention 是什么&quot;——这类问题靠记忆就能应付。第二种是面试官在听完基础概念后继续往下追：“为什么要这样设计”“这样做的代价是什么”“有没有不适合的场景”——这类问题靠记忆就会卡壳。</p><p>这篇文章的目的是建立一套回答深挖题的结构，然后用它覆盖十类最常见的追问。重点不在于背答案，而在于理解<strong>每个机制背后解决的是什么问题、为什么旧方案不够、新设计付出了什么代价</strong>。</p><hr><h2 id="ying-dui-zhui-wen-de-tong-yong-kuang-jia" tabindex="-1" id="应对追问的通用框架">应对追问的通用框架</h2><p>回答深挖题，有一个稳定的结构顺序：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">1. 先定义：这个问题本质上在讨论什么？</span><br><span class="line">   （不要立刻钻到实现细节）</span><br><span class="line"></span><br><span class="line">2. 说目标：系统在追求哪些设计目标？</span><br><span class="line">   （吞吐、延迟、显存效率、扩展性……）</span><br><span class="line"></span><br><span class="line">3. 说旧方案：传统方式为什么不够？</span><br><span class="line">   （先说痛点，再说方案才有必要性）</span><br><span class="line"></span><br><span class="line">4. 说新方案：具体怎么解决的？</span><br><span class="line"></span><br><span class="line">5. 说代价：这样做的边界条件和代价是什么？</span><br><span class="line"></span><br><span class="line">6. 一句收束</span><br></pre></td></tr></table></figure><p>举一个实际例子：面试官问&quot;为什么 vLLM 快&quot;，本质问题是<strong>如何提升单位 GPU 的有效服务能力</strong>，不是某个 kernel 有多快。把问题先定义成这个，回答自然就能展开到 KV cache 管理、连续批处理、前缀复用、执行路径优化这四个层次，而不是一句&quot;PagedAttention&quot;就结束了。</p><hr><h2 id="shi-lei-gao-pin-shen-wa-ti" tabindex="-1" id="十类高频深挖题">十类高频深挖题</h2><h3 id="1-wei-shi-yao-v-llm-kuai-shi-zhi-yin-wei-paged-attention-ma" tabindex="-1" id="1-为什么-vLLM-快——是只因为-PagedAttention-吗？">1. 为什么 vLLM 快——是只因为 PagedAttention 吗？</h3><p>不是。PagedAttention 很重要，但它只解决了 KV Cache 空间管理问题，不是 vLLM 快的全部原因。</p><p>从系统视角看，vLLM 的性能来自四层叠加：</p><table><thead><tr><th>层次</th><th>机制</th><th>解决的问题</th></tr></thead><tbody><tr><td>KV Cache 管理</td><td>PagedAttention / block 化</td><td>减少显存碎片，支持动态增长和复用</td></tr><tr><td>调度</td><td>continuous batching / unified scheduler</td><td>持续吸纳推进请求，不用静态 batch</td></tr><tr><td>请求复用</td><td>prefix caching</td><td>跳过重复前缀的 prefill</td></tr><tr><td>执行路径</td><td>chunked prefill / worker 拆分 / CUDA graph</td><td>降低实际线上服务的固定开销</td></tr></tbody></table><p>在线推理的瓶颈通常不只在单个算子，还在 batch 能否做大、显存是否允许高并发、decode 是否稳定推进、CPU 和 GPU 是否互相拖累。</p><p>如果追问&quot;只做 PagedAttention 不做 unified scheduler，够吗&quot;：PagedAttention 主要解决&quot;怎么放&quot;的问题，unified scheduler 解决&quot;怎么调&quot;的问题。没有调度层的配合，系统在长 prompt / 短 decode / 高并发混合场景下仍然会低效。</p><blockquote><p>一句话：vLLM 快不是因为某一个点特别快，而是因为它把 KV Cache 管理、连续批处理、前缀复用和执行路径优化做成了一个统一系统。</p></blockquote><hr><h3 id="2-wei-shi-yao-yao-zuo-unified-scheduler-er-bu-shi-ba-prefill-he-decode-yan-ge-fen-kai" tabindex="-1" id="2-为什么要做-unified-scheduler，而不是把-prefill-和-decode-严格分开？">2. 为什么要做 unified scheduler，而不是把 prefill 和 decode 严格分开？</h3><p><strong>本质问题</strong>：当系统里同时存在长 prompt 请求、正在 decode 的请求、命中过 prefix cache 的请求、可能被 preempt 的请求时，应该用什么调度抽象最稳定？</p><p><strong>严格二分的问题：</strong></p><ul><li>请求状态不是静态的——从 prefill 转 decode，还可能部分命中 cache、部分需要重算</li><li>不利于 chunked prefill：大 prompt 被切块后，某一轮既不是&quot;全 prefill&quot;也不是传统 decode</li><li>在线服务本质上是在争抢&quot;这一轮 GPU 能处理多少 token&quot;，不是争抢某个固定阶段标签</li></ul><p><strong>V1 unified scheduler 的做法</strong>：用一个 <code>&#123;request_id: num_tokens&#125;</code> 风格的 token budget 字典动态分配，统一回答&quot;这一步给哪些请求分配多少 token 预算&quot;，不区分请求是在做 prefill 还是 decode。</p><p><strong>收益</strong>：同一轮 batch 可以推进 decode、插入部分 prefill、处理 cache hit/miss 混合情况；chunked prefill、prefix caching、speculative decoding 都能自然接入，不需要为每种特性维护独立分支逻辑。</p><p><strong>代价</strong>：需要更稳定的 request state 管理、更精确的 token budget accounting，核心调度器实现质量要求更高。</p><blockquote><p>一句话：unified scheduler 把在线推理里本来非常碎片化的状态和优化机制，收敛到同一种 token budget 视角下处理。</p></blockquote><hr><h3 id="3-wei-shi-yao-chunked-prefill-neng-gai-shan-zheng-ti-fu-wu-biao-xian" tabindex="-1" id="3-为什么-chunked-prefill-能改善整体服务表现？">3. 为什么 chunked prefill 能改善整体服务表现？</h3><p><strong>问题背景</strong>：大 prompt 是在线服务的麻烦来源。一次完整 prefill 会长时间占住 batch 预算，让正在 decode 的请求等待，ITL 变差。</p><p><strong>chunked prefill 的设计目的</strong>：把长 prompt 从&quot;一个巨大的不可分割工作项&quot;改造成&quot;可以和 decode 请求交错执行的若干小工作项&quot;。</p><p>这样调度器可以：</p><ul><li>优先推进 decode（改善 ITL）</li><li>prefill 分块插空推进（不至于饿死）</li><li>一个 batch 里既有 compute-heavy（prefill）也有 memory-heavy（decode）工作，帮助整体 GPU 利用率更均衡</li></ul><p><strong>不是白送的优化</strong>：chunk size 和 token budget 设定直接影响 TTFT / ITL / 整体吞吐 / fairness 的平衡。Token budget 太小，decode 很流畅但 prompt 推进慢、TTFT 可能上升；太大，prefill 吃掉太多资源，decode 又被拖慢。这本质上是服务策略问题，不是算法正确性问题。</p><p>V1 默认倾向于启用 chunked prefill，是因为在真实服务 workload 下，长 prompt 破坏交互体验是最常见的问题之一。</p><blockquote><p>一句话：chunked prefill 的核心价值是把大 prompt 变成可与 decode 交错执行的细粒度任务，让两者更公平地共享同一轮执行预算。</p></blockquote><hr><h3 id="4-prefix-caching-wei-shi-yao-quot-ji-hu-xiang-bai-song-quot-dan-you-bu-shi-yong-yuan-you-yong" tabindex="-1" id="4-Prefix-caching-为什么-几乎像白送-，但又不是永远有用？">4. Prefix caching 为什么&quot;几乎像白送&quot;，但又不是永远有用？</h3><p>官方描述 prefix caching 为&quot;almost a free lunch&quot;，因为它跳过的是已经做过、且结果可复用的前缀计算，不改变模型输出，不引入语义变化——只是缓存复用，正确性上几乎没有代价。</p><p><strong>但收益高度依赖请求分布：</strong></p><table><thead><tr><th>场景</th><th>收益</th></tr></thead><tbody><tr><td>大量请求共享相同 system prompt、工具定义、few-shot 模板</td><td>很高</td></tr><tr><td>企业内部 agent 大量复用同一前缀，多轮会话前缀重复</td><td>很高</td></tr><tr><td>每个请求 prompt 都很独特，或前缀共享长度很短</td><td>有限</td></tr><tr><td>Block size 大但短前缀凑不满 full block</td><td>有限</td></tr><tr><td>Cache 容量小，热前缀频繁被 LRU 驱逐</td><td>有限</td></tr></tbody></table><p><strong>只缓存 full blocks 的原因</strong>：块作为独立缓存单元边界更稳定，哈希身份明确，LRU eviction 逻辑更简单。这是典型的系统设计取舍——牺牲一点极限命中粒度，换取缓存管理的简洁与稳定。</p><p><strong>一个容易被忽视的边界条件</strong>：如果请求需要 prompt logprobs，prefix cache 不能直接提供这些 logprobs，系统会重算完整 prompt prefill 来生成它们。复用 KV Cache 不等于所有前缀相关派生结果都能直接复用。</p><blockquote><p>一句话：prefix caching 几乎不影响正确性，但收益强依赖前缀复用率和缓存留存能力，是&quot;在合适 workload 下非常赚&quot;的优化。</p></blockquote><hr><h3 id="5-wei-shi-yao-paged-attention-yao-ba-kv-cache-qie-cheng-block-er-bu-shi-bao-chi-lian-xu" tabindex="-1" id="5-为什么-PagedAttention-要把-KV-Cache-切成-block，而不是保持连续？">5. 为什么 PagedAttention 要把 KV Cache 切成 block，而不是保持连续？</h3><p><strong>传统连续分配的问题</strong>：自回归推理里 KV Cache 会不断增长，在线服务中同时活跃的请求很多，且长度差异大、结束时间不同。要求每个请求的 KV Cache 在显存里是连续的一整段，就会出现大量内存洞、分配扩容麻烦、回收后很难形成可复用的大连续区。</p><p><strong>block 化的设计</strong>：借鉴了&quot;分页内存&quot;的思想——逻辑上请求仍然是一串连续 token，物理上对应的 KV 可以分散在多个 block 中，用映射关系把逻辑位置和物理 block 连起来。</p><p>收益：</p><ul><li>系统只需分配固定大小 block，不用每次找大连续区，碎片大幅减少</li><li>请求生成更多 token 时，只要再挂更多 block，无需迁移</li><li>Block 作为独立单元后，prefix caching 才自然成立——block 化是 prefix cache 的基础设施</li><li>可以从 block 粒度做驱逐和回收，不用移动整条超长序列</li></ul><p>代价：</p><ul><li>需要维护逻辑到物理 block 的映射表</li><li>Attention kernel 读取时要适应分页结构（不能直接访问连续数组）</li><li>实现复杂度高于简单连续数组</li></ul><blockquote><p>一句话：PagedAttention 的关键价值不只是减少碎片，而是把 KV Cache 从&quot;难以动态管理的大连续对象&quot;变成&quot;可以高效分配、复用、驱逐的小单元系统&quot;。</p></blockquote><hr><h3 id="6-ru-guo-gpu-xian-cun-yi-jing-hen-da-liao-huan-xu-yao-zhe-yao-fu-za-de-kv-guan-li-ma" tabindex="-1" id="6-如果-GPU-显存已经很大了，还需要这么复杂的-KV-管理吗？">6. 如果 GPU 显存已经很大了，还需要这么复杂的 KV 管理吗？</h3><p>需要。“显存大&quot;不等于&quot;管理问题消失”。</p><p>在线推理的问题不只是总容量，而是：活跃请求数量动态变化、请求长度不均匀、请求开始和结束时刻错开、prefix 可能复用、eviction 和 preemption 可能发生、多租户/多模型可能让可用空间进一步碎片化。</p><p>即使总显存很多，如果分配方式粗糙，batch 仍然上不去，可并发请求数仍然不稳定，prefix 复用仍然困难，OOM 仍然会提前出现。</p><p>不过，KV Cache 管理也只能解决一部分问题。如果瓶颈在 CPU scheduling、通信开销、模型本身算力、tokenizer 或网络层，盯着 KV Cache 调优也没用。</p><blockquote><p>一句话：显存容量决定天花板，显存管理方式决定能否稳定接近这个天花板。</p></blockquote><hr><h3 id="7-preemption-zai-v-llm-li-wei-shi-yao-he-li-wei-shi-yao-bu-yi-ding-shi-pi-shi" tabindex="-1" id="7-Preemption-在-vLLM-里为什么合理？为什么不一定是坏事？">7. Preemption 在 vLLM 里为什么合理？为什么不一定是坏事？</h3><p><strong>问题背景</strong>：在线服务不可能提前完美知道所有请求的显存需求。请求突然增多、prompt 变长、decode 持续增长，都可能让 KV Cache 不足。</p><p>如果系统完全不允许 preemption，只剩两个选择：非常保守地限制并发，或者直接因资源不足失败。</p><p><strong>Preemption 的设计思想</strong>：当资源瞬时紧张时，允许系统回收一部分工作进度，换取整体服务继续前进。在 vLLM 里，本质是让部分请求暂时让出 KV 资源，之后 recompute 恢复（V1 默认 RECOMPUTE 而不是 SWAP，因为 V1 架构下 recompute 开销更低）。</p><p>在线服务追求的是整体可服务性，不是每个请求永远不受影响。适度 preemption 可以让系统维持整体可运行、为更短的请求腾空间、避免全局阻塞。</p><p><strong>代价</strong>：被 preempt 的请求需要重算，TTFT / 延迟可能抖动。偶发 preemption 可以接受；高频 preemption 通常意味着配置不合理或负载超出设计范围。</p><blockquote><p>一句话：preemption 不是优化目标，而是资源紧张时保证系统继续工作的安全阀。</p></blockquote><hr><h3 id="8-wei-shi-yao-cpu-ye-hui-cheng-wei-v-llm-de-ping-jing" tabindex="-1" id="8-为什么-CPU-也会成为-vLLM-的瓶颈？">8. 为什么 CPU 也会成为 vLLM 的瓶颈？</h3><p>很多人把 LLM inference 当成纯 GPU 问题。但在线服务不只有矩阵乘法，还有：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">请求接收 / HTTP 处理          → CPU</span><br><span class="line">tokenization / detokenization  → CPU</span><br><span class="line">engine core 调度循环            → CPU（高频 busy loop）</span><br><span class="line">流式结果回传                    → CPU</span><br><span class="line">进程间通信                      → CPU</span><br><span class="line">采样与后处理                    → CPU</span><br><span class="line">指标采集和监控                  → CPU</span><br></pre></td></tr></table></figure><p>vLLM 的 engine core 要不断检查请求状态、分配 token budget、管理 KV Cache、dispatch 给 worker、收集结果再推进下一轮——这是持续运行的高频循环，不是偶尔触发。</p><p>如果 CPU 不够，GPU 可能不是&quot;算不过来&quot;，而是&quot;等不到活干&quot;。这说明 vLLM 必须被当成一个<strong>服务系统</strong>来看，CPU sizing 是核心配置之一，不是边角料。</p><blockquote><p>一句话：vLLM 的瓶颈不总在算子层，很多时候 GPU 性能发挥不出来，是因为 CPU 没有把请求组织、分发和回收这条链路喂饱。</p></blockquote><hr><h3 id="9-wei-shi-yao-speculative-decoding-bu-shi-zong-neng-ti-sheng-xing-neng" tabindex="-1" id="9-为什么-speculative-decoding-不是总能提升性能？">9. 为什么 speculative decoding 不是总能提升性能？</h3><p>Speculative decoding 的思想是&quot;先猜几个 token，再让大模型验证&quot;，希望减少大模型逐 token 推进的成本。但它不是无条件成立的：</p><ul><li><strong>猜错带来额外开销</strong>：draft/proposer 质量不高时，验证后接受的 token 不多，做了额外工作却没换来足够收益</li><li><strong>高负载下不划算</strong>：额外的 proposer/verification 路径本身消耗资源，未必优于把 batching 和 KV 管理做好</li><li><strong>采样策略影响收益</strong>：更高随机性让&quot;稳定提前猜中&quot;难度变大</li><li><strong>模型与硬件差异大</strong>：某些架构、某些 GPU、某些 batch 规模收益明显，换一套环境可能就没那么有效</li></ul><p>官方文档明确指出，speculative decoding 更适用于中低 QPS、memory-bound 场景。vLLM 把它作为可选优化层，而不是假设所有 workload 都必须依赖它——这个定位是正确的。</p><blockquote><p>一句话：speculative decoding 是有条件收益的高阶优化，重点在 workload 匹配，不是盲目开启。</p></blockquote><hr><h3 id="10-prefix-caching-wei-shi-yao-he-rag-agent-chang-jing-te-bie-da" tabindex="-1" id="10-Prefix-caching-为什么和-RAG-Agent-场景特别搭？">10. Prefix caching 为什么和 RAG/Agent 场景特别搭？</h3><p>RAG/Agent 系统往往把大量业务逻辑编码进 prompt 模板：system prompt、工具描述、output format、few-shot 模板、长文档里的公共上下文、多轮对话里未变化的历史前缀。这使得跨请求的前缀重复度往往比普通自由文本聊天更高——这正是 prefix caching 收益的前提条件。</p><p>但不是所有 agent 场景都适合。如果每个请求都拼接大量独特上下文（比如每次检索结果差异很大），或模板虽固定但共享前缀占比不高，收益同样会受限。</p><blockquote><p>一句话：agent/RAG 往往能从 prefix caching 获益，不是因为它们&quot;高级&quot;，而是因为它们更容易制造高重复率前缀。</p></blockquote><hr><h2 id="shi-ge-wen-ti-de-yi-tiao-zhu-xian" tabindex="-1" id="十个问题的一条主线">十个问题的一条主线</h2><p>如果面试官连续追问，可以把上面所有问题串成这段话：</p><blockquote><p>vLLM 的核心问题不是&quot;把一个模型跑起来&quot;，而是&quot;如何让很多状态不同、长度不同、生命周期不同的请求，在同一组 GPU 资源上高效共存&quot;。为了解这个问题，它用 PagedAttention 把 KV Cache 变成可高效管理的 block 系统，用 unified scheduler 把 prefill、decode、prefix reuse 等不同工作统一到 token budget 视角下处理，再通过 chunked prefill、prefix caching、可选 speculative decoding、worker 拆分与服务化链路优化，把这些能力转化成真实线上吞吐和时延收益。真正厉害的地方，不是某一个点特别炫，而是把这些点组织成了一整套相互配合的推理服务系统。</p></blockquote><hr><h2 id="wu-lei-chang-jian-zhui-wen-wu-qu" tabindex="-1" id="五类常见追问误区">五类常见追问误区</h2><table><thead><tr><th>误区</th><th>正确理解</th></tr></thead><tbody><tr><td>只讲概念，不讲问题背景</td><td>先说在线服务里大量 prompt 重复、重复 prefill 浪费——再说为什么要做 prefix caching</td></tr><tr><td>只讲优点，不讲代价</td><td>任何系统设计都有 trade-off，不讲代价显得没做过系统</td></tr><tr><td>把所有性能问题归结为 GPU 算力</td><td>调度、CPU、KV Cache、通信和 workload 形态都是瓶颈来源</td></tr><tr><td>把 vLLM 当&quot;高性能 generate 封装&quot;</td><td>vLLM 本质上是推理服务系统，不是单纯的模型调用库</td></tr><tr><td>把某个优化讲成&quot;永远有收益&quot;</td><td>没有这种东西——只要是系统优化，就一定和 workload 相关</td></tr></tbody></table><hr><h2 id="shi-tiao-he-xin-hua-zhu" tabindex="-1" id="十条核心话术">十条核心话术</h2><table><thead><tr><th>#</th><th>话术</th></tr></thead><tbody><tr><td>1</td><td>vLLM 的优势不是单点优化，而是把 KV 管理、调度、批处理和请求复用做成了统一系统</td></tr><tr><td>2</td><td>PagedAttention 解决的不只是碎片问题，而是让 KV Cache 变成可增量分配、复用和驱逐的小单元系统</td></tr><tr><td>3</td><td>unified scheduler 的关键价值，是把异构请求统一到 token budget 视角下调度</td></tr><tr><td>4</td><td>chunked prefill 的本质，是把大 prompt 变成可与 decode 交错执行的细粒度任务</td></tr><tr><td>5</td><td>prefix caching 是跨请求复用；普通 KV Cache 是请求内历史状态。两者层级不同</td></tr><tr><td>6</td><td>prefix caching 几乎不影响正确性，但收益强依赖前缀复用率和缓存留存能力</td></tr><tr><td>7</td><td>preemption 不是优化目标，而是资源紧张时的系统安全阀</td></tr><tr><td>8</td><td>在 vLLM 这种服务系统里，CPU 也可能成为关键瓶颈</td></tr><tr><td>9</td><td>speculative decoding 是有条件收益的高阶优化，不是无脑加速按钮</td></tr><tr><td>10</td><td>vLLM 更像推理服务操作系统，而不是单纯的模型调用库</td></tr></tbody></table><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/e7a2278e/">vLLM系统拆解-16-性能瓶颈定位：从现象到根因的分层诊断方法</a></li><li>下一篇：<a href="/posts/6f0af891/">vLLM系统拆解-18-通用LLM推理系统设计：从vLLM出发建立框架</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-17-mian-shi-shen-wa-mian-shi-guan-ji-xu-zhui-wen-wei-shi-yao-shi-zen-yao-hui-da&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="KV Cache" scheme="https://smarter.xin/tags/kv-cache/"/>
    
    <category term="Scheduler" scheme="https://smarter.xin/tags/scheduler/"/>
    
    <category term="面试" scheme="https://smarter.xin/tags/interview/"/>
    
    <category term="系统设计" scheme="https://smarter.xin/tags/system-design/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-16-性能瓶颈定位：从现象到根因的分层诊断方法</title>
    <link href="https://smarter.xin/posts/e7a2278e/"/>
    <id>https://smarter.xin/posts/e7a2278e/</id>
    <published>2026-04-02T08:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.292Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-16-xing-neng-ping-jing-ding-wei-cong-xian-xiang-dao-gen-yin-de-fen-ceng-zhen-duan-fang-fa" tabindex="-1">vLLM系统拆解-16-性能瓶颈定位：从现象到根因的分层诊断方法</h1><p>盲目调参是最常见的调优误区：吞吐上不去就加大 batch，显存不够就降并发，首 token 慢就加机器。问题在于，<strong>同一个现象可能由不同层的瓶颈导致，同一个参数可能同时改善一个指标、恶化另一个指标</strong>。</p><p>这篇文章的目的是把 scheduler、KV Cache、chunked prefill、prefix caching、worker、CPU/GPU 协同这些机制，从&quot;知识点&quot;转化为&quot;诊断能力&quot;。读完应该能做到：看到一个性能现象，先判断更可能是哪一层的问题，再决定看哪些指标、调哪些参数、预期付出什么代价。</p><hr><h2 id="xian-jian-li-fen-ceng-gui-yin-kuang-jia" tabindex="-1" id="先建立分层归因框架">先建立分层归因框架</h2><p>性能问题归根结底来自六个层次，每个层次的优化方向完全不同：</p><table><thead><tr><th>层次</th><th>典型症状来源</th><th>代表性问题</th></tr></thead><tbody><tr><td><strong>请求侧</strong></td><td>workload 本身不友好</td><td>prompt 极长、输出不可控、长度分布极端不均、前缀重复率低</td></tr><tr><td><strong>调度侧</strong></td><td>scheduler token budget 分配问题</td><td>token budget 过大/小、decode/prefill 混排不当、waiting queue 堆积</td></tr><tr><td><strong>KV Cache 侧</strong></td><td>缓存空间和复用问题</td><td>KV 空间不够、prefix cache 命中率低、频繁 preemption/recompute</td></tr><tr><td><strong>GPU 计算侧</strong></td><td>kernel 和执行路径问题</td><td>attention backend 不匹配、CUDA graph 未命中、量化策略不兼容</td></tr><tr><td><strong>CPU 侧</strong></td><td>前后处理和调度链路问题</td><td>tokenizer 太重、detokenization/streaming 抢 CPU、engine core 缺核</td></tr><tr><td><strong>分布式通信侧</strong></td><td>多卡并行带来的额外开销</td><td>TP 同步、PP 流水气泡、MoE all-to-all、跨机网络</td></tr></tbody></table><p>归因到正确的层次，才能避免&quot;在错误的地方用力&quot;。</p><hr><h2 id="zhi-biao-ti-xi-mei-you-zhi-biao-jiu-mei-you-zhen-duan" tabindex="-1" id="指标体系：没有指标就没有诊断">指标体系：没有指标就没有诊断</h2><p>线上没有这些指标，大多数判断只能靠猜。</p><p><strong>核心体验指标</strong></p><ul><li><strong>TTFT（Time To First Token）</strong>：受 prompt 长度、prefill 负载、scheduling 延迟、CPU 前处理、KV 压力影响</li><li><strong>ITL（Inter-Token Latency）</strong>：受 decode 是否被大 prefill 干扰、memory bandwidth 压力、decode batch 大小影响</li><li><strong>吞吐（tokens/s / requests/s）</strong>：受 GPU 是否被喂满、batch 能否做起来、KV 是否允许高并发、CPU 能否持续供给影响</li></ul><p><strong>服务运行态指标</strong>（vLLM 通过日志统计和 Prometheus 暴露）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">running requests        → 当前活跃请求数</span><br><span class="line">waiting requests        → 排队等待的请求数</span><br><span class="line">GPU cache usage         → KV Cache 占用率</span><br><span class="line">prompt tokens/s         → prefill 吞吐</span><br><span class="line">generation tokens/s     → decode 吞吐</span><br><span class="line">prefix cache hit rate   → 前缀复用命中率</span><br><span class="line">preemption count        → KV Cache 压力信号</span><br><span class="line">GPU utilization         → GPU 算力利用</span><br><span class="line">CPU utilization         → CPU 资源压力</span><br></pre></td></tr></table></figure><p>TTFT histogram 和 preemption 次数是判断 prefill 路径和 KV 压力最直接的信号。</p><hr><h2 id="qi-lei-zheng-zhuang-de-zhen-duan-lu-jing" tabindex="-1" id="七类症状的诊断路径">七类症状的诊断路径</h2><h3 id="zheng-zhuang-a-ttft-hen-gao-dan-sheng-cheng-jie-duan-zheng-chang" tabindex="-1" id="症状-A：TTFT-很高，但生成阶段正常">症状 A：TTFT 很高，但生成阶段正常</h3><p>这类问题的核心是<strong>从收到请求到进入稳定 decode 之前，某一段特别慢</strong>。</p><p><strong>A1. Prompt 本来就很长</strong><br>这是 workload 导致的必然 prefill 成本，不是系统 bug。长 prompt 的 prefill 是把全部输入一次性过模型，token 数越多计算越重，TTFT 自然变大。在诊断时，先区分&quot;必然高 TTFT&quot;还是&quot;额外延迟&quot;很关键。</p><p><strong>A2. Waiting requests 很多</strong><br>说明请求进入 engine 后没有尽快拿到执行机会——可能是 token budget 不够、当前并发太高、其他长请求占用太多 KV 或计算资源、CPU 调度环节变慢。</p><p><strong>A3. CPU 忙、GPU 却不满</strong><br>这是最容易被忽视的情况。官方文档明确提醒：CPU 不足会影响输入处理、scheduler 延迟、输出处理；如果 GPU 利用率比预期低，CPU contention 可能就是瓶颈。</p><p>排查方向：tokenizer/chat template 是否太重？detokenization/streaming 是否抢 CPU？engine core busy loop 是否缺 CPU 核？多进程部署下是否出现 CPU starvation？</p><p><strong>A4. <code>max_num_batched_tokens</code> 设得太小</strong><br>官方文档明确说：更大的 <code>max_num_batched_tokens</code> 通常更利于 TTFT 和高吞吐；更小的值更利于 ITL。原因很直接：token budget 越小，大 prompt 被切碎的程度越高，首 token 出来前要经历更多轮调度和执行。</p><p><strong>A5. Prefix cache hit rate 低</strong><br>业务里大量重复 system prompt，但没复用上——前缀&quot;很像&quot;和&quot;完全相同&quot;不是一回事。Prefix caching 是块级前缀一致，不是语义相似。常见原因：system prompt 混入时间戳或 request ID、chat template 不统一、few-shot 示例顺序偶尔变化、blocks 没形成足够多 full blocks、cache 容量不足导致热前缀被驱逐。</p><hr><h3 id="zheng-zhuang-b-ttft-huan-xing-dan-itl-hen-chai-sheng-cheng-yi-qia-yi-qia" tabindex="-1" id="症状-B：TTFT-还行，但-ITL-很差、生成一卡一卡">症状 B：TTFT 还行，但 ITL 很差、生成一卡一卡</h3><p>进入 decode 后单步生成不流畅，这通常是 decode 路径问题，不是 prefill 路径问题。</p><p><strong>B1. Decode 被 prefill 干扰</strong><br>这正是 chunked prefill 要解决的核心问题。V1 中 chunked prefill 默认尽量开启，调度策略优先 decode——这样 decode 不需要等待完整 prefill 执行完。</p><p>检查：是否关闭了 chunked prefill？是否给 prefill 过大的 token budget？是否存在超长 prompt 持续插队？</p><p><strong>B2. <code>max_num_batched_tokens</code> 过大</strong><br>这是典型的 trade-off：更大的 token budget 有利于 TTFT 和 aggregate throughput，但 decode 每一步需要等待更多 prefill token 处理完，ITL 会恶化。吞吐优化和单用户流式体验优化本质上是不同的目标。</p><p><strong>B3. 高 GPU 利用率下 ITL 仍差</strong><br>可能是有意的吞吐导向配置。vLLM 的 <code>performance-mode</code> 现在提供 <code>balanced</code>、<code>interactivity</code>、<code>throughput</code> 三种取向，<code>throughput</code> 模式会明确牺牲单请求延迟换取高并发 total tokens/s。需要结合业务目标判断是否合理。</p><hr><h3 id="zheng-zhuang-c-tun-tu-shang-bu-qu-gpu-li-yong-lu-ye-bu-gao" tabindex="-1" id="症状-C：吞吐上不去，GPU-利用率也不高">症状 C：吞吐上不去，GPU 利用率也不高</h3><p>这个症状非常高频，本能反应是&quot;batch 不够大&quot;，但真实原因有三种：</p><p><strong>C1. CPU 供给不足</strong><br>CPU 在输入处理、chat template 渲染、tokenizer、scheduler、detokenization、网络输出等环节卡住，GPU 就会出现阶段性断粮。表面看是 GPU utilization 低，根因是 CPU。</p><p><strong>C2. 请求形态不利于 batching</strong><br>请求很稀疏、到达率很低、prompt/output 长度差异极大、大量请求很快结束——这时系统不是&quot;不会 batch&quot;，而是&quot;没有足够好的 batch 机会&quot;。这是 workload 问题，不是参数问题。</p><p><strong>C3. KV Cache 压力让调度器保守</strong><br>如果再加请求就触发 preemption，scheduler 会变得保守，主动限制并发。吞吐上不去，不是调度器笨，而是 KV Cache 空间不允许。</p><p>调参方向：依次检查 <code>max_num_batched_tokens</code>、<code>max_num_seqs</code>、<code>gpu_memory_utilization</code> 是否过保守，是否需要 TP/PP 给 KV Cache 腾显存，以及 CPU 资源是否充分。</p><hr><h3 id="zheng-zhuang-d-ri-zhi-pin-fan-chu-xian-preemption-recompute" tabindex="-1" id="症状-D：日志频繁出现-preemption-recompute">症状 D：日志频繁出现 preemption / recompute</h3><p>Preemption 几乎直接指向 KV Cache 不够用。</p><p>官方文档说明：KV Cache 空间不足时 vLLM 会 preempt 请求释放空间，被 preempt 的请求后续 recompute；V1 默认采用 RECOMPUTE 而不是 SWAP，因为在 V1 架构下 recompute 开销更低。</p><p>Preemption 是 robustness 保底手段——空间不够时不让系统死掉，而是牺牲一部分重复计算换可运行性。偶发 preemption 可以接受，但高频 preemption 说明系统进入不健康运行区：被抢占请求后续重算、scheduler 决策空间变差、running/waiting 队列抖动、GPU 做了大量无效重复劳动。</p><p><strong>官方推荐的修复方向及其代价：</strong></p><table><thead><tr><th>动作</th><th>为什么有效</th><th>代价</th></tr></thead><tbody><tr><td>增大 <code>gpu_memory_utilization</code></td><td>给 KV Cache 更多预留显存</td><td>headroom 变小，workload 波动时更脆弱</td></tr><tr><td>减小 <code>max_num_seqs</code></td><td>减少同时占用 KV Cache 的请求量</td><td>并发能力下降，GPU 利用率可能降低</td></tr><tr><td>减小 <code>max_num_batched_tokens</code></td><td>减少每轮 KV Cache 消耗</td><td>吞吐可能下降</td></tr><tr><td>增大 <code>tensor_parallel_size</code></td><td>权重摊到更多 GPU，间接腾出 KV 空间</td><td>层内同步开销，通信延迟增加</td></tr><tr><td>增大 <code>pipeline_parallel_size</code></td><td>类似 TP，减少单卡权重占用</td><td>流水气泡，PP 延迟增加</td></tr></tbody></table><hr><h3 id="zheng-zhuang-e-xian-cun-jian-kong-mei-man-dan-xi-tong-reng-ran-oom-huo-bu-wen-ding" tabindex="-1" id="症状-E：显存监控没满，但系统仍然-OOM-或不稳定">症状 E：显存监控没满，但系统仍然 OOM 或不稳定</h3><p>“显存监控显示没满&quot;不等于&quot;KV Cache 空间够用”。真实系统里显存被多个部分共同占用：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">GPU 显存总量</span><br><span class="line">    ├── 模型权重           （静态，常驻）</span><br><span class="line">    ├── 激活和临时 buffer   （动态，随执行变化）</span><br><span class="line">    ├── KV Cache           （动态，随请求增长）</span><br><span class="line">    ├── CUDA Graph 开销     （预先 capture，额外占用）</span><br><span class="line">    ├── 通信 buffer        （多卡时额外）</span><br><span class="line">    └── 其他 runtime 开销</span><br></pre></td></tr></table></figure><p>系统需要的不只是&quot;还有多少 MB&quot;，而是&quot;当前这轮调度能不能放下更多 block、后续 decode 增长后还能不能持续、新请求进来后有没有足够 headroom&quot;。</p><p>如果 workload 波动很大、输出长度不可控、多模态输入偶发很大，或者还有并行通信开销，过于激进的 <code>gpu_memory_utilization</code> 会让系统在峰值时更脆弱。</p><hr><h3 id="zheng-zhuang-f-prefix-cache-ming-zhong-lu-di-ye-wu-shang-ti-shi-ci-quot-hen-xiang-quot" tabindex="-1" id="症状-F：Prefix-Cache-命中率低，业务上提示词-很像">症状 F：Prefix Cache 命中率低，业务上提示词&quot;很像&quot;</h3><p>这里最容易出错：语义&quot;很像&quot;不等于 block 级前缀&quot;完全相同&quot;。</p><p>Prefix caching 的复用逻辑是块级 token 序列完全一致后才能命中缓存（基于哈希）——只要某一段 token 不同，后面整条 block hash 链就会不同。</p><p><strong>六种常见根因：</strong></p><ol><li>system prompt 中混入时间戳、request ID、随机字段</li><li>chat template 不统一（空格/换行/顺序差异都算）</li><li>few-shot 示例顺序偶尔变化</li><li>tools/schema 每次拼接顺序不同</li><li>前缀虽然相同，但 block 大小内没凑满 full blocks</li><li>cache 容量不足，热前缀不断被 LRU 驱逐</li></ol><p><strong>正确的修复方向是输入标准化，不是调 vLLM 参数</strong>：统一 system prompt、消除前缀中的动态字段、固定 few-shot 和 tools 排序、固定 chat template 格式。Prefix caching 的收益强依赖上层产品/服务接入的规范程度。</p><hr><h3 id="zheng-zhuang-g-duo-qia-hou-xian-cun-ya-li-huan-jie-dan-yan-chi-fan-er-bian-chai" tabindex="-1" id="症状-G：多卡后显存压力缓解，但延迟反而变差">症状 G：多卡后显存压力缓解，但延迟反而变差</h3><p>这是分布式推理的经典现象。</p><p>如果模型单卡根本放不下，TP/PP 是必要条件没有选择。但如果模型单卡能放下，只是为了多一点 KV Cache 空间或更高并发而上多卡，必须对比节省出来的显存收益和新增的通信/同步开销：</p><ul><li>TP 引入层内同步（每个 Transformer 层的 all-reduce）</li><li>PP 引入 stage 间传递和流水气泡</li><li>MoE/EP 场景下有额外 all-to-all</li><li>跨机时网络更可能成为瓶颈</li></ul><p>加卡不是免费扩容。如果原先瓶颈是 decode memory bandwidth、CPU 调度或请求稀疏度，不是显存不够，加卡未必能提高 end-to-end 性能。</p><hr><h2 id="wu-ge-wan-zheng-an-li" tabindex="-1" id="五个完整案例">五个完整案例</h2><p>以&quot;现象 → 第一判断 → 验证指标 → 可能根因 → 动作&quot;格式整理，可对照实际情况使用。</p><table><thead><tr><th>案例</th><th>现象</th><th>第一判断层</th><th>优先验证</th><th>可能根因</th><th>动作方向</th></tr></thead><tbody><tr><td><strong>案例1</strong><br>首 token 慢，生成正常</td><td>TTFT↑，ITL 正常，GPU 中等，等待队列偶积</td><td>prefill 路径或 CPU 前处理</td><td>prompt 长度分布、prefix cache hit rate、CPU 利用率</td><td>prompt 太长 / token budget 太小 / prefix 没命中 / CPU 重</td><td>workload 长 prompt 接受成本；hit rate 低做模板标准化；CPU 紧补核或拆分前处理</td></tr><tr><td><strong>案例2</strong><br>token 一卡一卡，总吞吐不低</td><td>ITL↑，高并发时更明显</td><td>throughput 导向 batching 策略</td><td>performance-mode、chunked prefill 配置、token budget 大小</td><td>decode 被大 prefill 拖住 / token budget 过大 / throughput mode</td><td>降 token budget；切 interactivity 配置；对交互/离线流量分池部署</td></tr><tr><td><strong>案例3</strong><br>GPU 只跑到 40%</td><td>GPU 利用率低，CPU 高，running 不多</td><td>CPU 瓶颈</td><td>tokenizer/detokenizer CPU 占用、API server 与 engine 的核分配、streaming 开销</td><td>CPU 核不够 / chat template 或多模态前处理重 / 请求太稀疏</td><td>给足 CPU 核；优化前后处理；提高请求聚合度</td></tr><tr><td><strong>案例4</strong><br>高峰期延迟抖</td><td>平均值还行，P95/P99 很差，频繁 preemption</td><td>KV Cache 进入紧张区</td><td>GPU cache usage、preemption 次数、输出长度尾部分布</td><td>KV Cache 预算不够 / 输出长度不可控 / 并发参数过激进</td><td>降并发上限；长上下文流量单独分池；提高 KV Cache 预算</td></tr><tr><td><strong>案例5</strong><br>Prefix cache 命中低</td><td>相似业务请求多，hit rate 低，TTFT 无收益</td><td>接入层模板不稳定</td><td>system prompt token 级一致性、动态字段注入位置、full block 形成比例</td><td>前缀有隐藏动态字段 / 模板顺序不固定 / cache 容量不足</td><td>统一模板；消除随机字段；固定 tools/few-shot 顺序</td></tr></tbody></table><hr><h2 id="can-shu-diao-zheng-de-dai-jie" tabindex="-1" id="参数调整的代价">参数调整的代价</h2><p>每调一个参数都要同时考虑收益和代价，这是面试里体现系统思维的关键：</p><table><thead><tr><th>参数/动作</th><th>可能收益</th><th>可能代价</th></tr></thead><tbody><tr><td>调大 <code>max_num_batched_tokens</code></td><td>更好 TTFT、更高吞吐、更高 GPU 利用率</td><td>更差 ITL、更大 KV Cache 压力、高峰期更容易抖动</td></tr><tr><td>调大 <code>gpu_memory_utilization</code></td><td>更多 KV Cache 空间、更少 preemption、更高并发</td><td>headroom 变小、workload 波动时更脆弱、其他 runtime 容易顶满</td></tr><tr><td>调大 <code>max_num_seqs</code></td><td>更高并发潜力、更高 aggregate throughput</td><td>KV Cache 消耗更快、调度复杂度更高、对长请求更敏感</td></tr><tr><td>加 TP/PP</td><td>模型能放下、权重显存下降、间接给 KV Cache 腾空间</td><td>通信开销、同步延迟、不是所有 workload 都赚</td></tr><tr><td>切 throughput 模式</td><td>高并发 total tokens/s 更高</td><td>单用户体验可能下降，ITL 和尾延迟可能更差</td></tr></tbody></table><hr><h2 id="hen-duo-xing-neng-wen-ti-qi-shi-shi-workload-wen-ti" tabindex="-1" id="很多性能问题其实是-workload-问题">很多性能问题其实是 workload 问题</h2><p>这一点值得单独强调：很多问题来自 workload 形态本身，不是参数没调好。</p><ul><li>Prompt 极长 → 必然高 TTFT</li><li>输出极长且不可控 → KV Cache 持续扩张</li><li>请求到达很稀疏 → batch 很难组起来</li><li>请求长度分布极不均匀 → batch 一直处于次优状态</li><li>重复前缀很少 → prefix caching 收益接近零</li><li>并发高峰非常陡 → 峰值时 KV Cache 必然被挤</li></ul><p>vLLM 提供了很强的调度、KV Cache 和服务优化能力，但最终表现由 workload、硬件、并行配置、前后处理链路共同决定。性能调优不是单参数问题，而是 workload-aware 的系统工程问题。</p><hr><h2 id="zhen-duan-fang-fa-lun-zheng-zhuang-fen-ceng-gui-yin-dong-zuo-fu-zuo-yong" tabindex="-1" id="诊断方法论：症状-→-分层归因-→-动作-→-副作用">诊断方法论：症状 → 分层归因 → 动作 → 副作用</h2><pre><code class="highlight mermaid">flowchart TD    A[观察到性能现象] --&gt; B[归类症状\nTTFT高？ITL差？吞吐低？尾延迟差？频繁preemption？]    B --&gt; C[查核心指标\nrunning/waiting请求数\nGPU cache usage\nprefix cache hit rate\nCPU/GPU utilization\npreemption次数\nTTFT直方图]    C --&gt; D[分层归因\n请求侧→调度侧→KV Cache侧\nGPU计算侧→CPU侧→分布式通信侧]    D --&gt; E[确认根因所在层]    E --&gt; F[决定动作\n调参数 / 改架构 / 规范输入 / 分池部署]    F --&gt; G[预期副作用\n这个动作会改善哪个指标？\n同时会恶化哪个指标？]</code></pre><p>标准诊断步骤：</p><ol><li>归类症状（TTFT 高 / ITL 差 / 吞吐低 / preemption 频繁）</li><li>查核心指标（见上方指标列表）</li><li>按层归因（区分 workload / CPU / scheduler / KV Cache / GPU kernel / 分布式通信）</li><li>决定动作（调参 / 改架构 / 规范上层输入）</li><li>说明 trade-off（每个优化付出的代价是什么）</li></ol><hr><h2 id="shi-tiao-zhen-duan-zhun-ze" tabindex="-1" id="十条诊断准则">十条诊断准则</h2><ol><li>先分清问题属于 TTFT、ITL、吞吐还是尾延迟——这四者不能混在一起。</li><li>Prefill 问题不要按 decode 思路查，decode 问题不要按 prefill 思路查。</li><li>GPU 不满不一定是 GPU 问题，CPU 很可能是根因。</li><li>吞吐和交互体验往往是 trade-off，不能同时拉满。</li><li>频繁 preemption 基本等价于 KV Cache 空间不够。</li><li>Prefix cache 命中率低，优先查模板稳定性，不是先怪框架。</li><li>加卡不一定更快，显存收益要和通信开销一起算。</li><li>显存&quot;看着没满&quot;不代表系统就没有 cache 压力。</li><li>参数调优必须结合 workload 形态，不能脱离业务场景。</li><li>真正的性能定位不是改参数，而是先做分层归因。</li></ol><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/cf18dd3a/">vLLM系统拆解-15-调度、KV Cache与Prefix Caching：为什么必须三层协同</a></li><li>下一篇：<a href="/posts/32c30b06/">vLLM系统拆解-17-面试深挖：面试官继续追问为什么时怎么回答</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-16-xing-neng-ping-jing-ding-wei-cong-xian-xiang-dao-gen-yin-de-fen-ceng-zhen-duan-fang-fa&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="TTFT" scheme="https://smarter.xin/tags/ttft/"/>
    
    <category term="ITL" scheme="https://smarter.xin/tags/itl/"/>
    
    <category term="瓶颈定位" scheme="https://smarter.xin/tags/bottleneck-analysis/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-15-调度、KV Cache与Prefix Caching：为什么必须三层协同</title>
    <link href="https://smarter.xin/posts/cf18dd3a/"/>
    <id>https://smarter.xin/posts/cf18dd3a/</id>
    <published>2026-04-02T07:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.292Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-15-diao-du-kv-cache-yu-prefix-caching-wei-shi-yao-bi-xu-san-ceng-xie-tong" tabindex="-1">vLLM系统拆解-15-调度、KV Cache与Prefix Caching：为什么必须三层协同</h1><p>Scheduler、KV Cache、Prefix Cache——很多人学 vLLM 时是分开学这三块的。调度讲完讲 PagedAttention，PagedAttention 讲完讲 prefix caching。每块都&quot;挺厉害&quot;，但就是讲不清楚它们为什么必须一起存在，以及它们怎么协同工作。</p><p>这篇文章换一个叙述顺序：不按功能分章，而是按<strong>一次请求的完整生命周期</strong>把三块串起来，展示在真实 serving 场景里它们是怎么相互依赖的。读完应该能回答：prefix caching 为什么不是&quot;小优化&quot;、unified scheduler 为什么比&quot;prefill 一套逻辑 decode 一套逻辑&quot;更强，以及 block 化 KV Cache 为什么是前两者的基础设施。</p><hr><h2 id="san-ceng-fen-gong-yi-tiao-yi-lai-lian" tabindex="-1" id="三层分工，一条依赖链">三层分工，一条依赖链</h2><p>先把结论放在最前面：</p><ul><li><strong>Scheduler</strong> 是全局调度者——决定本轮 token 预算下哪些请求推进、推进多少</li><li><strong>KV Cache Manager</strong> 是内存管理者——把推进动作落到实际的 block 分配、共享、回收</li><li><strong>Prefix Cache</strong> 是跨请求复用机制——把已计算的 full blocks 复用给其他请求，减少重复 prefill</li></ul><p>三者不是并列关系，是有明确方向的依赖：</p><pre><code class="highlight mermaid">flowchart LR    subgraph 调度层        S[Scheduler\n决定推进策略]    end    subgraph 存储层        K[KV Cache Manager\n提供推进所需存储资源]    end    subgraph 复用层        P[Prefix Cache\n减少需要推进的工作量]    end    S -- 申请 block / 查可用量 --&gt; K    P -- 直接改变请求起跑位置 --&gt; S    P -- 复用已有 full blocks --&gt; K</code></pre><p>更准确的表达：scheduler 决定推进策略，KV cache manager 提供推进所需的存储资源，prefix cache 减少推进前必须重新计算的部分。</p><hr><h2 id="wei-shi-yao-san-kuai-bi-xu-yi-qi-cun-zai" tabindex="-1" id="为什么三块必须一起存在">为什么三块必须一起存在</h2><p>把任何一块单独拿出来，系统都会有明显的短板。</p><p><strong>只有 scheduler，没有 block 化 KV Cache。</strong> 假设 continuous batching 已经做得很好，能不断把新请求塞进 batch。但如果 KV Cache 还是传统的&quot;连续大段分配&quot;，请求长度不同、结束时间不同，显存就会快速碎片化——scheduler 想放更多请求进来，显存层腾不出足够整块空间。调度能力必须建立在可持续的 KV 内存管理之上。</p><p><strong>只有 block 化 KV Cache，没有 scheduler。</strong> 有漂亮的 PagedAttention 和 block pool，但调度策略弱：长 prompt 一下子把预算吃满，decode 请求迟迟排不上，TTFT/ITL 很差。高效内存管理不自动等于高效服务，最终还要靠 scheduler 决定资源怎么花。</p><p><strong>只有 KV Cache，没有跨请求复用。</strong> 即便 block 化做得很好，每个请求都把相同的 system prompt、相同工具描述再 prefill 一遍，仍然是在浪费 GPU 算力、TTFT 和 batch 预算。</p><hr><h2 id="yi-ci-qing-qiu-de-sheng-ming-zhou-qi" tabindex="-1" id="一次请求的生命周期">一次请求的生命周期</h2><p>这部分比较长，先看整体流程图，再展开每个阶段。</p><pre><code class="highlight mermaid">flowchart TD    A[请求进入系统] --&gt; B[Scheduler: 决定本轮推进策略\n查 token budget / 请求优先级]    B --&gt; C&#123;prefix cache 命中？&#125;    C -- 命中 --&gt; D[直接挂载已缓存的 full blocks\n起跑线前移，跳过已计算部分]    C -- 未命中 --&gt; E[从 token 0 开始推进]    D --&gt; F[KV Cache Manager: 申请剩余新增 blocks]    E --&gt; F    F --&gt; G[形成本轮 batch → Worker 执行]    G --&gt; H[新 KV 写入 blocks\nfull blocks 可进入 prefix cache]    H --&gt; I&#123;请求完成？&#125;    I -- 否 → decode 继续 --&gt; B    I -- 是 --&gt; J[引用计数减少\nfull blocks 按 LRU 保留或驱逐]</code></pre><h3 id="jie-duan-yi-scheduler-jie-guan-tong-yi-chou-xiang-de-qi-dian" tabindex="-1" id="阶段一：scheduler-接管，统一抽象的起点">阶段一：scheduler 接管，统一抽象的起点</h3><p>新请求进入后，系统首先面对的不是&quot;怎么算 attention&quot;，而是：当前 token 预算还有多少、哪些请求优先、这个请求的当前进度在哪里。</p><p>V1 unified scheduler 有一个关键的统一视角：它不把 prefill 和 decode 当成两套完全不同的调度问题，而是把&quot;让请求向前推进若干 token&quot;视为统一动作。每个请求都有&quot;已计算到哪里&quot;的位置，scheduler 的工作是在本轮预算内让每个请求尽量往目标前进。</p><p>这点的意义在后面会展开。</p><h3 id="jie-duan-er-prefix-cache-cha-xun-qi-pao-xian-qian-yi" tabindex="-1" id="阶段二：prefix-cache-查询，起跑线前移">阶段二：prefix cache 查询，起跑线前移</h3><p>请求不一定要从 token 0 开始 prefill。如果它的前缀和之前某个请求相同——相同 system prompt、相同 few-shot 示例、相同工具 schema——prefix cache 可以直接命中已有 blocks。</p><p>这里有一个细节很关键：prefix cache 的影响不是&quot;prefill 之后多了个小缓存&quot;，而是<strong>发生在调度之前</strong>——命中了前缀缓存，scheduler 在分配 token budget 时就不需要为这部分历史 token 花预算，KV cache manager 也不需要为这部分已存在的 full blocks 重新分配。</p><p>prefix cache 本质上直接改变了请求的起跑位置。</p><h3 id="jie-duan-san-kv-cache-manager-luo-ji-dao-wu-li-de-ying-she" tabindex="-1" id="阶段三：KV-Cache-Manager，逻辑到物理的映射">阶段三：KV Cache Manager，逻辑到物理的映射</h3><p>scheduler 决定推进多少 token 之后，现实问题是：这些 token 对应的 KV 要存在哪里？</p><p>KV cache manager 的核心职责不是&quot;算 attention&quot;，而是维护逻辑到物理的映射：这个请求当前持有哪些 blocks、哪些来自 prefix cache 复用、哪些是本请求新增的、block pool 还有多少空闲、不够时能否回收。</p><p>请求上下文在逻辑上连续，但在物理上不要求连续——这就是 PagedAttention 的价值所在。没有这一层，请求长度一增长就必须申请更大的连续显存区域，碎片化和搬迁开销都很重。Block 化之后，需要更多上下文时多拿几个 block，请求结束时把 block 还回去，两个请求共享前缀时共同引用相同的 cached blocks。</p><h3 id="jie-duan-si-worker-zhi-xing-hou-xin-full-blocks-cai-jin-ru-prefix-cache" tabindex="-1" id="阶段四：Worker-执行后，新-full-blocks-才进入-prefix-cache">阶段四：Worker 执行后，新 full blocks 才进入 prefix cache</h3><p>一轮 batch 执行后，新的 KV 写入 block，但<strong>不是所有新写入内容都会立刻成为 prefix cache 候选</strong>。</p><p>vLLM 只缓存 full blocks——只有 block 被完整填满后，才成为稳定、可哈希、可复用的缓存单元。原因：</p><ul><li><strong>边界稳定</strong>：不会因为后续 token 到来而改变 block 边界含义</li><li><strong>哈希身份明确</strong>：prefix caching 基于哈希，哈希成分包括 parent hash、block tokens、额外区分信息（如 LoRA adapter、tenant salt）；半满 block 的哈希身份不够稳定</li><li><strong>管理标准化</strong>：full block 可以当成独立的标准复用单元，LRU 驱逐逻辑也更简单</li></ul><p>所以 prefix cache 复用的是&quot;已形成稳定边界的历史块&quot;，不是任意长度的任意前缀。</p><h3 id="jie-duan-wu-decode-ji-xu-san-ceng-chi-xu-xie-tong" tabindex="-1" id="阶段五：Decode-继续，三层持续协同">阶段五：Decode 继续，三层持续协同</h3><p>请求不会因为完成一次 prefill 就脱离 scheduler。每轮 decode 仍然是同一条链：scheduler 决定本轮谁继续前进、KV cache manager 确认历史 blocks 和新增 blocks、worker 执行、请求状态更新。</p><p>Decode 和 prefill 的区别在于特征不同：每轮新增 token 少、频繁依赖历史 KV、更容易受显存带宽影响、对 ITL 更敏感。这就是 chunked prefill 开启时调度策略优先 decode 的原因——保证 decode 响应性，同时把 compute-bound 的 prefill 和 memory-bound 的 decode 混在同一个 batch 里提高 GPU 利用率。</p><h3 id="jie-duan-liu-qing-qiu-jie-shu-block-jie-chu-yin-yong-er-fei-zhi-jie-shan-chu" tabindex="-1" id="阶段六：请求结束，Block-解除引用而非直接删除">阶段六：请求结束，Block 解除引用而非直接删除</h3><p>请求完成后，block 不是&quot;全部删掉&quot;，而是引用计数减少。如果某些 full blocks 作为 prefix cache 仍有复用价值，可以暂时保留；cache 压力上来了，再按 LRU 驱逐。</p><p>这说明 prefix cache 是真正意义上的缓存系统，有完整的生命周期管理：block pool、free block queue、cache block map、request block map、LRU eviction。不是&quot;写个 dict 存一下&quot;这么简单。</p><hr><h2 id="san-qing-qiu-chang-jing-gong-xiang-fen-cha-bu-fen-ming-zhong" tabindex="-1" id="三请求场景：共享、分叉、部分命中">三请求场景：共享、分叉、部分命中</h2><p>这个例子把三层协同的真实效果展示得最清楚。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">场景：三个请求同时在系统中</span><br><span class="line"></span><br><span class="line">请求 A: [system prompt + 工具定义] + 用户问题1</span><br><span class="line">请求 B: [system prompt + 工具定义] + 用户问题2  ← 与 A 共享前缀</span><br><span class="line">请求 C: [system prompt + 工具定义] + RAG 证据 + 用户问题3  ← 部分共享</span><br><span class="line"></span><br><span class="line">                    ┌──────────────────────────────┐</span><br><span class="line">shared blocks       │  block1 │ block2 │ block3   │  ← A、B、C 共享</span><br><span class="line">                    └──────────────────────────────┘</span><br><span class="line">                          /             |         \</span><br><span class="line">         ┌─────────────┐     ┌──────────┘   ┌──────────────────┐</span><br><span class="line">A only   │ block4-A    │     │ block4-B    │   block4-C ...   │ C only</span><br><span class="line">         └─────────────┘     └─────────────┘   └──────────────────┘</span><br></pre></td></tr></table></figure><p><strong>A 先到</strong>，完成 prefill，shared blocks 成为 prefix cache 候选。</p><p><strong>B 后到</strong>，prefix cache 命中 <code>system prompt + 工具定义</code> 对应的 blocks，scheduler 看到它不需要从 token 0 开始，直接从分叉点之后继续新增 blocks。B 的&quot;少算&quot;，不是 worker 在 forward 里临时聪明一下，而是 scheduler + KV cache + prefix cache 三层提前协作的结果。</p><p><strong>A 和 B 同时 decode</strong> 时，shared blocks 继续共用，各自后续分叉出来的 blocks 独立。Block 化设计最适合做的事就是这样：前面共用，后面分叉。</p><p><strong>C 到来</strong>，只命中前半段。后半段 RAG 证据不同，必须重新 prefill。如果 RAG 内容很长，chunked prefill 会把它切成小块，避免拖死 A 和 B 的 decode。这时三层分工最清晰：prefix cache 解决&quot;能不能少算前半段&quot;，scheduler 解决&quot;后半段什么时候算&quot;，KV cache manager 解决&quot;共享块和新增块如何同时存在于 C 的请求上下文里&quot;。</p><hr><h2 id="chunked-prefill-yi-lai-zhe-san-ceng-bu-zhi-shi-diao-du-ji-qiao" tabindex="-1" id="Chunked-Prefill-依赖这三层，不只是调度技巧">Chunked Prefill 依赖这三层，不只是调度技巧</h2><p>很多人把 chunked prefill 理解为纯调度特性，但它和 KV Cache、Prefix Cache 强相关。</p><p>Chunked prefill 是 scheduler 对单请求推进量的再切分——本轮 token 预算里优先放 decode，剩余空间里放 prefill 的一部分，放不下就自动切块。</p><p>一旦切块，请求的历史状态会在多个轮次中逐渐增长，KV cache manager 需要支持这种逐块延展。Chunked prefill 不是&quot;与 KV cache 无关的纯调度技巧&quot;，它要求底层 KV 表示天然支持分段增长。</p><p>同时，如果请求前半段已经命中 prefix cache，它的 chunked prefill 不是从 token 0 开始切，而是从&quot;未命中的分叉点&quot;开始切。Prefix cache 改变了 scheduler 看到的剩余工作量，进而改变 chunked prefill 的起点。</p><hr><h2 id="unified-scheduler-de-he-xin-jie-zhi" tabindex="-1" id="Unified-Scheduler-的核心价值">Unified Scheduler 的核心价值</h2><p>V1 unified scheduler 的设计思想是：把 prefill、decode、chunked prefill、prefix cache 命中这些不同现象，统一成同一个问题——<strong>本轮预算下，这个请求还能往前走多远</strong>？</p><p>具体实现上，它按 <code>&#123;request_id: num_tokens&#125;</code> 风格，在固定 token budget 下动态分配，而不是把 prefill/decode 分裂成两套处理通道。</p><p>这个统一抽象的好处是可扩展性：如果系统把这些能力写成互相独立、彼此特判的分支（prefix cache 走一套逻辑、chunked prefill 走一套、speculative decode 再走一套），每加一个功能调度器复杂度就爆炸一次。V1 重构的核心价值之一，就是先抽象出&quot;请求推进&quot;这个统一问题，再把各种优化当成对推进起点、推进上限、预算占用方式的修正。</p><hr><h2 id="ji-ge-rong-yi-hun-yao-de-wen-ti" tabindex="-1" id="几个容易混淆的问题">几个容易混淆的问题</h2><p><strong>Prefix Cache 和普通 KV Cache 是一回事吗？</strong></p><p>不是。普通 KV Cache 是请求内复用——自己这个请求的历史上下文在后续 decode 中被复用。Prefix Cache 是请求间复用——这个请求已经计算过的前缀，被其他请求复用。两者都复用 KV blocks，但复用的对象层级不同。</p><p><strong>Prefix Cache 只影响 prefill 时间吗？</strong></p><p>主要收益体现在减少 prefill 重算，但影响不止于此。它还减少 token budget 占用，改变本轮 batch 能容纳多少其他请求，最终影响整体 TTFT、吞吐和并发能力。效果会传导到整个服务系统。</p><p><strong>Scheduler 是不是不需要知道 KV Cache 细节？</strong></p><p>它不需要知道每个底层张量怎么排布，但必须知道资源边界——可用 block 数量、cache 压力、是否需要 preemption / 延后。Scheduler 和 KV cache manager 职责不同，但绝不是完全隔离的。</p><p><strong>有了 Prefix Cache，是不是就不需要 Chunked Prefill 了？</strong></p><p>不是。Prefix Cache 只解决&quot;共享前缀不必重算&quot;的问题。但不是所有请求都共享长前缀，即使共享也常常只共享前半段，分叉后的剩余 prompt 可能仍然很长。这时 chunked prefill 仍然决定剩余 prefill 如何和 decode 共存。</p><hr><h2 id="mo-kuai-di-tu-yuan-ma-wei-zhi" tabindex="-1" id="模块地图：源码位置">模块地图：源码位置</h2><p>不需要逐行啃源码，但知道&quot;大概在哪&quot;有助于建立系统感：</p><table><thead><tr><th>模块</th><th>核心位置</th><th>关注点</th></tr></thead><tbody><tr><td>Scheduler</td><td><code>vllm/v1/engine/core.py</code><br><code>vllm/v1/core/sched/</code></td><td>请求如何进入调度循环、如何形成每轮 batch、如何与 token budget 绑定</td></tr><tr><td>KV Cache Manager</td><td><code>vllm/v1/core/kv_cache_manager.py</code></td><td>逻辑请求状态如何映射到 physical blocks</td></tr><tr><td>Prefix Cache</td><td>同上（集成在 KV cache 层）</td><td>hash-based block identity、block pool、free block queue、LRU eviction</td></tr></tbody></table><p>Prefix caching 的核心数据结构：block pool（全局 block 池）、free block queue（可分配的空闲 block）、cache block map（哈希 → cached block 的映射）、request block map（请求 → 其持有 block 的映射）、LRU eviction（驱逐策略）。</p><hr><h2 id="wu-ge-he-xin-jie-lun" tabindex="-1" id="五个核心结论">五个核心结论</h2><ol><li><p><strong>三层协同链</strong>：Scheduler 管推进，KV cache manager 管存储，prefix cache 管跨请求复用——三者是依赖链，不是独立特性的堆砌。</p></li><li><p><strong>Prefix cache 改变起跑线</strong>：它不是&quot;prefill 后的小缓存&quot;，而是在调度之前就改变了 scheduler 看到的剩余工作量，影响 token budget 分配和整体吞吐。</p></li><li><p><strong>Block 化是前提</strong>：prefix cache 之所以能做跨请求复用，根基是 block 化 KV cache 提供了可共享、可回收、有明确边界的存储单元。没有 block 化，prefix cache 无法高效实现。</p></li><li><p><strong>只缓存 full blocks</strong>：这是设计上的明智选择——边界稳定、哈希身份明确、驱逐管理标准化，使 prefix cache 更像标准缓存系统，而不是维护一棵不断变化的复杂树结构。</p></li><li><p><strong>Unified scheduler 的统一抽象</strong>：把 prefill / decode / chunked prefill / prefix hit 都统一成&quot;请求推进多少 token&quot;的问题，消除了各功能之间的特判分支，是 V1 重构里最重要的架构决策之一。</p></li></ol><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/bc6be055/">vLLM系统拆解-14-部署调优：从资源预算到线上问题诊断</a></li><li>下一篇：<a href="/posts/e7a2278e/">vLLM系统拆解-16-性能瓶颈定位：从现象到根因的分层诊断方法</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-15-diao-du-kv-cache-yu-prefix-caching-wei-shi-yao-bi-xu-san-ceng-xie-tong&quot; tabindex=&quot;-1&quot;&gt;vLLM系统拆解-15-调度、KV</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="KV Cache" scheme="https://smarter.xin/tags/kv-cache/"/>
    
    <category term="Scheduler" scheme="https://smarter.xin/tags/scheduler/"/>
    
    <category term="Prefix Caching" scheme="https://smarter.xin/tags/prefix-caching/"/>
    
    <category term="系统设计" scheme="https://smarter.xin/tags/system-design/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-14-部署调优：从资源预算到线上问题诊断</title>
    <link href="https://smarter.xin/posts/bc6be055/"/>
    <id>https://smarter.xin/posts/bc6be055/</id>
    <published>2026-04-02T07:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-14-bu-shu-diao-you-cong-zi-yuan-yu-suan-dao-xian-shang-wen-ti-zhen-duan" tabindex="-1">vLLM系统拆解-14-部署调优：从资源预算到线上问题诊断</h1><p>前几篇把 vLLM 的核心机制讲完了：PagedAttention、scheduler、prefix caching、chunked prefill、分布式并行……但这些理解停留在&quot;设计层面&quot;，还不够。</p><p>推理系统真正的问题从来不是&quot;这个设计对不对&quot;，而是：在当前这张 GPU、这个模型、这类请求、这个 QPS 下，哪里是瓶颈？应该调哪个参数？为什么改了之后反而更慢？</p><p>这篇文章的出发点是：<strong>vLLM 调优本质上是在给系统设置资源预算，而不是调几个孤立参数</strong>。围绕这个视角，会讲清楚关键指标、核心参数的系统含义、OOM 和 preemption 的诊断思路，以及不同 workload 对应的策略差异。</p><hr><h2 id="yi-ci-qing-qiu-xiao-hao-na-xie-zi-yuan" tabindex="-1" id="一次请求消耗哪些资源">一次请求消耗哪些资源</h2><p>调优之前，先要搞清楚一次推理请求到底会消耗什么。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│              一次在线推理请求的资源消耗                      │</span><br><span class="line">│                                                           │</span><br><span class="line">│  GPU 计算资源    prefill 的大规模矩阵乘法                   │</span><br><span class="line">│                  decode 在高并发时也有计算压力              │</span><br><span class="line">│                                                           │</span><br><span class="line">│  GPU 显存        模型权重常驻（静态）                        │</span><br><span class="line">│                  KV Cache 随历史长度增长（动态）             │</span><br><span class="line">│                  中间激活 / workspace / 采样开销（临时）     │</span><br><span class="line">│                                                           │</span><br><span class="line">│  GPU 访存带宽    decode 反复读 KV Cache                    │</span><br><span class="line">│                  → decode 往往是 memory-bound              │</span><br><span class="line">│                                                           │</span><br><span class="line">│  CPU 资源        HTTP 收发 / tokenization / detokenization │</span><br><span class="line">│                  engine core 调度循环（高频 busy loop）     │</span><br><span class="line">│                  worker 进程状态管理                        │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>线上性能不好，不能条件反射地说&quot;GPU 不够&quot;。常见的真实原因包括：GPU 显存不够导致 KV Cache 撑不住；decode 太多，GPU 不缺 FLOPS，缺的是显存带宽；batch 开得太大，吞吐上去了但 TTFT 飙升；CPU 调度跟不上，GPU 有余量但服务就是慢。</p><p>部署 vLLM，本质上是在给系统设置几个预算：KV Cache 可以用多少显存、每轮最多处理多少 token、每轮最多并发多少条序列、prefill 和 decode 如何共享批处理预算。这些预算通过几个关键参数体现出来。</p><hr><h2 id="xian-kan-zhi-biao-zai-dong-can-shu" tabindex="-1" id="先看指标，再动参数">先看指标，再动参数</h2><p>调优最常见的低效做法是：不清楚瓶颈在哪，上来就改参数，结果越调越乱。</p><p>先要稳定监控四类指标。</p><h3 id="yan-chi-ttft-yu-itl" tabindex="-1" id="延迟：TTFT-与-ITL">延迟：TTFT 与 ITL</h3><p><strong>首 token 延迟（Time To First Token，TTFT）</strong> 反映用户发请求后多久看到第一个 token。主要影响因素：prompt 长度、prefill 负载、当前排队情况、<code>max_num_batched_tokens</code> 是否过大挤压 decode、prefix caching 命中率。低延迟交互场景（chat、Copilot）TTFT 是核心指标。</p><p><strong>token 间延迟（Inter-Token Latency，ITL）</strong> 反映相邻输出 token 的平均间隔，直接决定用户感知的输出流畅度。主要影响因素：decode batch 是否过大、chunked prefill 是否给 decode 足够优先级、speculative decoding 有效性、GPU 是否 memory-bound。</p><p>TTFT 和 ITL 经常相互制约——为了降 TTFT 可能需要更快进入 prefill，但这可能挤占 decode，反而拉高 ITL。</p><h3 id="tun-tu-request-throughput-vs-token-throughput" tabindex="-1" id="吞吐：request-throughput-vs-token-throughput">吞吐：request throughput vs token throughput</h3><p>做 infra 更常用 <strong>token throughput</strong>（单位时间处理多少输入/输出 token），因为它更直接反映系统真实工作量。</p><p>要注意：高 token throughput 不代表用户体验好。很多提吞吐的设置会把 TTFT/ITL 拉坏；吞吐优化往往意味着更激进的 batch、更高的资源利用率、更强的尾延迟风险。评估吞吐提升时，要同时说清楚代价是什么，适用于什么场景。</p><h3 id="kv-cache-yu-xian-cun" tabindex="-1" id="KV-Cache-与显存">KV Cache 与显存</h3><p>重点关注：GPU 显存总体使用率、KV Cache 占用情况、是否发生 preemption / recomputation、当前 block 使用率是否过高。</p><p>频繁 preemption 通常意味着：KV Cache 空间不够、并发太高、请求太长、相关参数设置过激进，或 <code>gpu_memory_utilization</code> 给 KV Cache 的预算太保守。这类问题靠&quot;继续加 batch&quot;解决不了，往往要先收缩预算。</p><h3 id="cpu-zhi-biao" tabindex="-1" id="CPU-指标">CPU 指标</h3><p>这一点最容易被忽视。vLLM 不是纯 GPU 程序，它是 API server + engine core + worker 的多进程服务系统。CPU 负责 HTTP 收发、tokenization/detokenization、engine core 调度循环、请求状态维护。</p><p>CPU 不足时的典型现象：GPU 利用率不稳定，延迟抖动大，QPS 上不去，明明 GPU 还有余量但服务就是跑不快。面试里提到这一点会很加分，因为很多人把 vLLM 理解成&quot;纯 GPU 优化框架&quot;，忽视了它是多进程服务系统这个基本事实。</p><hr><h2 id="wu-ge-he-xin-can-shu-de-xi-tong-han-yi" tabindex="-1" id="五个核心参数的系统含义">五个核心参数的系统含义</h2><p>不需要背所有启动参数，但以下五个参数必须真正理解——它们控制的是整体资源格局。</p><table><thead><tr><th>参数</th><th>控制的本质</th><th>调高效果</th><th>调低效果</th><th>风险</th></tr></thead><tbody><tr><td><code>gpu_memory_utilization</code></td><td>实例级显存预算</td><td>KV Cache 空间增加，可承载更多并发/长上下文</td><td>KV Cache 收紧，preemption 风险上升</td><td>过高会挤压其他必要显存，多实例共卡时尤危险</td></tr><tr><td><code>kv_cache_memory_bytes</code></td><td>直接指定 KV Cache 字节数</td><td>更精细的缓存预算控制</td><td>—</td><td>需要准确估算权重和 buffer 占用</td></tr><tr><td><code>max_num_batched_tokens</code></td><td>每轮调度的 token 预算上限</td><td>吞吐更高，prefill 更容易吃满</td><td>decode 优先级更高，ITL 更好</td><td>过大会恶化 TTFT/ITL</td></tr><tr><td><code>max_num_seqs</code></td><td>单轮并发请求条数上限</td><td>并发能力更强</td><td>调度和管理开销降低</td><td>太大会增加状态维护开销</td></tr><tr><td><code>max_model_len</code></td><td>最大上下文长度上界</td><td>功能上支持更长输入</td><td>KV Cache 压力降低，并发能力上升</td><td>按模型理论上限设会大幅压缩可服务并发</td></tr></tbody></table><p>几点补充说明：</p><p><strong><code>max_num_batched_tokens</code> 不是&quot;batch 大小&quot;的同义词</strong>。它约束的是每轮总 token 工作量，而不是请求条数。在长 prompt 场景下，一个请求就可能消耗大量预算。<code>max_num_seqs</code> 和它是不同维度：前者更像限制 batch 的宽度（请求条数），后者更像限制 batch 的体积（token 总量）。</p><p><strong><code>max_model_len</code> 是容量规划参数，不是功能参数</strong>。把它设为模型理论上限（如 128K）会按最坏情况分配 KV Cache 预算，显著压缩可服务并发。生产环境里应按真实 workload 的 P99 长度来设，不是追求理论最大上下文。</p><p><strong><code>kv_cache_memory_bytes</code> 是 <code>gpu_memory_utilization</code> 的精细化版本</strong>。后者是比例配置，前者直接指定字节数。多实例同卡、固定化部署、容量规划压测等场景，精确字节配置比比例配置更可控。</p><hr><h2 id="oom-san-fen-lei" tabindex="-1" id="OOM-三分类">OOM 三分类</h2><p>很多人把 OOM 理解为&quot;显存不够&quot;，这太粗了。把 OOM 分成三类，诊断思路会清晰很多。</p><h3 id="mo-xing-quan-zhong-ceng-mian-oom" tabindex="-1" id="模型权重层面-OOM">模型权重层面 OOM</h3><p>模型太大、TP 不够、量化没做、单卡装不下。这种 OOM 通常在启动或初始化时就出现，属于静态问题。解决方向：更小模型、量化、增加 tensor parallel、换更大显存 GPU。</p><h3 id="kv-cache-rong-liang-ceng-mian-oom" tabindex="-1" id="KV-Cache-容量层面-OOM">KV Cache 容量层面 OOM</h3><p>模型已经起来，但随着请求积累——prompt 很长、输出也长、并发多、prefix cache 和 active requests 同时占空间——KV Cache 被撑爆。这是 vLLM 更常见也更关键的 OOM 来源。</p><p>处理方向：提高 <code>gpu_memory_utilization</code> / 精确设置 <code>kv_cache_memory_bytes</code>、降低 <code>max_num_batched_tokens</code> / <code>max_num_seqs</code>、降低 <code>max_model_len</code>、控制请求长度和并发规模。本质是调整&quot;缓存预算&quot;。</p><h3 id="yun-xing-shi-feng-zhi-oom" tabindex="-1" id="运行时峰值-OOM">运行时峰值 OOM</h3><p>不是常驻内存不够，而是某轮执行时的瞬时峰值太高——某次 prefill 特别大、某个 batch 请求组合极端、某些 kernel / buffer 临时分配冲高。这种 OOM 可能不是每次都复现，比静态 OOM 更难排查。</p><p>处理方向：降低单轮 token 预算、限制异常长请求、缩小 batch 规模、保留足够的显存安全余量。</p><hr><h2 id="preemption-yi-wei-zhao-shi-yao" tabindex="-1" id="Preemption-意味着什么">Preemption 意味着什么</h2><p>在 vLLM 日志或监控里看到 preemption，不要只把它当成一条 warning。</p><p>它的本质信号是：<strong>当前配置下，系统已经无法同时优雅容纳所有活跃请求的 KV Cache 需求，开始用代价更高的方式维持运行</strong>（V1 默认是 RECOMPUTE，即丢弃并重算）。</p><p>Preemption 往往伴随：recomputation 带来的时延上升、吞吐波动、尾延迟恶化。偶发 preemption 可以接受，但高频 preemption 通常说明配置不健康，workload 和资源预算之间出现了张力。</p><p>看到频繁 preemption 时，排查顺序：</p><ol><li>KV Cache 预算是否太紧（<code>gpu_memory_utilization</code> 过低）</li><li>并发是否压得太高（<code>max_num_seqs</code> 过激进）</li><li>请求长度分布是否超出预期</li><li><code>max_num_batched_tokens</code> 是否开太大</li><li><code>max_model_len</code> 是否定得远超真实 workload</li></ol><hr><h2 id="cpu-ping-jing-rong-yi-bei-hu-shi-de-xing-neng-tian-hua-ban" tabindex="-1" id="CPU-瓶颈：容易被忽视的性能天花板">CPU 瓶颈：容易被忽视的性能天花板</h2><p>GPU 看起来有余量，但服务跑不快——这个现象在 vLLM 里不少见，根因往往在 CPU。</p><p>vLLM 的 engine core 本身是一个高频 busy loop 型调度核心，持续在 CPU 上跑请求调度、block 管理、batch 组装。同时 API server 还要处理 HTTP 请求、做 tokenization 和 detokenization，这些全都是 CPU 密集操作。</p><p>如果物理 CPU 核数不够、被其他进程抢占严重、或者 NUMA 绑定不合理，调度效率就会下降，最终表现为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">GPU 利用率忽高忽低  → 调度循环不及时，GPU 被饿着</span><br><span class="line">延迟抖动大          → 调度响应不稳定</span><br><span class="line">QPS 上限低          → tokenization / detokenization 是瓶颈</span><br><span class="line">GPU 有余量但慢      → CPU 本身是天花板</span><br></pre></td></tr></table></figure><p>部署时 CPU 核数与进程数的匹配和 GPU 的容量规划同样重要，不能只看 GPU 规格。</p><hr><h2 id="bu-tong-workload-de-diao-you-ce-lue" tabindex="-1" id="不同-Workload-的调优策略">不同 Workload 的调优策略</h2><p>这是调优里最容易忽视的地方：不同业务目标，最优配置完全不同，甚至可能相反。</p><table><thead><tr><th>Workload 类型</th><th>核心目标</th><th>关键参数方向</th><th>特别注意</th></tr></thead><tbody><tr><td><strong>低并发交互型</strong>（chat / Copilot）</td><td>TTFT 低、ITL 低、输出流畅</td><td><code>max_num_batched_tokens</code> 偏保守，不追求峰值吞吐</td><td>prefix caching 在系统 prompt 高复用时价值大；speculative decoding 值得试验</td></tr><tr><td><strong>高并发吞吐型</strong>（API 平台 / 批处理）</td><td>token throughput 高、GPU 利用率高、单位成本低</td><td><code>max_num_batched_tokens</code> 可更激进，接受 TTFT 上升</td><td>严密监控 preemption 和尾延迟；吞吐和交互体验不是一套最优配置</td></tr><tr><td><strong>长上下文型</strong>（文档 QA / 代码仓库分析）</td><td>支撑长上下文、KV Cache 不爆、系统稳定</td><td><code>max_model_len</code> 按真实需求设，提高 KV Cache 预算，不追求高并发</td><td>长上下文最先打到的是缓存容量和访存压力，而不是算力</td></tr><tr><td><strong>多租户 / 多实例同卡</strong></td><td>资源隔离、峰值不互相干扰、稳定性</td><td>不要把 <code>gpu_memory_utilization</code> 拉满，精确显存预算，严格容量规划</td><td>平均可用 ≠ 峰值安全；多实例峰值叠加是主要风险</td></tr></tbody></table><hr><h2 id="diao-you-shun-xu-xian-wen-zai-kuai" tabindex="-1" id="调优顺序：先稳，再快">调优顺序：先稳，再快</h2><p>乱试参数是最低效的调优方式。一套有效的调优顺序应该是：</p><pre><code class="highlight mermaid">flowchart TD    A[第一步：定义目标\n优化 TTFT？ITL？吞吐？稳定性？] --&gt; B    B[第二步：测量 workload 特征\n平均/P99 prompt 长度、输出长度\n峰值并发、前缀重复率、请求类型] --&gt; C    C[第三步：容量安全\n模型稳定起来\n不频繁 OOM\npreemption 不失控\nCPU 核数与进程匹配] --&gt; D    D[第四步：调主干参数\ngpu_memory_utilization / kv_cache_memory_bytes\nmax_num_batched_tokens\nmax_num_seqs\nmax_model_len] --&gt; E    E[第五步：叠高级优化\nprefix caching 命中率\nspeculative decoding 有效性\n量化引入\n分布式并行策略]</code></pre><p>核心原则：先定义目标，再看指标，先求稳，再求快。用别人的 benchmark 参数直接照搬在自己场景往往无效，因为短 prompt 高频 chat 和长上下文文档问答，最优配置完全可能相反。</p><hr><h2 id="wu-wen-zhen-duan-kuang-jia" tabindex="-1" id="五问诊断框架">五问诊断框架</h2><p>遇到线上性能问题，强制自己按这五个问题想，大多数问题都会变得有条理：</p><p><strong>1. 计算瓶颈还是缓存/访存瓶颈？</strong><br>Prefill 更可能算力敏感；decode 更可能 memory-bound。不同瓶颈对应完全不同的优化方向。</p><p><strong>2. GPU 瓶颈还是 CPU 瓶颈？</strong><br>GPU 很闲但延迟很高，不一定是 GPU 问题。先看 engine core 和 tokenization 的 CPU 占用。</p><p><strong>3. Steady-state 容量问题还是峰值问题？</strong><br>平时都撑不住，还是只有长尾请求冲高时才出问题？前者要调基础容量，后者要留安全余量。</p><p><strong>4. 参数问题还是 workload 问题？</strong><br>当前配置不合理，还是用户请求长度/并发分布本身太极端？后者靠调参解决不了，可能需要限流或容量扩充。</p><p><strong>5. 想更快还是想更稳？</strong><br>提吞吐时通常不能同时保延迟；保稳定时往往要留 buffer。目标不清晰，调优没有终点。</p><hr><h2 id="xian-shang-gao-pin-wen-ti-de-zhen-duan-yao-dian" tabindex="-1" id="线上高频问题的诊断要点">线上高频问题的诊断要点</h2><p>把几个常见面试问题的分析思路整理在这里，可以直接对照实际场景使用。</p><p><strong>线上频繁 preemption，怎么分析？</strong><br>先把它当成 KV Cache 压力信号，依次排查：KV Cache 预算是否过小、并发是否过高、请求长度分布是否超出预期、<code>max_num_batched_tokens</code>/<code>max_num_seqs</code> 是否过激进、<code>max_model_len</code> 是否定得太大。高频 preemption 说明 workload 和资源预算出现了张力。</p><p><strong>GPU 没打满但服务很慢？</strong><br>先排查 CPU：engine core 调度循环是否饥饿、tokenization 是否是瓶颈、CPU 核数是否和进程数匹配。其次看是否有 preemption 导致 recomputation，以及是否有异常长请求拖慢整个 batch。</p><p><strong>OOM 如何定位是哪类？</strong><br>看 OOM 出现时机：启动/初始化时出现 → 模型权重层面；运行一段时间后随请求积累出现 → KV Cache 容量层面；偶发不规律 → 运行时峰值层面。不同类型的处理方向完全不同。</p><p><strong>为什么不能把 <code>max_model_len</code> 开到模型上限？</strong><br>因为 <code>max_model_len</code> 决定系统按多坏的情况预留 KV Cache。把它设到模型理论上限（如 128K）意味着系统要为每个请求预留极大的最坏情况缓存，会显著压缩可服务并发。生产环境里应按真实 workload 的 P99 长度来设。</p><hr><h2 id="he-xin-jie-lun" tabindex="-1" id="核心结论">核心结论</h2><p>调优思维可以收束为四句话：</p><ol><li><strong>vLLM 调优是在调资源预算</strong>，不是在调几个孤立参数——每个参数背后都有它影响的资源格局和机制链路。</li><li><strong>吞吐、TTFT、ITL、显存容量、并发能力之间天然存在张力</strong>，不可能同时极致，调优本质上是在给定业务目标下找平衡点。</li><li><strong>线上问题很多不是 GPU 算不动</strong>，而是 KV Cache 容量不足、CPU 调度跟不上、或 workload 形态和参数设置不匹配。</li><li><strong>成熟的调优方式</strong>是先定义目标、看指标、做容量安全，再逐层优化——先稳，再快。</li></ol><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/84b005d4/">vLLM系统拆解-13-推理框架对比：vLLM、TGI、TensorRT-LLM与SGLang</a></li><li>下一篇：<a href="/posts/cf18dd3a/">vLLM系统拆解-15-调度、KV Cache与Prefix Caching：为什么必须三层协同</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-14-bu-shu-diao-you-cong-zi-yuan-yu-suan-dao-xian-shang-wen-ti-zhen-duan&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="TTFT" scheme="https://smarter.xin/tags/ttft/"/>
    
    <category term="ITL" scheme="https://smarter.xin/tags/itl/"/>
    
    <category term="部署调优" scheme="https://smarter.xin/tags/deployment-tuning/"/>
    
    <category term="瓶颈定位" scheme="https://smarter.xin/tags/bottleneck-analysis/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-13-推理框架对比：vLLM、TGI、TensorRT-LLM与SGLang</title>
    <link href="https://smarter.xin/posts/84b005d4/"/>
    <id>https://smarter.xin/posts/84b005d4/</id>
    <published>2026-04-02T06:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-13-tui-li-kuang-jia-dui-bi-v-llm-tgi-tensor-rt-llm-yu-sg-lang" tabindex="-1">vLLM系统拆解-13-推理框架对比：vLLM、TGI、TensorRT-LLM与SGLang</h1><p>推理框架的对比题很容易被答成速度排名，但那种答案其实站不住脚——不同框架面向的问题不同，放在不同团队/不同场景下结论会完全不一样。</p><p>这篇文章建立一个比较框架，然后用它拆解四组对比：Transformers vs vLLM、TGI vs vLLM、TensorRT-LLM vs vLLM、SGLang vs vLLM。读完应该能回答&quot;为什么很多团队先考虑 vLLM&quot;，同时也能说清楚&quot;什么情况下 vLLM 不是最合适的选项&quot;。</p><hr><h2 id="bi-jiao-kuang-jia-xian-ming-que-zai-bi-shi-yao" tabindex="-1" id="比较框架：先明确在比什么">比较框架：先明确在比什么</h2><p>不同定位的系统放在一起比，结论必然失真。比较之前，先统一五个维度：</p><table><thead><tr><th>维度</th><th>要回答的问题</th></tr></thead><tbody><tr><td><strong>定位</strong></td><td>这是模型库、推理引擎，还是完整服务框架？</td></tr><tr><td><strong>核心优化点</strong></td><td>它主要靠什么变快——调度、KV 管理、底层 kernel，还是减少重复计算？</td></tr><tr><td><strong>工程风格</strong></td><td>更偏易用/通用，还是更偏硬件极致优化？</td></tr><tr><td><strong>适用场景</strong></td><td>离线、在线、研究、生产、多卡、MoE、复杂 agent 工作流等</td></tr><tr><td><strong>代价</strong></td><td>迁移成本、调试成本、模型支持成本、硬件依赖</td></tr></tbody></table><p>“核心优化点&quot;这个维度要说具体——快在 batching、快在 KV 管理、快在 kernel、快在减少重复计算，这些是有区别的。一个成熟的选型判断一定包含&quot;它强在哪，也弱在哪；适合谁，也不适合谁”。</p><p>推理系统选型，本质上是在<strong>性能、灵活性、可维护性、硬件绑定程度、生态兼容性</strong>之间做平衡，不是找峰值吞吐的冠军。</p><hr><h2 id="v-llm-vs-transformers-bu-zai-tong-yi-ceng-mei-you-zhi-jie-ke-bi-xing" tabindex="-1" id="vLLM-vs-Transformers：不在同一层，没有直接可比性">vLLM vs Transformers：不在同一层，没有直接可比性</h2><p>这是最基础的一组，也是最容易答歪的一组。</p><h3 id="liang-zhe-jie-jue-de-shi-bu-tong-ceng-ci-de-wen-ti" tabindex="-1" id="两者解决的是不同层次的问题">两者解决的是不同层次的问题</h3><p><strong>Transformers</strong> 是通用模型调用库：提供 tokenizer、model class、generate API，方便研究、实验、单机调用、微调前后衔接。它的目标不是高并发在线服务，模型覆盖广、训练/推理兼容、社区生态强、新模型接入快——这些才是它的优先项。</p><p><strong>vLLM</strong> 是面向 LLM serving 的推理引擎：重点解决多请求并发、KV Cache 管理、动态调度、吞吐和延迟平衡。</p><p>差别不只是性能数字，而是设计目标从一开始就不同。</p><h3 id="yong-code-model-generate-code-zuo-fu-wu-hui-yu-dao-shi-yao" tabindex="-1" id="用-model-generate-做服务会遇到什么">用 <code>model.generate()</code> 做服务会遇到什么</h3><p>直接基于 Transformers 做在线服务可以&quot;能用&quot;，但并发一多就会暴露三个典型问题：</p><p><strong>KV Cache 不够服务化。</strong> Transformers 的 KV Cache 更像&quot;单请求执行过程中的内部状态&quot;。vLLM 把 KV Cache 提升为一个被专门管理的系统资源——分 block 管理、可回收、可复用、可调度、可参与 prefix caching。这是&quot;库&quot;和&quot;引擎&quot;在设计层面的根本差异。</p><p><strong>批处理不面向在线场景。</strong> Transformers 可以做 batch，但不是围绕 continuous batching 和在线请求生命周期设计的。vLLM 从一开始就把&quot;不断有请求进入、退出、继续生成&quot;的场景当成核心。</p><p><strong>服务性能优化不是主线。</strong> Transformers 要同时服务研究、训练、推理、生态扩展等多个目标；vLLM 的目标更集中：把 LLM serving 这件事做强。</p><p>更准确的理解是：Transformers 主要解决&quot;模型怎么定义和调用&quot;，vLLM 主要解决&quot;模型怎么高效服务化&quot;。两者不是替代关系，是不同层次。实际工程里的常见做法——模型生态靠 HF，对外服务靠 vLLM。</p><hr><h2 id="v-llm-vs-tgi-tong-yang-mian-xiang-sheng-chan-gong-cheng-zhu-zhou-bu-tong" tabindex="-1" id="vLLM-vs-TGI：同样面向生产，工程主轴不同">vLLM vs TGI：同样面向生产，工程主轴不同</h2><p>这组是真正的直接竞品比较。TGI（Text Generation Inference）是 Hugging Face 官方的生产级推理服务框架，不要把它当成&quot;简陋 baseline&quot;——它本身就有 continuous batching、paged attention、KV Cache、streaming、quantization、tensor parallel 等能力，服务工程化成熟，与 HF 平台生态天然衔接。</p><p>两者在 feature 层面高度重叠，真正的差异在<strong>工程主轴</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">vLLM 的主轴：LLM runtime / inference engine</span><br><span class="line">  核心亮点：paged KV Cache、request scheduling、prefix caching、chunked prefill</span><br><span class="line">  优先目标：serving 性能和调优空间</span><br><span class="line"></span><br><span class="line">TGI 的主轴：完整生产服务框架</span><br><span class="line">  核心亮点：HF 生态衔接、工程链条完整、容器化部署和平台接入成熟</span><br><span class="line">  优先目标：稳定可用的文本生成服务</span><br></pre></td></tr></table></figure><p>这两个主轴有重叠，但侧重不同。一个直观的类比：vLLM 更像 runtime / engine，TGI 更像 production serving stack。</p><p><strong>选型参考：</strong></p><ul><li>更追求推理 runtime 的性能上限和调优空间 → vLLM 更自然</li><li>团队已深度基于 HF 工具链，追求快速搭建稳定服务 → TGI 也是合理选择</li></ul><hr><h2 id="v-llm-vs-tensor-rt-llm-tong-yong-qiang-vs-shen-du-nvidia-bang-ding" tabindex="-1" id="vLLM-vs-TensorRT-LLM：通用强-vs-深度-NVIDIA-绑定">vLLM vs TensorRT-LLM：通用强 vs 深度 NVIDIA 绑定</h2><p>这是最容易被说偏的一组。</p><h3 id="tensor-rt-llm-xian-zai-shi-shi-yao" tabindex="-1" id="TensorRT-LLM-现在是什么">TensorRT-LLM 现在是什么</h3><p>TensorRT-LLM 已经是完整的大模型推理系统栈，不只是底层 kernel 库。它同样有：</p><ul><li>In-flight batching（等同于 continuous batching）</li><li>Paged KV Cache / paged attention</li><li>Chunked context（等同于 chunked prefill）</li><li>Speculative decoding</li><li>大规模并行部署支持</li></ul><p>所以在 feature 层面两者重叠很多，&quot;TensorRT-LLM 就是底层 kernel&quot;这个说法已经不准确。</p><h3 id="zhen-zheng-de-chai-yi-gong-cheng-quan-heng-bu-tong" tabindex="-1" id="真正的差异：工程权衡不同">真正的差异：工程权衡不同</h3><table><thead><tr><th>维度</th><th>vLLM</th><th>TensorRT-LLM</th></tr></thead><tbody><tr><td>硬件绑定</td><td>相对通用，支持多种 GPU</td><td>深度 NVIDIA 绑定，针对 CUDA / 特定 GPU 架构优化</td></tr><tr><td>工程门槛</td><td>较低，与 HF 模型兼容性好</td><td>较高，需要更多编译和优化配置</td></tr><tr><td>新模型接入速度</td><td>快，紧跟 HF 生态</td><td>相对慢，需要更多适配工程</td></tr><tr><td>极致性能上限</td><td>高</td><td>在 NVIDIA 平台上通常更高</td></tr><tr><td>主要代价</td><td>某些极端场景不如专用方案</td><td>更强硬件绑定，工程复杂度更高</td></tr></tbody></table><p>TensorRT-LLM 适合的条件比较具体：单一 NVIDIA 集群、模型族相对固定、非常重视极限吞吐和低延迟、有足够 infra 人力支撑。这几个条件同时成立，TensorRT-LLM 往往很值得深入。</p><p>如果团队更需要较低迁移成本、快速接主流 HF 模型、更通用的 serving 框架，vLLM 通常更容易先落地。</p><p>两者不是&quot;一个先进、一个落后&quot;的关系，是面向不同约束条件下的不同工程权衡。</p><hr><h2 id="v-llm-vs-sg-lang-serving-yin-qing-vs-llm-cheng-xu-zhi-xing-kuang-jia" tabindex="-1" id="vLLM-vs-SGLang：serving-引擎-vs-LLM-程序执行框架">vLLM vs SGLang：serving 引擎 vs LLM 程序执行框架</h2><p>这组最容易被忽视，但近两年越来越重要。</p><h3 id="sg-lang-bu-zhi-shi-quot-ling-yi-ge-gao-xing-neng-serving-kuang-jia-quot" tabindex="-1" id="SGLang-不只是-另一个高性能-serving-框架">SGLang 不只是&quot;另一个高性能 serving 框架&quot;</h3><p>SGLang 有两个并列的设计目标：</p><ol><li>高性能 serving runtime（这部分和 vLLM 直接竞争）</li><li><strong>前端程序表达能力</strong>：支持多轮程序式调用、复杂控制流、agent / workflow / structured generation、多次生成之间的上下文复用</li></ol><p>第二点是 vLLM 没有着重做的部分。这让 SGLang 常被描述为&quot;既是 serving runtime，也是 LLM program runtime&quot;。</p><h3 id="liang-zhe-de-si-wei-zhong-xin" tabindex="-1" id="两者的思维重心">两者的思维重心</h3><p><strong>vLLM 的出发点</strong>：怎样把单轮/多请求 LLM serving 做得又快又稳。</p><p><strong>SGLang 的出发点</strong>：怎样把复杂 LLM 程序写出来，并让 runtime 帮你高效执行。</p><p>当然两者有重叠，但不是完全相同的问题。</p><h3 id="radix-attention-vs-prefix-caching" tabindex="-1" id="RadixAttention-vs-Prefix-Caching">RadixAttention vs Prefix Caching</h3><p>两者都重视上下文复用，但侧重不同：</p><ul><li>vLLM 的 prefix caching 是 serving 层面的跨请求 KV 块复用，核心在调度和内存管理</li><li>SGLang 的 RadixAttention 更强调多次相关调用之间的前缀共享，使程序式调用之间的 KV 复用更自然</li></ul><p>不建议在面试里把两者讲成&quot;谁更先进&quot;，更准确的说法是：两者都很重视上下文复用和高性能 serving，但 vLLM 更偏通用高性能推理引擎，SGLang 更进一步把复杂 LLM 程序执行当成一等公民。</p><p><strong>场景判断：</strong></p><ul><li>标准 chat completion、高并发文本生成、以 serving 稳定性和吞吐为主 → vLLM 更自然</li><li>agent workflow、多步程序式生成、多段 prompt 多次调用共享上下文 → SGLang 可能更有吸引力</li></ul><hr><h2 id="wu-xi-tong-heng-xiang-dui-bi" tabindex="-1" id="五系统横向对比">五系统横向对比</h2><p>把四组比较收束，加上 Transformers，整理成一张对比表：</p><table><thead><tr><th>系统</th><th>更像什么</th><th>核心优势</th><th>主要代价/局限</th><th>更适合的场景</th></tr></thead><tbody><tr><td><strong>Transformers</strong></td><td>通用模型库</td><td>模型生态最广，调用简单，研究友好</td><td>不以高并发服务为第一目标，缺乏服务化 KV 管理</td><td>研究、实验、原型、离线调用</td></tr><tr><td><strong>vLLM</strong></td><td>高性能通用 serving runtime</td><td>paged KV Cache、continuous batching、prefix caching、综合平衡强</td><td>某些极端场景不如专用框架</td><td>主流 LLM 在线服务、性能与工程成本平衡场景</td></tr><tr><td><strong>TGI</strong></td><td>生产级文本生成服务框架</td><td>HF 生态衔接自然，服务工程化成熟</td><td>runtime 调优主轴不如 vLLM 鲜明</td><td>HF 平台生态、快速搭建生产服务</td></tr><tr><td><strong>TensorRT-LLM</strong></td><td>NVIDIA 深度优化推理系统</td><td>NVIDIA 平台极致性能，底层优化深度高</td><td>强硬件绑定，工程复杂度更高</td><td>固定 NVIDIA 集群、追求极限性能、有足够 infra 投入</td></tr><tr><td><strong>SGLang</strong></td><td>高性能 runtime + LLM 程序执行框架</td><td>高性能 serving + 强程序表达能力</td><td>使用范式比标准 serving 更复杂</td><td>agent workflow、复杂多步程序式推理</td></tr></tbody></table><hr><h2 id="wei-shi-yao-hen-duo-tuan-dui-hui-xian-kao-lu-v-llm" tabindex="-1" id="为什么很多团队会先考虑-vLLM">为什么很多团队会先考虑 vLLM</h2><p>vLLM 成为很多团队默认优先选项，不是因为它在所有 benchmark 上绝对第一，原因更务实。</p><p><strong>有明显强于普通调用库的系统级优化。</strong> Paged KV Cache、continuous batching、prefix caching、chunked prefill、speculative decoding、serving runtime 级别的调度——这些机制使它和&quot;直接拿 Transformers 开服务&quot;不在一个层次。</p><p><strong>没有像某些极致优化方案那样把门槛拉得很高。</strong> 与 HF 模型衔接自然，上手相对容易，快速落地的同时仍保留很强的调优空间。</p><p><strong>处在一个实用的平衡点：</strong> 比通用库更像推理系统，比极致硬件框架更容易用，比平台封装方案更可研究和理解。</p><p>这个平衡点不是最极端的，但在真实工程约束下（模型经常迭代、硬件环境不是单一 NVIDIA 集群、团队 infra 人力有限），它是很多团队最容易接受的选择。</p><hr><h2 id="liu-ge-chang-jian-de-bi-jiao-wu-qu" tabindex="-1" id="六个常见的比较误区">六个常见的比较误区</h2><p>这六个误区在对比类问题里非常高频，值得专门列出来：</p><table><thead><tr><th>误区</th><th>正确理解</th></tr></thead><tbody><tr><td>把 Transformers 和 vLLM 当成同一层产品比较</td><td>一个是模型库，一个是服务引擎，层次不同，直接比快慢没有意义</td></tr><tr><td>只说&quot;vLLM 更快&quot;</td><td>要说清楚快在 KV 管理、调度、跨请求复用这些具体机制上</td></tr><tr><td>把 TensorRT-LLM 描述成&quot;只有底层 kernel&quot;</td><td>它已经是完整的大模型推理系统栈，有 in-flight batching / paged KV Cache / chunked context / speculative decoding</td></tr><tr><td>把 TGI 讲得很弱</td><td>TGI 是成熟的生产 serving 方案，不是简陋 baseline</td></tr><tr><td>把 SGLang 讲成&quot;另一个 vLLM&quot;</td><td>SGLang 还把复杂 LLM 程序执行当成一等公民，这是独立的设计目标</td></tr><tr><td>只比较速度，不比较工程成本</td><td>真正的选型考量包括：性能、延迟、模型兼容性、部署复杂度、调试成本、硬件依赖、运维成本</td></tr></tbody></table><hr><h2 id="xuan-xing-de-shi-zhi" tabindex="-1" id="选型的实质">选型的实质</h2><p>各系统的关键词——continuous batching、paged KV Cache、chunked prefill、speculative decoding——现在已经高度重叠，这意味着&quot;有没有这个 feature&quot;越来越不是核心差异。</p><p>真正拉开差别的，是它们把这些 feature 组织成了一个什么风格的系统：追求通用还是极致、绑定 HF 生态还是 NVIDIA 生态、把 serving 当终点还是把 LLM 程序执行当终点。</p><p>选型不是找峰值吞吐的冠军，而是在你的约束条件（硬件环境、模型迭代速度、团队能力、业务场景）下，找到性能与工程成本之间最合适的平衡点。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/354d88e4/">vLLM系统拆解-12-源码地图：每个核心能力落在哪一层</a></li><li>下一篇：<a href="/posts/bc6be055/">vLLM系统拆解-14-部署调优：从资源预算到线上问题诊断</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-13-tui-li-kuang-jia-dui-bi-v-llm-tgi-tensor-rt-llm-yu-sg-lang&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="TGI" scheme="https://smarter.xin/tags/tgi/"/>
    
    <category term="TensorRT-LLM" scheme="https://smarter.xin/tags/tensorrt-llm/"/>
    
    <category term="SGLang" scheme="https://smarter.xin/tags/sglang/"/>
    
    <category term="系统选型" scheme="https://smarter.xin/tags/system-selection/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-12-源码地图：每个核心能力落在哪一层</title>
    <link href="https://smarter.xin/posts/354d88e4/"/>
    <id>https://smarter.xin/posts/354d88e4/</id>
    <published>2026-04-02T06:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-12-yuan-ma-di-tu-mei-ge-he-xin-neng-li-luo-zai-na-yi-ceng" tabindex="-1">vLLM系统拆解-12-源码地图：每个核心能力落在哪一层</h1><p>这篇文章不要求逐行读源码，但需要建立一张「层次地图」：vLLM 的每个核心能力大致落在哪类模块，面试官追问实现位置时能接住，能把「设计目标→模块分工→运行链路」讲成完整逻辑。</p><p>理解 vLLM 源码结构，最重要的视角不是「核心代码在哪个文件」，而是「这个问题属于哪一层」。</p><hr><h2 id="v-llm-de-qi-ceng-jie-gou" tabindex="-1" id="vLLM-的七层结构">vLLM 的七层结构</h2><p>用一句话概括：<strong>上层负责接请求和组装结果，中层负责调度与 KV Cache 管理，下层负责 GPU 执行与采样。</strong></p><pre><code class="highlight mermaid">flowchart TD    A[&quot;入口层\nPython API / CLI / HTTP Server&quot;] --&gt; B    B[&quot;Engine 层\nLLMEngine / AsyncLLMEngine\n把请求转为内部 request，维护生命周期&quot;] --&gt; C    C[&quot;Engine Core\nbusy loop：持续调度循环&quot;] --&gt; D &amp; E    D[&quot;Scheduler 层\n决定谁进 batch、分多少 token budget&quot;] --&gt; E    E[&quot;KV Cache 层\n分配/回收/复用 block，支持 prefix caching&quot;]    C --&gt; F    F[&quot;Executor 层\n多进程 worker 管理与任务派发&quot;] --&gt; G    G[&quot;GPU Worker 层\n代表单张 GPU 的设备代理&quot;] --&gt; H    H[&quot;GPU Model Runner 层\n组织一次 forward、触发采样&quot;]    H --&gt; I[&quot;Sampler / Output 层\n从 logits 取 token，解码，流式返回&quot;]    I -.-&gt; A</code></pre><p>这张图之后在脑子里要随时能浮现。遇到任何问题，先定位它在哪一层，再解释设计目的。</p><hr><h2 id="san-tiao-zhu-xian-ren-he-zhui-wen-du-neng-zhao-dao-luo-dian" tabindex="-1" id="三条主线：任何追问都能找到落点">三条主线：任何追问都能找到落点</h2><p><strong>请求主线</strong>（一个用户请求怎么从 API 走到 GPU 再回来）：入口在哪 → engine 怎么接 → scheduler 怎么安排 → worker 怎么执行 → sampler 怎么出 token → 结果怎么流回去。</p><p><strong>显存主线</strong>（KV Cache 怎么分配、增长、复用、释放）：为什么要 block/page 化 → prefix cache 怎么命中 → 为什么能减少碎片 → preemption/eviction 在哪发生。</p><p><strong>性能主线</strong>（vLLM 为什么快）：continuous batching 在哪层体现 → chunked prefill 为什么能实现 → CPU/GPU 各自的瓶颈在哪里 → spec decode/CUDA graph/量化分别属于哪层优化。</p><p>把这三条主线抓住，大多数追问不会乱。</p><hr><h2 id="liu-ge-he-xin-mo-kuai" tabindex="-1" id="六个核心模块">六个核心模块</h2><h3 id="code-vllm-v-1-engine-core-py-code-kong-zhi-zhong-shu" tabindex="-1" id="vllm-v1-engine-core-py：控制中枢"><code>vllm/v1/engine/core.py</code>：控制中枢</h3><p>Engine core 是 vLLM 的心脏。官方架构文档说明，engine core process 负责：运行 scheduler、管理 KV Cache、协调 GPU workers、在 busy loop 中持续调度请求。</p><p>为什么需要持续循环？因为大模型在线推理不是「调用一次函数然后结束」：</p><ul><li>新请求随时到来</li><li>老请求可能继续 decode</li><li>有的请求刚做完 prefill，有的请求已结束需要释放 block</li><li>有的请求能命中 prefix cache，有的因资源不足需要被抢占或延后</li></ul><p>这些事情不是某个单一 forward 能解决的。必须有一个<strong>持续存在的控制循环</strong>，不断做资源再分配。</p><blockquote><p>面试表达：「vLLM 的核心不是单次 forward，而是 engine core 的 iteration-level scheduling。它通过持续循环管理请求状态、KV Cache 和 worker dispatch，把「很多异步请求共用一套 GPU 资源」的问题组织起来。」</p></blockquote><hr><h3 id="code-vllm-v-1-core-sched-scheduler-py-code-diao-du-ce-lue-da-nao" tabindex="-1" id="vllm-v1-core-sched-scheduler-py：调度策略大脑"><code>vllm/v1/core/sched/scheduler.py</code>：调度策略大脑</h3><p>Scheduler 不负责算模型，<strong>它负责决定谁有资格在这一轮被算</strong>。每轮 iteration，scheduler 回答：</p><ul><li>哪些请求进入 batch？</li><li>哪些做 decode，哪些做 prefill？</li><li>是否需要把长 prefill chunk 化？</li><li>token budget 用完了吗？</li><li>某个请求能否继续增长 KV Cache？</li><li>资源不够时，延后、抢占，还是驱逐某些缓存？</li></ul><p>很多推理系统的真正差异不在「模型会不会跑」，而在于<strong>面对大量不同长度、不同阶段、不同优先级的请求时，调度策略如何平衡吞吐和时延</strong>。Scheduler 就是这个策略的载体。</p><blockquote><p>面试表达：「scheduler 的目的，是把「请求级别的不确定性」变成「每轮 iteration 可执行的确定批次」，从而让 GPU 始终有活干，同时尽量控制 TTFT、ITL 和显存占用。」</p></blockquote><hr><h3 id="code-vllm-v-1-engine-llm-engine-py-code-yin-qing-feng-zhuang-ceng" tabindex="-1" id="vllm-v1-engine-llm-engine-py：引擎封装层"><code>vllm/v1/engine/llm_engine.py</code>：引擎封装层</h3><p>这个文件不是模型代码，而是<strong>引擎级控制平面接口</strong>。职责包括：</p><ul><li>接收 <code>add_request</code> / <code>generate</code> 等上层调用</li><li>把输入整理为内部 request 结构</li><li>调用 engine core</li><li>管理输出处理器</li><li>提供 reset prefix cache、sleep、profile 等引擎级操作</li><li>在 data parallel 场景中与其他引擎实例协调</li></ul><p>如果没有这一层：外层服务代码会直接依赖 scheduler/worker 细节；在线服务、离线推理、异步接口都会重复拼装底层组件；引擎控制操作无处安放。这层本质上是<strong>抽象隔离</strong>——把复杂的核心系统封装成稳定、清晰、可调用的 engine 抽象。</p><hr><h3 id="code-vllm-v-1-executor-multiproc-executor-py-code-worker-jin-cheng-zu-zhi-zhe" tabindex="-1" id="vllm-v1-executor-multiproc-executor-py：worker-进程组织者"><code>vllm/v1/executor/multiproc_executor.py</code>：worker 进程组织者</h3><p>调度和执行最好解耦。Engine core 决定「做什么」，但不应该自己承担 worker 进程生命周期、IPC、故障隔离这些任务。Executor 负责：</p><ul><li>启动 worker 进程</li><li>管理 worker 生命周期</li><li>engine core 和 worker 之间的任务传递</li><li>隔离控制平面和执行平面</li></ul><p>为什么多进程？GPU 服务需要每张 GPU 有相对独立的执行实体、清晰的资源边界、更稳定的故障隔离，避免把过多执行细节塞进单个主进程。这也是 V1 明确走多进程架构的重要原因。</p><hr><h3 id="code-vllm-v-1-worker-gpu-worker-py-code-dan-gpu-she-bei-dai-li" tabindex="-1" id="vllm-v1-worker-gpu-worker-py：单-GPU-设备代理"><code>vllm/v1/worker/gpu_worker.py</code>：单 GPU 设备代理</h3><p>Worker 是「代表一张 GPU 的服务代理人」，不只是简单调用模型：</p><ul><li>加载模型权重</li><li>探测可用显存，预留 KV Cache 空间</li><li>提供执行接口给上层</li><li>管理与该 GPU 绑定的状态</li><li>承担 cache / profile / LoRA / 多模态相关操作</li></ul><p>为什么不让 engine core 直接碰 GPU？那样会导致控制逻辑和设备逻辑强耦合、多 GPU 场景混乱、难以扩展到 TP/PP/DP/多机场景。Worker 这一层的存在，是把<strong>设备相关执行</strong>从<strong>全局调度策略</strong>里拆出来。</p><hr><h3 id="code-vllm-v-1-worker-gpu-model-runner-py-code-tie-jin-forward-de-zhi-xing-he-xin" tabindex="-1" id="vllm-v1-worker-gpu-model-runner-py：贴近-forward-的执行核心"><code>vllm/v1/worker/gpu_model_runner.py</code>：贴近 forward 的执行核心</h3><p>Worker 之下还有 model runner。Worker 偏「GPU 进程级容器」，model runner 偏「这一轮怎么真正组织 forward 和 sample」。</p><p>Model runner 通常负责：</p><ul><li>准备模型执行所需输入张量</li><li>调用模型 forward</li><li>更新 device 上的执行状态</li><li>获取输出，触发采样</li><li>与 CUDA graph capture、编译优化等机制衔接</li></ul><p>为什么 worker 下面还要拆 model runner？Worker 还承担很多进程/设备管理责任，如果把 forward 细节全塞进 worker，单个类职责过重。拆开后可维护性、可测试性都更好，新模型/新执行路径接入也更干净。</p><hr><h2 id="kv-cache-paged-attention-prefix-caching-de-kua-ceng-fen-bu" tabindex="-1" id="KV-Cache、PagedAttention、Prefix-Caching-的跨层分布">KV Cache、PagedAttention、Prefix Caching 的跨层分布</h2><p>这三个能力经常被当成「某个文件里的实现」，但实际上都是横跨多层的系统能力。</p><h3 id="kv-cache-kong-zhi-mian-zhi-xing-mian" tabindex="-1" id="KV-Cache：控制面-执行面">KV Cache：控制面 + 执行面</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">控制面（scheduler / engine core）        执行面（worker / model runner）</span><br><span class="line">  哪个请求需要多少 block         ←→      真正的 KV 内存布局</span><br><span class="line">  哪些 block 可以复用/回收       ←→      block 的 device 端更新</span><br><span class="line">  prefix cache 是否命中          ←→      attention 按逻辑顺序读取物理散落的 blocks</span><br><span class="line">  资源不足时驱逐哪些 block        ←→      某些优化 kernel 如何利用 paged layout</span><br></pre></td></tr></table></figure><p><strong>vLLM 的 KV Cache 是由调度层的生命周期管理和执行层的 block 化访问共同完成的，不是单点实现。</strong></p><h3 id="paged-attention-bu-zhi-shi-kernel" tabindex="-1" id="PagedAttention：不只是-kernel">PagedAttention：不只是 kernel</h3><p>PagedAttention 包括两个层面：</p><ol><li><strong>数据结构与内存管理思想</strong>：KV Cache 被切成 blocks/pages，不要求物理连续</li><li><strong>执行层的访问方式</strong>：attention kernel 能按逻辑顺序读取这些物理上不连续的 blocks</li></ol><p>所以它横跨上层 KV block 管理思想，以及下层 attention/kernel/runner 执行实现。</p><blockquote><p>「PagedAttention 不只是一个 kernel 名字，它背后是整套 paged KV Cache 设计。kernel 只是让这种 block 化内存布局在执行时仍然高效可读。」</p></blockquote><h3 id="prefix-caching-diao-du-ceng-kv-cache-ceng-de-kua-qing-qiu-fu-yong" tabindex="-1" id="Prefix-Caching：调度层-KV-Cache-层的跨请求复用">Prefix Caching：调度层 + KV Cache 层的跨请求复用</h3><p>Prefix caching 要解决的问题不是「一个请求内部怎么缓存」，而是：两个请求如果前缀相同，能否复用前面已计算过的 full blocks；如何识别命中；如何维持引用关系；在资源不足时如何驱逐。</p><p>源码映射上，它落在：prefix caching 设计文档 + scheduler/cache manager 相关逻辑 + engine 层的 <code>reset_prefix_cache</code> 等控制接口。</p><p>Prefix caching 是系统能力，不是某个小工具函数。</p><hr><h2 id="pei-zhi-mo-kuai-kong-zhi-xi-tong-xing-wei-quan-heng-de-wai-bu-xuan-niu" tabindex="-1" id="配置模块：控制系统行为权衡的外部旋钮">配置模块：控制系统行为权衡的外部旋钮</h2><p>配置不是「参数表」，而是<strong>控制系统如何在吞吐、时延、显存、并行性之间做权衡的外部 knobs</strong>。</p><table><thead><tr><th>配置类</th><th>影响参数</th><th>最终流向</th></tr></thead><tbody><tr><td>Scheduler config</td><td><code>max_num_batched_tokens</code>、<code>max_num_seqs</code>、chunked prefill 行为</td><td>Scheduler 层</td></tr><tr><td>Worker / Memory config</td><td><code>gpu_memory_utilization</code>、KV cache 内存上限</td><td>GPU Worker 层</td></tr><tr><td>Parallel config</td><td><code>tensor_parallel_size</code>、<code>pipeline_parallel_size</code>、<code>data_parallel_size</code></td><td>Executor / Worker 层</td></tr></tbody></table><p>如何通过配置定位性能问题：<strong>先找参数挂在哪个 config 类上，再看它最终流向 scheduler、worker 还是model runner，因为不同目标参数最终影响的是不同层。</strong></p><hr><h2 id="yu-dao-xin-gong-neng-ru-he-tui-duan-ta-luo-zai-na-ceng" tabindex="-1" id="遇到新功能，如何推断它落在哪层">遇到新功能，如何推断它落在哪层</h2><table><thead><tr><th>功能特征</th><th>优先看</th></tr></thead><tbody><tr><td>改变「请求怎么排队、怎么进 batch」</td><td>engine core、scheduler、config</td></tr><tr><td>改变「显存怎么分配、缓存怎么复用」</td><td>cache manager / scheduler / worker、prefix cache 相关设计</td></tr><tr><td>改变「模型怎么跑、张量怎么组织」</td><td>gpu worker、gpu model runner、model/kernel 层</td></tr><tr><td>改变「外部如何调用系统」</td><td>CLI、serve、engine 封装、API server 层</td></tr></tbody></table><p>举几个具体例子：</p><ul><li><strong>speculative decoding</strong>：改变 batch 形态和执行路径 → scheduler（speculation 深度/预算分配）+ worker（draft path + verify path）</li><li><strong>LoRA 管理</strong>：改变「模型怎么跑」→ gpu worker + gpu model runner 的 LoRA adapter 管理</li><li><strong>structured output</strong>：影响 sampling 约束 → sampler / output processor</li><li><strong>多模态输入</strong>：改变「模型怎么跑」+ 可能影响「请求怎么排队」→ model runner + engine 封装</li></ul><p>这个推断方法比记具体路径更有用，因为路径可能随版本变化，但分层逻辑是稳定的。</p><hr><h2 id="wei-shi-yao-yao-fen-zhe-yao-duo-ceng" tabindex="-1" id="为什么要分这么多层">为什么要分这么多层</h2><p>vLLM 不是简单几千行写一起，因为它面对的问题是：大量请求并发到来、每个请求处于不同阶段、显存有限、GPU 不应空转、延迟不能太差、还要支持不同模型/并行方式/硬件后端。</p><p>分层设计的四个原则：</p><table><thead><tr><th>分离关注点</th><th>控制侧</th><th>执行侧</th></tr></thead><tbody><tr><td>控制 vs 执行</td><td>engine core + scheduler：谁先跑、跑多少、资源够不够</td><td>worker + model runner：这一轮 forward 怎么完成</td></tr><tr><td>设备管理 vs 模型执行</td><td>gpu worker：设备进程容器</td><td>gpu model runner：单轮模型执行逻辑</td></tr><tr><td>外部接口 vs 引擎内部</td><td>LLM / serve / OpenAI protocol</td><td>core + scheduler + executor + worker</td></tr><tr><td>单请求逻辑 vs 系统级资源管理</td><td>sampling params、输出处理</td><td>KV Cache、batch token budget、抢占、prefix reuse</td></tr></tbody></table><p>从根上说，vLLM 的源码结构体现的是：<strong>它不是一个模型脚本，而是一个推理操作系统。</strong></p><hr><h2 id="su-cha-6-ge-he-xin-mo-kuai-yi-ju-hua-zong-jie" tabindex="-1" id="速查：6-个核心模块一句话总结">速查：6 个核心模块一句话总结</h2><table><thead><tr><th>模块路径</th><th>一句话职责</th><th>面试关键词</th></tr></thead><tbody><tr><td><code>vllm/v1/engine/core.py</code></td><td>控制中枢，持续调度循环</td><td>busy loop、iteration scheduling</td></tr><tr><td><code>vllm/v1/core/sched/scheduler.py</code></td><td>调度策略大脑，决定谁进 batch</td><td>token budget、preemption、chunked prefill</td></tr><tr><td><code>vllm/v1/engine/llm_engine.py</code></td><td>上层调用看到的引擎封装</td><td>抽象隔离、request lifecycle</td></tr><tr><td><code>vllm/v1/executor/multiproc_executor.py</code></td><td>worker 进程组织者</td><td>控制平面/执行平面解耦</td></tr><tr><td><code>vllm/v1/worker/gpu_worker.py</code></td><td>单 GPU 设备代理</td><td>显存探测、KV cache 预留</td></tr><tr><td><code>vllm/v1/worker/gpu_model_runner.py</code></td><td>贴近 forward/sampling 的执行核心</td><td>输入准备、CUDA graph、采样</td></tr></tbody></table><hr><h2 id="ji-ge-chang-jian-zhui-wen-de-can-kao-da-fa" tabindex="-1" id="几个常见追问的参考答法">几个常见追问的参考答法</h2><p><strong>「vLLM 的核心代码主要在哪？」</strong> 核心主干在 engine core、scheduler、executor、gpu worker 和 gpu model runner 这几层。engine core 管控制循环，scheduler 决定 iteration 级调度，worker/model runner 负责设备执行，PagedAttention 落在 KV Cache 与 attention 执行的结合处。外层 API 和 LLMEngine 更像是封装和门面。</p><p><strong>「continuous batching 相关实现，先看哪里？」</strong> 先看 engine core 和 scheduler。Continuous batching 本质不是某个 kernel，而是请求在 iteration 级别持续进入和退出 batch 的调度行为。worker 只是执行结果，策略发生在调度层。</p><p><strong>「prefix cache 实现先看哪里？」</strong> 先看 prefix caching 设计文档，然后看 scheduler/cache manager 相关逻辑。它属于跨请求 KV block 复用，不是单次 forward 里的临时缓存。</p><p><strong>「线上 OOM，先看哪一层？」</strong> 先分清是权重、KV Cache 还是 batch 膨胀导致的。分别对应：权重→模型本身 + 并行策略；KV Cache→worker 的可用显存探测和 cache 预分配；batch 膨胀→scheduler 的 batch/token budget 配置。OOM 常常不是「模型本身大」，而是调度层把并发推高了。</p><hr><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/c3e99548/">vLLM系统拆解-11-面试题精讲：从概念到系统设计如何讲清楚为什么</a></li><li>下一篇：<a href="/posts/84b005d4/">vLLM系统拆解-13-推理框架对比：vLLM、TGI、TensorRT-LLM与SGLang</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-12-yuan-ma-di-tu-mei-ge-he-xin-neng-li-luo-zai-na-yi-ceng&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="LLMEngine" scheme="https://smarter.xin/tags/llm-engine/"/>
    
    <category term="Scheduler" scheme="https://smarter.xin/tags/scheduler/"/>
    
    <category term="Worker" scheme="https://smarter.xin/tags/worker/"/>
    
    <category term="源码结构" scheme="https://smarter.xin/tags/source-structure/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-11-面试题精讲：从概念到系统设计如何讲清楚为什么</title>
    <link href="https://smarter.xin/posts/c3e99548/"/>
    <id>https://smarter.xin/posts/c3e99548/</id>
    <published>2026-04-02T05:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-11-mian-shi-ti-jing-jiang-cong-gai-nian-dao-xi-tong-she-ji-ru-he-jiang-qing-chu-wei-shi-yao" tabindex="-1">vLLM系统拆解-11-面试题精讲：从概念到系统设计如何讲清楚为什么</h1><p>这篇文章不是题库，而是把前 10 篇的核心知识压缩成面试可用的表达框架。</p><p>面试里回答 vLLM 相关问题，有一个有效的通用结构：<strong>先说它要解决的核心问题，再说采用了什么设计，再说直接收益，最后说边界与代价。</strong> 能主动讲出适用场景和边界，才像真正理解过推理系统——而不是记了几个名词。</p><p>这篇文章按三类问题组织：概念题、机制题、系统题。每道题的「追问」部分是面试里最容易暴露深度的地方，不能忽视。</p><hr><h2 id="gai-nian-ti" tabindex="-1" id="概念题">概念题</h2><h3 id="v-llm-shi-shi-yao" tabindex="-1" id="vLLM-是什么？">vLLM 是什么？</h3><p>vLLM 是一个面向大语言模型的高吞吐、显存高效的推理与服务引擎。它不只是「把模型跑起来」的库，而是专门针对<strong>在线服务场景下多请求并发推理</strong>做了系统级优化。</p><p>代表性设计包括：</p><ul><li><strong>PagedAttention</strong>：把 KV Cache 以 block/page 方式管理，减少显存碎片，提高可并发请求数</li><li><strong>Continuous batching</strong>：运行中持续调度新老请求，提高 GPU 利用率</li><li><strong>Unified scheduler（V1）</strong>：把 prompt token 和 output token 统一纳入 token budget 调度，chunked prefill、prefix caching、speculative decoding 都落在这一框架里</li><li><strong>Prefix caching</strong>：重复前缀直接复用已算过的 KV Cache</li><li><strong>Chunked prefill</strong>：把长 prompt 的 prefill 切块，与 decode 混合调度</li><li><strong>OpenAI-compatible serving</strong>：既能离线推理，也能直接作为服务后端</li></ul><p>vLLM 的关键不是「某个 kernel 很快」，而是从 <strong>KV Cache 管理、请求调度、GPU 执行、服务接口</strong>几个层面一起优化。</p><p><strong>面试官常见追问方向</strong>：为什么它比普通 Transformers <code>generate()</code> 更适合服务？PagedAttention 具体解决了什么？Continuous batching 和普通 batching 区别是什么？V1 为什么要统一调度？——所以第一题回答完后，自然引向「资源管理与调度」。</p><hr><h3 id="v-llm-wei-shi-yao-kuai" tabindex="-1" id="vLLM-为什么快？">vLLM 为什么快？</h3><p>不够好的回答是：「因为它用了 PagedAttention，所以快。」这暴露的是只记了一个名词。</p><p>更准确的回答：vLLM 快，是因为它同时优化了在线推理里最关键的几个瓶颈——</p><p><strong>（1）KV Cache 显存管理。</strong> 传统做法要求每个序列的 KV Cache 连续扩展，会产生显存碎片，明明还有显存却放不下更多请求。PagedAttention 把 KV Cache 切成固定大小的 block，用逻辑映射代替物理连续性，显存利用率更高，可并发请求更多。</p><p><strong>（2）动态多请求下的 batch 利用率。</strong> Continuous batching 在运行中持续把新请求插入、把已完成请求移出，而不是等整个静态 batch 全部完成再处理下一批，GPU 更不容易空转。</p><p><strong>（3）统一调度（V1）。</strong> 不再把 prefill 和 decode 硬分成两套逻辑，而是用统一 token budget 调度每轮每个请求需要推进多少 token，chunked prefill、prefix caching、spec decode 都可以自然地落进这个框架。</p><p><strong>（4）跨请求前缀复用。</strong> Prefix caching 让共享同一 system prompt 或 instruction template 的请求直接复用已计算的 KV Cache，跳过重复 prefill。</p><p><strong>（5）进一步的高阶优化。</strong> Chunked prefill、CUDA/HIP graph、speculative decoding、量化、FlashAttention/FlashInfer 集成——这些优化各自针对不同瓶颈，有些优化时延，有些优化吞吐，有些优化显存。</p><p>一句话压缩版：<strong>vLLM 快，核心是它把在线大模型推理里最贵的三件事一起优化了：KV Cache 管理、动态请求调度、GPU 执行路径，而不是只优化一次 forward。</strong></p><hr><h3 id="v-llm-he-hugging-face-transformers-code-generate-code-de-qu-bie-shi-shi-yao" tabindex="-1" id="vLLM-和-Hugging-Face-Transformers-generate-的区别是什么？">vLLM 和 Hugging Face Transformers <code>generate()</code> 的区别是什么？</h3><p>理解它们的层次差异比比较功能更重要：</p><ul><li><strong>Transformers <code>generate()</code></strong>：模型调用接口，解决「如何对一个输入完成生成」</li><li><strong>vLLM</strong>：推理服务引擎，解决「很多请求同时来时，如何把 GPU、KV Cache、调度和服务接口组织好」</li></ul><p>前者是模型级调用，后者是系统级推理与服务。如果只看单条样本、低并发、脚本式使用，两者都能做生成；进入在线服务场景，问题性质完全不同：</p><ul><li>不同请求长度不一样怎么办？</li><li>KV Cache 如何管理才不浪费显存？</li><li>新请求如何在老请求尚未结束时插入 batch？</li><li>重复前缀如何复用？</li><li>长 prompt 会不会拖慢 decode 请求？</li></ul><p>一句总结：Transformers 更适合模型开发与功能验证，vLLM 更适合生产级大模型推理后端。</p><hr><h3 id="v-llm-zuo-liao-fen-ye-shi-kv-cache-wei-shi-yao-huan-hui-oom" tabindex="-1" id="vLLM-做了分页式-KV-Cache，为什么还会-OOM？">vLLM 做了分页式 KV Cache，为什么还会 OOM？</h3><p>PagedAttention 解决的是 <strong>KV Cache 管理效率</strong>，不是「显存无限」。它降低了碎片和浪费，但没有改变「KV Cache 总量会随着请求数和上下文长度增长」这一事实。</p><p>OOM 的常见原因：</p><table><thead><tr><th>原因</th><th>说明</th></tr></thead><tbody><tr><td>模型权重本身太大</td><td>和 KV Cache 无关，权重本身就撑爆显存</td></tr><tr><td>并发请求太多</td><td>总 KV Cache 需求超出可用显存</td></tr><tr><td>上下文太长</td><td>长上下文下单请求的 KV Cache 就很大</td></tr><tr><td>batch/token budget 过激进</td><td><code>max_num_batched_tokens</code>、<code>max_num_seqs</code> 设得太高</td></tr><tr><td>并行/量化/显存预留不合理</td><td>权重以外的显存分配没有留出足够余量</td></tr><tr><td>负载瞬时波动</td><td>业务高峰期短时间内大量长请求同时进来</td></tr></tbody></table><hr><h2 id="ji-zhi-ti" tabindex="-1" id="机制题">机制题</h2><h3 id="shi-yao-shi-paged-attention-ta-de-ben-zhi-shou-yi-shi-shi-yao" tabindex="-1" id="什么是-PagedAttention？它的本质收益是什么？">什么是 PagedAttention？它的本质收益是什么？</h3><p>PagedAttention 的核心思想：把每个请求的 KV Cache 从「必须连续增长的一大段显存」改成「由很多固定大小的 block 组成的逻辑序列」。借鉴操作系统分页内存的思路：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">逻辑视角（对 attention 计算透明）：</span><br><span class="line">  token 0, 1, 2, ..., N  → 一整段连续上下文</span><br><span class="line"></span><br><span class="line">物理视角（实际内存布局）：</span><br><span class="line">  Block 7:  token 0–15</span><br><span class="line">  Block 23: token 16–31</span><br><span class="line">  Block 2:  token 32–47</span><br><span class="line">  ...（分散在不同位置）</span><br><span class="line"></span><br><span class="line">系统维护逻辑块→物理块的映射表，attention kernel 按映射取数据</span><br></pre></td></tr></table></figure><p>解决的两个问题：</p><p><strong>显存碎片。</strong> 在线服务里不同请求长度差异很大，如果 KV Cache 必须连续扩展，会造成很多「碎洞」，显存越来越难用。Block 化管理显著降低这种碎片。</p><p><strong>动态分配与回收。</strong> 在线请求持续进入、持续结束，KV Cache 生命周期动态变化。Block 化后分配和释放都更灵活，系统更容易在很多请求之间复用可用空间。</p><p><strong>本质收益不是「单次 attention 更快」，而是让 KV Cache 更容易被高效管理，从而支撑更大的有效 batch 与更高并发。</strong></p><p>几个容易被追问的点：</p><ul><li><em>为什么不用连续内存加 compaction？</em> 代价太高。Compaction 需要迁移已有 KV 数据，在高并发在线场景下不适合频繁做，分页思路本质上是避免频繁搬家。</li><li><em>会不会增加寻址开销？</em> 会有映射与访存组织成本，但这是自定义 attention kernel 需要处理的部分。</li></ul><hr><h3 id="shi-yao-shi-continuous-batching-wei-shi-yao-jing-tai-batch-bu-gou-yong-yu-zai-xian-fu-wu" tabindex="-1" id="什么是-continuous-batching？为什么静态-batch-不够用于在线服务？">什么是 continuous batching？为什么静态 batch 不够用于在线服务？</h3><p>静态 batch 的问题：先凑一批请求一起跑，这一批结束后再处理下一批。在在线服务里，这会导致：</p><ul><li>少数长请求拖住整批，其他已完成的 GPU 算力空转等待</li><li>新请求必须等上一整批全部完成才能进入，头部等待时间长</li><li>请求长度差异大时，GPU 利用率很差，尾延迟抖动明显</li></ul><p>Continuous batching 的思路：<strong>batch 不是一次性固定下来的</strong>，调度循环会持续把新请求插入、把已完成请求移出，每一轮根据当前状态重新组织可以一起运行的请求。</p><p>三个直接收益：</p><ol><li>提高 GPU 利用率——不会因少数长请求拖住整批</li><li>提高吞吐——更多请求及时进入执行路径</li><li>改善尾延迟——新请求不必等上一整批完全结束</li></ol><p>Continuous batching 真正体现了 vLLM 是一个服务系统而不是单纯模型库——它面对的是动态请求流，不是静态样本集合。</p><hr><h3 id="prefill-he-decode-you-shi-yao-qu-bie-wei-shi-yao-zhe-ge-chai-yi-hen-zhong-yao" tabindex="-1" id="Prefill-和-Decode-有什么区别？为什么这个差异很重要？">Prefill 和 Decode 有什么区别？为什么这个差异很重要？</h3><table><thead><tr><th></th><th>Prefill</th><th>Decode</th></tr></thead><tbody><tr><td>做什么</td><td>把整段 prompt 一次性编码，建立初始 KV Cache</td><td>每步生成少量 token，不断追加新 KV Cache</td></tr><tr><td>资源瓶颈</td><td>偏 <strong>compute-bound</strong>（大量矩阵计算）</td><td>偏 <strong>memory-bound</strong>（频繁访问越来越长的历史 KV Cache）</td></tr><tr><td>每轮处理量</td><td>多 token（整段 prompt）</td><td>少 token（通常每步 1 个）</td></tr></tbody></table><p>这个差异是 vLLM 很多设计的出发点：</p><ul><li><strong>chunked prefill</strong>：防止大 prefill 独占资源，拖慢 decode 请求的 ITL</li><li><strong>scheduler 优先 decode</strong>：更好地控制 inter-token latency</li><li><strong>prefix caching</strong>：直接减少重复 prefill 的计算量</li><li><strong>KV Cache 优化</strong>：主要影响 decode 阶段的效率与可扩展性</li></ul><hr><h3 id="v-1-wei-shi-yao-yao-zuo-unified-scheduler" tabindex="-1" id="V1-为什么要做-unified-scheduler？">V1 为什么要做 unified scheduler？</h3><p>V0 时代更容易把 prefill 和 decode 当成两套相对独立的调度逻辑；但随着 chunked prefill、prefix caching、speculative decoding 等特性增加，这种硬分裂让调度逻辑越来越复杂。</p><p>V1 的核心改动：把「一个请求此刻还需要多少 token 计算」抽象成统一 token budget 分配问题，而不是先判断它属于 prefill 还是 decode。</p><p>好处有三：</p><p><strong>调度模型更统一。</strong> 系统只关心：在这一轮预算内，给哪些请求分配多少 token 计算量，而不是先按类别分叉再拼回去。</p><p><strong>更容易支持复杂特性。</strong> Chunked prefill、prefix caching、speculative decoding 本质上都可以理解成「一个请求当前需要推进多少 token 的计算」，统一 scheduler 更自然地容纳它们。</p><p><strong>更便于后续扩展。</strong> 统一抽象后，新功能更容易接进现有框架，而不必重写一套单独调度路径。</p><p>一句话总结：<strong>V1 unified scheduler 的本质，是把「请求类型调度」升级为「token 预算调度」。</strong></p><hr><h3 id="shi-yao-shi-chunked-prefill-wei-shi-yao-you-yong" tabindex="-1" id="什么是-chunked-prefill？为什么有用？">什么是 chunked prefill？为什么有用？</h3><p>如果一个请求的 prompt 很长，整块 prefill 一口气做完，会长时间占用资源，影响其他 decode 请求，导致用户看到的逐 token 输出变慢。</p><p>Chunked prefill 的思路：不把巨大 prefill 一次吃完，而是切成更小块，与 decode 请求一起进入统一调度。V1 默认优先调度 decode，再用剩余 <code>max_num_batched_tokens</code> 预算安排 prefill，放不下的 prefill 自动切块延后。</p><p>三个收益：</p><ol><li><strong>改善 ITL</strong>：decode 请求不容易被超长 prompt 拖住，token 间输出间隔更平滑</li><li><strong>提高 GPU 利用率</strong>：prefill 偏计算密集，decode 偏带宽密集，混在同一 batch 里资源利用更均衡</li><li><strong>平衡吞吐与时延</strong>：在 TTFT、ITL、throughput 三者之间做更细粒度取舍</li></ol><p>边界同样重要：<code>max_num_batched_tokens</code> 越小，越偏 ITL 保护；越大，越偏 TTFT 和吞吐。没有一个值对所有业务都最优。</p><p><strong>chunked prefill 的本质不是把 prompt 切碎，而是让长 prompt 不再以粗粒度方式垄断调度预算。</strong></p><hr><h3 id="prefix-caching-he-pu-tong-kv-cache-you-shi-yao-qu-bie" tabindex="-1" id="Prefix-caching-和普通-KV-Cache-有什么区别？">Prefix caching 和普通 KV Cache 有什么区别？</h3><table><thead><tr><th></th><th>普通 KV Cache</th><th>Prefix caching</th></tr></thead><tbody><tr><td>服务范围</td><td>单请求内递增生成</td><td>跨请求共享前缀</td></tr><tr><td>目的</td><td>避免重复计算该请求历史 token</td><td>避免不同请求重复计算共同前缀</td></tr><tr><td>典型场景</td><td>所有 autoregressive 推理</td><td>system prompt、instruction template、few-shot 前缀相同</td></tr></tbody></table><p>普通 KV Cache 是「同一请求别重复算历史」，prefix caching 是「不同请求别重复算共同前缀」。</p><p><strong>为什么只缓存 full blocks？</strong> Prefix caching 的实现围绕 block/hash 组织。只缓存 full block 有利于保持缓存粒度一致、hash 关系稳定、管理逻辑简单；部分 block 的 hash 计算更复杂且不稳定，缓存收益也有限。</p><p><strong>代价：</strong> 需要维护缓存元数据、引用计数、LRU 驱逐机制；只有共享前缀较多的业务形态收益才明显；多租户场景还需要考虑 cache 隔离和 timing side-channel 风险，vLLM 支持用 <code>cache_salt</code> 做隔离。</p><hr><h2 id="xi-tong-ti" tabindex="-1" id="系统题">系统题</h2><h3 id="speculative-decoding-wei-shi-yao-bu-shi-zong-neng-jia-su" tabindex="-1" id="Speculative-decoding-为什么不是总能加速？">Speculative decoding 为什么不是总能加速？</h3><p>Speculative decoding 更适合被理解为<strong>延迟优化工具</strong>，而不是普适吞吐优化工具。</p><p>三类原因：</p><p><strong>收益依赖负载形态。</strong> 它更适合改善中低 QPS、memory-bound 场景下的 latency。高负载、吞吐优先场景中，额外的 draft 计算和复杂调度可能反而带来负担，收益不稳定。</p><p><strong>不同方法代价不同。</strong> vLLM 支持 EAGLE、MTP、draft model、PARD、MLP、n-gram、suffix decoding 等多种方法，各自的额外计算成本、命中率和收益差异很大。</p><p><strong>系统整体瓶颈不一定在这里。</strong> 如果瓶颈主要是显存、调度、通信或 CPU starvation，speculative decoding 不是最优先的优化手段。</p><p>使用时需要结合具体 workload、模型、QPS 和采样配置评估，而不是直接开启期待收益。</p><hr><h3 id="v-llm-wei-shi-yao-yao-chai-cheng-duo-ge-jin-cheng" tabindex="-1" id="vLLM-为什么要拆成多个进程？">vLLM 为什么要拆成多个进程？</h3><p>这是职责解耦的典型做法：</p><ul><li><strong>API server</strong>：对外接口、tokenization、输入预处理、流式输出</li><li><strong>Engine core</strong>：scheduler、KV Cache 管理、请求生命周期管理、派发执行计划</li><li><strong>GPU worker</strong>：加载模型、执行 forward、管理 GPU 侧资源</li><li><strong>DP coordinator</strong>（DP &gt; 1 时存在）：管理多 DP rank 间 load balancing 和 MoE 场景下的同步</li></ul><p>这样设计有三个实质原因：</p><p><strong>职责边界清晰。</strong> 服务层逻辑、调度逻辑、GPU 执行逻辑关注点不同，API 层的网络与预处理不应阻塞 GPU 执行控制，调度 busy loop 也需要尽可能稳定运行。</p><p><strong>适合扩展。</strong> Worker 可按 GPU 数量扩展，engine core 可按 data parallel rank 组织，每个 DP rank 各自自治。这说明 vLLM 不是把所有逻辑塞进一个大进程，而是在向真正的推理服务系统设计靠拢。</p><p><strong>减少相互干扰。</strong> 进程间隔离比线程隔离更彻底，各层的异常不会直接拖垮其他层。</p><hr><h3 id="gpu-li-yong-lu-bu-gao-ru-he-pan-duan-ping-jing-zai-na-li" tabindex="-1" id="GPU-利用率不高，如何判断瓶颈在哪里？">GPU 利用率不高，如何判断瓶颈在哪里？</h3><p>这道题考察系统性思维，而不只是 GPU 知识。</p><p>vLLM 不是单一 GPU kernel，而是完整推理系统。CPU 负责 API 请求处理、tokenization/detokenization、engine core 的 scheduler busy loop、进程间通信、控制路径和元数据管理。CPU 不足时，GPU 可能明明还有活可做，却吃不到。</p><p>判断框架：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">GPU 利用率偏低</span><br><span class="line">  ├── 是否有频繁 preemption？</span><br><span class="line">  │   → 是：先查显存侧（gpu_memory_utilization、kv_cache_dtype）</span><br><span class="line">  │          再查并发侧（max_num_seqs、max_num_batched_tokens）</span><br><span class="line">  ├── GPU 在等什么？</span><br><span class="line">  │   → 等 CPU：看调度延迟、tokenizer 开销、进程间通信</span><br><span class="line">  │   → 等 KV Cache：看可用 block 数量、是否频繁 GC</span><br><span class="line">  │   → 等网络（多机）：看节点间通信带宽是否成为瓶颈</span><br><span class="line">  └── batch 是否太小？</span><br><span class="line">      → 看 max_num_batched_tokens 和实际每轮 token 数</span><br></pre></td></tr></table></figure><p><strong>GPU 决定「算得快不快」，CPU 决定「系统能不能把请求及时组织并喂给 GPU」。</strong> 只盯 GPU 利用率而不看调度链，是很常见的分析盲区。</p><hr><h2 id="su-cha-he-xin-zhi-shi-dian-yi-ju-hua" tabindex="-1" id="速查：核心知识点一句话">速查：核心知识点一句话</h2><table><thead><tr><th>问题</th><th>一句话答案</th></tr></thead><tbody><tr><td>vLLM 是什么</td><td>面向大模型在线推理的高吞吐、显存高效服务引擎</td></tr><tr><td>为什么快</td><td>同时优化了 KV Cache 管理、动态请求调度和 GPU 执行路径</td></tr><tr><td>PagedAttention 解决什么</td><td>把 KV Cache 从连续大块变成 block 化分页管理，降低碎片并提升可并发性</td></tr><tr><td>Continuous batching 是什么</td><td>运行中持续加入和移出请求的动态 batch，不等整批完成再处理</td></tr><tr><td>Prefill vs Decode 最大区别</td><td>prefill 偏 compute-bound，decode 偏 memory-bound</td></tr><tr><td>V1 unified scheduler 本质</td><td>把请求推进统一抽象为 token budget 分配，不再硬分成两套调度逻辑</td></tr><tr><td>Chunked prefill 的作用</td><td>让长 prompt 不垄断调度预算，改善 ITL 和整体资源利用率</td></tr><tr><td>Prefix caching 的本质</td><td>跨请求复用共享前缀的 KV Cache，减少重复 prefill</td></tr><tr><td>Speculative decoding 为什么不总有效</td><td>收益取决于 workload、QPS、模型和系统瓶颈，是延迟优化而非普适吞吐优化</td></tr><tr><td>为什么 CPU 也重要</td><td>调度、通信、tokenization 都依赖 CPU，CPU 不够会让 GPU 吃不到活</td></tr></tbody></table><hr><h2 id="ji-chong-chang-jian-de-fan-mo-shi" tabindex="-1" id="几种常见的反模式">几种常见的反模式</h2><p><strong>只背名词。</strong> 「它有 PagedAttention、chunked prefill、prefix caching、speculative decoding。」这是念菜单，不是回答。</p><p><strong>只讲 kernel，不讲系统。</strong> 问「为什么快」时一直讲 attention 公式，不讲调度和 KV Cache 管理，视角太窄。</p><p><strong>把所有优化都说成无脑收益。</strong> 「speculative decoding 一定更快。」这很危险。成熟的回答必须带 workload 依赖。</p><p><strong>把 vLLM 说成单纯替代 Transformers 的库。</strong> 它确实能做替代后端，但本质上是 serving engine，解决的是服务层问题，而不只是生成质量问题。</p><p>真正像 infra 工程师的回答，往往不是「我知道某技术」，而是：「我知道它解决什么问题、为什么这样设计、这样设计的收益与边界是什么、如果线上遇到瓶颈我会优先怀疑哪里。」</p><hr><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/9ce7b444/">vLLM系统拆解-10-分布式部署：TP、PP、DP、EP各解决什么问题</a></li><li>下一篇：<a href="/posts/354d88e4/">vLLM系统拆解-12-源码地图：每个核心能力落在哪一层</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-11-mian-shi-ti-jing-jiang-cong-gai-nian-dao-xi-tong-she-ji-ru-he-jiang-qing-chu-wei-shi-yao&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="推理系统" scheme="https://smarter.xin/tags/inference-system/"/>
    
    <category term="面试" scheme="https://smarter.xin/tags/interview/"/>
    
    <category term="系统设计" scheme="https://smarter.xin/tags/system-design/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-10-分布式部署：TP、PP、DP、EP各解决什么问题</title>
    <link href="https://smarter.xin/posts/9ce7b444/"/>
    <id>https://smarter.xin/posts/9ce7b444/</id>
    <published>2026-04-02T05:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-10-fen-bu-shi-bu-shu-tp-pp-dp-ep-ge-jie-jue-shi-yao-wen-ti" tabindex="-1">vLLM系统拆解-10-分布式部署：TP、PP、DP、EP各解决什么问题</h1><p>前几篇讲的优化——scheduler、KV Cache、chunked prefill、spec decode——主要都在回答同一个问题：「一台机器 / 一组 GPU 上，如何把推理做得更高效？」</p><p>真实线上系统还会遇到另外几类问题，是单机优化无法解决的：</p><ul><li>模型太大，单张卡甚至单台机器装不下</li><li>请求太多，单个推理实例扛不住总流量</li><li>MoE 模型的计算与通信模式，和普通 dense 模型不一样</li><li>多节点部署下，网络通信本身也会成为瓶颈</li><li>prefill 想压 TTFT，decode 想提高 batch size，不同阶段目标不同</li></ul><p>这时问题的性质变了：从「kernel 快不快」变成「这份权重怎么切到多张 GPU、这批请求怎么分给多个 engine、不同 GPU 之间什么时候通信」。</p><p>这篇文章讲 vLLM 如何用四种并行策略和分层架构来处理这些问题。读完你能解释 TP/PP/DP/EP 各自解决什么、为什么多节点常用「节点内 TP + 节点间 PP」、MoE 场景为什么需要额外的 EP + DBO，以及 vLLM 分层架构背后的设计逻辑。</p><hr><h2 id="si-chong-bing-xing-de-qie-fen-wei-du" tabindex="-1" id="四种并行的切分维度">四种并行的切分维度</h2><p>先建立整体框架，再展开每一种。</p><table><thead><tr><th>并行策略</th><th>切分维度</th><th>主要解决的问题</th><th>典型代价</th></tr></thead><tbody><tr><td><strong>TP（张量并行）</strong></td><td>层内算子</td><td>单层权重过大 / 单卡算力不足</td><td>层内通信（all-reduce/all-gather），高度依赖高速互联</td></tr><tr><td><strong>PP（流水线并行）</strong></td><td>网络深度（层）</td><td>多节点扩展 / GPU 数与模型切分不整齐</td><td>流水线气泡，单请求路径更长</td></tr><tr><td><strong>DP（数据并行）</strong></td><td>请求流量</td><td>总吞吐 / 服务扩展</td><td>权重复制占显存，需要负载均衡</td></tr><tr><td><strong>EP（专家并行）</strong></td><td>MoE 专家集合</td><td>MoE 专家层的负载组织与局部性</td><td>稀疏路由的 all-to-all 通信</td></tr></tbody></table><p>这四种策略不互斥，可以组合使用。vLLM 官方文档明确支持把数据并行 attention 与专家并行或张量并行的 MoE 层结合做大规模部署。</p><hr><h2 id="tp-tong-yi-ceng-tai-da-heng-zhao-huo-shu-zhao-chai" tabindex="-1" id="TP：同一层太大，横着或竖着拆">TP：同一层太大，横着或竖着拆</h2><h3 id="gong-zuo-yuan-li" tabindex="-1" id="工作原理">工作原理</h3><p>TP 的本质是：同一层的参数和计算太大，一张 GPU 放不下或算不过来，于是把同一层的张量乘法拆到多张 GPU 上一起做。vLLM 实现了 Megatron-LM 的张量并行算法思路。</p><p>以大线性层为例，<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>Y</mi><mo>=</mo><mi>X</mi><mi>W</mi></mrow><annotation encoding="application/x-tex">Y = XW</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.22222em;">Y</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.07847em;">X</span><span class="mord mathnormal" style="margin-right:0.13889em;">W</span></span></span></span>，如果 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>W</mi></mrow><annotation encoding="application/x-tex">W</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">W</span></span></span></span> 很大，可以把 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>W</mi></mrow><annotation encoding="application/x-tex">W</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">W</span></span></span></span> 按列或按行拆到多张卡，每张卡算部分结果，最后聚合。</p><p>这是大模型推理里最常见的并行方式，代码层面很直接：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> vllm <span class="keyword">import</span> LLM</span><br><span class="line">llm = LLM(<span class="string">&quot;facebook/opt-13b&quot;</span>, tensor_parallel_size=<span class="number">4</span>)</span><br></pre></td></tr></table></figure><p>或服务端：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vllm serve facebook/opt-13b --tensor-parallel-size 4</span><br></pre></td></tr></table></figure><h3 id="dai-jie" tabindex="-1" id="代价">代价</h3><p>TP 不是白送性能。把一层拆到多张 GPU 后，每层结束时通常需要 all-reduce / all-gather / reduce-scatter 等集合通信。计算被分散了，但通信变多了。</p><p>这里有一个关键约束：<strong>TP 特别依赖高速互联</strong>。同机 NVLink 通信比跨机网络快得多，官方文档明确指出高效张量并行需要快速节点间通信，最好是 InfiniBand 这类高速网络。</p><p>所以 TP 最适合单机多 GPU 且节点内互联良好的场景；如果 GPU 间互联一般，大 TP 未必是最优选择。</p><hr><h2 id="pp-bu-chai-ceng-nei-er-shi-an-shen-du-qie-duan" tabindex="-1" id="PP：不拆层内，而是按深度切段">PP：不拆层内，而是按深度切段</h2><h3 id="gong-zuo-yuan-li-1" tabindex="-1" id="工作原理-2">工作原理</h3><p>PP 把模型的不同层分配到不同 GPU 或节点，一个请求像流水线一样依次经过各个 stage：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">80 层模型，4 张 GPU：</span><br><span class="line">  GPU 0: 1–20 层</span><br><span class="line">  GPU 1: 21–40 层</span><br><span class="line">  GPU 2: 41–60 层</span><br><span class="line">  GPU 3: 61–80 层</span><br><span class="line"></span><br><span class="line">请求 → GPU 0 → GPU 1 → GPU 2 → GPU 3 → 输出</span><br></pre></td></tr></table></figure><p>每张 GPU 只拿了模型的一段，stage 之间传递激活值，而不是频繁做层内同步。</p><h3 id="tp-vs-pp-de-xuan-ze-luo-ji" tabindex="-1" id="TP-vs-PP-的选择逻辑">TP vs PP 的选择逻辑</h3><table><thead><tr><th></th><th>TP</th><th>PP</th></tr></thead><tbody><tr><td>切分位置</td><td>层内算子</td><td>层间（按深度）</td></tr><tr><td>通信模式</td><td>每层结束做 all-reduce，频率高</td><td>stage 间传递激活，频率低</td></tr><tr><td>适合硬件</td><td>节点内高速互联（NVLink）</td><td>节点间慢速网络，或无 NVLink 场景（如 L40S）</td></tr><tr><td>GPU 数整除</td><td>要求整除关系</td><td>支持 uneven splits</td></tr><tr><td>代价</td><td>层内通信开销</td><td>流水线气泡，单请求路径更长</td></tr></tbody></table><p>官方文档给出了一个很实用的建议：单机但没有 NVLink 的场景（比如 L40S），可以考虑 <code>tensor_parallel_size=1</code>，直接用 <code>pipeline_parallel_size</code> 等于 GPU 数，避免层内高频同步的通信代价。</p><p>PP 的另一个优点是支持 uneven splits：当 GPU 数量与模型层数切分不那么整齐时，PP 更灵活。</p><p>PP 的代价不能忽视：不是每个 stage 时时刻刻都满载，存在流水线气泡；分段不均会导致木桶效应，某一段慢整条 pipeline 都被拖住。</p><h3 id="duo-jie-dian-de-dian-xing-zu-he-jie-dian-nei-tp-jie-dian-jian-pp" tabindex="-1" id="多节点的典型组合：节点内-TP-节点间-PP">多节点的典型组合：节点内 TP + 节点间 PP</h3><p>这个组合几乎是多节点大模型部署的标准思路，原因是顺着硬件拓扑走：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">2 节点 × 8 GPU = 16 GPU</span><br><span class="line">配置：tensor_parallel_size=8, pipeline_parallel_size=2</span><br><span class="line"></span><br><span class="line">节点 0: [GPU0-GPU7] ← TP=8，一起处理同一层（节点内 NVLink 快）</span><br><span class="line">   ↕ PP 跨节点传激活（网络慢，但只有 stage 切换时才通信）</span><br><span class="line">节点 1: [GPU0-GPU7] ← TP=8，处理模型另一段</span><br></pre></td></tr></table></figure><p>节点内 GPU 互联通常更快，适合做层内高频同步的 TP；节点间网络更慢，用 PP 按层分段，减少跨节点的高频通信。<strong>并行策略要尊重硬件通信层级，而不是在抽象层面看哪些维度都能并行就随便组合。</strong></p><hr><h2 id="dp-fu-wu-kuo-zhan-bu-shi-mo-xing-nei-bing-xing" tabindex="-1" id="DP：服务扩展，不是模型内并行">DP：服务扩展，不是模型内并行</h2><p>TP 和 PP 解决的是「模型怎么切」，DP 解决的是另一个问题：<strong>同一个模型副本太忙，需要多份副本一起接流量。</strong></p><p>DP 的本质不是让单个请求更快，而是让整体服务能力更强。多个副本各自有完整的模型权重，不同副本处理不同请求。这是服务层的扩展，不是算子层的扩展。</p><p>线上流量高并发、波动性强、长短请求混合，单个 engine 即使 continuous batching 做得很好，也总会到达极限。DP 是横向扩展服务副本的主要手段。</p><h3 id="v-llm-v-1-li-de-dp-jin-cheng-jie-gou" tabindex="-1" id="vLLM-V1-里的-DP-进程结构">vLLM V1 里的 DP 进程结构</h3><p>vLLM 的 DP 不只是「多几个模型副本」，而是形成了一套完整的多进程结构。官方架构文档说明：每个 DP rank 有一个 engine core，每个 GPU 有一个 worker 进程，当 <code>data_parallel_size &gt; 1</code> 时还会有一个 DP coordinator 进程。</p><p>具体进程数对比：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">单节点 vllm serve -tp=4：</span><br><span class="line">  1 API server + 1 engine core + 4 GPU workers = 6 个进程</span><br><span class="line"></span><br><span class="line">单节点 vllm serve -tp=2 -dp=4：</span><br><span class="line">  4 API servers + 4 engine cores + 8 GPU workers + 1 DP coordinator = 17 个进程</span><br></pre></td></tr></table></figure><p>每个 DP rank 有自己的 engine core，意味着局部调度相对自治，横向扩展更自然，不需要一个超级大总控进程统管所有 GPU。</p><h3 id="dp-coordinator-de-zuo-yong" tabindex="-1" id="DP-Coordinator-的作用">DP Coordinator 的作用</h3><p>DP coordinator 的存在不是为了「代替 scheduler」，而是做 rank 间协调：</p><ul><li>管理不同 DP rank 之间的 load balancing，避免某个 rank 过载</li><li>在 MoE 场景下协调 synchronized forward passes（各 rank 前向执行必须同步）</li></ul><p>这是一个很典型的系统分层设计：engine core 管自己 rank 内部怎么 batch，coordinator 管 rank 之间怎么配合。比一个大总控进程包办一切更容易扩展，也更清晰。</p><hr><h2 id="ep-yu-dbo-mo-e-wei-shi-yao-shi-te-shu-wen-ti" tabindex="-1" id="EP-与-DBO：MoE-为什么是特殊问题">EP 与 DBO：MoE 为什么是特殊问题</h2><h3 id="mo-e-de-ji-suan-lu-jing-shi-xi-shu-dong-tai-de" tabindex="-1" id="MoE-的计算路径是稀疏动态的">MoE 的计算路径是稀疏动态的</h3><p>Dense 模型里每个 token 都经过同样的 FFN。MoE 不同：每个 token 先经过 router，router 决定它去哪些 expert，不同 token 可能路由到不同专家。</p><p>这个特性带来两个后果：</p><ol><li>如果所有 expert 都堆在同一张卡，会非常拥挤</li><li>Token 会在不同 GPU 之间按 expert 路由，产生 all-to-all 通信</li></ol><p>MoE 的主要通信开销不是来自层内张量切分，而是来自 token 在 expert 间的 dispatch 和 combine。这是 EP 存在的根本原因。</p><h3 id="ep-an-zhuan-jia-wei-du-qie-fen" tabindex="-1" id="EP：按专家维度切分">EP：按专家维度切分</h3><p>EP 的核心：<strong>把不同 expert 分散部署到不同 GPU，使 expert 层沿「专家维度」切分。</strong> 这样同一个 expert 固定在某些 GPU 上，locality 更好，专家层不必硬套 dense 模型的切法，通信和计算可以更贴近 MoE 的结构特点来组织。</p><p>官方文档给出了 EP 的 GPU 数计算关系：</p><p><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mi>E</mi><mi>P</mi><mi mathvariant="normal">_</mi><mi>S</mi><mi>I</mi><mi>Z</mi><mi>E</mi><mo>=</mo><mi>T</mi><mi>P</mi><mi mathvariant="normal">_</mi><mi>S</mi><mi>I</mi><mi>Z</mi><mi>E</mi><mo>×</mo><mi>D</mi><mi>P</mi><mi mathvariant="normal">_</mi><mi>S</mi><mi>I</mi><mi>Z</mi><mi>E</mi></mrow><annotation encoding="application/x-tex">EP\_SIZE = TP\_SIZE \times DP\_SIZE</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.9933em;vertical-align:-0.31em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">EP</span><span class="mord" style="margin-right:0.02778em;">_</span><span class="mord mathnormal" style="margin-right:0.05764em;">S</span><span class="mord mathnormal" style="margin-right:0.07847em;">I</span><span class="mord mathnormal" style="margin-right:0.05764em;">ZE</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.9933em;vertical-align:-0.31em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">TP</span><span class="mord" style="margin-right:0.02778em;">_</span><span class="mord mathnormal" style="margin-right:0.05764em;">S</span><span class="mord mathnormal" style="margin-right:0.07847em;">I</span><span class="mord mathnormal" style="margin-right:0.05764em;">ZE</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.9933em;vertical-align:-0.31em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">D</span><span class="mord mathnormal" style="margin-right:0.13889em;">P</span><span class="mord" style="margin-right:0.02778em;">_</span><span class="mord mathnormal" style="margin-right:0.05764em;">S</span><span class="mord mathnormal" style="margin-right:0.07847em;">I</span><span class="mord mathnormal" style="margin-right:0.05764em;">ZE</span></span></span></span></span></p><p>EP 不是独立存在的一套小并行，而是嵌在整个分布式拓扑里的。官方也明确指出 EP 更高效的前提是与 DP 结合使用。</p><p>MoE 模型里不是每层都要 EP：attention 层仍按 TP/DP 组织，expert 层按 EP 切。这形成了「分层异构并行」结构：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">MoE 模型的并行结构：</span><br><span class="line"></span><br><span class="line">Transformer 层</span><br><span class="line">├── Attention 层 → TP / DP 组织</span><br><span class="line">└── MoE FFN 层</span><br><span class="line">    ├── Router（轻量）</span><br><span class="line">    └── Expert 0, 1, ..., N → EP 组织（不同 expert 在不同 GPU）</span><br><span class="line">                                ↑</span><br><span class="line">                          token 按路由做 all-to-all dispatch</span><br></pre></td></tr></table></figure><p>这正是 MoE 服务比 dense 模型更复杂的根源。</p><h3 id="dbo-ba-tong-xin-cang-dao-ji-suan-hou-mian" tabindex="-1" id="DBO：把通信藏到计算后面">DBO：把通信藏到计算后面</h3><p>MoE 的 expert 层通常伴随 sparse all-to-all 通信（token dispatch → expert 计算 → combine 结果）。如果通信和计算串行排队，即使 GPU 算力没问题，系统也在大量等待通信。</p><p>DBO（Dual Batch Overlap）的目标：<strong>把 MoE 层中的 sparse all-to-all 通信与周围计算重叠起来</strong>，只面向 DP+EP 部署场景。</p><p>实现思路是跨层协同优化：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">DBO 执行流程（简化）：</span><br><span class="line"></span><br><span class="line">原始 batch</span><br><span class="line">    ↓ 切成两个 microbatch（ubatch0, ubatch1）</span><br><span class="line"></span><br><span class="line">Thread 0                    Thread 1</span><br><span class="line">执行 ubatch0 compute    ⇄   等待 ubatch1 all-to-all</span><br><span class="line">等待 ubatch0 all-to-all ⇄   执行 ubatch1 compute</span><br><span class="line">（在 FusedMoEModularKernel 的 yield 点做 ping-pong）</span><br></pre></td></tr></table></figure><p>关键组件包括 <code>GPUModelRunner</code>、<code>UBatchWrapper</code>、<code>UBatchContext</code>。这不是「调一个 NCCL 参数」，而是涉及 batch 组织方式、CPU 线程调度、kernel 内部 yield 点、CUDA graph 执行包装的协同改动。</p><p>这类「让多个阶段彼此重叠」的系统级 overlap 思维，是 MoE infra 优化里最能体现深度的部分。</p><hr><h2 id="yun-xing-shi-xuan-ze-dan-ji-yong-multiprocessing-duo-ji-yong-ray" tabindex="-1" id="运行时选择：单机用-multiprocessing，多机用-Ray">运行时选择：单机用 multiprocessing，多机用 Ray</h2><p>官方文档的建议很直接：</p><ul><li><strong>单机推理</strong>：默认使用 Python multiprocessing</li><li><strong>多机推理</strong>：默认使用 Ray</li></ul><p>单机时 GPU 资源就在本机，进程创建和共享环境更简单，multiprocessing 足够直接且轻量。</p><p>到了多机，需要解决跨节点任务分配、各节点环境一致性、执行位置管理、容错与观测等问题。vLLM 使用 Ray 管理分布式任务执行，同时利用 Ray 提供生产级的 fault tolerance、scaling 和 distributed observability。</p><p>这不是 vLLM「不会做分布式」，而是多机编排本来就是系统工程问题，直接借助成熟分布式运行时更合理。</p><hr><h2 id="wei-shi-yao-v-llm-jia-gou-yao-fen-ceng" tabindex="-1" id="为什么-vLLM-架构要分层">为什么 vLLM 架构要分层</h2><p>理解了四种并行后，可以回过头看 vLLM V1 的大规模服务架构为什么要分成 API server / engine core / GPU worker / DP coordinator 四层。</p><pre><code class="highlight mermaid">flowchart TD    subgraph 服务层        A1[API Server 0]        A2[API Server 1]        A3[API Server 2]        A4[API Server 3]    end    subgraph 协调层        C[DP Coordinator]    end    subgraph 调度层        E1[Engine Core\nDP rank 0]        E2[Engine Core\nDP rank 1]        E3[Engine Core\nDP rank 2]        E4[Engine Core\nDP rank 3]    end    subgraph 执行层        W1[GPU Worker 0]        W2[GPU Worker 1]        W3[GPU Worker 2]        W4[GPU Worker 3]        W5[GPU Worker 4]        W6[GPU Worker 5]        W7[GPU Worker 6]        W8[GPU Worker 7]    end    A1 --&gt; E1    A2 --&gt; E2    A3 --&gt; E3    A4 --&gt; E4    C --- E1 &amp; E2 &amp; E3 &amp; E4    E1 --&gt; W1 &amp; W2    E2 --&gt; W3 &amp; W4    E3 --&gt; W5 &amp; W6    E4 --&gt; W7 &amp; W8</code></pre><p><em>示例：tp=2, dp=4 配置下的进程结构（共 17 个进程）</em></p><p>分层设计的三个目的：</p><p><strong>目的一：把「对外服务」和「内部执行」解耦。</strong> API server 负责 HTTP 接口、输入预处理、tokenizer、流式返回；engine core 负责 scheduler、KV Cache 管理、请求生命周期；GPU worker 负责装模型、执行 forward、管理 GPU 内存。职责清楚，服务层变化不必强耦合到底层执行。</p><p><strong>目的二：让 DP 成为「多个局部自治引擎」的扩展，而不是「单点大总控」的扩展。</strong> 每个 DP rank 各自有 engine core，局部调度相对自治。如果所有请求、KV Cache、worker 都被一个超级中心进程统管，CPU 压力极大，可扩展性更差，中心点出问题影响更广。</p><p><strong>目的三：针对不同并行维度做不同层次的协调。</strong> TP/PP 影响 worker 侧执行拓扑；DP 影响 engine core 数量与请求分流；EP/DBO 影响 MoE 层内执行与 rank 间同步；DP coordinator 处理 rank 间全局协同。不同问题交给最合适的角色，而不是平铺到同一层。</p><hr><h2 id="chang-jing-jue-ce-can-kao" tabindex="-1" id="场景决策参考">场景决策参考</h2><table><thead><tr><th>场景</th><th>推荐并行策略</th><th>关键原因</th></tr></thead><tbody><tr><td>单卡放不下，单机有高速互联</td><td>TP</td><td>最直接，节点内 NVLink 代价可控</td></tr><tr><td>单机但无 NVLink（如 L40S）或 GPU 数切分不整齐</td><td>PP（或 TP=1 + PP=GPU数）</td><td>避免层内高频通信，PP 支持 uneven splits</td></tr><tr><td>模型能跑起来，服务扛不住流量</td><td>DP</td><td>吞吐扩展和服务副本扩展的主要手段</td></tr><tr><td>MoE 模型，专家层通信重</td><td>EP + DP，考虑 DBO</td><td>MoE 稀疏路由有专属通信模式，dense 模型切法不适合</td></tr><tr><td>多节点大模型</td><td>节点内 TP + 节点间 PP（+ DP 扩流量）</td><td>顺硬件拓扑：节点内互联快→TP，节点间网络慢→PP</td></tr></tbody></table><hr><h2 id="he-xin-jie-lun" tabindex="-1" id="核心结论">核心结论</h2><p>大规模 LLM 推理服务的瓶颈不只是计算，还有通信、进程组织和请求分流。<strong>TP/PP 解决模型怎么切，DP 解决流量怎么扩，EP 解决 MoE 专家怎么组织，DBO 解决 MoE 通信和计算怎么 overlap——这四个问题是不同维度的，用一种并行解决所有问题往往会在某一维很差。</strong></p><p>多节点常用「节点内 TP + 节点间 PP」不是习惯，而是顺着硬件通信层级走的结果。</p><p>vLLM 的分层架构——API server / engine core / GPU worker / DP coordinator——是为了把不同层次的问题分别交给最合适的角色，既保持职责清晰，又让 DP 扩展变成「多个局部自治单元的水平扩展」，而不是「单点总控的垂直扩展」。</p><hr><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/a1df357b/">vLLM系统拆解-09-高级加速：Speculative Decoding、CUDA Graph与量化</a></li><li>下一篇：<a href="/posts/c3e99548/">vLLM系统拆解-11-面试题精讲：从概念到系统设计如何讲清楚为什么</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-10-fen-bu-shi-bu-shu-tp-pp-dp-ep-ge-jie-jue-shi-yao-wen-ti&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="分布式推理" scheme="https://smarter.xin/tags/distributed-inference/"/>
    
    <category term="系统架构" scheme="https://smarter.xin/tags/system-architecture/"/>
    
    <category term="张量并行" scheme="https://smarter.xin/tags/tensor-parallelism/"/>
    
    <category term="流水线并行" scheme="https://smarter.xin/tags/pipeline-parallelism/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-09-高级加速：Speculative Decoding、CUDA Graph与量化</title>
    <link href="https://smarter.xin/posts/a1df357b/"/>
    <id>https://smarter.xin/posts/a1df357b/</id>
    <published>2026-04-02T04:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-09-gao-ji-jia-su-speculative-decoding-cuda-graph-yu-liang-hua" tabindex="-1">vLLM系统拆解-09-高级加速：Speculative Decoding、CUDA Graph与量化</h1><p>前几篇把 vLLM 的主干讲清楚了：请求进入引擎 → scheduler 分配 token 预算 → worker 在 GPU 上做 forward → KV Cache 分页管理 → prefix caching 复用跨请求前缀。</p><p>这篇进入另一个层次：在主干系统已经做得比较好的基础上，还能从哪些方向继续榨性能？</p><p>vLLM 当前比较重要的高阶加速可以归为三类：</p><ul><li><strong>算法级</strong>：Speculative Decoding（减少 decode 串行等待）</li><li><strong>执行级</strong>：CUDA Graph（减少重复执行的固定开销）</li><li><strong>数值级</strong>：量化（降低显存占用与数据移动成本）</li></ul><p>这三类加速有一个共同特点：<strong>每一种都只对特定瓶颈有效，开错场景收益有限，甚至有副作用。</strong> 把它们当成无脑加速开关是常见误区。</p><hr><h2 id="san-lei-jia-su-zong-lan" tabindex="-1" id="三类加速总览">三类加速总览</h2><p>先建立整体认知，再展开每一类。</p><table><thead><tr><th>机制</th><th>本质优化对象</th><th>常见收益</th><th>常见代价</th></tr></thead><tbody><tr><td>Speculative Decoding</td><td>decode 串行生成的等待成本</td><td>降低 ITL，改善低 QPS 场景响应流畅度</td><td>额外 draft 计算、调度更复杂、高并发下收益不稳</td></tr><tr><td>CUDA Graph</td><td>重复 launch 与框架调度的固定开销</td><td>降低每轮执行开销，提升 decode 稳定性</td><td>需要额外 capture 内存，对动态 batch 不友好</td></tr><tr><td>量化</td><td>显存占用、数据移动与带宽压力</td><td>降低显存占用，可能提升吞吐</td><td>精度风险、算子兼容性、硬件收益差异大</td></tr></tbody></table><p>这三者不是 vLLM 的地基，而是在「调度 + 内存 + 执行链已经对齐」之后，继续拉高性能上限的手段。</p><hr><h2 id="speculative-decoding-jian-de-bu-shi-dan-ci-forward-shi-chuan-xing-deng-dai" tabindex="-1" id="Speculative-Decoding：减的不是单次-forward，是串行等待">Speculative Decoding：减的不是单次 forward，是串行等待</h2><h3 id="wei-shi-yao-decode-tian-ran-shi-chuan-xing-de" tabindex="-1" id="为什么-decode-天然是串行的">为什么 decode 天然是串行的</h3><p>Autoregressive 生成的根本约束是：<strong>生成第 t+1 个 token，必须等第 t 个 token 已经确定。</strong> 这意味着即使 GPU 算力很强，decode 阶段也只能一步一步串行走。</p><p>单次 forward 跑得再快，每一步之间的等待成本仍然存在。这是 speculative decoding 要解决的问题——注意，它不是让单次 forward 更快，而是<strong>减少必须严格串行等待的轮数</strong>。</p><h3 id="cai-ce-yan-zheng-de-ji-ben-kuang-jia" tabindex="-1" id="猜测-验证的基本框架">猜测-验证的基本框架</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────┐</span><br><span class="line">│              一轮 speculative decoding           │</span><br><span class="line">│                                                  │</span><br><span class="line">│  Drafter（便宜）→  token1, token2, token3, token4 │</span><br><span class="line">│                                ↓                 │</span><br><span class="line">│  Target model（一次并行验证）                      │</span><br><span class="line">│                  ✓     ✓     ✗                  │</span><br><span class="line">│                                ↓                 │</span><br><span class="line">│  接受 token1, token2，拒绝 token3 及之后           │</span><br><span class="line">└─────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>核心逻辑：先用一个更便宜的 drafter/proposer 预测接下来几个 token，再让 target model 并行验证。如果 drafter 猜得准，target model 一轮就能确认多个 token，平均串行等待时间下降，用户感受到的输出速度提高。</p><p>这套机制是正确的（被拒绝的 token 不会改变分布），但有成本：drafter 本身要算，verification 逻辑更复杂，batch 结构可能更不规则，scheduler 处理路径也更复杂。</p><h3 id="gua-yong-chang-jing-you-ming-que-bian-jie" tabindex="-1" id="适用场景有明确边界">适用场景有明确边界</h3><p>Speculative decoding 更适合：</p><ul><li>QPS 不高、memory-bound decode 是主要瓶颈的场景</li><li>用户对 ITL（inter-token latency）敏感、更在意对话流畅度的交互式应用</li></ul><p><strong>高并发吞吐压榨场景下，收益不一定稳定。</strong> 这时系统更怕额外计算和复杂调度，而不是怕单个请求的 token 间等待。用一句话概括：speculative decoding 是延迟优化工具，不是吞吐优化工具。</p><h3 id="v-llm-wei-shi-yao-zhi-chi-duo-chong-speculation-fang-fa" tabindex="-1" id="vLLM-为什么支持多种-speculation-方法">vLLM 为什么支持多种 speculation 方法</h3><p>vLLM 没有把 speculative decoding 做成单一机制，而是支持 draft model、EAGLE、MTP、PARD、MLP speculator、n-gram、suffix decoding 等多种方法。</p><table><thead><tr><th>方法类型</th><th>思路</th><th>优点</th><th>局限</th></tr></thead><tbody><tr><td>Draft model</td><td>额外小模型先草拟候选 token</td><td>通用，适合多数场景</td><td>需维护额外模型，drafter 精度和延迟都很关键</td></tr><tr><td>MTP（Multi-Token Prediction）</td><td>目标模型原生支持一次预测多个未来 token</td><td>若模型原生支持则路径更自然，协调成本低</td><td>依赖模型架构，不通用</td></tr><tr><td>n-gram / suffix</td><td>利用上下文重复模式或简单启发式猜测</td><td>轻量，易启用，在代码补全等重复性场景有价值</td><td>收益不如 model-based 稳定，上限有限</td></tr></tbody></table><p>这背后的设计判断很清楚：不同模型家族、不同硬件环境、不同业务目标，适合的方案并不一样。vLLM 给出的是一套统一的承载框架，而不是把某种具体方案写死。</p><h3 id="dui-zhu-gan-xi-tong-de-ying-xiang" tabindex="-1" id="对主干系统的影响">对主干系统的影响</h3><p>Speculative decoding 不只是「多了个 drafter」，它改变了整个调度-执行链的语义：</p><ul><li><strong>scheduler 层</strong>：需要考虑请求是否走 speculation 路径、当前 speculation 深度、accept/reject 后下一轮预算如何分配</li><li><strong>worker 层</strong>：需要执行 draft path、verify path、处理 accept/reject 逻辑、维护 speculative metadata</li><li><strong>性能分析层</strong>：不能只看原始吞吐，还要看 acceptance ratio、drafter 额外成本是否合算、高 batch 下是否退化</li></ul><hr><h2 id="cuda-graph-gu-ding-kai-xiao-wen-ti-bu-shi-ji-suan-wen-ti" tabindex="-1" id="CUDA-Graph：固定开销问题，不是计算问题">CUDA Graph：固定开销问题，不是计算问题</h2><h3 id="decode-jie-duan-de-gu-ding-kai-xiao-kun-jing" tabindex="-1" id="Decode-阶段的固定开销困境">Decode 阶段的固定开销困境</h3><p>Decode 每步工作量不大，但如果每步都要经历：Python 层发起调用 → 构图/调度 → 发起一串 GPU kernel launch，那么固定管理成本在整个执行时间里的占比会相当高。</p><p>batch 较小、模型较小、decode 路径重复度高的场景尤其明显。CUDA Graph 的出发点就是：<strong>既然很多 decode batch 的执行结构高度相似，为什么不能把这套执行图录下来，后面直接重放？</strong></p><p>这是执行路径的优化，不是数学计算本身的优化。Replay 后减少的是 CPU 参与度和 launch 开销，GPU 跑的还是同样的计算。</p><h3 id="wei-shi-yao-bu-neng-dui-suo-you-batch-du-yong-cuda-graph" tabindex="-1" id="为什么不能对所有-batch-都用-CUDA-Graph">为什么不能对所有 batch 都用 CUDA Graph</h3><p>关键约束：<strong>CUDA Graph 要求执行形状足够稳定。</strong> 但 LLM serving 的核心特点恰好是高度动态：</p><ul><li>batch size 在变</li><li>序列长度在变</li><li>prefill/decode/mixed batch 形态在变</li><li>LoRA、多模态、attention backend 条件在变</li></ul><p>这就是 vLLM CUDA Graph 设计复杂性的来源——不是 API 难，而是「大多数时候 batch 不足够稳定」。</p><h3 id="full-yu-piecewise-liang-chong-mo-shi" tabindex="-1" id="Full-与-Piecewise-两种模式">Full 与 Piecewise 两种模式</h3><p>vLLM 不是简单二选一，而是用分层退化策略：</p><table><thead><tr><th>模式</th><th>适合场景</th><th>优点</th><th>代价</th></tr></thead><tbody><tr><td>Full CUDA Graph</td><td>结构高度稳定的均匀 decode batch</td><td>固定开销压得最狠，性能上限高</td><td>capture 条件苛刻，额外内存更多</td></tr><tr><td>Piecewise CUDA Graph</td><td>更复杂的 batch，只对可稳定的子段做 capture</td><td>更灵活，对动态 batch 适配度更高</td><td>理论极限性能通常低于 full graph</td></tr><tr><td>Eager（不走 graph）</td><td>形态变化太大无法 capture 时兜底</td><td>无额外约束</td><td>每步都有 launch 开销</td></tr></tbody></table><p>实际策略：能 full 就尽量 full；不适合 full 时退到 piecewise；再不行就 eager。</p><h3 id="zai-v-1-li-cuda-graph-shi-yun-xing-shi-mo-shi-bu-shi-shou-dong-kai-guan" tabindex="-1" id="在-V1-里，CUDA-Graph-是运行时模式，不是手动开关">在 V1 里，CUDA Graph 是运行时模式，不是手动开关</h3><p>这一点很重要。在 vLLM V1 中，CUDA Graph 不是用户勾选的 feature，而是由 dispatcher 根据当前 batch 条件做模式选择：当前 batch 是否规整、attention backend 是否兼容、是否是 pure decode / mixed batch、当前 compilation level 是什么。</p><p>这个设计说明 vLLM 已经把 CUDA Graph 放进了主运行时体系，而不是外部包装器。理解这一点，就超过了「知道如何开启 CUDA Graph」这个层次。</p><p>CUDA Graph 的代价同样要正视：capture 本身占额外内存，启动阶段需要编译和准备多个 batch size 变体，batch 越动态则 graph 命中率越低，收益越小。用一句话总结：<strong>用更多前期准备与额外内存，换取运行期更低的固定开销。</strong></p><hr><h2 id="liang-hua-bu-shi-wei-liao-zhuang-de-xia-er-shi-wei-liao-pao-de-dong" tabindex="-1" id="量化：不是为了「装得下」，而是为了「跑得动」">量化：不是为了「装得下」，而是为了「跑得动」</h2><h3 id="si-lei-jie-zhi" tabindex="-1" id="四类价值">四类价值</h3><p>量化不只是「模型太大装不下所以压缩一下」。更完整的理解是：量化同时影响容量、带宽、计算路径、吞吐和部署成本。</p><p>具体来说，在推理里量化至少有四类价值：</p><ol><li>让更大的模型放进显存</li><li>减少权重读取的数据量（直接降低带宽压力）</li><li>在合适硬件上提高吞吐（使用低比特专用 kernel）</li><li>降低部署成本</li></ol><p>LLM 的 decode 阶段本来就容易是 memory-bound 的。如果把权重从 FP16/BF16 压到 INT8/FP8/INT4，等于从「数据移动成本」这个根上减负，直接影响 tokens/s。</p><h3 id="quan-zhong-liang-hua-vs-kv-cache-liang-hua-bi-xu-fen-kai-li-jie" tabindex="-1" id="权重量化-vs-KV-Cache-量化：必须分开理解">权重量化 vs KV Cache 量化：必须分开理解</h3><p>这是面试里很容易被追问的点，两类量化解决的是完全不同的问题：</p><table><thead><tr><th></th><th>权重量化</th><th>KV Cache 量化</th></tr></thead><tbody><tr><td>优化目标</td><td>减小模型参数占用与读取带宽</td><td>减小历史上下文缓存的显存占用</td></tr><tr><td>主要受益阶段</td><td>prefill 和 decode 都有帮助</td><td>decode 更关键，长上下文、高并发更关键</td></tr><tr><td>核心机制</td><td>每次 forward 访问权重的数据量减少</td><td>KV Cache 体积随序列增长和并发增长的累积速度降低</td></tr></tbody></table><p>两者最终都会反馈到调度层：可用 KV Cache 空间变大 → preemption 频率降低 → batch 能做更大 → 整体吞吐改善。<strong>量化虽然发生在数值表示层，但收益会传导到整个 serving 调度层。</strong></p><h3 id="v-llm-zhi-chi-da-liang-liang-hua-ge-shi-de-yuan-yin" tabindex="-1" id="vLLM-支持大量量化格式的原因">vLLM 支持大量量化格式的原因</h3><p>AWQ、GPTQ、bitsandbytes、GGUF、INT4 W4A16、INT8 W8A8、FP8 W8A8、KV cache 量化、TorchAO、Model Optimizer……量化方案很多，原因是<strong>量化从来不是「一招打天下」</strong>：</p><ul><li>某些方法更适合离线权重量化（部署前转换）</li><li>某些方法更适合特定 GPU 架构（依赖专用 kernel）</li><li>某些方法重点在保持精度，某些在极致压缩</li><li>某些方法需要模型架构层的配合</li></ul><p>所以 vLLM 的正确定位不是「某种量化工具」，而是「可接入多量化实现的推理平台」。</p><p>量化的代价也需要正视：精度损失风险（信息压缩必然损耗）、硬件专用性（不是所有平台都能把低比特收益吃满）、工程兼容性（模型结构、attention/MoE/LoRA 路径是否兼容对应 kernel）。不是 bit 数越低越好，正确思路是在可接受精度损失的前提下，找到最适合当前硬件和业务目标的方案。</p><hr><h2 id="san-lei-jia-su-yu-zhu-gan-xi-tong-de-guan-lian" tabindex="-1" id="三类加速与主干系统的关联">三类加速与主干系统的关联</h2><p>这一节值得单独拿出来。学完三类加速之后，最容易犯的错误是把它们当成孤立的 feature 去记忆。正确的方式是把它们嵌回 vLLM 的主干系统。</p><pre><code class="highlight mermaid">flowchart TD    subgraph 主干系统        A[Scheduler\n token 预算分配] --&gt; B[Worker\n GPU forward]        C[KV Cache Manager\n 分页显存] --&gt; B    end    subgraph 高级加速        D[Speculative Decoding] -- 改变调度语义\n speculation深度/accept/reject预算 --&gt; A        E[CUDA Graph] -- 改变 worker 执行模式\nfull/piecewise/eager dispatch --&gt; B        F[量化] -- 改变显存分配\n权重更小→KV Cache空间更大 --&gt; C    end    D -- 改变 worker 执行结构\ndraft path + verify path --&gt; B    F -- 影响 preemption 频率\n和可用 batch 预算 --&gt; A</code></pre><p>三类加速之间也会互相影响：</p><ul><li>speculative decoding 会改变 batch 形态和执行路径，可能影响 CUDA Graph 的可捕获性</li><li>量化影响 memory budget，从而影响 batch size、KV Cache 容量和 preemption 频率</li><li>CUDA Graph 本身需要额外内存，也会和量化、KV Cache 预算形成耦合</li></ul><p>这就是为什么 vLLM 要把这些优化放进统一运行时框架，而不是做成简单开关：现实 serving 环境的 batch 高度动态、硬件条件不同、模型家族不同、业务目标不同，每种优化都需要根据当前条件决定是否启用、以哪种模式启用。</p><hr><h2 id="ji-ge-he-xin-jie-lun" tabindex="-1" id="几个核心结论">几个核心结论</h2><p><strong>Speculative decoding</strong> 优化的是 decode 的串行等待，不是单次 forward 的底层效率。它更偏延迟优化，尤其适合低到中等 QPS、memory-bound 场景；高并发下收益不稳。</p><p><strong>CUDA Graph</strong> 优化的是重复执行中的固定开销，不改变数学计算本身。LLM serving batch 高度动态，所以 vLLM 必须有 full/piecewise/eager 的分层退化设计，而不是靠单一 graph 模式走到底。</p><p><strong>量化</strong>优化的是显存占用、数据移动和带宽压力，不是只为了「模型装得下」。权重量化和 KV Cache 量化解决的是两类不同问题，最终收益都会向上传导到调度层。</p><p>三类加速有一个共同的适用边界：<strong>它们都只对特定瓶颈有效。</strong> 判断系统当前真正的瓶颈是什么，比选择哪种加速机制更重要。</p><hr><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/b0da0eba/">vLLM系统拆解-08-性能优化：参数背后的系统级权衡</a></li><li>下一篇：<a href="/posts/9ce7b444/">vLLM系统拆解-10-分布式部署：TP、PP、DP、EP各解决什么问题</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-09-gao-ji-jia-su-speculative-decoding-cuda-graph-yu-liang-hua&quot; tabindex=&quot;-1&quot;&gt;vLLM系统拆解-09-高级加速：Speculative</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="speculative decoding" scheme="https://smarter.xin/tags/speculative-decoding/"/>
    
    <category term="CUDA Graph" scheme="https://smarter.xin/tags/cuda-graph/"/>
    
    <category term="推理加速" scheme="https://smarter.xin/tags/inference-acceleration/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-08-性能优化：参数背后的系统级权衡</title>
    <link href="https://smarter.xin/posts/b0da0eba/"/>
    <id>https://smarter.xin/posts/b0da0eba/</id>
    <published>2026-04-02T04:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-08-xing-neng-you-hua-can-shu-bei-hou-de-xi-tong-ji-quan-heng" tabindex="-1">vLLM系统拆解-08-性能优化：参数背后的系统级权衡</h1><p>很多教程讲 vLLM 调优，都是直接给一张参数表：<code>max_num_batched_tokens</code> 设多少，<code>gpu_memory_utilization</code> 设多少。这种方式的问题在于——你知道了答案，但不知道为什么，下次换个场景就不会用了。</p><p>这篇文章从 vLLM 处理混合工作负载的根本矛盾讲起，把 chunked prefill、preemption 和几个关键参数放到同一个框架下理解。读完后，你能解释这些参数的设计动机，也能针对不同业务场景给出合理的调优方向——而不只是背一个参数默认值。</p><p>前置知识：建议先读 <a href="/posts/1202d62c/">vLLM系统拆解-04-Scheduler：为什么调度单位是token budget</a> 和 <a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a>。</p><hr><h2 id="llm-tui-li-shi-yi-chong-hun-he-gong-zuo-fu-zai" tabindex="-1" id="LLM-推理是一种混合工作负载">LLM 推理是一种混合工作负载</h2><p>线上 LLM 推理和传统批处理服务有一个根本区别：它同时跑着性质完全不同的任务。</p><p>同一时刻，系统里可能有：刚进来的 16k token 长 prompt、正在生成第 200 个 token 的对话请求、还差几步就完成的短生成任务，以及前缀高度重复的批量请求。它们最优的处理方式并不一样。</p><p>更关键的是：<strong>prefill 和 decode 是两种截然不同的计算瓶颈</strong>。</p><p>Prefill 阶段一次性处理整段 prompt，通常是计算密集型（compute-bound）；decode 阶段每轮只生成 1 个 token，但必须读取所有历史 KV Cache，通常是显存带宽密集型（memory-bound）。两者混在同一系统里，单独优化任何一个都会损害另一个。</p><p>这就是 vLLM 性能优化要解决的核心矛盾，也是接下来所有参数设计的出发点。</p><p>vLLM 所有性能优化几乎都围绕四件事展开：</p><ol><li>让更多请求能够同时留在系统里</li><li>让 GPU 每一轮都吃到合适的工作量</li><li>不让长 prompt 把短请求和 decode 请求拖死</li><li>在显存不够时，系统不崩，而是优雅退化</li></ol><hr><h2 id="chunked-prefill-bu-shi-qie-prompt-er-shi-rang-prefill-bian-cheng-ke-diao-du-de-gong-zuo-xiang" tabindex="-1" id="Chunked-Prefill：不是切-prompt，而是让-prefill-变成可调度的工作项">Chunked Prefill：不是切 prompt，而是让 prefill 变成可调度的工作项</h2><h3 id="zhen-zheng-de-wen-ti-shi-shi-yao" tabindex="-1" id="真正的问题是什么">真正的问题是什么</h3><p>第一次听到 chunked prefill，很多人的理解是：「哦，就是把长 prompt 切成几段慢慢处理。」这只说对了表象。</p><p>真正的问题不是 prompt 太长，而是：<strong>长 prompt 如果以整块方式进入 batch，会强行占满本轮 token 预算，让 decode 请求被迫等待。</strong></p><p>举个具体例子：</p><ul><li>请求 A：16k token 的超长 prompt，刚进来</li><li>请求 B：已经生成了 150 个 token，正在 decode，用户盯着屏幕看</li></ul><p>如果调度器把 A 的整段 prefill 塞进来，本轮 token 预算几乎被 A 吃完。B 明明只需要生成下一个 token，却要等 A 的大块 prefill 跑完。用户感受到的就是生成流突然停顿了。</p><p>吞吐可能不低，但 ITL（inter-token latency，生成相邻两个 token 之间的延迟）会明显变差。这是线上服务最典型的「指标看起来还行，但体感很差」。</p><h3 id="she-ji-si-xiang" tabindex="-1" id="设计思想">设计思想</h3><p>vLLM 的 chunked prefill 是<strong>统一调度框架下的一种预算分配方式</strong>，核心逻辑如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">每轮 iteration：</span><br><span class="line">  ① decode 请求优先占位（每条各占 1 token）</span><br><span class="line">  ② 剩余 budget → 填充 prefill</span><br><span class="line">  ③ 某个 prefill 太大放不下 → 只放部分，下轮继续</span><br><span class="line">  ④ 重复，直到预算耗尽</span><br></pre></td></tr></table></figure><p>本质不是「切 prompt」，而是：<strong>让 prefill 以可中断、可分片、可混排的方式进入调度系统。</strong></p><h3 id="san-ge-hao-chu" tabindex="-1" id="三个好处">三个好处</h3><p><strong>好处一：decode ITL 更稳定。</strong> decode 拥有更高优先级，长 prompt 只能利用剩余预算推进，不会占满全部资源。</p><p><strong>好处二：GPU 利用率通常更好。</strong> 纯 prefill batch 是 compute-bound，纯 decode batch 是 memory-bound。把两者按预算混排后，GPU 更容易同时利用算力和显存带宽，不至于某一种资源过度闲置。</p><p><strong>好处三：长请求不再是系统节奏的破坏者。</strong> 没有 chunked prefill 时，遇到一个 16k 请求就是系统抖动；有了它，超长请求变成「可被吸收的细粒度工作项」，系统节奏更平稳。</p><h3 id="liang-ge-dai-jie" tabindex="-1" id="两个代价">两个代价</h3><table><thead><tr><th>代价</th><th>具体表现</th></tr></thead><tbody><tr><td>调度逻辑更复杂</td><td>不再只是「这轮跑谁」，要回答每个请求跑多少 token、谁让位给谁、多模态 token 能否被切分</td></tr><tr><td>三指标更容易拉扯</td><td>decode 保护越强，TTFT 越慢；TTFT 越优先，decode 越容易被挤；吞吐越大，稳定性越容易变差</td></tr></tbody></table><p>chunked prefill 给系统提供了更多权衡空间，但它本身不承诺「所有指标同时更好」。</p><hr><h2 id="san-ge-he-xin-can-shu" tabindex="-1" id="三个核心参数">三个核心参数</h2><p>这三个参数是大多数调优场景里最先要动的。</p><h3 id="code-max-num-batched-tokens-code-dan-lun-token-yu-suan-shang-xian" tabindex="-1" id="max-num-batched-tokens：单轮-token-预算上限"><code>max_num_batched_tokens</code>：单轮 token 预算上限</h3><p>这是调度行为最直接的控制旋钮。它决定<strong>单轮 iteration 最多允许处理多少 token</strong>，也就是上文 chunked prefill 逻辑里的「总预算」。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">单轮 token 预算</span><br><span class="line">   ├── decode 请求：每条 1 token（优先占位）</span><br><span class="line">   └── 剩余 → prefill token（按 chunked prefill 规则填充）</span><br></pre></td></tr></table></figure><p>调小和调大的效果截然相反：</p><table><thead><tr><th></th><th><code>max_num_batched_tokens</code> 较小</th><th><code>max_num_batched_tokens</code> 较大</th></tr></thead><tbody><tr><td>ITL</td><td>更好（decode 更容易被保护）</td><td>更差（prefill 可能挤占更多）</td></tr><tr><td>TTFT</td><td>更差（prompt 推进慢）</td><td>更好（prefill 大步推进）</td></tr><tr><td>吞吐</td><td>较低（每轮吃的 token 少）</td><td>较高（batch 做得更大）</td></tr><tr><td>适合场景</td><td>交互式聊天、中低并发</td><td>批量离线推理、吞吐优先</td></tr></tbody></table><p>一个有用的心理模型：这个参数控制的不是「batch 大小」，而是<strong>系统每轮愿意承担多少工作量，从而决定它更像低延迟交互服务还是高吞吐处理引擎</strong>。</p><h3 id="code-max-num-seqs-code-bing-fa-dui-xiang-shu-shang-xian" tabindex="-1" id="max-num-seqs：并发对象数上限"><code>max_num_seqs</code>：并发对象数上限</h3><p>和 <code>max_num_batched_tokens</code> 经常要一起看。<code>max_num_batched_tokens</code> 控制「装多少货」，<code>max_num_seqs</code> 控制「坐多少人」。</p><p>同样是 4096 token，可能是 4 条序列各 1024，也可能是 256 条序列各 16。序列数更多时：</p><ul><li>scheduler 元数据更多，调度开销更高</li><li>KV block 管理更分散</li><li>某些场景下显存碎片和管理开销更明显</li><li>活跃请求更多，占用的 KV Cache block 也更多</li></ul><p>最后一点直接关联 preemption：<strong><code>max_num_seqs</code> 设得过大，KV Cache 更容易不够，preemption 更容易发生，端到端延迟反而变差。</strong></p><h3 id="code-gpu-memory-utilization-code-gei-kv-cache-teng-duo-shao-di-pan" tabindex="-1" id="gpu-memory-utilization：给-KV-Cache-腾多少地盘"><code>gpu_memory_utilization</code>：给 KV Cache 腾多少地盘</h3><p>GPU 显存要同时装下：模型权重 + activation/runtime buffer + KV Cache。这个参数控制<strong>当前 vLLM 实例愿意使用多少比例的 GPU 显存</strong>，间接决定 KV Cache 能分到多大空间。</p><p>调高这个值通常意味着 KV Cache 空间变大 → 能容纳更多活跃请求 → preemption 频率下降。</p><p>但它不能设到 100%，原因很实际：runtime 期间有临时 buffer 需求，某些操作有瞬时峰值，别的进程也可能需要显存。设太满会让系统对突发负载更脆弱，稳定性下降。</p><hr><h2 id="preemption-wei-shi-yao-ta-bi-ran-cun-zai" tabindex="-1" id="Preemption：为什么它必然存在">Preemption：为什么它必然存在</h2><p>面试常见问题：「系统设计得够好，是不是就不需要 preemption？」</p><p>答案是否定的。在 LLM 推理里，尤其是高并发 + 长上下文场景，preemption 是系统<strong>必须具备</strong>的保护机制，而不是设计缺陷。</p><p>原因在于 autoregressive 推理的一个天然特性：</p><ul><li>请求在持续生成时，KV Cache 会持续增长</li><li>不同请求的长度和生成速度不同</li><li>活跃请求集合随时变化</li><li>某一轮看起来能容纳，下几轮未必还能容纳</li></ul><p>即使有分页式 KV 管理，也无法静态保证「任意时刻所有活跃请求都能同时住在显存里」。当空间不够时，系统只有三条路：崩掉、拒绝新请求、或者让部分请求暂时退出活跃执行。</p><p>vLLM 选的是第三条——<strong>优雅退化而非失控崩溃</strong>。</p><h3 id="recompute-huan-shi-swap" tabindex="-1" id="RECOMPUTE-还是-SWAP">RECOMPUTE 还是 SWAP</h3><p>被抢占的请求有两种恢复方式：</p><ul><li><strong>RECOMPUTE</strong>：丢弃被抢占请求的 KV Cache，恢复时重新计算</li><li><strong>SWAP</strong>：把被抢占请求的 KV Cache 搬到 CPU 内存，恢复时搬回来</li></ul><p>vLLM V1 默认更偏向 RECOMPUTE。原因是：在 V1 的整体架构下，重算的综合开销通常比维护复杂的 swap 路径更低。swap 不是免费的——CPU-GPU 数据搬运有延迟，状态管理也更复杂。RECOMPUTE 是<strong>用算力换显存和状态复杂度</strong>，是一种被明确接受的工程权衡，而不是低劣的兜底方案。</p><h3 id="preemption-de-dai-jie" tabindex="-1" id="Preemption-的代价">Preemption 的代价</h3><p>preemption 虽然保护了系统稳定性，但它打断了请求的连续推进。被抢占的请求需要重新排队、重新补齐执行状态，端到端 latency 会增加。<strong>频繁看到 preemption 日志，不应简单理解为「功能正常」，而应判断系统是否已经在显存压力下吃力维持了。</strong></p><h3 id="pai-cha-pin-fan-preemption" tabindex="-1" id="排查频繁-preemption">排查频繁 preemption</h3><pre><code class="highlight mermaid">flowchart TD    A[频繁 preemption] --&gt; B&#123;KV Cache 空间是否太小?&#125;    B --&gt;|是| C[提高 gpu_memory_utilization\n或指定 kv_cache_memory_bytes\n或换用更省的 kv_cache_dtype]    B --&gt;|否| D&#123;并发对象数是否过高?&#125;    D --&gt;|是| E[降低 max_num_seqs\n或降低 max_num_batched_tokens]    D --&gt;|否| F&#123;模型权重是否占了太多显存?&#125;    F --&gt;|是| G[增大 tensor_parallel_size / pipeline_parallel_size\n或使用量化 / offload]    G --&gt; H[注意: TP/PP 引入通信延迟\n不是免费午餐]</code></pre><p>注意模型并行度这一路：TP（张量并行）和 PP（流水线并行）能释放单卡显存压力，但会引入额外通信或流水线延迟。不要把「显存不够」机械地等同于「上更多并行度」。</p><hr><h2 id="cpu-ye-hui-cheng-wei-ping-jing" tabindex="-1" id="CPU-也会成为瓶颈">CPU 也会成为瓶颈</h2><p>只盯 GPU 利用率是一个常见误区。</p><p>vLLM 是多进程服务系统，不是单纯的 forward 库。API server 进程、engine core 进程、每张 GPU 的 worker 进程，全都需要 CPU 资源。其中 engine core 的 busy loop 对 CPU 调度能力很敏感。</p><p>CPU 资源不足时，典型现象是：</p><ul><li>GPU 明明还有活可做，却吃不到</li><li>你看到 GPU 利用率偏低，但问题出在 CPU 喂不动，而不是 GPU 不够强</li></ul><p>CPU 需要承担的工作包括：接收请求、tokenizer 处理、scheduler 做本轮决策、向 worker 分发执行指令、输出回传和流式发送、状态维护和日志统计。任何一个环节成为瓶颈，都会让 GPU 空转等待。</p><p>这也是为什么真正的 infra 调优需要先判断瓶颈在哪里：GPU、显存带宽、调度逻辑、CPU 核心数，还是进程间通信。</p><hr><h2 id="qi-yu-can-shu-jing-du-li-du-yu-rong-liang" tabindex="-1" id="其余参数：精度、粒度与容量">其余参数：精度、粒度与容量</h2><h3 id="code-kv-cache-dtype-code" tabindex="-1" id="kv-cache-dtype"><code>kv_cache_dtype</code></h3><p>控制 KV Cache 用什么数据类型存储。类型越省，同等显存下能容纳的 token 更多，但数值精度和实现支持会有差异。是精度、实现复杂度、KV 容量三者之间的取舍。</p><h3 id="code-block-size-code" tabindex="-1" id="block-size"><code>block_size</code></h3><p>分页式 KV Cache 的块大小，可以理解为「KV Cache 的页大小」。</p><table><thead><tr><th>block_size 较小</th><th>block_size 较大</th></tr></thead><tbody><tr><td>分配更灵活</td><td>管理更简单</td></tr><tr><td>末尾碎片可能更小</td><td>末尾浪费可能更明显</td></tr><tr><td>管理复杂度更高</td><td>不匹配粒度的请求浪费更多</td></tr></tbody></table><h3 id="offload" tabindex="-1" id="offload">offload</h3><p>把部分状态或权重借助 CPU 内存存储，以 CPU-GPU 间数据搬运换取「看起来更大的可用显存」。</p><p>核心点：<strong>offload 不是凭空增加性能，而是用带宽和搬运复杂度换容量</strong>。如果 CPU-GPU 互连不够快（比如 PCIe 带宽有限），offload 可能让性能明显下降。它是显存压力下的空间换时间手段，而不是默认加速手段。</p><h3 id="chunked-prefill-jin-jie-can-shu" tabindex="-1" id="Chunked-Prefill-进阶参数">Chunked Prefill 进阶参数</h3><p>这三个参数用于更细粒度地控制长 prompt 的调度行为：</p><table><thead><tr><th>参数</th><th>控制内容</th></tr></thead><tbody><tr><td><code>max_num_partial_prefills</code></td><td>允许多少条序列同时以「部分 prefill」方式推进</td></tr><tr><td><code>max_long_partial_prefills</code></td><td>长 prompt 里最多允许多少个同时进行部分 prefill</td></tr><tr><td><code>long_prefill_token_threshold</code></td><td>多长的 prompt 算「长 prompt」</td></tr></tbody></table><p><code>max_long_partial_prefills</code> 和 <code>max_num_partial_prefills</code> 配合使用有个典型效果：把前者设得更小，系统会更积极地让短 prompt 插队。背后的思路是——长请求不能把全场都堵死，短请求要有机会穿过。</p><hr><h2 id="san-zhi-biao-bu-neng-tong-shi-la-man" tabindex="-1" id="三指标不能同时拉满">三指标不能同时拉满</h2><p>这是调优里最需要理解清楚的约束。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">高吞吐 ←————→ 低 TTFT ←————→ 低 ITL</span><br><span class="line">    ↑                              ↑</span><br><span class="line">  互相拉扯，无法同时最大化</span><br></pre></td></tr></table></figure><p>三个方向各自要求的配置方向是相反的：</p><table><thead><tr><th>优化目标</th><th>倾向配置</th><th>代价</th></tr></thead><tbody><tr><td><strong>高吞吐</strong></td><td>更大的 <code>max_num_batched_tokens</code>、更高的显存利用率、更激进地容纳请求</td><td>decode 体验和端到端稳定性可能变差</td></tr><tr><td><strong>低 ITL</strong></td><td>更保护 decode，限制大 prefill 对单轮预算的侵占，<code>max_num_batched_tokens</code> 偏保守</td><td>TTFT 和整体吞吐可能下降</td></tr><tr><td><strong>低 TTFT</strong></td><td>给 prefill 更多预算，允许大 prompt 大步推进</td><td>decode 更容易被拖慢，ITL 恶化</td></tr></tbody></table><p>成熟的调优思路不是「把三个指标都拉满」，而是<strong>根据 workload 的业务目标，先确定优先级，再围绕这个优先级做取舍</strong>。</p><hr><h2 id="an-ye-wu-chang-jing-zuo-diao-you-jue-ce" tabindex="-1" id="按业务场景做调优决策">按业务场景做调优决策</h2><p>不同场景的优先级不同，参数方向也随之变化：</p><table><thead><tr><th>场景</th><th>核心指标</th><th>调优方向</th></tr></thead><tbody><tr><td><strong>交互式聊天</strong></td><td>ITL、生成流畅度</td><td><code>max_num_batched_tokens</code> 适中偏小；谨慎控制长 prompt 并发推进；减少 preemption；保护 decode</td></tr><tr><td><strong>批量离线推理</strong></td><td>吞吐、GPU 利用率</td><td><code>max_num_batched_tokens</code> 偏大；提高显存利用率；接受一定 ITL 退化</td></tr><tr><td><strong>长上下文混合流量</strong></td><td>防止超长请求拖崩系统</td><td>合理设置 <code>long_prefill_token_threshold</code>；限制 <code>max_long_partial_prefills</code>；更注意 KV Cache 空间与并发对象数</td></tr><tr><td><strong>显存紧张单卡</strong></td><td>系统稳定性</td><td>先检查 <code>gpu_memory_utilization</code> 是否合理；考虑更省的 <code>kv_cache_dtype</code>；必要时考虑 offload 或量化；目标是先让系统稳定运行，再谈极致指标</td></tr></tbody></table><p>显存紧张场景有一个常见误区：一上来就想着量化或 offload。正确顺序应该是先把 <code>gpu_memory_utilization</code> 和 <code>max_num_seqs</code> 调到合理范围，确认 preemption 频率降下来，系统能稳定跑目标并发，再考虑更激进的显存优化手段。</p><hr><h2 id="he-xin-jie-lun" tabindex="-1" id="核心结论">核心结论</h2><p>这篇文章讲了很多参数，但真正的结论只有一条：</p><blockquote><p><strong>vLLM 的「快」，不是来自某个神奇的 kernel，而是来自调度 + 显存 + 执行链 + 资源配置一起对齐。</strong></p></blockquote><p>每个参数都是这个系统的一个调节旋钮，调节某个旋钮必然在另一个方向上有代价。</p><p>几条实用判断依据：</p><ul><li>看到频繁 preemption → 先查显存侧，再查并发侧，最后才考虑模型并行</li><li>GPU 利用率偏低但吞吐不高 → 先排查 CPU 调度链是否成为瓶颈</li><li>ITL 差但吞吐还行 → 大概率是 prefill 在抢占 decode 的预算，减小 <code>max_num_batched_tokens</code> 或限制长 prefill 并发</li><li>TTFT 差但 ITL 正常 → prefill 推进慢，可以适当提高单轮 token 预算</li></ul><p>最后一点：preemption 本身不代表系统设计失败，它是高压场景下的优雅退化机制。但频繁 preemption 是一个信号，说明系统正在显存压力下吃力维持，需要找到并消除那个压力来源。</p><hr><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/f61cc956/">vLLM系统拆解-07-Worker执行链：调度决策如何变成GPU上的一步计算</a></li><li>下一篇：<a href="/posts/a1df357b/">vLLM系统拆解-09-高级加速：Speculative Decoding、CUDA Graph与量化</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-08-xing-neng-you-hua-can-shu-bei-hou-de-xi-tong-ji-quan-heng&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="TTFT" scheme="https://smarter.xin/tags/ttft/"/>
    
    <category term="ITL" scheme="https://smarter.xin/tags/itl/"/>
    
    <category term="部署调优" scheme="https://smarter.xin/tags/deployment-tuning/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-07-Worker执行链：调度决策如何变成GPU上的一步计算</title>
    <link href="https://smarter.xin/posts/f61cc956/"/>
    <id>https://smarter.xin/posts/f61cc956/</id>
    <published>2026-04-02T03:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-07-worker-zhi-xing-lian-diao-du-jue-ce-ru-he-bian-cheng-gpu-shang-de-yi-bu-ji-suan" tabindex="-1">vLLM系统拆解-07-Worker执行链：调度决策如何变成GPU上的一步计算</h1><p>前几篇讲的都是 Scheduler 侧的事：哪些请求本轮进入 batch，token budget 怎么分配，prefix cache 怎么命中。但到这里，Scheduler 只是完成了&quot;决策&quot;——GPU 还没有真正动起来。</p><p>把&quot;这一轮调度谁&quot;翻译成&quot;GPU 实际执行一步 forward&quot;的，是 Worker 执行链。这篇文章沿着这条链从头走一遍：为什么要有单独的 Worker 进程，Worker / Model Runner / Model / Sampler 四层各自解决什么问题，以及那些常被忽视但实际很重要的工程细节——input tensor 准备、persistent batch、Sampler 的完整执行步骤。</p><hr><h2 id="si-ceng-fen-gong-xian-jian-li-zheng-ti-shi-tu" tabindex="-1" id="四层分工：先建立整体视图">四层分工：先建立整体视图</h2><p>在深入每一层之前，先把层次关系建立清楚。</p><pre><code class="highlight mermaid">flowchart TD    A[&quot;Engine Core\n调度协调，维护全局请求状态&quot;] --&gt; B[&quot;Worker\n单 GPU 进程，设备归属与显存管理&quot;]    B --&gt; C[&quot;Model Runner\n执行编排：状态更新 + 输入准备 + 调用协调&quot;]    C --&gt; D[&quot;Model\ntorch.nn.Module，执行网络计算，输出 logits&quot;]    C --&gt; E[&quot;Sampler\n解码决策：从 logits 到最终 token 选择&quot;]    E --&gt; F[&quot;写回请求状态\n未结束→进入下一轮 decode\n已结束→释放资源&quot;]    F --&gt; A</code></pre><p>官方架构文档对 Worker 的定位非常直接：每个 GPU 对应一个 Worker 进程，Worker 负责加载模型权重、执行 forward、管理 GPU 内存。但这四层的划分背后有明确的设计原因，不只是简单的代码分组。</p><hr><h2 id="wei-shi-yao-xu-yao-dan-du-de-worker-jin-cheng" tabindex="-1" id="为什么需要单独的-Worker-进程">为什么需要单独的 Worker 进程</h2><p>这个设计选择直接决定了 vLLM 在多 GPU 和分布式场景下的可扩展性。</p><p><strong>设备归属清晰。</strong> 哪张卡由谁管、这张卡上的显存由谁分配、这张卡加载了什么权重、当前 batch 是什么——这些都归一个 Worker 管。如果多个上层组件同时直接操作 GPU，责任边界会混乱，显存生命周期难以追踪，多卡通信逻辑会散落在各处。</p><p><strong>更适合分布式扩展。</strong> 当引入 Tensor Parallelism（TP）、Pipeline Parallelism（PP）、Data Parallelism（DP）后，不同 rank 有不同的张量分片和计算职责。把&quot;每个设备的执行与状态&quot;收口到 Worker，可以让 Engine Core 只关心<strong>调度与协调</strong>，不必亲自管每张卡的细节。这是多 GPU 系统能够保持可维护性的基础。</p><p><strong>分离 CPU 控制路径与 GPU 执行路径。</strong> 高并发推理里，一个常见瓶颈是 CPU 端做了太多杂事而 GPU 被饿住。API server 处理请求、Engine Core 处理调度、Worker 处理模型执行——这三路分开后，不同性质的瓶颈更容易被隔离，也更容易各自优化。</p><hr><h2 id="model-runner-cai-shi-zhi-xing-lian-de-he-xin-bu-shi-model-ben-shen" tabindex="-1" id="Model-Runner-才是执行链的核心，不是-Model-本身">Model Runner 才是执行链的核心，不是 Model 本身</h2><p>这个判断对很多人来说是反直觉的。</p><p>模型本体（torch.nn.Module）只做一件事：给定输入张量，算出输出张量。它不知道外面在跑多少并发请求，不知道哪些是 prefill 哪些是 decode，不知道 KV cache 怎么索引，不知道当前 batch 是怎么组成的。</p><p>但一个推理系统真正要处理的远比这复杂：</p><ul><li>这一轮 batch 里有哪些请求，各自当前在哪一步</li><li>哪些是 prefill，哪些是 decode</li><li>本轮输入 token 怎么组织成张量</li><li>attention backend 要什么格式的 metadata</li><li>KV cache 的 block table / slot mapping 如何表达</li><li>是否可以复用 persistent state，是否启用 cudagraph</li><li>Sampler 需要哪些 request-level metadata</li></ul><p>这些都不是 torch.nn.Module 该处理的。<strong>Model Runner 就是连接&quot;调度世界&quot;和&quot;模型世界&quot;的翻译层</strong>——Scheduler 输出的是&quot;这一轮该处理哪些请求&quot;，Model 需要的是&quot;适合 kernel / backend / module 的张量输入&quot;，Model Runner 负责把前者翻译成后者。</p><h3 id="model-runner-bi-xu-bao-chi-tong-yong-he-zui-xiao-hua" tabindex="-1" id="Model-Runner-必须保持通用和最小化">Model Runner 必须保持通用和最小化</h3><p>官方 API 文档对 Model Runner 有一个明确要求：这个文件是所有模型共享的，它既服务文本模型也服务多模态模型，既服务 generative 也服务 embedding。因此这里<strong>只放所有模型都通用的逻辑</strong>，模型特定行为应放到各自的模型文件里。</p><p>这个约束有清晰的工程价值：</p><table><thead><tr><th>如果 Model Runner 保持稳定</th><th>如果 Model Runner 被模型差异污染</th></tr></thead><tbody><tr><td>新增模型只需补充模型文件</td><td>每次加模型都要改执行主干</td></tr><tr><td>persistent batch、cudagraph 等优化可以稳定落点</td><td>框架级优化难以实施</td></tr><tr><td>主执行路径行为可预测</td><td>系统随时间趋向失控</td></tr></tbody></table><p>这是一种很典型的 infra 克制：不是&quot;功能能不能加&quot;，而是&quot;加完后主路径会不会变成垃圾场&quot;。</p><hr><h2 id="zhi-xing-lian-de-wan-zheng-liu-cheng" tabindex="-1" id="执行链的完整流程">执行链的完整流程</h2><p>下面这条链是这篇文章的核心，逐步讲清楚每一步的实质。</p><pre><code class="highlight mermaid">sequenceDiagram    participant EC as Engine Core    participant W as Worker    participant MR as Model Runner    participant M as Model    participant S as Sampler    EC-&gt;&gt;W: 本轮调度结果\n（谁执行/谁继续/谁结束）    W-&gt;&gt;MR: 执行本轮 batch    MR-&gt;&gt;MR: 更新 active requests 状态\n同步 persistent state    MR-&gt;&gt;MR: 准备输入张量\n（input_ids / positions / attention metadata\nKV 索引 / block table / sampler metadata）    MR-&gt;&gt;M: model.forward(prepared inputs)    M--&gt;&gt;MR: logits    MR-&gt;&gt;S: logits + request metadata    S--&gt;&gt;MR: SamplerOutput（next tokens + logprobs）    MR-&gt;&gt;MR: 写回请求状态\n判断 EOS / max_tokens\n更新 KV cache 引用    MR--&gt;&gt;EC: 本轮结果\n已结束请求通知释放资源</code></pre><h3 id="di-yi-bu-jie-shou-scheduler-de-zhi-xing-ji-hua" tabindex="-1" id="第一步：接收-Scheduler-的执行计划">第一步：接收 Scheduler 的执行计划</h3><p>Scheduler 做完本轮选择后，把&quot;本轮执行计划&quot;交给 Worker，而不是自己去跑模型。</p><p>这是<strong>控制面（control plane）与执行面（execution plane）的分离</strong>。Engine Core 负责决定哪些请求执行、它们的 KV block 如何分配、哪些请求结束或被抢占；Worker 只负责按照这份计划执行。</p><p>如果让 Worker 自己也做调度判断，每个 Worker 都需要更多全局视角，多卡系统会更复杂。vLLM 的设计是：调度统一收口到 Engine Core，执行统一落到 Worker，让全局决策点更集中，系统行为更可预测。</p><h3 id="di-er-bu-model-runner-geng-xin-zhi-xing-zhuang-tai" tabindex="-1" id="第二步：Model-Runner-更新执行状态">第二步：Model Runner 更新执行状态</h3><p>Model Runner 接到本轮调度结果后，第一件事不是立刻 forward，而是<strong>更新执行状态</strong>。</p><p>推理不是一次性静态 batch，而是持续变化的动态 batch：有些请求刚加入，有些刚结束，有些在继续 decode，有些被 preempt 后又恢复。所以 Model Runner 要先回答：</p><ul><li>当前 active requests 是谁，在本轮 batch 里的顺序是什么</li><li>persistent state 里对应哪一行需要更新</li><li>sampler metadata 是否需要同步</li></ul><p>这一步的作用是把 Scheduler 的离散调度事件，转换成模型执行所需的连续运行状态。</p><h3 id="di-san-bu-zhun-bei-shu-ru-zhang-liang-zui-rong-yi-bei-di-gu-de-yi-bu" tabindex="-1" id="第三步：准备输入张量——最容易被低估的一步">第三步：准备输入张量——最容易被低估的一步</h3><p>很多人会以为&quot;输入不就是 token ids 吗，直接喂给 model 不就行了&quot;。实际上这是执行链里一大块工程工作。真正要准备的包括：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">input_ids               - 本轮实际输入的 token id</span><br><span class="line">positions/position_ids  - 每个 token 在序列中的位置</span><br><span class="line">attention metadata      - attention backend 所需的格式信息</span><br><span class="line">KV cache 索引           - 当前请求的 KV 存储位置</span><br><span class="line">block table             - 逻辑 block → 物理 block 的映射</span><br><span class="line">slot mapping            - 具体的 block slot 分配</span><br><span class="line">请求长度信息            - 各请求当前的 prefill/decode 状态</span><br><span class="line">sampler metadata        - 各请求的采样参数（temperature、top-k 等）</span><br><span class="line">多模态特征（如有）      - 图像/音频等输入的特征表示</span><br><span class="line">backend 特定布局        - 不同 attention backend 的 layout 要求</span><br></pre></td></tr></table></figure><p>这些内容大多不属于&quot;模型数学定义&quot;，而属于&quot;执行系统如何把动态请求集合包装成一次可计算的 forward&quot;。很多性能问题根本不出在 matmul 上，而出在：每步重建大量 CPU 端状态、频繁的张量重排、大量 host-to-device 小拷贝、batch 变化破坏执行稳定性。</p><p>这也是 Model Runner 的核心价值之一：让&quot;输入准备&quot;更统一、更少冗余、更接近后端需求。</p><h3 id="di-si-bu-model-forward" tabindex="-1" id="第四步：model-forward">第四步：model.forward</h3><p>输入准备完成后，才真正进入模型计算。Model 就是标准的 torch.nn.Module：Embedding → Transformer blocks（Attention + MLP + Norm）→ 输出 logits。</p><p>Model 不应该承担系统逻辑，原因是<strong>模型是高变的</strong>：不同模型家族的 attention 结构、RoPE 处理、KV layout 细节、多模态支持、任务目标都不同。如果把系统逻辑塞进 model，执行框架就会因模型差异而碎片化。vLLM 的分工是：Model Runner 负责&quot;怎样喂&quot;，Model 负责&quot;怎么算&quot;。</p><h3 id="prefill-yu-decode-zai-worker-ce-de-chai-yi" tabindex="-1" id="Prefill-与-Decode-在-Worker-侧的差异">Prefill 与 Decode 在 Worker 侧的差异</h3><p>这两种模式对 Worker 的要求差异很大：</p><table><thead><tr><th></th><th>Prefill</th><th>Decode</th></tr></thead><tbody><tr><td>输入规模</td><td>整段 prompt，可能很多 token</td><td>通常每请求 1 个新 token</td></tr><tr><td>计算特性</td><td>更偏 compute-bound（大矩阵乘法）</td><td>更偏 memory-bound（频繁读 KV Cache）</td></tr><tr><td>KV Cache 操作</td><td>写入整段历史的 K/V</td><td>读取长历史 + 写入当前步的 K/V</td></tr><tr><td>forward 输出</td><td>最后位置的 logits，用于采样第一个生成 token</td><td>当前位置的 logits，用于采样下一个 token</td></tr></tbody></table><p>Model Runner 的重要职责之一就是把这两种模式的差异吸收进统一执行框架中——对 Scheduler 来说是&quot;混合 batch&quot;，对 Model 来说是&quot;一次 forward&quot;，中间的转换在 Model Runner 里完成。</p><h3 id="di-wu-bu-sampler-bu-shi-mo-wei-sui-shou-xie-ji-xing-softmax" tabindex="-1" id="第五步：Sampler——不是末尾随手写几行-softmax">第五步：Sampler——不是末尾随手写几行 softmax</h3><p>model.forward 输出 logits，只是&quot;下一 token 的分布依据&quot;，不是最终决策。从 logits 到实际 token 选择，是 Sampler 的工作。</p><p>根据 vLLM API 文档，Sampler 处理一次 next token 的完整步骤是：</p><ol><li>如果用户请求 logprobs，先按模式准备要返回的 logprobs 信息</li><li>把 logits 转成 float32</li><li>应用 allowed token ids 白名单</li><li>应用 bad words exclusion</li><li>应用会影响 greedy 结果的 logit processors（最小生成长度、logit bias 等）</li><li>应用 penalties：repetition / frequency / presence penalty</li><li>进入实际采样：<ul><li>greedy 模式：直接取 argmax</li><li>非 greedy：先做 temperature scaling → 应用 argmax-invariant processors（如默认的 min_p）→ top-k / top-p → 从概率分布中采样</li></ul></li><li>如有需要，收集 top logprobs 和 sampled token 的 logprobs</li><li>返回 <code>SamplerOutput</code></li></ol><p>Sampler 实际承担了三个层面的职责：<strong>策略控制</strong>（怎么选 token）、<strong>约束控制</strong>（哪些 token 可选、哪些词禁止）、<strong>输出组织</strong>（当用户要求 logprobs 等附加信息时整理结果）。</p><p>把 Sampler 从 Model 里拆出来的核心理由是：采样是解码策略，不是模型数学。“预测”（模型输出 logits）和&quot;决策&quot;（选哪个 token）是两个层次的问题，混在一起会让模型实现承担不该承担的逻辑。</p><h3 id="di-liu-bu-xie-hui-qing-qiu-zhuang-tai" tabindex="-1" id="第六步：写回请求状态">第六步：写回请求状态</h3><p>采样完成后执行链还没结束，还要：</p><ul><li>把 sampled token 追加到对应请求的生成结果</li><li>更新请求长度</li><li>判断是否命中 EOS / stop 条件，或达到 <code>max_tokens</code></li><li>更新 sampler / request 的相关状态</li><li>已结束的请求：通知上层释放其 KV blocks，从 active set 移除</li><li>未结束的请求：保留在 active set，下一轮 decode 时只输入新 token，但仍需读完整历史 KV Cache</li></ul><p>这一步的本质是：<strong>把&quot;本轮 forward 的结果&quot;转换成&quot;下一轮调度所需的请求状态&quot;</strong>。这和 KV Cache 生命周期、Scheduler 状态机是强相关的——请求结束意味着 block 释放，请求继续意味着 KV 引用仍然有效。</p><h3 id="di-qi-bu-shu-chu-hui-chuan-gei-shang-ceng" tabindex="-1" id="第七步：输出回传给上层">第七步：输出回传给上层</h3><p>Worker 不直接提供 HTTP 服务。本轮执行结果会先回到 Engine Core，再由 API 层做输出组织和流式返回。</p><p>这一步也体现职责分离：Worker 只关心模型执行和设备状态，API 层关心用户可见的输出格式和流式协议（包括 HTTP 连接管理、request bookkeeping、OpenAI-compatible 格式）。如果混在一起，设备进程会承担太多非计算逻辑，性能隔离会变差。</p><hr><h2 id="persistent-batch-cpu-kai-xiao-ye-shi-tui-li-ping-jing" tabindex="-1" id="Persistent-Batch：CPU-开销也是推理瓶颈">Persistent Batch：CPU 开销也是推理瓶颈</h2><p>连续 decode 的相邻两步，batch 通常变化不大：上一步 batch 里有 N 个请求，下一步往往还是那 N 个，只有少数结束、少数加入，大多数请求只是&quot;继续生成下一个 token&quot;。</p><p>如果每一轮都把整个 batch 的所有输入张量从头构造一遍，会产生明显的 CPU 开销。vLLM V1 中的 Model Runner V2 使用了 persistent batch 思路：持久保存 batch 相关状态张量，每轮只做增量更新，利用&quot;相邻 step batch 大体相同&quot;这一事实来减少 CPU 侧 bookkeeping 成本（官方 Model Runner V2 设计文档明确指出这一点）。</p><p>这说明 vLLM 的优化不只在 GPU kernel 层，它同样重视 host 端执行开销。这也是 Model Runner 必须保持稳定的另一个原因：persistent batch 这类优化需要一个不随模型差异而变化的稳定执行框架来落脚。</p><hr><h2 id="zhe-tiao-zhi-xing-lian-zhui-qiu-de-si-ge-mu-biao" tabindex="-1" id="这条执行链追求的四个目标">这条执行链追求的四个目标</h2><p>理解了每一步之后，可以把这条链的设计意图归结为四点：</p><p><strong>连接动态服务世界与静态模型世界。</strong> 服务世界是动态的（请求随时来去，batch 持续变化），模型世界需要规整张量和稳定执行路径。Model Runner 是两者之间的桥。</p><p><strong>让高频执行路径尽量轻。</strong> Decode 是一步步不断重复的，每步多浪费一点 CPU 开销，累积起来代价很高。persistent state、最小化输入准备、通用执行路径稳定——这些都是为了让这条高频路径尽可能少做无用功。</p><p><strong>支持大量模型而不让主干失控。</strong> Worker 作为设备层，Model Runner 作为通用执行层，Model 作为模型定义层，Sampler 作为解码决策层——这个分层是为&quot;多模型、多任务、长期演化&quot;做的准备，不是过度设计。</p><p><strong>为高性能优化留下稳定的落脚点。</strong> cudagraph、persistent batch、未来的 sampler backend 优化——这些都依赖执行链边界清晰、主路径足够稳定。如果一开始不分层，后面这些优化的落脚点就不存在了。</p><hr><h2 id="yuan-ma-dui-ying-wei-zhi" tabindex="-1" id="源码对应位置">源码对应位置</h2><p>对应这条执行链的主要源码：</p><table><thead><tr><th>路径</th><th>职责</th></tr></thead><tbody><tr><td><code>vllm/v1/worker/gpu_worker.py</code></td><td>设备级 Worker 进程逻辑</td></tr><tr><td><code>vllm/v1/executor/multiproc_executor.py</code></td><td>多进程执行器，连接 Engine Core 和多个 Worker</td></tr><tr><td><code>vllm/v1/worker/gpu/model_runner.py</code></td><td>Model Runner 主执行逻辑（输入准备、编排、采样器衔接）</td></tr><tr><td><code>vllm/v1/sample/sampler.py</code></td><td>Sampler 主逻辑</td></tr><tr><td><code>vllm/engine/llm_engine.py</code></td><td>更上层的 Engine 入口与整体流程</td></tr></tbody></table><hr><h2 id="jie-yu" tabindex="-1" id="结语">结语</h2><p>Worker 执行链的本质不是&quot;把模型跑一下&quot;，而是<strong>把动态请求集合稳定、高效地翻译成 GPU 可执行的一步计算，再把结果正确映射回请求状态</strong>。</p><p>这条链的工程价值主要体现在三个地方：Model Runner 作为翻译层吸收了调度世界和模型世界之间的阻抗差；input tensor 准备这一步决定了高频路径上的 CPU 开销是否可控；Sampler 的独立存在让解码策略可以在不改动模型的情况下灵活演化。</p><p>这套分层设计的局限在于：层次越多，调试时跨层追踪问题越麻烦；Model Runner 的&quot;通用最小化&quot;要求需要持续约束才能维持，随着功能增加这个约束会承受压力。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/be098d92/">vLLM系统拆解-06-Prefix Caching：跨请求KV复用如何做到精确、高效且安全</a></li><li>下一篇：<a href="/posts/b0da0eba/">vLLM系统拆解-08-性能优化：参数背后的系统级权衡</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-07-worker-zhi-xing-lian-diao-du-jue-ce-ru-he-bian-cheng-gpu-shang-de-yi-bu-ji-suan&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="模型服务" scheme="https://smarter.xin/tags/model-serving/"/>
    
    <category term="调用链" scheme="https://smarter.xin/tags/call-chain/"/>
    
    <category term="Worker" scheme="https://smarter.xin/tags/worker/"/>
    
    <category term="推理执行链" scheme="https://smarter.xin/tags/inference-execution-chain/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-06-Prefix Caching：跨请求KV复用如何做到精确、高效且安全</title>
    <link href="https://smarter.xin/posts/be098d92/"/>
    <id>https://smarter.xin/posts/be098d92/</id>
    <published>2026-04-02T03:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-06-prefix-caching-kua-qing-qiu-kv-fu-yong-ru-he-zuo-dao-jing-que-gao-xiao-qie-an-quan" tabindex="-1">vLLM系统拆解-06-Prefix Caching：跨请求KV复用如何做到精确、高效且安全</h1><p>这篇文章要搞清楚的，不是&quot;vLLM 支持 prefix cache&quot;这句功能描述，而是它为什么要这样设计：为什么按 block 做而不是按整段 prompt 做，为什么命中条件必须精确到 token 级，为什么只缓存 full block，block hash 里的 parent hash 和 extra keys 分别解决什么问题，以及 cache salt 这个看起来和性能无关的东西为什么必须存在。</p><p>这篇文章的前置知识是 <a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a>：Prefix Caching 建立在 block/page 化 KV Cache 之上，理解 block 化设计是读这篇文章的基础。</p><hr><h2 id="pu-tong-kv-cache-de-bian-jie-zai-na-li" tabindex="-1" id="普通-KV-Cache-的边界在哪里">普通 KV Cache 的边界在哪里</h2><p><a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a> 讲的 KV Cache，解决的是<strong>单请求内部</strong>的重复计算问题：一个请求在 decode 阶段，每一步不需要重新算历史 token 的 K/V，因为之前的结果已经缓存下来了。</p><p>但如果来了另一个请求，它的前 128 个 token 和上一个请求的前 128 个 token 完全一样——普通 KV Cache 不会帮它复用那 128 个 token 的 K/V，它仍然要重新走一遍 prefill。</p><p>这就是普通 KV Cache 的边界：<strong>它只避免&quot;一个请求重复算自己的历史&quot;，但不能自动避免&quot;多个请求重复算同样的前缀&quot;。</strong></p><p>Prefix Caching 解决的正是这个问题：把已经算好的前缀 KV blocks 缓存起来，让后续有相同前缀的请求直接复用，跳过这部分 prefill。</p><hr><h2 id="na-xie-chang-jing-hui-chan-sheng-da-liang-kua-qing-qiu-qian-zhui-zhong-fu" tabindex="-1" id="哪些场景会产生大量跨请求前缀重复">哪些场景会产生大量跨请求前缀重复</h2><p>三类场景覆盖了大多数在线服务的情况：</p><p><strong>固定 system prompt。</strong> 企业服务通常会把角色定义、风格要求、安全规则、工具调用规范、输出格式模板这些内容固定在所有请求前面。这段内容可能很长，但每个请求都要重新 prefill 一遍。</p><p><strong>RAG 固定模板。</strong> 问答系统在用户问题前通常会拼接 instruction、引用规则、输出格式、工具规则等模板内容。这些模板高度重复。</p><p><strong>多轮对话公共前缀。</strong> 多轮对话里，前几轮的消息可能完全相同，只是后面追加了一点新内容。前面那一大段历史完全适合复用。</p><p>这几类场景的共同特征是：<strong>大量请求并不是从零开始的，它们有长度不等的共同前缀</strong>。Prefix Caching 的收益就来自这里。</p><hr><h2 id="wei-shi-yao-an-block-huan-cun-er-bu-shi-an-zheng-duan-prompt-huan-cun" tabindex="-1" id="为什么按-block-缓存，而不是按整段-prompt-缓存">为什么按 block 缓存，而不是按整段 prompt 缓存</h2><p>这是 Prefix Caching 设计里最关键的一个决策，理解它需要从三个角度来看。</p><p><strong>一、粒度一致性。</strong> vLLM 的 KV Cache 本来就是 block/page 化管理的。如果 Prefix Caching 选择以整段 prompt 为单位做缓存，就需要在现有 block 管理体系之外再定义一套&quot;大对象缓存&quot;，然后命中后还要把这个大对象重新拆回 block，才能接入后续计算。多一层转换就多一层复杂度。按 block 缓存，则和现有物理管理粒度天然一致。</p><p><strong>二、部分命中是常态。</strong> 现实里两个请求往往是&quot;前 10 个 block 相同，第 11 个 block 开始不同&quot;。如果按整段 prompt 缓存，命中逻辑就很僵硬：要么全段一样，要么不命中。按 block 链缓存，则可以做到：前 10 个 block 命中，后续从第 11 个 block 继续 prefill。这才是前缀复用真正该有的形态。</p><p><strong>三、生命周期管理更容易。</strong> Block 粒度的缓存对象更适合做引用计数、LRU 驱逐、部分共享。一整段 prompt 对应的大对象，生命周期管理要复杂得多。</p><hr><h2 id="ming-zhong-tiao-jian-yan-ge-token-ji-yi-zhi-bu-shi-yu-yi-xiang-si" tabindex="-1" id="命中条件：严格-token-级一致，不是语义相似">命中条件：严格 token 级一致，不是语义相似</h2><p>Prefix Caching 最容易产生的误解是：同一个 system prompt 只要&quot;看起来差不多&quot;就能命中。这不对。</p><p><strong>命中条件是：前缀 token 序列精确一致。</strong></p><p>原因很直接：KV Cache 是模型对具体 token 序列计算出的中间状态。一个 token 不同，后续所有 attention 的结果就可能不同。所以判断能否复用，必须是精确一致，不能是模糊匹配或语义近似。</p><p>这也是为什么 Prefix Caching 最终要落到 block hash，而不是字符串近似判断。</p><hr><h2 id="block-hash-she-ji-wei-shi-yao-xu-yao-parent-hash-he-extra-keys" tabindex="-1" id="Block-Hash-设计：为什么需要-parent-hash-和-extra-keys">Block Hash 设计：为什么需要 parent hash 和 extra keys</h2><p>这部分是 Prefix Caching 实现里最容易被忽视的细节，也是让它真正&quot;正确&quot;的关键。</p><p>根据 vLLM 设计文档和 <code>kv_cache_utils</code> 的说明，每个 full block 的 hash 由以下内容计算得出：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">block_hash = hash(</span><br><span class="line">    parent_block_hash,      # 前一个 block 的 hash</span><br><span class="line">    curr_block_token_ids,   # 当前 block 的 token 序列</span><br><span class="line">    extra_keys              # LoRA ID、多模态输入 hash、cache salt 等</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p><strong>为什么需要 parent_block_hash？</strong></p><p>考虑这个例子（block size = 4）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">请求 A：[a b c d]  [e f g h]</span><br><span class="line">            block 0     block 1</span><br><span class="line"></span><br><span class="line">请求 B：[x y z w]  [e f g h]</span><br><span class="line">            block 0     block 1</span><br></pre></td></tr></table></figure><p>两个请求的第二个 block token 完全相同，都是 <code>[e f g h]</code>。但它们的 prefix 完全不同，所以第二个 block 对应的 KV 也不同，不能复用。</p><p>如果只用当前块的 token 计算 hash，就会误判为可以复用。加入 parent_block_hash 后：</p><ul><li>A 的 block 1 hash 依赖 A 的 block 0 hash（由 <code>[a b c d]</code> 决定）</li><li>B 的 block 1 hash 依赖 B 的 block 0 hash（由 <code>[x y z w]</code> 决定）</li></ul><p>即使当前块 token 一样，最终 hash 也不同。这保证了<strong>前缀链级别的正确性</strong>。</p><p>一个 block 的 hash，隐含了&quot;从起点到当前块&quot;为止的完整前缀信息。</p><p><strong>为什么需要 extra_keys？</strong></p><p>即使 token 序列完全一样，底层推理条件不同时 KV Cache 也不能共享：</p><ul><li>使用不同 LoRA adapter：不同 adapter 的 K/V projection 权重不同，对同样 token 序列计算出的 KV 也不同</li><li>使用不同多模态输入：影响 attention 计算的上下文发生变化</li><li>使用不同 cache salt：用于隔离不同信任域（后面专门讲）</li></ul><p>把这些额外信息纳入 hash，确保命中条件不只是&quot;token 相同&quot;，而是&quot;同一前缀链、同一块 token、同一组影响推理语义的额外条件&quot;。</p><hr><h2 id="wei-shi-yao-zhi-huan-cun-full-block" tabindex="-1" id="为什么只缓存-Full-Block">为什么只缓存 Full Block</h2><p>这是 Prefix Caching 里一个设计决策，背后有四个清晰的工程理由。</p><p><strong>内容和边界都稳定。</strong> 一个还没填满的 block 仍然在接收新 token，它的内容还没有最终确定。既然内容会变，hash 就不稳定，缓存身份也不稳定。只有 full block 的内容固定、边界固定、hash 固定，才适合作为可复用缓存对象。</p><p><strong>避免频繁元数据更新。</strong> 如果半满 block 也参与缓存，那每追加一个 token，hash 就变一次，之前的缓存身份要不要作废、旧 hash 如何驱逐、多个请求共享时如何处理&quot;正在变动的尾块&quot;——这些问题会让缓存管理复杂度大幅上升。Full block 策略把这个问题直接切掉：尾块在未满前只当作普通的 request-owned block，满了之后才&quot;晋升&quot;为可缓存 block。</p><p><strong>与 block/page 化管理天然一致。</strong> vLLM 本来就以 page 为物理管理单位，一个完整 page 才更像稳定的物理单元。</p><p><strong>工程权衡的最优点。</strong> 理论上支持 partial block 缓存能多命中一点尾部前缀，但工程上实现更复杂、元数据更新更频繁、一致性更难维护，而收益并不大。牺牲一点理论最优命中率，换来实现简单、行为稳定、维护成本低——这是很典型的 infra 权衡逻辑。</p><hr><h2 id="prefix-caching-de-shu-ju-jie-gou" tabindex="-1" id="Prefix-Caching-的数据结构">Prefix Caching 的数据结构</h2><p>Prefix Caching 实现在 KV Cache Manager 里（路径：<code>vllm/v1/core/kv_cache_manager.py</code>），初始化时建立以下几个核心组件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">KV Cache Manager 内部结构：</span><br><span class="line"></span><br><span class="line">┌─────────────────────────────────────────────────────┐</span><br><span class="line">│                   Block Pool                        │</span><br><span class="line">│  所有 KVCacheBlock 对象在启动时一次性预分配           │</span><br><span class="line">│  避免推理过程中频繁创建 Python 对象                   │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│              Free Block Queue（双向链表）             │</span><br><span class="line">│  block 内部直接带链表指针，O(1) 移动中间元素           │</span><br><span class="line">│  用于 LRU 管理和空闲 block 回收                       │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│           Cache Blocks Map                          │</span><br><span class="line">│  block_hash → block_id                              │</span><br><span class="line">│  新请求按前缀逐块查询这张表                            │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│           Request Blocks Map                        │</span><br><span class="line">│  request_id → allocated block ids                   │</span><br><span class="line">│  每个请求当前持有哪些 block                           │</span><br><span class="line">├─────────────────────────────────────────────────────┤</span><br><span class="line">│                 ref_cnt（每个 block 持有）            │</span><br><span class="line">│  多个请求可共享同一批 immutable full block            │</span><br><span class="line">│  ref_cnt = 0 且不在活跃缓存中才可回收                 │</span><br><span class="line">└─────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>Block Pool 的设计体现了高性能系统里常见的对象池思路：<strong>高频路径上的对象管理尽量静态化、池化</strong>，避免运行时频繁创建和销毁 Python 对象的 GC 压力。Free Block Queue 的双向链表则让 LRU 的&quot;移动到尾部&quot;操作为 O(1)，不需要额外的包装容器。</p><p>ref_cnt 是共享 block 的关键机制：Prefix Caching 不是给每个请求复制一份 KV，而是让多个请求共同引用同一批 immutable full block。只有当没有请求再引用某个 block 时，它才能返回 free queue。</p><hr><h2 id="xin-qing-qiu-dao-lai-shi-de-wan-zheng-chu-li-liu-cheng" tabindex="-1" id="新请求到来时的完整处理流程">新请求到来时的完整处理流程</h2><pre><code class="highlight mermaid">flowchart TD    A[&quot;新请求进入 Scheduler&quot;] --&gt; B[&quot;get_computed_blocks()\n按 block 切分 prompt\n逐块计算 hash，查 Cache Blocks Map&quot;]    B --&gt; C&#123;&quot;是否有从头开始的\n连续 block 命中？&quot;&#125;    C -- &quot;有命中&quot; --&gt; D[&quot;命中的 full blocks\n直接作为 computed prefix\n跳过这部分 prefill&quot;]    C -- &quot;无命中&quot; --&gt; E[&quot;从头分配新 blocks&quot;]    D --&gt; F[&quot;allocate_slots()\n只为未命中部分分配新 blocks&quot;]    E --&gt; F    F --&gt; G[&quot;执行 prefill\n写入未命中的 block&quot;]    G --&gt; H&#123;&quot;某个新 block 写满？&quot;&#125;    H -- &quot;是&quot; --&gt; I[&quot;计算稳定 hash\n写入 Cache Blocks Map\n等待下次命中&quot;]    H -- &quot;否（尾块未满）&quot; --&gt; J[&quot;普通 request-owned block\n不进入 prefix cache&quot;]    I --&gt; K[&quot;请求继续 decode&quot;]    J --&gt; K    K --&gt; L[&quot;请求结束\n所有引用 block 的 ref_cnt--\nref_cnt=0 的 block 进入 LRU 候选&quot;]</code></pre><p>有一个细节值得注意：<strong>命中必须从前缀起点开始连续</strong>，不能跳着命中。因为 parent_block_hash 把前缀链编码进了每个 block 的 hash，中间断掉了就意味着后续的 hash 也全部不同，无法复用。</p><hr><h2 id="wei-shi-yao-prefix-caching-fang-zai-kv-cache-manager-li" tabindex="-1" id="为什么-Prefix-Caching-放在-KV-Cache-Manager-里">为什么 Prefix Caching 放在 KV Cache Manager 里</h2><p>初看起来，Prefix Caching 可以单独做成一个&quot;prompt cache service&quot;。vLLM 没这么做，背后有清晰的理由。</p><p><strong>缓存对象本身就是 KV blocks。</strong> Prefix Caching 缓存的不是文本字符串、不是 logits，而是 KV block 本身。既然缓存对象就是 KV block，最自然的归属地就是管理 KV block 的模块。</p><p><strong>block 的生命周期必须统一管理。</strong> Prefix cache 命中后，请求引用了已有 block，这些 block 仍然要参与 ref_cnt 管理、free queue 管理、驱逐策略、request 映射。如果把 Prefix Caching 单独拆出去，就会有两套状态需要同步：一套在缓存模块，一套在 KV manager。两套状态之间的一致性维护是额外成本。</p><p><strong>Scheduler 需要的接口可以统一。</strong> Scheduler 不关心&quot;缓存系统有多高级&quot;，它只需要知道：这个请求的前缀有哪些 block 已经计算好了，还需要分配多少新 block。把 Prefix Caching 放在 KV Cache Manager 里，这些问题通过同一个接口体系解决，scheduler 不需要感知两个模块。</p><p>这是一种典型的系统设计思路：<strong>让资源的所有权与生命周期管理尽量在同一层闭环</strong>，不要人为拆成多个需要相互同步的模块。</p><hr><h2 id="wei-shi-yao-yong-hash-lookup-er-bu-shi-trie" tabindex="-1" id="为什么用-Hash-Lookup-而不是-Trie">为什么用 Hash Lookup 而不是 Trie</h2><p>看到&quot;前缀复用&quot;，很容易联想到用 trie 来组织——它天然适合前缀共享和前缀检索。但 vLLM 当前设计以 block hash 和 Cache Blocks Map 为主，而不是显式的 trie。</p><p><strong>物理管理单位是 block，不是 token。</strong> 如果用 trie，更自然的倾向是按 token 组织前缀树。但这和 KV Cache 的物理粒度（block）不一致，命中之后还要再从 token 节点映射到对应的 block，增加了一层转换。</p><p><strong>Hash lookup 更直接，更符合缓存的使用模式。</strong> Prefix Caching 的核心需求是：快速判断是否有可复用 block，命中后直接得到 block。这比复杂的前缀索引更像一个缓存系统，hash lookup 天然适合。</p><p><strong>parent_block_hash 链已经隐含了前缀结构。</strong> 虽然没有显式 trie，但 <code>parent_block_hash + current_tokens</code> 的设计，本质上已经把&quot;前缀链&quot;编码进了 hash 关系里。它不是没有前缀结构，而是把前缀结构编码成了更轻量的 hash 链，不需要一棵显式树来维护。</p><hr><h2 id="lru-qu-zhu-zi-yuan-jin-zhang-shi-ru-he-quot-you-ya-di-wang-ji-quot" tabindex="-1" id="LRU-驱逐：资源紧张时如何-优雅地忘记">LRU 驱逐：资源紧张时如何&quot;优雅地忘记&quot;</h2><p>GPU 显存有限，缓存块不能无限保留。当 block 紧张时，系统要驱逐一些旧缓存块来给新请求腾出空间。</p><p>LRU（最近最少使用）是 vLLM 这里的驱逐策略，原因是它和 prefix cache 的使用模式非常匹配：</p><ul><li>热门前缀（比如被大量请求共享的 system prompt）会在短时间内被反复命中</li><li>冷前缀长期不被访问，留着收益低</li></ul><p>Free Block Queue 的双向链表设计正是为此服务的——每次命中时把对应 block 移到链表尾部（O(1) 操作），驱逐时从链表头部取。这不需要额外的包装容器，也不需要维护一个独立的 priority queue。</p><hr><h2 id="cache-salt-he-an-quan-xing-prefix-caching-bu-zhi-shi-xing-neng-wen-ti" tabindex="-1" id="Cache-Salt-和安全性：Prefix-Caching-不只是性能问题">Cache Salt 和安全性：Prefix Caching 不只是性能问题</h2><p>这是很多人学推理系统时会跳过、但在真实多租户部署中不得不面对的问题。</p><p><strong>风险在哪里？</strong></p><p>在共享推理环境里（比如公有云上的多租户服务），如果没有隔离机制：</p><ul><li>用户 A 先发了一个带特定 system prompt 的请求，该 prompt 的 KV blocks 被缓存</li><li>用户 B 猜测 A 可能用了这个 prompt，发一个相同前缀的请求</li><li>如果命中缓存，B 的 TTFT 会明显低于未命中时</li><li>B 通过观察响应延迟，就能推断出某个前缀是否之前被别人使用过</li></ul><p>这是一种<strong>时序侧信道攻击</strong>，可能造成隐私泄露。</p><p><strong>cache_salt 的作用：</strong></p><p>官方设计文档说明，<code>cache_salt</code> 会参与第一个 block 的 hash 计算。这意味着：</p><ul><li>即便 token 序列完全相同</li><li>只要 salt 不同，hash 就不同</li><li>cache 就不会跨 salt 共享</li></ul><p>通过给不同用户或不同信任域分配不同的 salt，可以把 prefix cache 的复用限制在&quot;同一信任组&quot;内部，而不是对所有用户开放。</p><p>这个设计的聪明之处在于：它不是把 prefix cache 整个禁掉，而是加了一层轻量、可控的隔离机制，在性能和安全之间找到了平衡点。</p><hr><h2 id="yu-scheduler-he-paged-attention-de-guan-xi" tabindex="-1" id="与-Scheduler-和-PagedAttention-的关系">与 Scheduler 和 PagedAttention 的关系</h2><p><strong>与 Scheduler 的关系</strong>：Prefix Caching 直接影响 Scheduler 每轮的工作量。KV Cache Manager 告诉 Scheduler 某个请求有哪些 block 已经计算好了，Scheduler 据此决定：这个请求本轮只需要多少 token budget（跳过已命中的部分）、哪些 blocks 还需要 prefill、剩余 budget 是否还够给其他请求。Prefix Caching 减少的不只是单请求的 prefill 成本，而是整个调度面上的 prefill 压力，让更多 budget 留给 decode 和新请求。</p><p><strong>与 PagedAttention 的关系</strong>：两者解决的是不同层次的问题。PagedAttention 解决的是&quot;KV Cache 如何以 block 形式存储，以及 attention kernel 如何在 paged layout 上高效运行&quot;——这是底层数据组织与访问机制。Prefix Caching 解决的是&quot;哪些 block 已经算过，是否可以跨请求复用&quot;——这是跨请求的资源复用机制。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PagedAttention → 提供 block 化 KV 的底座</span><br><span class="line">                         ↓</span><br><span class="line">Prefix Caching → 利用这个底座把一部分 blocks 变成跨请求可复用资源</span><br></pre></td></tr></table></figure><p>没有 block 化 KV，Prefix Caching 当然也能做，但管理会更笨重。有了 PagedAttention，按 block 复用就变得非常自然。</p><hr><h2 id="shou-yi-bian-jie-he-chang-jian-wu-qu" tabindex="-1" id="收益、边界和常见误区">收益、边界和常见误区</h2><p><strong>真实收益</strong>：减少重复 prefill 计算量；降低 TTFT（跳过前缀计算，更快进入真正的生成阶段）；提升系统吞吐（节省的 token budget 可以分给更多请求）。收益的大小取决于前缀重复率——对使用长固定 system prompt、RAG 模板或多轮对话的服务效果最显著。</p><p><strong>边界与限制</strong>：</p><table><thead><tr><th>场景</th><th>收益情况</th></tr></thead><tbody><tr><td>前缀各不相同（随机请求）</td><td>命中率低，有元数据维护成本但收益有限</td></tr><tr><td>尾块未满（最后一个不完整 block）</td><td>不进入 prefix cache，跨请求无法命中</td></tr><tr><td>Block 池紧张</td><td>缓存 block 会被 LRU 驱逐，命中率下降</td></tr><tr><td>多租户环境</td><td>若无 cache salt 隔离，存在时序侧信道风险</td></tr></tbody></table><p>几个常见误区需要澄清：</p><ul><li>Prefix Cache 缓存的是 KV blocks，<strong>不是模型的回答结果</strong></li><li>它是<strong>跨请求</strong>复用，与普通 KV Cache 的<strong>单请求内部</strong>复用不是同一件事</li><li>命中需要<strong>严格 token 级一致</strong>，语义相似不算命中</li><li>是否缓存 partial block 是工程权衡问题，不是命中率越高越好</li><li>Cache salt 的存在说明这个功能<strong>既是性能机制，也是安全机制</strong></li></ul><hr><h2 id="jie-yu" tabindex="-1" id="结语">结语</h2><p>Prefix Caching 的设计体现了 vLLM 的一个一贯思路：把看起来是&quot;高级 feature&quot;的东西，工程化地落在已有基础设施上，而不是另起炉灶。它没有单独发明一套缓存系统，而是沿用 block/page 化 KV 的粒度；它没有维护两套状态，而是把缓存逻辑收归 KV Cache Manager；它没有为了命中率而引入 partial block 缓存的复杂性，而是选择 full block 策略获得简单稳定的实现。</p><p>它的局限也很清晰：前缀重复率低时收益有限；缓存 block 本身占显存需要驱逐策略；多租户场景需要正确配置 cache salt，否则引入安全风险。</p><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="参考资料">参考资料</h2><ul><li>vLLM 官方设计文档：Automatic Prefix Caching，<a href="https://docs.vllm.ai/en/latest/design/prefix_caching/">https://docs.vllm.ai/en/latest/design/prefix_caching/</a></li><li>vLLM 源码：<code>vllm/v1/core/kv_cache_manager.py</code>，<code>vllm/v1/core/kv_cache_utils.py</code></li><li>vLLM 官方文档：Optimization and Tuning，<a href="https://docs.vllm.ai/en/stable/configuration/optimization/">https://docs.vllm.ai/en/stable/configuration/optimization/</a></li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a></li><li>下一篇：<a href="/posts/f61cc956/">vLLM系统拆解-07-Worker执行链：调度决策如何变成GPU上的一步计算</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-06-prefix-caching-kua-qing-qiu-kv-fu-yong-ru-he-zuo-dao-jing-que-gao-xiao-qie-an-quan&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="KV Cache" scheme="https://smarter.xin/tags/kv-cache/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="Prefix Caching" scheme="https://smarter.xin/tags/prefix-caching/"/>
    
    <category term="多租户安全" scheme="https://smarter.xin/tags/multi-tenant-security/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</title>
    <link href="https://smarter.xin/posts/e67da5b6/"/>
    <id>https://smarter.xin/posts/e67da5b6/</id>
    <published>2026-04-02T02:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-05-kv-cache-yu-paged-attention-he-xin-bu-shi-attention-gong-shi-er-shi-cun-fang-fang-shi" tabindex="-1">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</h1><p>如果要找 vLLM 相比早期推理框架最重要的改变，答案通常指向 PagedAttention。但&quot;PagedAttention&quot;这个名字很容易被误读——它 page 的不是 attention 的计算过程，而是 attention 所依赖的数据的存储方式。</p><p>这篇文章从 KV Cache 的基本机制讲起，解释为什么连续内存分配方案在在线服务中会失效，vLLM 的 block/page 化设计解决了什么问题，以及这个设计如何与调度器、Prefix Caching 形成系统合力。最后会区分 PagedAttention 和 FlashAttention，这两个概念在面试里经常被混淆。</p><hr><h2 id="kv-cache-shi-shi-yao-wei-shi-yao-bi-xu-you-ta" tabindex="-1" id="KV-Cache-是什么，为什么必须有它">KV Cache 是什么，为什么必须有它</h2><p>在自回归生成里，模型每生成一个新 token，都需要对&quot;之前所有 token&quot;做 attention。以生成第 101 个 token 为例：</p><ul><li>Query 只针对当前这一步的新 token</li><li>Key / Value 涉及前面全部 100 个历史 token</li></ul><p>如果每一步 decode 都从头把历史 token 的 K/V 重新算一遍，计算量会随序列长度线性增长，累计代价极高。所以实际系统都会把历史 token 对应的 K 和 V 保存下来，后续 decode 时：</p><ol><li>对新 token 只计算当前这一步的 Q/K/V</li><li>把新 K/V 追加进缓存</li><li>attention 用当前 Q 去读历史缓存中的 K/V</li></ol><p>这个保存下来的历史状态就是 <strong>KV Cache</strong>。</p><p>这不是&quot;锦上添花&quot;的优化，而是现代 LLM 服务中几乎默认存在的基础机制。没有它，长序列生成的代价会随步数平方级增长。</p><hr><h2 id="wei-shi-yao-kv-cache-hui-cheng-wei-zai-xian-fu-wu-de-zhu-yao-ping-jing" tabindex="-1" id="为什么-KV-Cache-会成为在线服务的主要瓶颈">为什么 KV Cache 会成为在线服务的主要瓶颈</h2><p>模型权重是固定的，但 KV Cache 是<strong>动态增长</strong>的。请求越长、生成越久，KV Cache 越大；而且不是一个请求在涨，是所有并发请求都在涨。</p><p>KV Cache 必须驻留在 GPU 上，因为 decode 每一步都要高频读取它。它和模型权重、激活值、中间 buffer 一起争夺显存。在很多在线服务场景里，真正卡住 batch size 的，不是算力，而是 KV Cache 的显存占用与管理方式。</p><p>这和 decode 阶段的计算特性密切相关。Decode 是 memory-bound 的：每一步新算的东西不多（只有一个 token），但要频繁访问历史 K/V。所以 KV Cache 的存储结构和访问方式，对 decode 延迟的影响比对 prefill 更直接。vLLM 官方优化文档把 decode 明确描述为 memory-bound，这也是 vLLM 把大量设计重心放在 KV Cache 管理上的原因。</p><hr><h2 id="lian-xu-fen-pei-wei-shi-yao-bu-gua-he-zai-xian-fu-wu" tabindex="-1" id="连续分配为什么不适合在线服务">连续分配为什么不适合在线服务</h2><p>直觉上，最简单的做法是给每个请求分配一段连续显存来存 KV Cache，序列变长时继续往后扩。这个方案在离线批量推理里可以工作，但在高并发在线服务里会暴露四个根本问题。</p><p><strong>长度不可预知。</strong> 请求进来时，你不知道它最终会生成多长。保守分配（先分小，不够再扩）会带来频繁扩容和数据搬迁；激进预留（一开始多预留）会大量浪费显存。这个矛盾无法通过调参根本解决。</p><p><strong>外部碎片。</strong> 不同请求的生命周期差异极大。短请求快速结束，释放了一段中间空间；新请求需要更大的连续区，但这段空洞不够用。总空闲显存足够，却因为不连续而无法利用——这是经典的外部碎片问题。</p><p><strong>扩容需要搬迁。</strong> 某个请求的 KV Cache 继续增长，但原来那段连续区后面已被占用。系统要找一块更大的连续区，把所有 K/V 复制过去，再更新引用。在高并发场景下，这个代价非常高。</p><p><strong>前缀共享不自然。</strong> 如果两个请求有相同的 system prompt，理想情况下这部分 K/V 可以共用。但在连续大 tensor 模型下，很难把&quot;共享的只读前缀&quot;和&quot;各自新增的部分&quot;分开管理，通常的结果是各自保留一份，甚至重新计算一份。</p><hr><h2 id="v-llm-de-jie-fa-ba-kv-cache-qie-cheng-gu-ding-da-xiao-de-block" tabindex="-1" id="vLLM-的解法：把-KV-Cache-切成固定大小的-block">vLLM 的解法：把 KV Cache 切成固定大小的 block</h2><p>vLLM 不给每个请求准备一段连续显存，而是：</p><ul><li>把 KV Cache 切成很多固定大小的 <strong>block</strong>（也叫 page）</li><li>每个 block 容纳固定数量 token 的 K/V</li><li>一个请求的逻辑序列由多个 block 串起来</li><li>这些 block 在物理显存上<strong>不需要连续</strong></li></ul><p>这个思路直接来自操作系统的分页内存管理：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">操作系统分页：</span><br><span class="line">  进程视图：连续的虚拟地址空间</span><br><span class="line">  物理实现：分散在不同位置的物理页</span><br><span class="line">  连接方式：页表（虚拟页号 → 物理页帧号）</span><br><span class="line"></span><br><span class="line">vLLM KV block：</span><br><span class="line">  请求视图：连续的历史 token 序列</span><br><span class="line">  物理实现：分散在显存各处的 KV block</span><br><span class="line">  连接方式：block table（逻辑块编号 → 物理块位置）</span><br></pre></td></tr></table></figure><p>以 block size = 16 为例，一个请求的历史 token 在逻辑上是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">逻辑序列：[token1...16] [token17...32] [token33...48] ...</span><br><span class="line">             逻辑 block 0   逻辑 block 1   逻辑 block 2</span><br></pre></td></tr></table></figure><p>但物理上可能是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│  请求 R 的 block table                                │</span><br><span class="line">│                                                      │</span><br><span class="line">│  逻辑 block 0  →  物理 block 8   (显存某处)           │</span><br><span class="line">│  逻辑 block 1  →  物理 block 21  (显存另一处)         │</span><br><span class="line">│  逻辑 block 2  →  物理 block 4   (显存又一处)         │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>对请求来说，历史仍然是连续的；对系统来说，只需维护一份&quot;逻辑块编号 → 物理块位置&quot;的映射。</p><hr><h2 id="quot-paged-quot-dao-di-page-liao-shi-yao" tabindex="-1" id="paged-到底-page-了什么">&quot;paged&quot;到底 page 了什么</h2><p>这是最容易产生混淆的地方。</p><p><strong>PagedAttention 中，被 page 化的是 KV Cache 的存储与索引方式，不是 attention 的计算数学。</strong></p><p>attention 仍然在做：当前 query 对历史 keys 做相关性计算，用权重加权历史 values。不同的是，历史 K/V 不再假设存在一段连续内存里，而是通过 block table 从多个分散的 block 中取出来参与计算。</p><p>变化的是：</p><ul><li>数据组织方式</li><li>内存管理方式</li><li>attention kernel 的访存逻辑</li></ul><p>不变的是：attention 的数学本身。</p><p>当历史 K/V 是连续存储时，kernel 可以直接按偏移量寻址。切换到 paged layout 后，kernel 在处理每个 block 时，需要先查 block table 确定物理位置，再加载对应数据。vLLM 使用专门为 paged KV Cache 设计的 multi-head query attention kernel——官方 Paged Attention 设计文档也特别说明，这里的&quot;block&quot;是 KV cache block，不是 GPU thread block，两者不要混淆。</p><p>理解这个设计的关键一句话：<strong>逻辑连续，物理离散，计算时按映射取数。</strong></p><hr><h2 id="block-hua-she-ji-jie-jue-liao-na-xie-wen-ti" tabindex="-1" id="Block-化设计解决了哪些问题">Block 化设计解决了哪些问题</h2><table><thead><tr><th>原有问题</th><th>block 化如何解决</th></tr></thead><tbody><tr><td>长度不可预知，难以预留</td><td>按需追加新 block，无需初始化时猜测最终长度</td></tr><tr><td>外部碎片</td><td>分配单位变为固定大小 block，回收后可直接给新请求复用</td></tr><tr><td>扩容需要搬迁</td><td>增长只需追加新 block，已有 block 原位不动</td></tr><tr><td>前缀共享不自然</td><td>前缀对应的 full block 可直接共用，分叉后各自追加新 block</td></tr><tr><td>并发承载量受限</td><td>碎片减少、回收高效，同等显存下能容纳更多活跃序列</td></tr></tbody></table><p>前缀共享这一点值得展开。block 化之后，前缀复用变成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">请求 A 和请求 B 有相同的 system prompt（前 32 个 token，block size=16）：</span><br><span class="line"></span><br><span class="line">  逻辑 block 0 [token1..16]  → 共享物理 block 5  （只读）</span><br><span class="line">  逻辑 block 1 [token17..32] → 共享物理 block 12 （只读）</span><br><span class="line"></span><br><span class="line">请求 A 之后的内容：</span><br><span class="line">  逻辑 block 2               → 物理 block 7  （A 私有）</span><br><span class="line"></span><br><span class="line">请求 B 之后的内容：</span><br><span class="line">  逻辑 block 2               → 物理 block 19 （B 私有）</span><br></pre></td></tr></table></figure><p>共享前缀等于共用旧 block，分叉后各自挂新 block。如果底层仍是连续大 tensor，这种细粒度共享几乎无法实现。</p><hr><h2 id="block-size-de-quan-heng" tabindex="-1" id="Block-Size-的权衡">Block Size 的权衡</h2><p>既然切成 block，block 大小就是一个设计参数：</p><table><thead><tr><th></th><th>block 太小</th><th>block 太大</th></tr></thead><tbody><tr><td>优点</td><td>分配粒度细，尾部浪费少；前缀复用更灵活</td><td>元数据少，映射关系简单；kernel 访存更规整</td></tr><tr><td>缺点</td><td>block 数量多，元数据管理复杂；kernel 索引开销增加</td><td>尾部未用空间增多；细粒度复用能力变弱</td></tr></tbody></table><p>本质是在<strong>管理粒度</strong>、<strong>显存浪费</strong>、<strong>索引复杂度</strong>、<strong>kernel 实现难度</strong>四者之间取平衡。具体默认值会随后端实现和模型配置变化，没有一个通用最优答案。</p><hr><h2 id="prefix-caching-wei-shi-yao-he-paged-kv-cache-tian-ran-da-dang" tabindex="-1" id="Prefix-Caching-为什么和-paged-KV-Cache-天然搭档">Prefix Caching 为什么和 paged KV Cache 天然搭档</h2><p>vLLM 的 <a href="/posts/be098d92/">Prefix Caching</a> 以 block 为粒度缓存已计算的 KV：</p><ul><li>只有 full block（已被填满的 block）会进入缓存</li><li>每个 full block 的缓存键由以下内容联合哈希得出：父 block 的 hash、当前 block 的 token 序列，以及 LoRA ID、多模态输入 hash、cache salt 等附加信息</li><li>新请求命中时，按 block 逐个匹配，直到某个位置开始不同为止，后续再新建 block</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Prefix Caching 命中过程：</span><br><span class="line"></span><br><span class="line">新请求：[block 0] → hash 命中，复用缓存</span><br><span class="line">        [block 1] → hash 命中，复用缓存</span><br><span class="line">        [block 2] → hash 命中，复用缓存</span><br><span class="line">        [block 3] → hash 未命中（序列从这里开始不同）→ 重新计算</span><br><span class="line">        [block 4] → 新内容，分配新 block</span><br></pre></td></tr></table></figure><p>如果底层是连续 tensor，“缓存部分前缀&quot;在数据结构上会更笨重；block 化之后，prefix caching 几乎是 block 化设计的自然延伸——前缀复用就是&quot;命中一批旧 block”。</p><hr><h2 id="wei-shi-yao-kv-cache-guan-li-he-diao-du-qi-bi-xu-xie-tong" tabindex="-1" id="为什么-KV-Cache-管理和调度器必须协同">为什么 KV Cache 管理和调度器必须协同</h2><p>KV Cache 不是独立模块，它和调度器是紧耦合的（第 4 篇中已介绍调度器的工作机制）。</p><p>调度器每一轮的决策都依赖 KV Cache 的当前状态：还剩多少 free block、哪些 block 被哪些请求引用、哪些 cached block 可以复用。反过来，调度器的决策也直接影响 KV Cache 的分配与回收节奏。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">┌────────────────────┐      free block 数量      ┌────────────────────────┐</span><br><span class="line">│    Scheduler       │ ◄──────────────────────── │   KV Cache Manager     │</span><br><span class="line">│                    │                           │                        │</span><br><span class="line">│  - 哪些请求运行    │ ─── 申请 / 释放 block ───► │  - block 分配          │</span><br><span class="line">│  - token budget    │                           │  - 引用计数             │</span><br><span class="line">│  - 是否 preempt    │ ◄─ cached block 可复用 ─── │  - prefix cache 索引   │</span><br><span class="line">└────────────────────┘                           └────────────────────────┘</span><br></pre></td></tr></table></figure><p>这也是 vLLM 架构中 scheduler 和 KV cache manager 被统一放在 engine core 里一体化思考的原因。</p><hr><h2 id="paged-attention-de-shou-yi-bu-zhi-shi-sheng-xian-cun" tabindex="-1" id="PagedAttention-的收益：不只是省显存">PagedAttention 的收益：不只是省显存</h2><p>把 PagedAttention 简单理解成&quot;显存优化&quot;没有错，但只看到了第一层。完整的收益链条是：</p><p><strong>显存碎片减少、按需增长</strong></p><p>↓ 同等显存下能容纳更多活跃序列</p><p>↓ 调度器手里可用候选请求更多，continuous batching 更有效</p><p>↓ Prefix caching 的复用粒度更细，命中收益更大</p><p>↓ 整体 serving 吞吐更高，延迟波动更小</p><p>vLLM 论文（<em>Efficient Memory Management for Large Language Model Serving with PagedAttention</em>）将吞吐提升和 near-zero waste 放在一起讲，就是在强调：显存管理不是附属优化，而是直接影响并发能力和吞吐能力的核心因素。</p><hr><h2 id="zhe-chong-she-ji-de-dai-jie" tabindex="-1" id="这种设计的代价">这种设计的代价</h2><p><strong>实现复杂度高。</strong> 连续 tensor 的思维很直观；paged KV Cache 需要 block 管理、block table、引用计数、回收策略、与调度器协调、定制化 attention kernel。工程复杂度明显更高。</p><p><strong>Kernel 无法假设内存连续。</strong> 因此需要专门适配 paged layout 的 attention kernel，不能直接复用原有的连续内存实现。官方设计文档专门为此独立成页，说明这不是一个小改动。</p><p><strong>元数据管理有开销。</strong> block table、hash 计算、引用计数、free block queue 都不是零成本。在大规模在线服务场景下，这些开销相对收益通常值得；但在小规模或单请求场景下，比值不一定划算。</p><p><strong>非标准硬件的适配难度更大。</strong> 因为涉及底层访存和 kernel 设计，某些 NPU 或边缘设备要完整复现 vLLM 的性能特性，难点往往集中在 paged attention 这一层。</p><hr><h2 id="paged-attention-he-flash-attention-bu-shi-tong-yi-jian-shi" tabindex="-1" id="PagedAttention-和-FlashAttention-不是同一件事">PagedAttention 和 FlashAttention 不是同一件事</h2><p>这两个概念经常被混淆，因为都和 attention 的&quot;高效&quot;有关。但它们关注的层次完全不同：</p><table><thead><tr><th></th><th>FlashAttention</th><th>PagedAttention</th></tr></thead><tbody><tr><td>核心问题</td><td>如何在已有 K/V 数据上高效计算 attention</td><td>如何组织和管理 KV Cache 数据本身</td></tr><tr><td>关注层次</td><td>计算 kernel 的 IO 效率（减少 HBM 读写，tile 化）</td><td>在线服务的内存管理（block 化、分散存储、映射）</td></tr><tr><td>解决场景</td><td>单次 attention 计算的 GPU 利用效率</td><td>多请求并发服务的显存碎片与复用问题</td></tr><tr><td>对内存布局的假设</td><td>通常假设 K/V 在连续或特定布局内存中</td><td>专门处理分散存储的 paged K/V layout</td></tr></tbody></table><p>两者并不冲突。vLLM 官方 README 说明，vLLM 集成了 FlashAttention / FlashInfer 等能力作为计算后端，PagedAttention 负责数据组织层，两层各司其职。</p><hr><h2 id="jie-yu" tabindex="-1" id="结语">结语</h2><p>PagedAttention 之所以重要，是因为它把一个&quot;看起来是内存细节&quot;的问题提升成了系统设计核心。KV Cache 的存放方式直接决定：</p><ul><li>一张 GPU 上能同时服务多少请求</li><li>调度器是否有足够大的批次可以操作</li><li>Prefix caching 能否有效落地</li><li>serving 吞吐的上限在哪里</li></ul><p>它的局限在于实现复杂度高，且对硬件 kernel 有依赖，适配非标准后端的成本不小。在小规模或对延迟要求极低的场景下，这套机制的工程成本可能超过收益。</p><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="参考资料">参考资料</h2><ol><li>vLLM 官方 README：<a href="https://github.com/vllm-project/vllm">https://github.com/vllm-project/vllm</a></li><li>vLLM 论文：<em>Efficient Memory Management for Large Language Model Serving with PagedAttention</em>，<a href="https://arxiv.org/abs/2309.06180">https://arxiv.org/abs/2309.06180</a></li><li>vLLM 官方设计文档：Paged Attention，<a href="https://docs.vllm.ai/en/latest/design/paged_attention/">https://docs.vllm.ai/en/latest/design/paged_attention/</a></li><li>vLLM 官方设计文档：Automatic Prefix Caching，<a href="https://docs.vllm.ai/en/latest/design/prefix_caching/">https://docs.vllm.ai/en/latest/design/prefix_caching/</a></li><li>vLLM 官方文档：Optimization and Tuning，<a href="https://docs.vllm.ai/en/stable/configuration/optimization/">https://docs.vllm.ai/en/stable/configuration/optimization/</a></li></ol><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/1202d62c/">vLLM系统拆解-04-Scheduler：为什么调度单位是token budget</a></li><li>下一篇：<a href="/posts/be098d92/">vLLM系统拆解-06-Prefix Caching：跨请求KV复用如何做到精确、高效且安全</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-05-kv-cache-yu-paged-attention-he-xin-bu-shi-attention-gong-shi-er-shi-cun-fang-fang-shi&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="PagedAttention" scheme="https://smarter.xin/tags/paged-attention/"/>
    
    <category term="KV Cache" scheme="https://smarter.xin/tags/kv-cache/"/>
    
    <category term="内存管理" scheme="https://smarter.xin/tags/memory-management/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-04-Scheduler：为什么调度单位是token budget</title>
    <link href="https://smarter.xin/posts/1202d62c/"/>
    <id>https://smarter.xin/posts/1202d62c/</id>
    <published>2026-04-02T02:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-04-scheduler-wei-shi-yao-diao-du-dan-wei-shi-token-budget" tabindex="-1">vLLM系统拆解-04-Scheduler：为什么调度单位是token budget</h1><p>对在线 LLM 服务来说，真正难的不是&quot;跑一次 forward&quot;，而是：很多请求并发，长度各不相同，有人刚进来做 prefill，有人正在 decode，有人共享 system prompt 可以复用缓存，有人把显存快占满了——在这种情况下，谁先算、谁后算、谁先暂停、谁可以少算一部分？</p><p>这就是 scheduler 存在的原因。vLLM 的 scheduler 不是一个给请求排队的模块，而是 engine core 里的决策核心：它每一轮决定如何把有限的 token 预算分配给当前最值得推进的请求。这篇文章从&quot;为什么需要 scheduler&quot;开始，把请求状态机、token budget、统一调度、decode 优先、chunked prefill、preemption 和 prefix caching 的调度含义逐一讲清楚。</p><hr><h2 id="jing-tai-batch-wei-shi-yao-bu-gou-yong" tabindex="-1" id="静态-batch-为什么不够用">静态 batch 为什么不够用</h2><p>在 LLM 推理框架的早期阶段，&quot;batch&quot;的概念直接来自训练：凑齐一批请求，一起跑，跑完再换下一批。这在离线推理里可以接受，在在线服务里会暴露三个根本问题。</p><p>**问题一：请求长度不一致，短请求被迫等长请求。**静态 batch 要等整批请求都完成才能切换，这意味着一批里最慢的那个请求决定了所有人的等待时间。</p><p>**问题二：prefill 和 decode 的资源特征完全不同。**第 2 篇中讲过，prefill 是 compute-bound 的，decode 是 memory-bound 的。用一套固定调度策略处理两者，结果通常是两头都不舒服：偏向 prefill，老请求的 ITL（Inter-Token Latency）变差；偏向 decode，新请求的 TTFT（首 token 延迟，Time To First Token）变差。</p><p>**问题三：KV cache 是动态增长的，不能只调度算力。**每个请求的 KV cache 都在增长，速度不同、结束时刻不同、可复用前缀也不同。一个请求能不能继续推进，不只是看有没有空闲算力，还要看有没有可用 KV block、有没有 token 预算、有没有 cache 命中。</p><p>所以 scheduler 实际上要同时协调：请求状态、GPU 计算机会、KV cache 资源、延迟与吞吐目标这四件事。它和 KV cache manager 是紧耦合的。</p><hr><h2 id="scheduler-zai-xi-tong-zhong-de-wei-zhi" tabindex="-1" id="Scheduler-在系统中的位置">Scheduler 在系统中的位置</h2><p>在 vLLM V1 架构里，scheduler 的默认类路径是 <code>vllm.v1.core.sched.scheduler.Scheduler</code>，它运行在 engine core 进程中：</p><pre><code class="highlight mermaid">flowchart TD    A[&quot;API Server / Frontend\n接收请求、做输入处理&quot;] --&gt; B[&quot;Engine Core\n运行主调度循环&quot;]    B --&gt; C[&quot;Scheduler\n决定本轮调度谁、调度多少&quot;]    C --&gt; D[&quot;KV Cache Manager\n提供 free block 数量与 cache 命中信息&quot;]    C --&gt; E[&quot;Worker（GPU）\n执行 forward pass&quot;]    E --&gt; F[&quot;Sampler\n从 logits 里采样 token&quot;]    F --&gt; B</code></pre><p>模型负责算，worker 负责执行，sampler 负责出 token，KV cache manager 负责管显存里的历史状态——但哪些请求本轮进入 batch、每个请求推进多少 token、哪个请求被抢占、哪个可以利用 prefix cache 少算一部分，这些全是 scheduler 的决定。</p><hr><h2 id="qing-qiu-zai-scheduler-yan-li-shi-yi-ge-zhuang-tai-ji" tabindex="-1" id="请求在-Scheduler-眼里是一个状态机">请求在 Scheduler 眼里是一个状态机</h2><p>用户发来一个请求，在 scheduler 的视角里，它不是一段文本，而是一个带状态的生成任务，会在以下几个状态之间转换：</p><pre><code class="highlight mermaid">stateDiagram-v2    [*] --&gt; waiting : 请求进入系统    waiting --&gt; running : 调度器选中，分配 token budget    running --&gt; running : 持续 decode（每轮推进）    running --&gt; preempted : 资源压力 / 更高优先级请求\n触发抢占    preempted --&gt; waiting : 放回等待队列    running --&gt; blocked : 当前资源 / budget 不足\n暂时跳过    blocked --&gt; waiting : 条件改善，重新参与调度    running --&gt; finished : 生成结束（EOS / max_tokens）    finished --&gt; [*]</code></pre><p>vLLM 源码中可以直接看到对应的设计：<code>_preempt_request</code>（触发抢占）、<code>_try_promote_blocked_waiting_request</code>（把 blocked 请求提升回 waiting）、finished / stopped 请求的释放逻辑，以及 scheduler 能返回的 running / waiting 请求数统计。</p><p>这意味着 scheduler 管的不是&quot;一次 batch 调用&quot;，而是<strong>请求的完整生命周期</strong>。</p><hr><h2 id="wei-shi-yao-bi-xu-shi-tong-yi-diao-du-qi-er-bu-shi-quot-prefill-diao-du-decode-diao-du-quot" tabindex="-1" id="为什么必须是统一调度器，而不是-prefill-调度-decode-调度">为什么必须是统一调度器，而不是&quot;prefill 调度 + decode 调度&quot;</h2><p>直觉上，可以给 prefill 和 decode 各写一套调度逻辑，拼在一起。vLLM V1 选择的是 unified scheduler（统一调度），原因在于真实请求根本不会干净地分层。</p><p>同一时刻，系统里可能同时存在：</p><ul><li>刚进来、还没开始计算的 prefill 请求</li><li>已经做了一半、剩余部分等待下轮推进的 chunked prefill 请求</li><li>正在 decode 的老请求</li><li>前缀部分已命中 cache、只剩一小段需要计算的请求</li><li>被抢占后重新入队的请求</li></ul><p>如果强行分成两个调度域，一个请求从 prefill 转 decode 时要跨域；chunked prefill 让&quot;纯 prefill&quot;的请求跨越多轮；prefix caching 让某些请求的&quot;prefill 量&quot;动态缩小；preemption 让请求可能在任意时刻退回 waiting。这些情况组合起来，双调度器的状态切换逻辑会很难维护。</p><p>统一调度的本质好处是：<strong>用同一个 token budget 框架管理所有请求的推进</strong>，不管它处于哪个阶段。</p><hr><h2 id="token-budget-li-jie-scheduler-de-yao-chi" tabindex="-1" id="Token-Budget：理解-Scheduler-的钥匙">Token Budget：理解 Scheduler 的钥匙</h2><p>这是 vLLM scheduler 最重要的概念，也是最容易被忽视的一个。</p><p>传统批量推理控制的是&quot;一批几个请求&quot;（request count）。vLLM 控制的是&quot;这一轮推进多少个 token&quot;（token budget）。两者差异很大：</p><table><thead><tr><th>控制维度</th><th>request count</th><th>token budget</th></tr></thead><tbody><tr><td>能反映工作量差异？</td><td>否（1 个 decode ≈ 1 token，1 个长 prefill ≈ 几千 token，count 都算 1）</td><td>是</td></tr><tr><td>能约束 GPU 负载？</td><td>间接、不准确</td><td>直接约束本轮 compute 和显存访问量</td></tr><tr><td>能处理混合阶段？</td><td>困难</td><td>天然支持（decode + prefill 共享同一个 budget 池）</td></tr><tr><td>prefill/decode 折中？</td><td>难表达</td><td>直接体现（budget 优先给 decode，剩余给 prefill）</td></tr></tbody></table><p>vLLM 文档中的 <code>max_num_batched_tokens</code> 就是这个思想的直接参数。scheduler 每轮不是在挑&quot;多少个请求&quot;，而是在一个有限的 token 预算池里，做多请求、多阶段的动态资源分配。</p><hr><h2 id="decode-you-xian-ce-lue-wei-shi-yao-zhao-gu-lao-qing-qiu-er-bu-shi-xin-qing-qiu" tabindex="-1" id="Decode-优先策略：为什么照顾老请求而不是新请求">Decode 优先策略：为什么照顾老请求而不是新请求</h2><p>vLLM 官方优化文档明确描述了 chunked prefill 下的调度策略：优先调度 decode 请求，再用剩余 budget 填 prefill；如果 prefill 放不下，自动切块。</p><p>原因很直接。在线聊天里，用户最敏感的体验指标是：</p><ul><li>是否很快看到第一个 token（TTFT）</li><li>后续 token 是否连续出现（ITL）</li><li>中间会不会突然停住</li></ul><p>其中 ITL 由 decode 的推进连续性直接决定。如果长 prompt 的 prefill 占据了大量 budget，decode 被压缩，用户会感觉回答在断断续续地输出。</p><p>而 prefill 对延迟的影响相对&quot;可缓冲&quot;：一个长 prompt 的 prefill 被分成几轮推进，用户多数情况下感知的是稍慢的 TTFT，而不是流式输出过程中的卡顿。</p><p>所以 decode 优先的本质是：<strong>保护流式交互体验</strong>，让已经在进行中的对话不被新进来的大请求打断。这不是说 prefill 不重要，而是在 ITL 和 TTFT 之间，前者对用户体验的即时影响更大。</p><hr><h2 id="chunked-prefill-shi-diao-du-wen-ti-bu-shi-shu-ju-chu-li-wen-ti" tabindex="-1" id="Chunked-Prefill-是调度问题，不是数据处理问题">Chunked Prefill 是调度问题，不是数据处理问题</h2><p>&quot;把长 prompt 分段喂给模型&quot;听起来像数据预处理，但 chunked prefill 之所以是系统优化，核心在于：<strong>切多少、怎么切，是基于当前轮次的剩余 budget 动态决定的</strong>，而不是固定分段。</p><p>调度器在决定一个 prefill 请求本轮能推进多少时，要知道：</p><ul><li>当前 decode 请求已经用掉多少 budget</li><li>剩余 budget 还有多少</li><li>这个 prefill 的剩余部分有多少 token</li><li>剩余 budget 够不够装下全部，不够的话装哪一段</li></ul><p>这些判断只有 scheduler 能做，因为只有它知道整个系统的当前状态。</p><p>Chunked prefill 的引入还改变了请求的状态结构：prefill 不再是&quot;单轮完成&quot;，而是&quot;多轮逐步推进&quot;，中间每一轮都在和 decode 请求共享同一个 budget 池。这要求 unified scheduler，因为 prefill 和 decode 此时已经深度交织在同一调度循环里。</p><p>带来的好处是：长 prompt 不再独占一整轮，compute-bound 的 prefill 和 memory-bound 的 decode 可以在同一轮内混合，GPU 利用率和整体延迟稳定性都有改善。</p><hr><h2 id="preemption-wei-shi-yao-zai-xian-tui-li-xi-tong-xu-yao-quot-da-duan-bing-hui-tui-quot" tabindex="-1" id="Preemption：为什么在线推理系统需要-打断并回退">Preemption：为什么在线推理系统需要&quot;打断并回退&quot;</h2><p>scheduler 里有 <code>_preempt_request</code>，这意味着 vLLM 不只是&quot;按顺序排队&quot;，还可以让已经在 running 状态的请求退回 waiting 队列。</p><p>原因是资源在运行时是动态竞争的：KV cache block 被占用和释放、token budget 每轮都在变、新请求不断进来、某些请求因为 prefix cache 或 encoder cache 状态变化而变得更&quot;容易推进&quot;。</p><p>如果系统不能抢占，一旦某个请求占住位置，即使当前轮次条件已经不再适合它继续推进（比如 KV block 不够了），它也会持续占用资源，而更合适的请求无法进来。</p><p>抢占在推理系统里的代价比训练里小很多。它不涉及大量的上下文切换开销，本质上是：把一个当前不合适继续推进的请求暂时放回 waiting，让更合适的请求先走，等后面条件改善再把它取回来。这是操作系统式资源调度思想在推理场景下的直接应用。</p><hr><h2 id="prefix-caching-de-diao-du-han-yi" tabindex="-1" id="Prefix-Caching-的调度含义">Prefix Caching 的调度含义</h2><p>Prefix caching 表面上是 KV cache 的事，但缓存命中本身不会自动产生系统收益。只有 scheduler 利用了这个命中，减少了该请求本轮的实际计算量，它才真正变成性能提升。</p><p>一个有前缀命中的请求进来时，scheduler 需要判断：</p><ul><li>哪一部分前缀已经命中 cache（实际需要计算的 token 数大幅减少）</li><li>cache miss 的剩余部分是否需要 chunk</li><li>这个请求本轮按什么方式进入 budget 分配</li><li>是否可以因此更快从 waiting 进入 running</li></ul><p>这些不是 cache manager 单独能决定的，最终推进决策仍然在 scheduler 侧。</p><p>所以 prefix caching 对调度的影响是：<strong>它改变了请求的实际可调度工作量</strong>。一个有大量前缀命中的请求，消耗的 token budget 可能比表面上的 prompt 长度小很多，scheduler 对这类请求的处理方式也应该相应调整。</p><hr><h2 id="duo-mu-biao-quan-heng-scheduler-zhen-zheng-de-gong-cheng-nan-dian" tabindex="-1" id="多目标权衡：Scheduler-真正的工程难点">多目标权衡：Scheduler 真正的工程难点</h2><p>调度器同时要在几个相互冲突的目标之间找平衡：</p><table><thead><tr><th>目标</th><th>含义</th><th>与其他目标的冲突</th></tr></thead><tbody><tr><td>吞吐</td><td>单位时间处理更多 token / 请求</td><td>budget 设大有利于吞吐，但可能拉高延迟抖动</td></tr><tr><td>TTFT</td><td>新请求快速进入系统</td><td>多做 prefill 有利于 TTFT，但会挤压 decode 的 ITL</td></tr><tr><td>ITL</td><td>decode 连续出 token，不卡顿</td><td>优先 decode 有利于 ITL，但新请求可能积压</td></tr><tr><td>显存利用率</td><td>KV cache 不浪费</td><td>更多活跃请求有利于利用率，但增加资源竞争风险</td></tr><tr><td>系统稳定性</td><td>不因长 prompt 或资源波动拖崩整体</td><td>chunked prefill 有利于稳定，但增加调度复杂度</td></tr></tbody></table><p>没有一个参数能让所有指标同时最优。<code>max_num_batched_tokens</code> 设大，吞吐可能上去，但交互延迟可能抖；设小，交互顺滑，但吞吐下降。decode 优先保护了 ITL，但 TTFT 在高负载下会变长。</p><p>这是 scheduler 的真正价值所在：不是某个指标拉满，而是根据部署场景在多目标之间做有意识的权衡。</p><hr><h2 id="yi-lun-wan-zheng-de-diao-du-xun-huan" tabindex="-1" id="一轮完整的调度循环">一轮完整的调度循环</h2><p>把上面的所有机制放在一起，scheduler 每一轮的工作流程如下：</p><pre><code class="highlight mermaid">flowchart TD    A[&quot;开始新一轮调度&quot;] --&gt; B[&quot;检查 token budget 上限\n（max_num_batched_tokens）&quot;]    B --&gt; C[&quot;优先处理 running 中的 decode 请求\n分配各自的 decode token&quot;]    C --&gt; D&#123;&quot;剩余 budget 是否充足？&quot;&#125;    D -- &quot;是&quot; --&gt; E[&quot;从 waiting 队列选候选 prefill 请求\n检查 prefix cache 命中情况&quot;]    D -- &quot;否&quot; --&gt; I[&quot;跳过 prefill，保护 decode&quot;]    E --&gt; F&#123;&quot;prefill token 量 &lt;= 剩余 budget？&quot;&#125;    F -- &quot;是，整块装入&quot; --&gt; G[&quot;该请求进入本轮 batch&quot;]    F -- &quot;否，需要 chunk&quot; --&gt; H[&quot;截取剩余 budget 可装的部分\n本轮只推进这一段&quot;]    H --&gt; G    G --&gt; J&#123;&quot;KV cache 有足够 free block？&quot;&#125;    J -- &quot;是&quot; --&gt; K[&quot;将请求加入本轮 batch&quot;]    J -- &quot;否&quot; --&gt; L[&quot;触发 preemption\n将低优先级 running 请求放回 waiting&quot;]    L --&gt; K    K --&gt; M[&quot;提交 batch 给 Worker 执行&quot;]    M --&gt; N[&quot;Sampler 采样输出 token&quot;]    N --&gt; O[&quot;更新请求状态\n更新 KV cache 占用\n释放已完成请求的 block&quot;]    O --&gt; P&#123;&quot;还有待处理请求？&quot;&#125;    P -- &quot;是&quot; --&gt; A    P -- &quot;否&quot; --&gt; Q[&quot;等待新请求进入&quot;]</code></pre><p>这个循环不会停。vLLM 是一个持续滚动推进请求的系统，不是&quot;处理一批、返回结果、等下一批&quot;的模式。</p><hr><h2 id="jie-yu" tabindex="-1" id="结语">结语</h2><p>vLLM scheduler 的设计核心可以用三句话概括：</p><ul><li><strong>调度的对象不是&quot;批次&quot;，而是请求的生命周期推进</strong>——每个请求在 waiting / running / preempted / finished 之间流转，scheduler 负责每一轮的状态推进决策。</li><li><strong>控制变量不是请求数，而是 token budget</strong>——这让不同阶段（prefill / decode）、不同长度的请求可以在同一个框架下被统一管理。</li><li><strong>统一调度的意义是把 chunked prefill、decode 优先、preemption、prefix caching 这些看似分散的机制，整合进一套动态资源分配框架里</strong>——它们都是在同一个 token budget 池上操作的不同策略。</li></ul><p>这种设计的局限在于：多目标权衡没有通用最优解，<code>max_num_batched_tokens</code> 等关键参数需要根据具体部署场景（交互式服务 vs 批量推理、短 prompt vs 长 prompt 为主）进行调整，默认值不一定适合所有场景。</p><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="参考资料">参考资料</h2><ul><li>vLLM V1 Scheduler 源码路径：<code>vllm.v1.core.sched.scheduler.Scheduler</code>（来源：vLLM Engine Args 官方文档）</li><li>vLLM 官方文档：Chunked Prefill 与 Optimization and Tuning</li><li>vLLM 官方 README：Continuous Batching 与 Scheduler 设计说明</li></ul><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/dbff781f/">vLLM系统拆解-03-入口分层：LLM、LLMEngine、AsyncLLMEngine与vllm serve</a></li><li>下一篇：<a href="/posts/e67da5b6/">vLLM系统拆解-05-KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-04-scheduler-wei-shi-yao-diao-du-dan-wei-shi-token-budget&quot; tabindex=&quot;-1&quot;&gt;vLLM系统拆解-04-Scheduler：为什么调度单位是token</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="Scheduler" scheme="https://smarter.xin/tags/scheduler/"/>
    
    <category term="TTFT" scheme="https://smarter.xin/tags/ttft/"/>
    
    <category term="ITL" scheme="https://smarter.xin/tags/itl/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-03-入口分层：LLM、LLMEngine、AsyncLLMEngine与vllm serve</title>
    <link href="https://smarter.xin/posts/dbff781f/"/>
    <id>https://smarter.xin/posts/dbff781f/</id>
    <published>2026-04-02T01:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-03-ru-kou-fen-ceng-llm-llm-engine-async-llm-engine-yu-vllm-serve" tabindex="-1">vLLM系统拆解-03-入口分层：LLM、LLMEngine、AsyncLLMEngine与vllm serve</h1><p>刚接触 vLLM 的人通常会有一个困惑：一个推理框架，为什么有 <code>LLM</code>、<code>LLMEngine</code>、<code>AsyncLLMEngine</code> 三个类，还有一个命令行工具 <code>vllm serve</code>？</p><p>这不是&quot;东西太多&quot;，而是三个不同层次的抽象，分别针对不同的使用场景：<strong>如何让用户用得简单</strong>、<strong>如何管理推理生命周期</strong>、<strong>如何把引擎接进 HTTP 服务</strong>。三层各自解决一类问题，共享同一套底层执行核心。</p><p>这篇文章先建立这套分层的整体视图，再逐层解释每层的设计逻辑，最后用完整时序图走一遍在线请求的调用链。</p><hr><h2 id="san-ceng-ru-kou-de-zheng-ti-jie-gou" tabindex="-1" id="三层入口的整体结构">三层入口的整体结构</h2><pre><code class="highlight mermaid">flowchart TD    PyUser[&quot;Python 用户\n离线脚本 / 测试 / Benchmark&quot;]    HTTPClient[&quot;HTTP 客户端\n应用服务 / OpenAI SDK&quot;]    subgraph L1[&quot;第一层：用户接口层&quot;]        LLM[&quot;LLM\nvllm/entrypoints/llm.py\n高层封装，屏蔽底层复杂度&quot;]    end    subgraph L3[&quot;第三层：服务入口层&quot;]        Serve[&quot;vllm serve\nvllm/entrypoints/openai/\nOpenAI-compatible 协议 + HTTP 服务&quot;]    end    subgraph L2[&quot;第二层：引擎层&quot;]        LLMEng[&quot;LLMEngine\nvllm/engine/llm_engine.py\n同步引擎，输入处理/调度/执行/输出&quot;]        AsyncEng[&quot;AsyncLLMEngine\nvllm/v1/engine/async_llm.py\n异步引擎，per-request stream + output handler&quot;]    end    subgraph Core[&quot;Engine Core（执行核心）&quot;]        EngCore[&quot;Engine Core\nvllm/v1/engine/core.py\nbusy loop 调度 + KV Cache 管理&quot;]        Workers[&quot;GPU Workers\nvllm/v1/worker/gpu_worker.py\n模型 forward + 显存管理&quot;]        EngCore --&gt; Workers    end    PyUser --&gt; LLM    HTTPClient --&gt; Serve    LLM --&gt; LLMEng    Serve --&gt; AsyncEng    LLMEng --&gt; EngCore    AsyncEng --&gt; EngCore</code></pre><p>这张图有几个关键点：</p><ul><li><code>LLM</code> 和 <code>vllm serve</code> 是两条不同的入口路径，面向不同使用场景</li><li>两条路径最终都收敛到同一个 Engine Core</li><li><code>LLMEngine</code>（同步）和 <code>AsyncLLMEngine</code>（异步）都是引擎抽象，针对不同的调用模型</li><li>Engine Core 下面才是真正执行推理的 GPU workers</li></ul><hr><h2 id="wei-shi-yao-xu-yao-san-ceng-er-bu-shi-yi-ge-tong-yi-ru-kou" tabindex="-1" id="为什么需要三层，而不是一个统一入口">为什么需要三层，而不是一个统一入口</h2><p>把所有东西放进一个大类在理论上可行，但工程上代价很高——因为三类使用者关注的问题差别很大：</p><p><strong>离线脚本用户</strong>关心的是：给我一个 prompt，返回结果，代码尽量短。</p><p><strong>服务系统开发者</strong>关心的是：请求如何排队、如何异步返回、如何不中断 event loop、如何同时处理大量连接。</p><p><strong>框架维护者</strong>关心的是：输入处理在哪层做、scheduler 放在哪里、worker 怎么起、engine core 怎么通信。</p><p>把这三类关注点混在一个类里，会导致接口臃肿、同步/异步模型混杂、生命周期管理混乱、难以单独演进某一层。</p><p>更合理的结构是：给普通用户一个简单入口（<code>LLM</code>），给系统内部一个控制核心（<code>LLMEngine</code>/<code>AsyncLLMEngine</code>），给服务场景一个协议适配入口（<code>vllm serve</code>）。每层职责单一，底层可复用。</p><hr><h2 id="di-yi-ceng-llm-lei" tabindex="-1" id="第一层：LLM-类">第一层：LLM 类</h2><p><code>LLM</code> 是面向普通 Python 用户的高层封装，典型用法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> vllm <span class="keyword">import</span> LLM, SamplingParams</span><br><span class="line"></span><br><span class="line">llm = LLM(model=<span class="string">&quot;Qwen/Qwen2.5-1.5B-Instruct&quot;</span>)</span><br><span class="line">sampling_params = SamplingParams(temperature=<span class="number">0.7</span>, top_p=<span class="number">0.9</span>, max_tokens=<span class="number">128</span>)</span><br><span class="line">outputs = llm.generate([<span class="string">&quot;解释一下什么是 KV Cache&quot;</span>], sampling_params)</span><br></pre></td></tr></table></figure><p>三行代码，不需要关心 request object、async stream、engine core、scheduler step、output handler、detokenizer queue——这些复杂度全部被 <code>LLM</code> 封装掉了。</p><p><strong><code>LLM</code> 的本质是对 engine 的高层封装，不是另一套推理系统。</strong> <code>LLM.generate()</code> 虽然看起来像一个普通函数调用，但底层仍然走 engine 的执行链路，完整的 batch 组织、cache 管理、调度优化都在里面。这也是离线使用 vLLM 时仍然比直接调 HuggingFace 快的原因。</p><hr><h2 id="di-er-ceng-llm-engine-tui-li-he-xin-bu-shi-model-er-shi-engine" tabindex="-1" id="第二层：LLMEngine——推理核心不是-model，而是-engine">第二层：LLMEngine——推理核心不是 model，而是 engine</h2><p>官方 Architecture Overview 对 <code>LLMEngine</code> 的描述很直接：核心组件，负责接收请求并生成输出，其中包括<strong>输入处理、调度、模型执行和输出处理</strong>。</p><p>为什么推理系统的核心必须是 engine，而不是 model？</p><p>因为在线推理最难的问题不是&quot;某一层 Transformer 怎么实现&quot;，而是：</p><ul><li>这个请求该在什么时候进 batch</li><li>这个请求现在是 Prefill 还是 Decode</li><li>这个请求的 KV Cache 够不够用</li><li>该不该抢占某个低优先级请求</li><li>输出如何增量回传</li><li>多张 GPU 如何协同</li><li>多进程如何通信</li></ul><p>这些都是 engine 层的问题，model 完全不管。</p><p>从设计目标上说：<strong>model 是算子与参数的承载体，engine 是请求生命周期的控制器</strong>。</p><p><code>LLMEngine</code> 负责的四件事：</p><table><thead><tr><th>阶段</th><th>做什么</th></tr></thead><tbody><tr><td>输入处理</td><td>标准化 prompt/token，应用 chat template，准备请求对象</td></tr><tr><td>调度</td><td>决定这一轮哪些请求进入 batch，分配 token budget 和 KV Cache block</td></tr><tr><td>执行</td><td>把 batch 交给执行层（GPU workers）跑 forward</td></tr><tr><td>输出处理</td><td>把 token ID 转成文本，整理输出格式，回传结果</td></tr></tbody></table><hr><h2 id="di-er-ceng-async-llm-engine-bu-zhi-shi-quot-jia-liao-async-await-quot" tabindex="-1" id="第二层：AsyncLLMEngine——不只是-加了-async-await">第二层：AsyncLLMEngine——不只是&quot;加了 async/await&quot;</h2><p>这是最容易被误解的一个类。很多人把 <code>AsyncLLMEngine</code> 理解为&quot;LLMEngine 的异步版本&quot;，这个说法没错但太表层。</p><p>真正的区别来自 server 场景的特殊需求。</p><p><strong>同步接口在 server 场景的问题</strong></p><p>同步思路是：发起调用 → 程序阻塞 → 等完整结果 → 继续。单请求脚本里完全够用，但 server 同时面对大量连接——有的请求刚进来，有的正在等 token，有的在流式输出，有的刚结束要清理资源。用同步阻塞包裹这一切，吞吐和响应性都会很差。</p><p><strong>AsyncLLMEngine 解决的核心问题</strong></p><p>官方文档对 <code>AsyncLLMEngine.generate()</code> 的流程描述如下：</p><ol><li>为该请求建立独立的 <code>AsyncStream</code></li><li>处理输入（tokenization 等）</li><li>把请求加入 <code>Detokenizer</code></li><li>把请求送入<strong>独立进程</strong>中的 <code>EngineCore</code></li><li>后台有一个独立的 <code>output_handler</code> 循环，持续从 EngineCore 拉取输出，放回对应请求的 <code>AsyncStream</code></li><li>调用方迭代这个异步生成器，拿到增量输出</li></ol><p>两个关键词值得注意：<strong>per-request stream</strong>（每个请求有独立的输出流）和 <strong>separate process</strong>（EngineCore 在独立进程里运行）。这两点说明 vLLM 在架构上已经把&quot;服务连接&quot;和&quot;核心执行&quot;明确解耦了。</p><p>这个设计带来的好处：</p><ul><li>前端连接层不需要直接盯着 worker 的执行状态</li><li>EngineCore 可以按自己的调度节奏推进，不受前端影响</li><li>每个请求独立消费自己的 AsyncStream，互不阻塞</li><li>天然适合多连接并发场景</li></ul><hr><h2 id="di-san-ceng-vllm-serve-xie-yi-ceng-bu-shi-yin-qing" tabindex="-1" id="第三层：vllm-serve——协议层，不是引擎">第三层：vllm serve——协议层，不是引擎</h2><p><code>vllm serve</code> 做的事情更接近&quot;把 vLLM engine 封装成可被外部访问的服务&quot;，而不是重新实现一套推理逻辑。</p><p>官方说明：vLLM 提供兼容 OpenAI Completions API 和 Chat API 的 HTTP server，通过 <code>vllm serve</code> 启动，默认可通过 <code>http://localhost:8000/v1</code> 访问。</p><p><strong>它实际做的事情</strong>：</p><ol><li>接受 HTTP 请求</li><li>解析 OpenAI 风格的请求 payload</li><li>调用 <code>AsyncLLMEngine</code></li><li>把结果重新包装成 OpenAI 风格响应</li><li>处理流式输出（SSE）、API key 校验、中间件、request ID 等服务层问题</li></ol><p><strong>它不做的事情</strong>：调度、KV Cache 管理、GPU forward——这些全在 engine 里。</p><p><strong>为什么 OpenAI 兼容很重要</strong></p><p>应用层通常不想绑定到某个具体的推理后端。如果 vLLM 使用 OpenAI 兼容协议，应用代码可以统一 SDK、统一消息格式、统一 streaming 方式，切换推理后端时上层代码几乎不用改。</p><p><code>vllm serve</code> 解决的是<strong>接入与服务化问题</strong>，engine 解决的是<strong>推理生命周期控制问题</strong>。分工清晰。</p><hr><h2 id="si-ge-jiao-se-de-ding-wei-dui-bi" tabindex="-1" id="四个角色的定位对比">四个角色的定位对比</h2><table><thead><tr><th>角色</th><th>面向谁</th><th>解决什么问题</th><th>不负责什么</th></tr></thead><tbody><tr><td><code>LLM</code></td><td>Python 用户</td><td>屏蔽复杂度，简化调用</td><td>推理控制逻辑</td></tr><tr><td><code>LLMEngine</code></td><td>系统内部</td><td>请求生命周期全流程管理</td><td>网络协议、服务层</td></tr><tr><td><code>AsyncLLMEngine</code></td><td>服务系统</td><td>异步/流式/多连接并发</td><td>具体执行（交给 EngineCore）</td></tr><tr><td><code>vllm serve</code></td><td>工程部署</td><td>HTTP 协议适配、OpenAI 兼容</td><td>调度、KV Cache、GPU 执行</td></tr></tbody></table><p>这四者的关系是<strong>逐层包装和适配</strong>，不是互相竞争。</p><hr><h2 id="yi-ci-zai-xian-qing-qiu-de-wan-zheng-diao-yong-lian" tabindex="-1" id="一次在线请求的完整调用链">一次在线请求的完整调用链</h2><p>把上面的结构转成时序图：</p><pre><code class="highlight mermaid">sequenceDiagram    participant C as HTTP 客户端    participant A as API Server (vllm serve)    participant AE as AsyncLLMEngine    participant EC as EngineCore (独立进程)    participant W as GPU Worker    participant OH as output_handler (后台循环)    participant AS as AsyncStream (per-request)    C-&gt;&gt;A: POST /v1/chat/completions    A-&gt;&gt;A: 鉴权 / 协议解析 / 参数校验&lt;br/&gt;OpenAI messages → 内部格式    A-&gt;&gt;AE: 提交请求    AE-&gt;&gt;AS: 创建 AsyncStream（与请求绑定）    AE-&gt;&gt;AE: 输入处理（tokenization 等）    AE-&gt;&gt;EC: 送入 EngineCore（独立进程）    loop 调度循环（每轮 Decode）        EC-&gt;&gt;EC: 调度决策 / 分配 KV Cache        EC-&gt;&gt;W: 下发 batch        W-&gt;&gt;W: 执行 forward / 更新 KV Cache        W-&gt;&gt;EC: 返回 logits / tokens        EC-&gt;&gt;OH: 输出 token    end    OH-&gt;&gt;AS: 将 token 放入对应请求的 AsyncStream    AE-&gt;&gt;A: 迭代 AsyncStream，获取增量输出    A-&gt;&gt;C: 流式返回（SSE / streaming response）    EC-&gt;&gt;EC: 检测结束条件（EOS / max_tokens）    EC-&gt;&gt;EC: 释放 KV Cache / 清理请求状态    A-&gt;&gt;C: 最终响应结束</code></pre><p>这条链路的核心特征：</p><ul><li><strong>请求进来 → API server 做协议转换，不做推理</strong></li><li><strong>转到 AsyncLLMEngine → 建立独立 AsyncStream，送入 EngineCore</strong></li><li><strong>EngineCore 独立运行调度循环，不受前端连接状态影响</strong></li><li><strong>output_handler 是一个后台异步循环，把 EngineCore 的输出分发回各请求的 stream</strong></li><li><strong>每个请求通过自己的 AsyncStream 独立消费输出，互不干扰</strong></li></ul><p>这和&quot;请求进来 → 直接调用 model → 返回字符串&quot;完全是两种设计思路。</p><hr><h2 id="wei-shi-yao-api-server-he-engine-core-bi-xu-chai-kai" tabindex="-1" id="为什么-API-server-和-engine-core-必须拆开">为什么 API server 和 engine core 必须拆开</h2><p>这里有个关键的设计决策值得单独说清楚。</p><p><strong>如果不拆，会出现什么</strong></p><p>如果 API server 直接持有完整推理执行逻辑，它要同时处理：网络收包、参数校验、tokenization、调度、GPU 执行控制、输出流式返回、连接管理。这会带来几个典型问题：</p><ul><li><strong>职责耦合</strong>：前端协议层一旦改动，容易触发执行层的变动</li><li><strong>异常传播范围大</strong>：某个请求处理失败可能直接影响整个服务进程</li><li><strong>扩展困难</strong>：无法单独替换前端协议或单独扩展执行层</li><li><strong>资源争用</strong>：前端网络事件和核心调度事件竞争 CPU，互相拖累</li><li><strong>可观测性差</strong>：难以区分是前端问题、调度问题还是 worker 问题</li></ul><p><strong>拆开之后的收益</strong></p><p>前端更像网关/协议层，core 更像计算与调度中枢，两边通过明确的通信机制交互。</p><p>官方文档也给出了一个具体的典型部署配置：单机 4 卡、TP=4 时，是 1 个 API server + 1 个 engine core + 4 个 GPU worker。这个组织方式本身说明 vLLM 把&quot;服务接入&quot;和&quot;执行控制&quot;视为不同层级的职责。</p><p>控制平面与数据平面分离、接入层与核心层分离，是成熟 infra 系统的通用做法，vLLM 的设计完全符合这个思路。</p><hr><h2 id="engine-guan-ce-lue-worker-guan-zhi-xing" tabindex="-1" id="engine-管策略，worker-管执行">engine 管策略，worker 管执行</h2><p>还有一个分工值得说明：engine core 为什么不直接做 GPU 计算，而是再委托给 worker？</p><p>engine core 的职责是&quot;决定做什么&quot;：谁该算、这轮 batch 是什么、资源怎么分、结果怎么回。</p><p>GPU worker 的职责是&quot;把它做出来&quot;：加载模型权重、管理本 GPU 显存、执行 forward、维护 KV Cache、参与分布式并行。</p><p>把 GPU 执行的重职责（权重常驻显存、KV Cache 显存维护、attention backend 选择、批处理执行、可能的 TP/PP 通信）全塞进 engine，会让 engine 的边界越来越模糊，调试和扩展都更困难。&quot;engine 管策略，worker 管执行&quot;是一个清晰的职责边界。</p><hr><h2 id="yuan-ma-lu-jing-su-cha" tabindex="-1" id="源码路径速查">源码路径速查</h2><table><thead><tr><th>层次</th><th>源码路径</th><th>职责</th></tr></thead><tbody><tr><td>用户接口层</td><td><code>vllm/entrypoints/llm.py</code></td><td><code>LLM</code> 类，高层封装</td></tr><tr><td>同步引擎</td><td><code>vllm/engine/llm_engine.py</code></td><td><code>LLMEngine</code>，核心控制</td></tr><tr><td>异步引擎</td><td><code>vllm/v1/engine/async_llm.py</code></td><td><code>AsyncLLMEngine</code>，per-request stream / output handler</td></tr><tr><td>服务前端</td><td><code>vllm/entrypoints/openai/</code></td><td>OpenAI-compatible 协议层，路由，请求解析，响应包装</td></tr><tr><td>Engine Core</td><td><code>vllm/v1/engine/core.py</code></td><td>busy loop 调度，资源管理，worker 协调</td></tr><tr><td>多进程执行器</td><td><code>vllm/v1/executor/multiproc_executor.py</code></td><td>启动 worker，连接 engine core 和实际 worker</td></tr><tr><td>GPU Worker</td><td><code>vllm/v1/worker/gpu_worker.py</code></td><td>GPU 执行，权重，显存，forward</td></tr></tbody></table><p>这些文件沿着&quot;入口 → 引擎 → 核心执行 → worker&quot;这条链组织，不是平铺的。</p><hr><h2 id="ji-ge-chang-jian-wu-qu" tabindex="-1" id="几个常见误区">几个常见误区</h2><p><strong>把 <code>LLM</code> 当成真正的核心</strong>：<code>LLM</code> 是高层入口，真正的系统核心是 engine。<code>LLM.generate()</code> 的执行能力来自底层的 engine，不是 <code>LLM</code> 类本身。</p><p><strong>把 <code>AsyncLLMEngine</code> 理解成&quot;只是多了 async/await&quot;</strong>：它的核心在于 per-request stream 和 output_handler 的设计，解决的是服务场景下异步流式请求处理的问题，不只是语法层面的改变。</p><p><strong>把 <code>vllm serve</code> 理解成重新实现了一套推理逻辑</strong>：它主要做协议适配和服务封装，调度、KV Cache、GPU 执行全部委托给 engine。</p><p><strong>把前后端拆开看作只是代码风格问题</strong>：这是为了职责边界清晰、故障隔离、扩展性和并发服务能力，不是风格偏好。</p><hr><h2 id="fen-ceng-she-ji-de-zong-he-shou-yi" tabindex="-1" id="分层设计的综合收益">分层设计的综合收益</h2><table><thead><tr><th>收益维度</th><th>具体表现</th></tr></thead><tbody><tr><td>对用户</td><td><code>LLM</code> + <code>SamplingParams</code> + <code>generate()</code> 三行代码即可使用</td></tr><tr><td>对服务系统</td><td><code>AsyncLLMEngine</code> 对接异步请求模型，天然支持多连接并发和流式输出</td></tr><tr><td>对框架本身</td><td>离线、在线、未来其他接入方式，底层都复用同一套 engine 能力</td></tr><tr><td>对系统演进</td><td>前端协议层和后端引擎层可以独立演进，互不强耦合</td></tr><tr><td>对稳定性</td><td>边界清晰后，异常隔离、资源规划、多进程扩展都更容易做</td></tr></tbody></table><p>这套分层设计是后续理解 scheduler、KV Cache Manager、PagedAttention 的基础——那些机制都运行在 engine core 这一层及其周边，而请求的进入方式、调用链的组织方式，就是由这篇文章描述的这套入口结构决定的。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/e62b6df0/">vLLM系统拆解-02-Prefill、Decode与KV Cache：理解vLLM之前的推理基础</a></li><li>下一篇：<a href="/posts/1202d62c/">vLLM系统拆解-04-Scheduler：为什么调度单位是token budget</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-03-ru-kou-fen-ceng-llm-llm-engine-async-llm-engine-yu-vllm-serve&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="模型服务" scheme="https://smarter.xin/tags/model-serving/"/>
    
    <category term="LLMEngine" scheme="https://smarter.xin/tags/llm-engine/"/>
    
    <category term="AsyncLLMEngine" scheme="https://smarter.xin/tags/async-llm-engine/"/>
    
    <category term="调用链" scheme="https://smarter.xin/tags/call-chain/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-02-Prefill、Decode与KV Cache：理解vLLM之前的推理基础</title>
    <link href="https://smarter.xin/posts/e62b6df0/"/>
    <id>https://smarter.xin/posts/e62b6df0/</id>
    <published>2026-04-02T01:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-02-prefill-decode-yu-kv-cache-li-jie-v-llm-zhi-qian-de-tui-li-ji-chu" tabindex="-1">vLLM系统拆解-02-Prefill、Decode与KV Cache：理解vLLM之前的推理基础</h1><p>学 vLLM 时有一个常见的模式：先记住 PagedAttention、continuous batching、chunked prefill、prefix caching 这几个名词，然后在被追问&quot;为什么需要这个&quot;时答不出来。</p><p>原因通常不是对源码不熟悉，而是跳过了推理的基本物理图景。比如：</p><ul><li>为什么 chunked prefill 能改善延迟？</li><li>为什么 decode 更怕显存带宽，prefill 更偏算力？</li><li>为什么 KV Cache 的管理比模型权重管理复杂得多？</li></ul><p>这些问题的答案，都来自对 Prefill、Decode 和 KV Cache 工作机制的理解。这篇文章就是建立这个基础。</p><hr><h2 id="cong-yi-ci-sheng-cheng-kai-shi-shi-jian-xian-shang-fa-sheng-liao-shi-yao" tabindex="-1" id="从一次生成开始：时间线上发生了什么">从一次生成开始：时间线上发生了什么</h2><p>假设用户发来一条请求：“请解释什么是 KV Cache，并举一个例子。”</p><p>这段 prompt 会被 tokenizer 切成若干 token（假设 100 个），然后模型开始生成回答。表面上看是一段文字不断冒出来，但底层分成两个阶段：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">时间线</span><br><span class="line">──────────────────────────────────────────────────────────────────►</span><br><span class="line"></span><br><span class="line">  ╔══════════════════════════════╗  ╔════╗  ╔════╗  ╔════╗</span><br><span class="line">  ║                              ║  ║    ║  ║    ║  ║    ║ ...</span><br><span class="line">  ║          Prefill             ║  ║ D1 ║  ║ D2 ║  ║ D3 ║</span><br><span class="line">  ║  (处理全部 100 个输入 token)  ║  ║    ║  ║    ║  ║    ║</span><br><span class="line">  ╚══════════════════════════════╝  ╚════╝  ╚════╝  ╚════╝</span><br><span class="line">       一次性处理，算力密集            每步生成一个 token，带宽密集</span><br><span class="line"></span><br><span class="line">  │← ─ ─ ─ ─ ─ ─ ─ 影响 TTFT ─ ─ ─ ─ ─ ─ ─►│← ─ 影响 ITL ─►</span><br></pre></td></tr></table></figure><ul><li><strong>Prefill</strong>：把所有输入 token 一次性&quot;吃进去&quot;，建立上下文，产出 KV Cache</li><li><strong>Decode</strong>：在已有上下文基础上逐步生成新 token，每步只新增一个</li></ul><p>这个划分不是随意的——两个阶段的计算特征、资源瓶颈和对用户体验的影响都截然不同。</p><hr><h2 id="prefill-jian-li-shang-xia-wen-bu-shi-quot-sheng-cheng-quot" tabindex="-1" id="Prefill：建立上下文，不是-生成">Prefill：建立上下文，不是&quot;生成&quot;</h2><p>Prefill 的本质是把用户已经给出的 prompt 转换成模型可持续使用的上下文状态。关键词是&quot;建立上下文&quot;，而不是&quot;输出内容&quot;。</p><p>如果 prompt 有 100 个 token，Prefill 阶段要对这 100 个 token 做一遍完整的 Transformer 前向：每一层上每个 token 都要计算，完成之后模型才第一次知道&quot;这个请求在说什么&quot;，然后才能生成第一个输出 token。</p><p><strong>Prefill 的产物是什么</strong></p><p>很多人以为 Prefill 只是产出了一个&quot;隐藏状态&quot;。从服务系统的角度看，Prefill 更重要的产物是：<strong>每一层、每个历史 token 对应的 KV Cache</strong>。</p><p>这些 KV Cache 是 Decode 阶段得以高效运行的前提——如果没有它们，Decode 每生成一个新 token，都要把整段 prompt 重新算一遍。</p><p><strong>为什么 Prefill 更偏 compute-bound</strong></p><p>Prefill 一次性处理很多 token，GPU 上形成的矩阵运算规模较大，张量核心更容易充分利用。这个阶段的瓶颈通常是算力（compute-bound），而不是显存访问速度。</p><p>prompt 越长，Prefill 的计算量越大，这也是为什么超长 prompt 会明显拉高首 token 延迟（Time To First Token，TTFT）。</p><hr><h2 id="decode-zeng-liang-tui-li-mei-bu-zhi-kan-quot-xin-zeng-quot" tabindex="-1" id="Decode：增量推理，每步只看-新增">Decode：增量推理，每步只看&quot;新增&quot;</h2><p>Decode 的本质是&quot;增量推理&quot;。有了 Prefill 建立的上下文之后，模型开始逐个生成新 token。</p><p>每生成一个新 token，模型做的事情是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">当前已有历史: [T1][T2]...[T100]  ←  存储在 KV Cache 中</span><br><span class="line">                                      ↓</span><br><span class="line">                              读取历史 K1..K100, V1..V100</span><br><span class="line">                              ↓</span><br><span class="line">新位置 T101:  只计算 Q101, K101, V101</span><br><span class="line">              用 Q101 做 attention（看所有历史 K）</span><br><span class="line">              按权重聚合历史 V → 输出 T101</span><br><span class="line">              ↓</span><br><span class="line">              把 K101, V101 追加写入 KV Cache</span><br><span class="line"></span><br><span class="line">下一步 T102:  读取 K1..K101, V1..V101（多了一个）</span><br><span class="line">              只计算 Q102, K102, V102</span><br><span class="line">              ...</span><br></pre></td></tr></table></figure><p>关键点：<strong>Decode 不会把历史 token 重新计算</strong>，只计算当前新 token 的 Q/K/V，然后读取历史 KV Cache 做 attention。</p><p><strong>为什么 Decode 更偏 memory-bound</strong></p><p>正是因为每步只新增少量计算，但每步都要读取完整的历史 KV Cache：</p><ul><li>序列越长，每次需要读取的历史 K/V 越多</li><li>请求越多，同时要维护和读取的 KV Cache 越庞大</li><li>这个阶段的瓶颈往往不是算力，而是显存访问速度和带宽（memory-bound）</li></ul><p>&quot;每次只生成一个 token&quot;听起来很轻，实际上不然。在线服务里不只有一个请求，多个请求同时在 Decode，GPU 的显存和带宽会被大量历史 KV Cache 持续占用。</p><hr><h2 id="prefill-he-decode-de-wan-zheng-dui-bi" tabindex="-1" id="Prefill-和-Decode-的完整对比">Prefill 和 Decode 的完整对比</h2><table><thead><tr><th>维度</th><th>Prefill</th><th>Decode</th></tr></thead><tbody><tr><td><strong>任务目标</strong></td><td>建立上下文，产出 KV Cache</td><td>利用上下文逐步生成新 token</td></tr><tr><td><strong>输入形态</strong></td><td>一次处理大量 token</td><td>每步只新增极少量 token（通常 1 个）</td></tr><tr><td><strong>计算特征</strong></td><td>大规模矩阵运算，compute-bound</td><td>小量新计算 + 大量历史读取，memory-bound</td></tr><tr><td><strong>用户体验</strong></td><td>决定 TTFT（首 token 何时出现）</td><td>决定 ITL（流式输出是否流畅）</td></tr><tr><td><strong>系统设计压力</strong></td><td>超长 prompt 会独占调度机会，拖慢其他请求</td><td>KV Cache 随请求数和长度放大，显存管理复杂</td></tr></tbody></table><p>这五条一旦清楚，vLLM 里很多设计的动机就能直接推导出来（后文详细说明）。</p><hr><h2 id="kv-cache-shi-shi-yao" tabindex="-1" id="KV-Cache-是什么">KV Cache 是什么</h2><p>理解 KV Cache 需要先回到 Transformer 的 attention 机制。</p><h3 id="attention-de-ji-ben-jie-gou" tabindex="-1" id="Attention-的基本结构">Attention 的基本结构</h3><p>在每一层 attention 中，当前 token 会产生 Query（Q）、Key（K）、Value（V）三个向量：</p><ul><li><strong>Q</strong>：表达&quot;我（当前 token）想关注什么&quot;</li><li><strong>K</strong>：历史每个位置的&quot;标签&quot;，用于被 Q 检索</li><li><strong>V</strong>：历史每个位置实际携带的信息</li></ul><p>计算过程是：当前 token 的 Q 与历史所有位置的 K 做相似度计算，得到权重，再按权重聚合所有位置的 V。这样，当前 token 就&quot;看到了&quot;历史上下文。</p><h3 id="wei-shi-yao-yao-huan-cun-k-he-v" tabindex="-1" id="为什么要缓存-K-和-V">为什么要缓存 K 和 V</h3><p>Decode 阶段每生成一个新 token，都要做一次 attention——用新 token 的 Q 去和历史所有位置的 K 匹配。</p><p>如果不缓存，历史 token 的 K 和 V 就要每次重算：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">不使用 KV Cache（prompt 1000 token，生成 100 个新 token）：</span><br><span class="line"></span><br><span class="line">  生成 T1001:  重算历史 1000 个位置的 K/V  → 计算量 ×1000</span><br><span class="line">  生成 T1002:  重算历史 1001 个位置的 K/V  → 计算量 ×1001</span><br><span class="line">  生成 T1003:  重算历史 1002 个位置的 K/V  → 计算量 ×1002</span><br><span class="line">  ...</span><br><span class="line">  生成 T1100:  重算历史 1099 个位置的 K/V  → 计算量 ×1099</span><br><span class="line"></span><br><span class="line">  累计重复计算：1000 + 1001 + ... + 1099 ≈ 105,000 份 K/V 重算</span><br><span class="line">  （其中绝大多数是完全相同的历史，被反复计算）</span><br><span class="line"></span><br><span class="line">使用 KV Cache：</span><br><span class="line"></span><br><span class="line">  Prefill 后，1000 个历史 token 的 K/V 已存入 Cache。</span><br><span class="line">  生成 T1001:  读取 Cache(K/V×1000)，只额外计算 K1001, V1001</span><br><span class="line">  生成 T1002:  读取 Cache(K/V×1001)，只额外计算 K1002, V1002</span><br><span class="line">  ...</span><br><span class="line">  每步只多算 1 份 K/V，其余全部复用。</span><br></pre></td></tr></table></figure><p>这个对比说明 KV Cache 的意义：不是&quot;快了一点点&quot;的优化，而是把自回归生成从二次方重复计算降到近似线性的必要机制。</p><h3 id="kv-cache-de-gong-cheng-ding-yi" tabindex="-1" id="KV-Cache-的工程定义">KV Cache 的工程定义</h3><p>从系统角度，KV Cache 是：<strong>模型在 Decode 过程中为避免重复计算而持久保存的历史上下文表示，按层、按头、按 token 位置不断增长，并在每次 Decode 时被读取</strong>。</p><p>五个关键属性：</p><table><thead><tr><th>属性</th><th>说明</th></tr></thead><tbody><tr><td><strong>按层存储</strong></td><td>模型每一层都有自己的 K/V，不是只有最后一层</td></tr><tr><td><strong>按 token 位置</strong></td><td>每新增一个 token，就多一份 K/V</td></tr><tr><td><strong>持续增长</strong></td><td>生成越长，Cache 越大</td></tr><tr><td><strong>高频读取</strong></td><td>每次 Decode 都要读一遍全部历史</td></tr><tr><td><strong>需要主动管理</strong></td><td>不是算完存着就行，而是要分配、复用、回收，可能还要跨请求共享</td></tr></tbody></table><p>最后一点是 KV Cache 成为系统设计核心的根本原因。</p><hr><h2 id="wei-shi-yao-kv-cache-shi-zai-xian-tui-li-zhen-zheng-nan-guan-de-zi-yuan" tabindex="-1" id="为什么-KV-Cache-是在线推理真正难管的资源">为什么 KV Cache 是在线推理真正难管的资源</h2><p><strong>权重是静态资产，KV Cache 是动态工作集</strong></p><p>模型权重虽然体积大，但有一个关键特点：加载完之后相对稳定，所有请求共享同一份，不会因为某个请求多生成几百 token 就突然变化。</p><p>KV Cache 完全不同：</p><ul><li>每个请求有自己独立的一份</li><li>长度随生成进度实时变化</li><li>生命周期不一——有的请求已经跑完几百 token，有的刚开始</li><li>请求结束后要释放，下一个请求还要重新分配</li><li>两个有相同 system prompt 的请求，理论上可以共享前缀对应的 K/V</li></ul><p>用一句话概括：<strong>权重像静态资产，KV Cache 像动态工作集</strong>。在线服务真正难管的是后者。</p><p><strong>多请求并发如何放大 KV Cache 的复杂度</strong></p><p>单请求时，管理一条序列的 Cache 还算直接。多请求并发时，同时面对的是：</p><ul><li>有的请求刚进入 Prefill，Cache 几乎是空的</li><li>有的请求正在 Decode 第 30 个 token，Cache 已经有一定规模</li><li>有的请求序列很长，Cache 占用大</li><li>有的请求马上结束，准备释放</li><li>有的请求与其他请求共享前缀，理论上可以复用 Cache</li><li>有的请求因为显存不够需要暂停，其 Cache 可能要被换出</li></ul><p>在这种状态下，KV Cache 不再只是一个&quot;张量&quot;，而是一个必须被调度系统持续管理的资源池。</p><p><strong>KV Cache 是吞吐的瓶颈</strong></p><p>服务型推理想要高吞吐，核心是：同一时刻能让多少请求一起在 GPU 上高效前进。这个上限往往不取决于算力，而取决于：</p><ul><li>显存里能放下多少并发请求的 KV Cache</li><li>Cache 管理是否存在大量碎片</li><li>有公共前缀的请求是否能共享 Cache</li><li>Prefill 和 Decode 的调度是否能高效协同</li></ul><p>这就是 PagedAttention 作为 vLLM 核心创新被重点介绍的原因——它直接解决的是 KV Cache 的显存管理问题。</p><hr><h2 id="si-ge-guan-jian-zhi-biao-ttft-itl-tpot-throughput" tabindex="-1" id="四个关键指标：TTFT、ITL、TPOT、Throughput">四个关键指标：TTFT、ITL、TPOT、Throughput</h2><p>推理系统的性能不能用单一指标衡量，不同角色关注的维度不同。</p><table><thead><tr><th>指标</th><th>全称</th><th>含义</th><th>主要受什么影响</th></tr></thead><tbody><tr><td><strong>TTFT</strong></td><td>Time To First Token</td><td>从请求发出到第一个输出 token 出现的时间</td><td>输入处理时间 + 调度等待 + Prefill 执行时间</td></tr><tr><td><strong>ITL</strong></td><td>Inter-Token Latency</td><td>相邻两个输出 token 之间的平均间隔</td><td>Decode 阶段的执行效率和调度稳定性</td></tr><tr><td><strong>TPOT</strong></td><td>Time Per Output Token</td><td>平均每个输出 token 的生成耗时</td><td>与 ITL 接近，更常用于系统级统计表述</td></tr><tr><td><strong>Throughput</strong></td><td>—</td><td>单位时间内系统处理的 token 数或请求数</td><td>batch 大小、GPU 利用率、KV Cache 管理效率</td></tr></tbody></table><p>从用户角度：TTFT 影响&quot;点发送后多久开始出文字&quot;，ITL 影响&quot;输出是否流畅&quot;。从服务提供方角度：Throughput 直接关系到资源利用率和运营成本。</p><p><strong>为什么这些指标会互相冲突</strong></p><p>提高 Throughput 的常见做法是增大 batch——把更多请求塞进同一轮 GPU 执行。但 batch 变大意味着：</p><ul><li>某些请求等待进入 batch 的时间变长，TTFT 上升</li><li>如果一个大 Prefill 任务独占了调度机会，正在 Decode 的请求要等更长时间才能被调度，ITL 变差</li></ul><p>这三个维度之间存在真实的权衡（trade-off），不存在让三者同时最优的魔法设置。vLLM 的很多设计本质上都在做这种动态平衡。</p><hr><h2 id="cong-zhe-tao-ji-chu-tui-dao-chu-v-llm-de-si-ge-guan-jian-she-ji" tabindex="-1" id="从这套基础推导出-vLLM-的四个关键设计">从这套基础推导出 vLLM 的四个关键设计</h2><p>理解了 Prefill、Decode 和 KV Cache，vLLM 的几个核心机制就不再是&quot;神奇的优化 trick&quot;，而是直接从问题推导出来的解法：</p><table><thead><tr><th>vLLM 机制</th><th>解决的核心问题</th><th>与基础概念的关系</th></tr></thead><tbody><tr><td><strong>continuous batching</strong></td><td>请求动态到达和结束，固定 batch 浪费 GPU</td><td>Decode 是步进式的，不同请求处于不同阶段，需要持续调度让请求随时加入和退出</td></tr><tr><td><strong>PagedAttention</strong></td><td>KV Cache 连续分配导致显存碎片严重</td><td>KV Cache 按 token 动态增长，连续大块分配会产生大量内部和外部碎片；按 block 分页管理可以大幅降低碎片</td></tr><tr><td><strong>prefix caching</strong></td><td>相同前缀的请求反复做 Prefill 浪费算力</td><td>Prefill 的核心产物是 KV Cache，相同前缀对应相同 K/V，可以跨请求复用</td></tr><tr><td><strong>chunked prefill</strong></td><td>超长 Prefill 独占调度，拖慢 Decode 的 ITL</td><td>Prefill 是 compute-bound，Decode 是 memory-bound，两者瓶颈不同；把 Prefill 切成小块，与 Decode 交织执行，可以让调度更均衡</td></tr></tbody></table><p>这四个设计的逻辑来自同一个底层认知：<strong>Prefill 和 Decode 是两类不同的计算任务，KV Cache 是连接它们的动态资源，而这个资源的管理质量直接决定系统的并发能力和延迟表现</strong>。</p><hr><h2 id="tui-li-xi-tong-ben-zhi-shang-shi-zi-yuan-diao-du-wen-ti" tabindex="-1" id="推理系统本质上是资源调度问题">推理系统本质上是资源调度问题</h2><p>从算法层面，自回归生成逻辑很简单：输入 prompt，预测下一个 token，把新 token 接回去，循环。</p><p>从系统层面，真正复杂的是：</p><ul><li>什么时候让哪个请求先做 Prefill</li><li>谁先进入 Decode，谁等待</li><li>token budget 怎么在不同请求之间分配</li><li>哪些请求可以合进同一个 batch</li><li>哪些 KV Cache 可以共享，哪些该回收</li><li>CPU 侧的调度逻辑能否跟得上 GPU 的执行节奏</li><li>在 TTFT 和 Throughput 之间如何实时权衡</li></ul><p>这些问题不属于模型算法范畴，而属于基础设施工程——更接近操作系统的资源调度，而不是深度学习的模型优化。</p><p>理解这一点，是从&quot;知道 vLLM 有哪些功能&quot;升级到&quot;理解 vLLM 为什么这样设计&quot;的关键转变。后续文章讲 scheduler、KV Cache Manager、PagedAttention 时，这个视角会持续有用。</p><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="参考资料">参考资料</h2><ol><li><a href="https://docs.vllm.ai/en/latest/design/arch_overview/">vLLM Architecture Overview（官方文档）</a></li><li><a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention（vLLM 论文）</a></li><li><a href="https://docs.vllm.ai/en/stable/configuration/optimization/">vLLM Optimization and Tuning（官方文档）</a></li></ol><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>上一篇：<a href="/posts/c8797d0d/">vLLM系统拆解-01-架构设计：为什么推理引擎不能是一个进程</a></li><li>下一篇：<a href="/posts/dbff781f/">vLLM系统拆解-03-入口分层：LLM、LLMEngine、AsyncLLMEngine与vllm serve</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-02-prefill-decode-yu-kv-cache-li-jie-v-llm-zhi-qian-de-tui-li-ji-chu&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="KV Cache" scheme="https://smarter.xin/tags/kv-cache/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="Prefill" scheme="https://smarter.xin/tags/prefill/"/>
    
    <category term="Decode" scheme="https://smarter.xin/tags/decode/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-01-架构设计：为什么推理引擎不能是一个进程</title>
    <link href="https://smarter.xin/posts/c8797d0d/"/>
    <id>https://smarter.xin/posts/c8797d0d/</id>
    <published>2026-04-02T00:30:00.000Z</published>
    <updated>2026-04-03T11:44:50.291Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-01-jia-gou-she-ji-wei-shi-yao-tui-li-yin-qing-bu-neng-shi-yi-ge-jin-cheng" tabindex="-1">vLLM系统拆解-01-架构设计：为什么推理引擎不能是一个进程</h1><p><code>model.generate()</code> 很好用，但它解决的是单请求问题——一个输入进去，一个输出出来，跑完拉倒。</p><p>真实的 LLM serving 场景不是这样：并发请求是常态，每个请求 prompt 长度不同、生成进度不一，有的刚开始 Prefill，有的已经 Decode 到一半，还有的在等 KV Cache 空间释放。在这种状态下，GPU 利用效率、请求排队延迟和整体吞吐，都不是&quot;把模型跑起来&quot;能解决的问题。</p><p>vLLM 的定位是<strong>高吞吐、显存高效的大模型推理与服务引擎</strong>。它不靠某一个 attention kernel 快多少取胜，而是在并发请求的上下文里提供一套完整的调度机制、KV Cache 管理和分层执行架构。</p><p>这篇文章从 vLLM 的整体架构出发，解释它为什么采用多进程设计，每个进程负责什么，以及一次请求是怎样在这套架构里流转的。理解这张&quot;总地图&quot;，是读懂后续 scheduler、KV Cache、PagedAttention 等机制的前提。</p><hr><h2 id="llm-serving-dao-di-nan-zai-na" tabindex="-1" id="LLM-serving-到底难在哪">LLM serving 到底难在哪</h2><p>单请求场景关注的是：模型能不能装进显存、forward 够不够快、结果是否正确。</p><p>服务型推理面对的是另一组问题：</p><ul><li>大量请求动态到达，prompt 长度、生成长度各不相同</li><li>有的请求处于 Prefill 阶段（计算初始 KV Cache），有的处于 Decode 阶段（逐 token 生成）</li><li>KV Cache 随 Decode 持续增长，且不同请求的 KV Cache 之间无法直接共享显存空间</li><li>需要流式返回，不能等全部生成完再输出</li><li>既要控制首 token 延迟（Time To First Token，TTFT），也要维持较高的整体吞吐</li></ul><p>这些特征加在一起，让问题的核心变成：<strong>如何把大量处于不同状态的请求，持续、高效地编排进 GPU 的执行流程</strong>。</p><p>有一个角度可以帮助理解 vLLM 在做什么：把它当成&quot;大模型推理操作系统&quot;。和操作系统类似，它接收外部任务（请求）、管理资源（GPU 显存、KV Cache）、做调度（决定谁先运行、谁暂停）、做隔离（不同请求彼此独立）、做复用（prefix caching）、做回收（请求结束后释放 KV Cache），还要支持并行扩展（多 GPU、多副本）。这不是官方定义，但作为理解模型它很准确。</p><hr><h2 id="liang-chong-shi-yong-ru-kou" tabindex="-1" id="两种使用入口">两种使用入口</h2><p>vLLM 同时支持两类场景，对应两种入口：</p><p><strong>入口一：Python 离线调用</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> vllm <span class="keyword">import</span> LLM, SamplingParams</span><br><span class="line"></span><br><span class="line">llm = LLM(model=<span class="string">&quot;Qwen/Qwen2.5-1.5B-Instruct&quot;</span>)</span><br><span class="line">sampling_params = SamplingParams(temperature=<span class="number">0.7</span>, top_p=<span class="number">0.9</span>, max_tokens=<span class="number">128</span>)</span><br><span class="line">outputs = llm.generate([<span class="string">&quot;什么是 KV Cache？&quot;</span>], sampling_params)</span><br><span class="line"><span class="built_in">print</span>(outputs[<span class="number">0</span>].outputs[<span class="number">0</span>].text)</span><br></pre></td></tr></table></figure><p>这种模式更像库调用，适合离线推理、模型测试、写 benchmark、嵌入到自有程序里。对应的核心类是 <code>LLM</code> 和 <code>LLMEngine</code>。</p><p><strong>入口二：OpenAI-compatible HTTP server</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vllm serve Qwen/Qwen2.5-1.5B-Instruct</span><br></pre></td></tr></table></figure><p>启动后，外部服务通过 OpenAI 兼容接口（<code>/v1/chat/completions</code>、<code>/v1/completions</code> 等）访问。适合工程/平台团队把 vLLM 部署成一个模型服务，对接已有上层应用。</p><p>两种入口存在的原因：研究/开发者需要在 Python 里直接用，工程/平台团队需要一个 HTTP 服务。vLLM 的做法是把<strong>核心推理引擎独立出来，再在上面承接不同入口</strong>——这个思路贯穿整个架构设计。</p><hr><h2 id="wei-shi-yao-bi-xu-shi-duo-jin-cheng" tabindex="-1" id="为什么必须是多进程">为什么必须是多进程</h2><p>理论上用一个进程做完所有事（接 HTTP、tokenization、调度、调 GPU、流式返回）是可以的。但这条路很快遇到工程天花板。</p><p><strong>CPU 任务与 GPU 任务相互干扰</strong></p><p>API server 处理 HTTP 请求、序列化/反序列化、tokenization，是典型的 CPU-heavy 工作。调度逻辑是高频小循环，对延迟极其敏感——每一轮 Decode 都要重新决定哪些请求继续执行、KV Cache 够不够用。两类工作放在同一进程，网络请求的抖动会直接拖慢调度频率，进而拉低 GPU 利用率、增加 token 间延迟（Inter-Token Latency，ITL）。</p><p><strong>服务接口和核心引擎不该耦合</strong></p><p>vLLM 同时支持 Python 离线调用和 HTTP server 两种入口。如果 HTTP 层和调度逻辑写死在一起，换协议、加新接口、单独优化 API server 都会很麻烦。核心引擎需要是一个独立能力层，不同入口挂在上面。</p><p><strong>多 GPU 场景天然要求多进程</strong></p><p>每张 GPU 有独立的显存、CUDA context 和执行队列。Tensor Parallel、Pipeline Parallel 等并行策略，最终都要落到不同 GPU 上执行。用一个进程管理多张 GPU 的运行状态，会引入不必要的上下文切换复杂度、错误隔离问题和调试难度。</p><p>vLLM V1 的解法：<strong>把服务入口、调度核心、GPU 执行三层彻底拆开，每层只做自己最擅长的事</strong>。官方明确将这次重构的目标定为：代码简洁模块化、CPU 额外开销近乎为零、关键优化统一到一套架构里，以及尽量零配置默认启用合理优化。</p><hr><h2 id="zheng-ti-jia-gou-si-ge-jin-cheng-jiao-se" tabindex="-1" id="整体架构：四个进程角色">整体架构：四个进程角色</h2><p>vLLM V1 的进程架构最多包含四个角色（DP Coordinator 仅在 data parallel 场景出现）：</p><pre><code class="highlight mermaid">flowchart TD    Client[&quot;用户 / 应用\nHTTP 请求 / Python 调用&quot;]    subgraph API_Proc[&quot;API Server Process&quot;]        direction TB        APIServer[&quot;API Server\n- 解析请求 / chat template\n- Tokenization\n- 多模态输入处理\n- 流式返回结果&quot;]    end    subgraph Core_Proc[&quot;Engine Core Process&quot;]        direction TB        Scheduler[&quot;Scheduler\n调度决策 / token budget&quot;]        KVMgr[&quot;KV Cache Manager\nblock 分配 / 回收&quot;]    end    subgraph Worker_Procs[&quot;GPU Worker Process(es)&quot;]        direction LR        W0[&quot;GPU Worker 0\nModel Runner\nForward / KV Cache 写入&quot;]        W1[&quot;GPU Worker 1\nModel Runner\nForward / KV Cache 写入&quot;]        Wn[&quot;...&quot;]    end    subgraph DP_Proc[&quot;DP Coordinator Process\n仅 data parallel 场景&quot;]        DPCoord[&quot;DP Coordinator\n负载均衡 / MoE 同步协调&quot;]    end    Client -- 请求进入 --&gt; API_Proc    API_Proc -- 内部 request 对象 --&gt; Core_Proc    Core_Proc -- 下发可执行 batch --&gt; Worker_Procs    Worker_Procs -- 输出 token / 状态 --&gt; Core_Proc    Core_Proc -- 结果 --&gt; API_Proc    API_Proc -- 流式返回 --&gt; Client    DP_Proc -. 跨副本协调 .-&gt; Core_Proc</code></pre><p>四个角色的职责和源码位置：</p><table><thead><tr><th>进程角色</th><th>核心职责</th><th>关键源码路径</th></tr></thead><tbody><tr><td><strong>API Server</strong></td><td>接收 HTTP 请求、tokenization、多模态输入处理、结果流式返回</td><td><code>vllm/entrypoints/openai/api_server.py</code></td></tr><tr><td><strong>Engine Core</strong></td><td>运行 scheduler、管理 KV Cache、协调 GPU workers、busy loop 持续调度</td><td><code>vllm/v1/engine/core.py</code></td></tr><tr><td><strong>GPU Worker</strong></td><td>加载模型权重、管理本 GPU 显存、执行 forward、写入更新 KV Cache</td><td><code>vllm/v1/worker/gpu_worker.py</code></td></tr><tr><td><strong>DP Coordinator</strong></td><td>DP ranks 之间负载均衡、MoE 模型同步 forward 协调（条件性存在）</td><td><code>vllm/v1/engine/coordinator.py</code></td></tr></tbody></table><hr><h2 id="api-server-qian-tai-bu-shi-da-nao" tabindex="-1" id="API-Server：前台，不是大脑">API Server：前台，不是大脑</h2><p>API Server 的工作是&quot;把用户能理解的输入，转成推理引擎能执行的内部表示，并把结果原路送回&quot;。具体包括：解析 HTTP 请求和参数、应用 chat template、tokenization、多模态数据加载（如果有），然后把生成好的内部 request 对象发给 Engine Core，最后把生成结果流式回传给客户端。</p><p>把 API Server 单独拆出来有三个直接收益：</p><ul><li><strong>服务层和核心引擎解耦</strong>：HTTP 层如何变（换协议、加新接口），不影响调度和执行逻辑。</li><li><strong>横向扩展更自然</strong>：data parallel 开启时，API Server 可以随副本数一起扩，与 Engine Core 的扩展逻辑解耦。</li><li><strong>CPU 资源规划更清晰</strong>：tokenization 和请求处理是明显的 CPU 密集操作，单独进程便于规划线程数和核数，不和调度侧竞争 CPU 时间。</li></ul><hr><h2 id="engine-core-xi-tong-de-diao-du-zhong-shu" tabindex="-1" id="Engine-Core：系统的调度中枢">Engine Core：系统的调度中枢</h2><p>Engine Core 是整个系统的控制中枢，官方的描述是：运行 scheduler、管理 KV Cache、协调 GPU workers，以 <strong>busy loop</strong> 方式不断循环执行。</p><p>&quot;busy loop&quot;是关键词。Engine Core 不是被动等待请求触发，而是持续运行一个调度循环，每一轮都做：</p><ol><li>决定哪些请求进入本轮 batch</li><li>为每个请求分配 token budget</li><li>查询和分配 KV Cache block</li><li>把请求组成可执行 batch</li><li>将 batch 下发给 GPU workers</li></ol><p>为了完成这些决策，Engine Core 要持续维护大量动态状态，包括每个请求的当前阶段、已生成 token 数、prompt 长度、是否已完成 Prefill、当前占用的 KV Cache block、batch budget 剩余情况。</p><p>Engine Core 为什么必须独立成进程，有三个原因：</p><ul><li><strong>调度是高频操作</strong>：每一步 Decode 都要重新决策（哪些请求继续、是否需要抢占/回收/推迟），频率极高。</li><li><strong>状态维护量大</strong>：请求数量越多，状态管理越复杂，与其他进程共享状态会引入不必要的同步开销。</li><li><strong>对 CPU 延迟敏感</strong>：调度层 CPU 一旦被其他工作拖慢，GPU 就会空转，ITL 直接上升。</li></ul><hr><h2 id="gpu-worker-zhi-xing-zhe-bu-shi-jue-ce-zhe" tabindex="-1" id="GPU-Worker：执行者，不是决策者">GPU Worker：执行者，不是决策者</h2><p>每张 GPU 对应一个独立的 GPU Worker 进程。职责直接：加载模型权重、管理本 GPU 显存、接受 Engine Core 下发的 batch、执行 forward、把新生成的 KV Cache 写入显存、产生 logits。</p><p>“Engine Core 决定做什么，GPU Worker 负责把它做出来”——这是两者的分工边界，Worker 不参与全局请求决策。</p><p>一张 GPU 对应一个 worker 是最自然的设计：</p><ul><li>每张 GPU 有独立的显存、CUDA context、执行队列，一对一对应职责最清晰</li><li>独立进程带来更好的错误隔离——某个 worker 出问题不直接影响其他 worker</li><li>Tensor Parallel 和 Pipeline Parallel 都需要把计算映射到不同 GPU，&quot;一 GPU 一 worker&quot;天然适配这类映射</li></ul><hr><h2 id="dp-coordinator-duo-fu-ben-chang-jing-de-xie-diao-ceng" tabindex="-1" id="DP-Coordinator：多副本场景的协调层">DP Coordinator：多副本场景的协调层</h2><p>DP Coordinator 只在启用 data parallel 时出现。</p><p>data parallel 是把一整套 Engine Core + GPU Workers 复制多份，同时服务更多请求。但&quot;多复制几份&quot;不是终点——还需要解决：请求路由到哪个 DP rank、各 rank 负载是否均衡、MoE 模型的专家路由是否需要跨 rank 同步（对 MoE 模型，各 rank 之间需要协调 forward，否则结果可能不一致）。</p><p>DP Coordinator 处理的就是这一层的问题：比 Engine Core 更高层的跨副本编排。</p><hr><h2 id="llm-engine-async-llm-engine-luo-ji-zu-jian-vs-jin-cheng-jiao-se" tabindex="-1" id="LLMEngine-AsyncLLMEngine：逻辑组件-vs-进程角色">LLMEngine / AsyncLLMEngine：逻辑组件 vs 进程角色</h2><p>这里有一个容易混淆的地方。前面说的是<strong>进程角色</strong>（API Server Process、Engine Core Process 等），而 <code>LLMEngine</code> 和 <code>AsyncLLMEngine</code> 是代码层面的<strong>逻辑组件</strong>，不是进程，两个维度不在同一层次。</p><pre><code class="highlight mermaid">graph LR    subgraph logical[&quot;逻辑组件（代码层）&quot;]        LLM[&quot;LLM\n用户调用接口&quot;]        LLMEng[&quot;LLMEngine\n同步引擎抽象&quot;]        AsyncEng[&quot;AsyncLLMEngine\n异步引擎抽象&quot;]        Sched[&quot;Scheduler&quot;]        KVM[&quot;KV Cache Manager&quot;]    end    subgraph proc[&quot;进程角色（运行时）&quot;]        AProc[&quot;API Server Process&quot;]        CProc[&quot;Engine Core Process&quot;]        WProc[&quot;GPU Worker Process&quot;]    end    LLM --&gt;|依赖| LLMEng    AsyncEng --&gt;|被| AProc    LLMEng -.-&gt;|概念上对应| CProc    Sched --&gt;|运行在| CProc    KVM --&gt;|运行在| CProc</code></pre><ul><li><strong><code>LLM</code></strong>：面向用户的顶层接口，<code>LLM.generate()</code> 就是从这里进入</li><li><strong><code>LLMEngine</code></strong>：核心引擎抽象，负责输入处理、调度、模型执行组织、输出处理，适合 Python 离线场景</li><li><strong><code>AsyncLLMEngine</code></strong>：异步版本，适合服务端并发请求、流式输出、异步 request lifecycle 管理；OpenAI-compatible API server 使用的是这个，因为服务端不能因为一个请求阻塞其他请求</li></ul><p><code>Engine Core</code>（进程）和 <code>LLMEngine</code>（类）有对应关系，但不是同义词。前者是运行时的进程角色，后者是代码层面的抽象类。</p><hr><h2 id="yi-ci-qing-qiu-de-wan-zheng-liu-zhuan" tabindex="-1" id="一次请求的完整流转">一次请求的完整流转</h2><p>把前面的模块串起来，看一次请求的完整路径：</p><pre><code class="highlight mermaid">sequenceDiagram    participant C as 用户/客户端    participant A as API Server    participant E as Engine Core    participant W as GPU Worker    participant S as Sampler    C-&gt;&gt;A: 发起请求（HTTP POST 或 LLM.generate）    A-&gt;&gt;A: 解析参数 / chat template / tokenization / 多模态处理    A-&gt;&gt;E: 提交内部 request 对象    loop 调度循环（每轮 Decode）        E-&gt;&gt;E: 调度决策：选本轮进入 batch 的请求        E-&gt;&gt;E: 分配 token budget / 分配 KV Cache block        E-&gt;&gt;W: 下发可执行 batch        W-&gt;&gt;W: 准备输入张量 / 调用 Model Runner        W-&gt;&gt;W: 执行 forward / 写入更新 KV Cache        W-&gt;&gt;S: 传递 logits        S-&gt;&gt;S: 采样（greedy / temperature / top-p / top-k 等）        S-&gt;&gt;E: 返回本轮输出 token        E-&gt;&gt;E: 更新请求状态        E-&gt;&gt;A: 传递输出 token        A-&gt;&gt;C: 流式返回 token（如启用）    end    E-&gt;&gt;E: 检测到结束条件（EOS / max_tokens）    E-&gt;&gt;E: 释放 KV Cache block / 清理请求状态    A-&gt;&gt;C: 返回完整结果</code></pre><p>每一步的要点：</p><p><strong>第 1 步：输入处理（API Server）</strong><br>把用户输入转为推理引擎的内部表示——解析请求参数、应用 chat template（对话模式下）、tokenization、多模态数据加载，生成内部 request 对象。</p><p><strong>第 2 步：进入引擎（Engine Core）</strong><br>请求进入 <code>LLMEngine / AsyncLLMEngine</code> 管理的流程，系统开始追踪它的状态：是新请求还是正在 Decode、当前需要多少 KV Cache、batch 能不能塞下它。</p><p><strong>第 3 步：调度决策（Engine Core，持续循环）</strong><br>在 busy loop 里，每轮决定：哪些请求进入本轮 batch、分配多少 token budget、KV Cache block 怎么分配，然后把可执行 batch 下发给 GPU workers。</p><p><strong>第 4 步：GPU 执行（GPU Worker）</strong><br>准备输入张量，调用 Model Runner，执行模型 forward，把新生成的 KV Cache 写入显存，产生 logits。</p><p><strong>第 5 步：采样（Sampler）</strong><br>从 logits 中根据采样策略选出下一个 token。支持 greedy、temperature、top-p、top-k 等策略，以及部分场景下的 beam search。</p><p><strong>第 6 步：输出与状态更新</strong><br>把 token 转为文本、流式回传给客户端（如果启用）、更新请求状态、检查是否满足结束条件。若请求结束，释放其占用的 KV Cache block 和请求状态；若未结束，继续参与下一轮调度。</p><p>最后一步有个细节值得注意：<strong>一个请求不是&quot;进来执行一次就结束&quot;，而是在多个调度轮次中逐步完成 Decode</strong>。这就是为什么 vLLM 需要请求生命周期管理，而不是一次函数调用完事。</p><hr><h2 id="wei-shi-yao-yao-chi-xu-diao-du-er-bu-shi-jing-tai-batch" tabindex="-1" id="为什么要持续调度，而不是静态-batch">为什么要持续调度，而不是静态 batch</h2><p>&quot;静态 batch&quot;的思路是：凑满一批请求，统一跑完，再接下一批。在离线 benchmark 里没问题，在在线服务场景里会遇到几个硬伤：</p><table><thead><tr><th>问题</th><th>静态 batch</th><th>持续调度（continuous batching）</th></tr></thead><tbody><tr><td>新请求何时能进入</td><td>必须等当前 batch 全部完成</td><td>任意 Decode 轮次都可以插入</td></tr><tr><td>batch 内请求长度不一</td><td>需要填充对齐，短请求浪费算力</td><td>按实际状态动态分配 token budget</td></tr><tr><td>某个请求提前结束</td><td>对应槽位空转到整个 batch 完成</td><td>立即释放资源，下一轮可接新请求</td></tr><tr><td>GPU 利用率</td><td>取决于 batch 内请求状态是否整齐</td><td>通过动态拼 batch 维持较高利用率</td></tr><tr><td>KV Cache 释放粒度</td><td>batch 结束后统一释放</td><td>每轮可做细粒度分配和回收</td></tr></tbody></table><p>持续调度的核心思想：<strong>系统持续运行调度循环，每一轮根据所有活跃请求的当前状态，重新决定这轮 GPU 应该执行什么</strong>。这让 vLLM 能够接纳新请求、继续旧请求、动态控制 batch 内容、更好利用 GPU 空隙时间、更精细地管理 KV Cache。</p><p>这也是为什么 vLLM 的价值主要体现在 serving 场景，而不是单条离线推理。</p><hr><h2 id="v-1-zhong-gou-wei-shi-yao-xu-yao-xi-tong-xing-gai-zao" tabindex="-1" id="V1-重构：为什么需要系统性改造">V1 重构：为什么需要系统性改造</h2><p>vLLM V1 不是小修小补，而是对核心架构的一次系统性重构。</p><p>V0 阶段，vLLM 快速扩展了对大量模型和硬件的支持，但不同特性是相对独立演进的——prefix caching、chunked prefill、speculative decoding、多模态等功能各自叠加，导致系统复杂度积累，技术债增加，核心调度路径越来越难以优化。</p><p>V1 想解决这些问题，目标明确：</p><ol><li><strong>代码更简单、模块化、易于 hack 和扩展</strong>：不同特性的实现要能统一在同一套架构里</li><li><strong>近乎零 CPU 额外开销</strong>：scheduler 和 sampler 的 CPU 开销在 V1 里被显著压缩</li><li><strong>关键优化统一到一套架构</strong>：PagedAttention、prefix caching、chunked prefill 等机制协同工作，而不是各自独立维护</li><li><strong>尽量零配置</strong>：默认启用合理优化，减少用户手动调参的负担</li></ol><p>这意味着本系列后面讲到的 scheduler、KV Cache 管理、PagedAttention，都是 V1 统一架构下的组成部分，不是独立的优化 trick。</p><hr><h2 id="yuan-ma-lu-jing-su-cha" tabindex="-1" id="源码路径速查">源码路径速查</h2><table><thead><tr><th>模块</th><th>源码路径</th></tr></thead><tbody><tr><td>Python 离线入口（CLI）</td><td><code>vllm/entrypoints/cli/main.py</code></td></tr><tr><td>OpenAI-compatible HTTP 服务</td><td><code>vllm/entrypoints/openai/api_server.py</code></td></tr><tr><td>同步引擎抽象</td><td><code>vllm/engine/llm_engine.py</code></td></tr><tr><td>异步引擎抽象</td><td><code>vllm/engine/async_llm_engine.py</code></td></tr><tr><td>V1 Engine Core（调度中枢）</td><td><code>vllm/v1/engine/core.py</code></td></tr><tr><td>V1 Engine 工具函数</td><td><code>vllm/v1/engine/utils.py</code></td></tr><tr><td>多进程执行器</td><td><code>vllm/v1/executor/multiproc_executor.py</code></td></tr><tr><td>GPU Worker</td><td><code>vllm/v1/worker/gpu_worker.py</code></td></tr><tr><td>DP Coordinator</td><td><code>vllm/v1/engine/coordinator.py</code></td></tr></tbody></table><hr><h2 id="ji-ge-chang-jian-hun-yao-dian" tabindex="-1" id="几个常见混淆点">几个常见混淆点</h2><p><strong><code>LLM</code> / <code>LLMEngine</code> / <code>AsyncLLMEngine</code> 的区别</strong></p><p>三者是不同层次的抽象：<code>LLM</code> 是面向用户的顶层接口，底层依赖 <code>LLMEngine</code>；<code>AsyncLLMEngine</code> 是服务端场景的异步版本，OpenAI API server 使用它，不会因单个请求阻塞其他请求。</p><p><strong>Engine Core 和 LLMEngine 不是同一个概念</strong></p><p><code>LLMEngine</code> 是代码层面的类抽象，Engine Core 是 V1 进程架构里负责调度协调的进程角色。两者有对应关系，但不是同义词——一个是逻辑组件，另一个是运行时进程。</p><p><strong>API Server 不做推理</strong></p><p>它做的是请求接入、输入处理（tokenization 等）和结果回传。调度逻辑在 Engine Core，GPU 计算在 Worker。</p><p><strong>GPU Worker 不是调度器</strong></p><p>Worker 只负责执行 Engine Core 下发的任务，不参与全局请求决策。决策权在 Engine Core，Worker 专注执行。</p><hr><h2 id="zhe-tao-jia-gou-de-she-ji-luo-ji" tabindex="-1" id="这套架构的设计逻辑">这套架构的设计逻辑</h2><p>把前面的内容串成一条逻辑链：</p><ol><li>LLM serving 是动态多请求问题，不是单次 forward 问题</li><li>动态多请求要求有请求生命周期管理</li><li>生命周期管理需要专门的调度中枢（Engine Core），持续运行调度循环</li><li>调度中枢是高频 CPU 循环，必须和 HTTP 服务层解耦，否则互相干扰</li><li>GPU 执行天然适合封装成独立 worker，与 GPU 资源一一对应</li><li>多副本 data parallel 场景需要更上层的跨副本协调（DP Coordinator）</li><li>最终形成：API Server → Engine Core → GPU Workers 的分层多进程架构</li></ol><p>这条逻辑链，是后续理解 scheduler、KV Cache 管理、PagedAttention、prefix caching 的基础——这些机制运行在这套架构的特定层次里，脱开架构单独看，很容易把它们当成相互独立的优化 trick，而看不到它们之间的系统联系。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>导读：<a href="/posts/f77e5973/">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</a></li><li>下一篇：<a href="/posts/e62b6df0/">vLLM系统拆解-02-Prefill、Decode与KV Cache：理解vLLM之前的推理基础</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-01-jia-gou-she-ji-wei-shi-yao-tui-li-yin-qing-bu-neng-shi-yi-ge-jin-cheng&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="模型服务" scheme="https://smarter.xin/tags/model-serving/"/>
    
    <category term="系统架构" scheme="https://smarter.xin/tags/system-architecture/"/>
    
    <category term="多进程" scheme="https://smarter.xin/tags/multi-process/"/>
    
  </entry>
  
  <entry>
    <title>vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</title>
    <link href="https://smarter.xin/posts/f77e5973/"/>
    <id>https://smarter.xin/posts/f77e5973/</id>
    <published>2026-04-02T00:00:00.000Z</published>
    <updated>2026-04-03T11:44:50.290Z</updated>
    
    <content type="html"><![CDATA[<h1 id="v-llm-xi-tong-chai-jie-00-dao-du-cong-jia-gou-diao-du-dao-mian-shi-biao-da-de-xue-xi-lu-xian" tabindex="-1">vLLM系统拆解-00-导读：从架构、调度到面试表达的学习路线</h1><h2 id="yue-du-ben-wen-hou-ying-neng" tabindex="-1" id="阅读本文后应能">阅读本文后应能</h2><ul><li>看清这个系列到底在回答哪些问题：架构、调度、KV Cache、执行链、调优、分布式、框架对比和面试表达。</li><li>按自己的目标选择阅读顺序，而不是把 19 篇文章当成只能线性推进的一条路线。</li><li>明确它和站内 Transformer、vLLM 入门实践、推理全景文章的边界，减少重复阅读。</li></ul><h2 id="zhe-ge-xi-lie-jie-jue-shi-yao-wen-ti" tabindex="-1" id="这个系列解决什么问题">这个系列解决什么问题</h2><p>一句话概括：<strong>先建立 LLM Serving 的物理图景，再把 vLLM 拆成“请求怎么进来、状态怎么推进、KV 怎么存、GPU 怎么跑、系统怎么调、架构怎么扩”六条主线。</strong></p><p>这套文章不是 API 手册，也不是源码逐文件导读，而是围绕真实 AI Infra 问题组织：为什么在线推理需要 Scheduler、为什么 KV Cache 会变成核心资源、为什么 Prefix Caching 和 PagedAttention 必须放在同一张图里看、为什么部署调优最终落到 TTFT、ITL、显存预算和 workload 分布上。</p><h2 id="yu-zhan-nei-qi-ta-wen-zhang-de-guan-xi" tabindex="-1" id="与站内其他文章的关系">与站内其他文章的关系</h2><table><thead><tr><th>文章</th><th>作用</th></tr></thead><tbody><tr><td><a href="/posts/4854dc00/">AI Infra学习之旅-Transformer知识地图</a></td><td>先补 Attention、KV Cache、Prefill/Decode 这些概念从模型层是怎么来的。</td></tr><tr><td><a href="/posts/9d0ab4a7/">现代大模型推理的核心技术全景</a></td><td>从更高一层看现代推理系统要解决的共性问题。</td></tr><tr><td><a href="/posts/b21cbc80/">AI-Infra学习之旅-第一个vLLM程序</a></td><td>如果还没跑过 vLLM，可以先用这篇建立最小使用体验。</td></tr><tr><td><a href="/posts/fbab2bc4/">AI-Infra学习之旅-PagedAttention解析</a></td><td>想先抓住 vLLM 最著名的单点机制，可以先读这篇，再回到本系列主线。</td></tr></tbody></table><h2 id="xi-lie-jie-gou" tabindex="-1" id="系列结构">系列结构</h2><ul><li><code>01</code> 到 <code>03</code>：建立系统入口和基本图景，先看为什么在线推理不是单次 <code>forward</code>，以及 vLLM 为什么天然需要分层入口。</li><li><code>04</code> 到 <code>07</code>：进入系统主干，分别拆 Scheduler、KV Cache / PagedAttention、Prefix Caching 和 Worker 执行链。</li><li><code>08</code> 到 <code>10</code>：把视角切到优化与扩展，讨论调参、高级加速和分布式并行。</li><li><code>11</code> 到 <code>19</code>：把前面的理解压缩成面试表达、源码地图、框架对比、线上调优和通用系统设计能力。</li></ul><h2 id="lu-xian-tu" tabindex="-1" id="路线图">路线图</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">00 导读</span><br><span class="line">  → 01 架构设计 → 02 Prefill / Decode / KV Cache → 03 入口分层</span><br><span class="line">  → 04 Scheduler → 05 KV Cache / PagedAttention → 06 Prefix Caching → 07 Worker 执行链</span><br><span class="line">  → 08 性能优化 → 09 高级加速 → 10 分布式部署</span><br><span class="line">  → 11 面试题精讲 → 12 源码地图 → 13 框架对比 → 14 部署调优</span><br><span class="line">  → 15 三层协同 → 16 瓶颈定位 → 17 面试深挖 → 18 通用系统设计 → 19 复盘手册</span><br></pre></td></tr></table></figure><h2 id="pian-mu-suo-yin" tabindex="-1" id="篇目索引">篇目索引</h2><table><thead><tr><th style="text-align:center">编号</th><th style="text-align:left">文章</th><th style="text-align:left">主题</th></tr></thead><tbody><tr><td style="text-align:center">01</td><td style="text-align:left"><a href="/posts/c8797d0d/">架构设计：为什么推理引擎不能是一个进程</a></td><td style="text-align:left">多进程分层、API Server / Engine Core / Worker 职责边界</td></tr><tr><td style="text-align:center">02</td><td style="text-align:left"><a href="/posts/e62b6df0/">Prefill、Decode与KV Cache：理解vLLM之前的推理基础</a></td><td style="text-align:left">自回归推理物理图景、TTFT / ITL 与缓存基础</td></tr><tr><td style="text-align:center">03</td><td style="text-align:left"><a href="/posts/dbff781f/">入口分层：LLM、LLMEngine、AsyncLLMEngine与vllm serve</a></td><td style="text-align:left">离线入口、在线入口和调用链分层</td></tr><tr><td style="text-align:center">04</td><td style="text-align:left"><a href="/posts/1202d62c/">Scheduler：为什么调度单位是token budget</a></td><td style="text-align:left">Continuous batching、chunked prefill、preemption</td></tr><tr><td style="text-align:center">05</td><td style="text-align:left"><a href="/posts/e67da5b6/">KV Cache与PagedAttention：核心不是Attention公式，而是存放方式</a></td><td style="text-align:left">Block 化缓存、页式管理和调度协同</td></tr><tr><td style="text-align:center">06</td><td style="text-align:left"><a href="/posts/be098d92/">Prefix Caching：跨请求KV复用如何做到精确、高效且安全</a></td><td style="text-align:left">前缀复用、block hash、多租户边界</td></tr><tr><td style="text-align:center">07</td><td style="text-align:left"><a href="/posts/f61cc956/">Worker执行链：调度决策如何变成GPU上的一步计算</a></td><td style="text-align:left">Executor、Worker、Model Runner、Sampler</td></tr><tr><td style="text-align:center">08</td><td style="text-align:left"><a href="/posts/b0da0eba/">性能优化：参数背后的系统级权衡</a></td><td style="text-align:left">token 预算、显存预算、吞吐与延迟权衡</td></tr><tr><td style="text-align:center">09</td><td style="text-align:left"><a href="/posts/a1df357b/">高级加速：Speculative Decoding、CUDA Graph与量化</a></td><td style="text-align:left">高阶优化针对的瓶颈和适用边界</td></tr><tr><td style="text-align:center">10</td><td style="text-align:left"><a href="/posts/9ce7b444/">分布式部署：TP、PP、DP、EP各解决什么问题</a></td><td style="text-align:left">模型切分、流量扩展和通信代价</td></tr><tr><td style="text-align:center">11</td><td style="text-align:left"><a href="/posts/c3e99548/">面试题精讲：从概念到系统设计如何讲清楚为什么</a></td><td style="text-align:left">高频问题的回答骨架</td></tr><tr><td style="text-align:center">12</td><td style="text-align:left"><a href="/posts/354d88e4/">源码地图：每个核心能力落在哪一层</a></td><td style="text-align:left">模块职责、路径速查、源码定位</td></tr><tr><td style="text-align:center">13</td><td style="text-align:left"><a href="/posts/84b005d4/">推理框架对比：vLLM、TGI、TensorRT-LLM与SGLang</a></td><td style="text-align:left">选型维度、系统风格和适用场景</td></tr><tr><td style="text-align:center">14</td><td style="text-align:left"><a href="/posts/bc6be055/">部署调优：从资源预算到线上问题诊断</a></td><td style="text-align:left">参数、OOM、CPU/GPU 瓶颈与线上排障</td></tr><tr><td style="text-align:center">15</td><td style="text-align:left"><a href="/posts/cf18dd3a/">调度、KV Cache与Prefix Caching：为什么必须三层协同</a></td><td style="text-align:left">统一请求生命周期视角</td></tr><tr><td style="text-align:center">16</td><td style="text-align:left"><a href="/posts/e7a2278e/">性能瓶颈定位：从现象到根因的分层诊断方法</a></td><td style="text-align:left">症状归因、指标和调优次序</td></tr><tr><td style="text-align:center">17</td><td style="text-align:left"><a href="/posts/32c30b06/">面试深挖：面试官继续追问为什么时怎么回答</a></td><td style="text-align:left">追问展开、trade-off 和反模式</td></tr><tr><td style="text-align:center">18</td><td style="text-align:left"><a href="/posts/6f0af891/">通用LLM推理系统设计：从vLLM出发建立框架</a></td><td style="text-align:left">从具体实现上升到共性设计原则</td></tr><tr><td style="text-align:center">19</td><td style="text-align:left"><a href="/posts/cc221b1e/">AI Infra与推理岗面试手册：vLLM学到什么程度才算够用</a></td><td style="text-align:left">系列收束、能力分层和复习建议</td></tr></tbody></table><h2 id="yue-du-shun-xu-jian-yi" tabindex="-1" id="阅读顺序建议">阅读顺序建议</h2><ul><li><strong>想先把系统主干看懂</strong>：按 <code>01 → 02 → 04 → 05 → 06 → 07</code>。</li><li><strong>想优先准备上线和调优</strong>：先读 <code>02 → 04 → 05 → 08 → 10 → 14 → 16</code>。</li><li><strong>想把内容转成面试表达</strong>：重点读 <code>11 → 12 → 13 → 17 → 18 → 19</code>。</li><li><strong>时间有限</strong>：至少读 <code>02</code>、<code>04</code>、<code>05</code>、<code>10</code>、<code>14</code>、<code>18</code>，这六篇能把系统主干、扩展和抽象框架串起来。</li></ul><h2 id="qi-shi-pian" tabindex="-1" id="起始篇">起始篇</h2><p>正文从 <a href="/posts/c8797d0d/">vLLM系统拆解-01-架构设计：为什么推理引擎不能是一个进程</a> 开始。</p><hr><h2 id="xi-lie-dao-hang" tabindex="-1" id="系列导航">系列导航</h2><ul><li>下一篇：<a href="/posts/c8797d0d/">vLLM系统拆解-01-架构设计：为什么推理引擎不能是一个进程</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;v-llm-xi-tong-chai-jie-00-dao-du-cong-jia-gou-diao-du-dao-mian-shi-biao-da-de-xue-xi-lu-xian&quot;</summary>
        
      
    
    
    
    <category term="AI Infra" scheme="https://smarter.xin/categories/ai-infra/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/categories/ai-infra/vllm/"/>
    
    
    <category term="AI Infrastructure" scheme="https://smarter.xin/tags/ai-infrastructure/"/>
    
    <category term="vLLM" scheme="https://smarter.xin/tags/vllm/"/>
    
    <category term="大模型推理" scheme="https://smarter.xin/tags/llm-inference/"/>
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="推理系统" scheme="https://smarter.xin/tags/inference-system/"/>
    
  </entry>
  
</feed>
