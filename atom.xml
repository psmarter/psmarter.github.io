<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Smarter&#39;s blog</title>
  
  <subtitle>要学习、要快乐</subtitle>
  <link href="https://smarter.xin/atom.xml" rel="self"/>
  
  <link href="https://smarter.xin/"/>
  <updated>2026-03-16T05:46:43.227Z</updated>
  <id>https://smarter.xin/</id>
  
  <author>
    <name>Smarter</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>CUDA C编程权威指南-第十章：程序实现的注意事项</title>
    <link href="https://smarter.xin/posts/98c27a24/"/>
    <id>https://smarter.xin/posts/98c27a24/</id>
    <published>2026-02-05T11:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-shi-zhang-cheng-xu-shi-xian-de-zhu-yi-shi-xiang" tabindex="-1">CUDA C编程权威指南-第十章：程序实现的注意事项</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/844cd057/">第9章 多GPU编程</a></p><blockquote><p>系列最后一篇（第 10 章）。前面九章把编程模型、执行模型、内存、流、库、多 GPU 都过了一遍；这一章回到<strong>工程实践</strong>：怎么把这些串成可重复的开发与调优流程、怎么调试、怎么从 C 移植到 CUDA，在实际项目里少踩坑、写出既对又快的程序。</p></blockquote><hr><h2 id="qian-yan-cong-xue-dao-yong" tabindex="-1" id="前言：从学到用">前言：从学到用</h2><p>前九章依次覆盖了 CUDA 的编程模型、执行模型、内存层次、共享内存与常量内存、流与并发、指令级原语、GPU 加速库与 OpenACC、多 GPU 编程。这些构成了「怎么写、怎么跑、怎么优化」的完整知识链。但在真实项目中，除了算法与 API，还需要：<strong>一套可重复的开发与调优流程</strong>、<strong>可靠的调试手段</strong>，以及<strong>从现有 C 代码迁移到 CUDA</strong> 的实战经验。</p><p>这一章会说明指出：第 10 章「程序实现的注意事项」不引入新的核心 API，而是将前面各章的知识<strong>串成工程实践</strong>。你将系统学到：开发流程（需求→热点→并行化策略→实现与验证→调优→部署）、剖析驱动的优化（nvprof/Nsight、关键指标、迭代流程）、CUDA 程序的调试（主机/设备端错误处理、cuda-memcheck、Nsight、printf 与单线程调试），以及从 C 到 CUDA 的移植（识别热点、数据与线程划分、常见陷阱与最佳实践）。通过本章，你将对「如何在实际项目中实现与交付 CUDA 程序」形成清晰图景，并为后续进阶打好工程基础。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-cheng-xu-shi-xian-de-zhu-yi-shi-xiang" tabindex="-1" id="1-1-为什么要学「程序实现的注意事项」">1.1 为什么要学「程序实现的注意事项」</h3><p>前几章我们主要在<strong>单 GPU</strong> 上学习怎么写核函数、怎么组织线程与内存、怎么用流与库、怎么扩展到多 GPU。需要留意的是：在实际项目中，<strong>规范的流程</strong>能减少返工、便于团队协作，并确保性能调优有的放矢；<strong>不检查 API 返回值、核函数后不同步就查错</strong>等习惯性疏漏，会导致难以定位的崩溃或错误结果；<strong>一上来就追求性能而忽略正确性</strong>，往往事倍功半。因此，本章回答的是：<strong>如何从「会写 CUDA 代码」进阶到「能在真实项目中正确实现、调试、调优并交付」</strong>。</p><p>学完本章，你应能建立「评估→并行化→优化→部署」的闭环思维，会用剖析工具定位瓶颈并解读关键指标，能系统地进行主机端与设备端错误检查与调试，并掌握从 C 到 CUDA 移植的典型步骤与常见陷阱，避免「仅用单块、忽略错误、未做边界检查」等致命错误。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解 <strong>CUDA C 开发流程</strong> 各阶段及产出</td><td>能说出需求与热点分析→并行化策略→实现与验证→性能调优→集成部署，并说明「能库则库、正确性优先」</td></tr><tr><td>掌握 <strong>APOD</strong> 循环的含义与用法</td><td>能解释 Assess / Parallelize / Optimize / Deploy 各步，并说明为何要「用剖析数据驱动优化对象」</td></tr><tr><td>会用 <strong>nvprof / Nsight</strong> 做快速剖析并解读关键指标</td><td>能说出 achieved_occupancy、gld_efficiency、有效带宽等的含义，以及「占用率是手段而非目的」</td></tr><tr><td>理解 <strong>延迟隐藏与占用率下界</strong> 的定性关系</td><td>能复述「足够活跃线程束以覆盖内存延迟」的思路，以及为何占用率过低往往需增大块或网格</td></tr><tr><td>掌握 <strong>主机端错误检查</strong>：CHECK 宏与同步捕获核函数错误</td><td>能写出 CHECK 宏并说明为何核函数错误需在 cudaDeviceSynchronize 或 cudaMemcpy 后检查</td></tr><tr><td>掌握 <strong>设备端调试</strong>：cuda-memcheck、printf、单线程</td><td>能说明 cuda-memcheck 的用法与局限；何时用 printf、何时用 &lt;&lt;&lt;1,1&gt;&gt;&gt; 做数值对比</td></tr><tr><td>掌握 <strong>从 C 到 CUDA 移植</strong> 的步骤与常见陷阱</td><td>能列出「仅用单块、忽略错误、未做边界检查、主机/设备指针混用、同步理解错误」及对应做法</td></tr><tr><td>会做 <strong>正确性验证</strong>：小数据、单块/单线程、与 CPU 对比</td><td>能写出「先正确再优化」的验证流程与简单回归对比</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">第 10 章  程序实现的注意事项</span><br><span class="line">│</span><br><span class="line">├── 二、CUDA C 开发流程（书 10.1 节）</span><br><span class="line">│   ├── 从需求到并行化策略（热点分析、能库则库）</span><br><span class="line">│   ├── APOD 与迭代优化 ★ 重难点</span><br><span class="line">│   ├── 正确性优先与测试（CHECK 宏与验证骨架）</span><br><span class="line">│   └── 开发阶段与典型产出表</span><br><span class="line">│</span><br><span class="line">├── 三、剖析驱动的性能调优（书 10.2 节）</span><br><span class="line">│   ├── 剖析工具与关键指标（nvprof、Nsight、占用率/内存/计算）</span><br><span class="line">│   ├── 延迟隐藏与占用率下界 ★ 易错点</span><br><span class="line">│   └── 迭代优化流程与有效带宽公式</span><br><span class="line">│</span><br><span class="line">├── 四、CUDA 程序的调试（书 10.3 节）</span><br><span class="line">│   ├── 主机端错误与 API 检查（CHECK、同步捕获核函数错误）</span><br><span class="line">│   ├── 设备端错误与 cuda-memcheck、printf、单线程</span><br><span class="line">│   └── 图形化调试器与 Nsight（-G、条件断点）</span><br><span class="line">│</span><br><span class="line">├── 五、从 C 到 CUDA 的移植（书 10.4 节）</span><br><span class="line">│   ├── 识别热点与可并行性（数据划分与线程组织一致）</span><br><span class="line">│   ├── 常见移植陷阱 ★ 易错点</span><br><span class="line">│   ├── 移植案例：标量循环到核函数</span><br><span class="line">│   └── 最佳实践小结</span><br><span class="line">│</span><br><span class="line">└── 六、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-cuda-c-kai-fa-liu-cheng-shu-10-1-jie" tabindex="-1" id="二、CUDA-C-开发流程（书-10-1-节）">二、CUDA C 开发流程（书 10.1 节）</h2><p>本节对应书中对 CUDA C 程序<strong>从无到有</strong>的完整开发流程的介绍。需要留意的是：规范的流程能减少返工、便于团队协作，并确保性能调优有的放矢。</p><h3 id="2-1-cong-xu-qiu-dao-bing-xing-hua-ce-lue" tabindex="-1" id="2-1-从需求到并行化策略">2.1 从需求到并行化策略</h3><p>开发任何 CUDA 程序的第一步都是<strong>明确需求</strong>：要解决什么问题、数据规模多大、精度与性能目标如何。接下来需要判断<strong>哪些部分适合放在 GPU 上</strong>。通常只有<strong>计算密集型、数据并行</strong>的代码段才值得移植到 GPU；若某段代码在 CPU 上仅占 5% 的运行时间，即使其在 GPU 上加速 10 倍，整体加速比也有限（<strong>阿姆达尔定律</strong>）。因此，<strong>先做热点分析</strong>（用 CPU 剖析工具或经验判断），把时间花在「真正占大头的循环或函数」上，是开发流程中的关键一步。</p><p>在确定要移植的热点之后，需要选择<strong>并行化策略</strong>。与第 1 章、第 2 章呼应，常见策略包括：(1) <strong>使用现成 GPU 库</strong>（如 cuBLAS、cuFFT、cuSPARSE），在满足接口与精度要求的前提下优先考虑；(2) <strong>手写核函数</strong>，当问题高度定制化或库无法直接覆盖时采用。书中建议：<strong>能库则库，不能库再手写</strong>；手写时要从<strong>数据划分</strong>和<strong>线程组织</strong>（网格、块、线程索引）出发，确保逻辑正确再谈优化。</p><p>下表归纳「开发阶段」与典型产出，便于在项目中对照执行：</p><table><thead><tr><th>阶段</th><th>主要活动</th><th>典型产出</th></tr></thead><tbody><tr><td>需求与热点</td><td>明确目标、CPU 热点分析</td><td>热点函数/循环列表、性能基线</td></tr><tr><td>并行化策略</td><td>选库或手写、数据与线程划分设计</td><td>核函数接口与执行配置草案</td></tr><tr><td>实现与验证</td><td>编写主机/设备代码、单元测试</td><td>正确性验证（与 CPU 或小数据对比）</td></tr><tr><td>性能调优</td><td>剖析、定位瓶颈、迭代优化</td><td>优化后内核与性能报告</td></tr><tr><td>集成与部署</td><td>与现有系统集成、发布</td><td>可交付程序与文档</td></tr></tbody></table><h3 id="2-2-apod-yu-die-dai-you-hua-zhong-nan-dian" tabindex="-1" id="2-2-APOD-与迭代优化-★-重难点">2.2 APOD 与迭代优化 ★ 重难点</h3><p>一种常见的 GPU 应用开发与优化循环是 <strong>APOD</strong>（Assess, Parallelize, Optimize, Deploy）。其含义为：</p><ul><li><strong>Assess（评估）</strong>：用剖析工具找出应用中耗时最多的部分，确定并行化候选；建立性能基线（如当前 CPU 运行时间）。</li><li><strong>Parallelize（并行化）</strong>：对候选部分进行 GPU 移植（库或手写核函数），<strong>先保证正确性，再考虑性能</strong>。</li><li><strong>Optimize（优化）</strong>：基于剖析结果迭代优化——内存访问、执行配置、占用率、指令级等（对应第 3～7 章内容）。</li><li><strong>Deploy（部署）</strong>：将优化后的版本集成并发布；之后可以重新 Assess，开始下一轮迭代。</li></ul><p>这一循环强调<strong>数据驱动</strong>：用剖析数据决定「优化哪里」，而不是凭感觉。在资源有限的情况下，应<strong>集中优化对整体性能影响最大的内核或传输</strong>，避免在次要路径上过度优化。</p><blockquote><p><strong>理解与体会</strong>：APOD 把「先正确再快」和「用数据说话」固化成了流程。很多人容易犯「一上来就调 block 大小、改访存」的毛病，结果要么数值不对查半天，要么在无关紧要的内核上花时间。养成「先 Assess 再动手、每次只改一类因素并记录」的习惯，能显著提高调优效率。</p></blockquote><h3 id="2-3-zheng-que-xing-you-xian-yu-ce-shi" tabindex="-1" id="2-3-正确性优先与测试">2.3 正确性优先与测试</h3><p>在开发流程中，<strong>正确性优先于性能</strong>。需要留意的是：先写出能得到与 CPU 或已知结果一致的 GPU 实现，再在此基础上做性能调优。典型做法包括：(1) <strong>小规模数据 + 单块单线程</strong>（如 <code>&lt;&lt;&lt;1, 1&gt;&gt;&gt;</code>）做数值对比，排除并行化带来的逻辑错误；(2) <strong>中间结果写回主机并用 CPU 校验</strong>（如逐元素比较）；(3) <strong>单元测试</strong>：对核函数或关键步骤写小型测试用例，便于回归。</p><p>下面给出一个与书中风格一致的、<strong>带 CHECK 宏与简单验证</strong>的 CUDA 程序骨架，对应「实现与验证」阶段的主流程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        cudaError_t err = (call);                                                   \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                    \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, code: %d, reason: %s\n&quot;</span>, __FILE__, __LINE__, err, \</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                         \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">myKernel</span><span class="params">(<span class="type">const</span> <span class="type">float</span> *in, <span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n)</span><br><span class="line">        out[i] = in[i] * <span class="number">2.0f</span>;  <span class="comment">// 示例：逐元素乘 2</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> n = <span class="number">1024</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *h_in  = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="type">float</span> *h_out = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) h_in[i] = (<span class="type">float</span>)i;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_in = <span class="literal">NULL</span>, *d_out = <span class="literal">NULL</span>;</span><br><span class="line">    CHECK(cudaMalloc(&amp;d_in, nBytes));</span><br><span class="line">    CHECK(cudaMalloc(&amp;d_out, nBytes));</span><br><span class="line">    CHECK(cudaMemcpy(d_in, h_in, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> blockSize = <span class="number">256</span>;</span><br><span class="line">    <span class="type">int</span> gridSize  = (n + blockSize - <span class="number">1</span>) / blockSize;</span><br><span class="line">    myKernel&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_in, d_out, n);</span><br><span class="line">    CHECK(cudaDeviceSynchronize());   <span class="comment">// 同步以捕获核函数错误</span></span><br><span class="line"></span><br><span class="line">    CHECK(cudaMemcpy(h_out, d_out, nBytes, cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 简单验证：out[i] == in[i] * 2</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (h_out[i] != h_in[i] * <span class="number">2.0f</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Mismatch at %d: got %f, expected %f\n&quot;</span>, i, h_out[i], h_in[i] * <span class="number">2.0f</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cudaFree(d_in);</span><br><span class="line">    cudaFree(d_out);</span><br><span class="line">    <span class="built_in">free</span>(h_in);</span><br><span class="line">    <span class="built_in">free</span>(h_out);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译与运行：<code>nvcc -o app app.cu &amp;&amp; ./app</code>。书中 2.1.7 节已说明：核函数启动是<strong>异步</strong>的，因此必须在核函数完成后<strong>同步</strong>（如 <code>cudaDeviceSynchronize()</code> 或一次 D2H 的 <code>cudaMemcpy</code>）再检查返回值，才能捕获核函数内部的错误。</p><hr><h2 id="san-pou-xi-qu-dong-de-xing-neng-diao-you-shu-10-2-jie" tabindex="-1" id="三、剖析驱动的性能调优（书-10-2-节）">三、剖析驱动的性能调优（书 10.2 节）</h2><p>本节对应书中对<strong>性能剖析</strong>与<strong>基于剖析结果进行优化</strong>的讲解。没有剖析就优化，往往事倍功半；剖析能告诉你「时间花在哪里、是计算受限还是内存受限」，从而有针对性地应用前面各章的优化手段。</p><h3 id="3-1-pou-xi-gong-ju-yu-guan-jian-zhi-biao" tabindex="-1" id="3-1-剖析工具与关键指标">3.1 剖析工具与关键指标</h3><p>CUDA 生态中常用的剖析工具包括 <strong>nvprof</strong>（命令行）、<strong>NVIDIA Nsight Systems</strong>（时间线、CPU/GPU 活动）、<strong>NVIDIA Nsight Compute</strong>（内核级详细指标）。<strong>本节要点</strong>：用 nvprof 可快速得到内核耗时与大致瓶颈（如 <code>nvprof ./myapp</code>）；若需更细的指标（如每内核的占用率、内存吞吐、warp 效率），则使用 Nsight Compute。无论使用哪种工具，都应<strong>先确保程序正确</strong>，再在<strong>有代表性的数据规模</strong>下进行剖析，避免小规模或异常路径主导结论。</p><p>下表归纳书中或 CUDA 文档中常见的<strong>性能指标</strong>及其含义，便于在剖析报告中解读：</p><table><thead><tr><th>指标类型</th><th>典型指标</th><th>含义与用途</th></tr></thead><tbody><tr><td>时间</td><td>内核/传输耗时</td><td>定位最耗时的内核或 H2D/D2H 传输</td></tr><tr><td>占用率</td><td>achieved_occupancy</td><td>实际活跃线程束数/理论最大，评估延迟隐藏</td></tr><tr><td>内存</td><td>gld_efficiency 等</td><td>全局加载/存储效率，与合并访问、对齐相关</td></tr><tr><td>计算</td><td>吞吐（GFlops）</td><td>判断计算受限 vs 内存受限</td></tr><tr><td>分支</td><td>warp_execution_efficiency</td><td>受线程束分化影响，越低越需优化控制流</td></tr></tbody></table><p>需要留意的是：<strong>占用率是手段而非目的</strong>——足够的占用率用于隐藏延迟，但盲目追求 100% 占用率可能导致寄存器溢出或共享内存不足，反而降低性能。优化时应结合「瓶颈在哪」：若内存带宽已饱和，再提高占用率对带宽受限内核帮助有限；若瓶颈在计算，则需从指令数、分支、循环展开等方面入手（第 7 章）。</p><p>使用 <strong>nvprof</strong> 进行快速剖析的典型命令如下（与书中及第 2 章用法一致）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvcc -o myapp myapp.cu</span><br><span class="line">nvprof ./myapp</span><br></pre></td></tr></table></figure><p>若需更细的内核级指标，可使用 <code>nvprof --metrics achieved_occupancy,gld_efficiency ./myapp</code> 等。NVIDIA 推荐在新环境中使用 <strong>Nsight Systems</strong>（<code>nsys profile ./myapp</code>）与 <strong>Nsight Compute</strong> 做更完整的分析；各工具有其适用场景与输出解读，可参考 Nsight 文档。</p><h3 id="3-2-yan-chi-yin-cang-yu-zhan-yong-lu-de-xia-jie-yi-cuo-dian" tabindex="-1" id="3-2-延迟隐藏与占用率的下界-★-易错点">3.2 延迟隐藏与占用率的下界 ★ 易错点</h3><p>第 3 章已给出「为隐藏延迟所需的最小线程数」的概念。要隐藏全局内存访问延迟（约 400～800 周期），每个 SM 上需要有足够多的<strong>活跃线程束</strong>，使得在等待内存的周期内，其他线程束可以执行。一个常用的<strong>下界估计</strong>是：每个 SM 上至少需要能「覆盖延迟 × 带宽」的并发度。从线程束数量角度，可写成：</p><p>[<br>\text{所需活跃线程束数} \gtrsim \frac{\text{内存延迟（周期）}}{\text{每线程束完成一次内存请求所需周期}}<br>]</p><p>例如，若内存延迟约 400 周期，而每个线程束平均每 32 周期能发出一批内存请求，则大约需要 (400/32 \approx 12) 个以上的活跃线程束才能较好地隐藏延迟。原书表 3-3 等会给出更具体的数值；此处强调：<strong>剖析得到的 achieved_occupancy 若过低</strong>（如远低于 0.25），往往意味着块太小或每块线程数过少，可尝试增大块大小或网格大小（在资源限制内）以提升占用率，从而更好地隐藏延迟。</p><blockquote><p><strong>难点与学习思考</strong>：不要孤立地「冲高占用率」——若内核已经受限于内存带宽，再增加块内线程数可能导致更多寄存器/共享内存使用，反而降低每 SM 可容纳的块数或引发溢出。第 3 章矩阵求和实验已经说明：<strong>最佳配置不一定是占用率最高的配置</strong>，要与 gld_efficiency、实际带宽一起看，做平衡。</p></blockquote><h3 id="3-3-die-dai-you-hua-liu-cheng-yu-you-xiao-dai-kuan-gong-shi" tabindex="-1" id="3-3-迭代优化流程与有效带宽公式">3.3 迭代优化流程与有效带宽公式</h3><p>典型的迭代优化流程为：(1) 用 nvprof 或 Nsight 找出耗时最高的内核；(2) 查看该内核的 achieved_occupancy、内存效率、计算吞吐；(3) 若内存效率低，则检查访问模式（第 4 章：合并、对齐、bank 冲突）；若占用率低，则检查块大小与资源使用（寄存器、共享内存）；(4) 修改代码后重新编译、运行剖析、对比前后指标。<strong>关键原则</strong>：<strong>每次只改一类因素</strong>，便于归因；并记录每次修改前后的数据，形成可复现的调优记录。</p><p><strong>有效带宽（Effective Bandwidth）</strong> 是评估内存相关优化效果的常用公式，第 2 章、第 4 章均有涉及。对一次内核执行而言：</p><p>[<br>\text{有效带宽（GB/s）} = \frac{(\text{读字节数} + \text{写字节数})}{\text{运行时间（秒）} \times 10^9}<br>]</p><p>单位换算后，若内核的「读+写」总量为 (B) 字节，运行时间为 (T) 秒，则有效带宽为 (B/(T \times 10^9)) GB/s。与设备峰值带宽对比，可得到「带宽利用率」；若接近峰值，说明该内核很可能是<strong>内存受限</strong>，进一步优化应侧重减少传输量或提高合并度，而非单纯增加计算。</p><hr><h2 id="si-cuda-cheng-xu-de-diao-shi-shu-10-3-jie" tabindex="-1" id="四、CUDA-程序的调试（书-10-3-节）">四、CUDA 程序的调试（书 10.3 节）</h2><p>本节对应书中对 <strong>CUDA 程序调试方法</strong> 的介绍。CUDA 的异步执行与主机/设备分离，会使错误表现得不直观；掌握系统化的调试手段能显著提高排错效率。</p><h3 id="4-1-zhu-ji-duan-cuo-wu-yu-api-jian-cha" tabindex="-1" id="4-1-主机端错误与-API-检查">4.1 主机端错误与 API 检查</h3><p>所有 CUDA 运行时 API（如 <code>cudaMalloc</code>、<code>cudaMemcpy</code>、核函数启动的同步包装）都会返回 <code>cudaError_t</code>。<strong>不检查返回值</strong>是实践中最常见的错误来源之一：例如设备内存不足时 <code>cudaMalloc</code> 返回 <code>cudaErrorMemoryAllocation</code>，若忽略则后续可能出现非法访问或难以理解的崩溃。需要留意的是：<strong>每个 API 调用都应用错误处理宏包裹</strong>（如前文及第 2 章的 CHECK 宏），在调试版本中至少打印出错位置与 <code>cudaGetErrorString(err)</code>。</p><p>核函数启动是<strong>异步</strong>的，因此内核内部的错误（如非法内存访问、断言失败）不会在 <code>kernel&lt;&lt;&lt;...&gt;&gt;&gt;()</code> 返回时立即反映到主机；主机得到的 <code>cudaSuccess</code> 只表示「启动请求已提交」。要捕获核函数错误，必须在核函数完成后<strong>同步</strong>一次，例如 <code>cudaDeviceSynchronize()</code> 或 <code>cudaMemcpy(..., cudaMemcpyDeviceToHost)</code>，再检查返回值。书中建议：在开发与调试阶段，在关键步骤后加上 <code>CHECK(cudaDeviceSynchronize())</code>，便于尽早发现设备端错误。</p><p>下表归纳书中提到的主机端常见错误类型与应对：</p><table><thead><tr><th>错误类型</th><th>典型原因</th><th>应对建议</th></tr></thead><tbody><tr><td>内存分配失败</td><td>设备内存不足、碎片</td><td>检查分配大小、减少峰值占用或分批</td></tr><tr><td>无效参数</td><td>指针为 NULL、size 为 0</td><td>检查传入参数与前置逻辑</td></tr><tr><td>未初始化/设备不可用</td><td>驱动与运行时未正确初始化</td><td>检查环境、驱动版本、当前设备</td></tr><tr><td>核函数错误</td><td>非法访问、断言、未同步</td><td>同步后检查、使用 cuda-memcheck</td></tr></tbody></table><blockquote><p><strong>理解与体会</strong>：很多人第一次写 CUDA 时会在「为什么 cudaMalloc 成功了但后面崩了」上栽跟头——其实是更早的某次 API 已经失败却没检查。养成「所有 CUDA API 都用 CHECK 包一层、核函数后加一次同步再查错」的习惯，能省下大量无谓的排查时间。</p></blockquote><h3 id="4-2-she-bei-duan-cuo-wu-yu-cuda-memcheck" tabindex="-1" id="4-2-设备端错误与-cuda-memcheck">4.2 设备端错误与 cuda-memcheck</h3><p>设备端错误（如越界访问、未对齐访问）往往表现为「某次运行崩溃」或「结果偶尔错误」，难以仅靠主机端 API 返回值定位。<strong>cuda-memcheck</strong>（或 CUDA 工具包中的 memcheck 工具）用法：在程序前加前缀运行，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cuda-memcheck ./myapp</span><br></pre></td></tr></table></figure><p>工具会报告非法内存访问、未对齐访问等问题的近似位置（如某核函数、某块某线程），便于缩小排查范围。<strong>打开 memcheck 时程序会变慢</strong>，因此适合在调试阶段、小数据或单次运行下使用，不适合做大规模性能测试。</p><p>此外，<strong>核函数内 printf</strong>（Fermi 及以后架构支持）和<strong>单线程执行</strong>（<code>&lt;&lt;&lt;1, 1&gt;&gt;&gt;</code>）是两种简单实用的调试手段：printf 可输出线程索引与关键变量，单线程执行可将核函数退化为串行，便于与 CPU 结果逐点对比。printf 会严重影响性能且可能改变执行顺序，仅用于调试，发布前应移除或条件编译。</p><h3 id="4-3-tu-xing-hua-diao-shi-qi-yu-nsight" tabindex="-1" id="4-3-图形化调试器与-Nsight">4.3 图形化调试器与 Nsight</h3><p>对于需要单步、断点、查看变量的应用，<strong>NVIDIA Nsight</strong> 系列（如 Nsight Visual Studio Edition、Nsight Systems、Nsight Compute）的调试能力：在主机端和设备端设断点、查看设备内存与变量、与时间线结合分析。使用图形化调试器时，通常需要以<strong>调试模式</strong>编译（如 <code>-G</code> 生成设备调试信息），并注意多线程/多块下的断点行为（例如条件断点只对特定 blockIdx/threadIdx 生效）。需要留意的是：调试版本不应用于性能评估，优化时要用 Release 构建并配合剖析工具。</p><hr><h2 id="wu-cong-c-dao-cuda-de-yi-zhi-an-li-yu-zui-jia-shi-jian-shu-10-4-jie" tabindex="-1" id="五、从-C-到-CUDA-的移植：案例与最佳实践（书-10-4-节）">五、从 C 到 CUDA 的移植：案例与最佳实践（书 10.4 节）</h2><p>本节对应书中对<strong>将现有 C（或 C++）程序移植到 CUDA</strong> 的流程、常见陷阱与案例的讲解。移植不是简单地把循环改成核函数，而是要<strong>识别数据并行、设计线程划分、处理数据依赖与边界</strong>，并避免一些典型错误。</p><h3 id="5-1-shi-bie-re-dian-yu-ke-bing-xing-xing" tabindex="-1" id="5-1-识别热点与可并行性">5.1 识别热点与可并行性</h3><p>移植的第一步是<strong>找到值得移植的热点</strong>。通常用 CPU 剖析工具（如 gprof、VTune、Visual Studio Profiler）找出占用时间最多的函数或循环；若某段代码占总时间比例很小，移植到 GPU 的收益有限。其次，分析该段代码的<strong>数据相关性</strong>：若循环迭代之间相互独立（仅读共享输入、写独立输出），则适合做数据并行；若存在严重的跨迭代依赖（如紧耦合的递推），可能需要算法级改造或仅对部分阶段做 GPU 加速。</p><p><strong>数据划分</strong>必须与<strong>线程组织</strong>一致。例如，对一维数组的逐元素操作，常用全局索引 (i = \text{blockIdx.x} \times \text{blockDim.x} + \text{threadIdx.x})，并在线程内做边界判断 <code>if (i &lt; n)</code>。对二维问题，可扩展为二维网格与块，或在一维索引下用 <code>i = row \times \text&#123;width&#125; + col</code> 计算行列。与第 2 章一致：先保证「每个数据元素由哪个线程处理」定义清晰且无遗漏、无重复，再考虑优化执行配置与内存访问。</p><h3 id="5-2-chang-jian-yi-zhi-xian-jing-yi-cuo-dian" tabindex="-1" id="5-2-常见移植陷阱-★-易错点">5.2 常见移植陷阱 ★ 易错点</h3><p><strong>常见移植陷阱</strong>归纳如下表，便于对照避免：</p><table><thead><tr><th>陷阱</th><th>表现与原因</th><th>建议做法</th></tr></thead><tbody><tr><td>仅用单块</td><td>无法利用多 SM，GPU 利用率极低</td><td>用网格覆盖全部数据，块数 ≥ SM 数倍数</td></tr><tr><td>忽略错误检查</td><td>分配失败或内核错误被忽略，难以定位</td><td>所有 API 用 CHECK 等宏包裹，调试时加同步检查</td></tr><tr><td>未做边界检查</td><td>数据规模不能被块大小整除时越界</td><td>核函数内使用 <code>if (i &lt; n)</code> 等保护</td></tr><tr><td>主机/设备指针混用</td><td>在设备代码中使用主机指针或反之</td><td>严格区分 h_/d_ 命名与拷贝时机</td></tr><tr><td>同步理解错误</td><td>以为核函数返回即执行完毕</td><td>需要结果时用 cudaMemcpy 或 cudaDeviceSynchronize</td></tr></tbody></table><p>书中还会提到：<strong>首次移植应先追求正确性</strong>——例如先用小数据、单块或少量块验证数值，再逐步增大规模与块数；同时保留一份 CPU 参考实现，便于回归对比。</p><blockquote><p><strong>难点与学习思考</strong>：「仅用单块」和「忽略错误检查」是初学者最容易犯的两个致命错误。前者会导致 GPU 只动用一块 SM，性能甚至可能不如 CPU；后者会让 cudaMalloc 失败或内核越界等问题被掩盖，直到在别的代码路径或数据规模下才暴露，排查成本很高。移植时务必：网格覆盖全数据、核函数内边界判断、所有 API 检查、核函数后同步查错。</p></blockquote><h3 id="5-3-yi-zhi-an-li-biao-liang-xun-huan-dao-he-han-shu" tabindex="-1" id="5-3-移植案例：标量循环到核函数">5.3 移植案例：标量循环到核函数</h3><p>下面给出一个与书中风格一致的<strong>简单移植案例</strong>：将 C 中的标量循环「数组每元素乘 2 并加 1」改为 CUDA 核函数，并保持与 CPU 结果一致。对应「从 C 到 CUDA」的典型步骤：识别独立循环 → 用线程索引替代循环变量 → 设备分配与拷贝 → 启动核函数 → 拷回并验证。</p><p><strong>原始 C 循环（书中风格）：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    out[i] = in[i] * <span class="number">2.0f</span> + <span class="number">1.0f</span>;</span><br></pre></td></tr></table></figure><p><strong>对应的 CUDA 核函数与主机调用：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">scaleAndShift</span><span class="params">(<span class="type">const</span> <span class="type">float</span> *in, <span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n)</span><br><span class="line">        out[i] = in[i] * <span class="number">2.0f</span> + <span class="number">1.0f</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 主机端：分配 d_in, d_out，H2D 拷贝 in -&gt; d_in</span></span><br><span class="line"><span class="type">int</span> blockSize = <span class="number">256</span>;</span><br><span class="line"><span class="type">int</span> gridSize  = (n + blockSize - <span class="number">1</span>) / blockSize;</span><br><span class="line">scaleAndShift&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_in, d_out, n);</span><br><span class="line"><span class="comment">// 然后 D2H 拷贝 d_out -&gt; out，并与 CPU 结果比较</span></span><br></pre></td></tr></table></figure><p>这里「每个 i 对应一个线程」且彼此无依赖，是典型的数据并行。执行配置使用常见的 256 线程/块，网格大小按 (n) 向上取整，核函数内用 <code>if (i &lt; n)</code> 做边界保护。在真实项目中，移植后要用<strong>相同输入</strong>在 CPU 与 GPU 上各算一遍，逐元素或统计误差比较，确认无误后再做性能调优（如调整 blockSize、检查合并访问等）。</p><h3 id="5-4-zui-jia-shi-jian-xiao-jie" tabindex="-1" id="5-4-最佳实践小结">5.4 最佳实践小结</h3><p>下表可作为「程序实现的注意事项」的速查表：</p><table><thead><tr><th>类别</th><th>最佳实践要点</th></tr></thead><tbody><tr><td>开发流程</td><td>先热点分析再并行化；能库则库；正确性优先，再迭代优化（APOD）。</td></tr><tr><td>错误处理</td><td>所有 CUDA API 用宏检查返回值；调试时在核函数后同步以捕获设备端错误。</td></tr><tr><td>剖析与调优</td><td>用 nvprof/Nsight 定位瓶颈；关注占用率、内存效率与计算吞吐；每次改一类因素。</td></tr><tr><td>移植</td><td>识别独立循环与数据划分；核函数内做边界检查；保留 CPU 参考实现做回归。</td></tr><tr><td>资源与配置</td><td>网格/块覆盖全部数据，避免仅单块；在寄存器/共享内存限制内选择块大小。</td></tr></tbody></table><hr><h2 id="liu-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="六、本章小结与重难点回顾">六、本章小结与重难点回顾</h2><p>本章系统梳理了<strong>程序实现的注意事项</strong>，将前几章的知识落实到工程流程、剖析、调试与移植中。下面用表格与要点形式做小结，便于复习与自测。</p><h3 id="6-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="6-1-知识小结（与书中对应）">6.1 知识小结（与书中对应）</h3><table><thead><tr><th>主题</th><th>要点</th></tr></thead><tbody><tr><td><strong>开发流程</strong></td><td>需求与热点分析→并行化策略（库优先）→实现与验证→性能调优→集成部署；APOD。</td></tr><tr><td><strong>剖析驱动调优</strong></td><td>用 nvprof/Nsight 找瓶颈；占用率、内存效率、有效带宽；单因子改动、记录前后数据。</td></tr><tr><td><strong>调试</strong></td><td>主机端：CHECK 宏、同步后查错；设备端：cuda-memcheck、printf、单线程；Nsight。</td></tr><tr><td><strong>C 到 CUDA 移植</strong></td><td>识别热点与可并行性；数据与线程划分一致；避免单块、忽略错误、越界；先正确再优化。</td></tr></tbody></table><h3 id="6-2-zhong-nan-dian-su-cha" tabindex="-1" id="6-2-重难点速查">6.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>APOD</strong></td><td>Assess→Parallelize→Optimize→Deploy；用剖析数据驱动优化对象；先正确再快。</td></tr><tr><td><strong>占用率</strong></td><td>手段而非目的；足够即可隐藏延迟，盲目追求可能溢出；与内存/计算瓶颈一起看。</td></tr><tr><td><strong>核函数错误捕获</strong></td><td>启动异步，必须 cudaDeviceSynchronize 或 D2H cudaMemcpy 后再检查 cudaGetLastError。</td></tr><tr><td><strong>移植陷阱</strong></td><td>仅用单块、忽略错误、未做边界检查、主机/设备指针混用、误解同步；对应做法见 5.2 节表。</td></tr><tr><td><strong>有效带宽</strong></td><td>(读字节+写字节)/(时间×10^9) GB/s；与峰值对比判断内存/计算受限；指导优化方向。</td></tr></tbody></table><h3 id="6-3-xue-xi-si-kao" tabindex="-1" id="6-3-学习思考">6.3 学习思考</h3><ul><li><strong>与第 2 章的衔接</strong>：第 2 章给出了 CHECK 宏、核函数后同步查错、nvprof 与有效带宽的初次使用；本章把这些纳入「开发流程」和「调试」规范，并强调正确性优先与 APOD 循环，形成从入门到工程的闭环。</li><li><strong>与第 3～7 章的关系</strong>：剖析得到的占用率、内存效率、分支效率等，直接对应第 3 章（执行模型、延迟隐藏）、第 4 章（合并访问、带宽）、第 5 章（共享内存、bank 冲突）、第 7 章（指令级优化）；本章强调「用剖析驱动」而不是盲目调参，每次只改一类因素便于归因。</li><li><strong>实践建议</strong>：(1) 新项目一开始就养成「所有 API 用 CHECK、关键路径后同步查错」的习惯；(2) 移植时先小数据+单块或 &lt;&lt;&lt;1,1&gt;&gt;&gt; 验证数值，再放大规模；(3) 调优前先跑 nvprof 或 Nsight 确定瓶颈再动手，并记录每次改动前后的指标；(4) 保留 CPU 参考实现与单元测试，便于回归。</li></ul><hr><h2 id="xi-lie-wan-jie-yu-hou-xu-fang-xiang" tabindex="-1" id="系列完结与后续方向">系列完结与后续方向</h2><p>本系列博客共 10 章，对应《CUDA C编程权威指南》全书。第 10 章为最后一章；至此，从<strong>异构并行概念</strong>到<strong>多 GPU 与集群</strong>、再到<strong>程序实现与移植</strong>的完整学习路径已全部覆盖。</p><p>若希望继续深入，可参考以下方向：</p><ul><li><strong>进阶主题</strong>：更复杂的多流与多 GPU 负载均衡（第 6、9 章）、CUDA 与 MPI 的深度结合、领域库（如 cuDNN、TensorRT）在具体应用中的使用。</li><li><strong>官方资源</strong>：NVIDIA CUDA 编程指南、Best Practices Guide、各库文档与样例代码。</li><li><strong>实践建议</strong>：在实际项目中重复「剖析→优化→部署」的循环，并积累自己的移植与调优案例。</li></ul><p>从第一章的「为什么需要 GPU」到本章的「如何实现与交付 CUDA 程序」——感谢你完成本系列的学习，祝你在 GPU 编程之路上越走越稳。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>APOD 各字母代表什么？为何强调「用剖析驱动优化」？</strong></li><li><strong>为什么核函数内部出错时，主机端在 kernel 返回后立刻检查 cudaGetLastError 可能仍得到 cudaSuccess？应如何正确捕获？</strong></li><li><strong>从 C 移植到 CUDA 时，常见陷阱有哪些？至少写出三条。</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>Assess（评估）、Parallelize（并行化）、Optimize（优化）、Deploy（部署）。用剖析驱动可避免盲目调参，先看时间花在哪里、是内存还是计算受限，再针对性应用第 3～7 章的优化手段，每次只改一类因素便于归因。</li><li>核函数启动是异步的，主机在启动后立即返回，此时设备可能尚未执行或尚未报错；核函数内的错误要等设备执行到该处才会发生。正确做法：在 kernel 后调用 <code>cudaDeviceSynchronize()</code> 或一次 D2H 的 <code>cudaMemcpy</code>，再检查返回值，才能捕获设备端错误。</li><li>常见陷阱包括：仅用单块导致规模受限、忽略 API 返回值导致难以定位崩溃、未做边界检查导致越界、主机/设备指针混用、误以为 kernel 返回即执行完毕而不同步就使用结果、未做正确性验证就优化等。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/844cd057/">第9章 多GPU编程</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 10 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-shi-zhang-cheng-xu-shi-xian-de-zhu-yi-shi-xiang&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="调试" scheme="https://smarter.xin/tags/debugging/"/>
    
    <category term="性能剖析" scheme="https://smarter.xin/tags/performance-profiling/"/>
    
    <category term="移植" scheme="https://smarter.xin/tags/portability/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第九章：多GPU编程</title>
    <link href="https://smarter.xin/posts/844cd057/"/>
    <id>https://smarter.xin/posts/844cd057/</id>
    <published>2026-02-05T10:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-jiu-zhang-duo-gpu-bian-cheng" tabindex="-1">CUDA C编程权威指南-第九章：多GPU编程</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/e466b0a5/">第8章 GPU加速库和OpenACC</a> | 下一篇：<a href="https://smarter.xin/posts/98c27a24/">第10章 程序实现的注意事项</a></p><blockquote><p>系列第 9 篇。之前都在单 GPU 上做文章；这一章扩展到<strong>多 GPU</strong>：单机多设备管理、在多卡上划分计算、点对点通信与跨设备同步，再延伸到 CUDA-aware MPI 和集群，从单卡到多卡、单机到多机串成一条线。</p></blockquote><hr><h2 id="qian-yan-cong-dan-gpu-dao-duo-gpu" tabindex="-1" id="前言：从单-GPU-到多-GPU">前言：从单 GPU 到多 GPU</h2><p>到目前为止，书中大部分示例都只使用<strong>一块 GPU</strong>。但在实际的高性能计算与数据中心环境中，单机多卡、多机多卡已成为常态。<strong>多 GPU 编程</strong>能让应用突破单卡算力与显存上限：通过<strong>聚合多块 GPU 的算力与带宽</strong>处理更大规模的问题，或通过<strong>任务/数据并行</strong>缩短总执行时间。这一章会说明：CUDA 为多 GPU 编程提供了完整支持——从单进程内多设备管理、点对点（P2P）内存访问与传输、到跨 GPU 的流与事件同步，再到基于 <strong>CUDA-aware MPI</strong> 与 <strong>GPUDirect RDMA</strong> 的集群级扩展。掌握多设备下的「当前设备」语义、P2P 的启用与使用、跨设备同步的 <strong>cudaStreamWaitEvent</strong>，以及多节点下的 MPI+GPU 数据交换，是写出可扩展、高性能多 GPU 应用的关键；本章与第 2 章（性能模型）、第 6 章（流与事件）紧密衔接。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-duo-gpu-bian-cheng" tabindex="-1" id="1-1-为什么要学「多-GPU-编程」">1.1 为什么要学「多 GPU 编程」</h3><p>前几章我们主要在<strong>单 GPU</strong> 上优化：编程模型、执行模型、内存层次、流与并发、指令级原语、以及用库与 OpenACC 获得加速。需要搞清楚的是指出：在实际的 HPC 与数据中心中，<strong>单机多卡、多机多卡</strong>已是常态；多 GPU 编程能突破单卡算力与显存上限，通过聚合多块 GPU 的算力与带宽处理更大规模问题，或通过任务/数据并行缩短总时间。<strong>多 GPU 的第一步</strong>是正确理解「当前设备」：<code>cudaSetDevice</code> 决定后续 <code>cudaMalloc</code>、核函数启动、流与事件所绑定的设备；<strong>点对点（P2P）</strong> 则允许设备间直接传输或直接访问对方内存，在支持 NVLink/PCIe P2P 的拓扑上显著降低延迟、提高带宽。跨设备同步依赖 <strong>cudaStreamWaitEvent</strong>（流可等待另一设备上的事件）；多节点则依赖 <strong>CUDA-aware MPI</strong> 与 <strong>GPUDirect RDMA</strong>。因此，理解多设备管理、P2P 的检查与启用、跨设备同步模式、多设备上的数据划分与计算-通信重叠，以及集群扩展时的进程-设备映射与亲和性，是写出可扩展、高性能多 GPU 应用的关键一步。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解<strong>当前设备</strong>对分配、启动、流与事件的约束</td><td>能说明 <code>cudaSetDevice</code> 后 cudaMalloc/核函数/流/事件与哪块设备绑定；为何核函数必须发往当前设备的流</td></tr><tr><td>掌握<strong>多设备下流、事件与 cudaMemcpy</strong> 的行为表</td><td>能说出哪些操作可跨设备、哪些会失败（如 EventRecord 跨设备失败、StreamWaitEvent 可跨设备）</td></tr><tr><td>掌握 <strong>P2P</strong> 的检查与启用：<code>cudaDeviceCanAccessPeer</code>、<code>cudaDeviceEnablePeerAccess</code></td><td>能写出双向 P2P 启用的代码；说明「当前设备」与「peer 设备」的关系</td></tr><tr><td>掌握 <strong>cudaMemcpyPeer</strong> / <strong>cudaMemcpyPeerAsync</strong> 及 UVA 下 <code>cudaMemcpyDefault</code></td><td>能说明何时用 Peer、何时可用 Default；P2P 传输与「经主机中转」的带宽差异</td></tr><tr><td>掌握<strong>跨设备同步</strong>：<code>cudaStreamWaitEvent(streamB, eventA, 0)</code></td><td>能写出「设备 B 的流等待设备 A 的事件」的完整流程</td></tr><tr><td>理解<strong>多设备计算划分</strong>：每设备独立内存与流、数据按设备划分、锁页主机内存</td><td>能写出两卡并行向量加等示例；说明粗粒度与细粒度划分的取舍</td></tr><tr><td>理解<strong>计算与通信重叠</strong>：多流 + 异步 P2P + 事件同步</td><td>能描述「传输流」与「计算流」如何用事件衔接，以隐藏设备间传输延迟</td></tr><tr><td>理解 <strong>CUDA-aware MPI</strong> 与 <strong>GPUDirect RDMA</strong> 的定位</td><td>能说明为何直接传设备指针可简化代码并利于 RDMA；集群扩展时进程-设备映射</td></tr><tr><td>了解 <strong>CPU 与 GPU 亲和性</strong> 对多 GPU 性能的影响</td><td>能说出将线程/进程绑定到靠近目标 GPU 的 CPU 可减少延迟、提高带宽</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">第 9 章  多 GPU 编程</span><br><span class="line">│</span><br><span class="line">├── 二、从一个 GPU 到多 GPU（书 9.1 节）★ 重难点</span><br><span class="line">│   ├── 在多 GPU 上执行（设备枚举、cudaSetDevice、当前设备语义）</span><br><span class="line">│   ├── 多设备下的流、事件与内存拷贝行为表 ★ 易错点</span><br><span class="line">│   ├── 点对点通信（检查与启用、cudaMemcpyPeer、UVA 与 P2P）</span><br><span class="line">│   └── 多 GPU 间同步（cudaStreamWaitEvent 跨设备）</span><br><span class="line">│</span><br><span class="line">├── 三、多 GPU 间细分计算（书 9.2 节）</span><br><span class="line">│   ├── 在多设备上分配内存与资源</span><br><span class="line">│   ├── 数据划分与负载均衡（粗/细粒度、强扩展与通信开销）</span><br><span class="line">│   └── 完整示例：多设备并行向量加法</span><br><span class="line">│</span><br><span class="line">├── 四、计算与通信的重叠（书 9.3 节）</span><br><span class="line">│   ├── 多 GPU 上重叠传输与计算</span><br><span class="line">│   └── 事件驱动与流水线/双缓冲模式</span><br><span class="line">│</span><br><span class="line">├── 五、使用 CUDA-aware MPI 交换数据（书 9.4 节）</span><br><span class="line">│   ├── 传统 MPI 与 CUDA-aware MPI 的对比</span><br><span class="line">│   └── 设备指针直接参与 MPI 通信</span><br><span class="line">│</span><br><span class="line">├── 六、在 GPU 加速集群上扩展应用（书 9.5 节）</span><br><span class="line">│   ├── 进程/线程与 GPU 的映射、数据与计算划分</span><br><span class="line">│   ├── GPUDirect RDMA 与 NCCL 简介</span><br><span class="line">│   └── 强扩展效率与通信开销</span><br><span class="line">│</span><br><span class="line">├── 七、CPU 与 GPU 亲和性（书 9.6 节）</span><br><span class="line">│   └── 绑定线程/进程到靠近目标 GPU 的 CPU</span><br><span class="line">│</span><br><span class="line">└── 八、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-cong-yi-ge-gpu-dao-duo-gpu-shu-9-1-jie" tabindex="-1" id="二、从一个-GPU-到多-GPU（书-9-1-节）">二、从一个 GPU 到多 GPU（书 9.1 节）</h2><p>本节对应书中对多 GPU 编程的入门：如何在程序中管理多块 GPU、在每块 GPU 上执行核函数，以及多设备下的流、事件与内存拷贝行为。<strong>多 GPU 编程的第一步是正确枚举设备、设置当前设备，并理解「当前设备」对后续分配与启动的约束</strong>。</p><h3 id="2-1-zai-duo-gpu-shang-zhi-xing" tabindex="-1" id="2-1-在多-GPU-上执行">2.1 在多 GPU 上执行</h3><p>在单 GPU 程序中，默认使用设备 0；所有 <code>cudaMalloc</code>、核函数启动、流与事件的创建都发生在该设备上。若要使用多块 GPU，必须<strong>显式选择当前设备</strong>，并在每次为某块 GPU 分配资源或发起计算前调用 <code>cudaSetDevice(deviceId)</code>。</p><h4 id="she-bei-mei-ju-yu-shu-xing-cha-xun" tabindex="-1" id="设备枚举与属性查询">设备枚举与属性查询</h4><p>使用 <code>cudaGetDeviceCount</code> 获取系统中可用的 CUDA 设备数量，然后对每个设备调用 <code>cudaGetDeviceProperties</code> 查询属性。典型用法与 CUDA 编程指南一致，如下所示：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                           \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                     \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                   \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, code: %d, reason: %s\n&quot;</span>, __FILE__, __LINE__, err, \</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                        \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                          \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> deviceCount = <span class="number">0</span>;</span><br><span class="line">    CHECK(cudaGetDeviceCount(&amp;deviceCount));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> device = <span class="number">0</span>; device &lt; deviceCount; device++) &#123;</span><br><span class="line">        cudaDeviceProp deviceProp;</span><br><span class="line">        CHECK(cudaGetDeviceProperties(&amp;deviceProp, device));</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Device %d: %s, Compute %d.%d, SM count %d\n&quot;</span>,</span><br><span class="line">               device, deviceProp.name, deviceProp.major, deviceProp.minor,</span><br><span class="line">               deviceProp.multiProcessorCount);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译与运行：<code>nvcc -o deviceQuery deviceQuery.cu &amp;&amp; ./deviceQuery</code>。在多卡机器上会列出每块 GPU 的名称、计算能力与多处理器数量。</p><h4 id="she-bei-xuan-ze-yu-dang-qian-she-bei" tabindex="-1" id="设备选择与当前设备">设备选择与当前设备</h4><p><strong><code>cudaSetDevice(int deviceId)</code></strong> 将指定设备设为当前主机线程的<strong>当前设备</strong>。此后，直到再次调用 <code>cudaSetDevice</code> 为止：</p><ul><li><strong><code>cudaMalloc</code></strong> 在该设备上分配内存；</li><li><strong>核函数启动</strong>（如 <code>kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(...)</code>）在该设备上执行；</li><li><strong><code>cudaStreamCreate</code></strong>、<strong><code>cudaEventCreate</code></strong> 创建的流和事件与该设备关联。</li></ul><p>这一章会说明：若不调用 <code>cudaSetDevice</code>，当前设备默认为 <strong>0</strong>。下面这段代码演示了在两块 GPU 上分别分配内存并启动同一核函数（与书中及 CUDA 编程指南 3.4.1.2 一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">MyKernel</span><span class="params">(<span class="type">float</span> *data)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    data[i] = (<span class="type">float</span>)i;  <span class="comment">// 示例：写入线程索引</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> size = <span class="number">1024</span> * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    <span class="type">float</span> *d_p0 = <span class="literal">NULL</span>;</span><br><span class="line">    cudaMalloc(&amp;d_p0, size);</span><br><span class="line">    MyKernel&lt;&lt;&lt;<span class="number">4</span>, <span class="number">256</span>&gt;&gt;&gt;(d_p0);</span><br><span class="line"></span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    <span class="type">float</span> *d_p1 = <span class="literal">NULL</span>;</span><br><span class="line">    cudaMalloc(&amp;d_p1, size);</span><br><span class="line">    MyKernel&lt;&lt;&lt;<span class="number">4</span>, <span class="number">256</span>&gt;&gt;&gt;(d_p1);</span><br><span class="line"></span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    cudaFree(d_p0);</span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    cudaFree(d_p1);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>要点：每次切换设备后，后续的分配与启动都作用于<strong>新当前设备</strong>；流和事件也<strong>与创建时所在设备绑定</strong>。</p><blockquote><p><strong>理解与体会</strong>：「当前设备」是主机线程的局部状态，不是全局的。同一进程内不同主机线程可以各自 <code>cudaSetDevice(i)</code> 控制不同的当前设备，从而实现多线程各自驱动一块 GPU；若单线程使用多 GPU，则必须在每次为某块 GPU 分配资源或发起计算前显式切换当前设备，否则极易出现「以为在 GPU 1 上分配，实际在 GPU 0 上」的隐蔽错误。</p></blockquote><h4 id="2-1-1-duo-she-bei-xia-de-liu-shi-jian-yu-nei-cun-kao-bei-yi-cuo-dian" tabindex="-1" id="2-1-1-多设备下的流、事件与内存拷贝-★-易错点">2.1.1 多设备下的流、事件与内存拷贝 ★ 易错点</h4><p>CUDA 编程指南强调以下行为（多设备下流、事件与内存拷贝的规则，下表与之对应）：</p><table><thead><tr><th>操作</th><th>多设备下的行为</th></tr></thead><tbody><tr><td><strong>核函数启动</strong></td><td>必须发往<strong>与当前设备关联</strong>的流；若流属于另一设备，启动会<strong>失败</strong>。</td></tr><tr><td><strong>cudaMemcpy</strong></td><td>即使当前设备与流所属设备不同，<strong>跨设备拷贝</strong>仍可成功（运行时会在正确的设备上执行拷贝）。</td></tr><tr><td><strong>cudaEventRecord</strong></td><td>若事件与流<strong>属于不同设备</strong>，则 <strong>失败</strong>。</td></tr><tr><td><strong>cudaEventElapsedTime</strong></td><td>若两个事件<strong>属于不同设备</strong>，则 <strong>失败</strong>。</td></tr><tr><td><strong>cudaEventSynchronize / cudaEventQuery</strong></td><td>即使事件属于非当前设备，也可<strong>成功</strong>（用于跨设备同步）。</td></tr><tr><td><strong>cudaStreamWaitEvent</strong></td><td>即使流与事件属于<strong>不同设备</strong>也可<strong>成功</strong>，因此可用于<strong>跨设备同步</strong>。</td></tr></tbody></table><p>因此，<strong><code>cudaStreamWaitEvent</code></strong> 是实现「设备 B 上的流等待设备 A 上的事件」的关键 API，书中在多 GPU 同步一节会用到。</p><p>每块设备有<strong>各自的默认流</strong>；发往设备 0 的默认流的命令与发往设备 1 的默认流的命令可以<strong>乱序或并发</strong>执行，彼此不阻塞。</p><blockquote><p><strong>易错点</strong>：<strong>核函数启动必须发往与当前设备关联的流</strong>。若当前设备是 0 却把 kernel 发往在设备 1 上创建的流，启动会失败。<strong>cudaEventRecord</strong> 时若事件与流属于不同设备则失败；<strong>cudaEventElapsedTime</strong> 若两个事件属于不同设备则失败。跨设备同步只能依赖「流等待另一设备上的事件」——即 <strong>cudaStreamWaitEvent</strong>，这是多 GPU 同步的核心 API，务必熟记。</p></blockquote><hr><h3 id="2-2-dian-dui-dian-tong-xin" tabindex="-1" id="2-2-点对点通信">2.2 点对点通信</h3><p>在多 GPU 之间交换数据时，若不经特殊处理，数据往往需要<strong>先从一个 GPU 拷回主机，再从主机拷到另一块 GPU</strong>（两次 PCIe 传输、经过主机内存）。<strong>点对点（Peer-to-Peer, P2P）</strong> 机制允许两块 GPU <strong>直接</strong>在设备内存之间传输数据（或直接访问对方内存），在支持 P2P 的拓扑（如通过 NVLink 或同一 PCIe 根下的 PCIe P2P）上可以显著降低延迟并提高带宽。书中将点对点分为<strong>点对点传输</strong>和<strong>点对点内存访问</strong>两类。</p><h4 id="jian-cha-yu-qi-yong-dian-dui-dian-fang-wen" tabindex="-1" id="检查与启用点对点访问">检查与启用点对点访问</h4><p><strong><code>cudaDeviceCanAccessPeer(int *canAccessPeer, int device, int peerDevice)</code></strong>：查询 <code>device</code> 是否可以直接访问 <code>peerDevice</code> 的设备内存。若拓扑支持（例如两块 GPU 通过 NVLink 或 PCIe P2P 相连），则 <code>*canAccessPeer == 1</code>。</p><p><strong><code>cudaDeviceEnablePeerAccess(int peerDevice, unsigned int flags)</code></strong>：在当前设备上启用对 <code>peerDevice</code> 的<strong>单向</strong>点对点访问。调用前需先 <code>cudaSetDevice(currentDevice)</code>；<code>flags</code> 通常传 0。这一章会说明：启用后，<strong>当前设备</strong>上的核函数可以<strong>直接读写</strong> <code>peerDevice</code> 上已分配的内存（需在支持 P2P 访问的架构上）。若需双向访问，两个设备上都要调用一次（例如设备 0 对设备 1 启用、设备 1 对设备 0 启用）。</p><p>典型用法（与书中及编程指南一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> canAccessPeer01 = <span class="number">0</span>, canAccessPeer10 = <span class="number">0</span>;</span><br><span class="line">cudaDeviceCanAccessPeer(&amp;canAccessPeer01, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">cudaDeviceCanAccessPeer(&amp;canAccessPeer10, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (canAccessPeer01 &amp;&amp; canAccessPeer10) &#123;</span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    cudaDeviceEnablePeerAccess(<span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    cudaDeviceEnablePeerAccess(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="dian-dui-dian-chuan-shu-cuda-memcpy-peer" tabindex="-1" id="点对点传输：cudaMemcpyPeer">点对点传输：cudaMemcpyPeer</h4><p>设备间拷贝可以使用 <strong><code>cudaMemcpyPeer</code></strong>（同步）或 <strong><code>cudaMemcpyPeerAsync</code></strong>（异步，需指定流）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMemcpyPeer</span><span class="params">(<span class="type">void</span> *dst, <span class="type">int</span> dstDevice, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">int</span> srcDevice, <span class="type">size_t</span> count)</span>;</span><br><span class="line">cudaError_t <span class="title function_">cudaMemcpyPeerAsync</span><span class="params">(<span class="type">void</span> *dst, <span class="type">int</span> dstDevice, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">int</span> srcDevice,</span></span><br><span class="line"><span class="params">                                <span class="type">size_t</span> count, cudaStream_t stream)</span>;</span><br></pre></td></tr></table></figure><p>当 <strong>P2P 访问已启用</strong> 时，这类拷贝会走<strong>设备间直连</strong>路径（如 NVLink 或 PCIe P2P），而<strong>不经过主机</strong>，带宽与延迟明显优于「Device→Host→Device」。在支持 UVA（统一虚拟寻址）的平台上，若已启用 P2P，也可以使用 <strong><code>cudaMemcpy(..., cudaMemcpyDeviceToDevice)</code></strong> 或 <strong><code>cudaMemcpy(..., cudaMemcpyDefault)</code></strong>，运行时可根据指针推断源与目标设备并选择 P2P 路径。</p><h4 id="tong-yi-xu-ni-xun-zhi-uva-yu-p-2-p" tabindex="-1" id="统一虚拟寻址（UVA）与-P2P">统一虚拟寻址（UVA）与 P2P</h4><p>在 64 位且支持 UVA 的系统中，<strong>主机与所有设备的地址处在同一虚拟地址空间</strong>。因此，通过指针值即可判断某地址属于哪块设备（或主机）。启用 P2P 后，从一块 GPU 上运行的核函数中，可以直接<strong>解引用</strong>指向另一块 GPU 内存的指针进行 load/store（即<strong>点对点内存访问</strong>），无需显式拷贝。此时同一指针可在不同设备上使用（例如在设备 1 上启用对设备 0 的 P2P 后，设备 1 的核函数可以接收并访问设备 0 上分配的指针）。UVA 下使用 <strong><code>cudaMemcpy</code></strong> 做设备间拷贝时，若使用 <code>cudaMemcpyDefault</code>，源和目标会由运行时根据地址自动识别。</p><table><thead><tr><th>方式</th><th>API</th><th>说明</th></tr></thead><tbody><tr><td>显式指定设备</td><td><code>cudaMemcpyPeer(dst, dstDev, src, srcDev, size)</code></td><td>不依赖 UVA，适用于所有支持 P2P 传输的环境</td></tr><tr><td>UVA + P2P</td><td><code>cudaMemcpy(dst, src, size, cudaMemcpyDefault)</code></td><td>需 UVA；运行时根据指针推断设备，若已启用 P2P 则走直连</td></tr></tbody></table><blockquote><p><strong>理解与体会</strong>：P2P 分为<strong>传输</strong>与<strong>访问</strong>两种用法。传输用 <code>cudaMemcpyPeer</code> 或 UVA 下的 <code>cudaMemcpy</code>，数据从一卡拷到另一卡而不经主机，带宽与延迟优于「Device→Host→Device」。访问则是在启用 P2P 后，某设备上的核函数<strong>直接解引用</strong>指向对端设备内存的指针做 load/store，适合细粒度、不规则访问模式；此时需确保对端内存在核函数执行期间有效且语义正确（避免竞态）。多数场景下先掌握 P2P 传输即可，P2P 访问在跨设备共享只读表或边界数据时很有用。</p></blockquote><hr><h3 id="2-3-duo-gpu-jian-tong-bu" tabindex="-1" id="2-3-多-GPU-间同步">2.3 多 GPU 间同步</h3><p>多 GPU 应用中，经常需要「等某块 GPU 上的计算或传输完成后再在另一块 GPU 上继续」。由于<strong>流和事件与设备绑定</strong>，同步应通过<strong>事件 + 流等待</strong>完成，而不是假设默认流的隐式顺序。</p><p>典型模式如下：</p><ol><li>在<strong>设备 A</strong> 上创建流 <code>streamA</code> 和事件 <code>eventA</code>；</li><li>在设备 A 的 <code>streamA</code> 中执行核函数或拷贝，然后 <strong><code>cudaEventRecord(eventA, streamA)</code></strong>；</li><li>在<strong>设备 B</strong> 上创建流 <code>streamB</code>；</li><li>在设备 B 上执行 <strong><code>cudaStreamWaitEvent(streamB, eventA, 0)</code></strong>，使 <code>streamB</code> 在 <code>eventA</code> 完成之后再执行后续操作；</li><li>在 <code>streamB</code> 中启动核函数或拷贝。</li></ol><p>这样，设备 B 上的工作会<strong>等待</strong>设备 A 上 <code>eventA</code> 之前的工作完成。这一章会说明：<code>cudaStreamWaitEvent</code> 即使流与事件属于不同设备也能成功，因而是跨 GPU 同步的核心 API。</p><p>下面是一段<strong>跨设备同步的完整示例</strong>（风格与书中一致）：设备 0 上先执行核函数并记录事件，设备 1 上的流等待该事件后再执行核函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">kernelA</span><span class="params">(<span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) out[i] = <span class="number">1.0f</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">kernelB</span><span class="params">(<span class="type">float</span> *in, <span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) out[i] = in[i] * <span class="number">2.0f</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> n = <span class="number">1024</span>;</span><br><span class="line">    <span class="type">size_t</span> bytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    <span class="type">float</span> *d_A0 = <span class="literal">NULL</span>, *d_B0 = <span class="literal">NULL</span>;</span><br><span class="line">    cudaMalloc(&amp;d_A0, bytes);</span><br><span class="line">    cudaMalloc(&amp;d_B0, bytes);</span><br><span class="line">    cudaStream_t s0;</span><br><span class="line">    cudaEvent_t e0;</span><br><span class="line">    cudaStreamCreate(&amp;s0);</span><br><span class="line">    cudaEventCreate(&amp;e0);</span><br><span class="line"></span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    <span class="type">float</span> *d_B1 = <span class="literal">NULL</span>;</span><br><span class="line">    cudaMalloc(&amp;d_B1, bytes);</span><br><span class="line">    cudaStream_t s1;</span><br><span class="line">    cudaStreamCreate(&amp;s1);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设备 0：在 s0 中执行 kernelA，然后记录 e0</span></span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    kernelA&lt;&lt;&lt;(n + <span class="number">255</span>) / <span class="number">256</span>, <span class="number">256</span>, <span class="number">0</span>, s0&gt;&gt;&gt;(d_B0, n);</span><br><span class="line">    cudaEventRecord(e0, s0);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设备 1：s1 等待 e0 后再执行 kernelB</span></span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    cudaStreamWaitEvent(s1, e0, <span class="number">0</span>);</span><br><span class="line">    kernelB&lt;&lt;&lt;(n + <span class="number">255</span>) / <span class="number">256</span>, <span class="number">256</span>, <span class="number">0</span>, s1&gt;&gt;&gt;(d_B1, d_B1, n);  <span class="comment">// 此处 d_B1 仅作示例</span></span><br><span class="line"></span><br><span class="line">    cudaDeviceSynchronize();  <span class="comment">// 等待所有设备</span></span><br><span class="line">    cudaSetDevice(<span class="number">0</span>);</span><br><span class="line">    cudaStreamDestroy(s0);</span><br><span class="line">    cudaEventDestroy(e0);</span><br><span class="line">    cudaFree(d_A0);</span><br><span class="line">    cudaFree(d_B0);</span><br><span class="line">    cudaSetDevice(<span class="number">1</span>);</span><br><span class="line">    cudaStreamDestroy(s1);</span><br><span class="line">    cudaFree(d_B1);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="san-duo-gpu-jian-xi-fen-ji-suan-shu-9-2-jie" tabindex="-1" id="三、多-GPU-间细分计算（书-9-2-节）">三、多 GPU 间细分计算（书 9.2 节）</h2><p>本节对应书中在多块 GPU 上<strong>分配工作与数据</strong>的方法：如何在多设备上分配内存、如何将问题划分到各 GPU，以及如何结合 P2P 或主机中转进行数据交换。书中区分<strong>粗粒度</strong>（每块 GPU 独立处理一块子问题）和<strong>细粒度</strong>（更细的数据划分与负载均衡）两种思路；粗粒度实现简单，细粒度有利于负载均衡但通信与协调成本更高。</p><h3 id="3-1-zai-duo-she-bei-shang-fen-pei-nei-cun" tabindex="-1" id="3-1-在多设备上分配内存">3.1 在多设备上分配内存</h3><p>多 GPU 程序通常需要为<strong>每块设备</strong>单独分配：</p><ul><li><strong>设备内存</strong>：<code>cudaSetDevice(i)</code> 后 <code>cudaMalloc(&amp;d_ptr_i, size)</code>；</li><li><strong>主机端资源</strong>：若使用异步传输，常用<strong>锁页主机内存</strong>（<code>cudaMallocHost</code>）以支持与多设备的异步拷贝；</li><li><strong>流和事件</strong>：每个设备创建自己的流和事件，用于该设备上的并发与同步。</li></ul><p><strong>典型模式</strong>：用循环对每个设备执行「设置当前设备 → 分配设备内存、创建流/事件」；主机端可分配一块或每设备一块锁页缓冲区，用于与各设备的 H2D/D2H 传输。数据划分时，常见做法是将<strong>总数据量按设备数均分</strong>（或按负载均衡策略分配），每块 GPU 处理自己的那一份；若有依赖（例如归约），则再通过 P2P 或主机做一次汇总。</p><p>下表归纳多设备下的资源与 API 使用：</p><table><thead><tr><th>资源类型</th><th>多设备下的做法</th></tr></thead><tbody><tr><td>设备内存</td><td>每设备 <code>cudaSetDevice(i)</code> 后 <code>cudaMalloc</code>，指针分别保存（如 <code>d_A[0]</code>, <code>d_A[1]</code>）</td></tr><tr><td>主机内存</td><td>锁页 <code>cudaMallocHost</code> 便于与多设备异步拷贝；或每设备一块 sub-buffer</td></tr><tr><td>流 / 事件</td><td>每设备创建自己的流和事件，核函数与拷贝发往对应设备的流</td></tr><tr><td>数据划分</td><td>按设备数均分（或按负载分配），每设备处理自己的区间</td></tr></tbody></table><p>两卡环境下按数据均分的逻辑可概括为（每块 GPU 处理一段连续子数据，结果写回设备内存，最后由主机合并或再经 P2P 归约）：</p><pre><code class="highlight mermaid">flowchart LR    subgraph Host [主机]        H_Mem[(锁页内存\n完整数组)]    end    subgraph GPU0 [GPU 0]        D0[(设备内存\n前半段)]        K0[核函数]    end    subgraph GPU1 [GPU 1]        D1[(设备内存\n后半段)]        K1[核函数]    end    H_Mem --&gt;|H2D 前半| D0    H_Mem --&gt;|H2D 后半| D1    D0 --&gt; K0    D1 --&gt; K1    K0 --&gt; D0    K1 --&gt; D1    D0 --&gt;|D2H| H_Mem    D1 --&gt;|D2H| H_Mem</code></pre><h3 id="3-2-wan-zheng-shi-li-duo-she-bei-shang-bing-xing-xiang-liang-jia-fa" tabindex="-1" id="3-2-完整示例：多设备上并行向量加法">3.2 完整示例：多设备上并行向量加法</h3><p>下面这段代码演示<strong>在两块 GPU 上按数据划分</strong>做向量加法：主机分配锁页内存与设备内存，每个设备负责一半元素，最后在主机上合并结果（书中常见风格，对应多设备分配与执行）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                           \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                     \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                   \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, reason: %s\n&quot;</span>, __FILE__, __LINE__,                \</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                        \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                          \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">addKernel</span><span class="params">(<span class="type">const</span> <span class="type">float</span> *A, <span class="type">const</span> <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> N = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;           <span class="comment">// 总元素数</span></span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> nBytes = N * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    <span class="type">int</span> nDevices = <span class="number">0</span>;</span><br><span class="line">    CHECK(cudaGetDeviceCount(&amp;nDevices));</span><br><span class="line">    <span class="keyword">if</span> (nDevices &lt; <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Need at least 2 GPUs.\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 主机锁页内存（整块数据）</span></span><br><span class="line">    <span class="type">float</span> *h_A = <span class="literal">NULL</span>, *h_B = <span class="literal">NULL</span>, *h_C = <span class="literal">NULL</span>;</span><br><span class="line">    CHECK(cudaMallocHost(&amp;h_A, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost(&amp;h_B, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost(&amp;h_C, nBytes));</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        h_A[i] = <span class="number">1.0f</span>;</span><br><span class="line">        h_B[i] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> nHalf = N / <span class="number">2</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> halfBytes = nHalf * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_A[<span class="number">2</span>], *d_B[<span class="number">2</span>], *d_C[<span class="number">2</span>];</span><br><span class="line">    cudaStream_t st[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> dev = <span class="number">0</span>; dev &lt; <span class="number">2</span>; dev++) &#123;</span><br><span class="line">        CHECK(cudaSetDevice(dev));</span><br><span class="line">        CHECK(cudaMalloc(&amp;d_A[dev], halfBytes));</span><br><span class="line">        CHECK(cudaMalloc(&amp;d_B[dev], halfBytes));</span><br><span class="line">        CHECK(cudaMalloc(&amp;d_C[dev], halfBytes));</span><br><span class="line">        CHECK(cudaStreamCreate(&amp;st[dev]));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 每设备：拷贝自己那一半，计算，拷回</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> dev = <span class="number">0</span>; dev &lt; <span class="number">2</span>; dev++) &#123;</span><br><span class="line">        CHECK(cudaSetDevice(dev));</span><br><span class="line">        <span class="type">int</span> offset = dev * nHalf;</span><br><span class="line">        CHECK(cudaMemcpyAsync(d_A[dev], h_A + offset, halfBytes, cudaMemcpyHostToDevice, st[dev]));</span><br><span class="line">        CHECK(cudaMemcpyAsync(d_B[dev], h_B + offset, halfBytes, cudaMemcpyHostToDevice, st[dev]));</span><br><span class="line">        addKernel&lt;&lt;&lt;(nHalf + <span class="number">255</span>) / <span class="number">256</span>, <span class="number">256</span>, <span class="number">0</span>, st[dev]&gt;&gt;&gt;(d_A[dev], d_B[dev], d_C[dev], nHalf);</span><br><span class="line">        CHECK(cudaMemcpyAsync(h_C + offset, d_C[dev], halfBytes, cudaMemcpyDeviceToHost, st[dev]));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    CHECK(cudaSetDevice(<span class="number">0</span>));</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line">    CHECK(cudaSetDevice(<span class="number">1</span>));</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 验证</span></span><br><span class="line">    <span class="type">int</span> ok = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) <span class="keyword">if</span> (h_C[i] != <span class="number">3.0f</span>) &#123; ok = <span class="number">0</span>; <span class="keyword">break</span>; &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, ok ? <span class="string">&quot;PASS&quot;</span> : <span class="string">&quot;FAIL&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> dev = <span class="number">0</span>; dev &lt; <span class="number">2</span>; dev++) &#123;</span><br><span class="line">        cudaSetDevice(dev);</span><br><span class="line">        cudaFree(d_A[dev]);</span><br><span class="line">        cudaFree(d_B[dev]);</span><br><span class="line">        cudaFree(d_C[dev]);</span><br><span class="line">        cudaStreamDestroy(st[dev]);</span><br><span class="line">    &#125;</span><br><span class="line">    cudaFreeHost(h_A);</span><br><span class="line">    cudaFreeHost(h_B);</span><br><span class="line">    cudaFreeHost(h_C);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译：<code>nvcc -o multiGPU_add multiGPU_add.cu</code>。运行需至少两块 GPU。该示例体现了：<strong>按设备划分数据、每设备独立流、异步传输与计算、最后双设备同步</strong>的典型多 GPU 流程。</p><blockquote><p><strong>理解与体会</strong>：多设备编程的「标准模板」是：为每个设备单独 <code>cudaSetDevice(i)</code> → <code>cudaMalloc</code>、<code>cudaStreamCreate</code>（及必要时 <code>cudaEventCreate</code>），用数组或结构保存各设备的指针与流；主机端若需与多设备异步拷贝，使用<strong>锁页内存</strong>（<code>cudaMallocHost</code>）以便多流并发执行 H2D/D2H；数据按设备划分后，每设备只处理自己的区间，最后在主机合并或通过 P2P 做归约。先跑通「每设备独立流 + 异步拷贝 + 各设备同步」的流程，再考虑 P2P 与计算-通信重叠。</p></blockquote><h3 id="3-3-shu-ju-hua-fen-yu-fu-zai-jun-heng" tabindex="-1" id="3-3-数据划分与负载均衡">3.3 数据划分与负载均衡</h3><p>这一章会说明：<strong>粗粒度</strong>划分（例如均匀分块）实现简单，但若各块工作量差异大，会出现负载不均衡。<strong>细粒度</strong>划分（例如按行/按任务动态分配）可以更好地平衡负载，但需要更多的设备间通信或主机协调。在强扩展（固定问题规模、增加 GPU 数）下，理想加速比可写为：</p><p>[<br>S_p = \frac{T_1}{T_p} \leq \frac{T_1}{T_1/p + T_{\text{comm}}} \approx \frac{p}{1 + p \cdot T_{\text{comm}}/T_1}<br>]</p><p>其中 (T_1) 为单 GPU 时间，(T_p) 为 (p) 块 GPU 的时间，(T_{\text{comm}}) 为通信与同步开销。当 (T_{\text{comm}}) 不可忽略时，加速比会低于 (p)；因此多 GPU 编程中<strong>减少设备间通信、重叠计算与通信</strong>是性能关键，与下一节「计算与通信的重叠」以及第 6 章流的思路一致。</p><hr><h2 id="si-ji-suan-yu-tong-xin-de-zhong-die-shu-9-3-jie" tabindex="-1" id="四、计算与通信的重叠（书-9-3-节）">四、计算与通信的重叠（书 9.3 节）</h2><p>与单 GPU 上重叠主机-设备传输与核函数执行类似，在多 GPU 上应尽量<strong>重叠设备间传输与各设备上的计算</strong>。第 2 章曾提到：若计算时间大于数据传输时间，可通过流来重叠计算与通信、隐藏传输延迟。在多 GPU 场景下，这一思想同样适用：当 GPU 0 正在计算时，GPU 1 与 GPU 0 之间的 P2P 传输可以在另一流中进行；或当 GPU 0 把结果通过 P2P 发给 GPU 1 时，GPU 1 可以在收到一部分数据后就开始计算（双缓冲/流水线），从而提高整体吞吐。</p><p>实现方式与第 6 章一致：使用 <strong>异步拷贝</strong>（<code>cudaMemcpyPeerAsync</code>、<code>cudaMemcpyAsync</code>）和<strong>多个流</strong>，让传输与核函数分属不同流，由运行时在硬件允许的情况下并发执行。跨设备时，用 <strong><code>cudaStreamWaitEvent</code></strong> 让「计算流」等待「传输完成事件」，再启动依赖该数据的核函数。概括思路：<strong>每设备多流 + 事件同步 + 异步 P2P</strong> 是实现多 GPU 上计算-通信重叠的标准做法。</p><blockquote><p><strong>理解与体会</strong>：多 GPU 上的「重叠」与单 GPU 上「传输与计算重叠」是同一思想：<strong>用异步操作和事件把依赖关系表达清楚，让硬件在满足依赖的前提下尽量并行</strong>。设备 A 的「发送流」与设备 B 的「接收流」配合 P2P 传输；设备 B 上用 <code>cudaStreamWaitEvent(computeStream, recvDoneEvent, 0)</code> 再启动核函数，即可保证「算的是已收到的数据」。若有多阶段计算与通信（如迭代 stencil），可采用双缓冲：一块缓冲区在接收，另一块供当前核函数使用，下一轮交换角色，形成流水线。</p></blockquote><p>下表归纳多 GPU 上「计算与通信重叠」的典型模式（与书中思想一致）：</p><table><thead><tr><th>模式</th><th>做法</th><th>目的</th></tr></thead><tbody><tr><td><strong>传输与计算并行</strong></td><td>设备 A 的流 1 做 P2P 发送，流 2 做本地核函数；设备 B 的流 1 做 P2P 接收，流 2 做依赖数据的核函数</td><td>隐藏设备间传输延迟</td></tr><tr><td><strong>流水线/双缓冲</strong></td><td>当一块缓冲区在传输时，另一块用于计算；交替使用</td><td>提高设备利用率</td></tr><tr><td><strong>事件驱动</strong></td><td>用 <code>cudaEventRecord</code> 标记传输完成，<code>cudaStreamWaitEvent</code> 让计算流等待</td><td>保证数据就绪后再计算，避免竞态</td></tr></tbody></table><hr><h2 id="wu-shi-yong-cuda-aware-mpi-jiao-huan-shu-ju-shu-9-4-jie" tabindex="-1" id="五、使用-CUDA-aware-MPI-交换数据（书-9-4-节）">五、使用 CUDA-aware MPI 交换数据（书 9.4 节）</h2><p>在<strong>多节点</strong>集群中，GPU 分布在不同主机上，设备间无法直接 P2P，必须借助<strong>消息传递</strong>。<strong>CUDA-aware MPI</strong> 是指 MPI 实现能够<strong>直接使用 GPU 指针</strong>作为发送/接收缓冲区：在调用 <code>MPI_Send</code>/<code>MPI_Recv</code>（或 <code>MPI_Isend</code>/<code>MPI_Irecv</code>）时传入设备指针，MPI 库内部会与 CUDA 协作，在 GPU 内存与网络之间搬运数据，<strong>无需</strong>程序员先把数据拷回主机再发送。这一章会说明：这样既简化了代码，又便于与 GPUDirect RDMA 结合，减少主机内存与 CPU 参与，提高带宽并降低延迟。</p><p>典型用法（概念性）：各进程绑定一块 GPU（<code>cudaSetDevice</code>），在设备上分配发送/接收缓冲区，然后直接以设备指针调用 MPI 的发送/接收接口（具体 API 以所用 MPI 文档为准）。支持 CUDA-aware 的 MPI 实现包括 <strong>MVAPICH2</strong>、<strong>Open MPI</strong>、<strong>IBM Platform MPI</strong> 等；编译与链接时需启用对应选项（如 <code>--with-cuda</code>）并链接 CUDA 运行时。多节点矩阵-向量乘或 Jacobi 等示例中，在各 rank 上设置设备、分配设备缓冲区并调用 MPI 集体通信或点对点通信即可。</p><table><thead><tr><th>方式</th><th>说明</th></tr></thead><tbody><tr><td><strong>传统 MPI</strong></td><td>进程先将 GPU 数据拷回主机，再用主机缓冲区调用 MPI_Send/Recv；接收后再拷到 GPU</td></tr><tr><td><strong>CUDA-aware MPI</strong></td><td>直接以设备指针调用 MPI；由 MPI 与 CUDA 协同完成与网络的传输</td></tr></tbody></table><blockquote><p><strong>理解与体会</strong>：传统 MPI 下，若数据在 GPU 上，必须先 D2H 拷回主机、用主机缓冲区 MPI_Send/Recv、再 H2D 拷到目标进程的 GPU，不仅代码繁琐，而且多了一次主机参与和内存拷贝。<strong>CUDA-aware MPI</strong> 允许直接传设备指针，MPI 库内部与 CUDA 协作（在支持 GPUDirect RDMA 的环境中还可让网卡直接访问 GPU 内存），既简化了代码，又为减少拷贝、提高带宽创造条件。使用前需确认所用 MPI 实现支持并已启用 CUDA 支持（如 Open MPI 的 <code>--with-cuda</code>、MVAPICH2 的对应选项），编译链接时也需正确链接 CUDA 运行时。</p></blockquote><hr><h2 id="liu-zai-gpu-jia-su-ji-qun-shang-kuo-zhan-ying-yong-shu-9-5-jie" tabindex="-1" id="六、在-GPU-加速集群上扩展应用（书-9-5-节）">六、在 GPU 加速集群上扩展应用（书 9.5 节）</h2><p>书中将多 GPU 编程从<strong>单节点多卡</strong>推广到<strong>多节点 GPU 集群</strong>：每节点可有多块 GPU，节点间通过 MPI 通信。扩展应用时需要考虑：</p><ul><li><strong>进程/线程与 GPU 的映射</strong>：通常每进程或每线程绑定一块 GPU（<code>cudaSetDevice</code>），MPI 秩与设备号的对应关系可由用户或运行时设置。</li><li><strong>数据与计算划分</strong>：按 MPI 进程（或按进程内多 GPU）划分域或任务，每进程负责本节点的 GPU 计算与本地通信，跨节点用 MPI 交换边界或全局数据。</li><li><strong>GPUDirect RDMA</strong>：在支持 InfiniBand 等网络的集群上，<strong>GPUDirect RDMA</strong> 允许网卡<strong>直接</strong>从 GPU 内存 DMA 到网络（或反向），无需经过主机内存。与 CUDA-aware MPI 结合后，多节点间的 GPU 到 GPU 传输可以接近线速，其优势与使用条件（驱动、硬件、MPI 实现支持）可参考原书或文档。</li></ul><p>集群扩展的理想强扩展效率可表示为：</p><p>[<br>\text{Efficiency} = \frac{S_p}{p} = \frac{T_1}{p \cdot T_p} \leq 1<br>]</p><p>效率越接近 1，说明并行扩展越好；通信与负载不均衡会拉低效率。</p><p>这一章会说明：实际集群上还需考虑 <strong>NCCL</strong>（NVIDIA Collective Communications Library）等库，它们针对多 GPU 与多节点的集体操作（如 All-Reduce、Broadcast）做了优化，可与 MPI 配合或替代部分 MPI 集体调用，以获得更高的多 GPU/多节点通信性能。</p><blockquote><p><strong>理解与体会</strong>：集群扩展时，<strong>扩展效率</strong> (E_p = S_p/p = T_1/(p \cdot T_p)) 越接近 1 越好；通信与负载不均衡会拉低效率。单节点内用 P2P 与多流重叠；跨节点则依赖 MPI（及可选 NCCL）与 GPUDirect RDMA。进程与 GPU 的映射（如每进程一 GPU、或 MPI rank 与 device id 的对应）需在程序初始化时明确设置（如通过 <code>cudaSetDevice(rank % ngpus)</code> 或环境变量 <code>CUDA_VISIBLE_DEVICES</code> 限制每进程可见的 GPU），避免多进程争抢同一块 GPU 或设备号与 NUMA 拓扑不匹配。</p></blockquote><hr><h2 id="qi-cpu-yu-gpu-qin-he-xing-shu-9-6-jie" tabindex="-1" id="七、CPU-与-GPU-亲和性（书-9-6-节）">七、CPU 与 GPU 亲和性（书 9.6 节）</h2><p><strong>CPU 与 GPU 亲和性</strong>指的是：在多 GPU、多 NUMA 节点或多核主机上，<strong>将主机线程或进程绑定到靠近某块 GPU 的 CPU 核心</strong>，可以减少 PCIe 访问延迟、提高传输带宽，并避免操作系统将线程迁移到远离该 GPU 的 CPU。</p><p>在 Linux 上可通过 <strong><code>numactl</code></strong>、<strong><code>pthread_setaffinity_np</code></strong> 等设置 CPU 亲和性；部分 MPI 实现也支持绑定进程到指定 CPU 与 GPU。例如，在单节点多卡上，可以让「控制 GPU 0 的主机线程」绑定到与 GPU 0 同 NUMA 节点的 CPU，从而减少跨 NUMA 访问带来的延迟。具体 API 或环境变量（如 <code>CUDA_VISIBLE_DEVICES</code> 与进程绑定的配合）可参考原书或文档。理解亲和性有助于在多 GPU 与多节点环境中获得更稳定、更高的性能。</p><blockquote><p><strong>理解与体会</strong>：在多 NUMA 节点、多 PCIe 根的主机上，GPU 与 CPU 的「距离」并不相同：某块 GPU 可能挂在某颗 CPU 的 PCIe 下，若控制该 GPU 的主机线程被调度到另一颗 CPU，则 PCIe 访问可能经过跨节点互联，延迟与带宽都会受影响。通过将线程/进程绑定到与目标 GPU 同 NUMA 节点的 CPU，可减少这类问题。在实际部署时，可结合 <code>nvidia-smi topo -m</code> 查看 GPU 与 CPU 的拓扑，再配合 <code>numactl</code> 或 MPI 的绑定选项做亲和性设置。</p></blockquote><hr><h2 id="ba-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="八、本章小结与重难点回顾">八、本章小结与重难点回顾</h2><p>本章系统梳理了 <strong>多 GPU 编程</strong> 从单节点多卡到集群扩展的核心内容，严格对应书中第 9 章各节。下面用表格与要点形式做小结，便于复习与自测。</p><h3 id="8-1-zhi-shi-dian-zong-lan" tabindex="-1" id="8-1-知识点总览">8.1 知识点总览</h3><table><thead><tr><th>主题</th><th>要点</th></tr></thead><tbody><tr><td><strong>多设备管理</strong></td><td><code>cudaGetDeviceCount</code>、<code>cudaSetDevice</code>；分配、流、事件与<strong>当前设备</strong>绑定；核函数必须发往<strong>当前设备</strong>的流。</td></tr><tr><td><strong>点对点</strong></td><td><code>cudaDeviceCanAccessPeer</code>、<code>cudaDeviceEnablePeerAccess</code>；<code>cudaMemcpyPeer</code> / <code>cudaMemcpyPeerAsync</code>；UVA 下可用 <code>cudaMemcpyDefault</code> 做设备间拷贝。</td></tr><tr><td><strong>跨设备同步</strong></td><td><strong><code>cudaStreamWaitEvent</code></strong> 可跨设备使用，实现「设备 B 的流等待设备 A 的某事件」；每设备有独立默认流。</td></tr><tr><td><strong>多设备计算</strong></td><td>每设备单独分配内存与流；数据按设备划分；锁页主机内存便于多设备异步传输。</td></tr><tr><td><strong>计算与通信重叠</strong></td><td>多流 + 异步 P2P 传输 + 事件同步，在各 GPU 上重叠传输与计算。</td></tr><tr><td><strong>CUDA-aware MPI</strong></td><td>直接使用设备指针进行 MPI 通信；集群扩展时结合 GPUDirect RDMA 提升跨节点 GPU 传输性能。</td></tr><tr><td><strong>亲和性</strong></td><td>将主机线程/进程绑定到靠近目标 GPU 的 CPU，可改善多 GPU 性能。</td></tr></tbody></table><h3 id="8-2-zhong-nan-dian-su-ji" tabindex="-1" id="8-2-重难点速记">8.2 重难点速记</h3><ul><li><strong>当前设备</strong>：<code>cudaSetDevice(i)</code> 后，后续的 <code>cudaMalloc</code>、核函数启动、<code>cudaStreamCreate</code>/<code>cudaEventCreate</code> 都作用于设备 <code>i</code>；核函数若发往<strong>非当前设备</strong>的流会失败。</li><li><strong>多设备下易错</strong>：<code>cudaEventRecord(event, stream)</code> 若 event 与 stream 属不同设备则失败；<code>cudaEventElapsedTime</code> 若两事件属不同设备则失败。跨设备同步<strong>只能</strong>用 <code>cudaStreamWaitEvent(streamB, eventA, 0)</code>。</li><li><strong>P2P</strong>：先 <code>cudaDeviceCanAccessPeer</code> 检查，再在各设备上 <code>cudaDeviceEnablePeerAccess(peer, 0)</code> 启用（双向需两设备各启用一次）；传输用 <code>cudaMemcpyPeer</code>/<code>cudaMemcpyPeerAsync</code>，UVA 下可用 <code>cudaMemcpy(..., cudaMemcpyDefault)</code>。</li><li><strong>多设备模板</strong>：每设备 <code>cudaSetDevice</code> → 分配设备内存与流（及事件）→ 数据按设备划分 → 异步 H2D → kernel → 异步 D2H → 各设备 <code>cudaDeviceSynchronize</code> 或事件同步；主机用锁页内存以支持多设备异步拷贝。</li></ul><h3 id="8-3-he-xin-yao-dian-hui-gu" tabindex="-1" id="8-3-核心要点回顾">8.3 核心要点回顾</h3><ol><li><strong>多 GPU 执行</strong>：通过 <code>cudaSetDevice</code> 切换当前设备，在每块 GPU 上分配资源并启动核函数；流和事件与<strong>创建时所在设备</strong>绑定。</li><li><strong>点对点</strong>：启用 P2P 后可在设备间直接传输或直接访问对方内存；使用 <code>cudaMemcpyPeer</code> 或 UVA 下的 <code>cudaMemcpy</code> 实现高效设备间拷贝。</li><li><strong>跨设备同步</strong>：用 <code>cudaStreamWaitEvent(streamB, eventA, 0)</code> 让设备 B 的流等待设备 A 上的事件，是实现多 GPU 依赖关系的核心 API。</li><li><strong>多设备编程模式</strong>：每设备独立内存与流、数据划分、异步传输与计算、必要时 P2P 或主机中转；集群场景使用 CUDA-aware MPI 与 GPUDirect RDMA；结合 CPU-GPU 亲和性可获得更稳定性能。</li></ol><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 10 章</strong>（全书最后一章），对 CUDA 学习进行总结与拓展：</p><ul><li>全书知识体系回顾与 CUDA 编程最佳实践</li><li>性能优化路线图与常见陷阱</li><li>进阶方向：多流、多 GPU、库与指令、集群与领域库</li></ul><p>从多 GPU 与集群，回到整体视角——我们下一章见。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>多 GPU 下「当前设备」的含义是什么？不调用 cudaSetDevice 时默认是哪个设备？</strong></li><li><strong>点对点（P2P）传输与经主机中转的 D2H+H2D 相比有何优势？</strong></li><li><strong>设备 B 上的流要等待设备 A 上的事件完成，应使用哪个 API？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>当前设备指后续的 <code>cudaMalloc</code>、kernel 启动等操作所针对的设备。不调用 <code>cudaSetDevice</code> 时默认为设备 0。</li><li>P2P 可在支持拓扑下直接在设备内存间传输，不经过主机内存，减少一次 D2H 和一次 H2D，延迟更低、带宽更高；经主机中转需两次 PCIe 传输且占用主机内存。</li><li>使用 <code>cudaStreamWaitEvent(stream_B, event_A, 0)</code>，使设备 B 上的 <code>stream_B</code> 等待设备 A 上记录的 <code>event_A</code> 完成后再执行后续操作。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/e466b0a5/">第8章 GPU加速库和OpenACC</a> | 下一篇：<a href="https://smarter.xin/posts/98c27a24/">第10章 程序实现的注意事项</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 9 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-jiu-zhang-duo-gpu-bian-cheng&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="多GPU" scheme="https://smarter.xin/tags/multi-gpu/"/>
    
    <category term="P2P" scheme="https://smarter.xin/tags/p2p/"/>
    
    <category term="MPI" scheme="https://smarter.xin/tags/mpi/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第八章：GPU加速库和OpenACC</title>
    <link href="https://smarter.xin/posts/e466b0a5/"/>
    <id>https://smarter.xin/posts/e466b0a5/</id>
    <published>2026-02-05T09:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-ba-zhang-gpu-jia-su-ku-he-open-acc" tabindex="-1">CUDA C编程权威指南-第八章：GPU加速库和OpenACC</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/1aaa52d4/">第7章 调整指令级原语</a> | 下一篇：<a href="https://smarter.xin/posts/844cd057/">第9章 多GPU编程</a></p><blockquote><p>系列第 8 篇。前面一直在手写核函数；这一章换条路——用<strong>现成 GPU 加速库</strong>和<strong>指令式并行</strong>：cuBLAS、cuFFT、cuSPARSE、cuRAND 等，以及 CUDA 6 的库特性与 OpenACC，少写甚至不写核函数也能拿到 GPU 加速。</p></blockquote><hr><h2 id="qian-yan-cong-shou-xie-nei-he-dao-ku-yu-zhi-ling" tabindex="-1" id="前言：从手写内核到库与指令">前言：从手写内核到库与指令</h2><p>前几章我们一直在写<strong>自己的核函数</strong>：从向量加法、矩阵加法到归约、转置，再到共享内存、流与并发、指令级原语。这条路径能让你对 GPU 的编程模型和执行模型有深刻理解。这一章会说明：在实际项目中，很多问题——<strong>线性代数</strong>（矩阵运算、求解器）、<strong>快速傅里叶变换（FFT）</strong>、<strong>稀疏矩阵运算</strong>、<strong>随机数生成</strong>——已有高度优化的现成实现；<strong>直接使用 CUDA 加速库</strong>，往往能以更少的代码、更短的开发时间获得接近甚至优于手写内核的性能。同时，<strong>OpenACC</strong> 等指令式方法允许在保留原有 C/Fortran 代码结构的前提下，通过编译器指令将热点循环或区域卸载到 GPU，适合遗留代码的渐进式加速。</p><p>本章将系统学习以下内容：</p><ul><li><strong>CUDA 库概览</strong>：库的定位、与手写内核的取舍、常见库的分类与选用场景。</li><li><strong>cuSPARSE</strong>：稀疏线性代数库，稀疏矩阵存储格式与典型运算（如 SpMV）。</li><li><strong>cuBLAS</strong>：密集线性代数库，对应 BLAS 的 GPU 实现（saxpy、gemm 等）。</li><li><strong>cuFFT</strong>：快速傅里叶变换库，一维/多维 FFT 的 GPU 加速。</li><li><strong>cuRAND</strong>：随机数生成库，设备端或主机端高质量随机数。</li><li><strong>CUDA 6 库特性</strong>：多 GPU 支持、统一内存与库的集成。</li><li><strong>库性能概览</strong>：各库性能关注点与选型建议。</li><li><strong>OpenACC</strong>：指令式并行编程模型，<code>#pragma acc</code> 与数据/计算区域、与 CUDA 的对比。</li></ul><p>通过本章，你将在「手写核函数」之外，掌握<strong>用库和指令</strong>快速获得 GPU 加速的两种主流方式。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-gpu-jia-su-ku-he-open-acc" tabindex="-1" id="1-1-为什么要学「GPU-加速库和-OpenACC」">1.1 为什么要学「GPU 加速库和 OpenACC」</h3><p>第 2～7 章建立了从编程模型、执行模型、内存层次、流与并发到指令级原语的完整「手写内核」能力。在工程实践中，<strong>优先考虑现成 GPU 库</strong>能显著减少开发与维护成本，并在多数场景下获得接近最优的性能；只有在问题高度定制化或对延迟与资源有极端要求时，才需要手写内核。<strong>OpenACC</strong> 则提供了另一条路径——通过编译器指令将现有 C/Fortran 代码中的热点区域卸载到 GPU，适合遗留代码的渐进式迁移或快速原型。因此，理解各库的适用域、API 风格（句柄/计划、设备指针、数据布局），以及 OpenACC 的指令语义与适用场景，是写出<strong>可维护、高性能</strong> GPU 应用的重要一环；本章与第 2 章（性能模型）、第 6 章（流与库结合）、第 9 章（多 GPU 与库）紧密衔接。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解<strong>为何优先用库</strong>及库与手写内核的取舍</td><td>能说明「能库则库、不能库再手写」的理由；能列举各库的典型用途</td></tr><tr><td>掌握 <strong>cuBLAS</strong> 的 API 风格（句柄、设备指针、列主序）及 saxpy/gemm 用法</td><td>能写出最小可运行的 cublasSaxpy 示例；说明列主序与 C 行主序的差异</td></tr><tr><td>掌握 <strong>cuFFT</strong> 的 Plan → Execute → Destroy 流程及 R2C 输出长度</td><td>能说明一维实数 FFT 输出为何是 N/2+1 个复数；知道 cufftExec* 为异步</td></tr><tr><td>理解 <strong>cuSPARSE</strong> 的稀疏格式（CSR 等）与 SpMV 流程</td><td>能说出 CSR 与 COO 的适用场景；知道格式选择对性能的影响</td></tr><tr><td>掌握 <strong>cuRAND</strong> 的主机端与设备端用法及典型 API</td><td>能写出主机端 curandGenerateUniform 示例；知道设备端在核函数内初始化的模式</td></tr><tr><td>理解 <strong>CUDA 6</strong> 中多 GPU、统一内存与库的集成（书中涉及部分）</td><td>能简述多 GPU 下每设备独立句柄、统一内存在库中的支持情况</td></tr><tr><td>理解 <strong>OpenACC</strong> 的定位与常用指令（data、parallel、loop、kernels）</td><td>能写出带 <code>#pragma acc data</code> 与 <code>parallel loop</code> 的向量加；说明与 CUDA 的对比</td></tr><tr><td>掌握<strong>库的链接与头文件</strong>、<strong>性能选型</strong>的一般建议</td><td>能正确链接 -lcublas -lcufft 等；能根据问题域选库并注意复用句柄/计划</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><p><strong>阅读建议</strong>：本章篇幅较长。二、四、五、九节建议<strong>精读</strong>（库概述、cuBLAS/cuFFT 与 OpenACC 最常用）；三、六节（cuSPARSE、cuRAND）可按需<strong>选读</strong>；七节（CUDA 6 库特性）为<strong>可略读</strong>，了解即可。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line">第 8 章  GPU 加速库和 OpenACC</span><br><span class="line">│</span><br><span class="line">├── 二、CUDA 库概述（书 8.1 节）【精读】</span><br><span class="line">│   ├── 为什么使用 GPU 加速库</span><br><span class="line">│   ├── 库的分类与选用场景</span><br><span class="line">│   └── 库的链接与头文件 ★ 实践必会</span><br><span class="line">│</span><br><span class="line">├── 三、cuSPARSE 库（书 8.2 节）★ 重难点【选读】</span><br><span class="line">│   ├── 简介与稀疏格式（COO、CSR、CSC、HYB）</span><br><span class="line">│   └── SpMV 流程与格式选择对性能的影响</span><br><span class="line">│</span><br><span class="line">├── 四、cuBLAS 库（书 8.3 节）★ 重难点【精读】</span><br><span class="line">│   ├── 简介与 API 风格（句柄、列主序、设备指针）</span><br><span class="line">│   ├── 完整示例：cublasSaxpy</span><br><span class="line">│   └── 矩阵乘法 cublasSgemm 与 leading dimension</span><br><span class="line">│</span><br><span class="line">├── 五、cuFFT 库（书 8.4 节）【精读】</span><br><span class="line">│   ├── 简介与 Plan/Execute/Destroy 流程</span><br><span class="line">│   ├── 一维实数 FFT（R2C）完整示例</span><br><span class="line">│   └── R2C 输出长度 N/2+1 与异步语义 ★ 易错点</span><br><span class="line">│</span><br><span class="line">├── 六、cuRAND 库（书 8.5 节）【选读】</span><br><span class="line">│   ├── 设备端 vs 主机端 API</span><br><span class="line">│   └── 主机端均匀分布示例与生成器类型</span><br><span class="line">│</span><br><span class="line">├── 七、CUDA 6 中的库特性（书 8.6 节）【可略读】</span><br><span class="line">│   ├── 多 GPU 与库</span><br><span class="line">│   └── 统一内存与库的集成</span><br><span class="line">│</span><br><span class="line">├── 八、CUDA 库性能概览（书 8.7 节）</span><br><span class="line">│   ├── 性能对比的意义与有效带宽</span><br><span class="line">│   └── 选型与使用建议（复用句柄/计划、结合流与多 GPU）</span><br><span class="line">│</span><br><span class="line">├── 九、使用 OpenACC（书 8.8 节）★ 重难点【精读】</span><br><span class="line">│   ├── OpenACC 简介与定位（渐进式加速）</span><br><span class="line">│   ├── 常用指令与数据区域（data、parallel、loop、kernels）</span><br><span class="line">│   ├── 简单示例：向量加法</span><br><span class="line">│   └── OpenACC 与 CUDA 的对比</span><br><span class="line">│</span><br><span class="line">└── 十、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-cuda-ku-gai-shu-shu-8-1-jie" tabindex="-1" id="二、CUDA-库概述（书-8-1-节）">二、CUDA 库概述（书 8.1 节）</h2><p>本节对应书中对「为什么要用 CUDA 库」以及「库的分类与选用」的总体介绍，为全章奠定概念基础。</p><h3 id="2-1-wei-shi-yao-shi-yong-gpu-jia-su-ku" tabindex="-1" id="2-1-为什么使用-GPU-加速库">2.1 为什么使用 GPU 加速库</h3><p>手写 CUDA 核函数能带来最大的灵活性和学习价值，但在工程实践中会面临<strong>开发成本高、维护难、性能调优复杂</strong>等问题。这一章会说明：NVIDIA 提供的 <strong>CUDA 加速库</strong>针对常见域（线性代数、FFT、稀疏运算、随机数等）做了深度优化，并随 CUDA 工具包一起发布，与驱动和硬件保持兼容。使用这些库可以：</p><ul><li><strong>减少开发时间</strong>：无需从零实现并调试复杂算法（如 FFT、稀疏求解器）。</li><li><strong>获得经过验证的性能</strong>：库内部通常针对多种 GPU 架构做了调优，包括内存访问模式、占用率、算法选择等。</li><li><strong>保持可移植性</strong>：同一套库 API 可在不同代际的 GPU 上运行，由库内部适配硬件。</li></ul><p>当然，库也有其适用范围：若问题非常定制化、或对延迟与资源有极端要求，手写内核仍是必要的。<strong>优先考虑库，再考虑手写</strong>，是多数应用开发的最佳策略。</p><h3 id="2-2-ku-de-fen-lei-yu-xuan-yong-chang-jing" tabindex="-1" id="2-2-库的分类与选用场景">2.2 库的分类与选用场景</h3><p>原书按<strong>功能域</strong>对常用 CUDA 库进行了分类。下表归纳常用库及其典型用途：</p><table><thead><tr><th>库名</th><th>全称 / 含义</th><th>典型用途</th></tr></thead><tbody><tr><td><strong>cuBLAS</strong></td><td>CUDA Basic Linear Algebra Subprograms</td><td>密集向量/矩阵运算：axpy、gemv、gemm 等</td></tr><tr><td><strong>cuFFT</strong></td><td>CUDA Fast Fourier Transform</td><td>一维/多维 FFT、实数和复数 FFT</td></tr><tr><td><strong>cuSPARSE</strong></td><td>CUDA Sparse matrix library</td><td>稀疏矩阵存储与运算（SpMV、稀疏求解等）</td></tr><tr><td><strong>cuRAND</strong></td><td>CUDA Random number generation</td><td>设备端/主机端高质量随机数生成</td></tr><tr><td><strong>Thrust</strong></td><td>模板库（C++）</td><td>排序、归约、扫描等通用并行算法（若原书涉及）</td></tr></tbody></table><p>选用原则：若你的问题可归结为<strong>稠密线性代数</strong>，优先考虑 cuBLAS；若涉及<strong>频谱分析、卷积、滤波</strong>等，考虑 cuFFT；若矩阵<strong>稀疏</strong>且规模大，考虑 cuSPARSE；若需要<strong>随机数</strong>（蒙特卡洛、随机算法），考虑 cuRAND。后续各节会分别介绍各库的 API 风格与完整示例。</p><h3 id="2-3-ku-de-lian-jie-yu-tou-wen-jian" tabindex="-1" id="2-3-库的链接与头文件">2.3 库的链接与头文件</h3><p>使用 CUDA 库时，需要包含对应头文件并在链接时指定库。各库的头文件与库文件名与 BLAS/FFT 等标准命名对应，便于在现有构建系统中集成。典型编译命令例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 以 cuBLAS 为例：链接 libcublas</span></span><br><span class="line">nvcc -o myapp myapp.cu -lcublas</span><br><span class="line"></span><br><span class="line"><span class="comment"># 多库时</span></span><br><span class="line">nvcc -o myapp myapp.cu -lcublas -lcufft -lcurand</span><br></pre></td></tr></table></figure><p>头文件通常为 <code>cublas_v2.h</code>、<code>cufft.h</code>、<code>cusparse.h</code>、<code>curand.h</code> 等。库的 API 多数采用**句柄（handle）<strong>或</strong>计划（plan）**管理状态：先创建句柄/计划，在其上调用运算，最后销毁。这种设计便于多线程或多流场景下为每个流维护独立状态。这一章会说明：链接时若缺少对应库（如 <code>-lcublas</code>），会报未定义引用错误；在 Windows 下需在项目属性中添加相应库依赖，与第 2 章中 nvcc 编译流程一致。</p><blockquote><p><strong>理解与体会</strong>：本章的「哲学」是：<strong>能库则库，不能库再手写</strong>。库不是替代你对 CUDA 执行模型和内存模型的理解，而是让你把精力放在业务逻辑和系统集成上；只有在库无法覆盖或需要极致定制时，才回到手写内核。选用库时，先看问题域（稠密/稀疏、FFT、随机数），再查对应库的 API 与数据布局（如 cuBLAS 的列主序），可少走弯路。</p></blockquote><hr><h2 id="san-cu-sparse-ku-shu-8-2-jie" tabindex="-1" id="三、cuSPARSE-库（书-8-2-节）">三、cuSPARSE 库（书 8.2 节）</h2><p>本节对应书中对 cuSPARSE 的讲解：稀疏矩阵的存储格式、描述符与典型运算（如 SpMV）。</p><h3 id="3-1-cu-sparse-jian-jie-yu-xi-shu-ge-shi" tabindex="-1" id="3-1-cuSPARSE-简介与稀疏格式">3.1 cuSPARSE 简介与稀疏格式</h3><p><strong>cuSPARSE</strong> 提供针对<strong>稀疏矩阵</strong>的线性代数运算，适用于非零元远少于总元素的大型矩阵。这一章会说明：稠密矩阵运算用 cuBLAS，稀疏矩阵运算用 cuSPARSE，二者互补。cuSPARSE 支持多种稀疏存储格式，常见包括：</p><table><thead><tr><th>格式</th><th>全称</th><th>特点</th></tr></thead><tbody><tr><td><strong>COO</strong></td><td>Coordinate</td><td>三元组 (row, col, value)，简单但未针对运算优化</td></tr><tr><td><strong>CSR</strong></td><td>Compressed Sparse Row</td><td>按行压缩，行指针 + 列索引 + 值，SpMV 等常用</td></tr><tr><td><strong>CSC</strong></td><td>Compressed Sparse Column</td><td>按列压缩</td></tr><tr><td><strong>HYB</strong></td><td>Hybrid</td><td>部分 ELL + 部分 COO，库可自动选择</td></tr></tbody></table><p>SpMV（Sparse Matrix-Vector multiplication）( y \leftarrow A x ) 是 cuSPARSE 中最常用的运算之一，对应 <code>cusparseSpMV</code> 或旧版 API 中的 <code>cusparseSpMV_bufferSize</code> + <code>cusparseSpMV</code>。使用前需用 <strong>cusparseSpMatDescr_t</strong>（或旧版的矩阵描述符）描述矩阵的格式、维度与数据指针。</p><h3 id="3-2-shi-yong-liu-cheng-gai-shu-yu-zhong-nan-dian" tabindex="-1" id="3-2-使用流程概述与重难点">3.2 使用流程概述与重难点</h3><p>典型流程（与书中一致）：(1) 创建 cuSPARSE 句柄 <code>cusparseCreate()</code>；(2) 创建矩阵描述符并设置格式、维度、设备指针（行偏移、列索引、值）；(3) 分配并传入向量 ( x )、( y ) 的设备指针；(4) 调用 SpMV 或其它稀疏运算；(5) 销毁描述符与句柄。概念性代码框架如下（具体 API 以 cuSPARSE 版本文档为准）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 概念性流程：创建句柄、描述矩阵、执行 SpMV</span></span><br><span class="line">cusparseHandle_t handle;</span><br><span class="line">cusparseCreate(&amp;handle);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置 CSR 格式的 A：行指针 d_csrRowPtr，列索引 d_csrColInd，值 d_csrVal</span></span><br><span class="line"><span class="comment">// 调用 cusparseSpMV 或 cusparseCsrmv 等（以实际版本 API 为准）</span></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">cusparseDestroy(handle);</span><br></pre></td></tr></table></figure><p><strong>稀疏矩阵的格式选择（CSR、CSC、HYB 等）会显著影响性能</strong>，cuSPARSE 文档会给出各格式的适用场景与性能建议。对于 SpMV ( y \leftarrow A x )，数学上即 ( y_i = \sum_j A_{ij} x_j )（对非零 ( A_{ij} ) 求和）；库内部会根据格式与矩阵结构选择最优内核，无需用户手写稀疏访问模式。</p><blockquote><p><strong>重难点</strong>：格式选择是 cuSPARSE 使用的关键。行访问为主的运算（如 SpMV 按行输出）通常用 <strong>CSR</strong> 更高效；列访问为主则考虑 <strong>CSC</strong>。<strong>HYB</strong> 适合非零元分布极不均匀的矩阵。若从稠密或其它格式转换，需注意转换 API 与描述符的一致性，避免指针或维度错误。</p></blockquote><hr><h2 id="si-cu-blas-ku-shu-8-3-jie" tabindex="-1" id="四、cuBLAS-库（书-8-3-节）">四、cuBLAS 库（书 8.3 节）</h2><p>本节对应书中对 cuBLAS 的讲解：API 风格、Level-1/2/3 运算概念，以及完整示例（saxpy、gemm）。</p><h3 id="4-1-cu-blas-jian-jie-yu-api-feng-ge" tabindex="-1" id="4-1-cuBLAS-简介与-API-风格">4.1 cuBLAS 简介与 API 风格</h3><p><strong>cuBLAS</strong> 是 NVIDIA 实现的 <strong>BLAS（Basic Linear Algebra Subprograms）</strong> 的 GPU 版本，提供与 BLAS 三个层次对应的例程：<strong>Level-1</strong>（向量与向量，如 saxpy）、<strong>Level-2</strong>（矩阵与向量，如 gemv）、<strong>Level-3</strong>（矩阵与矩阵，如 gemm）。这一章会说明：自 cuBLAS 5 起，API 采用 <strong>cuBLAS 2 风格</strong>：使用 <code>cublasHandle_t</code> 句柄管理库状态，支持多流与更好的异步行为；<strong>数据指针均为设备指针</strong>，由调用者在 GPU 上分配好内存后再传入。</p><p>cuBLAS 的命名规则与 BLAS 类似：例如 <code>cublasSaxpy</code> 表示<strong>单精度</strong>（S）的 <strong>axpy</strong>；<code>cublasDgemm</code> 表示<strong>双精度</strong>（D）的 <strong>gemm</strong>（矩阵乘法）。axpy 的数学形式为：</p><p>[<br>y \leftarrow \alpha x + y \quad \text{（逐元素： } y_i = \alpha x_i + y_i \text{）}<br>]</p><p>即向量 (x) 缩放后加至向量 (y)。下表归纳常用运算与典型 API（以单精度为例）：</p><table><thead><tr><th>运算</th><th>BLAS 含义</th><th>典型 cuBLAS API（单精度）</th></tr></thead><tbody><tr><td>axpy</td><td>( y \leftarrow \alpha x + y )</td><td><code>cublasSaxpy</code></td></tr><tr><td>dot</td><td>( \alpha = x^T y )</td><td><code>cublasSdot</code></td></tr><tr><td>gemv</td><td>( y \leftarrow \alpha A x + \beta y )</td><td><code>cublasSgemv</code></td></tr><tr><td>gemm</td><td>( C \leftarrow \alpha A B + \beta C )</td><td><code>cublasSgemm</code></td></tr></tbody></table><h3 id="4-2-wan-zheng-shi-li-cublas-saxpy" tabindex="-1" id="4-2-完整示例：cublasSaxpy">4.2 完整示例：cublasSaxpy</h3><p>下面给出使用 <strong>cublasSaxpy</strong> 实现 ( y \leftarrow \alpha x + y ) 的完整示例。包含：创建 cuBLAS 句柄、在设备上分配并初始化向量、调用 <code>cublasSaxpy</code>、将结果拷回主机并验证。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cublas_v2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                     \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                   \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;CUDA Error: %s:%d, %s\n&quot;</span>, __FILE__, __LINE__, cudaGetErrorString(err)); \</span></span><br><span class="line"><span class="meta">            exit(EXIT_FAILURE);                                                      \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CUBLAS_CHECK(call)                                                          \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        cublasStatus_t status = call;                                               \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (status != CUBLAS_STATUS_SUCCESS) &#123;                                      \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;cuBLAS Error: %s:%d\n&quot;</span>, __FILE__, __LINE__);                    \</span></span><br><span class="line"><span class="meta">            exit(EXIT_FAILURE);                                                      \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> N = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;  <span class="comment">// 1M 元素</span></span><br><span class="line">    <span class="type">size_t</span> size = N * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    <span class="type">float</span> alpha = <span class="number">2.0f</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 主机端分配并初始化</span></span><br><span class="line">    <span class="type">float</span> *h_x = (<span class="type">float</span> *)<span class="built_in">malloc</span>(size);</span><br><span class="line">    <span class="type">float</span> *h_y = (<span class="type">float</span> *)<span class="built_in">malloc</span>(size);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        h_x[i] = <span class="number">1.0f</span>;</span><br><span class="line">        h_y[i] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设备端分配</span></span><br><span class="line">    <span class="type">float</span> *d_x = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">float</span> *d_y = <span class="literal">NULL</span>;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_x, size));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_y, size));</span><br><span class="line">    CHECK(cudaMemcpy(d_x, h_x, size, cudaMemcpyHostToDevice));</span><br><span class="line">    CHECK(cudaMemcpy(d_y, h_y, size, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 cuBLAS 句柄并执行 saxpy: y = alpha*x + y</span></span><br><span class="line">    cublasHandle_t handle;</span><br><span class="line">    CUBLAS_CHECK(cublasCreate(&amp;handle));</span><br><span class="line">    CUBLAS_CHECK(cublasSaxpy(handle, N, &amp;alpha, d_x, <span class="number">1</span>, d_y, <span class="number">1</span>));</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 结果拷回主机</span></span><br><span class="line">    CHECK(cudaMemcpy(h_y, d_y, size, cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 简单验证：y[i] 应为 alpha*1 + 2 = 4.0</span></span><br><span class="line">    <span class="type">float</span> maxError = <span class="number">0.0f</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        <span class="type">float</span> expected = alpha * h_x[i] + <span class="number">2.0f</span>;</span><br><span class="line">        <span class="type">float</span> diff = fabsf(h_y[i] - expected);</span><br><span class="line">        <span class="keyword">if</span> (diff &gt; maxError) maxError = diff;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;cuBLAS Saxpy: max error = %e\n&quot;</span>, maxError);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放资源</span></span><br><span class="line">    CUBLAS_CHECK(cublasDestroy(handle));</span><br><span class="line">    CHECK(cudaFree(d_x));</span><br><span class="line">    CHECK(cudaFree(d_y));</span><br><span class="line">    <span class="built_in">free</span>(h_x);</span><br><span class="line">    <span class="built_in">free</span>(h_y);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译与运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvcc -o cublas_saxpy cublas_saxpy.cu -lcublas</span><br><span class="line">./cublas_saxpy</span><br></pre></td></tr></table></figure><p><strong>说明</strong>：<code>cublasSaxpy(handle, N, &amp;alpha, d_x, incx, d_y, incy)</code> 中，<code>incx</code>/<code>incy</code> 为步长（通常为 1）。cuBLAS 默认使用<strong>列主序</strong>（column-major）存储矩阵；对于一维向量，顺序与 C 数组一致。所有指针必须是<strong>设备指针</strong>，且由调用者保证分配尺寸正确。</p><h3 id="4-3-ju-zhen-cheng-fa-yu-cublas-sgemm" tabindex="-1" id="4-3-矩阵乘法与-cublasSgemm">4.3 矩阵乘法与 cublasSgemm</h3><p>矩阵乘法是 Level-3 BLAS 的核心，( C \leftarrow \alpha A B + \beta C )。cuBLAS 提供 <code>cublasSgemm</code>（单精度）、<code>cublasDgemm</code>（双精度）等。接口形式（与书中及 cuBLAS 文档一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">cublasStatus_t <span class="title function_">cublasSgemm</span><span class="params">(cublasHandle_t handle,</span></span><br><span class="line"><span class="params">    cublasOperation_t transa, cublasOperation_t transb,</span></span><br><span class="line"><span class="params">    <span class="type">int</span> m, <span class="type">int</span> n, <span class="type">int</span> k,</span></span><br><span class="line"><span class="params">    <span class="type">const</span> <span class="type">float</span> *alpha,</span></span><br><span class="line"><span class="params">    <span class="type">const</span> <span class="type">float</span> *A, <span class="type">int</span> lda,</span></span><br><span class="line"><span class="params">    <span class="type">const</span> <span class="type">float</span> *B, <span class="type">int</span> ldb,</span></span><br><span class="line"><span class="params">    <span class="type">const</span> <span class="type">float</span> *beta,</span></span><br><span class="line"><span class="params">    <span class="type">float</span> *C, <span class="type">int</span> ldc)</span>;</span><br></pre></td></tr></table></figure><p>其中 <code>m, n, k</code> 分别表示矩阵 ( C ) 的行数、列数，以及 ( A ) 的列数（也是 ( B ) 的行数）；<code>transa</code>/<code>transb</code> 表示是否对 ( A )、( B ) 做转置；<code>lda</code>、<code>ldb</code>、<code>ldc</code> 为各矩阵的 <strong>leading dimension</strong>（列主序下为行数，或转置后的逻辑行数）。使用 cuBLAS 做矩阵乘法时，通常能获得接近 GPU 峰值性能的吞吐，无需手写 gemm 核函数。</p><blockquote><p><strong>重难点</strong>：(1) <strong>列主序</strong>：cuBLAS 与 Fortran BLAS 一致，矩阵按列优先存储；若你的数据是 C 语言行主序，要么在调用前转置，要么利用 <code>transa</code>/<code>transb</code> 用逻辑上的「转置」来匹配。(2) <strong>设备指针</strong>：所有 <code>A、B、C、x、y</code> 必须指向设备内存，传主机指针会导致未定义行为。(3) <strong>leading dimension</strong>：在列主序下，<code>lda</code> 通常为矩阵的行数（即连续存储的一列的长度），用于表达子矩阵或 strided 布局。</p></blockquote><hr><h2 id="wu-cu-fft-ku-shu-8-4-jie" tabindex="-1" id="五、cuFFT-库（书-8-4-节）">五、cuFFT 库（书 8.4 节）</h2><p>本节对应书中对 cuFFT 的讲解：库的定位、一维/多维 FFT 的规划与执行，以及完整示例。</p><h3 id="5-1-cu-fft-jian-jie" tabindex="-1" id="5-1-cuFFT-简介">5.1 cuFFT 简介</h3><p><strong>cuFFT</strong> 是 NVIDIA 提供的 <strong>快速傅里叶变换（FFT）</strong> GPU 库，支持一维、二维、三维 FFT，以及实数到复数（R2C）、复数到实数（C2R）、复数到复数（C2C）等多种类型。这一章会说明：FFT 在信号处理、图像处理、求解偏微分方程等领域应用广泛，手写高性能 FFT 核函数难度大，使用 cuFFT 可以快速获得接近最优的性能。</p><p>cuFFT 的典型流程为：(1) <strong>创建计划</strong>（plan）——<code>cufftPlan1d</code> / <code>cufftPlan2d</code> / <code>cufftPlan3d</code> 或 <code>cufftPlanMany</code>；(2) <strong>执行 FFT</strong>——<code>cufftExecR2C</code>、<code>cufftExecC2R</code>、<code>cufftExecC2C</code> 等；(3) <strong>销毁计划</strong>——<code>cufftDestroy</code>。计划中会包含 FFT 的维度、批大小、数据类型等信息，执行阶段可多次复用同一计划，从而摊薄规划开销。</p><h3 id="5-2-yi-wei-shi-shu-fft-wan-zheng-shi-li" tabindex="-1" id="5-2-一维实数-FFT-完整示例">5.2 一维实数 FFT 完整示例</h3><p>下面给出与书中风格一致的、<strong>一维实数 FFT（R2C）</strong> 的完整示例：对长度为 <code>N</code> 的实序列做正向 FFT，得到 ( N/2+1 ) 个复数（利用对称性）；再做逆变换 C2R 可与原数据对比验证。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;math.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cufft.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                     \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                   \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;CUDA Error: %s:%d, %s\n&quot;</span>, __FILE__, __LINE__, cudaGetErrorString(err)); \</span></span><br><span class="line"><span class="meta">            exit(EXIT_FAILURE);                                                      \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> N 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="type">size_t</span> real_size = N * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    <span class="type">size_t</span> complex_size = (N / <span class="number">2</span> + <span class="number">1</span>) * <span class="keyword">sizeof</span>(cufftComplex);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 主机端：准备实数输入</span></span><br><span class="line">    <span class="type">float</span> *h_signal = (<span class="type">float</span> *)<span class="built_in">malloc</span>(real_size);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        h_signal[i] = (<span class="type">float</span>)<span class="built_in">sin</span>(<span class="number">2.0</span> * M_PI * <span class="number">7.0</span> * i / N);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设备端分配</span></span><br><span class="line">    <span class="type">float</span> *d_signal = <span class="literal">NULL</span>;</span><br><span class="line">    cufftComplex *d_spectrum = <span class="literal">NULL</span>;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_signal, real_size));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_spectrum, complex_size));</span><br><span class="line">    CHECK(cudaMemcpy(d_signal, h_signal, real_size, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 cuFFT 计划并执行 R2C</span></span><br><span class="line">    cufftHandle plan;</span><br><span class="line">    cufftPlan1d(&amp;plan, N, CUFFT_R2C, <span class="number">1</span>);</span><br><span class="line">    cufftExecR2C(plan, d_signal, d_spectrum);</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 可选：逆变换 C2R 验证（书中可给出完整验证）</span></span><br><span class="line">    cufftDestroy(plan);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaFree(d_signal));</span><br><span class="line">    CHECK(cudaFree(d_spectrum));</span><br><span class="line">    <span class="built_in">free</span>(h_signal);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;cuFFT R2C 1D completed for N = %d\n&quot;</span>, N);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译与运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvcc -o cufft_r2c cufft_r2c.cu -lcufft</span><br><span class="line">./cufft_r2c</span><br></pre></td></tr></table></figure><p><strong>说明</strong>：实数 FFT 结果具有共轭对称性，cuFFT 只输出 ( N/2+1 ) 个复数，节省存储与带宽。<code>cufftExec*</code> 为<strong>异步</strong>调用，若需与主机或其他内核同步，应在后续使用 <code>cudaDeviceSynchronize()</code> 或流/事件。</p><h3 id="5-3-cu-fft-yu-gong-shi-dui-ying-ji-yi-cuo-dian" tabindex="-1" id="5-3-cuFFT-与公式对应及易错点">5.3 cuFFT 与公式对应及易错点</h3><p>离散傅里叶变换（DFT）的正向形式（书中常给出）为：</p><p>[<br>X_k = \sum_{n=0}^{N-1} x_n , e^{-2\pi i k n / N}, \quad k = 0, \ldots, N-1.<br>]</p><p>对于实数输入，( X_k = \overline{X_{N-k}} )，因此只需存储 ( k = 0, \ldots, N/2 )。cuFFT 的 R2C 输出即对应 ( X_0, \ldots, X_{N/2} )。逆变换 C2R 由 ( X_0, \ldots, X_{N/2} ) 恢复实序列 ( x_n )。单位与归一化（如是否除以 ( N )）以 cuFFT 文档为准。</p><blockquote><p><strong>易错点</strong>：(1) <strong>R2C 输出长度</strong>：一维实数 FFT 输出是 <strong>N/2+1</strong> 个复数，不是 N；分配频谱缓冲区时必须按此大小，否则越界。(2) <strong>异步执行</strong>：<code>cufftExec*</code> 与 cuBLAS 类似，默认在当前流上异步执行；若紧接着在主机上使用结果或启动依赖该结果的内核，需显式同步或使用流与事件（第 6 章）。</p></blockquote><hr><h2 id="liu-cu-rand-ku-shu-8-5-jie" tabindex="-1" id="六、cuRAND-库（书-8-5-节）">六、cuRAND 库（书 8.5 节）</h2><p>本节对应书中对 cuRAND 的讲解：在设备端或主机端生成随机数、生成器类型与完整示例。</p><h3 id="6-1-cu-rand-jian-jie" tabindex="-1" id="6-1-cuRAND-简介">6.1 cuRAND 简介</h3><p><strong>cuRAND</strong> 提供在 GPU 上或由 GPU 支持的主机端<strong>高质量随机数生成</strong>。这一章会说明：蒙特卡洛模拟、随机算法、随机测试数据生成等场景都需要大量随机数，在设备端用 cuRAND 生成可以避免主机到设备的大规模传输，并可与核函数融合使用。cuRAND 支持多种生成器（如 XORWOW、MRG32k3a、Philox 等），以及均匀分布、正态分布等不同分布。</p><p>典型用法分为两类：(1) <strong>设备端 API</strong>：在核函数或设备代码中调用 <code>curand_init</code> 初始化状态，再用 <code>curand</code> 系列函数生成随机数；(2) <strong>主机端 API</strong>：使用 <code>curandGenerateUniform</code>、<code>curandGenerateNormal</code> 等，由库在 GPU 上生成随机数并写入设备内存，主机通过 <code>cudaMemcpy</code> 取回（若需要）。两种用法均有示例，可参考原书或库文档。</p><h3 id="6-2-zhu-ji-duan-api-shi-li-sheng-cheng-jun-yun-fen-bu-sui-ji-shu" tabindex="-1" id="6-2-主机端-API-示例：生成均匀分布随机数">6.2 主机端 API 示例：生成均匀分布随机数</h3><p>下面给出与书中风格一致的、使用 <strong>主机端 cuRAND API</strong> 在设备上生成均匀分布随机数并拷回主机的完整示例。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;curand.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                     \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                   \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;CUDA Error: %s:%d, %s\n&quot;</span>, __FILE__, __LINE__, cudaGetErrorString(err)); \</span></span><br><span class="line"><span class="meta">            exit(EXIT_FAILURE);                                                      \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CURAND_CHECK(call)                                                          \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                            \</span></span><br><span class="line"><span class="meta">        curandStatus_t status = call;                                               \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (status != CURAND_STATUS_SUCCESS) &#123;                                      \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;cuRAND Error: %s:%d\n&quot;</span>, __FILE__, __LINE__);                    \</span></span><br><span class="line"><span class="meta">            exit(EXIT_FAILURE);                                                      \</span></span><br><span class="line"><span class="meta">        &#125;                                                                           \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> N = <span class="number">1000000</span>;</span><br><span class="line">    <span class="type">size_t</span> size = N * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_data = <span class="literal">NULL</span>;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_data, size));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 cuRAND 生成器（主机端 API）</span></span><br><span class="line">    curandGenerator_t gen;</span><br><span class="line">    CURAND_CHECK(curandCreateGenerator(&amp;gen, CURAND_RNG_PSEUDO_XORWOW));</span><br><span class="line">    CURAND_CHECK(curandSetPseudoRandomGeneratorSeed(gen, <span class="number">1234ULL</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在设备上生成 [0, 1) 均匀分布</span></span><br><span class="line">    CURAND_CHECK(curandGenerateUniform(gen, d_data, N));</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 拷回主机并简单统计</span></span><br><span class="line">    <span class="type">float</span> *h_data = (<span class="type">float</span> *)<span class="built_in">malloc</span>(size);</span><br><span class="line">    CHECK(cudaMemcpy(h_data, d_data, size, cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    <span class="type">double</span> sum = <span class="number">0.0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; N; i++) sum += h_data[i];</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;cuRAND Uniform: mean = %.6f (expected 0.5)\n&quot;</span>, sum / N);</span><br><span class="line"></span><br><span class="line">    CURAND_CHECK(curandDestroyGenerator(gen));</span><br><span class="line">    CHECK(cudaFree(d_data));</span><br><span class="line">    <span class="built_in">free</span>(h_data);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译与运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvcc -o curand_uniform curand_uniform.cu -lcurand</span><br><span class="line">./curand_uniform</span><br></pre></td></tr></table></figure><p><strong>说明</strong>：<code>curandGenerateUniform(gen, d_data, N)</code> 在设备内存 <code>d_data</code> 中写入 N 个 ([0,1)) 均匀分布的单精度随机数。其它分布（如 <code>curandGenerateNormal</code>）以及设备端在核函数内调用 cuRAND 的写法可参考原书或 cuRAND 文档。</p><hr><h2 id="qi-cuda-6-zhong-de-ku-te-xing-shu-8-6-jie" tabindex="-1" id="七、CUDA-6-中的库特性（书-8-6-节）">七、CUDA 6 中的库特性（书 8.6 节）</h2><p>本节对应书中对 <strong>CUDA 6</strong> 引入的、与库相关特性的介绍：<strong>多 GPU 支持</strong>、<strong>统一内存（Unified Memory）</strong> 与库的集成等。</p><h3 id="7-1-duo-gpu-yu-ku" tabindex="-1" id="7-1-多-GPU-与库">7.1 多 GPU 与库</h3><p>这一章会说明：自 CUDA 6 起，部分库开始更好地支持<strong>多 GPU</strong> 场景。例如，应用可以在不同设备上创建各自的 cuBLAS 句柄，并配合 <code>cudaSetDevice()</code> 与多流，将不同线性代数任务分配到不同 GPU 上执行。库的 API 通常与单 GPU 一致，多 GPU 的协调由应用程序通过设备选择与流来管理。这与第 9 章多 GPU 编程的主题相衔接。</p><h3 id="7-2-tong-yi-nei-cun-yu-ku" tabindex="-1" id="7-2-统一内存与库">7.2 统一内存与库</h3><p><strong>统一内存（Unified Memory）</strong> 允许使用同一指针在主机与设备间透明迁移数据，减少显式的 <code>cudaMemcpy</code>。CUDA 6 中库对统一内存的支持方面：部分库在传入「托管」指针时能正确在设备上访问数据，从而简化主机端代码。具体支持情况以各库文档为准。开发者在使用库时，可优先查阅当前 CUDA 版本文档中「Unified Memory」与各库的兼容说明，以便在简化代码与性能之间做出选择。</p><hr><h2 id="ba-cuda-ku-xing-neng-gai-lan-shu-8-7-jie" tabindex="-1" id="八、CUDA-库性能概览（书-8-7-节）">八、CUDA 库性能概览（书 8.7 节）</h2><p>本节对应书中对<strong>库性能调查</strong>的总结：各库在典型问题规模下的性能关注点与选型建议。</p><h3 id="8-1-xing-neng-dui-bi-de-yi-yi" tabindex="-1" id="8-1-性能对比的意义">8.1 性能对比的意义</h3><p>cuBLAS、cuFFT、cuSPARSE 等在典型基准测试下的性能数据（如 GFlops、带宽利用率、与手写内核或 CPU 实现的对比）。目的是说明：<strong>正确使用库</strong>往往能达到或接近 GPU 理论峰值，且远快于未优化的 CPU 实现。与第 2 章、第 4 章一致，有效带宽可用来评估内存受限型库例程的表现：</p><p>[<br>\text{有效带宽（GB/s）} = \frac{(\text{读字节数} + \text{写字节数})}{\text{运行时间（秒）}}<br>]</p><p>下表为<strong>概念性归纳</strong>（与书中库性能调查对应；具体数值以原书该章性能表为准）：</p><table><thead><tr><th>库</th><th>典型运算</th><th>性能关注点</th></tr></thead><tbody><tr><td>cuBLAS</td><td>gemm, axpy</td><td>接近峰值 TFlops，带宽利用率</td></tr><tr><td>cuFFT</td><td>1D/2D/3D FFT</td><td>与 FFT 规模、批大小相关</td></tr><tr><td>cuSPARSE</td><td>SpMV</td><td>与稀疏模式、格式选择相关</td></tr><tr><td>cuRAND</td><td>均匀/正态分布</td><td>生成吞吐（样本/秒）</td></tr></tbody></table><h3 id="8-2-xuan-xing-yu-shi-yong-jian-yi" tabindex="-1" id="8-2-选型与使用建议">8.2 选型与使用建议</h3><p>选型与使用建议可归纳为：(1) <strong>优先选用与问题匹配的库</strong>，避免重复造轮子；(2) <strong>注意数据布局与 API 约定</strong>（如列主序、复数格式）；(3) <strong>复用计划/句柄</strong>，减少重复创建与销毁的开销；(4) <strong>结合流与多 GPU</strong>（第 6 章、第 9 章）实现更大规模的并行与重叠。这些与前面章节形成呼应。</p><hr><h2 id="jiu-shi-yong-open-acc-shu-8-8-jie" tabindex="-1" id="九、使用-OpenACC（书-8-8-节）">九、使用 OpenACC（书 8.8 节）</h2><p>本节对应书中对 <strong>OpenACC</strong> 的讲解：指令式并行编程模型、常用指令与数据管理、与 CUDA 的对比。</p><h3 id="9-1-open-acc-jian-jie" tabindex="-1" id="9-1-OpenACC-简介">9.1 OpenACC 简介</h3><p><strong>OpenACC</strong> 是一种基于<strong>编译器指令</strong>的并行编程模型，允许程序员在现有 C/C++ 或 Fortran 代码中通过 <strong>pragma</strong> 标注并行区域和数据映射，由支持 OpenACC 的编译器（如 PGI/NVIDIA HPC SDK 的 nvc/nvc++）自动生成主机与设备代码，并将指定区域卸载到 GPU 执行。这一章会说明：OpenACC 的定位是<strong>渐进式加速</strong>——不需要重写整个程序为 CUDA，只需在热点循环或区域添加指令，适合遗留代码或希望减少编程负担的场景。</p><p>OpenACC 与 CUDA 的关系可以概括为：<strong>OpenACC 是指令层抽象，CUDA 是显式 API 与核函数</strong>。OpenACC 编译器最终可能生成 CUDA 代码或直接调用 CUDA 运行时；使用 OpenACC 不要求开发者直接写 <code>__global__</code> 核函数，但理解 GPU 执行模型有助于写出更高效的指令（如循环划分、数据局部性）。</p><h3 id="9-2-chang-yong-zhi-ling-yu-shu-ju-qu-yu" tabindex="-1" id="9-2-常用指令与数据区域">9.2 常用指令与数据区域</h3><p>常用 OpenACC 指令包括：</p><table><thead><tr><th>指令类型</th><th>典型写法</th><th>含义</th></tr></thead><tbody><tr><td><strong>并行区域</strong></td><td><code>#pragma acc parallel</code></td><td>后续结构化块在加速器上并行执行</td></tr><tr><td><strong>循环</strong></td><td><code>#pragma acc loop</code></td><td>标注可并行化的循环，由编译器映射到 GPU 线程</td></tr><tr><td><strong>数据区域</strong></td><td><code>#pragma acc data copyin(A) copyout(B)</code></td><td>进入区域时将 A 拷入设备，离开时将 B 拷回主机</td></tr><tr><td><strong>计算区域</strong></td><td><code>#pragma acc kernels</code></td><td>由编译器自动分析并并行化区域内代码</td></tr></tbody></table><p>数据子句常见有：<code>copyin</code>（进入时主机→设备）、<code>copyout</code>（离开时设备→主机）、<code>copy</code>（进入拷入、离开拷出）、<code>create</code>（在设备上分配，不拷回）等。可用 <code>parallel</code> + <code>loop</code> 或 <code>kernels</code> 加速循环，具体示例见原书。</p><h3 id="9-3-jian-dan-open-acc-shi-li-xiang-liang-jia-fa" tabindex="-1" id="9-3-简单-OpenACC-示例：向量加法">9.3 简单 OpenACC 示例：向量加法</h3><p>下面给出与书中风格一致的、用 <strong>OpenACC</strong> 实现的向量加法示例。通过 <code>#pragma acc data</code> 管理数据，<code>#pragma acc parallel loop</code> 将循环卸载到 GPU。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> N (1024 * 1024)</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="type">float</span> *A = (<span class="type">float</span> *)<span class="built_in">malloc</span>(N * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    <span class="type">float</span> *B = (<span class="type">float</span> *)<span class="built_in">malloc</span>(N * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    <span class="type">float</span> *C = (<span class="type">float</span> *)<span class="built_in">malloc</span>(N * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        A[i] = <span class="number">1.0f</span>;</span><br><span class="line">        B[i] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// OpenACC：数据拷入设备，计算在加速器上执行，结果拷回</span></span><br><span class="line">    <span class="meta">#<span class="keyword">pragma</span> acc data copyin(A[0:N], B[0:N]) copyout(C[0:N])</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="meta">#<span class="keyword">pragma</span> acc parallel loop</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">            C[i] = A[i] + B[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 验证</span></span><br><span class="line">    <span class="type">float</span> maxError = <span class="number">0.0f</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        <span class="type">float</span> e = C[i] - <span class="number">3.0f</span>;</span><br><span class="line">        <span class="keyword">if</span> (e &lt; <span class="number">0</span>) e = -e;</span><br><span class="line">        <span class="keyword">if</span> (e &gt; maxError) maxError = e;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;OpenACC vector add: max error = %e\n&quot;</span>, maxError);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">free</span>(A);</span><br><span class="line">    <span class="built_in">free</span>(B);</span><br><span class="line">    <span class="built_in">free</span>(C);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译（需 OpenACC 编译器，如 NVIDIA HPC SDK 的 nvc）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvc -acc -Minfo=accel -o vecadd_acc vecadd_acc.c</span><br><span class="line">./vecadd_acc</span><br></pre></td></tr></table></figure><p><strong>说明</strong>：<code>#pragma acc data</code> 划定数据生命周期；<code>copyin(A[0:N], B[0:N])</code> 表示将 A、B 拷入设备；<code>copyout(C[0:N])</code> 表示在离开区域时将 C 拷回主机。<code>#pragma acc parallel loop</code> 表示接下来的 for 循环在加速器上并行执行。实际性能取决于编译器对循环的映射与数据移动策略，可通过 <code>-Minfo=accel</code> 查看编译器生成的加速信息。</p><h3 id="9-4-open-acc-yu-cuda-de-dui-bi" tabindex="-1" id="9-4-OpenACC-与-CUDA-的对比">9.4 OpenACC 与 CUDA 的对比</h3><p>OpenACC 与 CUDA 的简要对比归纳如下：</p><table><thead><tr><th>维度</th><th>OpenACC</th><th>CUDA C</th></tr></thead><tbody><tr><td><strong>编程方式</strong></td><td>指令标注，编译器生成并行代码</td><td>显式写核函数与内存/流 API</td></tr><tr><td><strong>可移植性</strong></td><td>可面向多厂商加速器（若编译器支持）</td><td>面向 NVIDIA GPU</td></tr><tr><td><strong>控制粒度</strong></td><td>较粗，依赖编译器优化</td><td>细粒度，可精确控制线程与内存</td></tr><tr><td><strong>适用场景</strong></td><td>遗留代码渐进式加速、快速原型</td><td>高性能需求、深度调优、库开发</td></tr></tbody></table><p>理解两者关系后，可以在同一项目中混用：热点用 CUDA 或库，其余用 OpenACC 快速移植。</p><blockquote><p><strong>理解与体会</strong>：OpenACC 的<strong>数据子句</strong>是易错点：<code>copyin</code>/<code>copyout</code>/<code>copy</code>/<code>create</code> 决定了数据何时在主机与设备间移动，写错会导致结果未更新或重复拷贝。另外，OpenACC 依赖<strong>支持 OpenACC 的编译器</strong>（如 nvc），与纯 nvcc 的 CUDA 项目在构建链上不同；若团队已有大量 Fortran/C 科学计算代码，用 OpenACC 做「最小侵入」的 GPU 加速往往比全量重写为 CUDA 更现实。</p></blockquote><hr><h2 id="shi-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="十、本章小结与重难点回顾">十、本章小结与重难点回顾</h2><h3 id="10-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="10-1-知识小结（与书中对应）">10.1 知识小结（与书中对应）</h3><ul><li><strong>CUDA 库</strong>：cuBLAS（稠密线性代数）、cuFFT（FFT）、cuSPARSE（稀疏运算）、cuRAND（随机数）；优先用库可减少开发成本并获接近最优性能。选用时按问题域匹配库，注意链接（-lcublas -lcufft 等）与头文件。</li><li><strong>库的通用模式</strong>：句柄/计划管理、<strong>设备指针</strong>、正确链接；注意数据布局（如 cuBLAS 列主序）与 API 约定；多数库调用为<strong>异步</strong>，需在需要时同步或配合流。</li><li><strong>cuBLAS</strong>：Level-1/2/3（saxpy、gemv、gemm）；列主序、leading dimension；所有数据指针为设备指针。</li><li><strong>cuFFT</strong>：Plan → Execute → Destroy；R2C 输出长度为 N/2+1；cufftExec* 为异步。</li><li><strong>cuSPARSE</strong>：稀疏格式（CSR、COO、CSC、HYB）与 SpMV；格式选择对性能影响大。</li><li><strong>cuRAND</strong>：主机端 API（curandGenerateUniform 等）与设备端在核函数内初始化；多种生成器与分布。</li><li><strong>CUDA 6</strong>：多 GPU 与统一内存在库中的支持，便于多设备与简化数据管理。</li><li><strong>OpenACC</strong>：指令式并行，<code>#pragma acc data</code>、<code>parallel</code>、<code>loop</code>、<code>kernels</code>；适合渐进式加速与遗留代码，与 CUDA 可互补。</li></ul><h3 id="10-2-zhong-nan-dian-su-cha" tabindex="-1" id="10-2-重难点速查">10.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>库的选用</strong></td><td>稠密线性代数→cuBLAS；FFT→cuFFT；稀疏→cuSPARSE；随机数→cuRAND。能库则库，不能库再手写。</td></tr><tr><td><strong>cuBLAS 列主序与设备指针</strong></td><td>矩阵按列主序；所有 A、B、C、x、y 为设备指针；C 行主序数据需转置或用 trans 参数。</td></tr><tr><td><strong>cuFFT R2C 长度与异步</strong></td><td>一维实数 FFT 输出为 N/2+1 个复数；cufftExec* 异步，需同步或流。</td></tr><tr><td><strong>cuSPARSE 格式选择</strong></td><td>CSR 常用于行访问为主的 SpMV；格式选择显著影响性能。</td></tr><tr><td><strong>OpenACC 数据子句</strong></td><td>copyin/copyout/copy/create 决定数据移动时机；写错会导致结果错误或多余拷贝。</td></tr><tr><td><strong>OpenACC 与 CUDA 的取舍</strong></td><td>遗留代码、快速移植用 OpenACC；高性能、深度调优、库开发用 CUDA。</td></tr></tbody></table><h3 id="10-3-xue-xi-si-kao" tabindex="-1" id="10-3-学习思考">10.3 学习思考</h3><ul><li><strong>与第 2 章的衔接</strong>：第 2 章介绍了性能模型与「计算时间 vs 传输时间」；使用库时，同样要关注数据在主机与设备间的移动（cudaMemcpy 或 OpenACC 的 data 子句），以及库调用与流的重叠（第 6 章），才能发挥库的吞吐优势。</li><li><strong>与第 6 章的关系</strong>：库的 API 多数支持将句柄与流绑定（如 cublasSetStream），从而在多流上重叠多个库调用或库与手写内核的重叠；第 9 章多 GPU 则涉及每设备独立句柄与流。</li><li><strong>实践建议</strong>：(1) 新项目优先查是否有现成库可覆盖问题域；(2) 使用库时严格遵循文档中的数据布局（列主序、R2C 长度等）和指针要求（设备指针）；(3) 复用句柄/计划，避免在循环内反复创建销毁；(4) 需要与主机或其他内核同步时，显式调用 cudaDeviceSynchronize 或使用事件与流。</li></ul><p>下表为本章<strong>自检要点</strong>（与书中描述一致）：</p><table><thead><tr><th>要点</th><th>说明</th></tr></thead><tbody><tr><td>库的选用</td><td>cuBLAS/cuFFT/cuSPARSE/cuRAND 按问题域选用；能库则库</td></tr><tr><td>cuBLAS</td><td>句柄、设备指针、列主序；saxpy、gemm 的典型参数</td></tr><tr><td>cuFFT</td><td>Plan → Exec → Destroy；R2C 输出 N/2+1；异步</td></tr><tr><td>cuSPARSE</td><td>CSR/COO 等格式；SpMV 流程；格式影响性能</td></tr><tr><td>cuRAND</td><td>主机端 curandGenerateUniform；设备端 curand_init + curand</td></tr><tr><td>OpenACC</td><td>data copyin/copyout；parallel loop；与 CUDA 对比</td></tr><tr><td>链接</td><td>-lcublas -lcufft -lcurand 等；头文件 cublas_v2.h、cufft.h 等</td></tr></tbody></table><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 9 章：多 GPU 编程</strong>：</p><ul><li>多 GPU 系统拓扑与通信（PCIe、NVLink 等）</li><li>单机多 GPU 的线程与进程模型（单进程多设备、多进程）</li><li>多 GPU 间的数据划分与负载均衡</li><li>多 GPU 与库、流的结合</li></ul><p>从单 GPU 的库与指令，扩展到多 GPU 的协同——我们下一章见。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>cuBLAS 的矩阵存储顺序是什么？若用 C 语言行主序存储，调用 cublasSgemm 时应注意什么？</strong></li><li><strong>一维实数 FFT（R2C）输出有多少个复数？为何不是 N？</strong></li><li><strong>OpenACC 中 <code>#pragma acc data</code> 与 <code>#pragma acc parallel loop</code> 分别负责什么？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>cuBLAS 使用列主序（column-major），与 C 的行主序相反。用 C 存矩阵时，可把 A、B 视为「逻辑上的转置」传入，或调用时交换 A/B 与 trans 参数以等效行主序乘法。</li><li>实数序列的 FFT 具有共轭对称性，只需存 N/2+1 个复数即可完整表示。因此 R2C 输出长度为 N/2+1，不是 N。</li><li><code>#pragma acc data</code> 定义数据区域，在进入区域时把数据拷到设备、离开时拷回（或指定 copyin/copyout 等）。<code>#pragma acc parallel loop</code> 将紧接着的循环在设备上并行执行，可配合 data 区域使用。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/1aaa52d4/">第7章 调整指令级原语</a> | 下一篇：<a href="https://smarter.xin/posts/844cd057/">第9章 多GPU编程</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 8 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-ba-zhang-gpu-jia-su-ku-he-open-acc&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="cuBLAS" scheme="https://smarter.xin/tags/cublas/"/>
    
    <category term="cuFFT" scheme="https://smarter.xin/tags/cufft/"/>
    
    <category term="OpenACC" scheme="https://smarter.xin/tags/openacc/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第七章：调整指令级原语</title>
    <link href="https://smarter.xin/posts/1aaa52d4/"/>
    <id>https://smarter.xin/posts/1aaa52d4/</id>
    <published>2026-02-05T08:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-qi-zhang-diao-zheng-zhi-ling-ji-yuan-yu" tabindex="-1">CUDA C编程权威指南-第七章：调整指令级原语</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/d47d974/">第6章 流和并发</a> | 下一篇：<a href="https://smarter.xin/posts/e466b0a5/">第8章 GPU加速库和OpenACC</a></p><blockquote><p>系列第 7 篇。第 6 章讲了流与并发；这一章回到<strong>单次内核内部</strong>，在内存和执行配置已经优化过的基础上，从<strong>指令级</strong>再挖一点性能：<strong>warp 级原语</strong>（洗牌、表决、同步、活跃掩码）、<strong>循环展开</strong>和<strong>快速数学</strong>，让内核的指令吞吐和延迟隐藏更好一点。</p></blockquote><hr><h2 id="qian-yan-cong-nei-cun-yu-liu-dao-zhi-ling-ji" tabindex="-1" id="前言：从内存与流到指令级">前言：从内存与流到指令级</h2><p>到目前为止，我们已经在<strong>编程模型</strong>（第2章）、<strong>执行模型</strong>（第3章）、<strong>全局内存</strong>（第4章）、<strong>共享内存与常量内存</strong>（第5章）以及<strong>流与并发</strong>（第6章）上做了系统学习。在单次内核内部，性能既受<strong>内存访问</strong>影响，也受<strong>指令发射与执行</strong>影响。这一节提到：当内存访问已经优化到一定程度后，<strong>指令级</strong>的细节——例如同一 warp 内线程如何高效协作、如何减少分支与循环开销、如何在精度与速度之间取舍——就会成为进一步榨取性能的关键。</p><p>本章将系统学习「调整指令级原语」，主要包括：</p><ul><li><strong>线程束洗牌再探</strong>：在第5章入门基础上，深入 <code>__shfl_*_sync</code> 的各类变体、掩码语义及与归约/扫描的完整结合。</li><li><strong>线程束表决函数</strong>：<code>__ballot_sync</code>、<code>__all_sync</code>、<code>__any_sync</code> 的用法与典型场景（条件计数、全/任一满足判断）。</li><li><strong>活跃掩码与 warp 同步</strong>：<code>__activemask()</code> 的适用与误用、<code>__syncwarp()</code> 的细粒度同步与内存栅栏。</li><li><strong>循环展开</strong>：<code>#pragma unroll</code> 与手写展开对指令吞吐与延迟隐藏的影响（与第3章归约中的展开相呼应）。</li><li><strong>快速数学与精度</strong>：编译器快速数学选项（如 <code>--use_fast_math</code>）、单/双精度与指令吞吐的取舍。</li></ul><p>通过本章，你将对「warp 内协作」和「指令级调优」有完整认识，并能在实际内核中正确、安全地使用这些原语。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-diao-zheng-zhi-ling-ji-yuan-yu" tabindex="-1" id="1-1-为什么要学「调整指令级原语」">1.1 为什么要学「调整指令级原语」</h3><p>第 4～6 章从<strong>内存</strong>（全局、共享、常量）和<strong>流与并发</strong>角度做了优化；但在单次内核内部，性能还受<strong>指令发射与执行</strong>制约。需要搞清楚的是：当内存访问已优化到一定程度后，<strong>指令级</strong>细节——同一 warp 内如何高效协作、如何减少分支与循环开销、如何在精度与速度之间取舍——会成为进一步榨取性能的关键。<strong>指令级原语</strong>是直接对应硬件能力、在 warp 内完成协作或信息交换的<strong>内建函数</strong>（洗牌、表决、warp 同步、活跃掩码）；配合<strong>循环展开</strong>与<strong>快速数学</strong>，可在不增加内存带宽的前提下提高有效指令吞吐、更好隐藏延迟。本章与第 3 章（延迟隐藏、归约与展开）、第 5 章（洗牌入门）紧密衔接，是「从内存优化到指令优化」的必经一步。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解<strong>指令级原语</strong>的动机与分类（洗牌、表决、同步、活跃掩码）</td><td>能说明为何在内存优化之后仍需指令级调优；能列举四类原语及典型用途</td></tr><tr><td>掌握<strong>洗牌四变体</strong>的语义与适用场景（sync/down/up/xor）</td><td>能写出 warp 内树形归约与蝶形归约片段；说明 mask 与 width 的含义</td></tr><tr><td>掌握<strong>参与掩码</strong>的正确获取方式（ballot 生成、不可用 activemask 代替）</td><td>能解释为何在分化分支内用 __activemask 做归约掩码会出错；会写 ballot_sync 生成 mask 再洗牌</td></tr><tr><td>掌握<strong>表决函数</strong>：ballot / all / any 的语义与典型用法</td><td>能写出「满足条件的线程数」与「全/任一为真」的集体判断</td></tr><tr><td>理解 <strong>__activemask</strong> 与 <strong>__syncwarp</strong> 的适用与误用</td><td>能说明 __activemask 不同步、不可替代 ballot 做参与掩码；__syncwarp 只同步 warp、不能替代 __syncthreads</td></tr><tr><td>理解<strong>循环展开</strong>对指令吞吐与延迟隐藏的作用</td><td>能说明 #pragma unroll 与手写展开的取舍；注意寄存器压力与占用率权衡</td></tr><tr><td>理解<strong>快速数学</strong>与单/双精度对吞吐与精度的影响</td><td>能说明 --use_fast_math 的利弊；单精度相对双精度的吞吐优势</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">第 7 章  调整指令级原语</span><br><span class="line">│</span><br><span class="line">├── 二、指令级优化与线程束原语概述（书 7.1 节）</span><br><span class="line">│   ├── 为什么需要指令级调整</span><br><span class="line">│   └── 线程束原语的分类与适用场景、_sync 与 mask 语义 ★</span><br><span class="line">│</span><br><span class="line">├── 三、线程束洗牌再探（书 7.2 节）★ 重难点</span><br><span class="line">│   ├── 洗牌四种变体（sync/down/up/xor）</span><br><span class="line">│   ├── 基于洗牌的 warp 内归约（完整实现）</span><br><span class="line">│   └── __shfl_xor 与蝶形归约</span><br><span class="line">│</span><br><span class="line">├── 四、线程束表决函数（书 7.3 节）</span><br><span class="line">│   ├── __ballot_sync：条件为真的线程位掩码、参与掩码的正确用法</span><br><span class="line">│   └── __all_sync 与 __any_sync</span><br><span class="line">│</span><br><span class="line">├── 五、活跃掩码与 warp 同步（书 7.4 节）★ 易错点</span><br><span class="line">│   ├── __activemask 的语义与误用（不可在分化分支内作归约掩码）</span><br><span class="line">│   └── __syncwarp：warp 内同步与内存栅栏</span><br><span class="line">│</span><br><span class="line">├── 六、循环展开与指令级并行（书 7.5 节）</span><br><span class="line">│   ├── 为何展开能提升指令吞吐</span><br><span class="line">│   └── #pragma unroll 与手写展开</span><br><span class="line">│</span><br><span class="line">├── 七、快速数学与精度取舍（书 7.6 节）</span><br><span class="line">│   ├── --use_fast_math 与 intrinsic 行为</span><br><span class="line">│   └── 单精度与双精度指令吞吐</span><br><span class="line">│</span><br><span class="line">└── 八、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-zhi-ling-ji-you-hua-yu-xian-cheng-shu-yuan-yu-gai-shu-shu-7-1-jie" tabindex="-1" id="二、指令级优化与线程束原语概述（书-7-1-节）">二、指令级优化与线程束原语概述（书 7.1 节）</h2><p>本节对应书中对「指令级」优化动机与线程束（warp）原语分类的总体介绍，为全章奠定概念基础。</p><h3 id="2-1-wei-shi-yao-xu-yao-zhi-ling-ji-diao-zheng" tabindex="-1" id="2-1-为什么需要指令级调整">2.1 为什么需要指令级调整</h3><p>GPU 的执行单元按 <strong>SIMT</strong>（单指令多线程）方式工作：同一 warp 内的 32 个线程在同一周期执行同一条指令，各自操作私有数据。第3章已经说明：<strong>指令延迟</strong>（算术约 10～20 周期、全局内存约 400～800 周期）需要靠足够多的<strong>活跃线程束</strong>来隐藏；<strong>线程束分化</strong>会导致同一 warp 内部分线程空转，降低有效吞吐。在内存访问已经通过合并、共享内存、常量内存等手段优化之后，若内核仍受<strong>指令吞吐</strong>或 <strong>warp 内协作效率</strong>限制，就需要在<strong>指令级</strong>做文章。</p><p>这一节提到：<strong>指令级原语</strong>是指那些直接对应硬件能力、在 warp 内完成协作或信息交换的<strong>内建函数</strong>（intrinsics）。它们通常具有以下特点：(1) 在 warp 内线程间直接交换数据或进行集体操作；(2) 不经过全局内存或共享内存，延迟极低；(3) 正确使用时能显著减少内存访问次数和同步开销，提升指令吞吐与能效。典型代表包括：<strong>洗牌</strong>（shuffle）、<strong>表决</strong>（vote）、<strong>warp 同步</strong>（syncwarp）与<strong>活跃掩码查询</strong>（activemask）。第5章已介绍过洗牌的基本用法；本章将系统展开所有与「调整指令级原语」相关的内建函数与编程模式。</p><p>从延迟隐藏的角度，第3章给出的「所需线程数」下界仍适用：每个 SM 上至少需要足够的活跃线程以覆盖指令延迟。原书表 3-3 等给出的典型值为：算术指令延迟约 10～20 周期，全局内存访问延迟约 400～800 周期。因此仅从算术角度，每 SM 至少需约「每 SM 核心数 × 算术延迟」个线程；<strong>指令级原语</strong>通过减少每条「逻辑操作」对应的内存访问和同步次数，在相同线程规模下提高<strong>有效指令吞吐</strong>，从而更好地利用这些线程隐藏延迟。下表归纳指令类型与典型延迟（与第3章一致）：</p><table><thead><tr><th>指令类型</th><th>典型延迟（周期）</th><th>说明</th></tr></thead><tbody><tr><td>算术指令</td><td>10～20</td><td>如 32 位浮点乘加</td></tr><tr><td>全局内存访问</td><td>400～800</td><td>需大量线程束隐藏</td></tr><tr><td>共享内存访问</td><td>约数十</td><td>远低于全局内存</td></tr><tr><td>洗牌/寄存器交换</td><td>约 1～2 条指令</td><td>无内存参与，延迟极低</td></tr></tbody></table><h3 id="2-2-xian-cheng-shu-yuan-yu-de-fen-lei-yu-gua-yong-chang-jing" tabindex="-1" id="2-2-线程束原语的分类与适用场景">2.2 线程束原语的分类与适用场景</h3><p>书中及 CUDA 编程指南将 warp 级原语按功能分为几类，便于我们在不同场景下选用。下表归纳了本章涉及的主要原语及其典型用途（与书中描述一致）：</p><table><thead><tr><th>类别</th><th>原语示例</th><th>典型用途</th></tr></thead><tbody><tr><td><strong>数据交换</strong></td><td><code>__shfl_sync</code>, <code>__shfl_down_sync</code>, <code>__shfl_up_sync</code>, <code>__shfl_xor_sync</code></td><td>warp 内归约、扫描、广播、butterfly 交换</td></tr><tr><td><strong>表决</strong></td><td><code>__ballot_sync</code>, <code>__all_sync</code>, <code>__any_sync</code></td><td>条件满足计数、全/任一为真判断、参与掩码计算</td></tr><tr><td><strong>同步与栅栏</strong></td><td><code>__syncwarp</code></td><td>warp 内细粒度同步、共享内存读写之间的栅栏</td></tr><tr><td><strong>活跃掩码</strong></td><td><code>__activemask</code></td><td>查询当前与己一起执行的线程掩码（需谨慎使用）</td></tr></tbody></table><p><strong>同步语义</strong>：自 CUDA 9 起，带 <strong><code>_sync</code></strong> 后缀的 warp 原语要求传入一个 <strong>32 位掩码</strong>（mask），指明<strong>参与该集体操作的线程</strong>。这些原语会在参与线程间形成<strong>隐式同步</strong>：所有在 mask 中的线程必须都执行到该原语，集体操作才会正确完成。在 Volta 及以后架构上，线程可独立取指（independent thread scheduling），同一 warp 内线程不再保证锁步执行，因此<strong>必须</strong>使用带 <code>_sync</code> 的原语并正确设置 mask，否则会出现未定义行为或错误结果。mask 应由<strong>程序逻辑</strong>确定（例如由 <code>__ballot_sync(FULL_MASK, predicate)</code> 根据条件计算），而不是在可能分化的分支内用 <code>__activemask()</code> 临时取得，后者在某些用法下会导致错误（见第五节）。</p><blockquote><p><strong>理解与体会</strong>：可以把「指令级原语」理解为 GPU 在 warp 级别提供的「硬件级集体操作」——不经过全局或共享内存，直接在寄存器与线程间交换或表决，延迟极低。用好它们，就能在保持相同线程规模的前提下，减少每条逻辑操作对应的内存访问和同步次数，从而提高有效指令吞吐、更好隐藏延迟。<strong>mask 的正确性</strong>是全章最容易出错的地方：mask 必须与「实际参与该集体操作的线程集合」一致，在部分线程因分支不参与时，必须用 <code>__ballot_sync(FULL_MASK, condition)</code> 在分支前算出参与掩码，而不能在分支内用 <code>__activemask()</code> 代替。</p></blockquote><hr><h2 id="san-xian-cheng-shu-xi-pai-zai-tan-shu-7-2-jie" tabindex="-1" id="三、线程束洗牌再探（书-7-2-节）">三、线程束洗牌再探（书 7.2 节）</h2><p>本节对应书中对 warp 洗牌指令的深入讲解：在第5章基础上，完整覆盖四种洗牌变体、掩码含义、以及它们在归约与扫描中的标准用法。</p><h3 id="3-1-xi-pai-zhi-ling-de-si-chong-bian-ti" tabindex="-1" id="3-1-洗牌指令的四种变体">3.1 洗牌指令的四种变体</h3><p><strong>线程束洗牌</strong>允许同一 warp 内的线程<strong>直接读取</strong>其他线程的<strong>寄存器</strong>值，而无需经过共享内存或全局内存。这一节提到：数据在寄存器间移动，延迟远低于共享内存访问（通常 1～2 条指令即可完成一次交换），且不占用共享内存，有利于提高占用率或把共享内存留给其他用途。与共享内存实现相比：一次 warp 内归约若用共享内存，通常需要「写共享内存 → __syncthreads() → 读共享内存」多轮，而洗牌仅需若干条 <code>__shfl_*_sync</code> 与算术指令，指令数与延迟都更小。</p><p>CUDA 提供四类洗牌内建函数（均带 <code>_sync</code>，需指定 mask）。各变体的数据移动方向可在归约、扫描、广播等模式中选用，简述如下：</p><table><thead><tr><th>函数签名（以 int 为例）</th><th>含义</th></tr></thead><tbody><tr><td><code>__shfl_sync(mask, val, src_lane, width)</code></td><td>从<strong>指定 lane 索引</strong> <code>src_lane</code> 的线程读取 <code>val</code></td></tr><tr><td><code>__shfl_down_sync(mask, val, offset, width)</code></td><td>从<strong>当前 lane + offset</strong> 的线程读取 <code>val</code>（若越界则返回本线程的 <code>val</code>）</td></tr><tr><td><code>__shfl_up_sync(mask, val, offset, width)</code></td><td>从<strong>当前 lane - offset</strong> 的线程读取 <code>val</code></td></tr><tr><td><code>__shfl_xor_sync(mask, val, lane_mask, width)</code></td><td>从 <strong>lane 索引 = 当前 lane XOR lane_mask</strong> 的线程读取 <code>val</code></td></tr></tbody></table><p>参数说明：<strong>mask</strong> 为参与集体操作的线程掩码（通常 <code>0xffffffff</code> 表示整个 warp）；<strong>width</strong> 可选，默认 <code>warpSize</code>（32），可指定更小的「逻辑 warp 宽度」以支持子 warp 操作。例如 <code>__shfl_down_sync</code> 常用于<strong>树形归约</strong>（每轮将下半区间的值「向下」传到当前 lane 并累加），<code>__shfl_xor_sync</code> 常用于<strong>蝶形</strong>（butterfly）模式的归约或扫描（各变体数据移动方向可参考原书图示）。</p><blockquote><p><strong>理解与体会</strong>：洗牌把「warp 内数据交换」从共享内存搬到了<strong>寄存器间直接交换</strong>，既省共享内存带宽与 bank 冲突顾虑，又降低延迟（约 1～2 条指令）。适合「先 warp 内归约/扫描，再块内跨 warp 合并」的两阶段设计；此时共享内存只需存每个 warp 的一个结果，而不是 32 个中间值，与第 5 章的洗牌入门一脉相承。</p></blockquote><h3 id="3-2-ji-yu-xi-pai-de-warp-nei-gui-yue-wan-zheng-shi-xian" tabindex="-1" id="3-2-基于洗牌的-warp-内归约（完整实现）">3.2 基于洗牌的 warp 内归约（完整实现）</h3><p>下面给出与书中风格一致的、<strong>基于洗牌的 warp 内归约</strong>完整实现：每个线程先持有一个值 <code>val</code>，经过 (\log_2(32)=5) 轮 <code>__shfl_down_sync</code> 累加，最终 lane 0 得到该 warp 的和。随后将每个 warp 的部分和写入共享内存，再在块内做一次跨 warp 归约（或第二次洗牌/共享内存合并），得到块级结果。公式上，warp 内归约的步数为：</p><p>[<br>\text{步数} = \lceil \log_2(\text{warpSize}) \rceil = 5 \quad (\text{warpSize}=32)<br>]</p><p>每步一次洗牌 + 一次加法，无共享内存读写，延迟极低。完整核函数示例（含块内跨 warp 合并）如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call) do &#123; \</span></span><br><span class="line"><span class="meta">    cudaError_t e = (call); \</span></span><br><span class="line"><span class="meta">    <span class="keyword">if</span> (e != cudaSuccess) &#123; \</span></span><br><span class="line"><span class="meta">        fprintf(stderr, <span class="string">&quot;CUDA error %s %d: %s\n&quot;</span>, __FILE__, __LINE__, cudaGetErrorString(e)); \</span></span><br><span class="line"><span class="meta">        return; \</span></span><br><span class="line"><span class="meta">    &#125; \</span></span><br><span class="line"><span class="meta">&#125; while(0)</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> warpSize = <span class="number">32</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FULL_MASK 0xffffffff</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 书中风格：warp 内归约，结果在 lane 0</span></span><br><span class="line">__device__ __forceinline__ <span class="type">float</span> <span class="title function_">warpReduceSum</span><span class="params">(<span class="type">float</span> val)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> offset = warpSize / <span class="number">2</span>; offset &gt; <span class="number">0</span>; offset &gt;&gt;= <span class="number">1</span>)</span><br><span class="line">        val += __shfl_down_sync(FULL_MASK, val, offset);</span><br><span class="line">    <span class="keyword">return</span> val;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceShuffleKernel</span><span class="params">(<span class="type">const</span> <span class="type">float</span> *__restrict__ g_in, <span class="type">float</span> *__restrict__ g_out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    __shared__ <span class="type">float</span> s_sum[<span class="number">32</span>];  <span class="comment">// 每 warp 一个槽位，最多 32 个 warp  per block</span></span><br><span class="line">    <span class="type">int</span> tid = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line">    <span class="type">int</span> lane = threadIdx.x % warpSize;</span><br><span class="line">    <span class="type">int</span> warpId = threadIdx.x / warpSize;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> val = (tid &lt; n) ? g_in[tid] : <span class="number">0.0f</span>;</span><br><span class="line">    val = warpReduceSum(val);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (lane == <span class="number">0</span>)</span><br><span class="line">        s_sum[warpId] = val;</span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 块内第一个 warp 将各 warp 部分和再归约</span></span><br><span class="line">    <span class="keyword">if</span> (warpId == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">float</span> blockVal = (lane &lt; (blockDim.x / warpSize)) ? s_sum[lane] : <span class="number">0.0f</span>;</span><br><span class="line">        blockVal = warpReduceSum(blockVal);</span><br><span class="line">        <span class="keyword">if</span> (lane == <span class="number">0</span>)</span><br><span class="line">            g_out[blockIdx.x] = blockVal;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述实现中，<strong>mask</strong> 使用 <code>FULL_MASK</code>，表示整个 warp 参与；若存在<strong>部分线程不参与</strong>（例如根据 <code>tid &lt; n</code> 判断），应先用 <code>__ballot_sync(FULL_MASK, tid &lt; n)</code> 得到参与掩码，再在参与分支内用该掩码调用 <code>__shfl_down_sync(mask, val, offset)</code>，这样未参与线程不会被错误地纳入归约（见第四节表决函数与书中说明）。</p><blockquote><p><strong>易错点</strong>：<strong>洗牌的 mask 必须与参与线程一致</strong>。若内核中有分支导致部分线程不参与归约，则不能使用全 warp 的 <code>FULL_MASK</code>，而必须用<strong>程序逻辑</strong>确定的参与掩码（如 <code>__ballot_sync(FULL_MASK, condition)</code> 的返回值），否则未参与线程的寄存器值会被错误地参与交换，得到错误结果或未定义行为。因此<strong>不要在分化分支内用 __activemask() 代替</strong>——详见第五节。</p></blockquote><p><strong>编译与运行</strong>：将上述核函数与下面的主机代码一起编译即可得到完整可执行程序。主机端负责分配设备内存、拷贝输入、配置网格与块、拷贝结果并验证（此处省略验证逻辑）。建议块大小设为 256 或 512（为 warpSize 的整数倍），以便每个块内 warp 数适中；网格大小根据 (n) 向上取整。编译命令示例：<code>nvcc -O3 -arch=sm_70 -o reduceShuffle reduceShuffle.cu</code>（计算能力按实际设备调整，洗牌需 3.0+）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 主机端：完整调用流程（与书中风格一致）</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">runReduceShuffle</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">size_t</span> bytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    <span class="type">float</span> *d_in = nullptr, *d_out = nullptr;</span><br><span class="line">    CHECK(cudaMalloc(&amp;d_in, bytes));</span><br><span class="line">    CHECK(cudaMalloc(&amp;d_out, ((n + <span class="number">255</span>) / <span class="number">256</span>) * <span class="keyword">sizeof</span>(<span class="type">float</span>)));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 假设已准备 h_in，拷贝到设备</span></span><br><span class="line">    <span class="comment">// CHECK(cudaMemcpy(d_in, h_in, bytes, cudaMemcpyHostToDevice));</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> blockSize = <span class="number">256</span>;</span><br><span class="line">    <span class="type">int</span> gridSize = (n + blockSize - <span class="number">1</span>) / blockSize;</span><br><span class="line">    reduceShuffleKernel&lt;&lt;&lt;gridSize, blockSize&gt;&gt;&gt;(d_in, d_out, n);</span><br><span class="line">    CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 若 gridSize &gt; 1，可在主机或再启动一次核函数对 d_out 做最终归约</span></span><br><span class="line">    CHECK(cudaFree(d_in));</span><br><span class="line">    CHECK(cudaFree(d_out));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-3-shfl-xor-yu-die-xing-gui-yue" tabindex="-1" id="3-3-shfl-xor-与蝶形归约">3.3 __shfl_xor 与蝶形归约</h3><p><strong><code>__shfl_xor_sync(mask, val, lane_mask, width)</code></strong> 的语义是：当前线程从 <strong>lane 索引 = 当前 lane XOR lane_mask</strong> 的线程读取 <code>val</code>。当 <code>lane_mask</code> 依次取 16、8、4、2、1 时，恰好形成<strong>蝶形</strong>连接模式，与树形归约一样可在 (\log_2(32)) 步内完成 warp 内求和。xor 方式在某些实现上能减少依赖链，可能对指令调度更友好。示例片段（仅 warp 内，lane 0 得和）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">__device__ __forceinline__ <span class="type">float</span> <span class="title function_">warpReduceSumXor</span><span class="params">(<span class="type">float</span> val)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> lane_mask = warpSize / <span class="number">2</span>; lane_mask &gt; <span class="number">0</span>; lane_mask &gt;&gt;= <span class="number">1</span>)</span><br><span class="line">        val += __shfl_xor_sync(FULL_MASK, val, lane_mask);</span><br><span class="line">    <span class="keyword">return</span> val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="si-xian-cheng-shu-biao-jue-han-shu-shu-7-3-jie" tabindex="-1" id="四、线程束表决函数（书-7-3-节）">四、线程束表决函数（书 7.3 节）</h2><p>本节对应书中对 <strong>warp 表决</strong>（vote）原语的介绍：<code>__ballot_sync</code>、<code>__all_sync</code>、<code>__any_sync</code> 的语义、典型用法以及与洗牌配合生成「参与掩码」的正确方式。</p><h3 id="4-1-ballot-sync-tiao-jian-wei-zhen-de-xian-cheng-wei-yan-ma" tabindex="-1" id="4-1-ballot-sync：条件为真的线程位掩码">4.1 __ballot_sync：条件为真的线程位掩码</h3><p><strong><code>__ballot_sync(mask, predicate)</code></strong> 的返回值是一个 <strong>32 位无符号整数</strong>：若某 lane 在 <strong>mask</strong> 中且其 <strong>predicate</strong> 非零，则返回值中该 lane 对应的位为 1，否则为 0。这一节提到：该原语常用于 (1) <strong>统计</strong> warp 内满足某条件的线程数（对返回值做 <code>__popc</code>  population count）；(2) <strong>生成参与后续洗牌/集体操作的掩码</strong>——只有满足条件的线程才参与归约时，应先用 <code>__ballot_sync(FULL_MASK, condition)</code> 得到参与掩码，再在该掩码下调用 <code>__shfl_*_sync(participant_mask, val, ...)</code>，这样未参与线程不会贡献错误数据。</p><blockquote><p><strong>重点</strong>：<strong>调用 <code>__ballot_sync</code> 时通常使用 FULL_MASK</strong>，因为此时要统计的是「整个 warp 中哪些线程满足条件」；所有线程都应执行到该调用，得到一致的 32 位掩码。随后，只有满足条件的线程进入归约分支，并在分支内使用<strong>刚才得到的</strong> participant_mask 调用洗牌，从而保证参与洗牌的线程集合与 mask 一致，语义正确。这是「部分线程参与归约」时<strong>正确</strong>获取参与掩码的方式，与「在分支内用 __activemask()」的误用形成对比（见第五节）。</p></blockquote><p>示例：warp 内「满足 <code>tid &lt; n</code> 的线程」参与归约，并得到参与线程数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceWithBallot</span><span class="params">(<span class="type">const</span> <span class="type">float</span> *g_in, <span class="type">float</span> *g_out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> tid = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line">    <span class="type">int</span> lane = threadIdx.x % warpSize;</span><br><span class="line">    <span class="type">int</span> warpId = threadIdx.x / warpSize;</span><br><span class="line">    <span class="type">bool</span> active = (tid &lt; n);</span><br><span class="line">    <span class="type">unsigned</span> participant_mask = __ballot_sync(FULL_MASK, active);</span><br><span class="line">    <span class="type">int</span> num_active = __popc(participant_mask);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> val = active ? g_in[tid] : <span class="number">0.0f</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> offset = warpSize / <span class="number">2</span>; offset &gt; <span class="number">0</span>; offset &gt;&gt;= <span class="number">1</span>)</span><br><span class="line">        val += __shfl_down_sync(participant_mask, val, offset);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (lane == <span class="number">0</span> &amp;&amp; num_active &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 每个 warp 的 lane 0 得到该 warp 内活跃线程的和，可写入共享内存再块内归约</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-2-all-sync-yu-any-sync" tabindex="-1" id="4-2-all-sync-与-any-sync">4.2 __all_sync 与 __any_sync</h3><p><strong><code>__all_sync(mask, predicate)</code></strong>：当且仅当 <strong>mask 中所有线程</strong>的 predicate 均非零时，返回非零；否则返回 0。<strong><code>__any_sync(mask, predicate)</code></strong>：当且仅当 <strong>mask 中至少一个线程</strong>的 predicate 非零时，返回非零。这一节提到：二者用于 warp 内的<strong>集体判断</strong>，无需再根据 ballot 结果做标量判断，可简化代码并减少分支。例如：若「warp 内全部线程都满足某条件」才执行某路径，可写 <code>if (__all_sync(FULL_MASK, cond)) &#123; ... &#125;</code>；若「至少一个满足」则用 <code>__any_sync</code>。</p><p>下表归纳三种表决函数的语义（与书中及 CUDA 编程指南一致）：</p><table><thead><tr><th>原语</th><th>返回值含义</th></tr></thead><tbody><tr><td><code>__ballot_sync(mask, pred)</code></td><td>32 位掩码：pred 非零且被 mask 包含的 lane 对应位为 1</td></tr><tr><td><code>__all_sync(mask, pred)</code></td><td>非零当且仅当 mask 内所有线程 pred 非零</td></tr><tr><td><code>__any_sync(mask, pred)</code></td><td>非零当且仅当 mask 内至少一个线程 pred 非零</td></tr></tbody></table><hr><h2 id="wu-huo-yue-yan-ma-yu-warp-tong-bu-shu-7-4-jie" tabindex="-1" id="五、活跃掩码与-warp-同步（书-7-4-节）">五、活跃掩码与 warp 同步（书 7.4 节）</h2><p>本节对应书中对 <strong><code>__activemask()</code></strong> 与 <strong><code>__syncwarp()</code></strong> 的说明：二者分别用于「查询当前活跃线程」和「warp 内细粒度同步」，但使用不当会导致错误，需注意正确用法与典型误用（见原书说明）。</p><h3 id="5-1-activemask-de-yu-yi-yu-wu-yong-yi-cuo-dian" tabindex="-1" id="5-1-activemask-的语义与误用-★-易错点">5.1 __activemask 的语义与误用 ★ 易错点</h3><p><strong><code>__activemask()</code></strong> 返回一个 32 位掩码，表示<strong>当前与调用线程一起执行到该点的、同一 warp 内的线程</strong>。这一节提到：该原语<strong>不</strong>进行同步，它只反映「当前这一刻」哪些线程与己同处同一执行点。因此它适用于调试、或「机会式」的 warp 内协作（例如已知某分支内线程会一起执行且逻辑上等价于同一掩码），但不能替代由程序逻辑显式确定的参与掩码。</p><p>一种<strong>错误用法</strong>是：在<strong>已分化的分支内部</strong>用 <code>__activemask()</code> 得到掩码，再将该掩码用于 <code>__shfl_down_sync(mask, val, offset)</code> 做归约。因为 CUDA 执行模型（尤其在 Volta 及以后）<strong>不保证</strong>「走同一分支的线程会同时执行到 __activemask()」；可能只有部分线程进入该分支，此时得到的「活跃」掩码只是子集，用其做归约会得到<strong>部分和</strong>而非整个 warp 的和，结果错误。正确做法是<strong>在分支之前</strong>用 <strong><code>__ballot_sync(FULL_MASK, condition)</code></strong> 根据逻辑条件计算「应参与归约」的线程掩码，再在分支内使用该掩码调用洗牌（见 4.1 节）。<strong>小结</strong>：<code>__activemask()</code> 只查询、不同步，且不保证与「逻辑上应参与集体操作的线程」一致，因此<strong>不能</strong>在分化分支内用作洗牌/归约的参与掩码。</p><h3 id="5-2-syncwarp-warp-nei-tong-bu-yu-nei-cun-zha-lan" tabindex="-1" id="5-2-syncwarp：warp-内同步与内存栅栏">5.2 __syncwarp：warp 内同步与内存栅栏</h3><p><strong><code>__syncwarp(mask)</code></strong> 使当前线程等待，直到 <strong>mask</strong> 所指定的同一 warp 内所有线程都执行到各自的 <code>__syncwarp(mask)</code>（且使用相同 mask）为止；同时提供 <strong>warp 级内存栅栏</strong>，保证在调用前后的共享内存（及可观测的全局内存）访问顺序。这一节提到：当 warp 内线程需要先写共享内存、再被其他 lane 读取时，应在写与读之间插入 <code>__syncwarp()</code>，以避免竞态。</p><blockquote><p><strong>重难点</strong>：<strong><code>__syncwarp</code> 只同步 mask 内的线程</strong>，<strong>不能替代</strong>块级 <code>__syncthreads()</code>；跨 warp 的协作仍须使用 <code>__syncthreads()</code>。此外，若在 warp 内归约中仅使用洗牌指令，每步洗牌之间<strong>不需要</strong>再插 <code>__syncwarp()</code>，因为 <code>__shfl_*_sync</code> 本身已带有参与线程的同步语义。<strong>错误用法</strong>：在共享内存版树形归约中，若在「读-改-写」之间仅用 <code>__syncwarp()</code> 而不同步整个块，可能与其他 warp 的写入产生竞态；此时必须使用 <code>__syncthreads()</code>。</p></blockquote><p>示例（书中风格）：warp 内先将各自寄存器写入共享内存的某个布局，再经 <code>__syncwarp()</code> 后按转置布局读取，实现 warp 内数据重排：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">__shared__ <span class="type">float</span> smem[<span class="number">32</span>];</span><br><span class="line"><span class="type">float</span> val = my_value();</span><br><span class="line"><span class="type">int</span> lane = threadIdx.x % warpSize;</span><br><span class="line">smem[lane] = val;</span><br><span class="line">__syncwarp();</span><br><span class="line">val = smem[(lane + <span class="number">1</span>) % warpSize];  <span class="comment">// 例如循环移位</span></span><br></pre></td></tr></table></figure><hr><h2 id="liu-xun-huan-zhan-kai-yu-zhi-ling-ji-bing-xing-shu-7-5-jie" tabindex="-1" id="六、循环展开与指令级并行（书-7-5-节）">六、循环展开与指令级并行（书 7.5 节）</h2><p>本节对应书中对<strong>循环展开</strong>在指令级优化中作用的说明；与第3章归约中的展开相呼应，强调其对指令吞吐与延迟隐藏的贡献。</p><h3 id="6-1-wei-he-zhan-kai-neng-ti-sheng-zhi-ling-tun-tu" tabindex="-1" id="6-1-为何展开能提升指令吞吐">6.1 为何展开能提升指令吞吐</h3><p>这一节提到：<strong>循环</strong>会产生分支（循环条件判断）与连续依赖（下一轮依赖上一轮结果），不利于硬件充分流水线化和隐藏延迟。<strong>循环展开</strong>通过减少迭代次数、在单次迭代中执行更多<strong>独立</strong>操作，使调度器有更多可发射的指令，从而更好地隐藏算术或内存延迟。第3章已给出「每线程处理多个元素」和「最后阶段展开到 32 线程后去掉 __syncthreads」的归约示例；本章从「指令级」角度强调：展开后同一 warp 内可用的<strong>独立指令</strong>增多，有利于提高 <strong>ILP</strong>（指令级并行）和占用率效益。</p><p>公式上，若原循环迭代次数为 (N)，展开因子为 (U)，则有效迭代轮数约为 (\lceil N/U \rceil)，分支与循环开销成比例下降；同时每轮内 (U) 次操作的调度灵活性提高。与第3章延迟隐藏公式一致：隐藏延迟所需的「有效并行度」与每 SM 核心数和指令延迟的乘积相关；展开在相同线程数下增加了「可并行调度的指令」数量，从而更易满足该需求。书中提醒：展开会<strong>增加每线程寄存器使用</strong>，可能降低占用率，需在「更多 ILP」与「更多活跃 warp」之间做权衡。</p><blockquote><p><strong>理解与体会</strong>：第 3 章归约里已经用过「每线程多元素」和「最后阶段完全展开、去掉 __syncthreads」；本章从指令级角度统一理解——展开的本质是<strong>减少循环分支、增加同一迭代内的独立指令</strong>，让调度器有更多可发射的指令来填满流水线、隐藏延迟。使用时要在「更多 ILP」和「寄存器压力/占用率」之间做权衡，避免过度展开导致寄存器溢出或块数下降。</p></blockquote><h3 id="6-2-pragma-unroll-yu-shou-xie-zhan-kai" tabindex="-1" id="6-2-pragma-unroll-与手写展开">6.2 #pragma unroll 与手写展开</h3><p>CUDA 支持 <strong><code>#pragma unroll</code></strong> 指示编译器对紧接的 for 循环进行展开。完整形式为 <code>#pragma unroll N</code>，表示展开因子为 N；也可写 <code>#pragma unroll</code> 由编译器自动选择因子。书中示例风格如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">kernelUnroll</span><span class="params">(<span class="type">float</span> *out, <span class="type">const</span> <span class="type">float</span> *in, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">float</span> sum = <span class="number">0.0f</span>;</span><br><span class="line">    <span class="meta">#<span class="keyword">pragma</span> unroll 4</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> k = <span class="number">0</span>; k &lt; <span class="number">4</span>; k++) &#123;</span><br><span class="line">        <span class="type">int</span> idx = i + k * blockDim.x * gridDim.x;</span><br><span class="line">        <span class="keyword">if</span> (idx &lt; n)</span><br><span class="line">            sum += in[idx];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n)</span><br><span class="line">        out[i] = sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>手写展开即显式写出多份相似代码，去掉循环变量，便于对关键路径做精细控制；书中在归约等内核中会同时使用「每线程多元素」与「最后一轮完全展开」两种方式。</p><p>下表简要归纳循环展开对指令级的影响（与书中描述一致）：</p><table><thead><tr><th>方面</th><th>效果</th></tr></thead><tbody><tr><td>分支与循环开销</td><td>迭代次数减少，分支频率降低</td></tr><tr><td>独立指令数</td><td>单次迭代内多条可并行调度指令，利于隐藏延迟</td></tr><tr><td>寄存器压力</td><td>展开因子越大，每线程寄存器越多，可能降低占用率</td></tr><tr><td>适用场景</td><td>归约、点积、固定步长访问等可批量处理的循环</td></tr></tbody></table><hr><h2 id="qi-kuai-su-shu-xue-yu-jing-du-qu-she-shu-7-6-jie" tabindex="-1" id="七、快速数学与精度取舍（书-7-6-节）">七、快速数学与精度取舍（书 7.6 节）</h2><p>本节对应书中对<strong>快速数学</strong>选项与<strong>单/双精度</strong>在指令吞吐与精度之间取舍的说明。</p><h3 id="7-6-1-use-fast-math-yu-intrinsic-xing-wei" tabindex="-1" id="7-6-1-use-fast-math-与-intrinsic-行为">7.6.1 --use_fast_math 与 intrinsic 行为</h3><p>这一节提到：NVCC 的 <strong><code>--use_fast_math</code></strong> 选项会将许多标准数学函数替换为<strong>更快、精度略低</strong>的实现（如降低尾数精度、使用更快的近似），从而<strong>提高指令吞吐</strong>、减少周期数。代价是结果可能与 IEEE 严格一致的结果有偏差，在科学计算或合规场景需谨慎使用。该选项还会影响<strong>乘加融合</strong>（FMA）等优化，进一步改变性能与精度。典型编译方式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nvcc -O3 --use_fast_math -o app kernel.cu</span><br></pre></td></tr></table></figure><p>某些内建函数（如 <code>__fmaf_rn</code>）可单独控制舍入与融合行为；若原书涉及会在此说明。下表概括快速数学的典型影响（与书中及文档一致）：</p><table><thead><tr><th>方面</th><th>常规编译</th><th>–use_fast_math</th></tr></thead><tbody><tr><td>部分数学函数</td><td>高精度、较慢</td><td>快速近似、略低精度</td></tr><tr><td>乘加等</td><td>可能分离为多条指令</td><td>更易融合为 FMA，吞吐更高</td></tr><tr><td>适用场景</td><td>对精度敏感的应用</td><td>对吞吐敏感、可接受小误差的应用</td></tr></tbody></table><h3 id="7-6-2-dan-jing-du-yu-shuang-jing-du-zhi-ling-tun-tu" tabindex="-1" id="7-6-2-单精度与双精度指令吞吐">7.6.2 单精度与双精度指令吞吐</h3><p>不同架构上<strong>单精度与双精度</strong>的每 SM 每周期操作数有差异：通常<strong>双精度</strong>的峰值吞吐低于单精度（例如 1:2 或 1:4），因此在不影响算法正确性的前提下，合理使用<strong>单精度</strong>能显著提高指令吞吐。同时，单精度占用的寄存器与带宽更少，有利于提高占用率与内存带宽利用率。</p><p>公式上，若某内核为<strong>计算受限</strong>，则理论执行时间可近似为：<br>[<br>T \approx \frac{\text{总运算数}}{\text{峰值指令吞吐（ops/s）}}<br>]<br>降低精度（如改用单精度）或使用快速数学可提高<strong>有效吞吐</strong>，从而缩短 (T)。原书会列出典型架构下单/双精度峰值比（如 Fermi/Kepler 的 FP32 vs FP64 每 SM 每周期操作数），供读者在精度与性能之间权衡。</p><hr><h2 id="ba-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="八、本章小结与重难点回顾">八、本章小结与重难点回顾</h2><h3 id="8-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="8-1-知识小结（与书中对应）">8.1 知识小结（与书中对应）</h3><ul><li><strong>指令级原语</strong>：在 warp 内直接协作或交换数据的内建函数，包括洗牌、表决、warp 同步与活跃掩码；正确使用可减少内存访问与同步、提升指令吞吐。自 CUDA 9 起须使用带 <code>_sync</code> 的原语并正确设置 <strong>mask</strong>（Volta 及以后架构上线程可独立取指，mask 由程序逻辑或 <code>__ballot_sync</code> 确定）。</li><li><strong>洗牌</strong>：<code>__shfl_sync</code>、<code>__shfl_down_sync</code>、<code>__shfl_up_sync</code>、<code>__shfl_xor_sync</code> 用于 warp 内归约、扫描、广播、蝶形交换；必须使用 <code>_sync</code> 版本，<strong>mask 必须与参与线程一致</strong>（部分线程参与时用 <code>__ballot_sync(FULL_MASK, condition)</code> 得到参与掩码再用于洗牌）。</li><li><strong>表决</strong>：<code>__ballot_sync</code> 得到条件为真的线程位掩码，用于计数或生成参与掩码；<code>__all_sync</code> / <code>__any_sync</code> 用于 warp 内全/任一为真的集体判断。调用 ballot 时通常用 FULL_MASK，所有线程执行到该点得到一致掩码后再在分支内用该掩码调用洗牌。</li><li><strong>活跃掩码与 warp 同步</strong>：<code>__activemask()</code> <strong>仅查询</strong>当前一起执行到该点的线程，<strong>不</strong>进行同步，<strong>不可</strong>在分化分支内替代 <code>__ballot_sync</code> 做归约参与掩码；<code>__syncwarp(mask)</code> 用于 warp 内细粒度同步与内存栅栏，<strong>不能替代</strong>块级 <code>__syncthreads()</code>。</li><li><strong>循环展开</strong>：减少迭代与分支、增加独立指令，利于隐藏延迟与提高 ILP；注意寄存器压力与占用率权衡；可用 <code>#pragma unroll</code> 或手写展开。与第 3 章归约中的展开相呼应。</li><li><strong>快速数学与精度</strong>：<code>--use_fast_math</code> 提高吞吐、降低部分精度；单精度相对双精度通常有更高指令吞吐，需按应用需求取舍。</li></ul><h3 id="8-2-zhong-nan-dian-su-cha" tabindex="-1" id="8-2-重难点速查">8.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>参与掩码</strong></td><td>部分线程参与归约/洗牌时，必须在<strong>分支前</strong>用 <code>__ballot_sync(FULL_MASK, condition)</code> 得到参与掩码，再在分支内用该 mask 调用洗牌；<strong>不可</strong>在分化分支内用 <code>__activemask()</code> 作参与掩码，否则得到部分和或未定义行为。</td></tr><tr><td><strong>_sync 与 mask</strong></td><td>Volta 及以后必须使用带 <code>_sync</code> 的 warp 原语；mask 须与「实际参与该集体操作的线程」一致。</td></tr><tr><td><strong>__activemask 误用</strong></td><td>只查询、不同步；不保证与「逻辑上应参与的线程」一致；在分化分支内用作洗牌/归约掩码会出错。</td></tr><tr><td><strong>__syncwarp 与 __syncthreads</strong></td><td><code>__syncwarp</code> 只同步 mask 内同一 warp 的线程，不能替代块级 <code>__syncthreads()</code>；跨 warp 协作或共享内存读改写须用 <code>__syncthreads()</code>。</td></tr><tr><td><strong>洗牌四变体</strong></td><td>sync/down/up/xor 对应指定 lane、向下偏移、向上偏移、XOR lane；树形归约用 down，蝶形用 xor。</td></tr><tr><td><strong>循环展开权衡</strong></td><td>提高 ILP、减少分支；过度展开会增加寄存器、降低占用率，需权衡。</td></tr><tr><td><strong>快速数学</strong></td><td><code>--use_fast_math</code> 提高吞吐、略降精度；对精度敏感场景慎用。</td></tr></tbody></table><h3 id="8-3-xue-xi-si-kao" tabindex="-1" id="8-3-学习思考">8.3 学习思考</h3><ul><li><strong>与第 3 章的衔接</strong>：第 3 章讲延迟隐藏、归约中的循环展开与最后阶段去掉 __syncthreads；本章从指令级统一理解「展开」与「warp 内协作」——洗牌减少共享内存访问与同步、展开增加可调度指令，二者都在不增加线程数的前提下提高有效吞吐、更好隐藏延迟。</li><li><strong>与第 5 章的关系</strong>：第 5 章引入线程束洗牌入门（__shfl_down_sync、warp 内求和）；本章系统展开四种洗牌变体、参与掩码的正确获取（ballot 而非 activemask）、表决与 warp 同步，形成「warp 内协作」的完整工具箱。归约类内核可先共享内存版，再在 warp 内用洗牌+正确 mask 做一层优化。</li><li><strong>实践建议</strong>：写使用 warp 原语的内核时，(1) 凡有分支导致「部分线程参与集体操作」的，一律在分支前用 <code>__ballot_sync(FULL_MASK, condition)</code> 得到 mask，再在分支内用该 mask 调用洗牌；(2) 不要用 <code>__activemask()</code> 作为归约/洗牌的参与掩码；(3) 仅 warp 内协作用 <code>__syncwarp()</code>，跨 warp 或共享内存可见性用 <code>__syncthreads()</code>；(4) 展开与快速数学在确认正确性后再用，并注意寄存器与精度取舍。</li></ul><p>下表为本章<strong>自检要点</strong>（与书中描述一致）：</p><table><thead><tr><th>要点</th><th>说明</th></tr></thead><tbody><tr><td>洗牌四变体</td><td><code>__shfl_sync</code> / <code>_down</code> / <code>_up</code> / <code>_xor_sync</code>；mask 与参与线程一致</td></tr><tr><td>参与掩码</td><td>部分线程参与时用 <code>__ballot_sync(FULL_MASK, condition)</code> 得 mask，再用于洗牌</td></tr><tr><td>表决</td><td><code>__ballot_sync</code> 得位图；<code>__all_sync</code> / <code>__any_sync</code> 得集体布尔结果</td></tr><tr><td>__activemask</td><td>仅查询，不同步；不可在分化分支内用作归约参与掩码</td></tr><tr><td>__syncwarp</td><td>warp 内同步+内存栅栏；不能替代 __syncthreads</td></tr><tr><td>循环展开</td><td>提高 ILP、减少分支；注意寄存器与占用率</td></tr><tr><td>快速数学</td><td>–use_fast_math；单/双精度吞吐差异</td></tr></tbody></table><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 8 章：GPU 加速库和 OpenACC</strong>：</p><ul><li><strong>CUDA 库概览</strong>：cuBLAS、cuFFT、cuSPARSE、cuRAND 等域专用库的定位与选用</li><li><strong>库的典型用法</strong>：密集/稀疏线性代数、FFT、随机数生成</li><li><strong>OpenACC</strong>：指令式并行、<code>#pragma acc</code> 与数据/计算区域</li></ul><p>从「单设备上的指令级调优」到「用现成库与指令快速获得 GPU 加速」，是向工程化 CUDA 应用迈进的重要一步。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>warp 洗牌（_<em>shfl</em>*_sync）与共享内存相比，在 warp 内归约时有何优势？</strong></li><li><strong>循环展开如何帮助隐藏延迟？展开时需注意什么权衡？</strong></li><li><strong><code>__ballot_sync</code> 与 <code>__activemask</code> 在归约中用作洗牌掩码时有何区别？应优先用哪个？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>洗牌在寄存器间直接交换数据，不经过共享内存，延迟更低、无 bank 冲突；warp 内归约用洗牌可减少共享内存访问与同步次数，提高指令吞吐。</li><li>展开减少循环迭代次数与分支，同一迭代内更多独立指令便于调度器隐藏算术/内存延迟。需注意展开会增加每线程寄存器使用，可能降低占用率，需在 ILP 与占用率之间权衡。</li><li><code>__activemask()</code> 只包含当前活跃的线程，在分支或提前退出时可能不包含整个 warp；<code>__ballot_sync(mask, 1)</code> 用参与线程的 mask 更可靠。做洗牌掩码时应优先用 <code>__ballot_sync</code> 得到的 mask，避免未参与线程影响结果。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/d47d974/">第6章 流和并发</a> | 下一篇：<a href="https://smarter.xin/posts/e466b0a5/">第8章 GPU加速库和OpenACC</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 7 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-qi-zhang-diao-zheng-zhi-ling-ji-yuan-yu&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="指令级原语" scheme="https://smarter.xin/tags/instruction-primitives/"/>
    
    <category term="warp洗牌" scheme="https://smarter.xin/tags/warp-shuffle/"/>
    
    <category term="循环展开" scheme="https://smarter.xin/tags/loop-unrolling/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第六章：流和并发</title>
    <link href="https://smarter.xin/posts/d47d974/"/>
    <id>https://smarter.xin/posts/d47d974/</id>
    <published>2026-02-05T07:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-liu-zhang-liu-he-bing-fa" tabindex="-1">CUDA C编程权威指南-第六章：流和并发</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/e1d6d733/">第5章 共享内存和常量内存</a> | 下一篇：<a href="https://smarter.xin/posts/1aaa52d4/">第7章 调整指令级原语</a></p><blockquote><p>系列第 6 篇。前面几章主要围绕单次内核、单次传输；这一章讲<strong>流（Stream）与并发</strong>：用 CUDA 流把主机与设备、传输与计算、多内核并发组织起来，用事件做同步和精确计时，顺带提一下流回调、流优先级和可视化分析。</p></blockquote><hr><h2 id="qian-yan-cong-nei-he-ji-bing-fa-dao-wang-ge-ji-bing-fa" tabindex="-1" id="前言：从内核级并发到网格级并发">前言：从内核级并发到网格级并发</h2><p>到目前为止，我们关注的一直是<strong>内核级并发</strong>：一个核函数由大量线程在 GPU 上并行执行，并从编程模型、执行模型和内存模型多个角度做了优化。这一章会讲到：除了这种「单任务、多线程」的并发，CUDA 还支持<strong>网格级并发</strong>——在单个设备上<strong>同时执行多个核函数启动</strong>，从而进一步提高设备利用率。</p><p>本章将系统学习如何用 <strong>CUDA 流</strong> 表达和实现网格级并发，并解决以下问题：</p><ul><li><strong>流与事件的本质</strong>：什么是流、默认流与阻塞行为，以及事件在流中的作用。</li><li><strong>重叠执行</strong>：主机与设备并发、数据传输与核函数执行的重叠，以及多流并发。</li><li><strong>同步机制</strong>：流同步、事件同步、避免不必要的同步，以及用事件精确计时。</li><li><strong>流回调与流优先级</strong>：在流中注册主机端回调、调整流优先级（原书若涉及可查阅）。</li><li><strong>可视化与多 GPU</strong>：用 NVIDIA Visual Profiler 查看执行时间线，以及多 GPU 编程入门（原书若涉及可查阅）。</li></ul><p>第 2 章曾提到：若<strong>计算时间大于数据传输时间</strong>，可通过流来重叠计算与通信，隐藏传输延迟。本章将把这一思想具象化为可用的 API 与完整示例。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-liu-he-bing-fa" tabindex="-1" id="1-1-为什么要学「流和并发」">1.1 为什么要学「流和并发」</h3><p>前几章我们一直在做<strong>内核级并发</strong>：单次核函数内大量线程在 GPU 上并行，并从编程模型、执行模型和内存模型做了优化。这里区分指出：除了这种「单任务、多线程」的并发，CUDA 还支持<strong>网格级并发</strong>——在单个设备上<strong>同时执行多个核函数启动</strong>，从而进一步提高设备利用率。<strong>使用 CUDA 流是实现网格级并发的主要手段</strong>。此外，通过流可以<strong>重叠</strong>：(1) 主机与设备的执行；(2) 数据传输（H2D/D2H）与核函数执行。当计算时间足以掩盖部分传输时间时（第 2 章已提及），重叠能显著缩短端到端时间、提高有效带宽。因此，理解流的本质、默认流的阻塞行为、事件在同步与计时中的作用，以及如何用多流实现「传输与计算」的流水线，是写出高性能、高吞吐 CUDA 程序的关键一步；本章与第 2 章（性能模型）、第 4 章（带宽）紧密衔接。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解<strong>两种并发级别</strong>（内核级 vs 网格级）及流在网格级并发中的作用</td><td>能说明「单核多线程」与「多核/多传输同时或交错执行」的区别</td></tr><tr><td>理解<strong>流与事件的本质</strong>：流是顺序操作队列，事件是流内标记点</td><td>能解释流内顺序执行、流间可并发的含义；事件的同步与计时用途</td></tr><tr><td>掌握<strong>流的创建与销毁</strong>、<strong>默认流与阻塞流</strong>的语义</td><td>能写出 <code>cudaStreamCreate</code>/<code>Destroy</code>；说明为何混用默认流会破坏多流并发</td></tr><tr><td>掌握在流中<strong>启动核函数</strong>与 <strong>cudaMemcpyAsync</strong>，以及<strong>固定内存</strong>的必要性</td><td>能写出 <code>kernel&lt;&lt;&lt;..., stream&gt;&gt;&gt;</code> 与 <code>cudaMemcpyAsync(..., stream)</code>；说明为何 H2D/D2H 需 pinned memory</td></tr><tr><td>理解<strong>重叠执行</strong>的三种形式：主机/设备、传输/计算、多流</td><td>能描述双流「H2D→kernel→D2H」流水线如何重叠</td></tr><tr><td>掌握<strong>流同步</strong>与 <strong>CUDA 事件</strong>：Synchronize、Record、ElapsedTime、StreamWaitEvent</td><td>能正确使用事件做跨流依赖与核函数计时；避免不必要的同步</td></tr><tr><td>了解<strong>流回调</strong>与<strong>流优先级</strong>（书中对应节）</td><td>能说出回调的限制（回调内不可调用 CUDA API）；优先级为提示性</td></tr><tr><td>会用 <strong>Visual Profiler</strong> 查看执行时间线以验证重叠</td><td>能根据时间线判断是否实现预期重叠、是否存在隐式同步</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">第 6 章  流和并发</span><br><span class="line">│</span><br><span class="line">├── 二、流与事件的本质（书 6.1 节）</span><br><span class="line">│   ├── 内核级并发与网格级并发</span><br><span class="line">│   ├── 流与事件的本质、异步执行与同步</span><br><span class="line">│   └── 隐式同步与默认流的影响</span><br><span class="line">│</span><br><span class="line">├── 三、CUDA 流（书 6.2 节）</span><br><span class="line">│   ├── 流的创建与销毁</span><br><span class="line">│   ├── 默认流与阻塞行为 ★ 易错点</span><br><span class="line">│   └── 在流中启动核函数与异步传输（固定内存）</span><br><span class="line">│</span><br><span class="line">├── 四、流的并发执行（书 6.3 节）★ 重难点</span><br><span class="line">│   ├── 主机与设备并发</span><br><span class="line">│   ├── 传输与计算的重叠</span><br><span class="line">│   └── 多流并发与流水线模式</span><br><span class="line">│</span><br><span class="line">├── 五、流同步与 CUDA 事件（书 6.4～6.5 节）</span><br><span class="line">│   ├── 流同步、避免不必要的同步</span><br><span class="line">│   ├── CUDA 事件 API 与跨流依赖</span><br><span class="line">│   └── 用事件精确计时</span><br><span class="line">│</span><br><span class="line">├── 六、流回调与流优先级（书对应节）</span><br><span class="line">│   ├── 注册设备回调函数（cudaLaunchHostFunc）</span><br><span class="line">│   └── 调整流优先级</span><br><span class="line">│</span><br><span class="line">├── 七、可视化与多 GPU 入门</span><br><span class="line">│   ├── NVIDIA Visual Profiler 执行时间线</span><br><span class="line">│   └── 多 GPU 与本章的衔接（简述）</span><br><span class="line">│</span><br><span class="line">└── 八、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-liu-yu-shi-jian-de-ben-zhi-shu-6-1-jie" tabindex="-1" id="二、流与事件的本质（书-6-1-节）">二、流与事件的本质（书 6.1 节）</h2><p>本节对应书中「Understanding the nature of streams and events」，为全章奠定概念基础。</p><h3 id="2-1-nei-he-ji-bing-fa-yu-wang-ge-ji-bing-fa" tabindex="-1" id="2-1-内核级并发与网格级并发">2.1 内核级并发与网格级并发</h3><p>这里区分区分了 CUDA C 编程中的两个并发级别：</p><table><thead><tr><th>并发级别</th><th>含义</th><th>此前章节侧重</th></tr></thead><tbody><tr><td><strong>内核级并发</strong></td><td>单个核函数由大量线程在 GPU 上并行执行（网格 → 块 → 线程）</td><td>第 2～5 章</td></tr><tr><td><strong>网格级并发</strong></td><td>多个核函数启动（或多个传输任务）在同一个设备上同时或交错执行</td><td>本章</td></tr></tbody></table><p>在网格级并发中，<strong>多个内核</strong>（或内核与内存传输）可以同时占用设备资源。例如：当一个内核在部分 SM 上执行时，其他 SM 可以执行另一个内核；或者，当 DMA 引擎在执行主机到设备的数据传输时，已就绪的核函数可以在设备上执行。这一章会讲到：<strong>使用 CUDA 流是实现网格级并发的主要手段</strong>。</p><h3 id="2-2-liu-yu-shi-jian-de-ben-zhi" tabindex="-1" id="2-2-流与事件的本质">2.2 流与事件的本质</h3><p><strong>CUDA 流</strong>可以理解为一个<strong>按顺序执行的操作队列</strong>。程序把一系列操作（如内存拷贝、核函数启动）按顺序加入某个流，流中的操作将<strong>按入队顺序</strong>依次执行。不同流之间的操作则可以由运行时<strong>交错或并发</strong>执行（取决于硬件与驱动），从而实现主机与设备、传输与计算、多内核之间的重叠。</p><p><strong>CUDA 事件</strong>是插入到流中的<strong>标记点</strong>，用于标记流中某一时刻的「完成」状态。事件可用来：(1) <strong>同步</strong>：让某个流等待另一流中的事件再继续；(2) <strong>计时</strong>：记录两个事件之间的时间差，从而精确测量某段核函数或传输的耗时。需要留意的是：理解流与事件的本质，是正确使用异步 API 和避免隐蔽同步的前提。</p><p>多流重叠执行时，传输与核函数可交错进行，典型时序如下（流 A 与流 B 并发，H2D 表示主机到设备传输，Kernel 表示核函数，D2H 表示设备到主机传输）：</p><pre><code class="highlight mermaid">sequenceDiagram    participant Host    participant StreamA as 流 A    participant StreamB as 流 B    participant GPU as 设备    Note over StreamA,StreamB: 流内顺序执行，流间可重叠    StreamA-&gt;&gt;GPU: H2D A    StreamB-&gt;&gt;GPU: H2D B    StreamA-&gt;&gt;GPU: Kernel A    StreamB-&gt;&gt;GPU: Kernel B    StreamA-&gt;&gt;Host: D2H A    StreamB-&gt;&gt;Host: D2H B</code></pre><h3 id="2-3-yi-bu-zhi-xing-yu-tong-bu" tabindex="-1" id="2-3-异步执行与同步">2.3 异步执行与同步</h3><p>CUDA 的许多 API 是<strong>异步</strong>的：调用返回时，操作可能尚未开始或尚未完成。例如：</p><ul><li><strong>核函数启动</strong>：<code>kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(...)</code> 会立即返回，内核在设备上异步执行。</li><li><strong>异步内存拷贝</strong>：<code>cudaMemcpyAsync()</code> 在指定流中排队传输，调用立即返回。</li></ul><p>若要安全地使用尚未完成的操作的结果（例如读回设备数据），必须进行<strong>显式同步</strong>，例如 <code>cudaDeviceSynchronize()</code>、<code>cudaStreamSynchronize(stream)</code> 或等待某个事件。这一章会讲到：<strong>隐式同步</strong>（如调用同步版 <code>cudaMemcpy()</code>）会阻塞所有流的进展，在追求重叠执行时应尽量避免。</p><blockquote><p><strong>理解与体会</strong>：异步是「发起即返回」，同步是「等到完成再用结果」。流和事件把「谁在等谁」表达清楚：流内顺序由入队顺序保证，流间依赖可用事件（<code>cudaStreamWaitEvent</code>）表达；滥用全局同步或默认流会破坏这种表达，把本可重叠的执行串行化。<strong>隐式同步</strong>是本章最容易踩的坑——一旦在多个流之间插入了默认流上的操作或同步版 <code>cudaMemcpy</code>，所有流都会与该操作「对齐」，失去并发；后面「默认流与阻塞」「避免不必要的同步」会反复呼应这一点。</p></blockquote><hr><h2 id="san-cuda-liu-shu-6-2-jie" tabindex="-1" id="三、CUDA-流（书-6-2-节）">三、CUDA 流（书 6.2 节）</h2><p>本节对应书中对流的创建与销毁、默认流、以及在流中启动核函数与异步传输的讲解。</p><h3 id="3-1-liu-de-chuang-jian-yu-xiao-hui" tabindex="-1" id="3-1-流的创建与销毁">3.1 流的创建与销毁</h3><p>流由类型为 <strong><code>cudaStream_t</code></strong> 的句柄表示。创建与销毁流的 API 如下（与书中及 CUDA 编程指南一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建流</span></span><br><span class="line">cudaError_t <span class="title function_">cudaStreamCreate</span><span class="params">(cudaStream_t *pStream)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 销毁流（若流中仍有未完成的工作，会等待其完成后再销毁）</span></span><br><span class="line">cudaError_t <span class="title function_">cudaStreamDestroy</span><span class="params">(cudaStream_t stream)</span>;</span><br></pre></td></tr></table></figure><p>典型用法示例（书中风格）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">cudaStream_t stream;</span><br><span class="line">CHECK(cudaStreamCreate(&amp;stream));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在该流中发起核函数、cudaMemcpyAsync 等操作</span></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">CHECK(cudaStreamDestroy(stream));</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：若在调用 <code>cudaStreamDestroy(stream)</code> 时设备仍在该流中执行任务，销毁会阻塞直到该流中所有工作完成。因此，若希望尽早释放资源，应在不再向该流提交新任务后、在适当时机再调用 <code>cudaStreamDestroy</code>。</p><h3 id="3-2-mo-ren-liu-yu-zu-sai-xing-wei-yi-cuo-dian" tabindex="-1" id="3-2-默认流与阻塞行为-★-易错点">3.2 默认流与阻塞行为 ★ 易错点</h3><p>当<strong>不指定流</strong>时，核函数启动和<strong>同步版</strong>内存拷贝（如 <code>cudaMemcpy</code>）会使用 <strong>默认流</strong>（也称 NULL 流或 stream 0）。书中及 CUDA 编程指南指出：默认流具有<strong>阻塞</strong>语义——</p><ul><li>在默认流中排入一个操作时，它会<strong>等待所有其他（阻塞）流中已排入的操作完成</strong>后才开始执行。</li><li>默认流中的一个操作<strong>未完成</strong>时，其他流中的操作<strong>不能开始</strong>。</li></ul><p>因此，若混用默认流和自定义流，且自定义流是用 <code>cudaStreamCreate()</code> 创建的<strong>阻塞流</strong>，则默认流会与这些流相互「卡住」，无法实现多流并发。若要让自定义流与默认流并发，可以：(1) 使用 <strong>非阻塞流</strong>（<code>cudaStreamCreateWithFlags(&amp;stream, cudaStreamNonBlocking)</code>）；或 (2) 在 CUDA 7 及以后，使用<strong>每线程默认流</strong>（编译选项 <code>--default-stream per-thread</code> 或宏 <code>CUDA_API_PER_THREAD_DEFAULT_STREAM</code>），使每个主机线程拥有独立的默认流。</p><p>下表归纳书中及官方文档中关于流的类型与默认流行为（便于对照原书）：</p><table><thead><tr><th>流类型</th><th>创建方式</th><th>与默认流的关系</th></tr></thead><tbody><tr><td><strong>默认流 (NULL)</strong></td><td>不指定流即使用</td><td>与所有阻塞流互相同步，不利于多流并发</td></tr><tr><td><strong>阻塞流</strong></td><td><code>cudaStreamCreate(&amp;stream)</code></td><td>与默认流互相同步</td></tr><tr><td><strong>非阻塞流</strong></td><td><code>cudaStreamCreateWithFlags(&amp;s, cudaStreamNonBlocking)</code></td><td>与默认流可并发</td></tr></tbody></table><blockquote><p><strong>易错点</strong>：<strong>混用默认流与自定义流</strong>是失去多流并发的常见原因。若代码中既有 <code>kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(...)</code>（默认流）又有 <code>kernel&lt;&lt;&lt;grid, block, 0, myStream&gt;&gt;&gt;(...)</code>，默认流会与所有阻塞流互相同步，时间线上会出现「所有流等默认流、默认流再等所有流」的串行化。对策：要么全部使用非默认流（并显式指定流），要么使用每线程默认流（<code>--default-stream per-thread</code>），避免在重叠路径上使用默认流或同步拷贝。</p></blockquote><h3 id="3-3-zai-liu-zhong-qi-dong-he-han-shu-yu-yi-bu-chuan-shu" tabindex="-1" id="3-3-在流中启动核函数与异步传输">3.3 在流中启动核函数与异步传输</h3><p><strong>在指定流中启动核函数</strong>：在执行配置中增加流参数，即第四个参数（第三参数可为共享内存大小或 0）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kernel_name&lt;&lt;&lt;gridDim, blockDim, <span class="number">0</span>, stream&gt;&gt;&gt;(argument <span class="built_in">list</span>);</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">dim3 <span class="title function_">block</span><span class="params">(<span class="number">256</span>)</span>;</span><br><span class="line">dim3 <span class="title function_">grid</span><span class="params">((n + block.x - <span class="number">1</span>) / block.x)</span>;</span><br><span class="line">myKernel&lt;&lt;&lt;grid, block, <span class="number">0</span>, stream&gt;&gt;&gt;(d_A, d_B, d_C, n);</span><br></pre></td></tr></table></figure><p><strong>在流中发起异步内存传输</strong>：使用 <code>cudaMemcpyAsync</code>，并传入目标流：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMemcpyAsync</span><span class="params">(<span class="type">void</span> *dst, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">size_t</span> count,</span></span><br><span class="line"><span class="params">                            cudaMemcpyKind kind, cudaStream_t stream)</span>;</span><br></pre></td></tr></table></figure><p>需要留意的是：要使<strong>主机与设备之间的</strong> <code>cudaMemcpyAsync</code> 真正异步并与其他操作重叠，<strong>主机端缓冲区必须是固定内存（Pinned Memory）</strong>。否则，运行时可能退化为同步传输，无法重叠。固定内存通常通过 <code>cudaMallocHost()</code> 分配（或 <code>cudaHostAlloc()</code>），并在用完后用 <code>cudaFreeHost()</code> 释放。</p><p>下面给出一个<strong>完整示例</strong>：在单个流中完成「主机→设备」拷贝、核函数执行、「设备→主机」拷贝（与书中「基于流的向量加法」风格一致，便于对照书中代码清单 6-x）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                           \</span></span><br><span class="line"><span class="meta">        cudaError_t err = (call);                                                   \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                  \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, code: %d, reason: %s\n&quot;</span>, __FILE__, __LINE__, err,\</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                        \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                          \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">vecAdd</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用固定内存以便 cudaMemcpyAsync 真正异步</span></span><br><span class="line">    <span class="type">float</span> *h_A, *h_B, *h_C;</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_A, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_B, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_C, nBytes));</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        h_A[i] = <span class="number">1.0f</span>; h_B[i] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_A, *d_B, *d_C;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_A, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_B, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_C, nBytes));</span><br><span class="line"></span><br><span class="line">    cudaStream_t stream;</span><br><span class="line">    CHECK(cudaStreamCreate(&amp;stream));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 流中顺序：H2D -&gt; kernel -&gt; D2H</span></span><br><span class="line">    CHECK(cudaMemcpyAsync(d_A, h_A, nBytes, cudaMemcpyHostToDevice, stream));</span><br><span class="line">    CHECK(cudaMemcpyAsync(d_B, h_B, nBytes, cudaMemcpyHostToDevice, stream));</span><br><span class="line"></span><br><span class="line">    dim3 <span class="title function_">block</span><span class="params">(<span class="number">256</span>)</span>;</span><br><span class="line">    dim3 <span class="title function_">grid</span><span class="params">((n + block.x - <span class="number">1</span>) / block.x)</span>;</span><br><span class="line">    vecAdd&lt;&lt;&lt;grid, block, <span class="number">0</span>, stream&gt;&gt;&gt;(d_A, d_B, d_C, n);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaMemcpyAsync(h_C, d_C, nBytes, cudaMemcpyDeviceToHost, stream));</span><br><span class="line"></span><br><span class="line">    CHECK(cudaStreamSynchronize(stream));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 简单验证</span></span><br><span class="line">    <span class="type">float</span> maxError = <span class="number">0.0f</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="type">float</span> e = fabsf(h_C[i] - <span class="number">3.0f</span>);</span><br><span class="line">        <span class="keyword">if</span> (e &gt; maxError) maxError = e;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;max error = %f\n&quot;</span>, maxError);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaStreamDestroy(stream));</span><br><span class="line">    CHECK(cudaFree(d_A)); CHECK(cudaFree(d_B)); CHECK(cudaFree(d_C));</span><br><span class="line">    CHECK(cudaFreeHost(h_A)); CHECK(cudaFreeHost(h_B)); CHECK(cudaFreeHost(h_C));</span><br><span class="line">    cudaDeviceReset();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译时可使用：<code>nvcc -o stream_vecadd stream_vecadd.cu</code>。本例中虽然只使用了一个流，但展示了<strong>固定内存 + 异步传输 + 流内顺序</strong>的完整写法，为下一节多流重叠打下基础。</p><hr><h2 id="si-liu-de-bing-fa-zhi-xing-shu-6-3-jie-zhong-nan-dian" tabindex="-1" id="四、流的并发执行（书-6-3-节）★-重难点">四、流的并发执行（书 6.3 节）★ 重难点</h2><p>本节对应书中「Overlapping CPU and GPU execution」「Overlapping kernel execution and data transfer」以及「Exploiting grid-level concurrency」，是本章提升性能的核心。</p><h3 id="4-1-zhu-ji-yu-she-bei-bing-fa" tabindex="-1" id="4-1-主机与设备并发">4.1 主机与设备并发</h3><p>由于核函数启动和 <code>cudaMemcpyAsync</code> 是异步的，<strong>主机线程在发起这些操作后可以立即继续执行</strong>，不必等待设备完成。因此，可以在同一段代码中交错执行「主机上的计算」与「设备上的计算或传输」，实现<strong>主机与设备并发</strong>。这一章会讲到：当设备在运行内核或进行 DMA 传输时，CPU 可以同时处理其他任务（如准备下一批数据、做 I/O 或后处理），从而提高整体吞吐。</p><p>实现要点：(1) 使用<strong>非默认流</strong>（或每线程默认流）和 <strong><code>cudaMemcpyAsync</code></strong>，避免在关键路径上使用会阻塞的 <code>cudaMemcpy</code>；(2) 仅在需要结果时再调用 <code>cudaStreamSynchronize(stream)</code> 或 <code>cudaDeviceSynchronize()</code>。</p><h3 id="4-2-chuan-shu-yu-ji-suan-de-zhong-die" tabindex="-1" id="4-2-传输与计算的重叠">4.2 传输与计算的重叠</h3><p>在支持<strong>并发拷贝与执行</strong>的 GPU 上（Fermi 及以后通常支持），可以在一段数据从主机传到设备的同时，对<strong>已经传好的另一段数据</strong>在设备上执行核函数；同理，可以在核函数写回的结果正在 D2H 传输的同时，对下一批数据执行 H2D 或另一内核。书中用「双缓冲」或「多流流水线」描述这类模式：把数据和任务分块，流 1 执行「块 1 的 H2D → 块 1 的 kernel → 块 1 的 D2H」，流 2 执行「块 2 的 H2D → 块 2 的 kernel → 块 2 的 D2H」，两个流交错执行，从而<strong>重叠传输与计算</strong>。</p><p>重叠效果受硬件限制：例如，同一时刻通常只能有一个「主机→设备」和一个「设备→主机」的传输在进行（双向拷贝可并发）；内核与传输能否并发取决于设备与驱动。这一章会讲到：<strong>通信计算比</strong>若满足「计算时间足以掩盖部分传输时间」，重叠能显著减少端到端时间（与第 2 章「若计算时间 &gt; 数据传输时间，可通过流重叠计算与通信」一致）。有效带宽公式（书中第 2、4 章已出现）可写为：</p><p>[<br>\text{有效带宽} = \frac{(\text{读字节数} + \text{写字节数}) \times 10^{-9}}{\text{总时间（秒）}} \ \text{GB/s}<br>]</p><p>在重叠场景下，「总时间」为从第一批传输开始到最后一批结果取回的时间，通常小于「所有传输时间 + 所有计算时间」的简单相加，从而有效带宽会提高。</p><blockquote><p><strong>重难点理解</strong>：重叠能否带来收益取决于<strong>计算与通信的比值</strong>。若每块数据的计算时间远小于传输时间，则重叠后总时间仍由传输主导，收益有限；若计算时间足以「盖住」一部分传输（例如流 1 的 kernel 执行时，流 2 的 H2D 在进行），则总时间会明显缩短。书中与第 2 章一致：当计算时间大于或接近传输时间时，流重叠最有价值。实践中可通过 Visual Profiler 时间线观察是否真的重叠。</p></blockquote><h3 id="4-3-duo-liu-bing-fa" tabindex="-1" id="4-3-多流并发">4.3 多流并发</h3><p><strong>多流并发</strong>即同时使用多个流，让多个「传输 + 内核」任务在不同流中排队，由运行时在设备上交错或并发执行。需要留意的是：要实现多流之间的真正并发，必须<strong>避免在中间插入默认流上的操作</strong>（如无参的 <code>cudaMemcpy</code> 或 <code>kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;()</code> 未指定流），否则会引发隐式同步，导致所有流等待默认流、默认流再等待所有流，失去并发性。</p><p>下面给出一个<strong>双流重叠传输与计算</strong>的完整示例（对应书中多流、分块处理的典型模式）。将向量加法按块划分到两个流中，每个流执行：H2D → kernel → D2H；两流交错提交，以实现传输与计算的重叠。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                           \</span></span><br><span class="line"><span class="meta">        cudaError_t err = (call);                                                   \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                  \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, code: %d, reason: %s\n&quot;</span>, __FILE__, __LINE__, err,\</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                        \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                          \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">vecAdd</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> nStreams = <span class="number">2</span>;</span><br><span class="line">    <span class="type">int</span> segment = n / nStreams;</span><br><span class="line">    <span class="type">size_t</span> segBytes = segment * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *h_A, *h_B, *h_C;</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_A, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_B, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span> **)&amp;h_C, nBytes));</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        h_A[i] = <span class="number">1.0f</span>; h_B[i] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_A, *d_B, *d_C;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_A, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_B, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_C, nBytes));</span><br><span class="line"></span><br><span class="line">    cudaStream_t stream[nStreams];</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nStreams; i++)</span><br><span class="line">        CHECK(cudaStreamCreate(&amp;stream[i]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nStreams; i++) &#123;</span><br><span class="line">        <span class="type">int</span> offset = i * segment;</span><br><span class="line">        CHECK(cudaMemcpyAsync(d_A + offset, h_A + offset, segBytes,</span><br><span class="line">                              cudaMemcpyHostToDevice, stream[i]));</span><br><span class="line">        CHECK(cudaMemcpyAsync(d_B + offset, h_B + offset, segBytes,</span><br><span class="line">                              cudaMemcpyHostToDevice, stream[i]));</span><br><span class="line"></span><br><span class="line">        dim3 <span class="title function_">block</span><span class="params">(<span class="number">256</span>)</span>;</span><br><span class="line">        dim3 <span class="title function_">grid</span><span class="params">((segment + block.x - <span class="number">1</span>) / block.x)</span>;</span><br><span class="line">        vecAdd&lt;&lt;&lt;grid, block, <span class="number">0</span>, stream[i]&gt;&gt;&gt;(d_A + offset, d_B + offset,</span><br><span class="line">                                              d_C + offset, segment);</span><br><span class="line"></span><br><span class="line">        CHECK(cudaMemcpyAsync(h_C + offset, d_C + offset, segBytes,</span><br><span class="line">                              cudaMemcpyDeviceToHost, stream[i]));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nStreams; i++)</span><br><span class="line">        CHECK(cudaStreamSynchronize(stream[i]));</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> maxError = <span class="number">0.0f</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="type">float</span> e = fabsf(h_C[i] - <span class="number">3.0f</span>);</span><br><span class="line">        <span class="keyword">if</span> (e &gt; maxError) maxError = e;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;max error = %f\n&quot;</span>, maxError);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nStreams; i++)</span><br><span class="line">        CHECK(cudaStreamDestroy(stream[i]));</span><br><span class="line">    CHECK(cudaFree(d_A)); CHECK(cudaFree(d_B)); CHECK(cudaFree(d_C));</span><br><span class="line">    CHECK(cudaFreeHost(h_A)); CHECK(cudaFreeHost(h_B)); CHECK(cudaFreeHost(h_C));</span><br><span class="line">    cudaDeviceReset();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>书中通常会对比「单流顺序执行」与「多流重叠」的 nvprof 或 Visual Profiler 时间线：多流下 H2D、kernel、D2H 在时间轴上会出现重叠，总耗时往往明显缩短。</p><blockquote><p><strong>学习体会</strong>：多流流水线的典型模式是「按数据分块、每块绑定一个流、每流内顺序为 H2D → kernel → D2H」。流数量并非越多越好：过多流会增加调度与内存占用（每流可能需独立缓冲区或分段），建议根据设备与问题规模选择流数量（如 2～8 个流）。固定内存是前提，否则 <code>cudaMemcpyAsync</code> 可能退化为同步，重叠失效。</p></blockquote><p>下表归纳本节涉及的三种并发模式（与书中描述一致）：</p><table><thead><tr><th>并发模式</th><th>做法简述</th><th>注意点</th></tr></thead><tbody><tr><td><strong>主机与设备</strong></td><td>主机在发起 kernel/Async 拷贝后继续做 CPU 工作</td><td>避免在关键路径用同步 API</td></tr><tr><td><strong>传输与计算</strong></td><td>多流或双缓冲，使 H2D/D2H 与 kernel 在时间上重叠</td><td>主机缓冲需固定内存；设备需支持</td></tr><tr><td><strong>多流网格级</strong></td><td>多流中各提交独立的内核或「传输+内核」序列</td><td>避免默认流插入导致隐式同步</td></tr></tbody></table><hr><h2 id="wu-liu-tong-bu-yu-cuda-shi-jian-shu-6-4-6-5-jie" tabindex="-1" id="五、流同步与-CUDA-事件（书-6-4～6-5-节）">五、流同步与 CUDA 事件（书 6.4～6.5 节）</h2><p>本节对应书中「Understanding synchronization mechanisms」「Avoiding unwanted synchronization」、CUDA 事件的使用以及用事件计时的内容。</p><h3 id="5-1-liu-tong-bu-yu-bi-mian-bu-bi-yao-de-tong-bu" tabindex="-1" id="5-1-流同步与避免不必要的同步">5.1 流同步与避免不必要的同步</h3><p>与流相关的常用同步方式包括：</p><ul><li><strong><code>cudaDeviceSynchronize()</code></strong>：阻塞主机，直到<strong>所有流</strong>中此前排入的所有工作完成。这一章会讲到：这是最「重」的同步，会等待整个设备空闲，在需要最大并发时应避免在中间频繁调用。</li><li><strong><code>cudaStreamSynchronize(stream)</code></strong>：阻塞主机，直到<strong>指定流</strong>中此前排入的所有工作完成。其他流可继续执行，因此比 <code>cudaDeviceSynchronize()</code> 更细粒度。</li><li><strong><code>cudaStreamQuery(stream)</code></strong>：非阻塞地查询流是否已空（所有排入的操作已完成），返回 <code>cudaSuccess</code> 表示已空，<code>cudaErrorNotReady</code> 表示未空。可用于轮询或条件触发后续逻辑。</li></ul><p>需要留意的是：<strong>避免不必要的同步</strong>（Avoiding unwanted synchronization）。例如，若在多个流之间没有数据依赖，就不要在每启动一个流后就调用 <code>cudaStreamSynchronize</code> 或 <code>cudaDeviceSynchronize</code>，否则会人为串行化本可并发的执行。应只在必须使用某流的结果时再同步该流（或等待相应事件）。<strong>原则</strong>：同步粒度越细、同步点越少，越有利于重叠；<code>cudaStreamSynchronize(stream)</code> 只等该流，优于 <code>cudaDeviceSynchronize()</code> 等全设备。</p><h3 id="5-2-cuda-shi-jian" tabindex="-1" id="5-2-CUDA-事件">5.2 CUDA 事件</h3><p><strong>CUDA 事件</strong>是流中的逻辑标记点，类型为 <code>cudaEvent_t</code>。常用 API 如下（与书中及编程指南一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">cudaEventCreate(cudaEvent_t *event);</span><br><span class="line">cudaEventDestroy(cudaEvent_t event);</span><br><span class="line">cudaEventRecord(cudaEvent_t event, cudaStream_t stream);  <span class="comment">// 将事件插入流</span></span><br><span class="line">cudaEventSynchronize(cudaEvent_t event);                    <span class="comment">// 阻塞直到事件完成</span></span><br><span class="line">cudaEventQuery(cudaEvent_t event);                          <span class="comment">// 非阻塞查询事件是否完成</span></span><br><span class="line">cudaEventElapsedTime(<span class="type">float</span> *ms, cudaEvent_t start, cudaEvent_t stop);  <span class="comment">// 两事件间耗时(ms)</span></span><br></pre></td></tr></table></figure><p><strong><code>cudaEventRecord(event, stream)</code></strong> 把事件插入到 <code>stream</code> 的当前末尾；当流执行到该点时，事件被「完成」。<strong><code>cudaEventSynchronize(event)</code></strong> 会阻塞主机直到该事件完成，因此可用来「等到流执行到某一位置」再继续，而不必等整个流结束。<strong><code>cudaStreamWaitEvent(stream, event, 0)</code></strong> 则让另一流在该事件完成之前不执行其后排入的操作，从而建立<strong>流与流之间的依赖</strong>。</p><h3 id="5-3-yong-shi-jian-ji-shi" tabindex="-1" id="5-3-用事件计时">5.3 用事件计时</h3><p>这一章会讲到：用 <strong>CPU 计时器</strong> 给核函数计时时，必须配合 <code>cudaDeviceSynchronize()</code> 或 <code>cudaStreamSynchronize()</code>，否则测到的只是「启动开销」。<strong>CUDA 事件</strong>可以在设备上精确记录时间戳，适合对<strong>单个流内</strong>的核函数或传输做计时，且不需要阻塞所有流。</p><p>典型模式：在流中在核函数前后各插入一个事件，然后同步流（或至少同步「停止」事件），再用 <code>cudaEventElapsedTime</code> 得到两次记录之间的毫秒数。公式上，若记 ( t_{\mathrm{start}} )、( t_{\mathrm{stop}} ) 为两事件对应的 GPU 时间戳，则：</p><p>[<br>\text{耗时（ms）} = t_{\mathrm{stop}} - t_{\mathrm{start}}<br>]</p><p><code>cudaEventElapsedTime</code> 返回的即为该差值（单位：毫秒）。注意：两个事件必须在<strong>同一流</strong>中，且 <code>start</code> 必须在 <code>stop</code> 之前被记录，否则行为未定义。</p><p>下面给出<strong>用事件对指定流中的核函数计时</strong>的完整示例（与书中「用事件计时」代码风格一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                                 \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                           \</span></span><br><span class="line"><span class="meta">        cudaError_t err = (call);                                                   \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                                  \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error: %s:%d, code: %d, reason: %s\n&quot;</span>, __FILE__, __LINE__, err,\</span></span><br><span class="line"><span class="meta">                   cudaGetErrorString(err));                                        \</span></span><br><span class="line"><span class="meta">            exit(1);                                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                                          \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">vecAdd</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (i &lt; n) C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = n * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_A, *d_B, *d_C;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_A, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_B, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_C, nBytes));</span><br><span class="line"></span><br><span class="line">    cudaStream_t stream;</span><br><span class="line">    cudaEvent_t start, stop;</span><br><span class="line">    CHECK(cudaStreamCreate(&amp;stream));</span><br><span class="line">    CHECK(cudaEventCreate(&amp;start));</span><br><span class="line">    CHECK(cudaEventCreate(&amp;stop));</span><br><span class="line"></span><br><span class="line">    dim3 <span class="title function_">block</span><span class="params">(<span class="number">256</span>)</span>;</span><br><span class="line">    dim3 <span class="title function_">grid</span><span class="params">((n + block.x - <span class="number">1</span>) / block.x)</span>;</span><br><span class="line"></span><br><span class="line">    CHECK(cudaEventRecord(start, stream));</span><br><span class="line">    vecAdd&lt;&lt;&lt;grid, block, <span class="number">0</span>, stream&gt;&gt;&gt;(d_A, d_B, d_C, n);</span><br><span class="line">    CHECK(cudaEventRecord(stop, stream));</span><br><span class="line"></span><br><span class="line">    CHECK(cudaStreamSynchronize(stream));</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> elapsedMs = <span class="number">0.0f</span>;</span><br><span class="line">    CHECK(cudaEventElapsedTime(&amp;elapsedMs, start, stop));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Kernel elapsed time = %.3f ms\n&quot;</span>, elapsedMs);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaEventDestroy(start));</span><br><span class="line">    CHECK(cudaEventDestroy(stop));</span><br><span class="line">    CHECK(cudaStreamDestroy(stream));</span><br><span class="line">    CHECK(cudaFree(d_A)); CHECK(cudaFree(d_B)); CHECK(cudaFree(d_C));</span><br><span class="line">    cudaDeviceReset();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>下表归纳本节涉及的同步与事件 API（便于与书中表 6-x 对照）：</p><table><thead><tr><th>API / 行为</th><th>作用</th></tr></thead><tbody><tr><td><code>cudaDeviceSynchronize()</code></td><td>等待所有流上此前的工作完成</td></tr><tr><td><code>cudaStreamSynchronize(s)</code></td><td>等待指定流此前的工作完成</td></tr><tr><td><code>cudaStreamQuery(s)</code></td><td>非阻塞查询流是否已空</td></tr><tr><td><code>cudaEventRecord(e,s)</code></td><td>在流 s 中插入事件 e</td></tr><tr><td><code>cudaEventSynchronize(e)</code></td><td>阻塞直到事件 e 完成</td></tr><tr><td><code>cudaStreamWaitEvent(s,e,0)</code></td><td>流 s 在 e 完成前不执行其后操作</td></tr><tr><td><code>cudaEventElapsedTime(&amp;t, e0, e1)</code></td><td>得到两事件间耗时（ms）</td></tr></tbody></table><hr><h2 id="liu-liu-hui-diao-yu-liu-you-xian-ji-shu-dui-ying-jie" tabindex="-1" id="六、流回调与流优先级（书对应节）">六、流回调与流优先级（书对应节）</h2><p>本节对应书中「Registering device callback functions」与「Adjusting stream priorities」的内容。</p><h3 id="6-1-liu-hui-diao" tabindex="-1" id="6-1-流回调">6.1 流回调</h3><p>CUDA 允许在流中插入<strong>主机端回调</strong>：当流执行到该点时，在主机上执行一个指定的函数。可用于在「某段设备工作完成后」自动触发主机逻辑（如解压下一批数据、更新 UI），而无需轮询或单独开线程。书中在「注册设备回调函数」一节中，若使用旧版 API 会提到 <strong><code>cudaStreamAddCallback</code></strong>；当前 CUDA 编程指南推荐使用 <strong><code>cudaLaunchHostFunc</code></strong>（<code>cudaStreamAddCallback</code> 已标记为弃用）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaLaunchHostFunc</span><span class="params">(cudaStream_t stream, <span class="type">void</span> (*fn)(<span class="type">void</span> *), <span class="type">void</span> *userData)</span>;</span><br></pre></td></tr></table></figure><p>回调函数签名为 <code>void hostFn(void *userData)</code>。<strong>限制</strong>：回调函数内<strong>不得调用任何 CUDA API</strong>，否则可能死锁或未定义行为。需要留意的是：回调在主机线程上执行，流在该回调执行期间被视为「空闲」，可安全访问与该流关联的、已完成的设备结果。典型用法是：在某个流中排入「核函数 → 主机回调」，主机端无需轮询即可在该流中核函数完成后执行后续逻辑（如准备下一批数据或写回文件）。</p><h3 id="6-2-liu-you-xian-ji" tabindex="-1" id="6-2-流优先级">6.2 流优先级</h3><p>在支持流优先级的设备上（如 Pascal 及更新架构），可以用 <strong><code>cudaStreamCreateWithPriority</code></strong> 创建带优先级的流，运行时在调度时会优先执行高优先级流中的工作（作为提示，不保证严格顺序）。书中若涉及，会给出类似下面的用法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> leastPriority, greatestPriority;</span><br><span class="line">cudaDeviceGetStreamPriorityRange(&amp;leastPriority, &amp;greatestPriority);</span><br><span class="line"><span class="comment">// greatestPriority 为高优先级（数值上可能更小，依实现而定）</span></span><br><span class="line">cudaStream_t streamHigh;</span><br><span class="line">cudaStreamCreateWithPriority(&amp;streamHigh, cudaStreamNonBlocking, greatestPriority);</span><br></pre></td></tr></table></figure><p>流优先级适合将「延迟敏感」的内核与「吞吐型」的内核分开到不同流并赋予不同优先级，以在共享设备上获得更可预测的延迟（书中表 6-x 若有优先级范围说明，可在此引用）。书中「调整流优先级」一节会说明：并非所有设备都支持流优先级，可通过 <code>cudaDeviceGetStreamPriorityRange</code> 查询；若设备不支持，创建带优先级的流可能回退为普通非阻塞流。</p><hr><h2 id="qi-ke-shi-hua-yu-duo-gpu-ru-men-shu-dui-ying-jie" tabindex="-1" id="七、可视化与多-GPU-入门（书对应节）">七、可视化与多 GPU 入门（书对应节）</h2><p>本节对应书中「Displaying application execution timelines with the NVIDIA Visual Profiler」；多 GPU 深入内容在原书中为后续章节（如第 9 章），此处仅作与本章知识的衔接简述。</p><h3 id="7-1-shi-yong-visual-profiler-cha-kan-zhi-xing-shi-jian-xian" tabindex="-1" id="7-1-使用-Visual-Profiler-查看执行时间线">7.1 使用 Visual Profiler 查看执行时间线</h3><p>可使用 <strong>NVIDIA Visual Profiler</strong>（或 Nsight Systems）查看应用程序的<strong>执行时间线</strong>。在时间线视图中，可以直观看到不同流上的 H2D、D2H、核函数在时间轴上的排列与重叠情况，从而判断是否实现了预期的传输与计算重叠、是否存在不必要的同步或默认流阻塞。通过时间线可以快速定位「本可并发的流被默认流或同步调用串行化」的问题。建议在运行示例时使用 <code>nvprof</code> 或 <code>nsys profile</code> 采集时间线，再用 Visual Profiler 打开分析。</p><blockquote><p><strong>实践建议</strong>：优化流与并发时，先用 profiler 看时间线——若看到各流的 H2D/kernel/D2H 几乎完全错开、没有重叠，多半是默认流或某处同步导致；若重叠明显，再结合有效带宽公式评估是否达到预期。</p></blockquote><h3 id="7-2-duo-gpu-yu-ben-zhang-de-xian-jie-jian-shu" tabindex="-1" id="7-2-多-GPU-与本章的衔接（简述）">7.2 多 GPU 与本章的衔接（简述）</h3><p>原书多 GPU 编程在后续章节（如第 9 章）展开。与本章直接相关的是：<strong>每个设备有各自的默认流和显式创建的流</strong>；在多设备程序中，分配与启动前需通过 <code>cudaSetDevice(deviceId)</code> 指定当前设备，流和事件都是「每设备」的。理解「单设备上的多流」后，多 GPU 时即为「每设备一套流」，再叠加设备间数据传输与 P2P 等主题。</p><p>下表归纳本章涉及的<strong>主要 API 与概念</strong>（与书中描述一致，便于自检）：</p><table><thead><tr><th>类别</th><th>内容</th></tr></thead><tbody><tr><td><strong>流</strong></td><td><code>cudaStreamCreate</code> / <code>cudaStreamDestroy</code>；默认流、阻塞流、非阻塞流；<code>kernel&lt;&lt;&lt;..., stream&gt;&gt;&gt;</code>；<code>cudaMemcpyAsync(..., stream)</code></td></tr><tr><td><strong>同步</strong></td><td><code>cudaDeviceSynchronize</code>；<code>cudaStreamSynchronize</code>；<code>cudaStreamQuery</code>；隐式同步（慎用默认流与同步拷贝）</td></tr><tr><td><strong>事件</strong></td><td><code>cudaEventCreate</code> / <code>cudaEventDestroy</code>；<code>cudaEventRecord</code>；<code>cudaEventSynchronize</code> / <code>cudaEventQuery</code>；<code>cudaEventElapsedTime</code>；<code>cudaStreamWaitEvent</code></td></tr><tr><td><strong>主机</strong></td><td>固定内存 <code>cudaMallocHost</code> / <code>cudaFreeHost</code>；流回调 <code>cudaLaunchHostFunc</code>；流优先级 <code>cudaStreamCreateWithPriority</code></td></tr><tr><td><strong>并发</strong></td><td>主机与设备重叠；传输与计算重叠；多流网格级并发；避免不必要的同步</td></tr></tbody></table><hr><h2 id="ba-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="八、本章小结与重难点回顾">八、本章小结与重难点回顾</h2><h3 id="8-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="8-1-知识小结（与书中对应）">8.1 知识小结（与书中对应）</h3><ul><li><strong>两种并发级别</strong>：内核级并发（单核函数多线程）与网格级并发（多核函数/多传输同时或交错执行）；本章重点为网格级并发，通过 <strong>CUDA 流</strong> 实现。</li><li><strong>流的本质</strong>：流是按顺序执行的操作队列；不同流间可并发。默认流会与阻塞流互相同步，要实现多流并发需使用非阻塞流或每线程默认流，并避免在中间插入默认流操作。</li><li><strong>异步 API</strong>：核函数启动、<code>cudaMemcpyAsync</code> 为异步；主机端参与 H2D/D2H 的缓冲区需<strong>固定内存</strong>（<code>cudaMallocHost</code>），否则异步传输可能退化为同步。</li><li><strong>重叠执行</strong>：主机与设备可并发；传输与计算可在多流中重叠；多流提交「H2D → kernel → D2H」的流水线可提高有效带宽、缩短总时间。</li><li><strong>同步</strong>：<code>cudaStreamSynchronize(stream)</code> 比 <code>cudaDeviceSynchronize()</code> 粒度更细；应<strong>避免不必要的同步</strong>以保留并发性。</li><li><strong>事件</strong>：用于在流内打点、跨流依赖（<code>cudaStreamWaitEvent</code>）以及<strong>精确计时</strong>（<code>cudaEventRecord</code> + <code>cudaEventElapsedTime</code>）。</li><li><strong>流回调与流优先级</strong>：书中「Registering device callback functions」对应 <code>cudaStreamAddCallback</code> / <code>cudaLaunchHostFunc</code>（回调内不可调用 CUDA API）；「Adjusting stream priorities」对应 <code>cudaStreamCreateWithPriority</code>（设备支持时）。</li><li><strong>可视化</strong>：用 NVIDIA Visual Profiler 查看执行时间线以验证重叠、定位隐式同步。多 GPU 时每设备独立流与 <code>cudaSetDevice</code> 是基础（深入见原书后续章节）。</li></ul><h3 id="8-2-zhong-nan-dian-su-cha" tabindex="-1" id="8-2-重难点速查">8.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>默认流与阻塞</strong></td><td>默认流与所有阻塞流互相同步；混用默认流会串行化多流。对策：非默认流 + 非阻塞流，或 per-thread 默认流。</td></tr><tr><td><strong>固定内存与异步传输</strong></td><td><code>cudaMemcpyAsync</code> 要真正异步、参与重叠，主机缓冲区必须是 <strong>pinned</strong>（<code>cudaMallocHost</code>）；否则可能退化为同步拷贝。</td></tr><tr><td><strong>隐式同步</strong></td><td>同步版 <code>cudaMemcpy</code>、默认流上的操作会触发隐式同步，阻塞所有流。追求重叠时避免在关键路径使用。</td></tr><tr><td><strong>传输与计算重叠</strong></td><td>多流流水线：每流 H2D → kernel → D2H；重叠效果取决于计算/通信比与设备能力；流数量需权衡（如 2～8）。</td></tr><tr><td><strong>事件的两大用途</strong></td><td>(1) 同步：<code>cudaEventSynchronize</code>、<code>cudaStreamWaitEvent</code> 建立流间依赖；(2) 计时：同流内两事件 + <code>cudaEventElapsedTime</code> 得 GPU 侧耗时。</td></tr><tr><td><strong>避免不必要的同步</strong></td><td>只在必须使用某流结果时再同步该流；细粒度同步优于全设备同步。</td></tr></tbody></table><h3 id="8-3-xue-xi-si-kao" tabindex="-1" id="8-3-学习思考">8.3 学习思考</h3><ul><li><strong>与第 2 章的衔接</strong>：第 2 章提到「若计算时间大于数据传输时间，可通过流重叠计算与通信」；本章把这一思想落实为流、固定内存、异步拷贝与多流流水线，并用有效带宽公式衡量。理解「计算/通信比」有助于判断何时值得上多流、何时收益有限。</li><li><strong>与第 4 章的关系</strong>：第 4 章关注单次内核的全局内存带宽与合并访问；本章关注「多次传输与多次内核」在时间轴上的重叠，从而从系统层面提高有效带宽。两者结合：单内核访存优化 + 流级重叠，才能把设备吞吐榨干。</li><li><strong>实践建议</strong>：写多流代码时，(1) 先保证单流逻辑正确（固定内存 + 异步 API + 指定流）；(2) 再扩展到多流，注意不要插入默认流或同步拷贝；(3) 用 Visual Profiler 看时间线确认重叠；(4) 事件计时优先于 CPU 计时，且两事件须在同一流内。</li></ul><p>下表归纳本章涉及的<strong>主要 API 与概念</strong>（与书中描述一致，便于自检）：</p><table><thead><tr><th>类别</th><th>内容</th></tr></thead><tbody><tr><td><strong>流</strong></td><td><code>cudaStreamCreate</code> / <code>cudaStreamDestroy</code>；默认流、阻塞流、非阻塞流；<code>kernel&lt;&lt;&lt;..., stream&gt;&gt;&gt;</code>；<code>cudaMemcpyAsync(..., stream)</code></td></tr><tr><td><strong>同步</strong></td><td><code>cudaDeviceSynchronize</code>；<code>cudaStreamSynchronize</code>；<code>cudaStreamQuery</code>；隐式同步（慎用默认流与同步拷贝）</td></tr><tr><td><strong>事件</strong></td><td><code>cudaEventCreate</code> / <code>cudaEventDestroy</code>；<code>cudaEventRecord</code>；<code>cudaEventSynchronize</code> / <code>cudaEventQuery</code>；<code>cudaEventElapsedTime</code>；<code>cudaStreamWaitEvent</code></td></tr><tr><td><strong>主机</strong></td><td>固定内存 <code>cudaMallocHost</code> / <code>cudaFreeHost</code>；流回调 <code>cudaLaunchHostFunc</code>；流优先级 <code>cudaStreamCreateWithPriority</code></td></tr><tr><td><strong>并发</strong></td><td>主机与设备重叠；传输与计算重叠；多流网格级并发；避免不必要的同步</td></tr></tbody></table><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 7 章：调整指令级原语</strong>：</p><ul><li><strong>线程束洗牌再探</strong>：在第 5 章入门基础上，深入 <code>__shfl_*_sync</code> 的各类变体与掩码语义</li><li><strong>线程束表决函数</strong>：<code>__ballot_sync</code>、<code>__all_sync</code>、<code>__any_sync</code></li><li><strong>活跃掩码与 warp 同步</strong>：<code>__activemask()</code>、<code>__syncwarp()</code></li><li><strong>循环展开</strong>与<strong>快速数学</strong>：指令级优化与精度取舍</li></ul><p>从「流与并发」到「单次内核内部的指令级调优」，是进一步榨取 GPU 性能的必经之路。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>内核级并发与网格级并发的区别是什么？流主要实现哪一种？</strong></li><li><strong>为何多流重叠执行时不能使用同步版 <code>cudaMemcpy()</code>？应改用何 API？</strong></li><li><strong>用事件做核函数计时的典型步骤是什么（Record → ? → ElapsedTime）？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>内核级并发指单次核函数内大量线程并行；网格级并发指多次核函数启动或传输在设备上同时/交错执行。流主要实现网格级并发（多流内操作可重叠）。</li><li>同步版 <code>cudaMemcpy()</code> 会阻塞主机直到传输完成，且会与所有流同步，导致多流无法重叠。应使用 <code>cudaMemcpyAsync(..., stream)</code> 并在流中排队，且主机端需使用锁页内存。</li><li>在核函数前 <code>cudaEventRecord(start, stream)</code>，核函数后 <code>cudaEventRecord(stop, stream)</code>，然后 <code>cudaEventSynchronize(stop)</code>，最后 <code>cudaEventElapsedTime(&amp;ms, start, stop)</code> 得到毫秒数。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/e1d6d733/">第5章 共享内存和常量内存</a> | 下一篇：<a href="https://smarter.xin/posts/1aaa52d4/">第7章 调整指令级原语</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 6 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-liu-zhang-liu-he-bing-fa&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="CUDA流" scheme="https://smarter.xin/tags/cuda-stream/"/>
    
    <category term="事件" scheme="https://smarter.xin/tags/event/"/>
    
    <category term="并发" scheme="https://smarter.xin/tags/concurrency/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第五章：共享内存和常量内存</title>
    <link href="https://smarter.xin/posts/e1d6d733/"/>
    <id>https://smarter.xin/posts/e1d6d733/</id>
    <published>2026-02-05T06:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-wu-zhang-gong-xiang-nei-cun-he-chang-liang-nei-cun" tabindex="-1">CUDA C编程权威指南-第五章：共享内存和常量内存</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/1f074790/">第4章 全局内存</a> | 下一篇：<a href="https://smarter.xin/posts/d47d974/">第6章 流和并发</a></p><blockquote><p>系列第 5 篇。第 4 章讲了全局内存的对齐与合并、矩阵转置里的读写取舍；这一章把数据「搬到片上」：<strong>共享内存</strong>的布局与 bank 冲突、用共享内存做矩阵转置与归约、<strong>常量内存</strong>和<strong>线程束洗牌</strong>怎么用，把「怎么访问全局内存」和「怎么在片上复用、重组数据」串成一条线。</p></blockquote><hr><h2 id="qian-yan-cong-quan-ju-nei-cun-dao-pian-shang-nei-cun" tabindex="-1" id="前言：从全局内存到片上内存">前言：从全局内存到片上内存</h2><p>第4章指出：当算法不得不以非合并方式访问全局内存时（例如矩阵转置中按列读或按列写），带宽利用率会下降。需要留意的是：<strong>在许多情况下，可以通过共享内存来改善全局内存的合并访问</strong>——先把数据以合并方式从全局内存加载到共享内存，在共享内存中按需重组或复用，再以合并方式写回全局内存。这样既减少了全局内存事务数，又利用了片上存储的低延迟与高带宽。</p><p>此外，<strong>共享内存</strong>与 <strong>L1 缓存</strong>在 Fermi 等架构上共享同一块片上存储（如 64 KB），程序员可通过运行时 API 调整二者比例；<strong>常量内存</strong>则提供只读、带专用缓存的存储，当线程束内所有线程访问同一地址时能获得广播优势。本章将系统覆盖：在共享内存中数据如何排布、什么是 bank 与 bank 冲突、如何用 padding 消除冲突、二维共享内存与线性全局内存的索引转换、矩阵转置与并行归约的共享内存实现、常量内存与只读缓存的区别，以及<strong>线程束洗牌指令</strong>（warp shuffle）的入门用法。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-gong-xiang-nei-cun-he-chang-liang-nei-cun" tabindex="-1" id="1-1-为什么要学「共享内存和常量内存」">1.1 为什么要学「共享内存和常量内存」</h3><p>第 4 章从<strong>全局内存</strong>回答了「如何对齐与合并、为何矩阵转置难以双端合并」；但性能瓶颈的突破往往在于<strong>把热数据搬到片上、按需复用</strong>。这一章会说明：<strong>大多数 GPU 应用程序受内存带宽限制</strong>，而共享内存是 SM 上的<strong>片上可编程存储器</strong>，延迟远低于全局内存、带宽更高；<strong>在许多情况下，可以通过共享内存来改善全局内存的合并访问</strong>。因此，理解共享内存的布局与 bank 冲突、掌握用共享内存做 tile 与归约、区分常量内存与只读缓存的适用场景，并会用<strong>线程束洗牌</strong>在 warp 内无共享内存参与地交换数据，是写出高性能核函数的关键一步；本章与第 3 章（执行模型、归约）、第 4 章（全局内存、矩阵转置）紧密衔接。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解<strong>共享内存</strong>的作用域、生命周期及静态/动态分配方式</td><td>能写出 <code>__shared__</code> 与 <code>extern __shared__</code> 的用法，并说明动态共享内存的第三个参数含义</td></tr><tr><td>理解 <strong><code>__syncthreads()</code></strong> 的语义及竞争条件</td><td>能解释「先加载到共享内存 → __syncthreads → 再计算」的模式，以及分支内误用的危险</td></tr><tr><td>掌握** bank 与 bank 冲突**的定义及 bank 索引公式</td><td>能判断「连续 32 个 float」「同一列」「步长 2」等访问是否冲突</td></tr><tr><td>掌握用 <strong>padding</strong> 消除二维共享内存按列访问的 bank 冲突</td><td>能写出 <code>tile[32][33]</code> 的声明及线性索引 <code>row*(BLOCK_DIM+1)+col</code>，并说明为何 +1 能消除冲突</td></tr><tr><td>掌握<strong>共享内存矩阵转置</strong>的流程与索引转换</td><td>能写出读入 <code>tile[ty][tx]</code>、写出 <code>tile[tx][ty]</code> 及 idx_in、idx_out 的推导</td></tr><tr><td>掌握<strong>共享内存归约</strong>的流程（加载→块内多轮归约→写回）</td><td>能与第 3 章全局内存归约对比，说明为何共享内存版本减少全局访问次数</td></tr><tr><td>理解<strong>常量内存</strong>的适用场景及 <strong>cudaMemcpyToSymbol</strong> 用法</td><td>能说明同址广播、不同址串行化及 64KB 限制；会写主机端拷贝与核函数内只读使用</td></tr><tr><td>区分<strong>常量缓存</strong>与<strong>只读缓存</strong>（__ldg）</td><td>能说出数据来源、容量与最佳访问模式的区别</td></tr><tr><td>理解<strong>线程束洗牌</strong>的概念及 <strong>__shfl_down_sync</strong> 等用法</td><td>能写出 warp 内归约片段，并说明 mask 与「无需共享内存」的优势</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">第 5 章  共享内存和常量内存</span><br><span class="line">│</span><br><span class="line">├── 二、共享内存概述（书 5.1 节）</span><br><span class="line">│   ├── 为什么使用共享内存、声明与生命周期（静态/动态）</span><br><span class="line">│   └── 同步与竞争条件、__syncthreads 的正确使用</span><br><span class="line">│</span><br><span class="line">├── 三、共享内存的布局与 Bank 冲突（书 5.2 节）★ 重难点</span><br><span class="line">│   ├── 数据排布、二维与线性索引</span><br><span class="line">│   ├── Bank 与 bank 冲突、典型访问模式表</span><br><span class="line">│   └── 使用 padding 消除 bank 冲突</span><br><span class="line">│</span><br><span class="line">├── 四、使用共享内存的矩阵转置（书 5.3 节）</span><br><span class="line">│   ├── 思路与索引转换（读入 tile、写出转置）</span><br><span class="line">│   └── 完整核函数与有效带宽</span><br><span class="line">│</span><br><span class="line">├── 五、使用共享内存的并行归约（书 5.4 节）</span><br><span class="line">│   ├── 思路与共享内存阶段</span><br><span class="line">│   └── 完整核函数与第 3 章归约的对比</span><br><span class="line">│</span><br><span class="line">├── 六、常量内存（书 5.5 节）</span><br><span class="line">│   ├── 常量内存与常量缓存、同址广播与不同址串行化</span><br><span class="line">│   ├── cudaMemcpyToSymbol 与核函数用法</span><br><span class="line">│   └── 常量缓存与只读缓存的区别</span><br><span class="line">│</span><br><span class="line">├── 七、线程束洗牌（书 5.6 节）</span><br><span class="line">│   ├── 概念与适用场景</span><br><span class="line">│   └── warp 内求和示例与 mask</span><br><span class="line">│</span><br><span class="line">└── 八、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-gong-xiang-nei-cun-gai-shu-shu-5-1-jie" tabindex="-1" id="二、共享内存概述（书-5-1-节）">二、共享内存概述（书 5.1 节）</h2><p>本节对应书中对共享内存的总体介绍：为什么需要共享内存、它与全局内存的关系，以及基本用法与同步要求。</p><h3 id="2-1-wei-shi-yao-shi-yong-gong-xiang-nei-cun" tabindex="-1" id="2-1-为什么使用共享内存">2.1 为什么使用共享内存</h3><p>这一节会说明：<strong>大多数设备端数据访问都是从全局内存开始的</strong>，且<strong>多数 GPU 应用程序受内存带宽限制</strong>。全局内存虽然容量大，但延迟高（约 400～800 周期）、带宽有限；而<strong>共享内存</strong>是 SM 上的<strong>片上可编程存储器</strong>，仅对同一线程块内的线程可见，延迟远低于全局内存（约数十周期），带宽也远高于全局内存。因此，把<strong>会被多次使用或需要在线程间共享</strong>的数据先加载到共享内存，在块内完成计算后再写回全局内存，可以显著减少对全局内存的访问次数并改善有效带宽。</p><p>共享内存的典型用途包括（业界共识）：(1) <strong>作为可编程缓存</strong>：将全局内存中的一块数据（tile）以合并方式读入共享内存，供块内线程多次访问；(2) <strong>重组访问模式</strong>：例如矩阵转置时，先按行合并读入 tile，在共享内存中按列写出，使写回全局内存时也是合并的；(3) <strong>块内协作与归约</strong>：同一块内线程通过共享内存交换中间结果，并用 <code>__syncthreads()</code> 保证可见性。第4章已提到：矩阵转置若只靠全局内存，要么读合并写不合并，要么写合并读不合并；<strong>使用共享内存做中转</strong>即可实现读写两端都接近合并。</p><blockquote><p><strong>理解与体会</strong>：共享内存的本质是「程序员可控的片上缓存」——CPU 的 L1/L2 对程序员透明，而 GPU 把这块存储暴露出来，让我们显式决定「先把哪块数据搬进来、以什么顺序读写」。用好共享内存，就能在「合并访问」与「复杂访问模式」之间架起桥梁：全局内存只负责合并地进、合并地出；中间的重组与复用全在片上完成。</p></blockquote><h3 id="2-2-gong-xiang-nei-cun-de-sheng-ming-yu-sheng-ming-zhou-qi" tabindex="-1" id="2-2-共享内存的声明与生命周期">2.2 共享内存的声明与生命周期</h3><p>共享内存在核函数内通过 <strong><code>__shared__</code></strong> 修饰符声明，其<strong>作用域为线程块</strong>，<strong>生命周期与线程块一致</strong>——块内所有线程共享同一块物理存储，块执行结束后该存储被释放。常见有两种分配方式：</p><table><thead><tr><th>分配方式</th><th>语法</th><th>特点</th></tr></thead><tbody><tr><td><strong>静态共享内存</strong></td><td><code>__shared__ float tile[N];</code></td><td>编译时确定大小，<code>N</code> 为常量</td></tr><tr><td><strong>动态共享内存</strong></td><td><code>extern __shared__ float s[];</code></td><td>大小在核函数启动时由第三参数指定</td></tr></tbody></table><p>动态共享内存的典型用法：在主机端启动核函数时，通过执行配置的<strong>第三个参数</strong>传入每个块所需的共享内存字节数，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 主机端：每个块需要 blockDim.x * blockDim.y * sizeof(float) 字节共享内存</span></span><br><span class="line"><span class="type">size_t</span> smemSize = block.x * block.y * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">kernel&lt;&lt;&lt;grid, block, smemSize&gt;&gt;&gt;(d_in, d_out, nx, ny);</span><br></pre></td></tr></table></figure><p>核函数内则声明为 <code>extern __shared__ float s[];</code>，由运行时根据 <code>smemSize</code> 分配。若一个核函数中同时需要多种用途的共享内存（例如一块做 tile、一块做归约缓冲），可在同一块 <code>extern __shared__</code> 中通过<strong>偏移</strong>手动划分区域。需要留意的是：<strong>共享内存是有限的资源</strong>；第3章已给出不同计算能力下「每块最大共享内存」等限制，分配过多会导致每 SM 可驻留的块数减少，影响占用率。</p><blockquote><p><strong>易错点</strong>：动态共享内存的<strong>第三个参数是字节数</strong>，不是元素个数；若传 <code>block.x * block.y</code> 而忘记乘 <code>sizeof(float)</code>，会导致越界或未定义行为。另外，<code>extern __shared__</code> 只能声明一次，多块共享内存需在同一块缓冲区里用偏移划分。</p></blockquote><h3 id="2-3-tong-bu-yu-jing-zheng-tiao-jian" tabindex="-1" id="2-3-同步与竞争条件">2.3 同步与竞争条件</h3><p>块内线程通过共享内存共享数据时，必须注意<strong>写后读、读后写</strong>等竞争条件。这一节会说明：在逻辑上并行执行的线程，在物理上并非同时执行；若线程 A 写入共享内存某位置，线程 B（可能在不同线程束中）读取该位置，<strong>必须</strong>在 A 写完之后、B 再读，否则结果未定义。<strong><code>__syncthreads()</code></strong> 提供了块内栅栏：当被调用时，块内所有线程必须都到达该点，并且在该点之前的所有全局内存和共享内存访问对该块内所有线程可见之后，才能继续执行。因此，典型模式是：先由所有线程合作把全局数据加载到共享内存 → <strong><code>__syncthreads()</code></strong> → 再在共享内存上做计算或重组 → 必要时再次 <strong><code>__syncthreads()</code></strong> → 最后写回全局内存。第3章已强调：<strong>不同块之间没有同步</strong>；块间协作只能通过结束内核、再启动新内核实现。</p><blockquote><p><strong>易错点</strong>：<strong><code>__syncthreads()</code> 必须被块内所有线程无差别地执行</strong>。若放在 <code>if (tid &lt; 32)</code> 等分支内，则只有部分线程到达栅栏，其余线程不执行该调用，会导致死锁或未定义行为。因此，要么所有线程都进入同一分支并调用 <code>__syncthreads()</code>，要么在分支外、所有线程必经的路径上调用。</p></blockquote><hr><h2 id="san-gong-xiang-nei-cun-de-bu-ju-yu-bank-chong-tu-shu-5-2-jie" tabindex="-1" id="三、共享内存的布局与-Bank-冲突（书-5-2-节）">三、共享内存的布局与 Bank 冲突（书 5.2 节）</h2><p>本节对应书中「数据在共享内存中如何排布」以及「bank 与 bank 冲突」的讲解，是使用共享内存时避免性能陷阱的核心。</p><h3 id="3-1-gong-xiang-nei-cun-de-shu-ju-pai-bu" tabindex="-1" id="3-1-共享内存的数据排布">3.1 共享内存的数据排布</h3><p>共享内存在物理上被组织为<strong>线性地址空间</strong>，按<strong>字节地址</strong>连续排列。这一节会说明：从编程视角看，声明为 <code>__shared__ float tile[32][32]</code> 的二维数组，在内存中仍按<strong>行优先</strong>线性存储——<code>tile[row][col]</code> 的线性字节地址可表示为：</p><p>[<br>\text{字节地址} = (\text{row} \times \text{列数} + \text{col}) \times \text{每元素字节数}<br>]</p><p>例如 <code>tile[row][col]</code>（float 为 4 字节），线性索引为 <code>row * 32 + col</code>，对应字节地址为 <code>(row*32+col)*4</code>。<strong>从二维共享内存到线性全局内存的索引转换</strong>是矩阵转置等核函数中的常见操作：全局内存中矩阵按行优先存储，块内线程通常负责一块子矩阵（tile）；线程 <code>(tx, ty)</code> 在块内对应 tile 的 <code>(tx, ty)</code>，在全局矩阵中的行、列则需要加上块的起始偏移（<code>blockIdx.y * blockDim.y</code>、<code>blockIdx.x * blockDim.x</code>），从而得到全局线性索引 <code>row * nx + col</code>。掌握这一转换是编写正确且高效的 tile 型核函数的基础。</p><h3 id="3-2-bank-yu-bank-chong-tu" tabindex="-1" id="3-2-Bank-与-Bank-冲突">3.2 Bank 与 Bank 冲突</h3><p>为了提供高带宽，共享内存被划分为多个 <strong>bank</strong>。这一节会说明：在 Fermi 及之后的架构中，共享内存通常被组织为 <strong>32 个 bank</strong>，<strong>每个 bank 的宽度为 4 字节</strong>；连续 4 字节字（word）依次映射到 bank 0、1、2、…、31，再循环。即：</p><p>[<br>\text{bank 索引} = (\text{字节地址} \div 4) \bmod 32<br>]</p><p>因此，若 32 个线程（一个 warp）各访问一个 float，且这些 float 的线性索引连续（如 <code>tid</code> 对应 <code>s[tid]</code>），则 32 个地址分别落在 32 个不同 bank，<strong>无冲突</strong>，可在一个周期内完成。但若 32 个线程访问<strong>同一 bank 的不同地址</strong>（例如同一列的不同行），则这些访问会<strong>串行化</strong>，产生 <strong>bank 冲突</strong>，导致多次事务、延迟增加。需要留意的是：<strong>若线程束内多个线程访问同一 bank 的不同 4 字节字，就会发生 bank 冲突</strong>；若访问的是<strong>同一 bank 内的同一地址</strong>，硬件会<strong>广播</strong>该字给所有请求线程，计为一次访问，不产生冲突。</p><p>下表归纳几种典型访问模式与是否冲突（以 32 bank、4 字节宽为例；原书配有共享内存 bank 布局示意图）：</p><table><thead><tr><th>访问模式</th><th>示例（32 线程）</th><th>Bank 情况</th><th>是否冲突</th></tr></thead><tbody><tr><td>连续 32 个 float</td><td><code>s[threadIdx.x]</code></td><td>各线程不同 bank</td><td>无冲突</td></tr><tr><td>同一地址</td><td><code>s[0]</code> 全 warp 读</td><td>同一 bank 同一地址，广播</td><td>无冲突</td></tr><tr><td>同一列（行优先二维）</td><td><code>s[row][0]</code>，row 不同</td><td>同一 bank 不同地址</td><td>有冲突</td></tr><tr><td>步长为 2 的间隔访问</td><td><code>s[threadIdx.x * 2]</code></td><td>偶数 bank 重复</td><td>2 路冲突</td></tr></tbody></table><p>因此，在编写使用二维共享内存的核函数时（如矩阵转置），<strong>按列访问</strong>共享内存往往会导致 bank 冲突，需要结合下文 padding 或索引重排来消除。</p><blockquote><p><strong>理解与体会</strong>：Bank 冲突的本质是「多个线程同时向同一个 bank 要不同地址」——硬件上每个 bank 一次只能服务一个地址，所以这些请求会被串行化。反之，若 32 个线程访问 32 个不同 bank，或访问同一 bank 的<strong>同一地址</strong>（硬件会广播），则无冲突。记忆窍门：<strong>连续、错开、同址</strong>往往安全；**同一逻辑列（行优先下的列）**在未 padding 时易冲突。</p></blockquote><h3 id="3-3-shi-yong-padding-xiao-chu-bank-chong-tu" tabindex="-1" id="3-3-使用-Padding-消除-Bank-冲突">3.3 使用 Padding 消除 Bank 冲突</h3><p>对于行优先的二维共享内存，有一种常见情况：对于行优先的二维共享内存 <code>tile[BLOCK_DIM][BLOCK_DIM]</code>，若线程按<strong>列</strong>读取（即 <code>tile[row][col]</code> 中 col 固定、row 由线程变化），则同一 warp 内不同 row 对应的线性索引相差「列数」的整数倍；当列数为 32 的约数时，不同行会落在同一 bank，产生冲突。<strong>Padding</strong> 的做法是：在每行末尾<strong>多分配一列</strong>（或若干列），使「逻辑列数」变为 33（或 32+1），这样按列访问时，相邻行对应的线性索引相差 33 个 4 字节字，其 bank 索引相差 (33 \bmod 32 = 1)，从而 32 行会分散到 32 个不同 bank，<strong>消除冲突</strong>。</p><p>声明示例（典型写法）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> BLOCK_DIM 32</span></span><br><span class="line"><span class="comment">// 无 padding：tile[32][32]，按列访问可能 32 路冲突</span></span><br><span class="line"><span class="comment">// 有 padding：tile[32][33]，按列访问时 bank 分散</span></span><br><span class="line">__shared__ <span class="type">float</span> tile[BLOCK_DIM][BLOCK_DIM + <span class="number">1</span>];  <span class="comment">// +1 即 padding 一列</span></span><br></pre></td></tr></table></figure><p>线性索引公式变为：<code>tile[row][col]</code> 对应线性索引 <code>row * (BLOCK_DIM + 1) + col</code>。这样，从全局内存「按行」合并读入时，写入共享内存的列下标仍为 <code>col</code>，行下标为 <code>row</code>，对应共享内存中的位置 <code>tile[row][col]</code>；当从共享内存「按列」读出并写回全局内存时，读的是 <code>tile[col][row]</code>（转置），由于 padding，同一 warp 内不同 <code>col</code> 对应的 <code>tile[col][row]</code> 会落在不同 bank。矩阵转置核函数中会使用带 padding 的共享内存，下面第四节给出完整实现。</p><blockquote><p><strong>易错点</strong>：使用 padding 后，若你在代码里<strong>手动计算线性索引</strong>（例如做一维化处理），必须用 <strong><code>row * (BLOCK_DIM + 1) + col</code></strong>，不能再用 <code>row * BLOCK_DIM + col</code>，否则会访问错误位置或越界。</p></blockquote><hr><h2 id="si-shi-yong-gong-xiang-nei-cun-de-ju-zhen-zhuan-zhi-shu-5-3-jie" tabindex="-1" id="四、使用共享内存的矩阵转置（书-5-3-节）">四、使用共享内存的矩阵转置（书 5.3 节）</h2><p>本节对应书中「用共享内存避免非合并的全局内存访问」的矩阵转置案例：先合并读入 tile，在共享内存中转置，再合并写出。</p><h3 id="4-1-si-lu-yu-suo-yin-zhuan-huan" tabindex="-1" id="4-1-思路与索引转换">4.1 思路与索引转换</h3><p>第4章已说明：若直接对全局内存做转置，读按行则写按列（非合并），写按行则读按列（非合并）。<strong>共享内存方案</strong>：每个线程块负责矩阵中的一个 <strong>tile</strong>（例如 32×32）；(1) 块内线程<strong>按行</strong>从全局内存把该 tile 读入共享内存（合并读）；(2) <strong><code>__syncthreads()</code></strong> 确保 tile 写满；(3) 块内线程<strong>按列</strong>从共享内存读出（对应转置后的行），写回全局内存的转置位置（合并写）。这样，全局内存的读和写都是合并的。</p><p><strong>二维共享内存到线性全局内存的索引</strong>（要点；原书通常配有 tile 与全局矩阵的索引对应图）：设块维度为 <code>(BLOCK_DIM, BLOCK_DIM)</code>，线程 <code>(tx, ty)</code> 在块内对应 tile 的 <code>(tx, ty)</code>。读入时：全局输入矩阵 <code>in</code> 的行、列为 <code>row = blockIdx.y * BLOCK_DIM + ty</code>，<code>col = blockIdx.x * BLOCK_DIM + tx</code>，线性索引 <code>idx_in = row * nx + col</code>。写入时：转置后输出矩阵 <code>out</code> 中，该元素应位于「原列作行、原行作列」，即 <code>out_col = row</code>，<code>out_row = col</code>，线性索引 <code>idx_out = out_row * ny + out_col = col * ny + row</code>。共享内存中，读入阶段 <code>tile[ty][tx] = in[idx_in]</code>；写出阶段从共享内存读 <code>tile[tx][ty]</code>（行列互换即转置），写 <code>out[idx_out] = tile[tx][ty]</code>。若使用 padding，共享内存声明为 <code>tile[BLOCK_DIM][BLOCK_DIM+1]</code>，则读入时仍为 <code>tile[ty][tx]</code>，写出时读 <code>tile[tx][ty]</code>，此时按「列」读共享内存（第一维为 tx，第二维为 ty），因 padding 而避免 bank 冲突。</p><blockquote><p><strong>理解与体会</strong>：矩阵转置是「用共享内存换双端合并」的典范：第 4 章在纯全局内存下只能二选一（读合并或写合并），这里用一块 32×33 的共享内存做中转，全局内存只做「按行整块读入」和「按行整块写出」，中间在片上完成行列互换。代价是多一次 <code>__syncthreads()</code> 和有限的共享内存占用；收益是有效带宽的显著提升，用 nvprof 对比即可验证。</p></blockquote><h3 id="4-2-wan-zheng-he-han-shu-yu-zhu-han-shu-kuang-jia" tabindex="-1" id="4-2-完整核函数与主函数框架">4.2 完整核函数与主函数框架</h3><p>下面给出<strong>带 padding 的共享内存矩阵转置</strong>核函数及简要主函数框架（含 CHECK 与执行配置）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                            \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                       \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                 \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                               \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error %s:%d, code %d, reason: %s\n&quot;</span>, __FILE__, __LINE__,   \</span></span><br><span class="line"><span class="meta">                   err, cudaGetErrorString(err));                               \</span></span><br><span class="line"><span class="meta">            exit(1);                                                            \</span></span><br><span class="line"><span class="meta">        &#125;                                                                      \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BLOCK_DIM 32</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用共享内存 + padding 的矩阵转置（读写均合并）</span></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">transposeShared</span><span class="params">(<span class="type">float</span> *in, <span class="type">float</span> *out, <span class="type">int</span> nx, <span class="type">int</span> ny)</span> &#123;</span><br><span class="line">    __shared__ <span class="type">float</span> tile[BLOCK_DIM][BLOCK_DIM + <span class="number">1</span>];  <span class="comment">// padding 消除 bank 冲突</span></span><br><span class="line"></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tx = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> ty = threadIdx.y;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> row = blockIdx.y * blockDim.y + ty;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> col = blockIdx.x * blockDim.x + tx;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (row &lt; ny &amp;&amp; col &lt; nx) &#123;</span><br><span class="line">        tile[ty][tx] = in[row * nx + col];  <span class="comment">// 按行读全局内存 → 合并</span></span><br><span class="line">    &#125;</span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转置：输出 (col, row) 对应输入 (row, col)</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> out_row = col;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> out_col = row;</span><br><span class="line">    <span class="keyword">if</span> (out_row &lt; nx &amp;&amp; out_col &lt; ny) &#123;</span><br><span class="line">        out[out_row * ny + out_col] = tile[tx][ty];  <span class="comment">// 按列读共享内存（padding 后无冲突），按行写全局 → 合并</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> nx = <span class="number">1024</span>, ny = <span class="number">1024</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = nx * ny * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *h_in  = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="type">float</span> *h_out = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="comment">// 初始化 h_in ...</span></span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_in, *d_out;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_in, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_out, nBytes));</span><br><span class="line">    CHECK(cudaMemcpy(d_in, h_in, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    dim3 <span class="title function_">block</span><span class="params">(BLOCK_DIM, BLOCK_DIM)</span>;</span><br><span class="line">    dim3 <span class="title function_">grid</span><span class="params">((nx + block.x - <span class="number">1</span>) / block.x, (ny + block.y - <span class="number">1</span>) / block.y)</span>;</span><br><span class="line">    transposeShared&lt;&lt;&lt;grid, block&gt;&gt;&gt;(d_in, d_out, nx, ny);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaMemcpy(h_out, d_out, nBytes, cudaMemcpyDeviceToHost));</span><br><span class="line">    <span class="comment">// 验证转置正确性 ...</span></span><br><span class="line">    cudaFree(d_in);</span><br><span class="line">    cudaFree(d_out);</span><br><span class="line">    <span class="built_in">free</span>(h_in);</span><br><span class="line">    <span class="built_in">free</span>(h_out);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>有效带宽</strong>（与第4章一致）可按下式估算：</p><p>[<br>\text{有效带宽（GB/s）} = \frac{(\text{读字节数} + \text{写字节数}) \times 10^{-9}}{\text{核函数执行时间（秒）}}<br>]</p><p>对于 (n \times n) 的 float 矩阵转置，读字节数 = 写字节数 = (n^2 \times 4)。可对比「仅全局内存的转置」与「共享内存 + padding 的转置」的 nvprof 带宽或耗时；通常共享内存版本能显著提高有效带宽，因为读写两端都实现了合并。</p><hr><h2 id="wu-shi-yong-gong-xiang-nei-cun-de-bing-xing-gui-yue-shu-5-4-jie" tabindex="-1" id="五、使用共享内存的并行归约（书-5-4-节）">五、使用共享内存的并行归约（书 5.4 节）</h2><p>本节对应书中「用共享内存缓存数据以减少全局内存访问」的并行归约：第3章已在全局内存上做了相邻/交错配对归约，本节把每块的输入先加载到共享内存，在共享内存中做多轮归约，最后只把块内结果写回全局内存，从而大幅减少全局内存访问次数。</p><h3 id="5-1-si-lu-yu-gong-xiang-nei-cun-jie-duan" tabindex="-1" id="5-1-思路与共享内存阶段">5.1 思路与共享内存阶段</h3><p>典型思路：每个线程块负责一段连续元素；块内每个线程先从全局内存读取一个（或若干个）元素到寄存器或共享内存，然后在<strong>共享内存</strong>中进行多轮成对归约（与第3章的交错配对类似），每轮后**<code>__syncthreads()</code><strong>，最后将块内部分和由线程 0 写回全局内存的 <code>g_odata[blockIdx.x]</code>。这样，每块只对全局内存做</strong>一次块大小的加载<strong>和</strong>一次标量写回**，而不是在全局内存上就地多轮读写，显著减少全局内存事务。</p><p>共享内存中的归约与第3章 reduceInterleaved 逻辑一致：stride 从 blockDim.x/2 开始减半，每次 <code>s[tid] += s[tid + stride]</code>，<code>__syncthreads()</code>。注意：共享内存数组 <code>s[]</code> 需在归约前由各线程用全局数据初始化，例如 <code>s[tid] = g_idata[blockIdx.x * blockDim.x + tid]</code>（需处理边界，如 <code>idx &gt;= n</code> 时赋 0 或单位元）。</p><blockquote><p><strong>理解与体会</strong>：第 3 章的归约是在全局内存上「就地」多轮读写，每轮都有大量全局内存访问；本章把「一块」的数据一次性搬进共享内存，在片上完成多轮归约，每块只写回一个标量。这样全局内存的读从「多轮、可能非合并」变成「一次块大小的合并加载 + 一次标量写回」，是典型的「用共享内存减少全局访问次数」的模式。</p></blockquote><h3 id="5-2-wan-zheng-gong-xiang-nei-cun-gui-yue-he-han-shu" tabindex="-1" id="5-2-完整共享内存归约核函数">5.2 完整共享内存归约核函数</h3><p>下面给出基于共享内存的整数求和归约核函数（与 reduce 系列对应），含块内加载、共享内存归约、写回：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceShared</span><span class="params">(<span class="type">int</span> *g_idata, <span class="type">int</span> *g_odata, <span class="type">unsigned</span> <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    __shared__ <span class="type">int</span> s[<span class="number">256</span>];  <span class="comment">// 假设 blockDim.x &lt;= 256</span></span><br><span class="line"></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从全局内存加载到共享内存，越界则填 0</span></span><br><span class="line">    s[tid] = (idx &lt; n) ? g_idata[idx] : <span class="number">0</span>;</span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> stride = blockDim.x / <span class="number">2</span>; stride &gt; <span class="number">0</span>; stride &gt;&gt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (tid &lt; stride) &#123;</span><br><span class="line">            s[tid] += s[tid + stride];</span><br><span class="line">        &#125;</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tid == <span class="number">0</span>) &#123;</span><br><span class="line">        g_odata[blockIdx.x] = s[<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>还可结合<strong>循环展开</strong>与 <strong>warp 内最后阶段不调用 __syncthreads()</strong> 等优化（第3章已提及）；进一步地，<strong>线程束洗牌</strong>（第七节）可在 warp 内无共享内存参与的情况下做归约，减少共享内存带宽压力。有效带宽公式仍为：(\text{有效带宽} = (\text{读字节数} + \text{写字节数}) / \text{时间})；共享内存版本因全局内存读写的总字节数不变但访问次数与模式更优，通常能获得更高的有效带宽与更短的执行时间。</p><hr><h2 id="liu-chang-liang-nei-cun-shu-5-5-jie" tabindex="-1" id="六、常量内存（书-5-5-节）">六、常量内存（书 5.5 节）</h2><p>本节对应书中对常量内存与常量缓存的介绍，以及常量内存与只读缓存的区别。</p><h3 id="6-1-chang-liang-nei-cun-yu-chang-liang-huan-cun" tabindex="-1" id="6-1-常量内存与常量缓存">6.1 常量内存与常量缓存</h3><p><strong>常量内存</strong>是设备上的一种只读内存，在源码中通过 <strong><code>__constant__</code></strong> 在全局/文件作用域声明，由<strong>主机</strong>在运行时通过 <strong><code>cudaMemcpyToSymbol</code></strong> 写入，设备端核函数只能读取。这一节会说明：常量内存在硬件上配有专用的<strong>常量缓存</strong>；当线程束内<strong>所有线程访问同一地址</strong>时，该地址的一个 32 位字会从常量缓存<strong>广播</strong>给整个 warp，因此只需一次读取即可满足 32 个线程，非常适合「所有线程用同一参数」的场景（如半径、系数表、查找表首地址等）。</p><p>常量内存有<strong>容量限制</strong>（如 64 KB），且当线程束内线程访问<strong>不同地址</strong>时，不同地址的请求会<strong>串行化</strong>，性能可能不如合并的全局内存访问。因此需要留意的是：<strong>常量内存最适合所有线程读取相同常量的情况</strong>；若各线程访问不同地址，应优先考虑合并的全局内存或只读缓存（<code>__ldg</code> / 只读数据缓存）。典型适用场景包括：小型的查找表或系数表、所有线程共用的配置参数（如滤波半径、块大小）、以及 kernel 启动前由主机一次性写入、设备端只读的常量数据。</p><blockquote><p><strong>理解与体会</strong>：常量内存与共享内存的「同址广播」类似，但作用域是全局、且为只读。其优势仅在 <strong>warp 内所有线程访问同一地址</strong> 时发挥；一旦 32 个线程访问 32 个不同地址，会退化成串行读取，反而不如合并的全局内存。所以使用前要问：这段数据是不是「全 warp 同参数」？若是小表但各线程查不同下标，更宜用全局内存 + <code>__ldg</code>。</p></blockquote><h3 id="6-2-chang-liang-nei-cun-de-yong-fa-cuda-memcpy-to-symbol-yu-he-han-shu" tabindex="-1" id="6-2-常量内存的用法：cudaMemcpyToSymbol-与核函数">6.2 常量内存的用法：cudaMemcpyToSymbol 与核函数</h3><p>主机端使用 <strong>cudaMemcpyToSymbol</strong> 将数据拷贝到设备上的 <code>__constant__</code> 变量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">__constant__ <span class="type">float</span> constData[<span class="number">256</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 主机端：拷贝到常量内存</span></span><br><span class="line">CHECK(cudaMemcpyToSymbol(constData, h_data, <span class="keyword">sizeof</span>(<span class="type">float</span>) * <span class="number">256</span>));</span><br></pre></td></tr></table></figure><p>核函数中直接按只读使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">useConstant</span><span class="params">(<span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; n) &#123;</span><br><span class="line">        out[idx] = constData[idx % <span class="number">256</span>];  <span class="comment">// 若 idx%256 在 warp 内相同则更优</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当 <code>idx % 256</code> 在 warp 内一致时（例如同一 warp 内 idx 连续且块大小 256 的整数倍），可发挥广播优势；否则会串行化。可对比「常量内存 + 同址访问」与「全局内存合并访问」的性能差异。</p><h3 id="6-3-chang-liang-huan-cun-yu-zhi-du-huan-cun-de-qu-bie" tabindex="-1" id="6-3-常量缓存与只读缓存的区别">6.3 常量缓存与只读缓存的区别</h3><p>（对应第 4 章只读缓存）：<strong>常量缓存</strong>专用于 <code>__constant__</code> 变量，粒度与广播行为如上所述。<strong>只读缓存</strong>（计算能力 3.5+）可用于<strong>全局内存</strong>的只读加载（通过 <code>__ldg</code> 或 <code>const __restrict__</code> 指针），加载粒度为 32 字节，对分散读取常优于 L1。二者区别可归纳为：</p><table><thead><tr><th>特性</th><th>常量缓存</th><th>只读缓存（全局内存只读）</th></tr></thead><tbody><tr><td>数据来源</td><td><code>__constant__</code>，cudaMemcpyToSymbol</td><td>全局内存（__ldg / const <strong>restrict</strong>）</td></tr><tr><td>容量</td><td>有限（如 64 KB）</td><td>独立于常量内存</td></tr><tr><td>最佳访问</td><td>同址（warp 内同一地址）广播</td><td>可分散，32 字节粒度</td></tr><tr><td>适用场景</td><td>小表、全 warp 同参数</td><td>大只读数组、分散读</td></tr></tbody></table><hr><h2 id="qi-xian-cheng-shu-xi-pai-warp-shuffle-shu-5-6-jie" tabindex="-1" id="七、线程束洗牌（Warp-Shuffle）（书-5-6-节）">七、线程束洗牌（Warp Shuffle）（书 5.6 节）</h2><p>本节对应书中「Programming with the warp shuffle instruction」：在同一 warp 内通过寄存器直接交换数据，无需共享内存，延迟更低。</p><h3 id="7-1-gai-nian-yu-gua-yong-chang-jing" tabindex="-1" id="7-1-概念与适用场景">7.1 概念与适用场景</h3><p><strong>线程束洗牌</strong>是计算能力 3.0 及以上支持的** warp 内<strong>寄存器级数据交换指令。这一节会说明：同一 warp 内的线程可以通过 <strong><code>__shfl</code></strong> 系列内建函数，根据</strong>源 lane 索引<strong>或</strong>偏移<strong>直接读取另一线程的寄存器值，而</strong>不经过共享内存**。这样可以在 warp 内做归约、扫描等操作时减少共享内存的 bank 冲突与带宽占用，并降低延迟。</p><p>典型用法：<strong>归约</strong>时，warp 内 32 个线程先各自持有一个值，通过 <code>__shfl_down_sync</code> 等逐步两两相加，最终 warp 内 lane 0 得到该 warp 的部分和；多个 warp 再通过共享内存或第二次核函数合并。<strong>同步</strong>：洗牌指令需指定<strong>参与线程的掩码</strong>（如 <code>__shfl_down_sync(0xffffffff, val, offset)</code> 表示整个 warp 参与），以保证语义正确。其他变体包括：<strong><code>__shfl_up_sync</code></strong>（从更高 lane 索引取）、<strong><code>__shfl_xor_sync</code></strong>（按 lane 索引异或得到源 lane，常用于 butterfly 式归约），以及按<strong>源 lane 索引</strong>直接读取的 <strong><code>__shfl_sync</code></strong>，便于实现更灵活的数据交换模式。</p><blockquote><p><strong>理解与体会</strong>：Warp shuffle 把「warp 内数据交换」从共享内存搬到了<strong>寄存器间直接交换</strong>，既省共享内存带宽，又降低延迟。适合「先 warp 内归约/扫描，再块内跨 warp 合并」的两阶段设计；此时共享内存只需存每个 warp 的一个结果，而不是 32 个中间值。</p></blockquote><h3 id="7-2-jian-dan-shi-li-warp-nei-qiu-he" tabindex="-1" id="7-2-简单示例：warp-内求和">7.2 简单示例：warp 内求和</h3><p>下面给出 <strong>warp 内</strong> 32 个线程对寄存器 <code>val</code> 做求和，结果在 lane 0 的片段（完整归约还需块内跨 warp 合并）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">__device__ __forceinline__ <span class="type">int</span> <span class="title function_">warpReduce</span><span class="params">(<span class="type">int</span> val)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> offset = warpSize / <span class="number">2</span>; offset &gt; <span class="number">0</span>; offset &gt;&gt;= <span class="number">1</span>) &#123;</span><br><span class="line">        val += __shfl_down_sync(<span class="number">0xffffffff</span>, val, offset);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> val;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceShuffle</span><span class="params">(<span class="type">int</span> *g_idata, <span class="type">int</span> *g_odata, <span class="type">unsigned</span> <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = blockIdx.x * blockDim.x + tid;</span><br><span class="line">    <span class="type">int</span> val = (idx &lt; n) ? g_idata[idx] : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> lane = tid % warpSize;</span><br><span class="line">    <span class="type">int</span> warpId = tid / warpSize;</span><br><span class="line"></span><br><span class="line">    val = warpReduce(val);  <span class="comment">// warp 内归约</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (lane == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 每个 warp 的 lane 0 写入共享内存，再在块内归约</span></span><br><span class="line">        <span class="comment">// 此处省略共享内存合并多 warp 的代码</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>__shfl_down_sync(mask, val, offset)</code></strong> 表示当前线程从「相对自己向下 offset 个 lane」的线程取 <code>val</code>；其他变体包括 <code>__shfl_up_sync</code>、<code>__shfl_xor_sync</code> 等，用于不同的数据移动模式。使用洗牌时需注意 <strong>mask</strong> 必须与当前活跃线程一致（例如整个 warp 参与则 mask = 0xffffffff），否则未定义行为。</p><blockquote><p><strong>易错点</strong>：<strong>Shuffle 的 mask 必须正确</strong>。若内核中有分支导致部分线程不参与归约，则不应使用全 warp 的 <code>0xffffffff</code>，而应使用实际参与线程的掩码（如 <code>__activemask()</code> 或根据分支计算出的 mask），否则未参与线程的寄存器值会被错误地参与交换，导致结果错误或未定义行为。</p></blockquote><hr><h2 id="ba-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="八、本章小结与重难点回顾">八、本章小结与重难点回顾</h2><h3 id="8-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="8-1-知识小结（与书中对应）">8.1 知识小结（与书中对应）</h3><ul><li><strong>共享内存</strong>：片上可编程、块内可见、低延迟高带宽；用于作可编程缓存、重组访问模式（如矩阵转置）、块内协作与归约。分静态（<code>__shared__ T arr[N]</code>）与动态（<code>extern __shared__ T s[]</code>，启动时传字节数）。</li><li><strong>布局与 bank</strong>：共享内存按行优先线性排布；Fermi 及以后为 32 bank、每 bank 4 字节，bank 索引 = (字节地址/4) mod 32。同一 warp 访问同一 bank 不同地址会 bank 冲突；同址则广播无冲突。</li><li><strong>Padding</strong>：二维共享内存每行多一列（如 <code>tile[32][33]</code>）可使按列访问时 bank 分散，消除典型矩阵转置中的 bank 冲突。</li><li><strong>矩阵转置</strong>：用共享内存 tile 先合并读入、<code>__syncthreads()</code>、再按列读出写回全局，实现读写双端合并；配合 padding 避免共享内存按列读时的 bank 冲突。</li><li><strong>并行归约</strong>：将块内数据先载入共享内存，在共享内存中做多轮交错配对归约，最后写回一块一个标量；可结合 warp 内洗牌进一步减少共享内存访问。</li><li><strong>常量内存</strong>：<code>__constant__</code> + <code>cudaMemcpyToSymbol</code>，只读、专用常量缓存；warp 内同址访问时广播，性能最优；不同址会串行化。</li><li><strong>常量缓存 vs 只读缓存</strong>：常量缓存面向 <code>__constant__</code>；只读缓存面向全局内存只读加载（<code>__ldg</code>），适合大数组或分散读。</li><li><strong>线程束洗牌</strong>：<code>__shfl_*_sync</code> 在 warp 内通过寄存器交换数据，无需共享内存，适合 warp 内归约/扫描；需正确设置 mask。</li></ul><h3 id="8-2-zhong-nan-dian-su-cha" tabindex="-1" id="8-2-重难点速查">8.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>共享内存 vs 全局内存</strong></td><td>共享内存片上、块内可见、低延迟高带宽；用作可编程缓存时，全局内存只负责合并进、合并出，中间重组在片上完成。</td></tr><tr><td><strong>Bank 与 bank 冲突</strong></td><td>32 bank、每 bank 4 字节；bank 索引 = (字节地址/4) mod 32。同一 warp 访问同一 bank 不同地址→冲突；同址→广播无冲突。</td></tr><tr><td><strong>Padding 消除冲突</strong></td><td>二维行优先下按列访问易冲突；每行多一列（如 33）使相邻行线性索引差 33 字，33 mod 32=1，32 行分散到 32 个 bank。</td></tr><tr><td><strong>矩阵转置索引</strong></td><td>读入 <code>tile[ty][tx]=in[row*nx+col]</code>，写出 <code>out[col*ny+row]=tile[tx][ty]</code>；row/col 含块偏移。</td></tr><tr><td><strong>__syncthreads 使用</strong></td><td>必须被块内所有线程执行，不能放在仅部分线程进入的分支内，否则死锁或未定义行为。</td></tr><tr><td><strong>常量内存适用场景</strong></td><td>仅当 warp 内同址访问时才有广播优势；不同址会串行化，且容量有限（如 64KB）。大表或分散读宜用全局内存+__ldg。</td></tr><tr><td><strong>Warp shuffle 与 mask</strong></td><td>warp 内寄存器级交换，无需共享内存；mask 须与参与线程一致，分支导致部分线程不参与时不能用 0xffffffff。</td></tr></tbody></table><h3 id="8-3-xue-xi-si-kao" tabindex="-1" id="8-3-学习思考">8.3 学习思考</h3><ul><li><strong>与第 4 章的衔接</strong>：第 4 章指出矩阵转置在纯全局内存下无法同时做到读合并与写合并；本章用共享内存做 tile 中转，先合并读入、在片上转置、再合并写出，是「用空间换带宽」的典型。理解这一点，就能举一反三到其他「访问模式与合并要求矛盾」的场景。</li><li><strong>与第 3 章的关系</strong>：第 3 章归约在全局内存上多轮读写；本章把块内数据搬进共享内存后多轮归约，每块只写回一个标量，大幅减少全局访问次数。进一步地，warp 内用 shuffle 做第一段归约，共享内存只存每个 warp 的一个结果，可同时减少共享内存占用与 bank 冲突风险。</li><li><strong>实践建议</strong>：写新内核时，若存在「先按一种顺序读、再按另一种顺序写」的需求，优先考虑共享内存 tile + __syncthreads；使用二维共享内存时，若按列访问，检查是否需 padding（如 tile[32][33]）；常量内存只留给「全 warp 同参数」的小表或配置；归约类内核可先实现共享内存版，再在 warp 内用 __shfl_down_sync 做一层优化。</li></ul><p>下表归纳本章涉及的三种可编程内存的典型用途与性能要点（与书中描述一致）：</p><table><thead><tr><th>内存类型</th><th>作用域</th><th>典型用途</th><th>性能要点</th></tr></thead><tbody><tr><td><strong>共享内存</strong></td><td>线程块</td><td>tile 缓存、转置/归约中转、块内协作</td><td>低延迟、高带宽；注意 bank 冲突与 padding</td></tr><tr><td><strong>全局内存</strong></td><td>全部</td><td>主数据、输入输出缓冲</td><td>对齐与合并；可配合共享内存减少访问</td></tr><tr><td><strong>常量内存</strong></td><td>全部</td><td>只读参数、小表、全 warp 同址数据</td><td>同址广播优；不同址易串行化，容量有限（如 64 KB）</td></tr></tbody></table><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 6 章：流和并发</strong>：</p><ul><li><strong>CUDA 流</strong>的概念与默认流</li><li><strong>基于流的重叠</strong>：主机与设备、传输与计算、多流并发</li><li><strong>流回调</strong>与事件（cudaEvent）</li><li><strong>多 GPU</strong> 编程入门</li></ul><p>从「单次内核与单次传输」到「用流与事件组织并发」，是进一步提升吞吐与隐藏延迟的关键一步。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>共享内存的 bank 冲突是什么？如何用 padding 消除 32×32 tile 的写冲突？</strong></li><li><strong>用共享内存做矩阵转置时，典型步骤是什么？为何能同时实现读合并与写合并？</strong></li><li><strong>块内线程在共享内存上协作时，为什么必须在「加载完成」与「使用数据前」之间加 <code>__syncthreads()</code>？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>同一 warp 内多线程若访问同一 bank 的不同地址，会串行化（bank 冲突）。32×32 按行存时，同一列属于同一 bank；在每行后多开一列（如 32×33）做 padding，使同一列元素分布到不同 bank，可消除写冲突。</li><li>步骤：块内线程按行从全局内存合并读入共享内存 → <code>__syncthreads()</code> → 按列从共享内存读出并写回全局转置位置（写合并）。全局侧只做「整块合并读入」和「整块合并写出」，中间在片上转置。</li><li>因为线程并行执行，写共享内存的线程与读共享内存的线程可能不同步；若不加 <code>__syncthreads()</code>，读线程可能读到未写入的数据。栅栏保证「所有线程写完、对块内可见」后再继续。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/1f074790/">第4章 全局内存</a> | 下一篇：<a href="https://smarter.xin/posts/d47d974/">第6章 流和并发</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 5 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-wu-zhang-gong-xiang-nei-cun-he-chang-liang-nei-cun&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="共享内存" scheme="https://smarter.xin/tags/shared-memory/"/>
    
    <category term="常量内存" scheme="https://smarter.xin/tags/constant-memory/"/>
    
    <category term="bank冲突" scheme="https://smarter.xin/tags/bank-conflict/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第四章：全局内存</title>
    <link href="https://smarter.xin/posts/1f074790/"/>
    <id>https://smarter.xin/posts/1f074790/</id>
    <published>2026-02-05T05:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-si-zhang-quan-ju-nei-cun" tabindex="-1">CUDA C编程权威指南-第四章：全局内存</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/bf3e0449/">第3章 CUDA执行模型</a> | 下一篇：<a href="https://smarter.xin/posts/e1d6d733/">第5章 共享内存和常量内存</a></p><blockquote><p>系列第 4 篇。第 3 章里有个现象：线程块最内层维度取线程束一半时，内存加载效率会明显变差——<strong>根因是对全局内存的访问模式不好</strong>。这一章就讲核函数和全局内存怎么打交道：内存模型、对齐与合并、带宽优化，以及固定内存、零拷贝、统一内存等用法。</p></blockquote><hr><h2 id="qian-yan-cong-zhi-xing-dao-fang-cun" tabindex="-1" id="前言：从执行到访存">前言：从执行到访存</h2><p>书中开篇即指出：在上章「用 nvprof 检测内存选项」中，将线程块的最内层维度设为线程束大小的一半时，内存加载效率会大幅下降——<strong>这一性能损失无法通过线程束调度或暴露的并行量来解释</strong>。真正的原因是<strong>对全局内存的访问模式不佳</strong>。</p><p>本章将帮助你（书中原意）：</p><ul><li><strong>剖析内核与全局内存的交互</strong>，理解这些交互如何影响性能</li><li><strong>解释 CUDA 内存模型</strong>，通过分析不同的全局内存访问模式，学会在核函数中高效使用全局内存</li></ul><p>大多数 GPU 应用受<strong>内存带宽</strong>限制；若不能正确组织对全局内存的访问，其他优化往往事倍功半。本章将系统覆盖：内存模型、内存管理（含固定内存、零拷贝、统一寻址）、对齐与合并、读写路径、矩阵转置案例、带宽测量、AoS/SoA，为第 5 章共享内存与常量内存打下基础。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-quan-ju-nei-cun" tabindex="-1" id="1-1-为什么要学「全局内存」">1.1 为什么要学「全局内存」</h3><p>第 3 章从<strong>执行模型</strong>回答了「线程如何被调度、为何 32 很重要、占用率与延迟隐藏」；但性能瓶颈往往不仅在计算，更在<strong>访存</strong>。需要搞清楚的是：将线程块最内层维度设为线程束大小的一半时，内存加载效率会大幅下降——<strong>这一现象无法仅用线程束调度或并行量解释</strong>，根本原因是对<strong>全局内存的访问模式不佳</strong>。因此，理解 CUDA 内存模型、掌握<strong>对齐与合并</strong>、学会选择合适的内存分配方式（固定内存、零拷贝、统一内存），是写出高性能核函数的基础；本章也为第 5 章「用共享内存改善全局内存访问」做铺垫。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解 <strong>CUDA 内存层次</strong>（寄存器、本地、共享、全局、常量、纹理）及 L1/L2/只读缓存</td><td>能说出各内存的作用域、生命周期及何时用谁</td></tr><tr><td>掌握 <strong>cudaMalloc / cudaMemcpy / 固定内存 / 零拷贝 / 统一内存</strong> 的用法与适用场景</td><td>能解释为何 H2D/D2H 要尽量减少、何时用固定内存、零拷贝的局限</td></tr><tr><td>理解 <strong>对齐</strong>与<strong>合并</strong>的定义及对事务数、带宽利用率的影响</td><td>能判断一段访问是否对齐、是否合并，并估算事务数</td></tr><tr><td>理解 <strong>128 字节与 32 字节</strong>事务、<strong>L1/L2/只读缓存</strong>三种读取路径</td><td>能说出写操作为何不经 L1、何时用 <code>__ldg</code> 或 <code>const __restrict__</code></td></tr><tr><td>掌握 <strong>矩阵转置</strong>中「合并读+非合并写」与「非合并读+合并写」的取舍</td><td>能解释为何写合并往往更关键，并知道第 5 章用共享内存可双端合并</td></tr><tr><td>会用 <strong>有效带宽公式</strong>与 nvprof 判断带宽受限、计算受限</td><td>能计算有效带宽并与理论峰值对比</td></tr><tr><td>理解 <strong>AoS vs SoA</strong> 对合并与带宽的影响</td><td>能解释为何只访问部分字段时 AoS 约 50% 带宽浪费</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">第 4 章  全局内存</span><br><span class="line">│</span><br><span class="line">├── 二、CUDA 内存模型概述（书 4.1 节）</span><br><span class="line">│   ├── 内存层次结构的必要性、可编程内存详解、GPU 缓存</span><br><span class="line">│   └── 重点：全局内存的「对齐+合并」决定带宽利用率</span><br><span class="line">│</span><br><span class="line">├── 三、内存管理（书 4.2 节）</span><br><span class="line">│   ├── 设备内存分配与释放、主机与设备间数据传输</span><br><span class="line">│   ├── 固定内存（cudaMallocHost）、零拷贝（cudaHostAlloc Mapped）</span><br><span class="line">│   └── 统一虚拟寻址与统一内存（cudaMallocManaged）</span><br><span class="line">│</span><br><span class="line">├── 四、内存访问模式（书 4.3 节）★ 重难点</span><br><span class="line">│   ├── 内存事务、对齐与合并的概念与四种模式表</span><br><span class="line">│   ├── 全局内存读取路径（L1/L2、只读缓存、__ldg/__restrict__）</span><br><span class="line">│   ├── 全局内存写入（仅 L2、32 字节段）</span><br><span class="line">│   └── 矩阵转置：合并读+非合并写 vs 非合并读+合并写</span><br><span class="line">│</span><br><span class="line">├── 五、核函数能达到的带宽与性能分析（书 4.4 节）</span><br><span class="line">│   └── 有效带宽公式、与理论峰值对比、计算受限 vs 带宽受限</span><br><span class="line">│</span><br><span class="line">├── 六、使用统一内存的矩阵加法（书 4.5 节）</span><br><span class="line">│   └── cudaMallocManaged 与传统方式的对比</span><br><span class="line">│</span><br><span class="line">├── 七、结构体数组（AoS）与数组结构体（SoA）（书 4.6 节）</span><br><span class="line">│   └── 只访问部分字段时 AoS 的带宽浪费与 SoA 的合并优势</span><br><span class="line">│</span><br><span class="line">└── 八、本章小结与重难点回顾</span><br></pre></td></tr></table></figure><hr><h2 id="er-cuda-nei-cun-mo-xing-gai-shu-shu-4-1-jie" tabindex="-1" id="二、CUDA-内存模型概述（书-4-1-节）">二、CUDA 内存模型概述（书 4.1 节）</h2><h3 id="2-1-nei-cun-ceng-ci-jie-gou-de-bi-yao-xing" tabindex="-1" id="2-1-内存层次结构的必要性">2.1 内存层次结构的必要性</h3><p>程序具有<strong>时间局部性</strong>（刚用过的数据很快会再用）和<strong>空间局部性</strong>（用过的数据附近的数据也会被用）。内存层次结构在「容量大、速度慢」与「容量小、速度快」之间折中，把热数据放在更快、更小的存储中。CUDA 与 CPU 的一大区别是：<strong>CUDA 将部分内存层次暴露给程序员</strong>（如共享内存、常量内存），便于我们显式控制数据放置与访问方式。</p><p><strong>CUDA 编程模型从 GPU 架构中抽象出一个内存层次结构</strong>（书中图 2-3 为简化的 GPU 内存结构，主要包含全局内存和共享内存）。在 GPU 内存层次结构中，最主要的两种内存是<strong>全局内存</strong>和<strong>共享内存</strong>：全局内存类似于 CPU 的系统内存，共享内存类似于 CPU 的缓存，但 <strong>GPU 的共享内存可以由 CUDA C 的内核直接控制</strong>。</p><blockquote><p><strong>理解与体会</strong>：CPU 的缓存对程序员是透明的；而 CUDA 把共享内存、常量内存等暴露出来，意味着我们既要理解「谁快谁慢」，也要在代码里显式决定数据放哪、怎么访问。这是「以空间换时间」和「以控制换性能」的典型体现。</p></blockquote><h3 id="2-2-ke-bian-cheng-nei-cun-xiang-jie" tabindex="-1" id="2-2-可编程内存详解">2.2 可编程内存详解</h3><p>书中按<strong>作用域、生命周期和缓存行为</strong>对可编程内存进行了分类。下表与书中描述一致，便于查阅：</p><table><thead><tr><th>内存类型</th><th>修饰符/分配方式</th><th>作用域</th><th>生命周期</th><th>特点</th></tr></thead><tbody><tr><td><strong>寄存器</strong></td><td>核函数内普通变量</td><td>线程私有</td><td>线程</td><td>最快；Fermi 每线程最多 63，Kepler 扩展至 255；溢出则进本地内存</td></tr><tr><td><strong>本地内存</strong></td><td>寄存器溢出、大数组、未知索引</td><td>线程私有</td><td>线程</td><td>实际在全局内存区，高延迟、低带宽</td></tr><tr><td><strong>共享内存</strong></td><td><code>__shared__</code></td><td>线程块</td><td>块</td><td>片上、可编程；与 L1 共享 64KB（Fermi），需注意 bank 冲突</td></tr><tr><td><strong>全局内存</strong></td><td><code>cudaMalloc</code> / <code>__device__</code></td><td>全部</td><td>应用</td><td>容量大、延迟高；访问需对齐与合并</td></tr><tr><td><strong>常量内存</strong></td><td><code>__constant__</code> + <code>cudaMemcpyToSymbol</code></td><td>全部</td><td>应用</td><td>只读、有专用常量缓存；同址广播优</td></tr><tr><td><strong>纹理内存</strong></td><td>纹理 API</td><td>全部</td><td>应用</td><td>只读、滤波、二维空间局部性优</td></tr></tbody></table><p>要点补充（书中对应内容）：</p><ul><li><strong>寄存器</strong>：核函数内未加特殊修饰的局部变量通常存放在寄存器中。寄存器是 SM 的稀缺资源；溢出部分会放到<strong>本地内存</strong>，对性能影响很大。</li><li><strong>本地内存</strong>：编译器可能将以下变量放在本地内存——不满足寄存器限定条件的变量、占用大量寄存器空间的大数组或结构体、使用未知索引引用的本地数组。本地内存与全局内存在同一物理存储区域，访问特性为高延迟、低带宽。</li><li><strong>共享内存</strong>：在核函数中用 <code>__shared__</code> 声明；生命周期与线程块一致。Fermi 上可通过 <code>cudaFuncSetCacheConfig</code> 在共享内存与 L1 之间调整 64KB 的划分（如 prefer shared / prefer L1 / prefer equal）。</li><li><strong>常量内存</strong>：用 <code>__constant__</code> 在全局范围声明，通过 <code>cudaMemcpyToSymbol</code> 由主机初始化；对所有设备只读。当线程束中所有线程从<strong>相同地址</strong>取数据时，常量内存表现最好（一次读取广播给整个 warp）；不同线程取不同地址时不如合并的全局内存访问。</li><li><strong>纹理内存</strong>：驻留在设备内存，通过专用只读缓存访问，支持滤波与二维局部性优化。</li><li><strong>全局内存</strong>：对应设备 DRAM，可动态或静态分配（<code>__device__</code>）；访问时<strong>对齐</strong>与<strong>跨线程的地址分布</strong>共同决定事务数量与带宽利用率。这一节会说明：一般情况下，满足内存请求的<strong>事务越多，未使用的字节被传输的可能性越大</strong>，数据吞吐量就会降低。</li></ul><h3 id="2-3-gpu-huan-cun-bu-ke-bian-cheng" tabindex="-1" id="2-3-GPU-缓存（不可编程）">2.3 GPU 缓存（不可编程）</h3><p>与 CPU 缓存类似，GPU 缓存不可编程，其行为在出厂时已设定。书中提到的四种缓存为：</p><ul><li><strong>一级缓存</strong>：每个 SM 一块，与共享内存共享片上存储（如 Fermi 64KB 可配置）。用于缓存全局内存和本地内存的加载；<strong>存储操作不经过 L1</strong>。</li><li><strong>二级缓存</strong>：所有 SM 共享，全局内存访问必经 L2（除非走只读缓存路径）。</li><li><strong>只读常量缓存</strong>：每 SM 一块，用于常量内存。</li><li><strong>只读纹理/只读缓存</strong>：用于纹理与（计算能力 3.5+）全局内存只读加载，粒度 32 字节。</li></ul><p>Fermi 及之后架构允许通过编译选项控制<strong>全局内存加载</strong>是否经 L1（见第四节）。</p><hr><h2 id="san-nei-cun-guan-li-shu-4-2-jie" tabindex="-1" id="三、内存管理（书-4-2-节）">三、内存管理（书 4.2 节）</h2><p>CUDA 编程模型假设系统由<strong>一个主机和一个设备</strong>组成，且<strong>各自拥有独立的内存</strong>。核函数在设备上运行；为使你有充分的控制权并使系统达到最佳性能，<strong>CUDA 运行时负责分配与释放设备内存，并在主机内存和设备内存之间传输数据</strong>。书中<strong>表 2-1</strong> 列出了标准 C 函数与对应的 CUDA C 内存操作函数。下表与书中一致，便于本章查阅与对照：</p><table><thead><tr><th>标准 C 函数</th><th>CUDA C 函数</th><th>功能说明</th></tr></thead><tbody><tr><td><code>malloc</code></td><td><code>cudaMalloc</code></td><td>分配内存（设备端线性内存）</td></tr><tr><td><code>memcpy</code></td><td><code>cudaMemcpy</code></td><td>拷贝数据（主机↔设备或设备↔设备）</td></tr><tr><td><code>memset</code></td><td><code>cudaMemset</code></td><td>将设备内存某区域设为指定值</td></tr><tr><td><code>free</code></td><td><code>cudaFree</code></td><td>释放设备内存</td></tr></tbody></table><p>主机端分配仍可用 <code>malloc</code>/<code>free</code>；设备端必须使用上表中的 CUDA API。<strong>不能</strong>在主机代码中解引用设备指针（如 <code>*d_A</code>），否则会导致崩溃；CUDA 6.0 起的统一寻址/统一内存提供了单指针模型，详见 3.5 小节。</p><h3 id="3-1-she-bei-nei-cun-de-fen-pei-yu-shi-fang" tabindex="-1" id="3-1-设备内存的分配与释放">3.1 设备内存的分配与释放</h3><p><strong>cudaMalloc</strong> — 在设备上分配线性内存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMalloc</span><span class="params">(<span class="type">void</span> **devPtr, <span class="type">size_t</span> count)</span>;</span><br></pre></td></tr></table></figure><p><code>devPtr</code> 为指针的指针，用于返回所分配内存的设备指针；<code>count</code> 为字节数。与标准 C 的 <code>malloc</code> 类似，只是在 GPU 内存中分配。分配失败时返回 <code>cudaErrorMemoryAllocation</code>。使用前应检查返回值；书中推荐用统一的错误处理宏（如 <code>CHECK(cudaMalloc(...))</code>）封装。</p><p><strong>cudaMemset</strong> — 设备内存初始化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMemset</span><span class="params">(<span class="type">void</span> *devPtr, <span class="type">int</span> value, <span class="type">size_t</span> count)</span>;</span><br></pre></td></tr></table></figure><p><strong>cudaFree</strong> — 释放设备内存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaFree</span><span class="params">(<span class="type">void</span> *devPtr)</span>;</span><br></pre></td></tr></table></figure><p>注意：<code>devPtr</code> 必须是由 <code>cudaMalloc</code>（或其它设备分配 API）得到的指针；重复释放或非法指针会报错。<strong>设备内存的分配和释放非常影响性能，应尽量重复利用</strong>已分配缓冲区。</p><h3 id="3-2-zhu-ji-yu-she-bei-jian-de-shu-ju-chuan-shu" tabindex="-1" id="3-2-主机与设备间的数据传输">3.2 主机与设备间的数据传输</h3><p><strong>cudaMemcpy</strong> — 在主机与设备之间拷贝数据：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMemcpy</span><span class="params">(<span class="type">void</span> *dst, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">size_t</span> count, cudaMemcpyKind kind)</span>;</span><br></pre></td></tr></table></figure><p><code>kind</code> 常用取值：</p><ul><li><code>cudaMemcpyHostToDevice</code>：主机 → 设备</li><li><code>cudaMemcpyDeviceToHost</code>：设备 → 主机</li><li><code>cudaMemcpyDeviceToDevice</code>：设备 → 设备</li></ul><p>此函数<strong>以同步方式执行</strong>：在 <code>cudaMemcpy</code> 返回且传输完成之前，主机应用程序是阻塞的。除内核启动外，大多数 CUDA API 会返回 <code>cudaError_t</code>；可用 <strong>cudaGetErrorString</strong> 将错误码转为可读字符串（类似 C 的 <code>strerror</code>）。</p><p><strong>带宽对比</strong>（书中以 Fermi 为例）：</p><ul><li>设备内存（GDDR5）：理论峰值约 <strong>144 GB/s</strong>（随型号不同）。</li><li>PCIe 总线：约 <strong>8 GB/s</strong> 量级。</li></ul><p>因此<strong>减少主机与设备之间的拷贝次数与数据量</strong>是优化的重要原则；应尽量在设备上完成多步计算后再回传，并善用固定内存、流重叠（第 6 章）或统一内存。</p><blockquote><p><strong>理解与体会</strong>：PCIe 带宽远低于设备内存带宽，因此「能少拷就少拷、能重叠就重叠」是金科玉律。固定内存的意义不仅在于单次传输更快，更在于为第 6 章的异步传输与流重叠创造条件。</p></blockquote><h3 id="3-3-gu-ding-nei-cun-pinned-memory" tabindex="-1" id="3-3-固定内存（Pinned-Memory）">3.3 固定内存（Pinned Memory）</h3><p>普通主机内存是<strong>可分页</strong>的：操作系统可能将物理页换出或移动。在向设备传输时，驱动可能先<strong>锁页</strong>，将源数据复制到一块临时的「锁页」缓冲区，再从该缓冲区 DMA 到设备——多一次拷贝且难以与计算重叠。</p><p><strong>固定内存</strong>（页锁定内存）在分配时就保证不会被换页或移动，设备可通过 DMA <strong>直接</strong>访问，从而：</p><ul><li>提高 H2D/D2H 传输带宽</li><li>便于与<strong>异步拷贝</strong>和<strong>流</strong>结合，实现传输与计算重叠（第 6 章）</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMallocHost</span><span class="params">(<span class="type">void</span> **ptr, <span class="type">size_t</span> count)</span>;</span><br><span class="line">cudaError_t <span class="title function_">cudaFreeHost</span><span class="params">(<span class="type">void</span> *ptr)</span>;</span><br></pre></td></tr></table></figure><p>这一节会说明：固定内存的<strong>分配与释放成本</strong>比可分页内存<strong>高</strong>，且会占用不可换页的物理页，不宜无节制使用。<strong>适合对传输性能敏感、且会多次传输的大块数据</strong>。下面给出书中风格的<strong>完整主函数示例</strong>：使用固定内存分配主机数组，拷贝到设备、执行核函数、再拷回主机，并与错误处理宏配合使用。核函数仍为简单的向量加法（与第 2 章一致）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                            \</span></span><br><span class="line"><span class="meta">    do &#123;                                                                       \</span></span><br><span class="line"><span class="meta">        cudaError_t err = call;                                                 \</span></span><br><span class="line"><span class="meta">        <span class="keyword">if</span> (err != cudaSuccess) &#123;                                               \</span></span><br><span class="line"><span class="meta">            printf(<span class="string">&quot;Error %s:%d, code %d, reason: %s\n&quot;</span>, __FILE__, __LINE__,   \</span></span><br><span class="line"><span class="meta">                   err, cudaGetErrorString(err));                               \</span></span><br><span class="line"><span class="meta">            exit(1);                                                            \</span></span><br><span class="line"><span class="meta">        &#125;                                                                      \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">sumArraysGPU</span><span class="params">(<span class="type">float</span> *a, <span class="type">float</span> *b, <span class="type">float</span> *c)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    c[i] = a[i] + b[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> nElem = <span class="number">1</span> &lt;&lt; <span class="number">14</span>;  <span class="comment">// 16384</span></span><br><span class="line">    <span class="type">size_t</span> nBytes = nElem * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 固定内存分配（主机端）</span></span><br><span class="line">    <span class="type">float</span> *h_a, *h_b, *h_c;</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span>**)&amp;h_a, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span>**)&amp;h_b, nBytes));</span><br><span class="line">    CHECK(cudaMallocHost((<span class="type">void</span>**)&amp;h_c, nBytes));</span><br><span class="line">    <span class="comment">// 初始化 h_a, h_b ...</span></span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> *d_a, *d_b, *d_c;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span>**)&amp;d_a, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span>**)&amp;d_b, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span>**)&amp;d_c, nBytes));</span><br><span class="line"></span><br><span class="line">    CHECK(cudaMemcpy(d_a, h_a, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line">    CHECK(cudaMemcpy(d_b, h_b, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    dim3 <span class="title function_">block</span><span class="params">(<span class="number">1024</span>)</span>;</span><br><span class="line">    dim3 <span class="title function_">grid</span><span class="params">(nElem / block.x)</span>;</span><br><span class="line">    sumArraysGPU&lt;&lt;&lt;grid, block&gt;&gt;&gt;(d_a, d_b, d_c);</span><br><span class="line"></span><br><span class="line">    CHECK(cudaMemcpy(h_c, d_c, nBytes, cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    cudaFree(d_a);</span><br><span class="line">    cudaFree(d_b);</span><br><span class="line">    cudaFree(d_c);</span><br><span class="line">    cudaFreeHost(h_a);</span><br><span class="line">    cudaFreeHost(h_b);</span><br><span class="line">    cudaFreeHost(h_c);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 <strong>nvprof</strong> 对比「可分页内存 + cudaMemcpy」与「固定内存 + cudaMemcpy」时，可看到固定内存路径的 H2D/D2H 耗时更短；当数据规模较大时，传输时间的节省往往能抵消固定内存分配的成本。</p><h3 id="3-4-ling-kao-bei-nei-cun-zero-copy-pinned-mapped-memory" tabindex="-1" id="3-4-零拷贝内存（Zero-Copy-Pinned-Mapped-Memory）">3.4 零拷贝内存（Zero-Copy / Pinned Mapped Memory）</h3><p>零拷贝内存是<strong>固定在主机上的内存</strong>，且<strong>设备端可通过另一套指针直接访问</strong>，无需显式 <code>cudaMemcpy</code>。书中适用场景包括：</p><ul><li>提高 PCIe 传输率（某些访问模式下）</li><li>避免主机和设备之间的显式内存传输</li><li>设备内存不足时利用主机内存</li></ul><p>创建零拷贝内存需使用 <strong>cudaHostAlloc</strong>，并传入 <strong>cudaHostAllocMapped</strong> 标志：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaHostAlloc</span><span class="params">(<span class="type">void</span> **pHost, <span class="type">size_t</span> count, <span class="type">unsigned</span> <span class="type">int</span> flags)</span>;</span><br></pre></td></tr></table></figure><p>常用 <code>flags</code>：<code>cudaHostAllocMapped</code>（产生零拷贝）、<code>cudaHostAllocPortable</code>（所有 CUDA 上下文可用）、<code>cudaHostAllocWriteCombined</code>（某些设备上写传输更优）。设备端不能直接使用 <code>pHost</code>，必须先取得「设备可见」的指针：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaHostGetDevicePointer</span><span class="params">(<span class="type">void</span> **pDevice, <span class="type">void</span> *pHost, <span class="type">unsigned</span> <span class="type">int</span> flags)</span>;</span><br></pre></td></tr></table></figure><p>核函数中使用 <code>pDevice</code> 访问的是主机上 <code>pHost</code> 指向的那块内存。<strong>注意</strong>：使用零拷贝时，主机与设备可能同时访问同一块内存，需注意<strong>主机与设备之间的竞争条件</strong>。零拷贝内存在物理上仍在主机，<strong>每次设备访问都经 PCIe</strong>；书中实验表明，对向量加法这类需要多次读写的内核，<strong>常规「先 cudaMemcpy 到设备再计算」往往比零拷贝更快</strong>，因为零拷贝下每次加载/存储都走 PCIe。零拷贝更适合「一次读、一次写」或设备显存不足时的权宜之计；在<strong>集成架构</strong>（如 Tegra，CPU 与 GPU 共享物理内存）上，零拷贝效果会好很多。</p><p>书中给出了零拷贝与常规设备内存的<strong>运行时间对比表</strong>（不同数据规模 (2^n) 元素）。下表与书中实验对应（单位：μs，为总耗时示例）：</p><table><thead><tr><th>数据规模 (n)（(2^n) 元素）</th><th>常规设备内存（μs）</th><th>零拷贝内存（μs）</th></tr></thead><tbody><tr><td>10</td><td>2.5</td><td>3.0</td></tr><tr><td>12</td><td>3.0</td><td>4.1</td></tr><tr><td>14</td><td>7.8</td><td>8.6</td></tr><tr><td>16</td><td>23.1</td><td>25.8</td></tr><tr><td>18</td><td>86.5</td><td>98.2</td></tr><tr><td>20</td><td>290.9</td><td>310.5</td></tr></tbody></table><p>零拷贝流程的典型主机端代码（书中风格）如下。注意：先用 <code>cudaHostAlloc(..., cudaHostAllocMapped)</code> 分配，再通过 <code>cudaHostGetDevicePointer</code> 取得设备端指针传给核函数；核函数执行时每次对 <code>a_dev</code>/<code>b_dev</code> 的访问都经 PCIe，因此数据量大时总时间更长。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">float</span> *a_host, *b_host, *res_d;</span><br><span class="line">CHECK(cudaHostAlloc((<span class="type">void</span>**)&amp;a_host, nBytes, cudaHostAllocMapped));</span><br><span class="line">CHECK(cudaHostAlloc((<span class="type">void</span>**)&amp;b_host, nBytes, cudaHostAllocMapped));</span><br><span class="line">CHECK(cudaMalloc((<span class="type">void</span>**)&amp;res_d, nBytes));</span><br><span class="line"><span class="comment">// 初始化 a_host, b_host ...</span></span><br><span class="line"></span><br><span class="line"><span class="type">float</span> *a_dev, *b_dev;</span><br><span class="line">CHECK(cudaHostGetDevicePointer((<span class="type">void</span>**)&amp;a_dev, a_host, <span class="number">0</span>));</span><br><span class="line">CHECK(cudaHostGetDevicePointer((<span class="type">void</span>**)&amp;b_dev, b_host, <span class="number">0</span>));</span><br><span class="line">sumArraysGPU&lt;&lt;&lt;grid, block&gt;&gt;&gt;(a_dev, b_dev, res_d);</span><br><span class="line">CHECK(cudaMemcpy(res_from_gpu_h, res_d, nBytes, cudaMemcpyDeviceToHost));</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">cudaFreeHost(a_host);</span><br><span class="line">cudaFreeHost(b_host);</span><br><span class="line">cudaFree(res_d);</span><br></pre></td></tr></table></figure><p>随规模增大，零拷贝的劣势更明显，因为内核执行时每次访存都经 PCIe。</p><blockquote><p><strong>重难点</strong>：零拷贝的物理位置始终在<strong>主机</strong>，设备每次访问都经 PCIe；因此对「多次读写」的内核（如向量加），常规「先 cudaMemcpy 再计算」通常更快。零拷贝更适合「一次读、一次写」或设备显存不足时的权宜之计；在集成 GPU（CPU 与 GPU 共享物理内存）上效果会好很多。</p></blockquote><h3 id="3-5-tong-yi-xu-ni-xun-zhi-uva-yu-tong-yi-nei-cun-unified-memory" tabindex="-1" id="3-5-统一虚拟寻址（UVA）与统一内存（Unified-Memory）">3.5 统一虚拟寻址（UVA）与统一内存（Unified Memory）</h3><p><strong>统一虚拟寻址（UVA）</strong>：自 CUDA 4.0（设备架构 2.0）起，设备内存和主机内存在<strong>同一虚拟地址空间</strong>中映射。这样，<strong>由 cudaHostAlloc 分配的固定主机内存在主机和设备上可使用同一地址</strong>，无需再通过 <code>cudaHostGetDevicePointer</code> 获取设备指针，可直接将主机返回的指针传给核函数（在支持 UVA 的平台上）。这简化了零拷贝的使用。</p><p><strong>统一内存（Unified Memory，CUDA 6+）</strong>：书中提到，从 CUDA 6.0 开始，NVIDIA 提出了名为「<strong>统一寻址</strong>」的编程模型改进，<strong>连接了主机内存和设备内存空间</strong>，可使用<strong>单个指针</strong>访问 CPU 和 GPU 内存，无须彼此之间手动拷贝数据。更详细的实现方式包括 <strong>cudaMallocManaged</strong> 或 <strong><strong>managed</strong></strong> 变量：分配得到的内存由驱动和运行时统一管理，主机与设备均可通过同一指针访问，访问时若数据不在当前处理器则触发迁移。统一内存在语义上<strong>简化编程</strong>（无需显式 cudaMemcpy），适合快速原型与教学；<strong>与零拷贝的区别</strong>在于：零拷贝的物理位置始终在主机，而统一内存的物理位置可由系统迁移，以优化访问延迟和一致性。统一内存的详细行为与限制（如计算能力 6.x 的按需页面迁移）见 NVIDIA 文档；本章重点是建立「存在这样一种简化模型」的认识。书中在 4.5 节用<strong>矩阵加法</strong>对比了传统 <code>cudaMalloc</code>+<code>cudaMemcpy</code> 与使用统一内存的写法。</p><hr><h2 id="si-nei-cun-fang-wen-mo-shi-shu-4-3-jie-ben-zhang-zhong-nan-dian" tabindex="-1" id="四、内存访问模式（书-4-3-节）★-本章重难点">四、内存访问模式（书 4.3 节）★ 本章重难点</h2><p><strong>大多数设备端数据访问都是从全局内存开始的</strong>，且<strong>多数 GPU 应用程序容易受内存带宽的限制</strong>。因此，<strong>最大限度地利用全局内存带宽是调控核函数性能的基本</strong>。若不能正确调控全局内存的使用，其他优化方案很可能也收效甚微。</p><p>为了在读写数据时达到最佳性能，内存访问操作必须满足一定条件。CUDA 执行模型的一个显著特征是：<strong>指令必须以线程束为单位发布和执行，存储操作也是同样</strong>。在执行内存指令时，线程束中的每个线程都提供一个正在加载或存储的内存地址；根据<strong>线程束中内存地址的分布</strong>，内存访问可被分成不同的模式。</p><h3 id="4-1-nei-cun-shi-wu-yu-dui-qi-he-bing-de-gai-nian" tabindex="-1" id="4-1-内存事务与对齐、合并的概念">4.1 内存事务与对齐、合并的概念</h3><p><strong>内存事务</strong>：从核函数发起一次加载或存储请求，到硬件完成该请求所涉及的总线/缓存操作。事务有固定的<strong>粒度</strong>。这一节会说明：核函数的内存请求通常是在 DRAM 设备和片上内存之间以 <strong>128 字节</strong>或 <strong>32 字节</strong>内存事务来实现的。所有对全局内存的访问都会通过<strong>二级缓存</strong>；许多访问还会通过<strong>一级缓存</strong>，取决于访问类型和 GPU 架构。若两级缓存都用到，则内存访问由** 128 字节**的设备内存事务实现；若只使用二级缓存，则由 <strong>32 字节</strong>的事务实现。</p><p>在优化应用程序时，需要注意设备内存访问的两个特性：</p><ol><li><strong>对齐内存访问（Aligned）</strong></li><li><strong>合并内存访问（Coalesced）</strong></li></ol><p><strong>对齐</strong>：当一个内存事务的<strong>首个访问地址</strong>是缓存粒度（32 或 128 字节）的<strong>整数倍</strong>时，称为对齐内存访问；否则为非对齐。判定方式可归纳为：</p><ul><li>若使用 <strong>L1 缓存</strong>（128 字节段）：首地址为 <strong>128 的整数倍</strong>（如 0、128、256）时为对齐。</li><li>若仅用 <strong>L2</strong>（32 字节段）：首地址为 <strong>32 的整数倍</strong>（如 0、32、64、128）时为对齐。</li></ul><p>即：<strong>首地址 (\bmod 32 = 0)（或 (\bmod 128 = 0)）</strong> 为对齐条件。非对齐访问会带来带宽浪费（多取整段却只用一部分，或需多段才能覆盖请求）。</p><p><strong>合并</strong>：当<strong>线程束内的线程访问的内存都在一个内存块里</strong>时，就会出现合并访问。<strong>对齐且合并</strong>是理想状态：线程束内所有线程访问的数据在一个内存块内，且从内存块的首地址开始被需要，此时用<strong>最少的事务次数</strong>满足<strong>最多的内存请求</strong>，效率最高。</p><p><strong>带宽利用率</strong>可理解为：有用字节数 / 实际传输字节数。设线程束请求 32 个 float（128 字节），若 1 个 128 字节事务即可满足且首地址对齐，则利用率 100%；若需 2 个 128 字节事务（例如连续但非对齐），则利用率约 50%。书中用图示说明：若一个事务加载的数据分布在<strong>多个对齐段</strong>上（例如部分在 0～127，部分在 128～255），则需要多次事务，利用率下降；最坏情况下每个线程访问不同段，需要多达 32 次事务，利用率可低至 (128/(32\times 128)=1/32) 或更差（按 128 字节段、每线程 4 字节计）。</p><blockquote><p><strong>理解与体会</strong>：第 3 章「块最内层维度为 16 时效率下降」的本质在这里揭晓：16 个线程只占半个 warp，若按连续索引访问，半 warp 只需 64 字节，可能仍落在 1 个 128 字节段内，但若与相邻半 warp 合在一起看，整体访问模式可能跨段或未对齐，导致事务数增加、利用率下降。所以<strong>让块最内层维度为 32 的倍数</strong>，本质是让每个 warp 的访问尽量落在同一对齐段内，实现合并。</p></blockquote><h3 id="4-2-dui-qi-yu-he-bing-de-zhi-guan-li-zi-yu-si-chong-mo-shi-biao" tabindex="-1" id="4-2-对齐与合并的直观例子与四种模式表">4.2 对齐与合并的直观例子与四种模式表</h3><p>书中以<strong>一个线程束加载数据</strong>为例（使用一级缓存，即 128 字节段）说明不同访问模式对事务数和利用率的影响。下表将四种典型情况归纳（书中多处图示对应）：</p><table><thead><tr><th>访问模式</th><th>描述（32 线程 × 4 字节）</th><th>事务数（128B 段）</th><th>总线利用率（约）</th></tr></thead><tbody><tr><td>对齐 + 合并</td><td>连续 128 字节，首地址对齐 128</td><td>1</td><td>100%</td></tr><tr><td>对齐但不合并</td><td>32 个线程访问同一 4 字节</td><td>1</td><td>4/128≈3.125%</td></tr><tr><td>合并但不对齐</td><td>连续 128 字节，首地址错开（如 1）</td><td>2</td><td>约 50%</td></tr><tr><td>不对齐且不合并</td><td>32 个线程落在 N 个不同 128B 段</td><td>N（1～32）</td><td>约 128/(N×128)=1/N</td></tr></tbody></table><ul><li><strong>理想情况</strong>：32 个线程各请求 4 字节，共 128 字节，且这 128 字节落在<strong>一个 128 字节的对齐段</strong>内 → <strong>1 个事务</strong>，总线利用率 100%。</li><li><strong>连续但非对齐</strong>：32 个线程请求连续 128 字节，但首地址不对齐，例如横跨 0～127 和 128～255 两段 → 需要 <strong>2 个 128 字节事务</strong>，利用率约 50%。</li><li><strong>分散</strong>：若 32 个线程访问的地址分布在 N 个不同的 128 字节段内（1≤N≤32），则需要 N 个事务，利用率约为 1/N。</li><li><strong>同一地址</strong>：若线程束内所有线程请求同一 4 字节，仍只需 1 个 128 字节段，但有用数据仅 4 字节，利用率 4/128≈3.125%。</li></ul><p><strong>结论</strong>：用<strong>最少的事务次数</strong>满足<strong>最多的内存请求</strong>；事务数量和吞吐量需求随设备的计算能力变化。</p><h3 id="4-3-quan-ju-nei-cun-du-qu-lu-jing-l-1-l-2-yu-zhi-du-huan-cun" tabindex="-1" id="4-3-全局内存读取路径：L1-L2-与只读缓存">4.3 全局内存读取路径：L1/L2 与只读缓存</h3><p>这一节会说明：全局内存加载操作是否会通过一级缓存取决于**(1) 编译器选项<strong>和</strong>(2) 设备的计算能力**（较老设备可能没有 L1）。在 Fermi 和 Kepler K40 及以后的 GPU（计算能力 3.5+）中，可通过编译器标志启用或禁用全局内存加载的一级缓存。</p><ul><li><p><strong>禁用 L1</strong>（仅用 L2）：<br><code>-Xptxas -dlcm=cg</code><br>所有对全局内存的加载请求直接进入 L2；若 L2 缺失则由 DRAM 完成。此时<strong>事务粒度为 32 字节</strong>，对非对齐或非合并的分散访问有时能获得更好的总线利用率（因为每次只取 32 字节，无效字节更少）。</p></li><li><p><strong>启用 L1</strong>：<br><code>-Xptxas -dlcm=ca</code><br>全局内存加载请求先经 L1；L1 缺失则走 L2；再缺失则由 DRAM 完成。此种模式下，<strong>一个内存加载请求由一个 128 字节的设备内存事务实现</strong>。</p></li></ul><p>书中特别说明：在 Kepler K10、K20、K20X 上，一级缓存<strong>不用于</strong>缓存全局内存加载，而是专门用于缓存寄存器溢出到本地内存的数据。</p><p><strong>只读缓存</strong>（计算能力 3.5+）：最初预留给纹理内存；对计算能力 3.5 及以上的设备，只读缓存也支持<strong>使用全局内存加载</strong>代替 L1。只读缓存的<strong>加载粒度是 32 字节</strong>，对<strong>分散读取</strong>通常优于 L1。书中给出了两种使用方式，下面用<strong>完整核函数</strong>示例（书中风格）：</p><p><strong>方式一：使用 <code>__ldg</code> 内建函数</strong>，强制通过只读缓存加载：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">copyKernelLdg</span><span class="params">(<span class="type">float</span> *in, <span class="type">float</span> *out, <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (idx &lt; n)</span><br><span class="line">        out[idx] = __ldg(&amp;in[idx]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>方式二：使用 <code>const __restrict__</code> 修饰只读指针</strong>，让 nvcc 自动选择只读缓存（无别名、只读）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">copyKernelRestrict</span><span class="params">(<span class="type">int</span> * __restrict__ out, <span class="type">const</span> <span class="type">int</span>* __restrict__ in)</span> &#123;</span><br><span class="line">    <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    out[idx] = in[idx];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样编译器会识别 <code>in</code> 为只读且无别名，可自动通过只读缓存加载，无需改写成 <code>__ldg</code>。</p><h3 id="4-4-quan-ju-nei-cun-xie-ru" tabindex="-1" id="4-4-全局内存写入">4.4 全局内存写入</h3><p>这一节会说明：<strong>存储操作</strong>在 Fermi 或 Kepler 上<strong>不能使用一级缓存</strong>；在发送到设备内存之前，存储操作<strong>只通过二级缓存</strong>。存储操作以 <strong>32 字节段</strong>的粒度执行；内存事务可被分为 1 段、2 段或 4 段。例如，若两个地址同属于一个 128 字节区域但不属于同一对齐的 64 字节区域，可能执行一个 4 段事务（比两次 1 段事务更高效）。对齐的、连续 128 字节写入可用一个 4 段事务完成；分散在 192 字节范围内则可能需 3 个 1 段事务。因此<strong>写端也应力求对齐与合并</strong>；在不少内核中，<strong>合并写</strong>对性能更关键，因为写不经过 L1。</p><blockquote><p><strong>重难点</strong>：读可以走 L1（128B 事务）或 L2/只读缓存（32B 事务），但<strong>写只走 L2、以 32 字节段为单位</strong>。因此「写合并」在带宽敏感型内核里往往比「读合并」更关键，矩阵转置中优先保证写合并正是基于这一点。</p></blockquote><h3 id="4-5-ju-zhen-zhuan-zhi-zhong-de-du-xie-mo-shi-shu-zhong-an-li-yu-wan-zheng-he-han-shu" tabindex="-1" id="4-5-矩阵转置中的读-写模式（书中案例与完整核函数）">4.5 矩阵转置中的读/写模式（书中案例与完整核函数）</h3><p>矩阵在全局内存中通常按<strong>行优先</strong>线性存储：元素 <code>(row, col)</code> 的线性下标为 <code>row * nx + col</code>。<strong>矩阵转置</strong>中，每个线程读一个元素、写一个元素，读和写的下标互换，因此必然出现「读合并则写不合并」或「读不合并则写合并」的取舍。</p><p><strong>方案一：读按行（合并读）、写按列（非合并写）</strong><br>线程 <code>(ix, iy)</code> 读 <code>A[iy*nx+ix]</code>（同一行连续），写 <code>B[ix*ny+iy]</code>（同一列，在行优先下不连续）。书中风格的核函数如下（<code>nx×ny</code> 矩阵，A 为输入，B 为输出）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">copyRowReadColWrite</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">int</span> nx, <span class="type">int</span> ny)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> ix = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> iy = blockIdx.y * blockDim.y + threadIdx.y;</span><br><span class="line">    <span class="keyword">if</span> (ix &lt; nx &amp;&amp; iy &lt; ny) &#123;</span><br><span class="line">        B[ix * ny + iy] = A[iy * nx + ix];  <span class="comment">// 读 A 按行→合并；写 B 按列→非合并</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>方案二：读按列（非合并读）、写按行（合并写）</strong><br>线程 <code>(ix, iy)</code> 读 <code>A[iy*nx+ix]</code> 可理解为「按 A 的行读」即合并；若改为按 B 的列对应到 A，即读 <code>A[ix*ny+iy]</code>（按 A 的列），则为非合并读，写 <code>B[iy*nx+ix]</code> 为按行写即合并。下面写法为「读 A 的 (iy,ix)、写 B 的 (ix,iy)」，读的是 A 的列，写的是 B 的行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">copyColReadRowWrite</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">int</span> nx, <span class="type">int</span> ny)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> ix = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> iy = blockIdx.y * blockDim.y + threadIdx.y;</span><br><span class="line">    <span class="keyword">if</span> (ix &lt; nx &amp;&amp; iy &lt; ny) &#123;</span><br><span class="line">        B[iy * nx + ix] = A[ix * ny + iy];  <span class="comment">// 读 A 按列→非合并；写 B 按行→合并</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>书中结论：<strong>合并写</strong>往往更影响性能（写路径无 L1）；因此常优先保证<strong>写合并</strong>（即采用方案二），接受读端非合并；若使用只读缓存或 <code>__ldg</code> 读 A，可缓解非合并读的损失。第 5 章将用<strong>共享内存</strong>做中转：先合并读入块对应的 tile，在共享内存中转置，再合并写出，从而读写两端都接近合并。</p><blockquote><p><strong>重难点</strong>：矩阵转置是典型的「读合并则写不合并、写合并则读不合并」场景。<strong>写端更应保证合并</strong>，因为存储不经过 L1，写路径对带宽更敏感。实际优化时优先选「合并写」方案，读端用只读缓存或共享内存（第 5 章）弥补。</p></blockquote><h3 id="4-6-dai-kuan-li-yong-lu-xiao-jie" tabindex="-1" id="4-6-带宽利用率小结">4.6 带宽利用率小结</h3><ul><li><strong>对齐 + 合并</strong>：理想，单事务（或最少事务）、高利用率。</li><li><strong>对齐但不合并</strong>：例如 32 个线程访问同一 4 字节，利用率极低。</li><li><strong>合并但不对齐</strong>：连续 128 字节跨两段，需 2 笔 128 字节事务，利用率约 50%。</li><li><strong>既不对齐也不合并</strong>：可能需多达 32 笔事务，利用率最低。</li></ul><p><strong>优化目标</strong>：最少事务数 + 每笔事务中请求的字节尽量多。</p><hr><h2 id="wu-he-han-shu-neng-da-dao-de-dai-kuan-yu-xing-neng-fen-xi-shu-4-4-jie" tabindex="-1" id="五、核函数能达到的带宽与性能分析（书-4-4-节）">五、核函数能达到的带宽与性能分析（书 4.4 节）</h2><p>本节对应书中「核函数能达到多少带宽」及与理论峰值的对比。书中通过 <strong>nvprof</strong> 等工具测量内核的<strong>实际带宽</strong>（如 GB/s），并与<strong>理论峰值</strong>对比，以判断内核是<strong>计算受限</strong>还是<strong>带宽受限</strong>，以及离理论上限还有多少空间。这是判断优化方向（改访存还是改计算、改配置）的重要依据。</p><p><strong>实测带宽</strong>可按下式估算（书中思路）：</p><p>[<br>\text{有效带宽（GB/s）} = \frac{(\text{读字节数} + \text{写字节数}) \times 10^{-9}}{\text{核函数执行时间（秒）}}<br>]</p><p>例如，若某内核每个线程读 4 字节、写 4 字节，共 (N) 个线程，则总字节数 (= N \times 8)；用 nvprof 得到核函数时间 (T)（秒），则有效带宽 (\approx N \times 8 / (T \times 10^9)) GB/s。将该值与设备理论峰值带宽（如 Fermi 144 GB/s）比较，可得到<strong>带宽利用率</strong>，从而判断是否还有优化空间。</p><p>第 2 章已介绍「<strong>指令:字节</strong>」比（如 Tesla K10 约 13.6:1）：若每字节访问对应的指令数高于该比值，偏<strong>计算受限</strong>；否则偏<strong>带宽受限</strong>。大多数 HPC 工作负载是带宽受限的。</p><p><strong>提高带宽利用</strong>的两类思路（与第 3 章一致）：</p><ol><li><strong>足够的并发内存操作</strong>，以隐藏内存延迟（通过执行配置和每线程独立访存）。</li><li><strong>对齐及合并内存访问</strong>，减少带宽浪费。</li></ol><p>这一节会说明：可通过 <strong>(1) 对核函数启动的执行配置进行试验</strong> 以充分体现每个 SM 的并行性，<strong>(2) 增加每个线程中执行独立内存操作的数量</strong>（如展开技术）来优化。将实测带宽与理论峰值比较，可判断瓶颈在算法（计算）还是带宽（访存）；再结合 <strong>gld_throughput</strong>、<strong>gst_throughput</strong>、<strong>gld_efficiency</strong>、<strong>gst_efficiency</strong> 等 nvprof 指标做针对性改进。</p><hr><h2 id="liu-shi-yong-tong-yi-nei-cun-de-ju-zhen-jia-fa-shu-4-5-jie" tabindex="-1" id="六、使用统一内存的矩阵加法（书-4-5-节）">六、使用统一内存的矩阵加法（书 4.5 节）</h2><p>书中用<strong>矩阵加法</strong>对比了传统方式与<strong>统一内存</strong>的写法。传统方式：主机分配 h_A、h_B、h_C，设备分配 d_A、d_B、d_C，执行 cudaMemcpy H2D → 核函数 → cudaMemcpy D2H。使用统一内存时，可用 <strong>cudaMallocManaged</strong> 分配一块既可被主机访问又可被设备访问的内存，主机端初始化后直接将该指针传给核函数，核函数执行完毕后主机可直接读取结果，<strong>无需显式 cudaMemcpy</strong>。这样代码更简洁，适合快速原型与教学；首次访问时由系统迁移数据，可能有一次性的迁移开销。追求极限性能时，仍可能需显式分配与流重叠（第 6 章）。</p><hr><h2 id="qi-jie-gou-ti-shu-zu-ao-s-yu-shu-zu-jie-gou-ti-so-a-shu-4-6-jie" tabindex="-1" id="七、结构体数组（AoS）与数组结构体（SoA）（书-4-6-节）">七、结构体数组（AoS）与数组结构体（SoA）（书 4.6 节）</h2><p>书中以存储<strong>成对的浮点数据 (x, y)</strong> 为例，对比两种布局对合并与带宽的影响。</p><p><strong>AoS（Array of Structures）</strong>：每个逻辑点是一个结构体，x、y 在内存中相邻存放。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">InnerStruct</span> &#123;</span></span><br><span class="line">    <span class="type">float</span> x;</span><br><span class="line">    <span class="type">float</span> y;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">InnerStruct</span> <span class="title">myAoS</span>[<span class="title">N</span>];</span>   <span class="comment">// 内存布局: x0,y0, x1,y1, x2,y2, ...</span></span><br></pre></td></tr></table></figure><p>若核函数<strong>只使用 x</strong>（例如对 x 做平方），典型写法如下。此时 32 个线程访问 <code>myAoS[i].x</code>，在内存上间隔 8 字节（每个结构体 8 字节），<strong>不是连续 4 字节</strong>，无法合并成少量事务；且每次加载会带出不需要的 y。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">useX_AoS</span><span class="params">(<span class="keyword">struct</span> InnerStruct *data, <span class="type">float</span> *out)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    out[i] = data[i].x * data[i].x;  <span class="comment">// 只用到 x，但会加载 x 和 y</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这一节会说明：在 GPU 上以 AoS 存储并执行只使用 x 的应用程序，将导致<strong>约 50% 的带宽损失</strong>（因为每 32 字节或 128 字节段中有一半是未使用的 y），且会在不需要的 y 上浪费二级缓存空间。</p><p><strong>SoA（Structure of Arrays）</strong>：同一字段的所有值放在一个数组里，逻辑上同一「点」的 x、y 分布在不同数组。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">InnerArray</span> &#123;</span></span><br><span class="line">    <span class="type">float</span> x[N];</span><br><span class="line">    <span class="type">float</span> y[N];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">InnerArray</span> <span class="title">mySoA</span>;</span>   <span class="comment">// x[0..N-1] 连续，y[0..N-1] 连续</span></span><br></pre></td></tr></table></figure><p>若 32 个线程访问 <code>mySoA.x[i]</code>，地址连续，<strong>易合并</strong>，且不加载 y：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">useX_SoA</span><span class="params">(<span class="type">float</span> *x, <span class="type">float</span> *out)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    out[i] = x[i] * x[i];  <span class="comment">// 连续访问 x[]，合并</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>书中结论：<strong>并行编程范式（尤其是 SIMD/SIMT）对 SoA 更友好</strong>；CUDA 中普遍倾向于 SoA，因为这种内存访问可以有效地合并。</p><blockquote><p><strong>理解与体会</strong>：AoS 更符合「面向对象」的直觉（一个点就是一个 struct），但在 GPU 上若只访问部分字段，会白拉一整条 cache line、浪费带宽。SoA 是「数据导向」的布局，更贴合 SIMT 的合并访问需求，是性能敏感代码中的常见选择。</p></blockquote><hr><h2 id="ba-ben-zhang-xiao-jie-yu-zhong-nan-dian-hui-gu" tabindex="-1" id="八、本章小结与重难点回顾">八、本章小结与重难点回顾</h2><h3 id="8-1-zhi-shi-xiao-jie-yu-shu-zhong-dui-ying" tabindex="-1" id="8-1-知识小结（与书中对应）">8.1 知识小结（与书中对应）</h3><ul><li><strong>CUDA 内存模型</strong>：寄存器、本地内存、共享内存、全局内存、常量内存、纹理内存各有作用域与生命周期；L1/L2 与只读缓存不可编程但影响访问粒度和路径；全局内存与共享内存是层次中的两个核心，本章重点在全局内存。</li><li><strong>内存管理</strong>：<code>cudaMalloc</code>/<code>cudaFree</code>、<code>cudaMemcpy</code> 是基础；<strong>固定内存</strong>（cudaMallocHost）提升 H2D/D2H 带宽并利于与计算重叠；<strong>零拷贝</strong>（cudaHostAlloc Mapped + cudaHostGetDevicePointer）省显式拷贝但经 PCIe 访问，适合特定场景；<strong>统一虚拟寻址</strong>与<strong>统一内存</strong>（cudaMallocManaged）简化编程。</li><li><strong>访问模式</strong>：<strong>对齐</strong>（首地址为 32/128 字节整数倍）和<strong>合并</strong>（线程束内访问落在尽可能少的内存段内）是提升全局内存带宽利用的关键；事务越少、每笔有用字节越多，有效带宽越高。</li><li><strong>读取路径</strong>：L1+L2（128B 事务）、仅 L2（32B 事务）、只读缓存（<code>__ldg</code>/<code>const __restrict__</code>，32B）；写仅经 L2，以 32 字节段为单位，<strong>写合并常更关键</strong>。</li><li><strong>矩阵转置</strong>：典型「合并读+非合并写」或「非合并读+合并写」；第 5 章用共享内存可实现读写双端合并。</li><li><strong>AoS vs SoA</strong>：并行内核中 SoA 更易实现合并访问，只访问部分字段时 AoS 约 50% 带宽浪费。</li></ul><h3 id="8-2-zhong-nan-dian-su-cha" tabindex="-1" id="8-2-重难点速查">8.2 重难点速查</h3><table><thead><tr><th>重难点</th><th>要点</th></tr></thead><tbody><tr><td><strong>对齐与合并</strong></td><td>对齐看首地址是否为 32/128 的整数倍；合并看线程束内地址是否落在尽量少的内存段内；二者共同决定事务数与带宽利用率。</td></tr><tr><td><strong>128B vs 32B 事务</strong></td><td>经 L1 的加载以 128B 为单位；仅 L2 或只读缓存以 32B 为单位；写只经 L2，32B 段。</td></tr><tr><td><strong>为何写合并更关键</strong></td><td>存储不经过 L1，写路径对带宽更敏感；矩阵转置等场景优先保证写合并，读端可用只读缓存或共享内存弥补。</td></tr><tr><td><strong>固定 vs 零拷贝 vs 统一内存</strong></td><td>固定：主机锁页，DMA 快、利于重叠；零拷贝：设备直接访问主机内存，每次经 PCIe；统一内存：单指针、自动迁移，编程简单，极限性能仍可能用显式拷贝+流。</td></tr><tr><td><strong>AoS 的 50% 浪费</strong></td><td>只访问一个字段时，相邻元素在 AoS 中间隔一个字段，无法连续合并，且会加载无用字段。SoA 下同字段连续，易合并。</td></tr></tbody></table><h3 id="8-3-xue-xi-si-kao" tabindex="-1" id="8-3-学习思考">8.3 学习思考</h3><ul><li><strong>第 3 章与本章的衔接</strong>：第 3 章「块最内层维度为 16 时效率下降」的根因，是全局内存访问未按 warp 对齐、合并；本章给出了具体判定方法（对齐、合并、事务数）和优化方向（对齐+合并、只读缓存、写优先）。</li><li><strong>与第 5 章的关系</strong>：当算法天然导致「读合并写不合并」或反过来时，单靠全局内存只能二选一；第 5 章用<strong>共享内存做中转</strong>，先合并读入 tile、在块内重组、再合并写出，可实现读写双端高效，是矩阵转置、归约等模式的标准做法。</li><li><strong>实践建议</strong>：写新内核时先保证<strong>索引连续、首地址对齐</strong>（如 <code>cudaMalloc</code> 自然对齐）；用 nvprof 看 <code>gld_efficiency</code>/<code>gst_efficiency</code> 判断是否合并；只读数据可尝试 <code>__ldg</code> 或 <code>const __restrict__</code>；结构体数据优先考虑 SoA 或混合布局。</li></ul><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 5 章：共享内存和常量内存</strong>：</p><ul><li>共享内存的<strong>布局、bank 与 bank 冲突</strong>，以及用 <strong>padding</strong> 消除冲突</li><li>用共享内存做<strong>矩阵转置</strong>的中转，实现读写双端合并</li><li><strong>并行归约</strong>中共享内存的使用与 warp 内优化</li><li><strong>常量内存</strong>与只读缓存的使用场景</li><li><strong>线程束洗牌指令</strong>（warp shuffle）</li></ul><p>从「全局内存怎么访问」到「把数据搬到片上、按需复用」，是突破带宽瓶颈的下一步。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>什么是「合并访问」？为何它对全局内存带宽至关重要？</strong></li><li><strong>矩阵转置时，若只使用全局内存，为何无法同时做到读合并与写合并？</strong></li><li><strong>有效带宽公式是什么？如何用它判断内核是带宽受限还是计算受限？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>合并访问指同一 warp 内线程访问的全局内存地址落在尽量少的连续段（如 128 字节）内，可合并成少量事务，提高带宽利用率；否则会触发多次事务，带宽浪费。</li><li>按行读则地址连续（读合并），但按行写转置结果时写的是不连续列（写不合并）；反之按列读则读不合并、按列写则写合并。单用全局内存只能二选一。</li><li>有效带宽 = (读字节数 + 写字节数) / 核函数执行时间。与理论峰值对比：若接近理论带宽则带宽受限，远低于则可能计算受限或存在非合并访问等问题。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/bf3e0449/">第3章 CUDA执行模型</a> | 下一篇：<a href="https://smarter.xin/posts/e1d6d733/">第5章 共享内存和常量内存</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 4 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-si-zhang-quan-ju-nei-cun&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="全局内存" scheme="https://smarter.xin/tags/global-memory/"/>
    
    <category term="内存对齐" scheme="https://smarter.xin/tags/memory-alignment/"/>
    
    <category term="合并访问" scheme="https://smarter.xin/tags/coalesced-access/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第三章：CUDA执行模型</title>
    <link href="https://smarter.xin/posts/bf3e0449/"/>
    <id>https://smarter.xin/posts/bf3e0449/</id>
    <published>2026-02-05T04:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-san-zhang-cuda-zhi-xing-mo-xing" tabindex="-1">CUDA C编程权威指南-第三章：CUDA执行模型</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/bdd22b42/">第2章 CUDA编程模型</a> | 下一篇：<a href="https://smarter.xin/posts/1f074790/">第4章 全局内存</a></p><blockquote><p>系列第 3 篇。第 2 章里我们学会了组织网格与块、给核函数计时，也看到<strong>同一算法换一种执行配置性能差很多</strong>（比如矩阵加法里 32×16 的块比 16×16 快）。这一章从<strong>硬件视角</strong>解释原因：线程束怎么调度、分支分化怎么回事、占用率和延迟隐藏怎么指导配置，以及动态并行、循环展开等进阶内容。</p></blockquote><hr><h2 id="qian-yan-cong-zen-yao-xie-dao-zen-yao-pao" tabindex="-1" id="前言：从「怎么写」到「怎么跑」">前言：从「怎么写」到「怎么跑」</h2><p><strong>执行模型</strong>描述的是在特定计算架构上<strong>指令如何被执行</strong>。第 2 章的编程模型给出了「网格—块—线程」的逻辑视图与写法；本章则从<strong>硬件视角</strong>揭示：GPU 如何把块分配到 SM、如何把线程组织成线程束（warp）调度、为何有的配置更快、如何用占用率和延迟隐藏指导优化。从「怎么写」到「为什么这样写更好」，是迈向高性能 CUDA 的必经一步。</p><p>本章围绕<strong>指令吞吐量与计算资源</strong>展开；第 4、5 章将在此基础上深入<strong>内存访问与带宽</strong>。</p><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-zhi-xing-mo-xing" tabindex="-1" id="1-1-为什么要学「执行模型」">1.1 为什么要学「执行模型」</h3><p>第 2 章回答了「怎么写」：如何组织网格与块、如何写核函数、如何计时。但你一定会问：<strong>为什么 32×16 的块比 16×16 更快？有没有选择网格和块配置的准则？</strong> 这一章会说明：尽管可以通过反复试验找到最佳配置，但若不了解<strong>硬件如何调度与执行线程</strong>，就很难系统性地优化。</p><p><strong>执行模型</strong>描述的是在特定计算架构上<strong>指令如何被执行</strong>。它从硬件视角揭示：线程束（warp）是什么、SM 如何调度、为何会有分支分化、占用率与延迟隐藏如何影响性能。学完本章，你就能从「试参数」进阶到「看指标、懂原理、做权衡」。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解 <strong>SM、线程束、SIMT</strong> 与「32」的含义</td><td>能解释为什么块最内层维度应为 32 的倍数</td></tr><tr><td>理解<strong>线程束分化</strong>的成因与代价</td><td>能写出避免分化的数据分区方式（如按 warp 分组）</td></tr><tr><td>理解<strong>占用率</strong>的定义及与寄存器/共享内存的关系</td><td>能用 -Xptxas -v 和占用率计算器分析内核</td></tr><tr><td>理解<strong>延迟隐藏</strong>（算术 vs 内存延迟）及所需并行量</td><td>能复述 Fermi 上隐藏内存延迟约需 36 个线程束/SM 的推导思路</td></tr><tr><td>掌握<strong>网格和块的启发式</strong>（块大小、最内层维度、块数）</td><td>能解释为何高占用率不一定带来最高性能</td></tr><tr><td>会用 <strong>nvprof</strong> 看 achieved_occupancy、gld_efficiency 等指标</td><td>能结合矩阵求和实验说明「指标平衡」</td></tr><tr><td>理解<strong>并行归约</strong>中分化来源及改进（相邻/交错、索引重组）</td><td>能写出 reduceNeighboredLess 式的条件避免同一 warp 内分化</td></tr><tr><td>了解<strong>循环展开</strong>与<strong>动态并行</strong>的作用与适用场景</td><td>能说出展开对延迟隐藏的意义、动态并行对递归的意义</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><p><strong>阅读建议</strong>：本章较长，二、三、四、五节建议<strong>精读</strong>（执行模型与归约是后续内存与优化基础）；六节中「动态并行」为<strong>选读</strong>，首次学习可跳过，待掌握流与多 GPU 后再回看。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">第 3 章  CUDA 执行模型</span><br><span class="line">│</span><br><span class="line">├── 二、CUDA 执行模型概述（书 3.1 节）【精读】</span><br><span class="line">│   ├── GPU 架构概述（SM、线程束、SIMT、32、资源与同步）</span><br><span class="line">│   ├── Fermi / Kepler 架构要点</span><br><span class="line">│   └── 配置文件驱动优化（nvvp、nvprof、事件与指标）</span><br><span class="line">│</span><br><span class="line">├── 三、理解线程束执行的本质（书 3.2 节）【精读】</span><br><span class="line">│   ├── 线程束与线程块、硬件视图下一维化</span><br><span class="line">│   ├── 线程束分化、断定、避免分化的数据分区</span><br><span class="line">│   ├── 资源分配、活跃线程束分类</span><br><span class="line">│   ├── 延迟隐藏（算术/内存延迟、Fermi 公式）</span><br><span class="line">│   ├── 占用率与网格/块准则</span><br><span class="line">│   ├── 同步栅栏</span><br><span class="line">│   └── 可扩展性</span><br><span class="line">│</span><br><span class="line">├── 四、并行性的表现（书 3.3 节）【精读】</span><br><span class="line">│   └── 矩阵求和与 nvprof、网格与块启发式</span><br><span class="line">│</span><br><span class="line">├── 五、避免分支分化（书 3.4 节）【精读】</span><br><span class="line">│   └── 并行归约、分化与改进（相邻/交错配对）</span><br><span class="line">│</span><br><span class="line">├── 六、展开循环与动态并行（书 3.5、3.6 节）【精读：展开 | 选读：动态并行】</span><br><span class="line">│</span><br><span class="line">└── 七、总结与重难点小结（书 3.7 节）</span><br></pre></td></tr></table></figure><hr><h2 id="er-cuda-zhi-xing-mo-xing-gai-shu-shu-3-1-jie" tabindex="-1" id="二、CUDA-执行模型概述（书-3-1-节）">二、CUDA 执行模型概述（书 3.1 节）</h2><h3 id="2-1-gpu-jia-gou-gai-shu-shu-3-1-1" tabindex="-1" id="2-1-GPU-架构概述（书-3-1-1）">2.1 GPU 架构概述（书 3.1.1）</h3><p>一般来说，执行模型会提供一个操作视图，说明如何在特定的计算架构上执行指令。<strong>CUDA 执行模型</strong>揭示了 GPU 并行架构的抽象视图，使我们能够据此分析线程的并发。第2章已经介绍了 CUDA 编程模型中两个主要的抽象概念：<strong>内存层次结构</strong>和<strong>线程层次结构</strong>。本章将重点介绍<strong>指令吞吐量</strong>，第4章和第5章将介绍更多关于高效内存访问的内容。</p><p>GPU 架构是围绕**流式多处理器（Streaming Multiprocessor, SM）**的可扩展阵列搭建的。可以通过复制这种架构的构建块来实现 GPU 的硬件并行。</p><p><strong>Fermi SM</strong> 的关键组件（原书图 3-1）包括：</p><ul><li><strong>CUDA 核心</strong>（整数 ALU + 浮点 FPU）</li><li><strong>共享内存 / 一级缓存</strong></li><li><strong>寄存器文件</strong></li><li><strong>加载/存储单元</strong>（LD/ST）</li><li><strong>特殊功能单元</strong>（SFU，用于正弦、余弦、平方根、插值等）</li><li><strong>线程束调度器</strong></li></ul><p>下面用框图概括 SM 内各组件的逻辑关系（建议配合原书图 3-1 理解）：</p><pre><code class="highlight mermaid">flowchart TB    subgraph SM [流式多处理器 SM]        WarpSched[线程束调度器]        Cores[CUDA 核心]        RegFile[寄存器文件]        SharedL1[共享内存 / L1 缓存]        LDST[加载存储单元]        SFU[特殊功能单元 SFU]    end    WarpSched --&gt; Cores    WarpSched --&gt; LDST    WarpSched --&gt; SFU    Cores --&gt; RegFile    LDST --&gt; SharedL1</code></pre><p>GPU 中的每一个 SM 都能支持数百个线程并发执行，每个 GPU 通常有多个 SM，所以在一个 GPU 上并发执行数千个线程是有可能的。当启动一个内核网格时，<strong>线程块被分布在可用的 SM 上</strong>执行。线程块一旦被调度到一个 SM 上，其中的线程<strong>只会在那个指定的 SM 上</strong>并发执行。多个线程块可能会被分配到同一个 SM 上，而且是根据 SM 资源的可用性进行调度的。</p><p>同一线程中的指令利用<strong>指令级并行</strong>进行流水线化；此外，CUDA 还提供了<strong>线程级并行</strong>。CUDA 采用<strong>单指令多线程（SIMT）<strong>架构来管理和执行线程：<strong>每 32 个线程为一组，被称为线程束（warp）</strong>。线程束中的所有线程</strong>同时执行相同的指令</strong>。每个线程都有自己的指令地址计数器和寄存器状态，利用自身的数据执行当前的指令。每个 SM 都将分配给它的线程块划分到包含 32 个线程的线程束中，然后在可用的硬件资源上调度执行。</p><h4 id="simt-yu-simd-de-qu-bie" tabindex="-1" id="SIMT-与-SIMD-的区别">SIMT 与 SIMD 的区别</h4><p>SIMT 架构与 SIMD（单指令多数据）架构相似——两者都是将相同的指令广播给多个执行单元来实现并行。一个关键的区别是：<strong>SIMD 要求同一个向量中的所有元素要在一个统一的同步组中一起执行</strong>，而 <strong>SIMT 允许属于同一线程束的多个线程独立执行</strong>。尽管一个线程束中的所有线程在相同的程序地址上同时开始执行，但是单独的线程仍有可能有不同的行为。</p><p>SIMT 模型包含三个 SIMD 所不具备的关键特征（需要掌握的三点）：</p><ol><li><strong>每个线程可以有一个独立的执行路径</strong></li><li><strong>每个线程都有自己的寄存器状态</strong></li><li><strong>每个线程都有自己的指令地址计数器</strong></li></ol><p>因此 SIMT 确保可以编写独立的线程级并行代码、标量线程以及用于协调线程的数据并行代码。</p><h4 id="shen-qi-de-shu-zi-32" tabindex="-1" id="神奇的数字：32">神奇的数字：32</h4><p>尤其要注意的是：<strong>在 CUDA 程序里，32 是一个神奇的数字</strong>。它来自于硬件系统，也对软件的性能有着重要的影响。从概念上讲，它是 SM 用 SIMD 方式所同时处理的工作粒度。<strong>优化工作负载以适应线程束（一组 32 个线程）的边界，一般这样会更有效地利用 GPU 计算资源</strong>。在后面的章节中将会多次看到「块的最内层维度应为线程束大小的倍数」这一准则。</p><h4 id="zi-yuan-yu-tong-bu" tabindex="-1" id="资源与同步">资源与同步</h4><p>一个线程块<strong>只能在一个 SM 上</strong>被调度。一旦线程块在一个 SM 上被调度，就会保存在该 SM 上直到执行完成。在同一时间，一个 SM 可以容纳多个线程块。</p><p>在 SM 中，<strong>共享内存</strong>和<strong>寄存器</strong>是非常重要的资源。共享内存被分配在 SM 上的常驻线程块中，寄存器在线程中被分配。线程块中的线程通过这些资源可以进行相互的合作和通信。</p><p>尽管线程块里的所有线程都可以逻辑地并行运行，但是<strong>并不是所有线程都可以同时在物理层面执行</strong>。因此，线程块里的不同线程可能会以不同的速度前进。在并行线程中共享数据可能会引起<strong>竞争</strong>：多个线程使用未定义的顺序访问同一个数据，从而导致不可预测的程序行为。CUDA 提供了一种用来<strong>同步线程块里的线程</strong>的方法（<code>__syncthreads()</code>），从而保证所有线程在进一步动作之前都达到执行过程中的一个特定点。然而，<strong>没有提供块间同步的原语</strong>。</p><p>尽管线程块里的线程束可以任意顺序调度，但活跃的线程束的数量还是会由 SM 的资源所限制。当线程束由于任何理由闲置的时候（如等待从设备内存中读取数值），SM 可以从同一 SM 上的常驻线程块中调度其他可用的线程束。<strong>在并发的线程束间切换并没有开销</strong>，因为硬件资源已经被分配到了 SM 上的所有线程和块中，所以最新被调度的线程束的状态已经存储在 SM 上。</p><blockquote><p><strong>理解与体会</strong>：可以把 SM 想象成「线程束的调度中心」——块一旦被分配到某个 SM，就一直在该 SM 上执行；块内的线程被拆成若干个 warp，调度器轮流执行「就绪」的 warp。正因为切换无开销，才需要足够多的活跃 warp 来隐藏内存延迟，这也是后面「占用率」和「延迟隐藏」的出发点。</p></blockquote><hr><h3 id="2-2-fermi-jia-gou-shu-3-1-2" tabindex="-1" id="2-2-Fermi-架构（书-3-1-2）">2.2 Fermi 架构（书 3.1.2）</h3><p>Fermi 架构是第一个完整的 GPU 计算架构，能够为大多数高性能计算应用提供所需要的功能，已被广泛应用于加速生产工作负载。</p><p>Fermi 架构的逻辑框图（原书图 3-3）（重点为 GPU 计算，忽略图形具体组成部分）。Fermi 的特征包括：</p><ul><li><strong>最多 512 个 CUDA 核心</strong>（加速器核心）。每个 CUDA 核心都有一个全流水线的整数算术逻辑单元（ALU）和一个浮点运算单元（FPU），每个时钟周期执行一个整数或浮点数指令。</li><li>CUDA 核心被组织到 <strong>16 个 SM</strong> 中，每一个 SM 含有 <strong>32 个 CUDA 核心</strong>。</li><li><strong>6 个 384 位的 GDDR5 DRAM 存储器接口</strong>，支持多达 6GB 的全局机载内存。</li><li><strong>GigaThread 引擎</strong>：全局调度器，用来分配线程块到 SM 的线程束调度器上。</li><li><strong>768 KB 的二级缓存</strong>，被 16 个 SM 所共享。</li></ul><p>每个多处理器有（原书图 3-1）：</p><ul><li><strong>16 个加载/存储单元</strong>：允许每个时钟周期内有 16 个线程（线程束的一半）计算源地址和目的地址。</li><li><strong>特殊功能单元（SFU）</strong>：执行固有指令，如正弦、余弦、平方根和插值。每个 SFU 在每个时钟周期内的每个线程上执行一个固有指令。</li><li><strong>2 个线程束调度器</strong>和 <strong>2 个指令调度单元</strong>。当一个线程块被指定给一个 SM 时，线程块中的所有线程被分成线程束。两个线程束调度器选择两个线程束，再把一个指令从线程束中发送到一个组上——组里有 16 个 CUDA 核心、16 个加载/存储单元或 4 个特殊功能单元（原书图 3-4）。</li></ul><p><strong>Fermi 架构（计算能力 2.x）可以在每个 SM 上同时处理 48 个线程束</strong>，即可在一个 SM 上同时常驻 <strong>1536 个线程</strong>。</p><p>Fermi 架构的一个关键特征是有一个 <strong>64 KB 的片内可配置存储器</strong>，它在<strong>共享内存与一级缓存</strong>之间进行分配。对于许多高性能的应用程序，共享内存是影响性能的一个关键因素。CUDA 提供了运行时 API 来调整共享内存和一级缓存的数量；根据给定的内核中共享内存或缓存的使用修改片内存储器的配置，可以提高性能（第 4、5 章详细介绍）。</p><p>Fermi 架构也支持<strong>并发内核执行</strong>：在相同的 GPU 上、相同应用程序的上下文中，同时启动多个内核。Fermi 允许多达 <strong>16 个内核</strong>同时在设备上运行。从程序员的角度看，并发内核执行使 GPU 表现得更像 MIMD 架构。</p><hr><h3 id="2-3-kepler-jia-gou-shu-3-1-3" tabindex="-1" id="2-3-Kepler-架构（书-3-1-3）">2.3 Kepler 架构（书 3.1.3）</h3><p>发布于 2012 年秋季的 <strong>Kepler</strong> GPU 架构是一种快速、高效、高性能的计算架构。书中以 Kepler K20X 为例，其包含 <strong>15 个 SM</strong> 和 6 个 64 位的内存控制器。Kepler 的三大重要创新为：</p><ol><li><strong>强化的 SM</strong></li><li><strong>动态并行</strong></li><li><strong>Hyper-Q 技术</strong></li></ol><p>每个 Kepler SM 单元包含（原书图 3-7）：</p><ul><li><strong>192 个单精度 CUDA 核心</strong>、<strong>64 个双精度单元</strong></li><li><strong>32 个特殊功能单元（SFU）</strong>、<strong>32 个加载/存储单元（LD/ST）</strong></li></ul><p>每个 Kepler SM 包括 <strong>4 个线程束调度器</strong>和 <strong>8 个指令调度器</strong>，以确保在单一的 SM 上同时发送和执行 <strong>4 个线程束</strong>。<strong>Kepler K20X 架构（计算能力 3.5）可以同时在每个 SM 上调度 64 个线程束</strong>，即在一个 SM 上可同时常驻 <strong>2048 个线程</strong>。K20X 架构中寄存器文件容量达到 <strong>64 KB</strong>（Fermi 为 32 KB），同时 K20X 还允许片内存储器在共享内存和一级缓存间有更多的分区。K20X 能够提供超过 <strong>1 TFlop</strong> 的峰值双精度计算能力，相较于 Fermi 的设计，功率效率提高了 80%，每瓦性能也提升了三倍。</p><ul><li><strong>动态并行</strong>：允许 GPU 动态启动新的网格。任一内核都能启动其他的内核，并管理核间需要的依赖关系。这一特点也让你更容易创建和优化递归及与数据相关的执行模式。原书图 3-8 对比了「无动态并行时主机在 GPU 上启动每一个内核」与「有动态并行时 GPU 能够启动嵌套内核，消除与 CPU 通信的需求」。</li><li><strong>Hyper-Q 技术</strong>：增加了更多的 CPU 和 GPU 之间的同步硬件连接，确保 CPU 核心能够在 GPU 上同时运行更多的任务。Fermi 依赖一个单一的硬件工作队列，可能导致一个单独的任务阻塞队列中该任务之后的所有其他任务；Kepler Hyper-Q 消除了这个限制，在主机与 GPU 之间提供了 <strong>32 个硬件工作队列</strong>（原书图 3-9）。</li></ul><p>书中<strong>表 3-1</strong> 简要总结了不同计算能力下的主要架构特点。下表根据书中内容整理，便于查阅与配置内核：</p><table><thead><tr><th>计算能力</th><th>架构类型</th><th>每 SM 最大线程数</th><th>每 SM 最大线程束数</th><th>每块最大线程数</th><th>每 SM 寄存器(32位)</th><th>每块共享内存</th></tr></thead><tbody><tr><td>2.0</td><td>Fermi</td><td>1536</td><td>48</td><td>1024</td><td>32 K</td><td>48 KB</td></tr><tr><td>2.1</td><td>Fermi</td><td>1536</td><td>48</td><td>1024</td><td>32 K</td><td>48 KB</td></tr><tr><td>3.0</td><td>Kepler</td><td>2048</td><td>64</td><td>1024</td><td>64 K</td><td>48 KB</td></tr><tr><td>3.5</td><td>Kepler</td><td>2048</td><td>64</td><td>1024</td><td>64 K</td><td>48 KB</td></tr></tbody></table><blockquote><p>实际限制以 <code>cudaGetDeviceProperties</code> 为准；不同型号可能有细微差异。</p></blockquote><hr><h3 id="2-4-pei-zhi-wen-jian-qu-dong-you-hua-shu-3-1-4" tabindex="-1" id="2-4-配置文件驱动优化（书-3-1-4）">2.4 配置文件驱动优化（书 3.1.4）</h3><p>性能分析是通过检测来分析程序性能的行为，包括：</p><ul><li>函数调用的频率和持续时间</li><li>特殊指令的使用</li><li>应用程序代码的空间（内存）或时间复杂度</li></ul><p>性能分析是程序开发中的关键一步，特别是对于优化 HPC 应用程序代码。<strong>性能分析往往需要对平台的执行模型有一个基本的理解</strong>，才能制定应用程序的优化方法。这里提到：开发一个 HPC 应用程序通常包括两个主要步骤——<strong>提高代码的正确性</strong>和<strong>提高代码的性能</strong>。对于第二步，使用<strong>配置文件驱动的方法</strong>是很自然的。</p><p>配置文件驱动的发展对于 CUDA 编程尤为重要，原因主要有：</p><ol><li>CUDA 提供了一个硬件架构的抽象，它能够让用户控制线程并发；性能分析工具可以检测和优化，并将优化可视化。</li><li>CUDA 将 SM 中的计算资源在该 SM 中的多个常驻线程块之间进行分配，这种分配形式导致一些资源成为了性能限制者；性能分析工具能帮助我们理解计算资源是如何被利用的。</li><li>一个单纯的内核应用一般不会产生最佳的性能；性能分析工具能帮助你找到代码中影响性能的关键部分，也就是<strong>性能瓶颈</strong>。</li></ol><p><strong>CUDA 提供的两个主要性能分析工具</strong>：</p><ul><li><strong>nvvp</strong>：独立的可视化分析器，可显示 CPU 与 GPU 上的程序活动时间表，并分析潜在的性能瓶颈并给出建议。既可独立使用，也可作为 Nsight Eclipse Edition 的一部分。</li><li><strong>nvprof</strong>：命令行分析器（随 CUDA 5 一起发布）。可以获得 CPU 与 GPU 上 CUDA 关联活动的时间表（内核执行、内存传输、CUDA API 调用），也可以获得硬件计数器和 CUDA 内核的性能指标。除了预定义的指标，还可以利用基于分析器获得的硬件计数器来自定义指标。</li></ul><p><strong>事件与指标</strong>（书中概念）：</p><ul><li><strong>事件（Event）</strong>：可计算的活动，对应一个在内核执行期间被收集的硬件计数器。</li><li><strong>指标（Metric）</strong>：内核的特征，由一个或多个事件计算得到。</li></ul><p>注意：大多数计数器是按流式多处理器报告的，而不是整个 GPU；一次运行只能获得几个计数器；有些计数器的获得是相互排斥的，因此<strong>多次性能分析运行往往需要获取所有相关的计数器</strong>。由于 GPU 执行中的变化（如线程块和线程束调度），经重复运行，计数器值可能不是完全相同的。</p><p>常见的有 <strong>3 种常见的限制内核性能的因素</strong>：</p><ol><li><strong>指令和内存延迟</strong></li><li><strong>计算资源</strong>（寄存器、共享内存等）</li><li><strong>存储带宽</strong></li></ol><p>本章主要介绍 1 和 2；后续章节将讨论其余因素。<strong>了解硬件资源的详细信息</strong>：作为 CUDA C 程序员，如果想改善内核的性能，必须对硬件资源有一定的了解；即使仅掌握最基本的 GPU 体系架构知识，也能够编写出更好的代码，并充分开发设备的性能。</p><hr><h2 id="san-li-jie-xian-cheng-shu-zhi-xing-de-ben-zhi-shu-3-2-jie" tabindex="-1" id="三、理解线程束执行的本质（书-3-2-节）">三、理解线程束执行的本质（书 3.2 节）</h2><h3 id="3-1-xian-cheng-shu-he-xian-cheng-kuai-shu-3-2-1" tabindex="-1" id="3-1-线程束和线程块（书-3-2-1）">3.1 线程束和线程块（书 3.2.1）</h3><p>启动内核时，从软件的角度看，似乎所有的线程都是并行运行的——在逻辑上这是正确的。但从<strong>硬件的角度</strong>来看，不是所有线程在物理上都可以同时并行地执行。<strong>线程束是 SM 中基本的执行单元</strong>。</p><p>当一个线程块的网格被启动后，网格中的线程块分布在 SM 中。一旦线程块被调度到一个 SM 上，线程块中的线程会被进一步划分为<strong>线程束</strong>。<strong>一个线程束由 32 个连续的线程组成</strong>；在一个线程束中，所有的线程按照 SIMT 方式执行——即所有线程执行相同的指令，每个线程在私有数据上进行操作。原书图 3-10 展示了线程块的逻辑视图和硬件视图之间的关系。</p><p><strong>重要</strong>：从硬件的角度来看，所有的线程都被组织成了<strong>一维</strong>的。线程块可以被配置为一维、二维或三维，但在一个块中，每个线程都有一个唯一的 ID；对于一维的线程块，唯一的线程 ID 存储在 <code>threadIdx.x</code> 中，并且 <strong>拥有连续 <code>threadIdx.x</code> 值的线程被分组到线程束中</strong>。</p><p>例如，一个有 <strong>128 个线程</strong>的一维线程块被组织成 <strong>4 个线程束</strong>：</p><ul><li>Warp 0：threadIdx.x = 0～31</li><li>Warp 1：threadIdx.x = 32～63</li><li>Warp 2：threadIdx.x = 64～95</li><li>Warp 3：threadIdx.x = 96～127</li></ul><p>因此，<strong>块的最内层维度</strong>（如一维块的 <code>blockDim.x</code>）若不是 32 的倍数，会导致部分线程束「未填满」，影响合并访问与占用率。</p><blockquote><p><strong>重点</strong>：硬件视角下线程在块内是<strong>一维</strong>排列的，连续 32 个 threadIdx（如 threadIdx.x=0～31）组成一个 warp。所以二维块 (16,16) 时，最内层维度是 16，每 16 个线程一组，<strong>半个 warp 一组</strong>，会导致加载效率下降（见 3.3 节矩阵求和实验）。</p></blockquote><hr><h3 id="3-2-xian-cheng-shu-fen-hua-warp-divergence-shu-3-2-2" tabindex="-1" id="3-2-线程束分化（Warp-Divergence）（书-3-2-2）">3.2 线程束分化（Warp Divergence）（书 3.2.2）</h3><p>控制流是高级编程语言的基本构造。GPU 支持传统的、C 风格的、显式的控制流结构，如 <code>if…then…else</code>、<code>for</code>、<code>while</code>。<strong>CPU 拥有复杂的硬件以执行分支预测</strong>——在每个条件检查中预测应用程序的控制流会使用哪个分支。如果预测正确，CPU 中的分支只需付出很小的性能代价；如果预测不正确，CPU 可能会停止运行很多个周期，因为指令流水线被清空了。<strong>GPU 是相对简单的设备，它没有复杂的分支预测机制</strong>。</p><p><strong>一个线程束中的所有线程在同一周期中必须执行相同的指令</strong>。如果一个线程执行一条指令，那么线程束中的所有线程都必须执行该指令。如果在同一线程束中的线程使用不同的路径通过同一个应用程序，就会产生问题。例如（书中示例）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (cond) &#123;</span><br><span class="line">    <span class="comment">// 分支 A：16 个线程执行</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 分支 B：另外 16 个线程执行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设在一个线程束中有 16 个线程的 <code>cond</code> 为 true，另外 16 个为 false。一半的线程束需要执行 if 块中的指令，另一半需要执行 else 块中的指令。<strong>在同一线程束中的线程执行不同的指令，被称为线程束分化</strong>。</p><p>硬件如何解决这一「悖论」？<strong>如果一个线程束中的线程产生分化，线程束将连续执行每一个分支路径，而禁用不执行这一路径的线程</strong>。即：先执行 if 块（此时 else 对应的 16 个线程被禁用），再执行 else 块（此时 if 对应的 16 个线程被禁用）。<strong>线程束分化会导致性能明显地下降</strong>——在前面的例子中，线程束中并行线程的数量减少了一半。条件分支越多，并行性削弱越严重。</p><p><strong>注意</strong>：线程束分化只发生在<strong>同一个线程束中</strong>。在不同的线程束中，不同的条件值不会引起线程束分化。</p><h4 id="bi-mian-fen-hua-de-shu-ju-fen-qu-shi-li" tabindex="-1" id="避免分化的数据分区示例">避免分化的数据分区示例</h4><p>书中用<strong>简单的算术内核</strong>做了对比。可以用「偶数和奇数线程」的方法模拟导致分化的条件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 导致分化：同一 warp 内一半走 if、一半走 else</span></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">mathKernel1</span><span class="params">(<span class="type">float</span> *c)</span> &#123;</span><br><span class="line">    <span class="type">int</span> tid = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> (tid % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">        c[tid] = <span class="number">1.0f</span>;  <span class="comment">// 偶数线程</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        c[tid] = <span class="number">2.0f</span>;  <span class="comment">// 奇数线程</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 不导致分化：按 warp 分组，同一 warp 内走同一分支</span></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">mathKernel2</span><span class="params">(<span class="type">float</span> *c)</span> &#123;</span><br><span class="line">    <span class="type">int</span> tid = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">if</span> ((tid / warpSize) % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">        c[tid] = <span class="number">1.0f</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        c[tid] = <span class="number">2.0f</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>mathKernel2</code> 中，条件 <code>(tid / warpSize) % 2</code> 使同一线程束内的 32 个线程要么全为 0 要么全为 1，因此同一 warp 内不会分化。书中对应的示例可从 Wrox 网站下载 <strong><a href="http://simpleDivergence.cu">simpleDivergence.cu</a></strong>；测量时建议添加一次「预热」内核启动以去除首次运行的间接开销。</p><h4 id="duan-ding-predication-yu-bian-yi-qi-you-hua" tabindex="-1" id="断定（Predication）与编译器优化">断定（Predication）与编译器优化</h4><p>有一个常见现象：用 nvprof 测量时，<strong>没有报告显示出有分支分化（即分支效率是 100%）</strong>。这是因为 <strong>CUDA 编译器将短的、有条件的代码段用断定指令（predication）取代了分支指令</strong>。在断定中，根据条件把每个线程中的一个断定变量设置为 1 或 0；两种条件流路径被完全执行，但只有断定为 1 的指令的结果被保留，断定为 0 的指令不写回，但相应线程也不会停止。<strong>只有在条件语句的指令数小于某个阈值时</strong>，编译器才用断定指令替换分支指令；<strong>一段很长的代码路径肯定会导致线程束分化</strong>。</p><p>书中通过重写 <code>mathKernel1</code> 为 <code>mathKernel3</code>（将 if-else 分离为多个 if 语句），使分化分支的数量翻倍，从而让 nvprof 能直接显示出分支分化。<strong>重要提示</strong>（书中总结）：</p><ul><li>当一个分化的线程采取不同的代码路径时，会产生线程束分化。</li><li>不同的 if-then-else 分支会<strong>连续</strong>执行。</li><li>尝试<strong>调整分支粒度以适应线程束大小的倍数</strong>，避免线程束分化。</li><li>不同线程束可以执行不同代码且无须以牺牲性能为代价。</li></ul><blockquote><p><strong>难点与学习思考</strong>：分支分化是「同一 warp 内」的概念——不同 warp 走不同分支不会互相拖累。优化时优先考虑「让同一 warp 内的 32 个线程走同一条分支」：要么按数据分区（如 <code>(tid/warpSize)%2</code>），要么重组索引（如归约中让参与计算的线程 ID 连续）。另外，nvprof 可能显示 100% 分支效率，是因为短分支被编译器替换成了断定指令；只有分支体足够长时才会真正分化。</p></blockquote><hr><h3 id="3-3-zi-yuan-fen-pei-shu-3-2-3" tabindex="-1" id="3-3-资源分配（书-3-2-3）">3.3 资源分配（书 3.2.3）</h3><p>线程束的本地执行上下文主要由以下资源组成：</p><ul><li><strong>程序计数器</strong></li><li><strong>寄存器</strong></li><li><strong>共享内存</strong></li></ul><p>由 SM 处理的每个线程束的执行上下文，在整个线程束的生存期中是保存在芯片内的。因此，<strong>从一个执行上下文切换到另一个执行上下文没有损失</strong>。每个 SM 都有固定大小的寄存器组（存储在寄存器文件中，在线程中分配）和固定数量的共享内存（在线程块中分配）。对于一个给定的内核，同时存在于同一个 SM 中的线程块和线程束的数量取决于在 SM 中可用的且内核所需的寄存器和共享内存的数量。</p><p>书中<strong>图 3-13</strong> 显示：若每个线程消耗的寄存器越多，则可以放在一个 SM 中的线程束就越少。<strong>图 3-14</strong> 显示：若一个线程块消耗的共享内存越多，则在一个 SM 中可以被同时处理的线程块就会变少。资源可用性通常会限制 SM 中常驻线程块的数量；每个 SM 中寄存器和共享内存的数量因设备计算能力而不同。<strong>如果每个 SM 没有足够的寄存器或共享内存去处理至少一个块，那么内核将无法启动</strong>。</p><p>书中<strong>表 3-2</strong> 给出了不同计算能力下的一些关键限度。下表根据书中内容整理，便于与占用率计算器对照（实际以 <code>cudaGetDeviceProperties</code> 为准）：</p><table><thead><tr><th>计算能力</th><th>每线程最大寄存器数</th><th>每块最大共享内存</th><th>每块最大线程数</th><th>每 SM 最大线程数</th><th>每 SM 最大线程束数</th></tr></thead><tbody><tr><td>2.0</td><td>63</td><td>48 KB</td><td>1024</td><td>1536</td><td>48</td></tr><tr><td>3.0</td><td>63</td><td>48 KB</td><td>1024</td><td>2048</td><td>64</td></tr><tr><td>3.5</td><td>255</td><td>48 KB</td><td>1024</td><td>2048</td><td>64</td></tr></tbody></table><p>书中<strong>代码清单 3-2</strong> 提供了<strong>简单设备属性查询</strong>的完整示例（<a href="http://simpleDeviceQuery.cu">simpleDeviceQuery.cu</a>）。下面给出书中风格的完整代码，可直接编译运行以查询当前 GPU 的上述限制：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> deviceCount;</span><br><span class="line">    cudaGetDeviceCount(&amp;deviceCount);</span><br><span class="line">    <span class="keyword">if</span> (deviceCount == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;No CUDA device found.\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> dev = <span class="number">0</span>;</span><br><span class="line">    cudaSetDevice(dev);</span><br><span class="line"></span><br><span class="line">    cudaDeviceProp deviceProp;</span><br><span class="line">    cudaGetDeviceProperties(&amp;deviceProp, dev);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Device %d: \&quot;%s\&quot;\n&quot;</span>, dev, deviceProp.name);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Compute capability: %d.%d\n&quot;</span>, deviceProp.major, deviceProp.minor);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Multiprocessor count: %d\n&quot;</span>, deviceProp.multiProcessorCount);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Max threads per block: %d\n&quot;</span>, deviceProp.maxThreadsPerBlock);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Max threads per multiprocessor: %d\n&quot;</span>, deviceProp.maxThreadsPerMultiProcessor);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Max warps per multiprocessor: %d\n&quot;</span>, deviceProp.maxThreadsPerMultiProcessor / <span class="number">32</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Registers per block: %d\n&quot;</span>, deviceProp.regsPerBlock);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;  Shared memory per block: %zu bytes\n&quot;</span>, deviceProp.sharedMemPerBlock);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译运行：<code>nvcc simpleDeviceQuery.cu -o simpleDeviceQuery &amp;&amp; ./simpleDeviceQuery</code>。<strong>最大线程束数</strong>用于占用率计算：<code>maxWarpsPerSM = maxThreadsPerMultiProcessor / 32</code>。</p><h4 id="huo-yue-xian-cheng-shu-de-fen-lei" tabindex="-1" id="活跃线程束的分类">活跃线程束的分类</h4><p>当计算资源（如寄存器和共享内存）已分配给线程块时，线程块被称为<strong>活跃的块</strong>，它所包含的线程束被称为<strong>活跃的线程束</strong>。活跃的线程束可以进一步分为三种类型：</p><ul><li><strong>选定的线程束（Selected）</strong>：正在被调度到执行单元上执行的线程束。</li><li><strong>符合条件的线程束（Eligible）</strong>：已就绪但尚未被调度。</li><li><strong>阻塞的线程束（Blocked）</strong>：没有做好执行的准备（例如等待内存操作完成）。</li></ul><p><strong>线程束符合执行条件</strong>需要同时满足（书中）：(1) 32 个 CUDA 核心可用于执行当前指令；(2) 当前指令中所有的参数都已就绪。例如，Kepler SM 上活跃的线程束数量在任何时候都必须小于或等于 64（架构限度）；在任何周期中，<strong>选定的</strong>线程束数量都小于或等于 4。如果线程束阻塞，线程束调度器会令一个符合条件的线程束代替它去执行。由于计算资源是在线程束之间分配的，而且在线程束的整个生存期中都保持在芯片内，因此<strong>线程束上下文的切换是非常快的</strong>。为了隐藏由线程束阻塞造成的延迟，需要让大量的线程束保持活跃。</p><hr><h3 id="3-4-yan-chi-yin-cang-shu-3-2-4" tabindex="-1" id="3-4-延迟隐藏（书-3-2-4）">3.4 延迟隐藏（书 3.2.4）</h3><p>SM 依赖<strong>线程级并行</strong>以最大化功能单元的利用率，因此利用率与常驻线程束的数量直接相关。<strong>指令延迟</strong>定义为：在指令发出和完成之间的时钟周期。当每个时钟周期中所有的线程调度器都有一个符合条件的线程束时，可以达到计算资源的完全利用——通过在其他常驻线程束中发布其他指令，可以隐藏每个指令的延迟。<strong>与在 CPU 上用 C 语言编程相比，延迟隐藏在 CUDA 编程中尤为重要</strong>：CPU 核心是为同时最小化一个或两个线程的延迟而设计的，而 GPU 则是为处理大量并发和轻量级线程以最大化吞吐量而设计的。GPU 的指令延迟被其他线程束的计算隐藏。</p><p>指令可分为两种基本类型（书中）：</p><ul><li><strong>算术指令</strong>：延迟约为 <strong>10～20 个周期</strong>。</li><li><strong>内存指令</strong>：延迟约为<strong>全局内存访问 400～800 个周期</strong>。</li></ul><p>书中<strong>图 3-15</strong> 表示线程束 0 阻塞时，线程束调度器选取其他线程束执行，当线程束 0 符合条件时再执行它。</p><p><strong>带宽</strong>通常指理论峰值，<strong>吞吐量</strong>指已达到的值。对于算术运算，所需的并行可以表示成「隐藏算术延迟所需要的操作数量」。书中<strong>表 3-3</strong> 列出了 Fermi 和 Kepler 设备上示例算术运算（32 位浮点乘加）的每 SM 每周期操作数等；吞吐量因不同的算术指令而不同。</p><h4 id="fermi-nei-cun-yan-chi-yin-cang-ji-suan" tabindex="-1" id="Fermi-内存延迟隐藏计算">Fermi 内存延迟隐藏计算</h4><p>书中以 Fermi 为例，给出了<strong>隐藏全局内存延迟所需并行量</strong>的具体计算：</p><ul><li>Fermi 内存频率（在 Tesla C2070 上测量）为 <strong>1.566 GHz</strong>。带宽 <strong>144 GB/s</strong> 可转换为每周期字节数：<br><strong>144 GB/s ÷ 1.566 GHz ≈ 92 字节/周期</strong>。</li><li>用内存延迟乘以每周期字节数：若取约 800 周期，则约 <strong>74 KB</strong> 的内存 I/O 运行才能实现充分利用（该值针对整个设备，因为内存带宽是针对整个设备的）。</li><li>假设每个线程将**一个 float（4 字节）**从全局内存移动到 SM 中用于计算，则：<br><strong>74 KB ÷ 4 字节/线程 ≈ 18 500 个线程</strong><br><strong>18 500 ÷ 32 ≈ 579 个线程束</strong><br>Fermi 有 16 个 SM，因此 <strong>579 ÷ 16 ≈ 36 个线程束/SM</strong>，才能隐藏所有的内存延迟。</li></ul><p>如果每个线程执行多个独立的 4 字节加载，隐藏内存延迟需要的线程就可以更少。<strong>延迟隐藏取决于每个 SM 中活跃线程束的数量</strong>，这一数量由执行配置和资源约束（内核中寄存器和共享内存的使用情况）隐式决定。<strong>选择一个最优执行配置的关键是在延迟隐藏和资源利用之间找到一种平衡</strong>。</p><p><strong>显示充足的并行</strong>：计算所需并行的一个简单公式是，用每个 SM 核心的数量乘以在该 SM 上一条算术指令的延迟。例如，Fermi 有 32 个单精度浮点流水线，算术指令延迟约 20 个周期，所以每个 SM 至少需要有 <strong>32×20＝640 个线程</strong>使设备处于忙碌状态——然而这只是一个下边界。</p><p><strong>延迟隐藏所需的并行量</strong>的显式公式与数值（以 Fermi 为例）如下。下面将关键公式与计算过程单独列出，便于对照书本与实验：</p><p><strong>(1) 算术延迟隐藏（下界）</strong></p><p>[<br>\text{每 SM 所需线程数} \geq \text{每 SM CUDA 核心数} \times \text{算术指令延迟（周期）}<br>]</p><p>例如 Fermi：(32 \times 20 = 640) 线程/SM。</p><p><strong>(2) 全局内存延迟隐藏（书中 Fermi 示例）</strong></p><ul><li>内存带宽 144 GB/s，内存频率 1.566 GHz → 每周期字节数：<br>[<br>\frac{144\ \text{GB/s}}{1.566\ \text{GHz}} \approx 92\ \text{字节/周期}<br>]</li><li>取内存延迟约 800 周期，则充分隐藏延迟所需「在途」数据量约：<br>[<br>92 \times 800 \approx 74\ \text{KB（设备级）}<br>]</li><li>若每个线程只加载 1 个 float（4 字节）：<br>[<br>\text{所需线程数} \approx \frac{74\ \text{KB}}{4\ \text{字节}} \approx 18500,\quad<br>\text{所需线程束数} \approx \frac{18500}{32} \approx 579<br>]</li><li>Fermi 共 16 个 SM，故<strong>每 SM 约需</strong> (579/16 \approx 36) <strong>个活跃线程束</strong>才能较好隐藏全局内存延迟。</li></ul><p>原书<strong>表 3-3</strong> 给出了 Fermi 与 Kepler 在示例算术运算（如 32 位浮点乘加）下的每 SM 每周期操作数等，用于与实测吞吐量对比。下表为典型含义（具体数值以原书表 3-3 为准）：</p><table><thead><tr><th>设备类型</th><th>算术指令延迟（周期）</th><th>每 SM 每周期操作数（示例）</th></tr></thead><tbody><tr><td>Fermi</td><td>~20</td><td>见原书表 3-3</td></tr><tr><td>Kepler</td><td>~20</td><td>见原书表 3-3</td></tr></tbody></table><blockquote><p><strong>理解与体会</strong>：延迟隐藏的本质是「用其他 warp 的计算把等待时间填满」。内存延迟（400～800 周期）远大于算术延迟（10～20 周期），所以内存型内核更需要大量活跃 warp。「每 SM 约 36 个线程束」是 Fermi 上的经验量级，实际以你设备的带宽和延迟为准；关键是建立「并行量 ↔ 延迟隐藏」的直觉，而不是死记数字。</p></blockquote><hr><h3 id="3-5-zhan-yong-lu-occupancy-shu-3-2-5" tabindex="-1" id="3-5-占用率（Occupancy）（书-3-2-5）">3.5 占用率（Occupancy）（书 3.2.5）</h3><p>在每个 CUDA 核心中指令是顺序执行的。当一个线程束阻塞时，SM 切换执行其他符合条件的线程束。理想情况下，我们想要有足够的线程束占用设备的核心。<strong>占用率</strong>定义为：<strong>每个 SM 中活跃的线程束占最大线程束数量的比值</strong>。公式为：</p><p>[<br>\text{占用率} = \frac{\text{每个 SM 上活跃的线程束数}}{\text{每个 SM 上最大线程束数}}<br>]</p><p>其中<strong>每个 SM 上最大线程束数</strong>为：</p><p>[<br>\text{最大线程束数} = \frac{\texttt{maxThreadsPerMultiProcessor}}{32}<br>]</p><p>例如，Fermi 的 maxThreadsPerMultiProcessor = 1536，则最大线程束数 = 1536/32 = <strong>48</strong>。活跃的线程束数由执行配置（网格与块的维度）以及内核对<strong>寄存器和共享内存</strong>的占用共同决定：若每个线程用的寄存器过多，或每个块用的共享内存过多，则 SM 上能同时驻留的块和线程束会减少，从而占用率低于理论最大值。因此需要留意：<strong>为了提高占用率，需要调整线程块配置或重新调整资源的使用情况</strong>，以允许更多的线程束同时处于活跃状态。</p><p><strong>CUDA 占用率计算器</strong>：CUDA 工具包包含一个电子表格（原书图 3-17），有助于选择网格和块的维数以使一个内核的占用率最大化。使用步骤大致为：</p><ol><li>提供 GPU 的计算能力；物理限制部分会自动填充。</li><li><strong>输入内核资源信息</strong>：每个块的线程数（执行配置，如 256、1024）、每个线程的寄存器数、每个块的共享内存（字节）。</li><li><strong>获取资源使用</strong>：编译时加 <code>-Xptxas -v</code> 可报告每个内核的寄存器和共享内存使用量，例如：<br><code>nvcc -Xptxas -v -o app kernel.cu</code><br>输出中会看到类似 <code>Used 32 registers, 0 bytes smem</code> 的信息。</li><li><strong>限制寄存器以调节占用率</strong>：使用 <strong>-maxrregcount=NUM</strong> 可限制每个线程使用的寄存器数上限，从而允许更多线程束常驻。例如：<br><code>nvcc -maxrregcount=32 -o app kernel.cu</code><br>占用率计算器会根据你填写的「每线程寄存器」和「每块共享内存」给出当前配置下的占用率及建议。</li></ol><p><strong>极端地操纵线程块会限制资源的利用</strong>（书中）：</p><ul><li><strong>小线程块</strong>：每个块中线程太少，会在所有资源被充分利用之前导致硬件达到每个 SM 的线程束数量限制。</li><li><strong>大线程块</strong>：每个块中有太多的线程，会导致在每个 SM 中每个线程可用的硬件资源较少。</li></ul><p><strong>网格和线程块大小的准则</strong>（书中总结）：</p><ul><li>保持每个块中线程数量是<strong>线程束大小（32）的倍数</strong>。</li><li>避免块太小：每个块至少要有 <strong>128 或 256 个线程</strong>。</li><li>根据内核资源的需求调整块大小。</li><li>块的数量要<strong>远远多于 SM 的数量</strong>，从而在设备中显示有足够的并行。</li><li><strong>通过实验</strong>得到最佳执行配置和资源使用情况。</li></ul><p>占用率只关注「每个 SM 里有多少并发线程/线程束」；但在每个 SM 中并发线程或线程束的数量；然而，<strong>充分的占用率不是性能优化的唯一目标</strong>。内核一旦达到一定级别的占用率，进一步增加占用率可能不会改进性能；还有许多其他因素需要在后续章节中考虑。</p><blockquote><p><strong>重难点</strong>：本章矩阵求和实验会直接说明——<strong>(16,16) 占用率最高但不是最快</strong>，因为最内层维度 16 导致加载效率（gld_efficiency）下降。所以优化时要<strong>在占用率、内存访问模式、分支效率之间找平衡</strong>，而不是盲目追求 100% 占用率。</p></blockquote><hr><h3 id="3-6-tong-bu-zha-lan-shu-3-2-6" tabindex="-1" id="3-6-同步栅栏（书-3-2-6）">3.6 同步栅栏（书 3.2.6）</h3><p>同步是一个在许多并行编程语言中都很常见的原语。在 CUDA 中，同步可以在两个级别执行（书中）：</p><ul><li><strong>系统级</strong>：等待主机和设备完成所有的工作。</li><li><strong>块级</strong>：在设备执行过程中等待一个线程块中所有线程到达同一点。</li></ul><p>对于主机，由于许多 CUDA API 调用和所有的内核启动是<strong>异步</strong>的，可以使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaDeviceSynchronize</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>阻塞主机应用程序，直到所有的 CUDA 操作（复制、核函数等）完成。</p><p>当 <strong><code>__syncthreads()</code></strong> 被调用时，在同一个线程块中每个线程都必须等待直至该线程块中<strong>所有其他线程</strong>都已经达到这个同步点。在栅栏之前所有线程产生的所有全局内存和共享内存访问，将会在栅栏后对线程块中所有其他的线程可见。该函数可以协调同一个块中线程之间的通信，但它<strong>强制线程束空闲</strong>，从而可能对性能产生负面影响。</p><p><strong>竞争条件</strong>：线程块中的线程可以通过共享内存和寄存器来共享数据。当线程之间共享数据时，要避免<strong>竞争条件（race condition）</strong>。例如，<strong>写后读</strong>竞争：当一个位置的无序读发生在写操作之后时，读应该在写前还是写后加载值是未定义的。其他例子还有读后写、写后写。当线程块中的线程在逻辑上并行运行时，在物理上并不是所有的线程都可以在同一时间执行——如果线程 A 试图读取由线程 B（在不同线程束中）写的数据，必须使用适当的同步确定线程 B 已经写完，否则会出现竞争条件。<strong>在不同的块之间没有线程同步</strong>；块间同步唯一安全的方法是在每个内核执行结束时使用全局同步点：终止当前核函数，开始执行新的核函数。不同块中的线程不允许相互同步，因此 GPU 可以以任意顺序执行块，这使得 CUDA 程序在大规模并行 GPU 上是可扩展的。</p><hr><h3 id="3-7-ke-kuo-zhan-xing-shu-3-2-7" tabindex="-1" id="3-7-可扩展性（书-3-2-7）">3.7 可扩展性（书 3.2.7）</h3><p>对于任何并行应用程序而言，<strong>可扩展性</strong>是一个理想的特性：为并行应用程序提供额外的硬件资源，相对于增加的资源，并行应用程序会产生加速。例如，若一个 CUDA 程序在两个 SM 中是可扩展的，则与在一个 SM 中运行相比，在两个 SM 中运行会使运行时间减半。<strong>可扩展性意味着增加的计算核心可以提高性能</strong>。串行代码本身是不可扩展的；并行代码有可扩展的潜能，但真正的可扩展性取决于算法设计和硬件特性。</p><p><strong>透明可扩展性</strong>：能够在可变数量的计算核心上执行相同的应用程序代码的能力。CUDA 内核启动时，线程块分布在多个 SM 中；网格中的线程块以并行或连续或任意的顺序被执行，这种<strong>独立性</strong>使得 CUDA 程序在任意数量的计算核心间可以扩展。书中<strong>图 3-18</strong> 展示了可扩展性例子：左侧 GPU 有 2 个 SM，可同时执行 2 个块；右侧 GPU 有 4 个 SM，可同时执行 4 个块。不修改任何代码，应用程序可以在不同的 GPU 配置上运行，所需的执行时间根据可用的资源而改变。这里提到：<strong>可扩展性比效率更重要</strong>——一个可扩展但效率很低的系统可以通过简单添加硬件核心来处理更大的工作负载；一个效率很高但不可扩展的系统可能很快会达到可实现性能的上限。</p><hr><h2 id="si-bing-xing-xing-de-biao-xian-shu-3-3-jie" tabindex="-1" id="四、并行性的表现（书-3-3-节）">四、并行性的表现（书 3.3 节）</h2><p>书中通过<strong>矩阵求和</strong>核函数 <code>sumMatrixOnGPU2D</code>，使用不同的执行配置和 <strong>nvprof</strong> 指标，说明为什么有些网格/块的维数组合比其他的更好，并建立<strong>网格和块的启发式</strong>。</p><h3 id="4-1-yong-nvprof-jian-ce-huo-yue-de-xian-cheng-shu-yu-xing-neng-ji-zhun-shu-3-3-1" tabindex="-1" id="4-1-用-nvprof-检测活跃的线程束与性能基准（书-3-3-1）">4.1 用 nvprof 检测活跃的线程束与性能基准（书 3.3.1）</h3><p>对块大小为 <strong>(32,32)</strong>、(32,16)、(16,32)、(16,16)** 的配置进行测试（Tesla M2070 上）。从结果中可以观察到（书中）：</p><ul><li>第二种情况（如 32×16）块数比第一种多，设备可以有更多活跃的线程束，可能有更高的可实现占用率和更好的性能。</li><li><strong>第四种情况（16×16）有最高的可实现占用率，但它不是最快的</strong>——因此，<strong>更高的占用率并不一定意味着有更高的性能</strong>，肯定有其他因素限制 GPU 的性能。</li></ul><h3 id="4-2-yong-nvprof-jian-ce-nei-cun-cao-zuo-shu-3-3-2" tabindex="-1" id="4-2-用-nvprof-检测内存操作（书-3-3-2）">4.2 用 nvprof 检测内存操作（书 3.3.2）</h3><p>在 <code>sumMatrix</code> 内核（<code>C[idx]=A[idx]+B[idx]</code>）中有 3 个内存操作：两个加载、一个存储。可以用 <strong>gld_throughput</strong>（全局加载吞吐量）等指标检查内存读取效率。书中结果说明：<strong>最后两种情况（块最内层维度为 16）的加载效率是最前面两种情况的一半</strong>——这可以解释为什么最后两种配置下更高的加载吞吐量和可实现占用率没有产生较好的性能。尽管在最后两种情况下正在执行的加载数量（吞吐量）很多，但<strong>那些加载的有效性（效率）是较低的</strong>。<strong>对网格和块启发式算法来说，最内层的维数应该总是线程束大小的倍数</strong>（32）；第 4 章将讨论半个线程束大小的线程块是如何影响性能的。</p><h3 id="4-3-zeng-da-bing-xing-xing-shu-3-3-3" tabindex="-1" id="4-3-增大并行性（书-3-3-3）">4.3 增大并行性（书 3.3.3）</h3><p>在建立性能基准后，书中通过测试更大范围的线程配置（如块 (64,2)、(64,4)、(128,2)、(256,2)、(256,8) 等）得到结论：</p><ul><li>块大小为 (256,8) 时，一个块中线程总数超过 1024（GPU 硬件限制），配置无效。</li><li>最好的结果可能是块 (128,2) 等；块 (64,2) 虽然启动的线程块最多，但不是最快的。</li><li><strong>线程块最内层维度的大小对性能起着关键的作用</strong>（重复了前一节的结论）；<strong>增大并行性</strong>仍然是性能优化的一个重要因素。</li></ul><p>用 nvprof 检测 <strong>achieved_occupancy</strong> 可验证：最好的执行配置既不具有最高的可实现占用率，也不具有最高的加载吞吐量。</p><p>书中在 Tesla M2070 上对 <strong>sumMatrixOnGPU2D</strong> 做了多组测试。下表为书中风格的<strong>执行配置与性能对照表</strong>（数值为示例，实际以你本机 nvprof 为准），用于体会「块最内层维度」与「占用率、加载效率」的关系：</p><table><thead><tr><th>块大小 (block.x, block.y)</th><th>块数量（相对）</th><th>核函数时间（示例）</th><th>achieved_occupancy（示例）</th><th>gld_efficiency（示例）</th></tr></thead><tbody><tr><td>(32, 32)</td><td>基准</td><td>较慢</td><td>较低</td><td>高</td></tr><tr><td>(32, 16)</td><td>2×</td><td><strong>较快（约 2×）</strong></td><td>较高</td><td>高</td></tr><tr><td>(16, 32)</td><td>2×</td><td>较快</td><td>较高</td><td><strong>明显下降</strong></td></tr><tr><td>(16, 16)</td><td>4×</td><td>介于中间</td><td><strong>最高</strong></td><td><strong>明显下降</strong></td></tr></tbody></table><p>可以看到：<strong>(16, 16)</strong> 时块数最多、占用率最高，但<strong>加载效率</strong>因最内层维度为 16（线程束大小的一半）而变差，整体并非最快。<strong>(32, 16)</strong> 在书中实验中往往更优。这直接说明：<strong>最内层维度应为线程束大小（32）的倍数</strong>；单看占用率或单看吞吐量都不足以优化，需在多个指标间找平衡。</p><p><strong>从这些实验中可以推断出</strong>（书中）：</p><ul><li><strong>没有一个单独的指标能直接优化性能</strong>。</li><li>需要在几个相关的指标间寻找一个恰当的平衡来达到最佳的总体性能。</li><li><strong>指标与性能</strong>：在大部分情况下，一个单独的指标不能产生最佳的性能；与总体性能最直接相关的指标或事件取决于内核代码的本质；要在相关的指标与事件之间寻求一个好的平衡；从不同角度查看内核以寻找相关指标间的平衡。</li><li><strong>网格/块启发式算法</strong>为性能调节提供了一个很好的起点。</li></ul><blockquote><p><strong>学习思考</strong>：这一节的实验是全书第一次用<strong>多指标</strong>解释性能——占用率、加载吞吐量、加载效率要一起看。最内层维度 32 的倍数能保证「每个 warp 的线程在访问全局内存时更易合并」，第 4 章会从内存访问模式上严格说明。建议自己跑一遍 nvprof，对照 achieved_occupancy 和 gld_efficiency 体会「平衡」的含义。</p></blockquote><hr><h2 id="wu-bi-mian-fen-zhi-fen-hua-shu-3-4-jie" tabindex="-1" id="五、避免分支分化（书-3-4-节）">五、避免分支分化（书 3.4 节）</h2><p>书中以<strong>并行归约</strong>为例，介绍避免分支分化的基本技术。</p><h3 id="5-1-bing-xing-gui-yue-wen-ti-shu-3-4-1" tabindex="-1" id="5-1-并行归约问题（书-3-4-1）">5.1 并行归约问题（书 3.4.1）</h3><p>假设要对一个有 N 个元素的整数数组求和。串行代码很容易实现；若有大量数据，则可以通过并行计算快速求和。鉴于加法的结合律和交换律，数组元素可以以任何顺序求和。常用方法（书中）：</p><ol><li>将输入向量划分到更小的数据块中。</li><li>用一个线程计算一个数据块的部分和。</li><li>对每个数据块的部分和再求和得出最终结果。</li></ol><p><strong>迭代成对实现</strong>：一个数据块只包含一对元素，一个线程对这两个元素求和产生一个局部结果；这些局部结果在最初的输入向量中就地保存，作为下一次迭代的输入。每次迭代后输出元素数量减半，当长度为 1 时得到最终和。根据每次迭代后输出元素就地存储的位置，可分为（书中）：</p><ul><li><strong>相邻配对</strong>：元素与它们直接相邻的元素配对（步长 1、2、4…）。</li><li><strong>交错配对</strong>：根据给定的跨度配对元素（初始跨度块大小的一半，每次减半）。</li></ul><p>任何满足<strong>交换律和结合律</strong>的运算都可以代替加法（如 max、min、乘积等）。在向量中执行这类运算称为<strong>归约问题</strong>；<strong>并行归约</strong>是这种运算的并行执行，是许多并行算法中的关键运算。书中实现了多个不同的并行归约核函数，并测试不同实现对内核性能的影响。</p><h3 id="5-2-bing-xing-gui-yue-zhong-de-fen-hua-shu-3-4-2" tabindex="-1" id="5-2-并行归约中的分化（书-3-4-2）">5.2 并行归约中的分化（书 3.4.2）</h3><p><strong>相邻配对</strong>方法的内核实现流程（原书图 3-21）如下：每个线程将相邻的两个元素相加产生部分和。内核中有两个全局内存数组：一个大数组存放整个数组用于归约，一个小数组存放每个线程块的部分和。每个线程块在数组的一部分上独立执行；循环中迭代一次执行一个归约步骤，归约是<strong>就地</strong>完成的。<strong><code>__syncthreads()</code></strong> 保证线程块中的任一线程在进入下一次迭代之前，当前迭代里每个线程的所有部分和都已写入全局内存。<strong>跨度</strong>初始为 1，每次归约循环结束后乘以 2。因为线程块间无法同步，每个线程块产生的部分和需要复制回主机并在主机上做串行求和（原书图 3-22）。完整源代码可从 Wrox 网站 <strong><a href="http://reduceInteger.cu">reduceInteger.cu</a></strong> 获取。</p><p><strong>分化来源</strong>：条件表达式 <strong><code>if ((tid % (2 * stride)) == 0)</code></strong> 只对偶数 ID 的线程为 true，会导致<strong>很高的线程束分化</strong>。在第一次迭代中只有 ID 为偶数的线程执行条件语句主体，但所有线程都必须被调度；第二次迭代中只有四分之一的线程活跃，但所有线程仍被调度。</p><blockquote><p><strong>难点</strong>：<code>tid % (2*stride)==0</code> 在同一 warp 内会交替为 true/false（例如 stride=1 时 tid=0,2,4,… 为 true），所以同一 warp 内一半线程走 if、一半闲置，然后反过来，造成明显的串行化。改进方向是让「参与计算的线程」在每轮中<strong>连续</strong>（如 tid=0～stride-1），这样同一 warp 内要么都参与要么都不参与。</p></blockquote><p><strong>相邻配对</strong>归约核函数（reduceNeighbored）的典型实现如下。注意：<code>stride</code> 从 1 开始，每轮翻倍；只有满足 <code>(tid % (2*stride)) == 0</code> 的线程参与加法，因此存在严重分化。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceNeighbored</span><span class="params">(<span class="type">int</span> *g_idata, <span class="type">int</span> *g_odata, <span class="type">unsigned</span> <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> *idata = g_idata + blockIdx.x * blockDim.x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (idx &gt;= n) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 就地归约，stride 1, 2, 4, ...</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> stride = <span class="number">1</span>; stride &lt; blockDim.x; stride *= <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((tid % (<span class="number">2</span> * stride)) == <span class="number">0</span>) &#123;</span><br><span class="line">            idata[tid] += idata[tid + stride];</span><br><span class="line">        &#125;</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tid == <span class="number">0</span>)</span><br><span class="line">        g_odata[blockIdx.x] = idata[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-3-gai-shan-bing-xing-gui-yue-de-fen-hua-shu-3-4-3" tabindex="-1" id="5-3-改善并行归约的分化（书-3-4-3）">5.3 改善并行归约的分化（书 3.4.3）</h3><p>通过<strong>重新组织每个线程的数组索引</strong>，让 <strong>ID 相邻的线程</strong>执行求和操作，线程束分化就能被减少。书中<strong>图 3-23</strong> 展示了这种实现：部分和的存储位置并没有改变，但工作线程的索引已更新，使同一线程束内线程走相同路径。修改后的内核（书中称为 reduceNeighboredLess）中，参与加法的线程在每轮中连续排列，从而同一 warp 内要么都参与要么都不参与，避免分化。核心改动：用 <code>tid</code> 直接作为写入下标，且只让「前一半」线程做加法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceNeighboredLess</span><span class="params">(<span class="type">int</span> *g_idata, <span class="type">int</span> *g_odata, <span class="type">unsigned</span> <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">int</span> *idata = g_idata + blockIdx.x * blockDim.x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (idx &gt;= n) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> stride = <span class="number">1</span>; stride &lt; blockDim.x; stride *= <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="type">int</span> index = <span class="number">2</span> * stride * tid;</span><br><span class="line">        <span class="keyword">if</span> (index &lt; blockDim.x) &#123;</span><br><span class="line">            idata[index] += idata[index + stride];</span><br><span class="line">        &#125;</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (tid == <span class="number">0</span>)</span><br><span class="line">        g_odata[blockIdx.x] = idata[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-4-jiao-cuo-pei-dui-de-gui-yue-shu-3-4-4" tabindex="-1" id="5-4-交错配对的归约（书-3-4-4）">5.4 交错配对的归约（书 3.4.4）</h3><p>与相邻配对相比，<strong>交错配对</strong>颠倒了元素的跨度：初始跨度是线程块大小的一半，然后在每次迭代中减半（原书图 3-24）。在每次循环中，每个线程对两个被当前跨度隔开的元素求和。<strong>交错配对</strong>归约核函数（reduceInterleaved）如下。注意：<code>stride</code> 从 <code>blockDim.x / 2</code> 开始，每轮减半；写入位置为 <code>tid</code>，同一 warp 内线程仍连续工作，分化与 reduceNeighboredLess 类似，但<strong>全局内存访问模式</strong>更利于合并（第 4 章）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">reduceInterleaved</span><span class="params">(<span class="type">int</span> *g_idata, <span class="type">int</span> *g_odata, <span class="type">unsigned</span> <span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> tid = threadIdx.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    <span class="type">int</span> *idata = g_idata + blockIdx.x * blockDim.x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (idx &gt;= n) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> stride = blockDim.x / <span class="number">2</span>; stride &gt; <span class="number">0</span>; stride &gt;&gt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (tid &lt; stride) &#123;</span><br><span class="line">            idata[tid] += idata[tid + stride];</span><br><span class="line">        &#125;</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (tid == <span class="number">0</span>)</span><br><span class="line">        g_odata[blockIdx.x] = idata[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>书中实验表明：交错实现比第一个（reduceNeighbored）实现快了约 <strong>1.69 倍</strong>，比第二个（reduceNeighboredLess）实现快了约 <strong>1.34 倍</strong>。这种性能提升<strong>主要是由 reduceInterleaved 函数里的全局内存加载/存储模式</strong>导致的——第 4 章会介绍全局内存加载/存储模式对内核性能的影响。</p><blockquote><p><strong>理解与体会</strong>：归约这一节把「分支分化」和「内存访问模式」绑在一起：先通过索引重组消除分化（reduceNeighboredLess），再通过交错配对改善访存（reduceInterleaved）。性能优化往往是多步叠加的，先保证正确性和可读性，再逐步加「少分化 → 好访存 → 展开 → 共享内存」等优化。</p></blockquote><hr><h2 id="liu-zhan-kai-xun-huan-yu-dong-tai-bing-xing-shu-3-5-3-6-jie" tabindex="-1" id="六、展开循环与动态并行（书-3-5、3-6-节）">六、展开循环与动态并行（书 3.5、3.6 节）</h2><h3 id="6-1-zhan-kai-xun-huan-shu-3-5-jie" tabindex="-1" id="6-1-展开循环（书-3-5-节）">6.1 展开循环（书 3.5 节）</h3><p><strong>循环展开</strong>通过减少循环迭代次数和分支频率，增加可被调度器利用的<strong>独立指令</strong>，从而更好地隐藏指令或内存延迟。书中在归约等内核中应用了展开技术：例如每个线程在一次循环中处理多个元素（如 4 个），等价于展开；当剩余活动线程数等于一个 warp（32）时，可以<strong>完全展开</strong>最后一阶段，并去掉不必要的 <code>__syncthreads()</code>，减少同步开销。注意：展开会提高每线程寄存器使用量，需在占用率与指令级并行之间取得平衡；过度展开可能导致寄存器溢出或块内线程数下降，反而变慢。第 5 章还会结合共享内存进一步优化归约。</p><h3 id="6-2-dong-tai-bing-xing-shu-3-6-jie" tabindex="-1" id="6-2-动态并行（书-3-6-节）">6.2 动态并行（书 3.6 节）</h3><p>这里提到：到目前为止，所有核函数都是从主机线程中调用的，GPU 的工作负载完全在 CPU 的控制下。<strong>CUDA 的动态并行</strong>允许在 <strong>GPU 端直接创建和同步新的 GPU 内核</strong>——在一个核函数中的任意点动态增加 GPU 应用程序的并行性。这样就不需要把算法设计为单独的、大规模数据并行的内核启动；动态并行提供了一个更有层次结构的方法，使并发性可以在一个 GPU 内核的多个级别中表现出来。使用动态并行可以让<strong>递归算法</strong>更加清晰易懂；可以推迟到运行时决定需要在 GPU 上创建多少个块和网格；可以动态地利用 GPU 硬件调度器和负载平衡器，适应数据驱动或工作负载。在 GPU 端直接创建工作的能力可以减少主机和设备之间传输执行控制和数据的需求。书中通过使用动态并行实现<strong>递归归约</strong>核函数的例子，对如何利用动态并行做了基本介绍。实现有效的嵌套内核时，必须注意<strong>设备运行时的使用</strong>，包括子网格启动策略、父子同步和嵌套层的深度。<strong>计算能力 3.5 及以上</strong>的设备才支持动态并行。</p><hr><h2 id="qi-zong-jie-yu-zhong-nan-dian-xiao-jie-shu-3-7-jie" tabindex="-1" id="七、总结与重难点小结（书-3-7-节）">七、总结与重难点小结（书 3.7 节）</h2><p>书中总结：本章从硬件的角度分析了内核执行。在 GPU 设备上，CUDA 执行模型有两个最显著的特性：(1) 在线程块与线程中分配了硬件资源；(2) 使用 SIMT 方式在线程束中执行线程。这些执行模型的特征使得我们在提高并行性和性能时，能控制应用程序是如何让指令和内存带宽饱和的。不同计算能力的 GPU 设备有不同的硬件限制，因此<strong>网格和线程块的启发式算法</strong>在为不同平台优化内核性能方面发挥了非常重要的作用。动态并行使设备能够直接创建新的工作，确保我们可以用一种更自然和更易于理解的方式来表达递归或依赖数据并行的算法。本章也介绍了使用命令行分析工具 <strong>nvprof</strong> 详细分析内核性能的方法；因为一个单纯的内核实现可能不会产生很好的性能，所以<strong>配置文件驱动的方法</strong>在 CUDA 编程中尤其重要。性能分析对内核行为提供了详细的分析，并能找到产生最佳性能的主要因素。第 4 章和第 5 章将从 CUDA 内存模型的角度继续介绍内核执行的内容。</p><h3 id="7-1-he-xin-yao-dian-hui-gu" tabindex="-1" id="7-1-核心要点回顾">7.1 核心要点回顾</h3><ul><li><strong>线程束</strong>是 SM 的基本执行单位，32 个连续线程一组；块的最内层维度应为 32 的倍数。</li><li><strong>线程束分化</strong>会串行化同一 warp 内的分支执行；可通过数据分区（如 <code>(tid/warpSize)%2</code>）或重组索引减少分化；短分支可能被编译器用断定优化。</li><li><strong>资源分配</strong>：寄存器和共享内存限制每 SM 可容纳的块和线程束；活跃线程束分为选定、符合条件的、阻塞的；线程束间切换几乎无开销。</li><li><strong>延迟隐藏</strong>：算术延迟约 10～20 周期，全局内存约 400～800 周期；Fermi 上约需 36 个线程束/SM 才能较好隐藏内存延迟；需在延迟隐藏与资源利用之间平衡。</li><li><strong>占用率</strong> = 活跃线程束数 / 最大线程束数；高占用率不是唯一目标；准则包括块大小为 32 的倍数、至少 128～256 线程/块、块数远多于 SM 数。</li><li><strong>同步</strong>：<code>__syncthreads()</code> 做块内同步，需避免分支内不同步导致死锁；块间无同步，只能通过结束内核再启动新内核。</li><li><strong>可扩展性</strong>：块独立使同一代码可在不同 SM 数的 GPU 上扩展；可扩展性比单机效率更重要。</li><li><strong>并行归约</strong>：相邻配对与交错配对、分化与访存模式共同决定性能；交错配对 + 少分化可带来明显加速。</li><li><strong>动态并行</strong>：计算能力 3.5+，内核内启动子网格，适合递归与数据依赖型算法。</li></ul><h3 id="7-2-zhong-nan-dian-zi-jian-yu-xue-xi-si-kao" tabindex="-1" id="7-2-重难点自检与学习思考">7.2 重难点自检与学习思考</h3><table><thead><tr><th>主题</th><th>自检问题</th><th>要点</th></tr></thead><tbody><tr><td><strong>32 与 warp</strong></td><td>为什么说「32 是神奇的数字」？块最内层维度为何要是 32 的倍数？</td><td>硬件按 32 线程一组调度；非 32 倍数会导致未填满的 warp、降低加载效率。</td></tr><tr><td><strong>分支分化</strong></td><td>分化发生在什么粒度？如何避免？为何 nvprof 有时显示 100% 分支效率？</td><td>同一 warp 内；数据分区或索引重组使同 warp 同路径；短分支可能被断定替代。</td></tr><tr><td><strong>占用率 vs 性能</strong></td><td>高占用率一定更快吗？矩阵求和 (16,16) 说明了什么？</td><td>不一定；要同时看 gld_efficiency 等；最内层 32 的倍数常比单纯高占用率更重要。</td></tr><tr><td><strong>延迟隐藏</strong></td><td>算术延迟与内存延迟量级？Fermi 上隐藏内存延迟约需多少活跃 warp/SM？</td><td>约 10～20 vs 400～800 周期；约 36 个线程束/SM（书中推导）。</td></tr><tr><td><strong>网格与块启发式</strong></td><td>块大小、最内层维度、块数量分别怎么选？</td><td>32 的倍数、至少 128～256 线程/块、块数远多于 SM 数；结合实验与 -Xptxas -v 调优。</td></tr><tr><td><strong>归约优化</strong></td><td>reduceNeighbored 的分化来自哪里？reduceNeighboredLess / reduceInterleaved 各解决什么？</td><td><code>tid % (2*stride)==0</code> 导致同 warp 内交替；前者消除分化，后者进一步改善访存合并。</td></tr></tbody></table><p><strong>学习思考</strong>：第 2 章解决「怎么写」——网格、块、核函数；本章解决「为什么这样配置更好」——从 warp、SM、占用率、延迟隐藏到分支分化和归约改进。理解执行模型后，看 nvprof 的 achieved_occupancy、gld_efficiency 不再盲目，而是能对应到「活跃 warp 数量」「合并访问」等硬件行为。下一章从<strong>内存访问模式</strong>继续深入，解释「为何最内层 32 的倍数能提高加载效率」。</p><h3 id="7-3-yu-qian-hou-zhang-de-lian-xi" tabindex="-1" id="7-3-与前后章的联系">7.3 与前后章的联系</h3><ul><li><strong>第 2 章</strong>：编程模型给出网格—块—线程与核函数写法；本章解释这些线程在硬件上如何被调度（warp、SM）、为何不同配置性能不同。</li><li><strong>第 4 章</strong>：全局内存访问模式（对齐与合并）——矩阵求和中「最内层维度 32」对 gld_efficiency 的影响将在第 4 章从访存角度严格说明。</li><li><strong>第 5 章</strong>：共享内存与归约的进一步优化（bank 冲突、层次化归约等），与本章的循环展开、减少分化形成完整优化链。</li></ul><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>在下一篇博客中，我们将进入<strong>第 4 章：全局内存</strong>，从「执行与资源」转向「数据放在哪、怎么访问」：</p><ul><li>CUDA 内存模型与全局内存、缓存</li><li>内存管理：固定内存、零拷贝、统一寻址（Unified Memory）</li><li><strong>内存访问模式</strong>：对齐与合并、对带宽的影响</li><li>矩阵转置等案例与带宽测量</li></ul><p>理解全局内存的访问模式，是突破<strong>带宽瓶颈</strong>的第一步。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>为什么块的最内层维度建议为 32 的倍数？与线程束有何关系？</strong></li><li><strong>什么是线程束分化？减少分化的常见做法是什么？</strong></li><li><strong>占用率与性能的关系如何？为何「高占用率」不一定是「最快配置」？</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>GPU 按线程束（32 线程）为单位调度；最内层维度为 32 的倍数时，同一 warp 内线程连续，利于合并访问与满载执行；否则会产生未填满的 warp，降低内存与计算利用率。</li><li>同一 warp 内部分线程走 if、部分走 else，导致串行执行两条路径，称为分化。常见做法：数据分区使同 warp 内线程走相同分支、或用索引重组（如交错归约）避免同一 warp 内条件不一致。</li><li>占用率是「活跃线程束数/最大线程束数」，高占用率有助于隐藏延迟，但性能还受内存带宽、合并访问等影响；若内存效率低，再高占用率也受限于带宽。矩阵求和中 (32,16) 比 (16,16) 更快说明需综合看 gld_efficiency 与占用率。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/bdd22b42/">第2章 CUDA编程模型</a> | 下一篇：<a href="https://smarter.xin/posts/1f074790/">第4章 全局内存</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 3 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-san-zhang-cuda-zhi-xing-mo-xing&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="执行模型" scheme="https://smarter.xin/tags/execution-model/"/>
    
    <category term="线程束" scheme="https://smarter.xin/tags/thread-warp/"/>
    
    <category term="占用率" scheme="https://smarter.xin/tags/occupancy/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第二章：CUDA编程模型</title>
    <link href="https://smarter.xin/posts/bdd22b42/"/>
    <id>https://smarter.xin/posts/bdd22b42/</id>
    <published>2026-02-05T03:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-er-zhang-cuda-bian-cheng-mo-xing" tabindex="-1">CUDA C编程权威指南-第二章：CUDA编程模型</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/2b2e171a/">第1章 基于CUDA的异构并行计算</a> | 下一篇：<a href="https://smarter.xin/posts/bf3e0449/">第3章 CUDA执行模型</a></p><blockquote><p>本篇是系列第 2 篇，对应原书第 2 章。第 1 章搭好了并行与异构的宏观认知，这一章从「概念」落到「代码」：内存管理、线程组织、核函数怎么写、怎么计时和选设备，用<strong>向量加法</strong>和<strong>矩阵加法</strong>两个例子串起来。</p></blockquote><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><h3 id="1-1-wei-shi-yao-yao-xue-bian-cheng-mo-xing" tabindex="-1" id="1-1-为什么要学「编程模型」">1.1 为什么要学「编程模型」</h3><p>第 1 章回答了「为什么需要 GPU」和「CUDA 是什么」；本章回答的是：<strong>具体怎么写 CUDA 程序？</strong></p><p>这一章里强调：<strong>编程模型（Programming Model）</strong> 提供了计算机架构的<strong>抽象</strong>，作为应用程序与底层硬件之间的桥梁。它从逻辑上给出一种特定的计算架构视图，让程序员专注于算法逻辑，而不必纠缠硬件细节。CUDA 编程模型在 GPU 架构上提供了两个核心抽象：</p><ul><li><strong>一种通过层次结构在 GPU 中组织线程的方法</strong>（本章 + 第 3 章）</li><li><strong>一种通过层次结构在 GPU 中访问内存的方法</strong>（第 4 章 + 第 5 章）</li></ul><p>因此，学完本章你就掌握了「如何组织线程」和「如何管理主机/设备内存」的<strong>编程层</strong>用法；至于「为什么这样组织更快」「如何访问内存更高效」，将在第 3～5 章从执行模型与内存层次展开。</p><h3 id="1-2-xue-wan-ben-zhang-ni-ying-gai-neng-hui-da" tabindex="-1" id="1-2-学完本章，你应该能回答">1.2 学完本章，你应该能回答</h3><table><thead><tr><th>学习目标</th><th>检验方式</th></tr></thead><tbody><tr><td>区分<strong>主机（Host）<strong>与</strong>设备（Device）</strong>，以及典型 CUDA 程序流程</td><td>能写出「六步法」并说明每步作用</td></tr><tr><td>使用 <strong>cudaMalloc / cudaMemcpy / cudaFree</strong> 管理设备内存与数据传输</td><td>能说明 <code>cudaMemcpyKind</code> 的四种方向及<strong>同步</strong>含义</td></tr><tr><td>理解<strong>网格（Grid）— 线程块（Block）— 线程（Thread）</strong> 两层层次</td><td>能根据数据量写出 <code>grid</code> 与 <code>block</code> 的配置</td></tr><tr><td>掌握 <strong>threadIdx / blockIdx / blockDim / gridDim</strong> 的含义与用法</td><td>能写出一维/二维下的<strong>全局索引公式</strong></td></tr><tr><td>理解核函数 <strong>异步启动</strong> 与 <strong>cudaMemcpy 隐式同步</strong></td><td>能解释「为何计时时要在 kernel 后加 cudaDeviceSynchronize」</td></tr><tr><td>会编写 <strong><strong>global</strong></strong> 核函数，并从 C 循环转化为「每线程做一事」</td><td>能写出向量加法的核函数并说明与串行循环的对应关系</td></tr><tr><td>会用<strong>错误处理宏</strong>封装 CUDA API，并会做核函数结果验证</td><td>能写出 CHECK 宏并说明核函数错误检查方式</td></tr><tr><td>会用 <strong>CPU 计时</strong> 与 <strong>nvprof</strong> 度量核函数时间，理解<strong>带宽/计算受限</strong></td><td>能解释「通信计算比」对重叠计算与传输的指导意义</td></tr><tr><td>对<strong>矩阵</strong>会用二维/一维/混合方式组织线程，并做<strong>边界检查</strong></td><td>能写出 <code>ix, iy, idx</code> 的映射及 <code>if (ix &lt; nx &amp;&amp; iy &lt; ny)</code> 的原因</td></tr><tr><td>会查询 <strong>cudaDeviceProp</strong> 与 <strong>nvidia-smi</strong>，理解块/网格限制</td><td>能说出每个块最大线程数（如 1024）与网格维度上限</td></tr></tbody></table><h3 id="1-3-bo-ke-yue-du-dao-tu-ben-zhang-jia-gou" tabindex="-1" id="1-3-博客阅读导图（本章架构）">1.3 博客阅读导图（本章架构）</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">第 2 章  CUDA 编程模型</span><br><span class="line">│</span><br><span class="line">├── 二、CUDA 编程模型概述（书 2.1 节）</span><br><span class="line">│   ├── 2.1.1 编程结构：Host/Device、典型流程、nvcc（书 2.1.1）</span><br><span class="line">│   ├── 2.1.2 内存管理：cudaMalloc / cudaMemcpy / 全局与共享内存（书 2.1.2）</span><br><span class="line">│   ├── 2.1.3 线程管理：Grid–Block–Thread、内置变量、网格/块尺寸（书 2.1.3）</span><br><span class="line">│   ├── 2.1.4 启动核函数：&lt;&lt;&lt;grid, block&gt;&gt;&gt;、异步与同步（书 2.1.4）</span><br><span class="line">│   ├── 2.1.5 编写核函数：__global__、从 C 循环到核函数、全局索引（书 2.1.5）</span><br><span class="line">│   ├── 2.1.6 验证核函数（书 2.1.6）</span><br><span class="line">│   ├── 2.1.7 错误处理（书 2.1.7）</span><br><span class="line">│   └── 2.1.8 完整示例：GPU 向量加法（书 2.1.8）</span><br><span class="line">│</span><br><span class="line">├── 三、给核函数计时（书 2.2 节）</span><br><span class="line">│   ├── CPU 计时器与 cudaDeviceSynchronize</span><br><span class="line">│   ├── nvprof 工具</span><br><span class="line">│   └── 理论峰值与通信计算比</span><br><span class="line">│</span><br><span class="line">├── 四、组织并行线程（书 2.3 节）</span><br><span class="line">│   ├── 二维网格 + 二维块（矩阵索引与边界检查）</span><br><span class="line">│   ├── 一维网格 + 一维块（每线程处理一列）</span><br><span class="line">│   ├── 二维网格 + 一维块（混合）</span><br><span class="line">│   └── 执行配置对性能的影响（为第 3 章铺垫）</span><br><span class="line">│</span><br><span class="line">├── 五、设备管理（书 2.4 节）</span><br><span class="line">│   ├── cudaGetDeviceProperties 与常用属性</span><br><span class="line">│   ├── 多 GPU 下选择设备与 nvidia-smi / CUDA_VISIBLE_DEVICES</span><br><span class="line">│   └── 块/网格硬件限制</span><br><span class="line">│</span><br><span class="line">└── 六、本章小结与重难点</span><br></pre></td></tr></table></figure><hr><h2 id="er-cuda-bian-cheng-mo-xing-gai-shu-shu-2-1-jie" tabindex="-1" id="二、CUDA-编程模型概述（书-2-1-节）">二、CUDA 编程模型概述（书 2.1 节）</h2><h3 id="2-1-1-bian-cheng-jie-gou-yi-ge-cuda-cheng-xu-de-sheng-ming-zhou-qi-shu-2-1-1" tabindex="-1" id="2-1-1-编程结构：一个-CUDA-程序的生命周期（书-2-1-1）">2.1.1 编程结构：一个 CUDA 程序的生命周期（书 2.1.1）</h3><p>在<strong>异构环境</strong>中，CPU 与 GPU 各自拥有<strong>独立内存</strong>，通过 PCIe 总线相连。需要区分清楚（表 2-1 对应）：</p><table><thead><tr><th>术语</th><th>含义</th></tr></thead><tbody><tr><td><strong>主机（Host）</strong></td><td>CPU 及其内存（主机内存）</td></tr><tr><td><strong>设备（Device）</strong></td><td>GPU 及其内存（设备内存）</td></tr></tbody></table><blockquote><p><strong>命名约定</strong>：书中示例代码用 <code>h_</code> 前缀表示主机内存中的变量，用 <code>d_</code> 前缀表示设备内存中的变量。坚持这一习惯有助于阅读和调试。</p></blockquote><p><strong>典型 CUDA 程序流程</strong>（建议配合原书图 2-2 理解）：</p><ol><li>把数据从 CPU 内存拷贝到 GPU 内存（Host → Device）</li><li>调用核函数对 GPU 内存中的数据进行操作</li><li>把结果从 GPU 内存传送回 CPU 内存（Device → Host）</li></ol><p>下面用流程图概括上述数据流与执行顺序（建议配合原书图 2-2 理解）：</p><pre><code class="highlight mermaid">flowchart LR    subgraph host [主机 Host]        H2D[&quot;cudaMemcpy\nHost→Device&quot;]        D2H[&quot;cudaMemcpy\nDevice→Host&quot;]    end    subgraph device [设备 Device]        Kernel[&quot;核函数执行&quot;]    end    HostData[(主机内存)] --&gt; H2D    H2D --&gt; DevData[(设备内存)]    DevData --&gt; Kernel    Kernel --&gt; DevData    DevData --&gt; D2H    D2H --&gt; HostData</code></pre><p><strong>两个重要特性</strong>：</p><ul><li><strong>异步性</strong>：核函数启动后，控制权<strong>立刻</strong>返回给主机；CPU 可在 GPU 计算的同时执行其他任务，这是 CUDA 编程模型的天然优势。</li><li><strong>编译分离</strong>：NVIDIA 的 <strong>nvcc</strong> 将主机代码与设备代码分离——主机代码交标准 C 编译器，设备代码由 CUDA 编译器处理。你可将主机与设备代码放在同一源文件或不同文件中构建应用。</li></ul><p>从 <strong>CUDA 6.0</strong> 起引入了<strong>统一寻址（Unified Memory）</strong>，可用单一指针访问 CPU 与 GPU 内存，无需手动拷贝；细节在第 4 章。本章重点仍是<strong>程序员显式管理</strong>的主机/设备内存分配与拷贝，以便理解数据流并为后续优化打基础。</p><blockquote><p><strong>理解与体会</strong>：把「主机串行 + 设备并行」想成两条流水线：主机负责准备数据、发起 kernel、取回结果；设备负责大规模并行计算。理解「异步」后，后面学流（Stream）重叠计算与传输时会更自然。</p></blockquote><h3 id="2-1-2-nei-cun-guan-li-shu-2-1-2" tabindex="-1" id="2-1-2-内存管理（书-2-1-2）">2.1.2 内存管理（书 2.1.2）</h3><p>GPU 拥有独立内存空间，CUDA 运行时提供与标准 C 对应的内存管理函数（原书表 2-1）：</p><table><thead><tr><th>标准 C 函数</th><th>CUDA C 函数</th><th>功能</th></tr></thead><tbody><tr><td><code>malloc()</code></td><td><code>cudaMalloc()</code></td><td>在设备上分配线性内存</td></tr><tr><td><code>memcpy()</code></td><td><code>cudaMemcpy()</code></td><td>主机与设备间拷贝数据</td></tr><tr><td><code>memset()</code></td><td><code>cudaMemset()</code></td><td>设备内存初始化</td></tr><tr><td><code>free()</code></td><td><code>cudaFree()</code></td><td>释放设备内存</td></tr></tbody></table><p><strong>cudaMalloc</strong> — 在设备上分配一定字节的线性内存，通过指针返回：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMalloc</span><span class="params">(<span class="type">void</span> **devPtr, <span class="type">size_t</span> size)</span>;</span><br></pre></td></tr></table></figure><p><strong>cudaMemcpy</strong> — 从 <code>src</code> 复制 <code>count</code> 字节到 <code>dst</code>，方向由 <code>kind</code> 指定：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaMemcpy</span><span class="params">(<span class="type">void</span> *dst, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">size_t</span> count, cudaMemcpyKind kind)</span>;</span><br></pre></td></tr></table></figure><table><thead><tr><th>kind 值</th><th>方向</th></tr></thead><tbody><tr><td><code>cudaMemcpyHostToDevice</code></td><td>主机 → 设备</td></tr><tr><td><code>cudaMemcpyDeviceToHost</code></td><td>设备 → 主机</td></tr><tr><td><code>cudaMemcpyDeviceToDevice</code></td><td>设备 → 设备</td></tr><tr><td><code>cudaMemcpyHostToHost</code></td><td>主机 → 主机</td></tr></tbody></table><p>这里要搞清楚：<strong>cudaMemcpy 以同步方式执行</strong>——在拷贝完成并返回之前，主机应用程序是阻塞的。而核函数启动是<strong>异步</strong>的。这一区别在调试和性能分析时非常重要。</p><p><strong>GPU 内存结构（简化）</strong>（原书图 2-3）：主要包含两层——</p><ul><li><strong>全局内存（Global Memory）</strong>：类似 CPU 的系统内存，所有线程可访问，容量大、延迟高。</li><li><strong>共享内存（Shared Memory）</strong>：类似 CPU 缓存，但可由程序员显式控制，容量小、速度高。</li></ul><p>内存层次与访问模式的详细内容在第 4、5 章展开。</p><h3 id="2-1-3-xian-cheng-guan-li-wang-ge-kuai-yu-xian-cheng-shu-2-1-3" tabindex="-1" id="2-1-3-线程管理：网格、块与线程（书-2-1-3）">2.1.3 线程管理：网格、块与线程（书 2.1.3）</h3><p>当核函数在主机端启动时，执行转移到设备，设备会生成<strong>大量线程</strong>，每个线程执行核函数中的相同语句。需要留意的是：<strong>了解如何组织线程是 CUDA 编程的关键部分。</strong> CUDA 采用<strong>两层线程层次结构</strong>（原书图 2-5）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">网格（Grid）</span><br><span class="line"> ├── 线程块（Block 0）</span><br><span class="line"> │    ├── 线程 0, 1, 2, ...</span><br><span class="line"> │    └── 可通过共享内存与同步协作</span><br><span class="line"> ├── 线程块（Block 1）</span><br><span class="line"> │    └── ...</span><br><span class="line"> └── ...</span><br></pre></td></tr></table></figure><p>核心规则（原书表述）：</p><ul><li>由一个核函数启动的<strong>所有线程</strong>统称为一个<strong>网格（Grid）</strong>；</li><li>同一网格中的线程共享<strong>全局内存</strong>；</li><li>一个网格由多个**线程块（Block）**组成；</li><li><strong>同一线程块内</strong>的线程可通过<strong>共享内存</strong>和<strong>同步</strong>协作；</li><li><strong>不同线程块内</strong>的线程<strong>不能</strong>协作。</li></ul><h4 id="nei-zhi-zuo-biao-bian-liang-shu-2-1-3" tabindex="-1" id="内置坐标变量（书-2-1-3）">内置坐标变量（书 2.1.3）</h4><p>线程通过以下<strong>预初始化的内置变量</strong>区分彼此（原书表 2-2）：</p><table><thead><tr><th>变量</th><th>含义</th><th>类型</th></tr></thead><tbody><tr><td><code>threadIdx</code></td><td>线程在块内的索引</td><td><code>uint3</code>（.x, .y, .z）</td></tr><tr><td><code>blockIdx</code></td><td>线程块在网格内的索引</td><td><code>uint3</code></td></tr><tr><td><code>blockDim</code></td><td>线程块的维度（每块线程数）</td><td><code>dim3</code></td></tr><tr><td><code>gridDim</code></td><td>网格的维度（网格中块数）</td><td><code>dim3</code></td></tr></tbody></table><p>网格和块均可组织为<strong>一维、二维或三维</strong>；未指定的维度默认为 1。</p><blockquote><p><strong>主机端 vs 设备端</strong>：主机端用 <strong>dim3</strong> 定义网格和块的维度（作为 kernel 调用的一部分）；设备端（核函数内）可访问 <strong>uint3</strong> 类型的 <code>threadIdx</code>、<code>blockIdx</code>，以及 <strong>dim3</strong> 类型的 <code>blockDim</code>、<code>gridDim</code>。尤其要注意：主机端手动定义的 dim3 仅在主机可见，设备端预初始化的变量仅在设备端可见。</p></blockquote><h4 id="que-ding-wang-ge-he-kuai-chi-cun-de-yi-ban-bu-zou-shu-2-1-3" tabindex="-1" id="确定网格和块尺寸的一般步骤（书-2-1-3）">确定网格和块尺寸的一般步骤（书 2.1.3）</h4><p>典型调用顺序为：</p><ol><li><strong>确定块的大小</strong>——需考虑 GPU 资源限制与内核性能特性（第 3 章详述）；</li><li><strong>在已知数据大小和块大小的基础上计算网格维度</strong>。</li></ol><p>一维示例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">dim3 <span class="title function_">block</span><span class="params">(<span class="number">256</span>)</span>;</span><br><span class="line">dim3 <span class="title function_">grid</span><span class="params">((nElem + block.x - <span class="number">1</span>) / block.x)</span>;</span><br></pre></td></tr></table></figure><p>向上取整保证在数据量不是块大小整数倍时仍能覆盖全部数据。</p><blockquote><p><strong>重难点</strong>：块大小不能任意取，受硬件限制（如 Fermi 上每块最多 1024 线程）；网格维度也受限制（如每维最多 65535）。先掌握「先定块、再算网格」的流程，第 3 章会解释为何某些块大小更优。</p></blockquote><h3 id="2-1-4-qi-dong-he-han-shu-shu-2-1-4" tabindex="-1" id="2-1-4-启动核函数（书-2-1-4）">2.1.4 启动核函数（书 2.1.4）</h3><p>核函数通过 <strong>&lt;&lt;&lt;&gt;&gt;&gt;</strong> 指定执行配置（书中对 C 函数调用语法的扩展）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kernel_name&lt;&lt;&lt;grid, block&gt;&gt;&gt;(argument <span class="built_in">list</span>);</span><br></pre></td></tr></table></figure><ul><li>第一个参数：<strong>网格维度</strong>（启动的块数）；</li><li>第二个参数：<strong>块维度</strong>（每块线程数）。</li></ul><p>例如对 32 个数据元素（书中举例）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 4 个块，每块 8 个线程</span></span><br><span class="line">kernel&lt;&lt;&lt;<span class="number">4</span>, <span class="number">8</span>&gt;&gt;&gt;(args);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1 个块，32 个线程</span></span><br><span class="line">kernel&lt;&lt;&lt;<span class="number">1</span>, <span class="number">32</span>&gt;&gt;&gt;(args);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 32 个块，每块 1 个线程</span></span><br><span class="line">kernel&lt;&lt;&lt;<span class="number">32</span>, <span class="number">1</span>&gt;&gt;&gt;(args);</span><br></pre></td></tr></table></figure><p>核函数调用与主机线程<strong>异步</strong>：调用后控制权立刻返回主机。若需等待所有设备任务完成，可调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cudaError_t <span class="title function_">cudaDeviceSynchronize</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>需要留意的是：<strong>部分 CUDA 运行时 API 是隐式同步的</strong>。例如 <strong>cudaMemcpy</strong> 在主机与设备间拷贝时，主机会阻塞直到拷贝完成；且在此拷贝开始前，<strong>之前所有已启动的核函数都需执行完毕</strong>。</p><h3 id="2-1-5-bian-xie-he-han-shu-shu-2-1-5" tabindex="-1" id="2-1-5-编写核函数（书-2-1-5）">2.1.5 编写核函数（书 2.1.5）</h3><p>核函数用 <strong><strong>global</strong></strong> 修饰符声明，<strong>必须返回 void</strong>（语法规定）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">kernel_name</span><span class="params">(argument <span class="built_in">list</span>)</span> &#123;</span><br><span class="line">    <span class="comment">// 每个线程执行的代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>CUDA C 三种函数类型限定符（原书表 2-2）：</p><table><thead><tr><th>限定符</th><th>执行位置</th><th>调用方</th><th>说明</th></tr></thead><tbody><tr><td><code>__global__</code></td><td>设备</td><td>主机（或设备，compute capability ≥ 3.2）</td><td>核函数，必须返回 void</td></tr><tr><td><code>__device__</code></td><td>设备</td><td>设备</td><td>仅被核函数或其他 device 函数调用</td></tr><tr><td><code>__host__</code></td><td>主机</td><td>主机</td><td>默认行为，可省略</td></tr></tbody></table><p><code>__device__</code> 与 <code>__host__</code> 可<strong>同时使用</strong>，使同一函数在主机与设备上各有一份实现；<code>__global__</code> 不能与二者组合。</p><h4 id="cong-c-xun-huan-dao-cuda-he-han-shu-shu-2-1-5" tabindex="-1" id="从-C-循环到-CUDA-核函数（书-2-1-5）">从 C 循环到 CUDA 核函数（书 2.1.5）</h4><p>书中用向量加法对比：主机端 C 代码是<strong>迭代 N 次的串行循环</strong>；核函数中<strong>循环消失</strong>，用线程坐标替代循环变量。</p><p><strong>C 串行版本：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">sumArraysOnHost</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">const</span> <span class="type">int</span> N)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        C[i] = A[i] + B[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>CUDA 核函数版本（仅当 N = 块内线程数时可直接用 threadIdx.x）：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">sumArraysOnGPU</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = threadIdx.x;</span><br><span class="line">    C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当使用<strong>一维网格 + 一维块</strong>的通用配置时，<strong>全局唯一索引</strong>由下式给出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br></pre></td></tr></table></figure><blockquote><p><strong>理解与体会</strong>：可以把「核函数」理解为：<strong>把原来的循环体抽出来，让每个线程执行一次；循环变量 i 换成「当前线程的全局索引」</strong>。这个公式是后续所有一维数据映射的基础，务必熟练。</p></blockquote><h3 id="2-1-6-yan-zheng-he-han-shu-shu-2-1-6" tabindex="-1" id="2-1-6-验证核函数（书-2-1-6）">2.1.6 验证核函数（书 2.1.6）</h3><p>书中建议的验证方式包括：</p><ol><li><strong>主机端验证函数</strong>：用 CPU 实现相同算法，逐元素比较结果（如 <code>checkResult</code>）。</li><li><strong>核函数内 printf</strong>：Fermi 及以上架构支持在核函数内使用 <code>printf</code> 输出调试信息（需 <code>-arch=sm_20</code> 等）。</li><li><strong>单线程执行</strong>：将配置设为 <code>&lt;&lt;&lt;1, 1&gt;&gt;&gt;</code>，相当于单线程串行执行，便于对比数值正确性。</li></ol><h3 id="2-1-7-cuo-wu-chu-li-shu-2-1-7" tabindex="-1" id="2-1-7-错误处理（书-2-1-7）">2.1.7 错误处理（书 2.1.7）</h3><p>由于许多 CUDA 调用是<strong>异步</strong>的，错误可能不会在发生处立即暴露。书中建议：<strong>定义一个错误处理宏，封装所有 CUDA API 调用</strong>，便于定位出错步骤与可读错误信息。示例（与书中一致）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CHECK(call)                                                        \</span></span><br><span class="line"><span class="meta">&#123;                                                                          \</span></span><br><span class="line"><span class="meta">    const cudaError_t <span class="keyword">error</span> = call;                                        \</span></span><br><span class="line"><span class="meta">    <span class="keyword">if</span> (<span class="keyword">error</span> != cudaSuccess) &#123;                                            \</span></span><br><span class="line"><span class="meta">        printf(<span class="string">&quot;Error: %s:%d, &quot;</span>, __FILE__, __LINE__);                       \</span></span><br><span class="line"><span class="meta">        printf(<span class="string">&quot;code: %d, reason: %s\n&quot;</span>, <span class="keyword">error</span>, cudaGetErrorString(<span class="keyword">error</span>)); \</span></span><br><span class="line"><span class="meta">        exit(1);                                                           \</span></span><br><span class="line"><span class="meta">    &#125;                                                                      \</span></span><br><span class="line"><span class="meta">&#125;</span></span><br></pre></td></tr></table></figure><p>使用示例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_A, nBytes));</span><br><span class="line">CHECK(cudaMemcpy(d_A, h_A, nBytes, cudaMemcpyHostToDevice));</span><br></pre></td></tr></table></figure><p><strong>核函数错误的检查</strong>：核函数本身不返回错误码，需在 kernel 调用后通过同步再检查：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(args);</span><br><span class="line">CHECK(cudaDeviceSynchronize());</span><br></pre></td></tr></table></figure><p>这一章里强调：<code>cudaDeviceSynchronize()</code> 会阻塞主机直到设备上所有请求完成，因此<strong>仅建议在调试时使用</strong>，以避免掩盖异步带来的性能优势。</p><h3 id="2-1-8-wan-zheng-shi-li-gpu-xiang-liang-jia-fa-shu-2-1-8-dai-ma-qing-dan-2-4" tabindex="-1" id="2-1-8-完整示例：GPU-向量加法（书-2-1-8-代码清单-2-4）">2.1.8 完整示例：GPU 向量加法（书 2.1.8 / 代码清单 2-4）</h3><p>将上述内容串联，完整流程如下（书中 <a href="http://sumArraysOnGPU-small-case.cu">sumArraysOnGPU-small-case.cu</a> 对应）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">sumArraysOnGPU</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    C[i] = A[i] + B[i];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> nElem = <span class="number">32</span>;</span><br><span class="line">    <span class="type">size_t</span> nBytes = nElem * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 主机端分配并初始化</span></span><br><span class="line">    <span class="type">float</span> *h_A = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="type">float</span> *h_B = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="type">float</span> *h_C = (<span class="type">float</span> *)<span class="built_in">malloc</span>(nBytes);</span><br><span class="line">    <span class="comment">// ... 初始化 h_A, h_B ...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 设备端分配</span></span><br><span class="line">    <span class="type">float</span> *d_A, *d_B, *d_C;</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_A, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_B, nBytes));</span><br><span class="line">    CHECK(cudaMalloc((<span class="type">void</span> **)&amp;d_C, nBytes));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 主机 → 设备</span></span><br><span class="line">    CHECK(cudaMemcpy(d_A, h_A, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line">    CHECK(cudaMemcpy(d_B, h_B, nBytes, cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 启动核函数</span></span><br><span class="line">    dim3 <span class="title function_">block</span><span class="params">(<span class="number">32</span>)</span>;</span><br><span class="line">    dim3 <span class="title function_">grid</span><span class="params">(nElem / block.x)</span>;</span><br><span class="line">    sumArraysOnGPU&lt;&lt;&lt;grid, block&gt;&gt;&gt;(d_A, d_B, d_C);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5. 设备 → 主机</span></span><br><span class="line">    CHECK(cudaMemcpy(h_C, d_C, nBytes, cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 6. 验证、释放</span></span><br><span class="line">    cudaFree(d_A); cudaFree(d_B); cudaFree(d_C);</span><br><span class="line">    <span class="built_in">free</span>(h_A); <span class="built_in">free</span>(h_B); <span class="built_in">free</span>(h_C);</span><br><span class="line">    cudaDeviceReset();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p><strong>小结</strong>：<strong>六步法</strong>——主机分配 → 设备分配 → H2D 传输 → 核函数执行 → D2H 传输 → 释放。这是绝大多数 CUDA 程序的主干，建议熟记。</p></blockquote><hr><h2 id="san-gei-he-han-shu-ji-shi-shu-2-2-jie" tabindex="-1" id="三、给核函数计时（书-2-2-节）">三、给核函数计时（书 2.2 节）</h2><p>性能度量是优化的前提。常用有两种方式。</p><h3 id="3-1-cpu-ji-shi-qi" tabindex="-1" id="3-1-CPU-计时器">3.1 CPU 计时器</h3><p>在主机端用系统时钟测量包含 kernel 在内的代码段。注意：<strong>必须</strong>在核函数调用后调用 <code>cudaDeviceSynchronize()</code>，否则测到的只是「启动 kernel」的开销，而非实际执行时间（核函数启动是异步的）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">double</span> iStart = cpuSecond();</span><br><span class="line">kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(args);</span><br><span class="line">cudaDeviceSynchronize();</span><br><span class="line"><span class="type">double</span> iElaps = cpuSecond() - iStart;</span><br></pre></td></tr></table></figure><h3 id="3-2-nvprof-gong-ju" tabindex="-1" id="3-2-nvprof-工具">3.2 nvprof 工具</h3><p>从 CUDA 5.0 起，NVIDIA 提供命令行性能分析工具 <strong>nvprof</strong>，可得到 CPU/GPU 活动时间线、各 kernel 与内存传输耗时等（书中 2.2 节）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nvprof ./sumArraysOnGPU</span><br></pre></td></tr></table></figure><h3 id="3-3-li-lun-feng-zhi-yu-tong-xin-ji-suan-bi-shu-2-2-jie" tabindex="-1" id="3-3-理论峰值与通信计算比（书-2-2-节）">3.3 理论峰值与通信计算比（书 2.2 节）</h3><p>这一章里强调：将应用<strong>测量值</strong>与<strong>理论峰值</strong>对比很重要。例如以 Tesla K10 为例：单精度峰值性能、内存带宽峰值、以及<strong>指令与字节比</strong>（如 13.6 : 1）。若应用<strong>每访问一字节产生的指令数大于该比</strong>，则受限于<strong>计算</strong>；否则受限于<strong>内存带宽</strong>。多数 HPC 工作负载是<strong>带宽受限</strong>的。</p><blockquote><p><strong>理解与体会</strong>：若<strong>计算时间 &gt; 数据传输时间</strong>，可通过<strong>流（Stream）<strong>重叠计算与传输，隐藏传输延迟（第 6 章）；若</strong>计算时间 &lt; 传输时间</strong>，则应尽量减少主机–设备间的数据搬运。这就是「通信计算比」对设计的指导意义。</p></blockquote><hr><h2 id="si-zu-zhi-bing-xing-xian-cheng-shu-2-3-jie" tabindex="-1" id="四、组织并行线程（书-2-3-节）">四、组织并行线程（书 2.3 节）</h2><p>本节通过<strong>矩阵加法</strong>展示如何用不同方式组织线程，并说明<strong>执行配置对性能有显著影响</strong>（书中用不同块大小做实验）。</p><h3 id="4-1-yong-kuai-he-xian-cheng-jian-li-ju-zhen-suo-yin-shu-2-3-1" tabindex="-1" id="4-1-用块和线程建立矩阵索引（书-2-3-1）">4.1 用块和线程建立矩阵索引（书 2.3.1）</h3><p>矩阵在全局内存中以<strong>行优先</strong>线性存储。对 <code>nx × ny</code> 矩阵，需要建立：</p><ul><li>线程/块索引 → 矩阵坐标 (ix, iy) → 线性全局偏移 idx</li></ul><p>二维网格、二维块时（书中公式）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">int</span> ix = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> iy = threadIdx.y + blockIdx.y * blockDim.y;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> idx = iy * nx + ix;</span><br></pre></td></tr></table></figure><h3 id="4-2-er-wei-wang-ge-er-wei-kuai-shu-2-3-2" tabindex="-1" id="4-2-二维网格-二维块（书-2-3-2）">4.2 二维网格 + 二维块（书 2.3.2）</h3><p>每个线程处理矩阵的一个元素，核函数内必须做<strong>边界检查</strong>：因为网格维度按「向上取整」计算，可能产生超出 <code>nx×ny</code> 的线程，若不判断会导致非法访问。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">sumMatrixOnGPU2D</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> nx, <span class="type">int</span> ny)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> ix = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> iy = threadIdx.y + blockIdx.y * blockDim.y;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> idx = iy * nx + ix;</span><br><span class="line">    <span class="keyword">if</span> (ix &lt; nx &amp;&amp; iy &lt; ny) &#123;</span><br><span class="line">        C[idx] = A[idx] + B[idx];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行配置示例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">dim3 <span class="title function_">block</span><span class="params">(<span class="number">32</span>, <span class="number">32</span>)</span>;   <span class="comment">// 每块 32×32 = 1024 线程</span></span><br><span class="line">dim3 <span class="title function_">grid</span><span class="params">((nx + block.x - <span class="number">1</span>) / block.x, (ny + block.y - <span class="number">1</span>) / block.y)</span>;</span><br></pre></td></tr></table></figure><p>书中实验结果：仅改变块大小，性能就有明显差异（如 32×32、32×16、16×16 等）。<strong>块数更多并不一定更快</strong>——例如 32×16 可能优于 16×16。原因与 GPU 的**线程束（warp）**调度和资源占用有关，第 3 章会详细解释。</p><h3 id="4-3-yi-wei-wang-ge-yi-wei-kuai-shu-2-3-3" tabindex="-1" id="4-3-一维网格-一维块（书-2-3-3）">4.3 一维网格 + 一维块（书 2.3.3）</h3><p>每个线程处理<strong>一列</strong>（ny 个元素），核函数内用循环遍历行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">__global__ <span class="type">void</span> <span class="title function_">sumMatrixOnGPU1D</span><span class="params">(<span class="type">float</span> *A, <span class="type">float</span> *B, <span class="type">float</span> *C, <span class="type">int</span> nx, <span class="type">int</span> ny)</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> ix = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line">    <span class="keyword">if</span> (ix &lt; nx) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> iy = <span class="number">0</span>; iy &lt; ny; iy++) &#123;</span><br><span class="line">            <span class="type">int</span> idx = iy * nx + ix;</span><br><span class="line">            C[idx] = A[idx] + B[idx];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>配置为一维：<code>dim3 block(256); dim3 grid((nx + block.x - 1) / block.x);</code>。书中实验表明，这种「一维块」配置有时反而更快。</p><h3 id="4-4-er-wei-wang-ge-yi-wei-kuai-shu-2-3-4" tabindex="-1" id="4-4-二维网格-一维块（书-2-3-4）">4.4 二维网格 + 一维块（书 2.3.4）</h3><p>网格二维、块一维，每线程仍处理一个元素，其中 <code>iy = blockIdx.y</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">int</span> ix = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> iy = blockIdx.y;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> idx = iy * nx + ix;</span><br></pre></td></tr></table></figure><h3 id="4-5-xian-cheng-zu-zhi-de-san-dian-qi-shi-shu-2-3-jie-zong-jie" tabindex="-1" id="4-5-线程组织的三点启示（书-2-3-节总结）">4.5 线程组织的三点启示（书 2.3 节总结）</h3><p>书中总结：</p><ol><li><strong>改变执行配置对内核性能有显著影响</strong>；</li><li><strong>直觉上「最自然」的实现不一定最优</strong>；</li><li><strong>对同一核函数，尝试不同网格与块大小可获更好性能</strong>。</li></ol><p>这些现象的本质将在第 3 章（执行模型、warp、占用率）中揭示。</p><hr><h2 id="wu-she-bei-guan-li-shu-2-4-jie" tabindex="-1" id="五、设备管理（书-2-4-节）">五、设备管理（书 2.4 节）</h2><p>合理设置执行配置需要了解设备能力与限制，可以用运行时 API 或命令行工具查询。</p><h3 id="5-1-yun-xing-shi-api-cuda-get-device-properties-shu-2-4-1" tabindex="-1" id="5-1-运行时-API：cudaGetDeviceProperties（书-2-4-1）">5.1 运行时 API：cudaGetDeviceProperties（书 2.4.1）</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cudaDeviceProp deviceProp;</span><br><span class="line">cudaGetDeviceProperties(&amp;deviceProp, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p><code>cudaDeviceProp</code> 常用字段包括：<code>name</code>、<code>totalGlobalMem</code>、<code>sharedMemPerBlock</code>、<strong>maxThreadsPerBlock</strong>（如 1024）、<code>maxThreadsDim[3]</code>、<code>maxGridSize[3]</code>、<strong>warpSize</strong>（通常 32）、<code>multiProcessorCount</code>、<code>major</code>/<code>minor</code>（计算能力）等。Fermi 上每块最大线程数为 <strong>1024</strong>，网格每维最大 <strong>65535</strong>，这些直接限制执行配置的设计。</p><h3 id="5-2-duo-gpu-xia-xuan-ze-she-bei-shu-2-4-2" tabindex="-1" id="5-2-多-GPU-下选择设备（书-2-4-2）">5.2 多 GPU 下选择设备（书 2.4.2）</h3><p>可通过遍历设备、比较 <code>multiProcessorCount</code> 等选择「最优」GPU，并用 <code>cudaSetDevice()</code> 设定当前设备。</p><h3 id="5-3-nvidia-smi-yu-cuda-visible-devices-shu-2-4-3-2-4-4" tabindex="-1" id="5-3-nvidia-smi-与-CUDA-VISIBLE-DEVICES（书-2-4-3～2-4-4）">5.3 nvidia-smi 与 CUDA_VISIBLE_DEVICES（书 2.4.3～2.4.4）</h3><ul><li><strong>nvidia-smi</strong>：查看 GPU 状态、列表、详细信息（如 <code>nvidia-smi -q -i 0</code>）。</li><li><strong>CUDA_VISIBLE_DEVICES</strong>：在不改代码的前提下指定可见 GPU（如 <code>export CUDA_VISIBLE_DEVICES=2</code> 仅使用 GPU 2）。</li></ul><hr><h2 id="liu-ben-zhang-xiao-jie-yu-zhong-nan-dian" tabindex="-1" id="六、本章小结与重难点">六、本章小结与重难点</h2><h3 id="6-1-zhi-shi-jie-gou-hui-gu" tabindex="-1" id="6-1-知识结构回顾">6.1 知识结构回顾</h3><table><thead><tr><th>模块</th><th>要点</th></tr></thead><tbody><tr><td><strong>内存管理</strong></td><td>cudaMalloc / cudaFree；cudaMemcpy 四种方向与<strong>同步</strong>；全局内存与共享内存的抽象</td></tr><tr><td><strong>线程管理</strong></td><td>Grid → Block → Thread；threadIdx / blockIdx / blockDim / gridDim；先定块、再算网格</td></tr><tr><td><strong>核函数</strong></td><td><strong>global</strong> / <strong>device</strong> / <strong>host</strong>；&lt;&lt;&lt;grid, block&gt;&gt;&gt;；异步启动；全局索引 <code>i = blockIdx.x*blockDim.x + threadIdx.x</code></td></tr><tr><td><strong>验证与错误</strong></td><td>主机验证、printf、单线程运行；CHECK 宏与 cudaDeviceSynchronize 查 kernel 错误</td></tr><tr><td><strong>性能</strong></td><td>CPU 计时 + 同步；nvprof；理论峰值与带宽/计算受限、通信计算比</td></tr><tr><td><strong>线程组织</strong></td><td>二维/一维/混合；矩阵索引 ix, iy, idx；边界检查；执行配置影响性能</td></tr><tr><td><strong>设备</strong></td><td>cudaDeviceProp、maxThreadsPerBlock、warpSize；nvidia-smi、CUDA_VISIBLE_DEVICES</td></tr></tbody></table><h3 id="6-2-zhong-nan-dian-yu-yi-cuo-dian" tabindex="-1" id="6-2-重难点与易错点">6.2 重难点与易错点</h3><ol><li><strong>异步 vs 同步</strong>：kernel 启动是异步的；cudaMemcpy 是同步的，且会等待之前所有 kernel 完成。计时时若不加 <code>cudaDeviceSynchronize()</code>，测到的不是 kernel 真实时间。</li><li><strong>全局索引公式</strong>：一维时 <code>i = blockIdx.x * blockDim.x + threadIdx.x</code>；二维矩阵时 <code>ix, iy</code> 再算 <code>idx = iy * nx + ix</code>。务必熟练，并始终做边界检查（<code>ix &lt; nx &amp;&amp; iy &lt; ny</code>）。</li><li><strong>块与网格限制</strong>：块最大线程数（如 1024）、网格维度上限（如 65535），设计配置时不能超出。</li><li><strong>从循环到核函数</strong>：循环变量 → 线程全局索引；循环体 → 核函数体。核函数里不要写「循环遍历所有数据」（除非刻意让每线程处理多元素，如 2.3.3 的按列处理）。</li></ol><h3 id="6-3-yu-qian-hou-zhang-de-lian-xi" tabindex="-1" id="6-3-与前后章的联系">6.3 与前后章的联系</h3><ul><li><strong>第 1 章</strong>：Host/Device、异构分工、第一个 kernel；本章在此基础上展开「完整程序」与线程组织。</li><li><strong>第 3 章</strong>：为何不同执行配置性能差异大（warp、SM、占用率、延迟隐藏）；为何 32 是「神奇的数字」。</li><li><strong>第 4～5 章</strong>：全局内存与共享内存的访问模式、对齐与合并、bank 冲突等，都建立在「网格–块–线程」与「全局索引」之上。</li></ul><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>第 3 章 <strong>CUDA 执行模型</strong> 将从<strong>硬件视角</strong>回答本章留下的问题：GPU 如何调度线程、什么是<strong>线程束（Warp）</strong>、为何 32×16 的块可能优于 16×16、<strong>分支分化</strong>的危害与规避、<strong>占用率</strong>与延迟隐藏、循环展开与动态并行等。从「怎么写」到「怎么跑」——理解执行模型是写出高性能 CUDA 代码的必经之路。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>写出典型 CUDA 程序的「六步」流程（从分配主机数据到取回结果）。</strong></li><li><strong>为何给核函数计时时要在 kernel 调用后加 <code>cudaDeviceSynchronize()</code>？</strong></li><li><strong>一维向量加法中，若数组长度为 1000，块大小为 256，网格维度应如何取？写出全局索引公式。</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>分配主机/设备内存 → 初始化主机数据 → 将数据从主机拷到设备（H2D）→ 配置并启动核函数 → 将结果从设备拷回主机（D2H）→ 释放设备与主机内存。</li><li>核函数启动是异步的，调用立即返回，若不同步就计时会把「启动到返回」的时间算进去而非实际执行时间；<code>cudaDeviceSynchronize()</code> 会阻塞直到设备上所有操作完成，之后计时才准确。</li><li>网格块数 = (1000 + 256 - 1) / 256 = 4；即 <code>gridDim.x = 4</code>, <code>blockDim.x = 256</code>。全局索引公式：<code>int i = blockIdx.x * blockDim.x + threadIdx.x</code>，核函数内需加边界判断 <code>if (i &lt; n)</code>。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 上一篇：<a href="https://smarter.xin/posts/2b2e171a/">第1章 基于CUDA的异构并行计算</a> | 下一篇：<a href="https://smarter.xin/posts/bf3e0449/">第3章 CUDA执行模型</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 2 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-er-zhang-cuda-bian-cheng-mo-xing&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="编程模型" scheme="https://smarter.xin/tags/programming-model/"/>
    
    <category term="核函数" scheme="https://smarter.xin/tags/kernel-function/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-第一章：基于CUDA的异构并行计算</title>
    <link href="https://smarter.xin/posts/2b2e171a/"/>
    <id>https://smarter.xin/posts/2b2e171a/</id>
    <published>2026-02-05T02:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-di-yi-zhang-ji-yu-cuda-de-yi-gou-bing-xing-ji-suan" tabindex="-1">CUDA C编程权威指南-第一章：基于CUDA的异构并行计算</h1><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 下一篇：<a href="https://smarter.xin/posts/bdd22b42/">第2章 CUDA编程模型</a></p><blockquote><p>本篇是《CUDA C编程权威指南》读书笔记系列的第 1 篇，从并行计算与异构架构讲起：为什么需要 GPU、什么是异构计算、CUDA 如何定位。先把「并行思维」和「CPU+GPU 分工」搞清楚，后面学编程模型、执行模型和内存优化会顺很多。</p></blockquote><hr><h2 id="yi-ben-zhang-zai-quan-shu-zhong-de-wei-zhi-yu-xue-xi-mu-biao" tabindex="-1" id="一、本章在全书中的位置与学习目标">一、本章在全书中的位置与学习目标</h2><p>第 1 章不写复杂代码，主要解决<strong>观念与语境</strong>问题——先把「并行计算」和「异构计算」讲清楚，是因为：</p><ul><li>CUDA 编程的本质是<strong>数据并行</strong> + <strong>在 GPU 上执行</strong>，若不清楚并行性的类型和 CPU/GPU 的差异，后面学线程网格、共享内存时会缺少动机。</li><li>主机（Host）与设备（Device）、主机代码与设备代码的区分将贯穿全书，第1章就必须建立清晰术语。</li></ul><p><strong>学完本章，你应该能回答：</strong></p><table><thead><tr><th>目标</th><th>检验方式</th></tr></thead><tbody><tr><td>理解串行与并行的区别，以及<strong>数据相关性</strong>对并行性的约束</td><td>能解释「为什么某段代码难以并行」</td></tr><tr><td>区分任务并行与数据并行，并知道 CUDA 主攻哪一类</td><td>能说出块划分与周期划分的大致思路</td></tr><tr><td>理解弗林分类中的 SISD/SIMD/MIMD，以及 GPU 的 SIMT</td><td>能简述 SIMT 与 SIMD 的联系与区别</td></tr><tr><td>理解同构与异构、主机与设备、GPU 作为协处理器与硬件加速器</td><td>能画出一个简单异构节点（CPU + GPU + PCIe）</td></tr><tr><td>理解 CPU 与 GPU 的设计哲学差异（延迟 vs 吞吐量、线程轻重）</td><td>能说明「为何 GPU 适合大规模数据并行」</td></tr><tr><td>会编写、编译并运行一个最简单的核函数（Hello World）</td><td>能解释 <code>__global__</code> 和 <code>&lt;&lt;&lt;1,10&gt;&gt;&gt;</code> 的含义</td></tr></tbody></table><hr><h2 id="er-bing-xing-ji-suan-shu-1-1-jie" tabindex="-1" id="二、并行计算（书-1-1-节）">二、并行计算（书 1.1 节）</h2><h3 id="2-1-chuan-xing-bian-cheng-yu-bing-xing-bian-cheng" tabindex="-1" id="2-1-串行编程与并行编程">2.1 串行编程与并行编程</h3><p>这一节里把概念厘清一下：用程序解决问题时，我们会自然地把问题划分成许多<strong>运算块</strong>，每个块执行一个指定任务。若这些块<strong>按先后顺序依次执行</strong>，就是<strong>串行程序</strong>。</p><p>两个计算块之间要么存在<strong>执行次序约束</strong>（必须串行），要么<strong>没有次序约束</strong>（可以并发）。<strong>所有包含并发执行任务的程序都是并行程序</strong>。尤其要注意的是：一个并行程序中<strong>可能既有并行部分也有串行部分</strong>（例如先串行初始化，再并行计算，再串行汇总）。</p><blockquote><p><strong>理解与体会</strong>：并行化不是「全盘同时执行」，而是找出<strong>可以同时执行</strong>的子集，其余部分仍保持顺序。这对后面理解「主机上串行、设备上并行」的分工很重要。</p></blockquote><p><strong>数据相关性</strong>（这一章会反复用到）：</p><ul><li>一个任务若<strong>依赖</strong>另一个任务的输出，则两者<strong>相关</strong>；否则<strong>独立</strong>。</li><li><strong>在并行算法实现中，分析数据相关性是最基本的内容，因为相关性是限制并行性的主要因素。</strong></li></ul><blockquote><p><strong>难点</strong>：很多性能问题源于「以为能并行、实则存在隐式依赖」。写 CUDA 时，要习惯先问：这段计算依赖哪些数据、是否被其他线程写入？</p></blockquote><p>并行计算涉及两个紧密相关的层面（书中 1.1 节）：</p><table><thead><tr><th>层面</th><th>关注点</th></tr></thead><tbody><tr><td><strong>计算机架构</strong>（硬件）</td><td>在结构级别上支持并行执行</td></tr><tr><td><strong>并行程序设计</strong>（软件）</td><td>充分利用硬件能力并发地解决问题</td></tr></tbody></table><p>这里提到：编写串行程序时，未必需要深入理解计算机架构；但在多核/众核上编写并行程序时，<strong>对底层架构的基本认识就显得非常重要</strong>。现代处理器多采用<strong>哈佛体系结构</strong>：内存（指令与数据）、中央处理单元（控制单元与算术逻辑单元）、输入/输出接口。高性能计算的关键是 CPU；早期单芯片单 CPU 为单核，现在多核处理器将多个核心集成在同一芯片上，<strong>并行程序设计可以看作把问题的计算分配给可用核心以实现并行的过程</strong>。</p><hr><h3 id="2-2-bing-xing-xing-de-liang-chong-ji-ben-lei-xing-shu-1-1-2" tabindex="-1" id="2-2-并行性的两种基本类型（书-1-1-2）">2.2 并行性的两种基本类型（书 1.1.2）</h3><p>并行性有两种基本类型：</p><table><thead><tr><th>类型</th><th>含义</th><th>重点</th></tr></thead><tbody><tr><td><strong>任务并行</strong></td><td>许多任务或函数可以<strong>独立地、大规模地</strong>并行执行</td><td>利用多核系统<strong>对任务进行分配</strong></td></tr><tr><td><strong>数据并行</strong></td><td>可以<strong>同时处理许多数据</strong></td><td>利用多核系统<strong>对数据进行分配</strong></td></tr></tbody></table><p><strong>CUDA 编程非常适合解决数据并行问题，本书的重点便是如何使用 CUDA 解决数据并行问题。</strong></p><p>数据并行程序设计的第一步是<strong>把数据依据线程进行划分</strong>，使每个线程处理一部分数据。两种常见划分方式（原书图 1-4、1-5）：</p><table><thead><tr><th>划分方式</th><th>做法</th><th>特点</th></tr></thead><tbody><tr><td><strong>块划分（block partitioning）</strong></td><td>一组<strong>连续</strong>的数据被分到一个块内；每个数据块以任意次序分配给一个线程，线程通常<strong>同一时间只处理一个数据块</strong></td><td>每个线程只处理数据的一部分</td></tr><tr><td><strong>周期划分（cyclic partitioning）</strong></td><td><strong>更少</strong>的数据被分到一个块内；<strong>相邻线程处理相邻数据块</strong>，每个线程可以处理<strong>多个数据块</strong>；选新块时相当于「跳过与现有线程数相同数量的数据块」</td><td>每个线程处理数据的多个部分</td></tr></tbody></table><p>对<strong>二维数据</strong>，可以沿 x 轴、y 轴或两者做块划分或周期划分（原书图 1-5 给出了沿 y 轴块划分、沿 x 和 y 块划分、沿 x 轴周期划分等例子）。</p><blockquote><p><strong>注意</strong>：<strong>数据通常在物理上是一维存储的</strong>，即便逻辑上是多维，也要映射到一维地址空间。<strong>如何在线程中分配数据，不仅与数据的物理存储方式密切相关，而且与每个线程的执行次序也有很大关系；组织线程的方式对程序性能有很大影响。</strong> 这一点在第 2 章（网格与块）、第 4 章（全局内存访问模式）会反复出现。</p></blockquote><hr><h3 id="2-3-ji-suan-ji-jia-gou-fen-lei-fu-lin-fen-lei-fa-shu-1-1-3" tabindex="-1" id="2-3-计算机架构分类：弗林分类法（书-1-1-3）">2.3 计算机架构分类：弗林分类法（书 1.1.3）</h3><p>弗林分类法（Flynn’s Taxonomy）根据<strong>指令</strong>和<strong>数据</strong>进入 CPU 的方式，将计算机架构分为四类（原书图 1-6）：</p><table><thead><tr><th>类型</th><th>全称</th><th>说明</th></tr></thead><tbody><tr><td><strong>SISD</strong></td><td>单指令单数据</td><td>传统串行架构：一个核心，任一时刻只有一个指令流处理一个数据流</td></tr><tr><td><strong>SIMD</strong></td><td>单指令多数据</td><td>多个核心，同一时刻<strong>所有核心只有一个指令流</strong>，处理<strong>不同数据流</strong>；向量机是典型代表；现代很多机器都采用 SIMD</td></tr><tr><td><strong>MISD</strong></td><td>多指令单数据</td><td>多个指令流处理<strong>同一</strong>数据流，实际中较少见</td></tr><tr><td><strong>MIMD</strong></td><td>多指令多数据</td><td>多个核心用多个指令流<strong>异步</strong>处理多个数据流，实现空间上的并行；许多 MIMD 架构还包含 SIMD 子组件</td></tr></tbody></table><p>这里提到：<strong>SIMD 的一大优势是，程序员可以继续按串行逻辑思考，而对并行数据的操作由编译器等实现并行加速。</strong></p><p><strong>延迟、带宽与吞吐量</strong>（书中 1.1.3）：</p><table><thead><tr><th>指标</th><th>含义</th><th>常用单位</th></tr></thead><tbody><tr><td><strong>延迟（latency）</strong></td><td>一个操作从开始到完成所需的时间</td><td>微秒（μs）</td></tr><tr><td><strong>带宽（bandwidth）</strong></td><td>单位时间内可处理的数据量</td><td>MB/s、GB/s</td></tr><tr><td><strong>吞吐量（throughput）</strong></td><td>单位时间内成功处理的运算数量</td><td>GFlops（每秒十亿次浮点运算）等</td></tr></tbody></table><p><strong>延迟</strong>衡量完成<strong>单次</strong>操作的时间，<strong>吞吐量</strong>衡量<strong>单位时间内</strong>处理的操作量。<strong>架构设计会在降低延迟、提高带宽、提高吞吐量之间做权衡。</strong></p><p><strong>按内存组织的分类</strong>（书中 1.1.3）：</p><ul><li><strong>分布式内存的多节点系统</strong>（集群）：多个处理器通过网络连接，各有本地内存。</li><li><strong>共享内存的多处理器系统</strong>：多个处理器共享同一物理内存，或通过低延迟链路（如 <strong>PCIe</strong>）相连；共享内存指共享地址空间，不一定是单一物理内存。这类系统包括多核单片系统，以及多芯片、每芯片多核的系统。</li></ul><p><strong>从多核到众核</strong>：<strong>「众核」（many-core）</strong> 通常指具有很多核心（几十到几百个）的多核架构。近年来架构正从多核转向众核。<strong>GPU 代表众核架构</strong>，几乎包含前述所有并行结构：多线程、MIMD、SIMD 以及指令级并行。<strong>NVIDIA 将这种架构称为 SIMT（单指令多线程）。</strong></p><blockquote><p><strong>理解与体会</strong>：SIMT 可以理解为「在 GPU 上一条指令对应很多线程，每条线程处理自己的数据」，既保留「单指令」的编程简洁性，又实现大规模多线程与 MIMD 的灵活性。后续学到 warp、线程块时会再具体化。</p></blockquote><p><strong>CPU 核心与 GPU 核心</strong>（书中原意）：尽管可用多核与众核来区分 CPU 与 GPU，但两种「核心」本质不同。<strong>CPU 核心较重</strong>，用于处理非常复杂的控制逻辑，优化<strong>串行</strong>程序执行；<strong>GPU 核心较轻</strong>，用于优化具有<strong>简单控制逻辑</strong>的<strong>数据并行</strong>任务，注重<strong>吞吐量</strong>。</p><hr><h2 id="san-yi-gou-ji-suan-shu-1-2-jie" tabindex="-1" id="三、异构计算（书-1-2-节）">三、异构计算（书 1.2 节）</h2><h3 id="3-1-cong-tong-gou-dao-yi-gou-shu-1-2-1" tabindex="-1" id="3-1-从同构到异构（书-1-2-1）">3.1 从同构到异构（书 1.2.1）</h3><p>最初计算机只有 CPU；近年来，高性能计算主流机器不断加入其他处理单元，其中最主要的就是 <strong>GPU</strong>。GPU 最初为图形并行计算设计，现已演化为<strong>强大的、多用途的、完全可编程的</strong>处理器，适合<strong>任务与数据并行</strong>的大规模计算。</p><p><strong>CPU 和 GPU 是两个独立的处理器，通过单个计算节点中的 PCI-Express 总线相连。</strong> 在这种典型架构中，GPU 作为<strong>离散设备</strong>存在。值得留意的是：<strong>从同构系统到异构系统的转变，是高性能计算史上的一个里程碑。</strong></p><table><thead><tr><th>概念</th><th>含义</th></tr></thead><tbody><tr><td><strong>同构计算</strong></td><td>使用<strong>同一架构</strong>下的一个或多个处理器执行应用</td></tr><tr><td><strong>异构计算</strong></td><td>使用<strong>不同架构</strong>的处理器（如 CPU + GPU）执行应用，<strong>为任务选择适合的架构</strong>，以改进性能</td></tr></tbody></table><p>书中也指出：异构系统虽能带来优势，但<strong>有效利用受限于应用程序设计的复杂性</strong>；若你已有并行编程基础，适应异构并行会相对容易。</p><p><strong>典型异构计算节点</strong>：包含<strong>两个多核 CPU 插槽</strong>和<strong>两个或更多众核 GPU</strong>。<strong>GPU 不是独立运行平台，而是 CPU 的协处理器</strong>，必须通过 <strong>PCIe 总线</strong>与基于 CPU 的<strong>主机</strong>相连才能工作（原书图 1-9）。因此：</p><ul><li><strong>主机端（Host）</strong>：CPU 所在位置</li><li><strong>设备端（Device）</strong>：GPU 所在位置</li></ul><p><strong>异构应用的组成</strong>：</p><ul><li><strong>主机代码</strong>：在 CPU 上运行</li><li><strong>设备代码</strong>：在 GPU 上运行</li></ul><p>应用通常由 <strong>CPU 初始化</strong>；在把计算密集型任务加载到设备之前，<strong>CPU 负责管理设备端的环境、代码和数据</strong>。当使用与 CPU 物理上分离的硬件来加速应用中<strong>计算密集型</strong>部分时，该组件就称为<strong>硬件加速器</strong>；<strong>GPU 是最常见的硬件加速器之一</strong>。</p><hr><h3 id="3-2-gpu-chan-pin-xian-yu-xing-neng-zhi-biao-shu-1-2-1" tabindex="-1" id="3-2-GPU-产品线与性能指标（书-1-2-1）">3.2 GPU 产品线与性能指标（书 1.2.1）</h3><p>书中简要列举了应用 NVIDIA GPU 计算平台的产品线：</p><table><thead><tr><th>产品线</th><th>面向</th></tr></thead><tbody><tr><td><strong>Tegra</strong></td><td>移动与嵌入式（如平板、手机）</td></tr><tr><td><strong>GeForce</strong></td><td>消费级图形</td></tr><tr><td><strong>Quadro</strong></td><td>专业绘图与设计</td></tr><tr><td><strong>Tesla</strong></td><td>大规模并行计算（如 HPC 加速器）</td></tr></tbody></table><p><strong>Fermi</strong> 是 Tesla 系列中的一种 GPU 加速器，在高性能计算中应用广泛。<strong>NVIDIA 于 2010 年发布的 Fermi 架构是世界上第一款完整的 GPU 计算架构</strong>；<strong>Kepler</strong> 于 2012 年发布，处理能力相比以往有较大提升。本书多数示例可在 Fermi 与 Kepler 上运行；部分示例需要 Kepler 中特殊架构支持。</p><p>描述 GPU 能力的两个重要特征（书中 1.2.1）：</p><ul><li><strong>CUDA 核心数量</strong>：决定并行规模</li><li><strong>内存大小</strong>：决定可处理的数据规模</li></ul><p>对应的两个性能指标：</p><table><thead><tr><th>指标</th><th>含义</th><th>单位</th></tr></thead><tbody><tr><td><strong>峰值计算性能</strong></td><td>每秒能处理的单精度/双精度浮点运算数量</td><td>GFlops、TFlops</td></tr><tr><td><strong>内存带宽</strong></td><td>从内存读取或写入数据的速率</td><td>GB/s</td></tr></tbody></table><p><strong>计算能力（Compute Capability）</strong>：NVIDIA 用「计算能力」描述 Tesla 系列 GPU 的<strong>硬件版本</strong>。<strong>主版本号相同的设备具有相同的核心架构</strong>。例如：主版本号 3 对应 Kepler 类架构，主版本号 2 对应 Fermi 类架构，主版本号 1 对应早期 Tesla 类架构。<strong>本书所有示例都需要计算能力 2.0 及以上。</strong></p><hr><h3 id="3-3-yi-gou-ji-suan-fan-li-cpu-yu-gpu-de-fen-gong-shu-1-2-2" tabindex="-1" id="3-3-异构计算范例：CPU-与-GPU-的分工（书-1-2-2）">3.3 异构计算范例：CPU 与 GPU 的分工（书 1.2.2）</h3><p>这里区分清楚：<strong>GPU 计算并不是要取代 CPU 计算。</strong> 对特定程序而言，各有适用场景；<strong>CPU 与 GPU 结合能有效提高大规模计算问题的处理速度与性能。</strong></p><ul><li><strong>CPU</strong>：针对<strong>动态工作负载</strong>优化——短序列计算、<strong>不可预测的控制流</strong>。</li><li><strong>GPU</strong>：针对<strong>由计算主导、控制流简单</strong>的工作负载。</li></ul><p>可从<strong>并行级别</strong>和<strong>数据规模</strong>两方面区分（原书图 1-10）：</p><table><thead><tr><th>特征</th><th>更适合 CPU</th><th>更适合 GPU</th></tr></thead><tbody><tr><td>数据规模</td><td>较小</td><td>较大</td></tr><tr><td>控制逻辑</td><td>复杂</td><td>简单</td></tr><tr><td>并行级别</td><td>低（如指令级并行）</td><td>高（大规模数据并行）</td></tr></tbody></table><p><strong>最佳实践</strong>（原书图 1-11）：<strong>为获得最佳性能，应同时使用 CPU 和 GPU</strong>——在 <strong>CPU</strong> 上执行<strong>串行部分或任务并行部分</strong>，在 <strong>GPU</strong> 上执行<strong>数据密集型并行部分</strong>。这种分工使 CPU + GPU 系统得以充分利用。</p><p><strong>CPU 线程与 GPU 线程</strong>（书中 1.2.2 重要对比）：</p><ul><li><strong>CPU 上的线程</strong>：通常是<strong>重量级</strong>实体；操作系统需要切换线程以提供多线程能力，<strong>上下文切换缓慢且开销大</strong>。四核 CPU 上可同时运行约 16 个线程（若支持超线程则可达约 32 个）。</li><li><strong>GPU 上的线程</strong>：<strong>高度轻量级</strong>；典型系统中有<strong>成千上万</strong>个线程在排队。若 GPU 需等待一组线程结束，可以转而执行另一组线程。</li><li><strong>设计目标</strong>：<strong>CPU 核心</strong>旨在<strong>尽可能减少一个或少量线程的延迟</strong>；<strong>GPU 核心</strong>旨在<strong>处理大量并发、轻量级的线程，以最大化吞吐量</strong>。</li><li><strong>数量对比</strong>：现代 NVIDIA GPU 每个多处理器最多可<strong>并发</strong>支持 <strong>1536</strong> 个活跃线程；一块有 16 个多处理器的 GPU 可<strong>同时</strong>支持<strong>超过 24000</strong> 个活跃线程。</li></ul><hr><h3 id="3-4-cuda-yi-chong-yi-gou-ji-suan-ping-tai-shu-1-2-3" tabindex="-1" id="3-4-CUDA：一种异构计算平台（书-1-2-3）">3.4 CUDA：一种异构计算平台（书 1.2.3）</h3><p><strong>CUDA</strong> 是 NVIDIA 推出的<strong>通用并行计算平台和编程模型</strong>，利用 GPU 中的并行计算引擎更有效地解决复杂计算问题。通过 CUDA，可以像在 CPU 上那样<strong>用 GPU 进行计算</strong>。</p><p>CUDA 平台可通过多种方式使用（原书图 1-12）：</p><ul><li><strong>CUDA 加速库</strong>（如 cuBLAS、cuFFT 等）</li><li><strong>编译器指令</strong>（如 OpenACC）</li><li><strong>行业标准语言的扩展</strong>（C、C++、Fortran、Python 等）</li></ul><p><strong>本书重点介绍 CUDA C。</strong> <strong>CUDA C 是标准 ANSI C 的扩展</strong>，通过<strong>少量语言扩展</strong>使异构编程成为可能，并通过 <strong>API</strong> 管理设备、内存等。CUDA 还是<strong>可扩展的编程模型</strong>：程序能在具有不同数量核心的 GPU 上透明地扩展并行性，对熟悉 C 的程序员也较易上手。</p><p>一个 CUDA 程序包含<strong>主机代码</strong>（在 CPU 上运行）与<strong>设备代码</strong>（在 GPU 上运行）的混合。<strong>NVIDIA 的 nvcc 编译器在编译过程中将设备代码从主机代码中分离出来</strong>（原书图 1-14）：</p><ul><li><strong>主机代码</strong>：标准 C，由 <strong>C 编译器</strong>编译。</li><li><strong>设备代码</strong>（核函数）：用带有关键字标记的 <strong>CUDA C</strong> 编写，由 <strong>nvcc</strong> 编译。</li><li><strong>链接阶段</strong>：在核函数调用与显式 GPU 操作中链接 <strong>CUDA 运行时库</strong>。</li></ul><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">源代码(.cu)</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">  nvcc ──→ 分离主机代码与设备代码</span><br><span class="line">    │              │</span><br><span class="line">    ▼              ▼</span><br><span class="line"> C 编译器      CUDA 编译器</span><br><span class="line"> (主机代码)    (设备代码/核函数)</span><br><span class="line">    │              │</span><br><span class="line">    ▼              ▼</span><br><span class="line">    链接 + CUDA 运行时库</span><br><span class="line">              │</span><br><span class="line">              ▼</span><br><span class="line">          可执行程序</span><br></pre></td></tr></table></figure><hr><h2 id="si-yong-gpu-shu-chu-hello-world-shu-1-3-jie" tabindex="-1" id="四、用-GPU-输出-Hello-World（书-1-3-节）">四、用 GPU 输出 Hello World（书 1.3 节）</h2><p>下面是书中风格的第一个 CUDA 程序：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">__global__ <span class="type">void</span> <span class="title function_">helloFromGPU</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello World from GPU!\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello World from CPU!\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    helloFromGPU&lt;&lt;&lt;<span class="number">1</span>, <span class="number">10</span>&gt;&gt;&gt;();</span><br><span class="line"></span><br><span class="line">    cudaDeviceReset();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>要点说明</strong>：</p><ul><li><strong><code>__global__</code></strong>：修饰符，表示这是一个<strong>核函数（kernel）</strong>——在设备上执行，由主机<strong>调用</strong>。</li><li><strong><code>&lt;&lt;&lt;1, 10&gt;&gt;&gt;</code></strong>：<strong>执行配置</strong>。第一个数 <strong>1</strong> 表示使用 <strong>1 个线程块</strong>，第二个数 <strong>10</strong> 表示<strong>每个块 10 个线程</strong>，因此共 <strong>10 个</strong> GPU 线程执行该核函数。每个线程都会执行一次 <code>helloFromGPU</code>，因此会打印 10 次 “Hello World from GPU!”。</li><li><strong><code>cudaDeviceReset()</code></strong>：释放当前设备上的资源，并促使设备端输出缓冲区被刷新（便于在主机端看到 GPU 的 printf 输出）。</li></ul><p>编译与运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvcc hello.cu -o hello</span><br><span class="line">./hello</span><br></pre></td></tr></table></figure><p>典型输出（顺序可能因线程调度略有差异）：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Hello World from CPU!</span><br><span class="line">Hello World from GPU!</span><br><span class="line">Hello World from GPU!</span><br><span class="line">…（共 10 次）</span><br></pre></td></tr></table></figure><blockquote><p><strong>理解与体会</strong>：同一段核函数代码被 10 个线程「同时」执行，正是<strong>数据并行</strong>的直观体现——所有线程执行相同代码，区别仅在于线程索引（后续会用 <code>threadIdx</code> 等区分每个线程负责的数据）。</p></blockquote><hr><h2 id="wu-shi-yong-cuda-c-bian-cheng-nan-ma-shu-1-4-jie" tabindex="-1" id="五、使用-CUDA-C-编程难吗？（书-1-4-节）">五、使用 CUDA C 编程难吗？（书 1.4 节）</h2><p><strong>不难，但需要思维转换。</strong> 主要区别在于对 GPU 架构的熟悉程度；一旦学会<strong>用并行思维思考</strong>并对 GPU 架构有基本了解，编写面向众多核心的并行程序会变得自然。</p><p><strong>数据局部性</strong>（书中 1.4）：高性能编程中的核心概念——通过<strong>数据重用</strong>降低内存访问延迟。</p><ul><li><strong>时间局部性</strong>：在较短时间内重复访问同一数据。</li><li><strong>空间局部性</strong>：访问存储空间中相邻的数据。</li></ul><p>CPU 通过大容量缓存自动利用局部性；在 CUDA 中，程序员可通过<strong>共享内存</strong>等机制<strong>显式</strong>控制数据局部性，这是性能优化的关键之一。</p><p><strong>CUDA 的三个核心抽象</strong>（书中 1.4）：</p><ol><li><strong>线程组的层次结构</strong>：线程 → 线程块 → 网格</li><li><strong>内存的层次结构</strong>：寄存器、共享内存、全局内存等</li><li><strong>同步机制</strong>：如屏障同步</li></ol><p>编写 CUDA 程序时，你主要编写<strong>被单个线程执行的一小段串行代码</strong>；GPU 负责启动大量线程并行执行这段代码。<strong>概念上，把串行代码中的循环「展开」成由不同线程执行，就得到核函数。</strong></p><p><strong>开发工具链</strong>（书中 1.4）：NVIDIA 提供 Nsight 集成开发环境、CUDA-GDB 调试器、Visual Profiler 性能分析、CUDA-MEMCHECK 内存检查、nvidia-smi 设备管理等，后续章节会逐步用到。</p><hr><h2 id="liu-zhong-nan-dian-yu-xue-xi-ti-hui-xiao-jie" tabindex="-1" id="六、重难点与学习体会小结">六、重难点与学习体会小结</h2><p>下表集中梳理本章容易混淆或需要反复强化的点，便于复习与后续章节衔接。</p><table><thead><tr><th>主题</th><th>书中要点</th><th>个人理解/易错点</th></tr></thead><tbody><tr><td><strong>数据相关性</strong></td><td>限制并行性的主要因素；分析数据相关性是并行算法实现的基本工作</td><td>写核函数前先想清楚：每个线程读哪些数据、写哪些数据，是否有写冲突或未定义依赖</td></tr><tr><td><strong>块划分 vs 周期划分</strong></td><td>块划分：连续数据成块，每线程一块；周期划分：块更小，相邻线程处理相邻块，每线程多块</td><td>与第 2 章 grid/block 对应：块划分更易对应「连续内存访问」，周期划分在某些负载均衡场景有用；数据物理上一维存储，分配方式影响访问模式与性能</td></tr><tr><td><strong>SIMT</strong></td><td>GPU 结合多线程、MIMD、SIMD 等，NVIDIA 称为单指令多线程</td><td>与 SIMD 类似处是「一条指令、多份数据」；不同处在于 GPU 线程可有自己的分支、更灵活，后续会学到 warp 内的 SIMD 执行</td></tr><tr><td><strong>主机 vs 设备</strong></td><td>CPU=主机，GPU=设备；主机代码在 CPU 运行，设备代码在 GPU 运行；GPU 是协处理器，通过 PCIe 连接</td><td>全书术语统一：分配设备内存、拷贝到设备、在设备上启动核函数；不要混淆主机指针与设备指针</td></tr><tr><td><strong>CPU vs GPU 设计哲学</strong></td><td>CPU：少线程、低延迟、复杂控制；GPU：海量轻量级线程、高吞吐、简单控制</td><td>不要用「跑得快」笼统比较；要问「延迟敏感还是吞吐敏感」「数据规模与并行度如何」</td></tr><tr><td><strong>计算能力</strong></td><td>主版本号相同则核心架构相同；本书示例需 2.0+</td><td>选 GPU 或写条件编译时要注意 Compute Capability</td></tr></tbody></table><hr><h2 id="qi-ben-zhang-xiao-jie" tabindex="-1" id="七、本章小结">七、本章小结</h2><p>第1章建立了三个层次的认知（对应书中全章结构）：</p><table><thead><tr><th>层次</th><th>内容</th></tr></thead><tbody><tr><td><strong>计算思维</strong></td><td>串行 vs 并行；数据相关性限制并行性；任务并行 vs 数据并行；块划分与周期划分；弗林分类与 SIMT；延迟、带宽、吞吐量；多核到众核</td></tr><tr><td><strong>异构架构</strong></td><td>同构→异构；主机/设备；主机代码/设备代码；GPU 为协处理器与硬件加速器；NVIDIA 产品线与计算能力；CPU 与 GPU 分工；CPU 线程与 GPU 线程的差异</td></tr><tr><td><strong>CUDA 平台</strong></td><td>CUDA 为异构计算平台与编程模型；CUDA C 为 C 的扩展；nvcc 分离主机/设备代码；第一个核函数与执行配置 <code>&lt;&lt;&lt;1,10&gt;&gt;&gt;</code></td></tr></tbody></table><p><strong>本章对我而言最重要的三点：</strong></p><ol><li><strong>数据相关性是并行性的天花板</strong>——能并行多少，首先取决于依赖关系，其次才是硬件核心数。</li><li><strong>主机与设备、主机代码与设备代码</strong>——从第1章就要建立清晰概念，否则后面内存管理、拷贝、同步都会乱。</li><li><strong>CPU 优化延迟、GPU 优化吞吐量；GPU 用海量轻量级线程换高吞吐</strong>——这决定了什么问题该放 CPU、什么问题该放 GPU，以及为什么 CUDA 适合数据并行。</li></ol><hr><h2 id="xia-yi-zhang-yu-gao" tabindex="-1" id="下一章预告">下一章预告</h2><p>第 <strong>2 章：CUDA 编程模型</strong> 将把第1章的概念落地为可编写、可度量的程序。你将系统学习：</p><ul><li>CUDA 程序的生命周期：主机与设备内存、数据拷贝、核函数启动</li><li>内存管理 API（<code>cudaMalloc</code>、<code>cudaMemcpy</code> 等）</li><li>线程层次：网格、线程块、线程索引（<code>gridDim</code>、<code>blockIdx</code>、<code>blockDim</code>、<code>threadIdx</code>）</li><li>如何给核函数计时、如何选择网格与块的大小</li><li>设备查询与选择（为多 GPU 与正确性验证打基础）</li></ul><p>从「为什么用 GPU」到「怎么写、怎么测」——我们下一章见。</p><hr><h2 id="ben-zhang-zi-ce" tabindex="-1" id="本章自测">本章自测</h2><ol><li><strong>数据并行与任务并行的区别是什么？CUDA 主要针对哪一种？</strong></li><li><strong>弗林分类中，SIMT 与 SIMD 各属哪一类？GPU 属于哪种？</strong></li><li><strong>写出一个最简单的 <code>__global__</code> 核函数并在主机上用 <code>&lt;&lt;&lt;1, 10&gt;&gt;&gt;</code> 启动，说明两个数字的含义。</strong></li></ol><p><strong>答案与解析</strong></p><ol><li>数据并行是「同时处理多份数据」，任务并行是「同时执行多个不同任务」。CUDA 主要针对数据并行，通过大量线程对数据划分并行处理。</li><li>SIMD 属「单指令多数据」，SIMT 是 NVIDIA 对「单指令多线程」的命名，本质也是单指令多数据的一种实现方式；GPU 采用 SIMT，每 32 线程为一 warp 执行相同指令。</li><li>例如 <code>__global__ void hello() &#123; &#125;</code>，主机调用 <code>hello&lt;&lt;&lt;1, 10&gt;&gt;&gt;()</code>。<code>&lt;&lt;&lt;1, 10&gt;&gt;&gt;</code> 表示 1 个块、每块 10 个线程，即共 10 个线程执行该核函数。</li></ol><hr><p><strong>系列导航</strong>：<a href="https://smarter.xin/posts/92dc1028/">导读</a> | 下一篇：<a href="https://smarter.xin/posts/bdd22b42/">第2章 CUDA编程模型</a></p><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列博客第 1 篇，共 10 章。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-di-yi-zhang-ji-yu-cuda-de-yi-gou-bing-xing-ji-suan&quot; tabindex=&quot;-1&quot;&gt;CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
    <category term="异构计算" scheme="https://smarter.xin/tags/heterogeneous-computing/"/>
    
    <category term="并行计算入门" scheme="https://smarter.xin/tags/parallel-computing-basics/"/>
    
  </entry>
  
  <entry>
    <title>CUDA C编程权威指南-导读</title>
    <link href="https://smarter.xin/posts/92dc1028/"/>
    <id>https://smarter.xin/posts/92dc1028/</id>
    <published>2026-02-05T01:00:00.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cuda-c-bian-cheng-quan-wei-zhi-nan-dao-du" tabindex="-1">CUDA C编程权威指南-导读</h1><p>本系列为《Professional CUDA C Programming》（中文版《CUDA C编程权威指南》，John Cheng、Max Grossman、Ty McKercher 著）的读书笔记与学习总结，共 10 章，从异构并行概念到多 GPU 与工程实践，形成一条完整的 CUDA 学习路径。</p><hr><h2 id="yi-ben-shu-yu-xi-lie-ding-wei" tabindex="-1" id="一、本书与系列定位">一、本书与系列定位</h2><ul><li><strong>原书</strong>：面向已有 C 基础、希望系统掌握 GPU 编程的读者，兼顾概念与实现，配有大量示例与性能分析。</li><li><strong>本系列</strong>：按章提炼要点、重难点与前后章衔接，并补充导图、自测题与部分图示，便于复习与自测；每章对应书中一章，节号与「书 X.Y 节」对应，便于与实体书对照。</li></ul><p>适合人群：准备系统学 CUDA 的开发者、上过并行计算/体系结构课想动手写 GPU 程序的学生、以及需要把现有 C 代码移植到 GPU 的工程师。</p><hr><h2 id="er-jian-yi-yue-du-shun-xu" tabindex="-1" id="二、建议阅读顺序">二、建议阅读顺序</h2><p>建议按<strong>第一章 → 第十章</strong>顺序阅读；每篇文末有「上一篇」「下一篇」链接，便于连续阅读。</p><table><thead><tr><th>序号</th><th>章节</th><th>链接</th></tr></thead><tbody><tr><td>0</td><td>导读（本文）</td><td>—</td></tr><tr><td>1</td><td>第一章：基于CUDA的异构并行计算</td><td><a href="https://smarter.xin/posts/2b2e171a/">第1章</a></td></tr><tr><td>2</td><td>第二章：CUDA编程模型</td><td><a href="https://smarter.xin/posts/bdd22b42/">第2章</a></td></tr><tr><td>3</td><td>第三章：CUDA执行模型</td><td><a href="https://smarter.xin/posts/bf3e0449/">第3章</a></td></tr><tr><td>4</td><td>第四章：全局内存</td><td><a href="https://smarter.xin/posts/1f074790/">第4章</a></td></tr><tr><td>5</td><td>第五章：共享内存和常量内存</td><td><a href="https://smarter.xin/posts/e1d6d733/">第5章</a></td></tr><tr><td>6</td><td>第六章：流和并发</td><td><a href="https://smarter.xin/posts/d47d974/">第6章</a></td></tr><tr><td>7</td><td>第七章：调整指令级原语</td><td><a href="https://smarter.xin/posts/1aaa52d4/">第7章</a></td></tr><tr><td>8</td><td>第八章：GPU加速库和OpenACC</td><td><a href="https://smarter.xin/posts/e466b0a5/">第8章</a></td></tr><tr><td>9</td><td>第九章：多GPU编程</td><td><a href="https://smarter.xin/posts/844cd057/">第9章</a></td></tr><tr><td>10</td><td>第十章：程序实现的注意事项</td><td><a href="https://smarter.xin/posts/98c27a24/">第10章</a></td></tr></tbody></table><hr><h2 id="san-ge-zhang-nei-rong-gai-lan" tabindex="-1" id="三、各章内容概览">三、各章内容概览</h2><table><thead><tr><th>章节</th><th>核心内容</th></tr></thead><tbody><tr><td><strong>第1章</strong></td><td>并行与异构、弗林分类、Host/Device、第一个 kernel</td></tr><tr><td><strong>第2章</strong></td><td>内存管理、网格与块、核函数、计时与设备管理</td></tr><tr><td><strong>第3章</strong></td><td>SM、线程束、分支分化、占用率、归约与展开</td></tr><tr><td><strong>第4章</strong></td><td>内存模型、对齐与合并、矩阵转置、有效带宽</td></tr><tr><td><strong>第5章</strong></td><td>共享内存、bank 冲突、转置/归约、常量内存、warp 洗牌</td></tr><tr><td><strong>第6章</strong></td><td>流与事件、重叠执行、同步、回调与优先级</td></tr><tr><td><strong>第7章</strong></td><td>warp 洗牌/表决/同步、循环展开、快速数学</td></tr><tr><td><strong>第8章</strong></td><td>cuBLAS/cuFFT/cuSPARSE/cuRAND、OpenACC</td></tr><tr><td><strong>第9章</strong></td><td>多设备、P2P、跨设备同步、CUDA-aware MPI、集群</td></tr><tr><td><strong>第10章</strong></td><td>开发流程、APOD、剖析、调试、C 到 CUDA 移植</td></tr></tbody></table><hr><blockquote><p><strong>本文为「CUDA C编程权威指南」系列导读。基于《Professional CUDA C Programming》by John Cheng, Max Grossman, Ty McKercher。</strong></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;cuda-c-bian-cheng-quan-wei-zhi-nan-dao-du&quot; tabindex=&quot;-1&quot;&gt;CUDA C编程权威指南-导读&lt;/h1&gt;
&lt;p&gt;本系列为《Professional CUDA C Programming》（中文版《CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="CUDA C编程权威指南" scheme="https://smarter.xin/categories/knowledge-sharing/cuda-c-programming-guide/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="读书笔记" scheme="https://smarter.xin/tags/reading-notes/"/>
    
    <category term="CUDA入门" scheme="https://smarter.xin/tags/cuda-basics/"/>
    
  </entry>
  
  <entry>
    <title>常见数据结构-图</title>
    <link href="https://smarter.xin/posts/74667ea3/"/>
    <id>https://smarter.xin/posts/74667ea3/</id>
    <published>2026-02-04T08:46:49.000Z</published>
    <updated>2026-03-16T05:46:43.133Z</updated>
    
    <content type="html"><![CDATA[<h1 id="tu-xiang-jie-jie-dian-yu-bian-de-lian-jie-yi-zhu" tabindex="-1">图详解：节点与边的连接艺术</h1><blockquote><p>图是描述&quot;关系&quot;的终极数据结构——社交网络、地图导航、依赖管理都离不开它</p></blockquote><hr><p>图在算法题中出现的频率很高，但很多人觉得它比树难理解。其实图可以看作是树的&quot;升级版&quot;——树是没有环的图，图则更加灵活，节点之间可以任意连接。</p><p>掌握图的关键是：<strong>理解存储方式 + 熟练运用 DFS/BFS</strong>。这两个遍历算法是解决几乎所有图问题的基础。</p><hr><h2 id="shi-yao-shi-tu" tabindex="-1" id="什么是图">什么是图</h2><p>图由<strong>节点（顶点）<strong>和</strong>边</strong>组成。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">    0 ---- 1</span><br><span class="line">    |      |</span><br><span class="line">    |      |</span><br><span class="line">    3 ---- 2</span><br><span class="line"></span><br><span class="line">节点：0, 1, 2, 3</span><br><span class="line">边：(0,1), (1,2), (2,3), (0,3)</span><br></pre></td></tr></table></figure><h3 id="tu-de-fen-lei" tabindex="-1" id="图的分类">图的分类</h3><table><thead><tr><th>类型</th><th>特点</th><th>例子</th></tr></thead><tbody><tr><td>无向图</td><td>边没有方向</td><td>社交网络的好友关系</td></tr><tr><td>有向图</td><td>边有方向</td><td>微博的关注关系</td></tr><tr><td>加权图</td><td>边有权重</td><td>地图上的距离</td></tr><tr><td>无环图(DAG)</td><td>有向图且没有环</td><td>课程依赖关系</td></tr></tbody></table><pre><code class="highlight mermaid">graph LR    subgraph 无向图    A((0)) --- B((1))    A --- C((2))    B --- C    end        subgraph 有向图    D((0)) --&gt; E((1))    E --&gt; F((2))    F --&gt; D    end        style A fill:#FFE4B5    style D fill:#87CEEB</code></pre><hr><h2 id="tu-de-cun-chu-fang-shi" tabindex="-1" id="图的存储方式">图的存储方式</h2><h3 id="lin-jie-ju-zhen" tabindex="-1" id="邻接矩阵">邻接矩阵</h3><p>用二维数组表示，<code>matrix[i][j] = 1</code> 表示 i 和 j 之间有边。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// n个节点的图</span></span><br><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">matrix</span>(n, <span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;(n, <span class="number">0</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加边 (无向图)</span></span><br><span class="line">matrix[<span class="number">0</span>][<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">matrix[<span class="number">1</span>][<span class="number">0</span>] = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">    0  1  2  3</span><br><span class="line">0 [ 0  1  0  1 ]</span><br><span class="line">1 [ 1  0  1  0 ]</span><br><span class="line">2 [ 0  1  0  1 ]</span><br><span class="line">3 [ 1  0  1  0 ]</span><br></pre></td></tr></table></figure><p><strong>优点</strong>：查询两点是否相连 O(1)<br><strong>缺点</strong>：空间 O(n²)，稀疏图浪费空间</p><h3 id="lin-jie-biao" tabindex="-1" id="邻接表">邻接表</h3><p>每个节点存储它的邻居列表。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// n个节点的图</span></span><br><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">adj</span>(n);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加边 (无向图)</span></span><br><span class="line">adj[<span class="number">0</span>].<span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">adj[<span class="number">1</span>].<span class="built_in">push_back</span>(<span class="number">0</span>);</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0 -&gt; [1, 3]</span><br><span class="line">1 -&gt; [0, 2]</span><br><span class="line">2 -&gt; [1, 3]</span><br><span class="line">3 -&gt; [0, 2]</span><br></pre></td></tr></table></figure><p><strong>优点</strong>：空间 O(V + E)，适合稀疏图<br><strong>缺点</strong>：查询两点是否相连需要遍历</p><h3 id="ru-he-xuan-ze" tabindex="-1" id="如何选择？">如何选择？</h3><table><thead><tr><th>场景</th><th>推荐方式</th></tr></thead><tbody><tr><td>稀疏图（边少）</td><td>邻接表</td></tr><tr><td>稠密图（边多）</td><td>邻接矩阵</td></tr><tr><td>需要快速判断连通</td><td>邻接矩阵</td></tr><tr><td>LeetCode 刷题</td><td>邻接表</td></tr></tbody></table><hr><h2 id="tu-de-bian-li-dfs" tabindex="-1" id="图的遍历：DFS">图的遍历：DFS</h2><p>深度优先搜索：一条路走到黑，走不通再回头。</p><h3 id="ji-ben-mo-ban" tabindex="-1" id="基本模板">基本模板</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">vector&lt;<span class="type">bool</span>&gt; visited;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; adj, <span class="type">int</span> node)</span> </span>&#123;</span><br><span class="line">    visited[node] = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> neighbor : adj[node]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!visited[neighbor]) &#123;</span><br><span class="line">            <span class="built_in">dfs</span>(adj, neighbor);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng" tabindex="-1" id="执行过程">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">图：</span><br><span class="line">0 -- 1</span><br><span class="line">|    |</span><br><span class="line">3 -- 2</span><br><span class="line"></span><br><span class="line">邻接表：</span><br><span class="line">0 -&gt; [1, 3]</span><br><span class="line">1 -&gt; [0, 2]</span><br><span class="line">2 -&gt; [1, 3]</span><br><span class="line">3 -&gt; [0, 2]</span><br><span class="line"></span><br><span class="line">从节点0开始 DFS：</span><br><span class="line"></span><br><span class="line">dfs(0)</span><br><span class="line">├── visited[0] = true</span><br><span class="line">├── 邻居1未访问 -&gt; dfs(1)</span><br><span class="line">│   ├── visited[1] = true</span><br><span class="line">│   ├── 邻居0已访问，跳过</span><br><span class="line">│   ├── 邻居2未访问 -&gt; dfs(2)</span><br><span class="line">│   │   ├── visited[2] = true</span><br><span class="line">│   │   ├── 邻居1已访问，跳过</span><br><span class="line">│   │   ├── 邻居3未访问 -&gt; dfs(3)</span><br><span class="line">│   │   │   ├── visited[3] = true</span><br><span class="line">│   │   │   ├── 所有邻居已访问</span><br><span class="line">│   │   │   └── 返回</span><br><span class="line">│   │   └── 返回</span><br><span class="line">│   └── 返回</span><br><span class="line">├── 邻居3已访问，跳过</span><br><span class="line">└── 返回</span><br><span class="line"></span><br><span class="line">遍历顺序：0 -&gt; 1 -&gt; 2 -&gt; 3</span><br></pre></td></tr></table></figure><hr><h2 id="tu-de-bian-li-bfs" tabindex="-1" id="图的遍历：BFS">图的遍历：BFS</h2><p>广度优先搜索：一层一层往外扩展，像水波一样。</p><h3 id="ji-ben-mo-ban-1" tabindex="-1" id="基本模板-2">基本模板</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">bfs</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; adj, <span class="type">int</span> start)</span> </span>&#123;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">bool</span>&gt; <span class="title">visited</span><span class="params">(adj.size(), <span class="literal">false</span>)</span></span>;</span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    </span><br><span class="line">    q.<span class="built_in">push</span>(start);</span><br><span class="line">    visited[start] = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="type">int</span> node = q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> neighbor : adj[node]) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!visited[neighbor]) &#123;</span><br><span class="line">                visited[neighbor] = <span class="literal">true</span>;</span><br><span class="line">                q.<span class="built_in">push</span>(neighbor);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-1" tabindex="-1" id="执行过程-2">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">同样的图，从节点0开始 BFS：</span><br><span class="line"></span><br><span class="line">初始：queue = [0], visited = &#123;0&#125;</span><br><span class="line"></span><br><span class="line">Step 1: 取出0，加入邻居1, 3</span><br><span class="line">        queue = [1, 3], visited = &#123;0, 1, 3&#125;</span><br><span class="line"></span><br><span class="line">Step 2: 取出1，加入邻居2（0已访问）</span><br><span class="line">        queue = [3, 2], visited = &#123;0, 1, 3, 2&#125;</span><br><span class="line"></span><br><span class="line">Step 3: 取出3，邻居都已访问</span><br><span class="line">        queue = [2]</span><br><span class="line"></span><br><span class="line">Step 4: 取出2，邻居都已访问</span><br><span class="line">        queue = []</span><br><span class="line"></span><br><span class="line">遍历顺序：0 -&gt; 1 -&gt; 3 -&gt; 2</span><br></pre></td></tr></table></figure><h3 id="dfs-vs-bfs" tabindex="-1" id="DFS-vs-BFS">DFS vs BFS</h3><table><thead><tr><th>特点</th><th>DFS</th><th>BFS</th></tr></thead><tbody><tr><td>数据结构</td><td>栈（递归/显式栈）</td><td>队列</td></tr><tr><td>空间复杂度</td><td>O(h) 递归深度</td><td>O(w) 最大宽度</td></tr><tr><td>适用场景</td><td>路径问题、连通性</td><td>最短路径、层级遍历</td></tr><tr><td>实现难度</td><td>递归简单</td><td>迭代简单</td></tr></tbody></table><hr><h2 id="jing-dian-wen-ti-dao-yu-shu-liang" tabindex="-1" id="经典问题：岛屿数量">经典问题：岛屿数量</h2><blockquote><p><strong>LeetCode 200</strong>: 给你一个由 ‘1’（陆地）和 ‘0’（水）组成的二维网格，请计算网格中岛屿的数量。</p></blockquote><h3 id="si-lu" tabindex="-1" id="思路">思路</h3><p>遇到一个 ‘1’，就用 DFS/BFS 把整个岛屿标记掉，然后计数 +1。</p><h3 id="dfs-jie-fa" tabindex="-1" id="DFS-解法">DFS 解法</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">numIslands</span><span class="params">(vector&lt;vector&lt;<span class="type">char</span>&gt;&gt;&amp; grid)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> m = grid.<span class="built_in">size</span>(), n = grid[<span class="number">0</span>].<span class="built_in">size</span>();</span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; m; ++i) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; n; ++j) &#123;</span><br><span class="line">            <span class="keyword">if</span> (grid[i][j] == <span class="string">&#x27;1&#x27;</span>) &#123;</span><br><span class="line">                <span class="built_in">dfs</span>(grid, i, j);</span><br><span class="line">                count++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(vector&lt;vector&lt;<span class="type">char</span>&gt;&gt;&amp; grid, <span class="type">int</span> i, <span class="type">int</span> j)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> m = grid.<span class="built_in">size</span>(), n = grid[<span class="number">0</span>].<span class="built_in">size</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 边界检查 + 是否是陆地</span></span><br><span class="line">    <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= m || j &lt; <span class="number">0</span> || j &gt;= n || grid[i][j] != <span class="string">&#x27;1&#x27;</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    grid[i][j] = <span class="string">&#x27;0&#x27;</span>;  <span class="comment">// 标记为已访问</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 四个方向</span></span><br><span class="line">    <span class="built_in">dfs</span>(grid, i + <span class="number">1</span>, j);</span><br><span class="line">    <span class="built_in">dfs</span>(grid, i - <span class="number">1</span>, j);</span><br><span class="line">    <span class="built_in">dfs</span>(grid, i, j + <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">dfs</span>(grid, i, j - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-2" tabindex="-1" id="执行过程-3">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">grid:</span><br><span class="line">1 1 0 0 0</span><br><span class="line">1 1 0 0 0</span><br><span class="line">0 0 1 0 0</span><br><span class="line">0 0 0 1 1</span><br><span class="line"></span><br><span class="line">遍历过程：</span><br><span class="line"></span><br><span class="line">(0,0)=&#x27;1&#x27; -&gt; DFS淹没整个岛屿 -&gt; count=1</span><br><span class="line">grid:</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 1 0 0</span><br><span class="line">0 0 0 1 1</span><br><span class="line"></span><br><span class="line">(2,2)=&#x27;1&#x27; -&gt; DFS淹没 -&gt; count=2</span><br><span class="line">grid:</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 1 1</span><br><span class="line"></span><br><span class="line">(3,3)=&#x27;1&#x27; -&gt; DFS淹没整个岛屿 -&gt; count=3</span><br><span class="line">grid:</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line">0 0 0 0 0</span><br><span class="line"></span><br><span class="line">答案：3</span><br></pre></td></tr></table></figure><p><strong>时间复杂度</strong>：O(m × n)<br><strong>空间复杂度</strong>：O(m × n) 最坏情况递归深度</p><hr><h2 id="jing-dian-wen-ti-ke-cheng-biao-tuo-bu-pai-xu" tabindex="-1" id="经典问题：课程表（拓扑排序）">经典问题：课程表（拓扑排序）</h2><blockquote><p><strong>LeetCode 207</strong>: 给定课程总数和先修关系，判断是否可能完成所有课程。</p></blockquote><h3 id="si-lu-1" tabindex="-1" id="思路-2">思路</h3><p>这是一个<strong>检测有向图是否有环</strong>的问题。如果有环，说明存在循环依赖，无法完成。</p><p>可以用 <strong>DFS 检测环</strong> 或 <strong>BFS 拓扑排序（Kahn算法）</strong>。</p><h3 id="bfs-tuo-bu-pai-xu" tabindex="-1" id="BFS-拓扑排序">BFS 拓扑排序</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">canFinish</span><span class="params">(<span class="type">int</span> numCourses, vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; prerequisites)</span> </span>&#123;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">indegree</span><span class="params">(numCourses, <span class="number">0</span>)</span></span>;</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">adj</span>(numCourses);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 建图 + 计算入度</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; p : prerequisites) &#123;</span><br><span class="line">        adj[p[<span class="number">1</span>]].<span class="built_in">push_back</span>(p[<span class="number">0</span>]);</span><br><span class="line">        indegree[p[<span class="number">0</span>]]++;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 入度为0的节点入队</span></span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; numCourses; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span> (indegree[i] == <span class="number">0</span>) &#123;</span><br><span class="line">            q.<span class="built_in">push</span>(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="type">int</span> course = q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        count++;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 删除这个节点的出边</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> next : adj[course]) &#123;</span><br><span class="line">            <span class="keyword">if</span> (--indegree[next] == <span class="number">0</span>) &#123;</span><br><span class="line">                q.<span class="built_in">push</span>(next);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> count == numCourses;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-3" tabindex="-1" id="执行过程-4">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">numCourses = 4</span><br><span class="line">prerequisites = [[1,0], [2,0], [3,1], [3,2]]</span><br><span class="line"></span><br><span class="line">含义：</span><br><span class="line">0 -&gt; 1 -&gt; 3</span><br><span class="line">0 -&gt; 2 -&gt; 3</span><br><span class="line"></span><br><span class="line">建图后：</span><br><span class="line">adj[0] = [1, 2]</span><br><span class="line">adj[1] = [3]</span><br><span class="line">adj[2] = [3]</span><br><span class="line">adj[3] = []</span><br><span class="line"></span><br><span class="line">入度：[0, 1, 1, 2]</span><br><span class="line"></span><br><span class="line">初始队列：[0]（入度为0）</span><br><span class="line"></span><br><span class="line">Step 1: 取出0，count=1</span><br><span class="line">        删除0的出边，更新入度</span><br><span class="line">        入度变为 [0, 0, 0, 2]</span><br><span class="line">        节点1, 2入队</span><br><span class="line">        queue = [1, 2]</span><br><span class="line"></span><br><span class="line">Step 2: 取出1，count=2</span><br><span class="line">        入度变为 [0, 0, 0, 1]</span><br><span class="line">        queue = [2]</span><br><span class="line"></span><br><span class="line">Step 3: 取出2，count=3</span><br><span class="line">        入度变为 [0, 0, 0, 0]</span><br><span class="line">        节点3入队</span><br><span class="line">        queue = [3]</span><br><span class="line"></span><br><span class="line">Step 4: 取出3，count=4</span><br><span class="line">        queue = []</span><br><span class="line"></span><br><span class="line">count(4) == numCourses(4) -&gt; true，可以完成！</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-ke-long-tu" tabindex="-1" id="经典问题：克隆图">经典问题：克隆图</h2><blockquote><p><strong>LeetCode 133</strong>: 深拷贝一个无向图。</p></blockquote><h3 id="si-lu-2" tabindex="-1" id="思路-3">思路</h3><p>用 HashMap 记录&quot;旧节点 -&gt; 新节点&quot;的映射，DFS 遍历时创建新节点。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    vector&lt;Node*&gt; neighbors;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">unordered_map&lt;Node*, Node*&gt; visited;</span><br><span class="line"></span><br><span class="line"><span class="function">Node* <span class="title">cloneGraph</span><span class="params">(Node* node)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!node) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果已经克隆过，直接返回</span></span><br><span class="line">    <span class="keyword">if</span> (visited.<span class="built_in">count</span>(node)) &#123;</span><br><span class="line">        <span class="keyword">return</span> visited[node];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 创建新节点</span></span><br><span class="line">    Node* clone = <span class="keyword">new</span> <span class="built_in">Node</span>(node-&gt;val);</span><br><span class="line">    visited[node] = clone;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 递归克隆邻居</span></span><br><span class="line">    <span class="keyword">for</span> (Node* neighbor : node-&gt;neighbors) &#123;</span><br><span class="line">        clone-&gt;neighbors.<span class="built_in">push_back</span>(<span class="built_in">cloneGraph</span>(neighbor));</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> clone;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-zui-duan-lu-jing" tabindex="-1" id="经典问题：最短路径">经典问题：最短路径</h2><blockquote><p><strong>LeetCode 1091</strong>: 在 n×n 的网格中，找从左上角到右下角的最短路径（可以走8个方向）。</p></blockquote><h3 id="si-lu-3" tabindex="-1" id="思路-4">思路</h3><p>BFS 天然适合求无权图的最短路径——第一次到达终点时的步数就是最短距离。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">shortestPathBinaryMatrix</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; grid)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> n = grid.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">if</span> (grid[<span class="number">0</span>][<span class="number">0</span>] == <span class="number">1</span> || grid[n<span class="number">-1</span>][n<span class="number">-1</span>] == <span class="number">1</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    </span><br><span class="line">    vector&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;&gt; dirs = &#123;</span><br><span class="line">        &#123;<span class="number">-1</span>,<span class="number">-1</span>&#125;, &#123;<span class="number">-1</span>,<span class="number">0</span>&#125;, &#123;<span class="number">-1</span>,<span class="number">1</span>&#125;,</span><br><span class="line">        &#123;<span class="number">0</span>,<span class="number">-1</span>&#125;,          &#123;<span class="number">0</span>,<span class="number">1</span>&#125;,</span><br><span class="line">        &#123;<span class="number">1</span>,<span class="number">-1</span>&#125;,  &#123;<span class="number">1</span>,<span class="number">0</span>&#125;,  &#123;<span class="number">1</span>,<span class="number">1</span>&#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    queue&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;&gt; q;</span><br><span class="line">    q.<span class="built_in">push</span>(&#123;<span class="number">0</span>, <span class="number">0</span>&#125;);</span><br><span class="line">    grid[<span class="number">0</span>][<span class="number">0</span>] = <span class="number">1</span>;  <span class="comment">// 标记距离（同时起到visited作用）</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="keyword">auto</span> [x, y] = q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (x == n<span class="number">-1</span> &amp;&amp; y == n<span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> grid[x][y];</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span> [dx, dy] : dirs) &#123;</span><br><span class="line">            <span class="type">int</span> nx = x + dx, ny = y + dy;</span><br><span class="line">            <span class="keyword">if</span> (nx &gt;= <span class="number">0</span> &amp;&amp; nx &lt; n &amp;&amp; ny &gt;= <span class="number">0</span> &amp;&amp; ny &lt; n &amp;&amp; grid[nx][ny] == <span class="number">0</span>) &#123;</span><br><span class="line">                grid[nx][ny] = grid[x][y] + <span class="number">1</span>;</span><br><span class="line">                q.<span class="built_in">push</span>(&#123;nx, ny&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="chang-jian-ji-qiao-zong-jie" tabindex="-1" id="常见技巧总结">常见技巧总结</h2><h3 id="1-fang-xiang-shu-zu" tabindex="-1" id="1-方向数组">1. 方向数组</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 四方向</span></span><br><span class="line"><span class="type">int</span> dx[] = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">-1</span>&#125;;</span><br><span class="line"><span class="type">int</span> dy[] = &#123;<span class="number">1</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 八方向</span></span><br><span class="line">vector&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;&gt; dirs = &#123;</span><br><span class="line">    &#123;<span class="number">-1</span>,<span class="number">-1</span>&#125;, &#123;<span class="number">-1</span>,<span class="number">0</span>&#125;, &#123;<span class="number">-1</span>,<span class="number">1</span>&#125;,</span><br><span class="line">    &#123;<span class="number">0</span>,<span class="number">-1</span>&#125;,          &#123;<span class="number">0</span>,<span class="number">1</span>&#125;,</span><br><span class="line">    &#123;<span class="number">1</span>,<span class="number">-1</span>&#125;,  &#123;<span class="number">1</span>,<span class="number">0</span>&#125;,  &#123;<span class="number">1</span>,<span class="number">1</span>&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="2-jian-tu-ji-qiao" tabindex="-1" id="2-建图技巧">2. 建图技巧</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 边列表 -&gt; 邻接表</span></span><br><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">adj</span>(n);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">auto</span>&amp; edge : edges) &#123;</span><br><span class="line">    adj[edge[<span class="number">0</span>]].<span class="built_in">push_back</span>(edge[<span class="number">1</span>]);</span><br><span class="line">    adj[edge[<span class="number">1</span>]].<span class="built_in">push_back</span>(edge[<span class="number">0</span>]);  <span class="comment">// 无向图</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-bi-mian-zhong-fu-fang-wen" tabindex="-1" id="3-避免重复访问">3. 避免重复访问</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方法1：visited 数组</span></span><br><span class="line"><span class="function">vector&lt;<span class="type">bool</span>&gt; <span class="title">visited</span><span class="params">(n, <span class="literal">false</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 方法2：直接修改原数组（适用于网格）</span></span><br><span class="line">grid[i][j] = <span class="string">&#x27;#&#x27;</span>;  <span class="comment">// 标记为已访问</span></span><br></pre></td></tr></table></figure><hr><h2 id="si-wei-dao-tu" tabindex="-1" id="思维导图">思维导图</h2><pre><code class="highlight mermaid">graph TD    A[图] --&gt; B[存储方式]    A --&gt; C[遍历算法]    A --&gt; D[经典问题]        B --&gt; B1[邻接矩阵]    B --&gt; B2[邻接表]        C --&gt; C1[DFS 深度优先]    C --&gt; C2[BFS 广度优先]        D --&gt; D1[岛屿问题]    D --&gt; D2[拓扑排序]    D --&gt; D3[最短路径]    D --&gt; D4[连通分量]        style A fill:#FFE4B5    style C fill:#90EE90    style D fill:#87CEEB</code></pre><hr><h2 id="zong-jie" tabindex="-1" id="总结">总结</h2><h3 id="he-xin-yao-dian" tabindex="-1" id="核心要点">核心要点</h3><ol><li><strong>邻接表是刷题首选</strong>：空间效率高，适合大多数场景</li><li><strong>DFS 用于路径/连通性问题</strong>：递归实现简洁</li><li><strong>BFS 用于最短路径问题</strong>：层序遍历的天然优势</li><li><strong>拓扑排序检测环</strong>：入度为 0 的节点逐个删除</li></ol><h3 id="leet-code-tui-jian-ti-dan" tabindex="-1" id="LeetCode-推荐题单">LeetCode 推荐题单</h3><p><strong>入门</strong>：</p><ul><li>200 岛屿数量</li><li>733 图像渲染</li><li>547 省份数量</li></ul><p><strong>进阶</strong>：</p><ul><li>207 / 210 课程表</li><li>133 克隆图</li><li>1091 二进制矩阵最短路径</li></ul><p><strong>挑战</strong>：</p><ul><li>743 网络延迟时间（Dijkstra）</li><li>787 K站中转最便宜航班</li><li>127 单词接龙</li></ul><p><strong>推荐阅读</strong>：</p><ul><li><a href="/posts/5948d104/">常见数据结构-二叉树</a> - 树是无环连通图的特例</li><li><a href="https://leetcode.cn/tag/graph/">LeetCode 图专题</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;tu-xiang-jie-jie-dian-yu-bian-de-lian-jie-yi-zhu&quot;</summary>
        
      
    
    
    
    <category term="算法" scheme="https://smarter.xin/categories/algorithm/"/>
    
    <category term="数据结构" scheme="https://smarter.xin/categories/algorithm/data-structure/"/>
    
    
    <category term="C++" scheme="https://smarter.xin/tags/cpp/"/>
    
    <category term="LeetCode" scheme="https://smarter.xin/tags/leetcode/"/>
    
    <category term="DFS" scheme="https://smarter.xin/tags/dfs/"/>
    
    <category term="BFS" scheme="https://smarter.xin/tags/bfs/"/>
    
    <category term="图" scheme="https://smarter.xin/tags/graph/"/>
    
    <category term="拓扑排序" scheme="https://smarter.xin/tags/topological-sort/"/>
    
  </entry>
  
  <entry>
    <title>UESTC校园网自动连接方案</title>
    <link href="https://smarter.xin/posts/1f06d3bd/"/>
    <id>https://smarter.xin/posts/1f06d3bd/</id>
    <published>2026-02-02T15:21:53.000Z</published>
    <updated>2026-03-16T05:46:43.098Z</updated>
    
    <content type="html"><![CDATA[<h1 id="han-jia-zai-jia-ye-neng-meng-meng-ke-yan-uestc-xiao-yuan-wang-zi-dong-bao-huo-fang-an-niu-ma-zhuan-yong-ban" tabindex="-1">寒假在家也能猛猛科研？UESTC 校园网自动保活方案（牛马专用版）</h1><h2 id="xie-zai-qian-mian" tabindex="-1" id="写在前面">写在前面</h2><p>放寒假了，作为一名致力于为祖国科研事业添砖加瓦的优秀研究生（指我），回家自然也是要远程控制教研室电脑“猛猛学习”的。但众所周知，学校不仅放假偶尔会断电，那个深澜校园网认证也是个时不时抽风的“磨人小妖精”。</p><p>这就导致了一个经典的死循环：</p><ol><li>师弟帮忙开了机（或者配置了 BIOS 上电自启）。</li><li>你满怀期待打开 ToDesk/RayLink/Tsun。</li><li><strong>显示设备离线</strong>。</li><li>因为校园网登录过期或者断网了，此时你只能对着屏幕流下悔恨的泪水，痛斥为什么不能通过意念写 Paper。</li></ol><p>为了保障各位英才的科研之路畅通无阻，不再被这点小事绊倒，特写了此脚本为祖国未来的人才保驾护航。</p><p>（注：断电重连的前提是你得先配置好 BIOS 的“来电自启/Restore on AC Power Loss”，这个大家根据自己的电脑品牌自行 Google，我就不展开了）</p><h2 id="ta-neng-gan-sha" tabindex="-1" id="它能干啥？">它能干啥？</h2><p>简单来说，这就是个帮你“盯梢”的脚本，主打一个<strong>死缠烂打，永不掉线</strong>。</p><ol><li><strong>开机自动认证</strong>：电脑只要一通电启动，脚本立马在后台静默运行，自动完成校园网认证。</li><li><strong>掉线自动重连</strong>：它会每隔几十秒（可配）悄悄 <code>ping</code> 一下外网。一旦发现网断了，立马触发重连机制，直到连上为止。</li><li><strong>微信状态通知</strong>：由于人在千里之外，看不见电脑屏幕。所以我加了企业微信通知。连上了、断开了、重连成功了，手机上都能第一时间收到消息，心里稳得一批。</li></ol><h2 id="shi-ce-xiao-guo" tabindex="-1" id="实测效果">实测效果</h2><ol><li><p><strong>断开校园网</strong><br><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-02-23-36-13-6bba8c.png" alt="image-20260202232705190"></p></li><li><p><strong>启动一次性登录脚本</strong><br><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-02-23-36-13-9f695b.png" alt="image-20260202232713619"></p></li><li><p><strong>持续监控</strong><br><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-02-23-36-13-b5fe62.png" alt="image-20260202232722139"></p></li></ol><h2 id="he-xin-yuan-li" tabindex="-1" id="核心原理">核心原理</h2><p>虽然初心是为了“牛马”方便，但代码写都写了，还是稍微正经聊两句实现逻辑。</p><h3 id="1-deng-lu-xie-yi-ni-xiang" tabindex="-1" id="1-登录协议逆向">1. 登录协议逆向</h3><p>其实深澜（Srun）的认证系统虽然前端花里胡哨，核心就是几个 HTTP 请求。我们只需要抓包分析一下它的挑战（Challenge）过程和加密算法。</p><p>主要流程大概是：</p><ul><li><code>get_challenge</code>: 获取 token 和 client ip。</li><li><code>hmd5/chksum</code>: 根据 token 对密码进行一顿加密操作（魔改的 MD5 和 Base64）。</li><li><code>srun_portal</code>: 带着加密后的 info 字段去请求登录接口。</li></ul><p>本项目对应的 <code>LoginManager</code> 封装了所有复杂的加密逻辑，你只需要填账号密码就行。</p><h3 id="2-zhuang-tai-jian-ce-daemon" tabindex="-1" id="2-状态监测-Daemon">2. 状态监测 Daemon</h3><p>光能登录不行，关键是得“保活”。写了个简单的死循环监控（Daemon）：</p><ul><li><strong>检测策略</strong>：优先 <code>ping</code> 指定 IP（比如阿里的 DNS），如果 ping 不通，再尝试 HTTP 请求（防止被 portal 劫持误判）。</li><li><strong>防抖动</strong>：不会一断网就疯狂请求，设置了重试阈值，只有连续几次失败才判定为真·断网，发起重连。</li></ul><h3 id="3-kai-ji-zi-qi-fang-an" tabindex="-1" id="3-开机自启方案">3. 开机自启方案</h3><p>为了让大家（包括我自己）用得爽，我写了个批处理脚本 <code>install_autostart.bat</code>。</p><p>它会调用 Windows 的 <code>schtasks</code> 命令，创建一个<strong>任务计划</strong>（名为 <code>UESTC-NET</code>）。</p><ul><li><strong>触发器</strong>：用户登录时（OnLogon）。</li><li><strong>权限</strong>：最高权限运行（防止部分系统权限限制导致从脚本无法执行）。</li><li><strong>隐蔽性</strong>：配合 <code>pythonw.exe</code>，后台静默运行，平时你根本感知不到它的存在。</li></ul><p><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-03-00-07-15-1d1351.png" alt="image-20260203000709246"></p><h2 id="zen-yao-yong" tabindex="-1" id="怎么用？">怎么用？</h2><p>代码我已经开源了，直接整就行。</p><h3 id="di-yi-bu-zhun-bei-gong-zuo" tabindex="-1" id="第一步：准备工作">第一步：准备工作</h3><ol><li><p><strong>下载项目</strong>：<a href="https://github.com/psmarter/UESTC-NET">GitHub 链接</a>，把代码拉到本地（建议放在非 C 盘目录下）。</p></li><li><p><strong>安装环境</strong>：你需要有 Python。然后运行 <code>pip install -r requirements.txt</code> 装一下依赖。</p></li><li><p><strong>填配置</strong>：<br>复制 <code>config.example.py</code> 为 <code>config.py</code>，主要修改以下几项：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;user&#x27;</span>: User(<span class="string">&#x27;202xxxxxxxx&#x27;</span>, <span class="string">&#x27;Password&#x27;</span>),  <span class="comment"># 你的学号和密码</span></span><br><span class="line"><span class="string">&#x27;ac_id&#x27;</span>: <span class="string">&#x27;1&#x27;</span>,  <span class="comment"># 主楼/实验室填 1，寝室填 3</span></span><br><span class="line"><span class="string">&#x27;domain&#x27;</span>: <span class="string">&#x27;@dx-uestc&#x27;</span>,  <span class="comment"># 默认校园网不用动（或者直接不填），电信改成 @dx</span></span><br></pre></td></tr></table></figure></li></ol><h3 id="di-er-bu-yi-jian-wan-cheng-tui-jian" tabindex="-1" id="第二步：一键完成-推荐">第二步：一键完成 (推荐)</h3><p>直接双击项目目录下的 <strong><code>install_autostart.bat</code></strong>。</p><ul><li>如果你有管理员权限，它会自动帮你建好任务计划。</li><li>如果没有，它会自动生成一个 VBS 脚本丢到启动文件夹里（也能用，就是没那么优雅）。</li></ul><p>看到“成功”俩字，这事就算成了。</p><p><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-03-00-13-17-0b6739.png" alt="image-20260202233159011"></p><h3 id="di-san-bu-shou-dong-dang-ying-he-wan-jia-ke-xuan" tabindex="-1" id="第三步：手动挡（硬核玩家可选）">第三步：手动挡（硬核玩家可选）</h3><p>如果你喜欢自己动手，或者脚本运行失败，可以用命令行手动配置：</p><p><strong>方案 A：任务计划程序 (推荐)</strong></p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">schtasks /create /tn &quot;UESTC-<span class="built_in">NET</span>&quot; /tr &quot;\&quot;&lt;Python路径&gt;\pythonw.exe\&quot; \&quot;&lt;项目路径&gt;\\always_online.py\&quot;&quot; /sc onlogon /rl HIGHEST /f</span><br></pre></td></tr></table></figure><p><img src="https://cdn.jsdelivr.net/gh/psmarter/blog-imgs/UESTC%E6%A0%A1%E5%9B%AD%E7%BD%91%E8%87%AA%E5%8A%A8%E8%BF%9E%E6%8E%A5%E6%96%B9%E6%A1%88/2026-02-03-00-12-57-bc1be9.png" alt="image-20260202233536593"></p><p><em>(注：请将 <code>&lt;Python路径&gt;</code> 替换为你的 <code>python.exe</code> 所在目录，<code>&lt;项目路径&gt;</code> 替换为代码实际路径)</em></p><p><strong>方案 B：NSSM 服务</strong><br>如果你有洁癖，想把它做成系统服务：</p><figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">nssm install UESTC-<span class="built_in">NET</span> &quot;&lt;Python路径&gt;\pythonw.exe&quot; &quot;&lt;项目路径&gt;\\always_online.py&quot;</span><br><span class="line">nssm <span class="built_in">set</span> UESTC-<span class="built_in">NET</span> AppDirectory &quot;&lt;项目路径&gt;&quot;</span><br><span class="line">nssm <span class="built_in">set</span> UESTC-<span class="built_in">NET</span> <span class="built_in">Start</span> SERVICE_AUTO_START</span><br></pre></td></tr></table></figure><h2 id="chang-jian-wen-ti-q-amp-a" tabindex="-1" id="常见问题-Q-A">常见问题 (Q&amp;A)</h2><p><strong>Q: 运行报错“缺少 requests 模块”？</strong><br>A: 请确保你运行了 <code>pip install -r requirements.txt</code>，并且 <code>always_online.py</code> 使用的 Python 环境和你安装依赖的环境是同一个。</p><p><strong>Q: 怎么看它有没有在运行？</strong><br>A: 打开任务管理器，找找有没有 <code>pythonw.exe</code> 进程。或者去 <code>logs/</code> 目录下看看有没有当天的日志文件。</p><p><strong>Q: 我想卸载怎么办？</strong><br>A: 项目里贴心地准备了 <code>uninstall.bat</code>，双击运行即可一键清理任务和文件。清理完后，整个文件夹直接删掉就行。</p><h2 id="jie-yu" tabindex="-1" id="结语">结语</h2><p>工具是造出来了，至于有没有由于网络过于通畅，导致寒假真的写了很多代码/论文…那就是另一个悲伤的故事了。</p><p>希望能帮到同样需要在假期远程“搬砖”的各位。<strong>为了科研（毕业），冲！</strong></p><hr><blockquote><p>项目地址：<a href="https://github.com/psmarter/UESTC-NET">https://github.com/psmarter/UESTC-NET</a><br>如果觉得好用，点个 Star 也是极好的。</p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;han-jia-zai-jia-ye-neng-meng-meng-ke-yan-uestc-xiao-yuan-wang-zi-dong-bao-huo-fang-an-niu-ma-zhuan-yong-ban&quot;</summary>
        
      
    
    
    
    <category term="UESTC" scheme="https://smarter.xin/categories/uestc/"/>
    
    
    <category term="Python" scheme="https://smarter.xin/tags/python/"/>
    
    <category term="校园网" scheme="https://smarter.xin/tags/campus-network/"/>
    
    <category term="深澜" scheme="https://smarter.xin/tags/deepin/"/>
    
    <category term="网络监控" scheme="https://smarter.xin/tags/network-monitoring/"/>
    
    <category term="脚本" scheme="https://smarter.xin/tags/script/"/>
    
    <category term="开机自启" scheme="https://smarter.xin/tags/autostart/"/>
    
  </entry>
  
  <entry>
    <title>常见数据结构-链表</title>
    <link href="https://smarter.xin/posts/be40069a/"/>
    <id>https://smarter.xin/posts/be40069a/</id>
    <published>2026-02-02T09:23:33.000Z</published>
    <updated>2026-03-16T05:46:43.133Z</updated>
    
    <content type="html"><![CDATA[<h1 id="lian-biao-xiang-jie-zhi-zhen-de-chuan-zhen-yin-xian" tabindex="-1">链表详解：指针的穿针引线</h1><blockquote><p>画图是调试链表的最好方法，每一步指针变化都画出来！</p></blockquote><hr><p>链表是我刷题过程中觉得最&quot;绕&quot;的数据结构之一。不像数组那样可以直接用下标访问，链表的操作全靠指针穿针引线。刚开始经常写出死循环或者丢失节点，后来发现画图是最好的办法——把每一步指针的指向都画出来，问题就清晰了。</p><p>链表题目的核心技巧就那么几个：虚拟头节点、快慢指针、反转链表，掌握这些，大部分题目都能解决。</p><hr><h2 id="shi-yao-shi-lian-biao" tabindex="-1" id="什么是链表">什么是链表</h2><p>链表是一种线性数据结构，每个元素（节点）包含数据和指向下一个节点的指针。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">单链表：</span><br><span class="line">  head → [1] → [2] → [3] → [4] → null</span><br><span class="line">          ↑     ↑     ↑     ↑</span><br><span class="line">         节点  节点  节点  节点</span><br><span class="line"></span><br><span class="line">双向链表：</span><br><span class="line">  null ← [1] ⇄ [2] ⇄ [3] ⇄ [4] → null</span><br></pre></td></tr></table></figure><h3 id="jie-dian-ding-yi" tabindex="-1" id="节点定义">节点定义</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 单链表节点</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ListNode</span> &#123;</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    ListNode* next;</span><br><span class="line">    <span class="built_in">ListNode</span>(<span class="type">int</span> x) : <span class="built_in">val</span>(x), <span class="built_in">next</span>(<span class="literal">nullptr</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 双向链表节点</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">DoublyListNode</span> &#123;</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    DoublyListNode* prev;</span><br><span class="line">    DoublyListNode* next;</span><br><span class="line">    <span class="built_in">DoublyListNode</span>(<span class="type">int</span> x) : <span class="built_in">val</span>(x), <span class="built_in">prev</span>(<span class="literal">nullptr</span>), <span class="built_in">next</span>(<span class="literal">nullptr</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="lian-biao-vs-shu-zu" tabindex="-1" id="链表-vs-数组">链表 vs 数组</h3><table><thead><tr><th>操作</th><th>数组</th><th>链表</th></tr></thead><tbody><tr><td>随机访问</td><td>O(1)</td><td>O(n)</td></tr><tr><td>头部插入</td><td>O(n)</td><td>O(1)</td></tr><tr><td>尾部插入</td><td>O(1)</td><td>O(n)*</td></tr><tr><td>中间插入</td><td>O(n)</td><td>O(1)**</td></tr><tr><td>内存布局</td><td>连续</td><td>分散</td></tr></tbody></table><p>*尾部插入如果有尾指针则是O(1)<br>**找到位置后插入是O(1)，找位置需要O(n)</p><hr><h2 id="ji-ben-cao-zuo" tabindex="-1" id="基本操作">基本操作</h2><h3 id="bian-li-lian-biao" tabindex="-1" id="遍历链表">遍历链表</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">traverse</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    ListNode* curr = head;</span><br><span class="line">    <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">        cout &lt;&lt; curr-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">        curr = curr-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zai-tou-bu-cha-ru" tabindex="-1" id="在头部插入">在头部插入</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">insertAtHead</span><span class="params">(ListNode* head, <span class="type">int</span> val)</span> </span>&#123;</span><br><span class="line">    ListNode* newNode = <span class="keyword">new</span> <span class="built_in">ListNode</span>(val);</span><br><span class="line">    newNode-&gt;next = head;</span><br><span class="line">    <span class="keyword">return</span> newNode;  <span class="comment">// 新的头节点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zai-wei-bu-cha-ru" tabindex="-1" id="在尾部插入">在尾部插入</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">insertAtTail</span><span class="params">(ListNode* head, <span class="type">int</span> val)</span> </span>&#123;</span><br><span class="line">    ListNode* newNode = <span class="keyword">new</span> <span class="built_in">ListNode</span>(val);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!head) <span class="keyword">return</span> newNode;</span><br><span class="line">    </span><br><span class="line">    ListNode* curr = head;</span><br><span class="line">    <span class="keyword">while</span> (curr-&gt;next) &#123;</span><br><span class="line">        curr = curr-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    curr-&gt;next = newNode;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="shan-chu-jie-dian" tabindex="-1" id="删除节点">删除节点</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">deleteNode</span><span class="params">(ListNode* head, <span class="type">int</span> val)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 虚拟头节点，简化边界处理</span></span><br><span class="line">    <span class="function">ListNode <span class="title">dummy</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">    dummy.next = head;</span><br><span class="line">    </span><br><span class="line">    ListNode* prev = &amp;dummy;</span><br><span class="line">    ListNode* curr = head;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">        <span class="keyword">if</span> (curr-&gt;val == val) &#123;</span><br><span class="line">            prev-&gt;next = curr-&gt;next;</span><br><span class="line">            <span class="keyword">delete</span> curr;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        prev = curr;</span><br><span class="line">        curr = curr-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> dummy.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="he-xin-ji-qiao-xu-ni-tou-jie-dian" tabindex="-1" id="核心技巧：虚拟头节点">核心技巧：虚拟头节点</h2><p>链表操作中，头节点经常需要特殊处理。使用**虚拟头节点（dummy node）**可以简化代码，统一操作逻辑。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 没有虚拟头节点：需要单独处理头节点</span></span><br><span class="line"><span class="function">ListNode* <span class="title">removeElements</span><span class="params">(ListNode* head, <span class="type">int</span> val)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 删除头部所有等于val的节点</span></span><br><span class="line">    <span class="keyword">while</span> (head &amp;&amp; head-&gt;val == val) &#123;</span><br><span class="line">        head = head-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!head) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    </span><br><span class="line">    ListNode* curr = head;</span><br><span class="line">    <span class="keyword">while</span> (curr-&gt;next) &#123;</span><br><span class="line">        <span class="keyword">if</span> (curr-&gt;next-&gt;val == val) &#123;</span><br><span class="line">            curr-&gt;next = curr-&gt;next-&gt;next;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            curr = curr-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用虚拟头节点：代码更简洁</span></span><br><span class="line"><span class="function">ListNode* <span class="title">removeElements</span><span class="params">(ListNode* head, <span class="type">int</span> val)</span> </span>&#123;</span><br><span class="line">    <span class="function">ListNode <span class="title">dummy</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">    dummy.next = head;</span><br><span class="line">    </span><br><span class="line">    ListNode* curr = &amp;dummy;</span><br><span class="line">    <span class="keyword">while</span> (curr-&gt;next) &#123;</span><br><span class="line">        <span class="keyword">if</span> (curr-&gt;next-&gt;val == val) &#123;</span><br><span class="line">            curr-&gt;next = curr-&gt;next-&gt;next;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            curr = curr-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> dummy.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="he-xin-ji-qiao-kuai-man-zhi-zhen" tabindex="-1" id="核心技巧：快慢指针">核心技巧：快慢指针</h2><p>快慢指针是链表中最常用的技巧之一。</p><h3 id="zhao-zhong-jian-jie-dian" tabindex="-1" id="找中间节点">找中间节点</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">findMiddle</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    ListNode* slow = head;</span><br><span class="line">    ListNode* fast = head;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (fast &amp;&amp; fast-&gt;next) &#123;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> slow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行过程：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">链表: 1 → 2 → 3 → 4 → 5</span><br><span class="line"></span><br><span class="line">初始:  slow=1, fast=1</span><br><span class="line">第1步: slow=2, fast=3</span><br><span class="line">第2步: slow=3, fast=5</span><br><span class="line">第3步: fast-&gt;next=null，停止</span><br><span class="line"></span><br><span class="line">中间节点: 3</span><br></pre></td></tr></table></figure><h3 id="pan-duan-shi-fou-you-huan" tabindex="-1" id="判断是否有环">判断是否有环</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">hasCycle</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    ListNode* slow = head;</span><br><span class="line">    ListNode* fast = head;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (fast &amp;&amp; fast-&gt;next) &#123;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (slow == fast) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;  <span class="comment">// 相遇说明有环</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhao-huan-de-ru-kou" tabindex="-1" id="找环的入口">找环的入口</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">detectCycle</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    ListNode* slow = head;</span><br><span class="line">    ListNode* fast = head;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 第一步：找到相遇点</span></span><br><span class="line">    <span class="keyword">while</span> (fast &amp;&amp; fast-&gt;next) &#123;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (slow == fast) &#123;</span><br><span class="line">            <span class="comment">// 第二步：从head和相遇点同时出发</span></span><br><span class="line">            ListNode* p1 = head;</span><br><span class="line">            ListNode* p2 = slow;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">while</span> (p1 != p2) &#123;</span><br><span class="line">                p1 = p1-&gt;next;</span><br><span class="line">                p2 = p2-&gt;next;</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">return</span> p1;  <span class="comment">// 环的入口</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>为什么这样能找到环入口？这涉及到一点数学推导，简单来说：设头到环入口距离为a，环入口到相遇点距离为b，环的长度为c。相遇时slow走了a+b，fast走了a+b+nc（n圈）。因为fast是slow的两倍速度，所以2(a+b) = a+b+nc，得到a = nc-b。这意味着从头走a步和从相遇点走a步会在入口相遇。</p><hr><h2 id="he-xin-ji-qiao-fan-zhuan-lian-biao" tabindex="-1" id="核心技巧：反转链表">核心技巧：反转链表</h2><p>反转链表是链表操作的基础，很多题目都会用到。</p><h3 id="die-dai-fa" tabindex="-1" id="迭代法">迭代法</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">reverseList</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    ListNode* prev = <span class="literal">nullptr</span>;</span><br><span class="line">    ListNode* curr = head;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">        ListNode* next = curr-&gt;next;  <span class="comment">// 保存下一个节点</span></span><br><span class="line">        curr-&gt;next = prev;             <span class="comment">// 反转指针</span></span><br><span class="line">        prev = curr;                   <span class="comment">// prev前进</span></span><br><span class="line">        curr = next;                   <span class="comment">// curr前进</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> prev;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行过程：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">原链表: 1 → 2 → 3 → null</span><br><span class="line"></span><br><span class="line">初始: prev=null, curr=1</span><br><span class="line"></span><br><span class="line">Step 1: next=2, 1→null, prev=1, curr=2</span><br><span class="line">        null ← 1   2 → 3 → null</span><br><span class="line"></span><br><span class="line">Step 2: next=3, 2→1, prev=2, curr=3</span><br><span class="line">        null ← 1 ← 2   3 → null</span><br><span class="line"></span><br><span class="line">Step 3: next=null, 3→2, prev=3, curr=null</span><br><span class="line">        null ← 1 ← 2 ← 3</span><br><span class="line"></span><br><span class="line">结果: 3 → 2 → 1 → null</span><br></pre></td></tr></table></figure><h3 id="di-gui-fa" tabindex="-1" id="递归法">递归法</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">reverseList</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!head || !head-&gt;next) &#123;</span><br><span class="line">        <span class="keyword">return</span> head;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ListNode* newHead = <span class="built_in">reverseList</span>(head-&gt;next);</span><br><span class="line">    head-&gt;next-&gt;next = head;  <span class="comment">// 让下一个节点指向自己</span></span><br><span class="line">    head-&gt;next = <span class="literal">nullptr</span>;      <span class="comment">// 断开原来的指向</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> newHead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-he-bing-liang-ge-you-xu-lian-biao" tabindex="-1" id="经典问题：合并两个有序链表">经典问题：合并两个有序链表</h2><blockquote><p><strong>LeetCode 21</strong>: 将两个升序链表合并为一个新的升序链表。</p></blockquote><h3 id="die-dai-fa-1" tabindex="-1" id="迭代法-2">迭代法</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">mergeTwoLists</span><span class="params">(ListNode* l1, ListNode* l2)</span> </span>&#123;</span><br><span class="line">    <span class="function">ListNode <span class="title">dummy</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">    ListNode* curr = &amp;dummy;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (l1 &amp;&amp; l2) &#123;</span><br><span class="line">        <span class="keyword">if</span> (l1-&gt;val &lt;= l2-&gt;val) &#123;</span><br><span class="line">            curr-&gt;next = l1;</span><br><span class="line">            l1 = l1-&gt;next;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            curr-&gt;next = l2;</span><br><span class="line">            l2 = l2-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        curr = curr-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 接上剩余部分</span></span><br><span class="line">    curr-&gt;next = l1 ? l1 : l2;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> dummy.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng" tabindex="-1" id="执行过程">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">l1: 1 → 2 → 4</span><br><span class="line">l2: 1 → 3 → 4</span><br><span class="line"></span><br><span class="line">Step 1: 1 &lt;= 1, 取l1的1</span><br><span class="line">        dummy → 1</span><br><span class="line"></span><br><span class="line">Step 2: 2 &gt; 1, 取l2的1</span><br><span class="line">        dummy → 1 → 1</span><br><span class="line"></span><br><span class="line">Step 3: 2 &lt;= 3, 取l1的2</span><br><span class="line">        dummy → 1 → 1 → 2</span><br><span class="line"></span><br><span class="line">Step 4: 4 &gt; 3, 取l2的3</span><br><span class="line">        dummy → 1 → 1 → 2 → 3</span><br><span class="line"></span><br><span class="line">Step 5: 4 &lt;= 4, 取l1的4</span><br><span class="line">        dummy → 1 → 1 → 2 → 3 → 4</span><br><span class="line"></span><br><span class="line">Step 6: l1为空，接上l2剩余的4</span><br><span class="line">        dummy → 1 → 1 → 2 → 3 → 4 → 4</span><br><span class="line"></span><br><span class="line">结果: 1 → 1 → 2 → 3 → 4 → 4</span><br></pre></td></tr></table></figure><p><strong>时间复杂度</strong>: O(m + n)<br><strong>空间复杂度</strong>: O(1)</p><hr><h2 id="jing-dian-wen-ti-shan-chu-dao-shu-di-n-ge-jie-dian" tabindex="-1" id="经典问题：删除倒数第N个节点">经典问题：删除倒数第N个节点</h2><blockquote><p><strong>LeetCode 19</strong>: 删除链表的倒数第N个节点。</p></blockquote><h3 id="si-lu" tabindex="-1" id="思路">思路</h3><p>用快慢指针：让fast先走N步，然后fast和slow一起走，当fast到达末尾时，slow刚好在倒数第N个节点的前一个位置。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">removeNthFromEnd</span><span class="params">(ListNode* head, <span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="function">ListNode <span class="title">dummy</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">    dummy.next = head;</span><br><span class="line">    </span><br><span class="line">    ListNode* fast = &amp;dummy;</span><br><span class="line">    ListNode* slow = &amp;dummy;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// fast先走n+1步</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;= n; ++i) &#123;</span><br><span class="line">        fast = fast-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 一起走，直到fast到达末尾</span></span><br><span class="line">    <span class="keyword">while</span> (fast) &#123;</span><br><span class="line">        fast = fast-&gt;next;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 删除slow的下一个节点</span></span><br><span class="line">    slow-&gt;next = slow-&gt;next-&gt;next;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> dummy.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-1" tabindex="-1" id="执行过程-2">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">链表: 1 → 2 → 3 → 4 → 5, n=2</span><br><span class="line"></span><br><span class="line">dummy → 1 → 2 → 3 → 4 → 5 → null</span><br><span class="line"></span><br><span class="line">Step 1: fast先走3步 (n+1=3)</span><br><span class="line">        fast指向3</span><br><span class="line"></span><br><span class="line">Step 2: 一起走</span><br><span class="line">        fast=4, slow=1</span><br><span class="line">        fast=5, slow=2</span><br><span class="line">        fast=null, slow=3</span><br><span class="line"></span><br><span class="line">Step 3: 删除slow的下一个节点(4)</span><br><span class="line">        3 → 5</span><br><span class="line"></span><br><span class="line">结果: 1 → 2 → 3 → 5</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-xiang-jiao-lian-biao" tabindex="-1" id="经典问题：相交链表">经典问题：相交链表</h2><blockquote><p><strong>LeetCode 160</strong>: 找到两个单链表相交的起始节点。</p></blockquote><h3 id="si-lu-1" tabindex="-1" id="思路-2">思路</h3><p>让两个指针分别从两个链表头出发，走到末尾后跳到另一个链表头继续走。如果有交点，它们会在交点相遇；如果没有交点，它们会同时到达null。</p><p>原理：设链表A长度为a，链表B长度为b，公共部分长度为c。指针1走的路径：a + (b-c)，指针2走的路径：b + (a-c)。两者长度相等，所以会同时到达交点。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">getIntersectionNode</span><span class="params">(ListNode* headA, ListNode* headB)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!headA || !headB) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    </span><br><span class="line">    ListNode* pA = headA;</span><br><span class="line">    ListNode* pB = headB;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (pA != pB) &#123;</span><br><span class="line">        pA = pA ? pA-&gt;next : headB;</span><br><span class="line">        pB = pB ? pB-&gt;next : headA;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> pA;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-2" tabindex="-1" id="执行过程-3">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">A: 1 → 2 ↘</span><br><span class="line">           6 → 7</span><br><span class="line">B: 3 → 4 → 5 ↗</span><br><span class="line"></span><br><span class="line">pA的路径: 1 → 2 → 6 → 7 → null → 3 → 4 → 5 → 6</span><br><span class="line">pB的路径: 3 → 4 → 5 → 6 → 7 → null → 1 → 2 → 6</span><br><span class="line"></span><br><span class="line">相遇点: 6</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-hui-wen-lian-biao" tabindex="-1" id="经典问题：回文链表">经典问题：回文链表</h2><blockquote><p><strong>LeetCode 234</strong>: 判断链表是否是回文链表。</p></blockquote><h3 id="si-lu-2" tabindex="-1" id="思路-3">思路</h3><ol><li>用快慢指针找到中点</li><li>反转后半部分</li><li>比较前半部分和后半部分</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isPalindrome</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!head || !head-&gt;next) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1. 找中点</span></span><br><span class="line">    ListNode* slow = head;</span><br><span class="line">    ListNode* fast = head;</span><br><span class="line">    <span class="keyword">while</span> (fast-&gt;next &amp;&amp; fast-&gt;next-&gt;next) &#123;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 反转后半部分</span></span><br><span class="line">    ListNode* secondHalf = <span class="built_in">reverseList</span>(slow-&gt;next);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. 比较</span></span><br><span class="line">    ListNode* p1 = head;</span><br><span class="line">    ListNode* p2 = secondHalf;</span><br><span class="line">    <span class="type">bool</span> result = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (p2) &#123;</span><br><span class="line">        <span class="keyword">if</span> (p1-&gt;val != p2-&gt;val) &#123;</span><br><span class="line">            result = <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        p1 = p1-&gt;next;</span><br><span class="line">        p2 = p2-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 4. 恢复链表（可选）</span></span><br><span class="line">    slow-&gt;next = <span class="built_in">reverseList</span>(secondHalf);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-3" tabindex="-1" id="执行过程-4">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">链表: 1 → 2 → 2 → 1</span><br><span class="line"></span><br><span class="line">找中点: slow=2 (第一个2)</span><br><span class="line"></span><br><span class="line">反转后半部分: 2 → 1 变成 1 → 2</span><br><span class="line"></span><br><span class="line">比较:</span><br><span class="line">  p1=1, p2=1 ✓</span><br><span class="line">  p1=2, p2=2 ✓</span><br><span class="line"></span><br><span class="line">结果: true</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-pai-xu-lian-biao" tabindex="-1" id="经典问题：排序链表">经典问题：排序链表</h2><blockquote><p><strong>LeetCode 148</strong>: 对链表进行排序，要求O(n log n)时间复杂度。</p></blockquote><h3 id="si-lu-3" tabindex="-1" id="思路-4">思路</h3><p>归并排序最适合链表：找中点、分割、递归排序、合并。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">ListNode* <span class="title">sortList</span><span class="params">(ListNode* head)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!head || !head-&gt;next) <span class="keyword">return</span> head;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 1. 找中点并分割</span></span><br><span class="line">    ListNode* slow = head;</span><br><span class="line">    ListNode* fast = head-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (fast &amp;&amp; fast-&gt;next) &#123;</span><br><span class="line">        slow = slow-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ListNode* mid = slow-&gt;next;</span><br><span class="line">    slow-&gt;next = <span class="literal">nullptr</span>;  <span class="comment">// 断开</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 递归排序</span></span><br><span class="line">    ListNode* left = <span class="built_in">sortList</span>(head);</span><br><span class="line">    ListNode* right = <span class="built_in">sortList</span>(mid);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 3. 合并</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">mergeTwoLists</span>(left, right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="chang-jian-xian-jing" tabindex="-1" id="常见陷阱">常见陷阱</h2><h3 id="1-wang-ji-bao-cun-next-zhi-zhen" tabindex="-1" id="1-忘记保存next指针">1. 忘记保存next指针</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ❌ 错误：修改指针前没有保存next</span></span><br><span class="line"><span class="keyword">while</span> (curr) &#123;</span><br><span class="line">    curr-&gt;next = prev;</span><br><span class="line">    prev = curr;</span><br><span class="line">    curr = curr-&gt;next;  <span class="comment">// 已经被修改了！</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 正确：先保存next</span></span><br><span class="line"><span class="keyword">while</span> (curr) &#123;</span><br><span class="line">    ListNode* next = curr-&gt;next;  <span class="comment">// 先保存</span></span><br><span class="line">    curr-&gt;next = prev;</span><br><span class="line">    prev = curr;</span><br><span class="line">    curr = next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-kong-zhi-zhen-fang-wen" tabindex="-1" id="2-空指针访问">2. 空指针访问</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ❌ 危险：没有检查curr是否为空</span></span><br><span class="line"><span class="keyword">while</span> (curr-&gt;next) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 安全：先检查curr</span></span><br><span class="line"><span class="keyword">while</span> (curr &amp;&amp; curr-&gt;next) &#123; ... &#125;</span><br></pre></td></tr></table></figure><h3 id="3-wang-ji-chu-li-tou-jie-dian" tabindex="-1" id="3-忘记处理头节点">3. 忘记处理头节点</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用虚拟头节点可以避免很多边界问题</span></span><br><span class="line"><span class="function">ListNode <span class="title">dummy</span><span class="params">(<span class="number">0</span>)</span></span>;</span><br><span class="line">dummy.next = head;</span><br></pre></td></tr></table></figure><hr><h2 id="si-wei-dao-tu" tabindex="-1" id="思维导图">思维导图</h2><pre><code class="highlight mermaid">graph TD    A[链表] --&gt; B[基本操作]    A --&gt; C[核心技巧]    A --&gt; D[经典问题]        B --&gt; B1[遍历]    B --&gt; B2[插入/删除]        C --&gt; C1[虚拟头节点]    C --&gt; C2[快慢指针]    C --&gt; C3[反转链表]        D --&gt; D1[合并链表]    D --&gt; D2[环检测]    D --&gt; D3[回文链表]    D --&gt; D4[排序链表]        style A fill:#FFE4B5    style C fill:#90EE90    style D fill:#87CEEB</code></pre><hr><h2 id="zong-jie" tabindex="-1" id="总结">总结</h2><h3 id="he-xin-yao-dian" tabindex="-1" id="核心要点">核心要点</h3><ol><li><strong>画图是最好的调试方法</strong>：每一步指针变化都画出来</li><li><strong>虚拟头节点简化边界处理</strong>：特别是涉及删除头节点的情况</li><li><strong>快慢指针解决大量问题</strong>：找中点、检测环、倒数第N个</li><li><strong>反转链表是基础操作</strong>：很多题目都会用到</li></ol><h3 id="leet-code-tui-jian-ti-dan" tabindex="-1" id="LeetCode推荐题单">LeetCode推荐题单</h3><p><strong>入门</strong>：</p><ul><li>206 反转链表</li><li>21 合并两个有序链表</li><li>141 环形链表</li></ul><p><strong>进阶</strong>：</p><ul><li>19 删除倒数第N个节点</li><li>160 相交链表</li><li>234 回文链表</li></ul><p><strong>挑战</strong>：</p><ul><li>25 K个一组反转链表</li><li>148 排序链表</li><li>146 LRU缓存</li></ul><p><strong>推荐阅读</strong>：</p><ul><li><a href="/posts/5948d104/">常见数据结构-二叉树</a> - 树和链表的遍历思想相通</li><li><a href="https://leetcode.cn/tag/linked-list/">LeetCode 链表专题</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;lian-biao-xiang-jie-zhi-zhen-de-chuan-zhen-yin-xian&quot;</summary>
        
      
    
    
    
    <category term="算法" scheme="https://smarter.xin/categories/algorithm/"/>
    
    <category term="数据结构" scheme="https://smarter.xin/categories/algorithm/data-structure/"/>
    
    
    <category term="C++" scheme="https://smarter.xin/tags/cpp/"/>
    
    <category term="LeetCode" scheme="https://smarter.xin/tags/leetcode/"/>
    
    <category term="递归" scheme="https://smarter.xin/tags/recursion/"/>
    
    <category term="链表" scheme="https://smarter.xin/tags/linked-list/"/>
    
    <category term="双指针" scheme="https://smarter.xin/tags/two-pointers/"/>
    
  </entry>
  
  <entry>
    <title>常见数据结构-二叉树</title>
    <link href="https://smarter.xin/posts/5948d104/"/>
    <id>https://smarter.xin/posts/5948d104/</id>
    <published>2026-02-02T09:08:27.000Z</published>
    <updated>2026-03-16T05:46:43.133Z</updated>
    
    <content type="html"><![CDATA[<h1 id="er-cha-shu-xiang-jie-di-gui-de-yi-zhu" tabindex="-1">二叉树详解：递归的艺术</h1><blockquote><p>大部分二叉树问题都可以分解为&quot;处理当前节点 + 递归处理子树&quot;</p></blockquote><hr><p>二叉树是我刷题过程中遇到最多的数据结构之一。刚开始觉得递归很绕，后来发现只要抓住&quot;根-左-右&quot;的思路，大部分问题都能迎刃而解。如果你对递归还不太熟悉，建议先画图，把每一步的调用栈写出来，会清晰很多。</p><hr><h2 id="shi-yao-shi-er-cha-shu" tabindex="-1" id="什么是二叉树">什么是二叉树</h2><p>二叉树是一种树形结构，每个节点最多有两个子节点，分别称为<strong>左子节点</strong>和<strong>右子节点</strong>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">    1          ← 根节点</span><br><span class="line">   / \</span><br><span class="line">  2   3        ← 左子节点、右子节点</span><br><span class="line"> / \   \</span><br><span class="line">4   5   6      ← 叶子节点（没有子节点）</span><br></pre></td></tr></table></figure><h3 id="jie-dian-ding-yi" tabindex="-1" id="节点定义">节点定义</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TreeNode</span> &#123;</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    TreeNode* left;</span><br><span class="line">    TreeNode* right;</span><br><span class="line">    <span class="built_in">TreeNode</span>(<span class="type">int</span> x) : <span class="built_in">val</span>(x), <span class="built_in">left</span>(<span class="literal">nullptr</span>), <span class="built_in">right</span>(<span class="literal">nullptr</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="ji-ge-gai-nian" tabindex="-1" id="几个概念">几个概念</h3><table><thead><tr><th>术语</th><th>含义</th></tr></thead><tbody><tr><td>根节点</td><td>树的最顶端节点</td></tr><tr><td>叶子节点</td><td>没有子节点的节点</td></tr><tr><td>深度</td><td>从根到该节点的边数</td></tr><tr><td>高度</td><td>从该节点到最远叶子的边数</td></tr><tr><td>层</td><td>深度相同的节点在同一层（从1开始）</td></tr></tbody></table><hr><h2 id="er-cha-shu-de-fen-lei" tabindex="-1" id="二叉树的分类">二叉树的分类</h2><pre><code class="highlight mermaid">graph TD    A[二叉树] --&gt; B[满二叉树]    A --&gt; C[完全二叉树]    A --&gt; D[二叉搜索树 BST]    A --&gt; E[平衡二叉树 AVL]        style A fill:#FFE4B5    style D fill:#90EE90    style E fill:#87CEEB</code></pre><h3 id="man-er-cha-shu" tabindex="-1" id="满二叉树">满二叉树</h3><p>每一层都是满的，节点数 = 2^h - 1（h是层数）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">    1</span><br><span class="line">   / \</span><br><span class="line">  2   3</span><br><span class="line"> / \ / \</span><br><span class="line">4  5 6  7</span><br></pre></td></tr></table></figure><h3 id="wan-quan-er-cha-shu" tabindex="-1" id="完全二叉树">完全二叉树</h3><p>除了最后一层，其他层都是满的，且最后一层节点靠左排列。堆就是完全二叉树。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">    1</span><br><span class="line">   / \</span><br><span class="line">  2   3</span><br><span class="line"> / \</span><br><span class="line">4   5</span><br></pre></td></tr></table></figure><h3 id="er-cha-sou-suo-shu-bst" tabindex="-1" id="二叉搜索树（BST）">二叉搜索树（BST）</h3><p>左子树所有节点 &lt; 根 &lt; 右子树所有节点。中序遍历是有序的。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">        5</span><br><span class="line">       / \</span><br><span class="line">      3   7</span><br><span class="line">     / \ / \</span><br><span class="line">    2  4 6  8</span><br><span class="line">    </span><br><span class="line">中序遍历：2 3 4 5 6 7 8（有序！）</span><br></pre></td></tr></table></figure><h3 id="ping-heng-er-cha-shu" tabindex="-1" id="平衡二叉树">平衡二叉树</h3><p>任意节点的左右子树高度差不超过1。</p><hr><h2 id="si-chong-bian-li-fang-shi" tabindex="-1" id="四种遍历方式">四种遍历方式</h2><p>二叉树的遍历是基础中的基础，必须熟练掌握。</p><h3 id="qian-xu-bian-li-gen-zuo-you" tabindex="-1" id="前序遍历（根-左-右）">前序遍历（根-左-右）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">preorder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span>;</span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>;  <span class="comment">// 先访问根</span></span><br><span class="line">    <span class="built_in">preorder</span>(root-&gt;left);       <span class="comment">// 再遍历左子树</span></span><br><span class="line">    <span class="built_in">preorder</span>(root-&gt;right);      <span class="comment">// 最后遍历右子树</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhong-xu-bian-li-zuo-gen-you" tabindex="-1" id="中序遍历（左-根-右）">中序遍历（左-根-右）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">inorder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">inorder</span>(root-&gt;left);        <span class="comment">// 先遍历左子树</span></span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>;  <span class="comment">// 再访问根</span></span><br><span class="line">    <span class="built_in">inorder</span>(root-&gt;right);       <span class="comment">// 最后遍历右子树</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="hou-xu-bian-li-zuo-you-gen" tabindex="-1" id="后序遍历（左-右-根）">后序遍历（左-右-根）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">postorder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">postorder</span>(root-&gt;left);      <span class="comment">// 先遍历左子树</span></span><br><span class="line">    <span class="built_in">postorder</span>(root-&gt;right);     <span class="comment">// 再遍历右子树</span></span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>;  <span class="comment">// 最后访问根</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="ceng-xu-bian-li-bfs" tabindex="-1" id="层序遍历（BFS）">层序遍历（BFS）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">levelOrder</span>(TreeNode* root) &#123;</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; result;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> result;</span><br><span class="line">    </span><br><span class="line">    queue&lt;TreeNode*&gt; q;</span><br><span class="line">    q.<span class="built_in">push</span>(root);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="type">int</span> size = q.<span class="built_in">size</span>();</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; level;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; size; ++i) &#123;</span><br><span class="line">            TreeNode* node = q.<span class="built_in">front</span>();</span><br><span class="line">            q.<span class="built_in">pop</span>();</span><br><span class="line">            level.<span class="built_in">push_back</span>(node-&gt;val);</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> (node-&gt;left) q.<span class="built_in">push</span>(node-&gt;left);</span><br><span class="line">            <span class="keyword">if</span> (node-&gt;right) q.<span class="built_in">push</span>(node-&gt;right);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        result.<span class="built_in">push_back</span>(level);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="bian-li-guo-cheng-shi-li" tabindex="-1" id="遍历过程示例">遍历过程示例</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">        1</span><br><span class="line">       / \</span><br><span class="line">      2   3</span><br><span class="line">     / \</span><br><span class="line">    4   5</span><br><span class="line"></span><br><span class="line">前序：1 2 4 5 3  （根-左-右）</span><br><span class="line">中序：4 2 5 1 3  （左-根-右）</span><br><span class="line">后序：4 5 2 3 1  （左-右-根）</span><br><span class="line">层序：1 | 2 3 | 4 5</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-zui-da-shen-du" tabindex="-1" id="经典问题：最大深度">经典问题：最大深度</h2><blockquote><p><strong>LeetCode 104</strong>: 给定一个二叉树，找出其最大深度。</p></blockquote><h3 id="di-gui-jie-fa" tabindex="-1" id="递归解法">递归解法</h3><p>思路：树的最大深度 = max(左子树深度, 右子树深度) + 1</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">maxDepth</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> leftDepth = <span class="built_in">maxDepth</span>(root-&gt;left);</span><br><span class="line">    <span class="type">int</span> rightDepth = <span class="built_in">maxDepth</span>(root-&gt;right);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">max</span>(leftDepth, rightDepth) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng" tabindex="-1" id="执行过程">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">        3</span><br><span class="line">       / \</span><br><span class="line">      9  20</span><br><span class="line">         / \</span><br><span class="line">        15  7</span><br><span class="line"></span><br><span class="line">调用 maxDepth(3)</span><br><span class="line">├── 调用 maxDepth(9)</span><br><span class="line">│   ├── maxDepth(null) = 0</span><br><span class="line">│   └── maxDepth(null) = 0</span><br><span class="line">│   └── 返回 max(0,0)+1 = 1</span><br><span class="line">├── 调用 maxDepth(20)</span><br><span class="line">│   ├── maxDepth(15) = 1</span><br><span class="line">│   └── maxDepth(7) = 1</span><br><span class="line">│   └── 返回 max(1,1)+1 = 2</span><br><span class="line">└── 返回 max(1,2)+1 = 3</span><br><span class="line"></span><br><span class="line">答案：3</span><br></pre></td></tr></table></figure><p><strong>时间复杂度</strong>：O(n)，每个节点访问一次<br><strong>空间复杂度</strong>：O(h)，h是树的高度（递归栈）</p><hr><h2 id="jing-dian-wen-ti-pan-duan-dui-cheng-er-cha-shu" tabindex="-1" id="经典问题：判断对称二叉树">经典问题：判断对称二叉树</h2><blockquote><p><strong>LeetCode 101</strong>: 给定一个二叉树，检查它是否是镜像对称的。</p></blockquote><h3 id="si-lu" tabindex="-1" id="思路">思路</h3><p>对称的条件：</p><ol><li>根节点的值相等</li><li>左子树的左子树 = 右子树的右子树</li><li>左子树的右子树 = 右子树的左子树</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">    1</span><br><span class="line">   / \</span><br><span class="line">  2   2       ← 对称</span><br><span class="line"> / \ / \</span><br><span class="line">3  4 4  3     ← 对称</span><br></pre></td></tr></table></figure><h3 id="dai-ma-shi-xian" tabindex="-1" id="代码实现">代码实现</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isSymmetric</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">isMirror</span>(root-&gt;left, root-&gt;right);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isMirror</span><span class="params">(TreeNode* left, TreeNode* right)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!left &amp;&amp; !right) <span class="keyword">return</span> <span class="literal">true</span>;   <span class="comment">// 都为空</span></span><br><span class="line">    <span class="keyword">if</span> (!left || !right) <span class="keyword">return</span> <span class="literal">false</span>;  <span class="comment">// 只有一个为空</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 值相等，且子树对称</span></span><br><span class="line">    <span class="keyword">return</span> left-&gt;val == right-&gt;val</span><br><span class="line">        &amp;&amp; <span class="built_in">isMirror</span>(left-&gt;left, right-&gt;right)</span><br><span class="line">        &amp;&amp; <span class="built_in">isMirror</span>(left-&gt;right, right-&gt;left);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-1" tabindex="-1" id="执行过程-2">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">        1</span><br><span class="line">       / \</span><br><span class="line">      2   2</span><br><span class="line">     / \ / \</span><br><span class="line">    3  4 4  3</span><br><span class="line"></span><br><span class="line">isMirror(2, 2)?</span><br><span class="line">├── 2-&gt;val == 2-&gt;val ✓</span><br><span class="line">├── isMirror(3, 3)?</span><br><span class="line">│   ├── 3-&gt;val == 3-&gt;val ✓</span><br><span class="line">│   ├── isMirror(null, null) = true</span><br><span class="line">│   └── isMirror(null, null) = true</span><br><span class="line">│   └── 返回 true</span><br><span class="line">├── isMirror(4, 4)?</span><br><span class="line">│   └── 返回 true</span><br><span class="line">└── 返回 true</span><br><span class="line"></span><br><span class="line">答案：true</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-er-cha-shu-de-zui-jin-gong-gong-zu-xian" tabindex="-1" id="经典问题：二叉树的最近公共祖先">经典问题：二叉树的最近公共祖先</h2><blockquote><p><strong>LeetCode 236</strong>: 给定一个二叉树，找到两个节点的最近公共祖先（LCA）。</p></blockquote><h3 id="si-lu-1" tabindex="-1" id="思路-2">思路</h3><p>递归查找 p 和 q：</p><ul><li>如果当前节点是 p 或 q，返回当前节点</li><li>递归查找左右子树</li><li>如果 p 和 q 分别在左右子树中，当前节点就是 LCA</li><li>如果都在同一侧，返回那一侧的结果</li></ul><h3 id="dai-ma-shi-xian-1" tabindex="-1" id="代码实现-2">代码实现</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">TreeNode* <span class="title">lowestCommonAncestor</span><span class="params">(TreeNode* root, TreeNode* p, TreeNode* q)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 递归终止条件</span></span><br><span class="line">    <span class="keyword">if</span> (!root || root == p || root == q) &#123;</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 在左右子树中查找</span></span><br><span class="line">    TreeNode* left = <span class="built_in">lowestCommonAncestor</span>(root-&gt;left, p, q);</span><br><span class="line">    TreeNode* right = <span class="built_in">lowestCommonAncestor</span>(root-&gt;right, p, q);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// p 和 q 分别在左右子树</span></span><br><span class="line">    <span class="keyword">if</span> (left &amp;&amp; right) &#123;</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 都在某一侧</span></span><br><span class="line">    <span class="keyword">return</span> left ? left : right;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-2" tabindex="-1" id="执行过程-3">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">        3</span><br><span class="line">       / \</span><br><span class="line">      5   1</span><br><span class="line">     / \ / \</span><br><span class="line">    6  2 0  8</span><br><span class="line">      / \</span><br><span class="line">     7   4</span><br><span class="line"></span><br><span class="line">查找 p=5, q=4 的 LCA</span><br><span class="line"></span><br><span class="line">LCA(3, 5, 4)</span><br><span class="line">├── LCA(5, 5, 4)</span><br><span class="line">│   └── root == p，返回 5</span><br><span class="line">├── LCA(1, 5, 4)</span><br><span class="line">│   ├── LCA(0, 5, 4) = null</span><br><span class="line">│   └── LCA(8, 5, 4) = null</span><br><span class="line">│   └── 返回 null</span><br><span class="line">└── left=5, right=null，返回 5</span><br><span class="line"></span><br><span class="line">答案：节点5</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-er-cha-shu-de-xu-lie-hua-yu-fan-xu-lie-hua" tabindex="-1" id="经典问题：二叉树的序列化与反序列化">经典问题：二叉树的序列化与反序列化</h2><blockquote><p><strong>LeetCode 297</strong>: 设计一个算法，将二叉树序列化为字符串，并能反序列化。</p></blockquote><h3 id="qian-xu-bian-li-fang-an" tabindex="-1" id="前序遍历方案">前序遍历方案</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Codec</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 序列化</span></span><br><span class="line">    <span class="function">string <span class="title">serialize</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="string">&quot;#&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">to_string</span>(root-&gt;val) + <span class="string">&quot;,&quot;</span> </span><br><span class="line">             + <span class="built_in">serialize</span>(root-&gt;left) + <span class="string">&quot;,&quot;</span> </span><br><span class="line">             + <span class="built_in">serialize</span>(root-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 反序列化</span></span><br><span class="line">    <span class="function">TreeNode* <span class="title">deserialize</span><span class="params">(string data)</span> </span>&#123;</span><br><span class="line">        queue&lt;string&gt; nodes;</span><br><span class="line">        string token;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">char</span> c : data) &#123;</span><br><span class="line">            <span class="keyword">if</span> (c == <span class="string">&#x27;,&#x27;</span>) &#123;</span><br><span class="line">                nodes.<span class="built_in">push</span>(token);</span><br><span class="line">                token.<span class="built_in">clear</span>();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                token += c;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        nodes.<span class="built_in">push</span>(token);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">buildTree</span>(nodes);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function">TreeNode* <span class="title">buildTree</span><span class="params">(queue&lt;string&gt;&amp; nodes)</span> </span>&#123;</span><br><span class="line">        string val = nodes.<span class="built_in">front</span>();</span><br><span class="line">        nodes.<span class="built_in">pop</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (val == <span class="string">&quot;#&quot;</span>) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">        </span><br><span class="line">        TreeNode* node = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(<span class="built_in">stoi</span>(val));</span><br><span class="line">        node-&gt;left = <span class="built_in">buildTree</span>(nodes);</span><br><span class="line">        node-&gt;right = <span class="built_in">buildTree</span>(nodes);</span><br><span class="line">        <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-3" tabindex="-1" id="执行过程-4">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">        1</span><br><span class="line">       / \</span><br><span class="line">      2   3</span><br><span class="line">         / \</span><br><span class="line">        4   5</span><br><span class="line"></span><br><span class="line">序列化：1,2,#,#,3,4,#,#,5,#,#</span><br><span class="line"></span><br><span class="line">反序列化过程：</span><br><span class="line">1 → 创建节点1</span><br><span class="line">2 → 创建节点2，作为1的左子节点</span><br><span class="line"># → 2的左子节点为空</span><br><span class="line"># → 2的右子节点为空</span><br><span class="line">3 → 创建节点3，作为1的右子节点</span><br><span class="line">4 → 创建节点4，作为3的左子节点</span><br><span class="line">...</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-cong-bian-li-jie-guo-gou-zao-er-cha-shu" tabindex="-1" id="经典问题：从遍历结果构造二叉树">经典问题：从遍历结果构造二叉树</h2><blockquote><p><strong>LeetCode 105</strong>: 根据前序遍历和中序遍历构造二叉树。</p></blockquote><h3 id="si-lu-2" tabindex="-1" id="思路-3">思路</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">前序：[3, 9, 20, 15, 7]  → 根-左-右，第一个是根</span><br><span class="line">中序：[9, 3, 15, 20, 7]  → 左-根-右，根的左边是左子树</span><br></pre></td></tr></table></figure><ol><li>前序的第一个元素是根</li><li>在中序中找到根的位置，左边是左子树，右边是右子树</li><li>递归构造</li></ol><h3 id="dai-ma-shi-xian-2" tabindex="-1" id="代码实现-3">代码实现</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">TreeNode* <span class="title">buildTree</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; preorder, vector&lt;<span class="type">int</span>&gt;&amp; inorder)</span> </span>&#123;</span><br><span class="line">    unordered_map&lt;<span class="type">int</span>, <span class="type">int</span>&gt; inorderIndex;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; inorder.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">        inorderIndex[inorder[i]] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">build</span>(preorder, <span class="number">0</span>, preorder.<span class="built_in">size</span>() - <span class="number">1</span>,</span><br><span class="line">                 inorder, <span class="number">0</span>, inorder.<span class="built_in">size</span>() - <span class="number">1</span>, inorderIndex);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">TreeNode* <span class="title">build</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; preorder, <span class="type">int</span> preStart, <span class="type">int</span> preEnd,</span></span></span><br><span class="line"><span class="params"><span class="function">                vector&lt;<span class="type">int</span>&gt;&amp; inorder, <span class="type">int</span> inStart, <span class="type">int</span> inEnd,</span></span></span><br><span class="line"><span class="params"><span class="function">                unordered_map&lt;<span class="type">int</span>, <span class="type">int</span>&gt;&amp; inorderIndex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (preStart &gt; preEnd) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> rootVal = preorder[preStart];</span><br><span class="line">    TreeNode* root = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(rootVal);</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> rootIndex = inorderIndex[rootVal];</span><br><span class="line">    <span class="type">int</span> leftSize = rootIndex - inStart;</span><br><span class="line">    </span><br><span class="line">    root-&gt;left = <span class="built_in">build</span>(preorder, preStart + <span class="number">1</span>, preStart + leftSize,</span><br><span class="line">                       inorder, inStart, rootIndex - <span class="number">1</span>, inorderIndex);</span><br><span class="line">    root-&gt;right = <span class="built_in">build</span>(preorder, preStart + leftSize + <span class="number">1</span>, preEnd,</span><br><span class="line">                        inorder, rootIndex + <span class="number">1</span>, inEnd, inorderIndex);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-4" tabindex="-1" id="执行过程-5">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">前序：[3, 9, 20, 15, 7]</span><br><span class="line">中序：[9, 3, 15, 20, 7]</span><br><span class="line"></span><br><span class="line">Step 1: 根 = 3</span><br><span class="line">        中序中 3 的位置 = 1</span><br><span class="line">        左子树：前序[9], 中序[9]</span><br><span class="line">        右子树：前序[20,15,7], 中序[15,20,7]</span><br><span class="line"></span><br><span class="line">Step 2: 构造左子树，根 = 9</span><br><span class="line">Step 3: 构造右子树，根 = 20</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">结果：</span><br><span class="line">        3</span><br><span class="line">       / \</span><br><span class="line">      9  20</span><br><span class="line">         / \</span><br><span class="line">        15  7</span><br></pre></td></tr></table></figure><hr><h2 id="jing-dian-wen-ti-yan-zheng-er-cha-sou-suo-shu" tabindex="-1" id="经典问题：验证二叉搜索树">经典问题：验证二叉搜索树</h2><blockquote><p><strong>LeetCode 98</strong>: 判断一棵树是否是有效的二叉搜索树。</p></blockquote><h3 id="si-lu-3" tabindex="-1" id="思路-4">思路</h3><p>利用 BST 的性质：中序遍历是严格递增的。</p><p>或者用递归：每个节点的值必须在一个合法范围内。</p><h3 id="fang-fa-1-zhong-xu-bian-li" tabindex="-1" id="方法1：中序遍历">方法1：中序遍历</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isValidBST</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="type">long</span> <span class="type">long</span> prev = LLONG_MIN;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">inorderCheck</span>(root, prev);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">inorderCheck</span><span class="params">(TreeNode* node, <span class="type">long</span> <span class="type">long</span>&amp; prev)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!node) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查左子树</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">inorderCheck</span>(node-&gt;left, prev)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查当前节点</span></span><br><span class="line">    <span class="keyword">if</span> (node-&gt;val &lt;= prev) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    prev = node-&gt;val;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查右子树</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">inorderCheck</span>(node-&gt;right, prev);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="fang-fa-2-di-gui-fan-wei-jian-cha" tabindex="-1" id="方法2：递归范围检查">方法2：递归范围检查</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isValidBST</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">validate</span>(root, LLONG_MIN, LLONG_MAX);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">validate</span><span class="params">(TreeNode* node, <span class="type">long</span> <span class="type">long</span> minVal, <span class="type">long</span> <span class="type">long</span> maxVal)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!node) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (node-&gt;val &lt;= minVal || node-&gt;val &gt;= maxVal) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">validate</span>(node-&gt;left, minVal, node-&gt;val)</span><br><span class="line">        &amp;&amp; <span class="built_in">validate</span>(node-&gt;right, node-&gt;val, maxVal);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhi-xing-guo-cheng-5" tabindex="-1" id="执行过程-6">执行过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">        5</span><br><span class="line">       / \</span><br><span class="line">      1   4</span><br><span class="line">         / \</span><br><span class="line">        3   6</span><br><span class="line"></span><br><span class="line">validate(5, -∞, +∞)</span><br><span class="line">├── validate(1, -∞, 5) = true</span><br><span class="line">├── validate(4, 5, +∞)</span><br><span class="line">│   └── 4 不在 (5, +∞) 范围内！</span><br><span class="line">│   └── 返回 false</span><br><span class="line"></span><br><span class="line">答案：false（不是有效BST）</span><br></pre></td></tr></table></figure><hr><h2 id="die-dai-bian-li-fei-di-gui" tabindex="-1" id="迭代遍历（非递归）">迭代遍历（非递归）</h2><p>有时候面试官会要求用迭代方式实现遍历。</p><h3 id="qian-xu-bian-li-die-dai" tabindex="-1" id="前序遍历（迭代）">前序遍历（迭代）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">preorderTraversal</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; result;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> result;</span><br><span class="line">    </span><br><span class="line">    stack&lt;TreeNode*&gt; stk;</span><br><span class="line">    stk.<span class="built_in">push</span>(root);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (!stk.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        TreeNode* node = stk.<span class="built_in">top</span>();</span><br><span class="line">        stk.<span class="built_in">pop</span>();</span><br><span class="line">        result.<span class="built_in">push_back</span>(node-&gt;val);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 先压右，再压左（因为栈是后进先出）</span></span><br><span class="line">        <span class="keyword">if</span> (node-&gt;right) stk.<span class="built_in">push</span>(node-&gt;right);</span><br><span class="line">        <span class="keyword">if</span> (node-&gt;left) stk.<span class="built_in">push</span>(node-&gt;left);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zhong-xu-bian-li-die-dai" tabindex="-1" id="中序遍历（迭代）">中序遍历（迭代）</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">inorderTraversal</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; result;</span><br><span class="line">    stack&lt;TreeNode*&gt; stk;</span><br><span class="line">    TreeNode* curr = root;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (curr || !stk.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="comment">// 一路向左</span></span><br><span class="line">        <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">            stk.<span class="built_in">push</span>(curr);</span><br><span class="line">            curr = curr-&gt;left;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 访问节点</span></span><br><span class="line">        curr = stk.<span class="built_in">top</span>();</span><br><span class="line">        stk.<span class="built_in">pop</span>();</span><br><span class="line">        result.<span class="built_in">push_back</span>(curr-&gt;val);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 转向右子树</span></span><br><span class="line">        curr = curr-&gt;right;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="chang-jian-ji-qiao-zong-jie" tabindex="-1" id="常见技巧总结">常见技巧总结</h2><h3 id="1-di-gui-san-yao-su" tabindex="-1" id="1-递归三要素">1. 递归三要素</h3><ol><li><strong>终止条件</strong>：<code>if (!root) return ...</code></li><li><strong>递归操作</strong>：处理当前节点</li><li><strong>递归调用</strong>：处理左右子树</li></ol><h3 id="2-bian-li-xuan-ze" tabindex="-1" id="2-遍历选择">2. 遍历选择</h3><table><thead><tr><th>场景</th><th>推荐遍历</th></tr></thead><tbody><tr><td>求深度/高度</td><td>后序（先知道子树信息）</td></tr><tr><td>BST 相关</td><td>中序（有序性）</td></tr><tr><td>路径问题</td><td>前序（从上到下）</td></tr><tr><td>层级相关</td><td>层序（BFS）</td></tr><tr><td>序列化/反序列化</td><td>前序 + 标记空节点</td></tr></tbody></table><h3 id="3-chang-jian-xian-jing" tabindex="-1" id="3-常见陷阱">3. 常见陷阱</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ❌ 错误：没有处理空节点</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">maxDepth</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">max</span>(<span class="built_in">maxDepth</span>(root-&gt;left), <span class="built_in">maxDepth</span>(root-&gt;right)) + <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 当 root 为空时会崩溃！</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ 正确：先判空</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">maxDepth</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">max</span>(<span class="built_in">maxDepth</span>(root-&gt;left), <span class="built_in">maxDepth</span>(root-&gt;right)) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="si-wei-dao-tu" tabindex="-1" id="思维导图">思维导图</h2><pre><code class="highlight mermaid">graph TD    A[二叉树] --&gt; B[基本概念]    A --&gt; C[遍历方式]    A --&gt; D[经典问题]        B --&gt; B1[节点定义]    B --&gt; B2[满/完全/BST]        C --&gt; C1[前序 根-左-右]    C --&gt; C2[中序 左-根-右]    C --&gt; C3[后序 左-右-根]    C --&gt; C4[层序 BFS]        D --&gt; D1[最大深度]    D --&gt; D2[对称二叉树]    D --&gt; D3[最近公共祖先]    D --&gt; D4[验证BST]    D --&gt; D5[序列化]        style A fill:#FFE4B5    style C fill:#90EE90    style D fill:#87CEEB</code></pre><hr><h2 id="zong-jie" tabindex="-1" id="总结">总结</h2><h3 id="he-xin-yao-dian" tabindex="-1" id="核心要点">核心要点</h3><ol><li><strong>递归是处理二叉树的核心</strong>：大部分问题都可以分解为&quot;处理当前节点 + 递归处理子树&quot;</li><li><strong>掌握四种遍历</strong>：前序、中序、后序、层序，各有应用场景</li><li><strong>BST 的中序遍历是有序的</strong>：很多 BST 问题可以利用这个性质</li><li><strong>空节点要先判断</strong>：递归的第一步往往是 <code>if (!root) return ...</code></li></ol><h3 id="leet-code-tui-jian-ti-dan" tabindex="-1" id="LeetCode-推荐题单">LeetCode 推荐题单</h3><p><strong>入门</strong>：</p><ul><li>104 最大深度</li><li>101 对称二叉树</li><li>226 翻转二叉树</li></ul><p><strong>进阶</strong>：</p><ul><li>102 层序遍历</li><li>236 最近公共祖先</li><li>105 前序+中序构造二叉树</li></ul><p><strong>挑战</strong>：</p><ul><li>124 二叉树最大路径和</li><li>297 序列化与反序列化</li><li>99 恢复BST</li></ul><p><strong>推荐阅读</strong>：</p><ul><li><a href="/posts/60d62905/">常见数据结构-堆</a> - 堆是完全二叉树的经典应用</li><li><a href="https://leetcode.cn/tag/binary-tree/">LeetCode 二叉树专题</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;er-cha-shu-xiang-jie-di-gui-de-yi-zhu&quot; tabindex=&quot;-1&quot;&gt;二叉树详解：递归的艺术&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;大部分二叉树问题都可以分解为&amp;quot;处理当前节点 +</summary>
        
      
    
    
    
    <category term="算法" scheme="https://smarter.xin/categories/algorithm/"/>
    
    <category term="数据结构" scheme="https://smarter.xin/categories/algorithm/data-structure/"/>
    
    
    <category term="C++" scheme="https://smarter.xin/tags/cpp/"/>
    
    <category term="LeetCode" scheme="https://smarter.xin/tags/leetcode/"/>
    
    <category term="二叉树" scheme="https://smarter.xin/tags/binary-tree/"/>
    
    <category term="递归" scheme="https://smarter.xin/tags/recursion/"/>
    
    <category term="DFS" scheme="https://smarter.xin/tags/dfs/"/>
    
    <category term="BFS" scheme="https://smarter.xin/tags/bfs/"/>
    
  </entry>
  
  <entry>
    <title>PMPP-大规模并行处理器程序设计：导读</title>
    <link href="https://smarter.xin/posts/30730973/"/>
    <id>https://smarter.xin/posts/30730973/</id>
    <published>2026-01-24T09:11:42.000Z</published>
    <updated>2026-03-16T05:46:43.227Z</updated>
    
    <content type="html"><![CDATA[<h2 id="qian-yan" tabindex="-1" id="前言">前言</h2><p>《Programming Massively Parallel Processors: A Hands-on Approach》（简称 PMPP）是 GPU 并行编程领域的经典教材，由 NVIDIA 首席科学家 David Kirk 和 Wen-mei Hwu 教授编写。第四版于 2022 年出版，涵盖了截至 Ampere 架构的最新技术。</p><p>这本书不只是 CUDA API 手册，更是一部**并行计算思维（Parallel Computational Thinking）**的养成指南。从最基础的线程模型到高级的分布式计算，从简单的向量加法到复杂的深度学习优化，循序渐进地构建完整的并行编程知识体系。</p><p>本系列博客完整覆盖了全书 22 章内容，每章一篇。这篇导读将帮助你：</p><ul><li>快速了解全书结构和各章核心内容</li><li>找到每章博客的链接</li><li>根据自身背景选择合适的学习路线</li></ul><blockquote><p><strong>📦 配套资源</strong>：本系列文章配有完整的 <a href="https://github.com/psmarter/PMPP-Learning">GitHub 仓库</a>，包含每章的练习题解答、CUDA 代码实现和详细注释。所有代码都经过测试，可以直接运行。</p></blockquote><h2 id="quan-shu-jie-gou" tabindex="-1" id="全书结构">全书结构</h2><p>PMPP 第四版共 22 章，可以分为四大部分：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────────┐</span><br><span class="line">│  第一部分：CUDA 基础（1-6章）                                 │</span><br><span class="line">│  建立 GPU 编程的基础概念和核心技能                             │</span><br><span class="line">├─────────────────────────────────────────────────────────────┤</span><br><span class="line">│  第二部分：并行模式（7-15章）                                  │</span><br><span class="line">│  学习 9 种核心并行算法模式                                     │</span><br><span class="line">├─────────────────────────────────────────────────────────────┤</span><br><span class="line">│  第三部分：实际应用（16-18章）                                 │</span><br><span class="line">│  在真实场景中应用并行技术                                      │</span><br><span class="line">├─────────────────────────────────────────────────────────────┤</span><br><span class="line">│  第四部分：高级主题（19-22章）                                 │</span><br><span class="line">│  计算思维、分布式计算、高级特性                                 │</span><br><span class="line">└─────────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><hr><h2 id="di-yi-bu-fen-cuda-ji-chu-1-6-zhang" tabindex="-1" id="第一部分：CUDA-基础（1-6章）">第一部分：CUDA 基础（1-6章）</h2><p>这是入门必读部分，建立 GPU 编程的完整基础。</p><h3 id="ge-zhang-gai-yao" tabindex="-1" id="📖-各章概要">📖 各章概要</h3><table><thead><tr><th>章节</th><th>标题</th><th>核心内容</th><th>博客链接</th></tr></thead><tbody><tr><td>1</td><td>引言</td><td>CPU vs GPU 设计哲学、CUDA 生态、异构计算模型</td><td><a href="https://smarter.xin/posts/10d278b0/">阅读</a></td></tr><tr><td>2</td><td>异构数据并行计算</td><td>Thread/Block/Grid 概念、向量加法、错误处理</td><td><a href="https://smarter.xin/posts/3ee22ce5/">阅读</a></td></tr><tr><td>3</td><td>多维网格和数据</td><td>2D/3D 索引计算、图像处理、边界检查</td><td><a href="https://smarter.xin/posts/6b7045b6/">阅读</a></td></tr><tr><td>4</td><td>计算架构和调度</td><td>SM 结构、Warp 调度、资源限制、占用率</td><td><a href="https://smarter.xin/posts/bd5d1d6/">阅读</a></td></tr><tr><td>5</td><td>内存架构和数据局部性</td><td>内存层次、共享内存、<strong>Tiling 技术</strong></td><td><a href="https://smarter.xin/posts/3bb3179b/">阅读</a></td></tr><tr><td>6</td><td>性能方面的考虑</td><td>合并访问、Bank 冲突、分支发散、资源分配</td><td><a href="https://smarter.xin/posts/220818c3/">阅读</a></td></tr></tbody></table><h3 id="guan-jian-zhi-shi-dian" tabindex="-1" id="🔑-关键知识点">🔑 关键知识点</h3><p><strong>硬件架构</strong>：</p><ul><li>GPU 由多个流式多处理器（SM，Streaming Multiprocessor）组成</li><li>每个 SM 有自己的寄存器、共享内存、L1 缓存</li><li>线程以32个一组（线程束，Warp）在 SM 上执行</li></ul><p><strong>编程模型</strong>：</p><ul><li>核函数（Kernel）是在 GPU 上执行的并行函数</li><li>线程组织为块（Block），块组织为网格（Grid）</li><li>线程通过 <code>threadIdx</code>、<code>blockIdx</code>、<code>blockDim</code> 确定自己的位置</li></ul><p><strong>内存优化</strong>（最重要）：</p><ul><li>寄存器 → 共享内存 → L2 缓存 → 全局内存，速度差异可达100倍</li><li><strong>分块（Tiling）</strong>：将数据分块加载到共享内存，块内复用</li><li><strong>合并访问（Coalesced Access）</strong>：让线程束内的线程访问连续地址</li></ul><h3 id="xue-xi-jian-yi" tabindex="-1" id="💡-学习建议">💡 学习建议</h3><ol><li>第一遍快速浏览 1-3 章，理解基本概念</li><li>重点学习 4-6 章，这是性能优化的基础</li><li><strong>第 5 章的 Tiled 矩阵乘法必须亲手实现</strong></li></ol><hr><h2 id="di-er-bu-fen-bing-xing-mo-shi-7-15-zhang" tabindex="-1" id="第二部分：并行模式（7-15章）">第二部分：并行模式（7-15章）</h2><p>这是本书的核心部分，介绍 9 种可复用的并行算法模式。</p><h3 id="ge-zhang-gai-yao-1" tabindex="-1" id="📖-各章概要-2">📖 各章概要</h3><table><thead><tr><th>章节</th><th>标题</th><th>核心模式</th><th>博客链接</th></tr></thead><tbody><tr><td>7</td><td>卷积</td><td>Tiling + Halo 处理、常量内存</td><td><a href="https://smarter.xin/posts/1c778456/">阅读</a></td></tr><tr><td>8</td><td>模板</td><td>寄存器 Tiling、分块时间迭代</td><td><a href="https://smarter.xin/posts/93c68d7a/">阅读</a></td></tr><tr><td>9</td><td>并行直方图</td><td>原子操作、私有化、聚合</td><td><a href="https://smarter.xin/posts/d29973f1/">阅读</a></td></tr><tr><td>10</td><td>归约</td><td>树形归约、Warp Shuffle、CUB 库</td><td><a href="https://smarter.xin/posts/43b40d12/">阅读</a></td></tr><tr><td>11</td><td>前缀和</td><td>Kogge-Stone、Brent-Kung、分层 Scan</td><td><a href="https://smarter.xin/posts/a6fc4cf6/">阅读</a></td></tr><tr><td>12</td><td>归并</td><td>Co-Rank 技术、并行归并路径</td><td><a href="https://smarter.xin/posts/31928809/">阅读</a></td></tr><tr><td>13</td><td>排序</td><td>基数排序、归并排序、Thrust 库</td><td><a href="https://smarter.xin/posts/d9ee9484/">阅读</a></td></tr><tr><td>14</td><td>稀疏矩阵</td><td>CSR/ELL/COO/HYB 格式、SpMV</td><td><a href="https://smarter.xin/posts/7af84cf7/">阅读</a></td></tr><tr><td>15</td><td>图遍历</td><td>并行 BFS、边界推进、层级同步</td><td><a href="https://smarter.xin/posts/70b05668/">阅读</a></td></tr></tbody></table><h3 id="guan-jian-zhi-shi-dian-1" tabindex="-1" id="🔑-关键知识点-2">🔑 关键知识点</h3><p><strong>数据并行模式</strong>：</p><table><thead><tr><th>模式</th><th>特点</th><th>典型应用</th></tr></thead><tbody><tr><td>Map</td><td>一对一变换</td><td>向量运算、图像滤波</td></tr><tr><td>Reduce</td><td>多对一归约</td><td>求和、最大值</td></tr><tr><td>Scan</td><td>前缀操作</td><td>压缩、流分配</td></tr><tr><td>Gather/Scatter</td><td>不规则访问</td><td>稀疏矩阵、图计算</td></tr></tbody></table><p><strong>输出冲突处理</strong>（第 9 章）：</p><ul><li>原子操作：正确但慢</li><li>私有化：每线程/每 Block 私有副本</li><li>聚合：相同地址只做一次原子操作</li></ul><p><strong>树形并行</strong>（第 10-11 章）：</p><ul><li>归约：多个值 → 一个值</li><li>扫描：多个值 → 多个前缀和</li><li>都是 O(log N) 步，但 Scan 更复杂</li></ul><h3 id="xue-xi-jian-yi-1" tabindex="-1" id="💡-学习建议-2">💡 学习建议</h3><ol><li>第 7 章卷积是第 5 章 Tiling 的进阶应用</li><li><strong>第 10-11 章（归约和前缀和）是最重要的两章</strong></li><li>稀疏矩阵和图遍历展示了如何处理不规则数据</li></ol><hr><h2 id="di-san-bu-fen-shi-ji-ying-yong-16-18-zhang" tabindex="-1" id="第三部分：实际应用（16-18章）">第三部分：实际应用（16-18章）</h2><p>将前面学到的技术应用于真实的科学计算和工程问题。</p><h3 id="ge-zhang-gai-yao-2" tabindex="-1" id="📖-各章概要-3">📖 各章概要</h3><table><thead><tr><th>章节</th><th>标题</th><th>应用领域</th><th>博客链接</th></tr></thead><tbody><tr><td>16</td><td>深度学习</td><td>卷积神经网络、Im2col、Tensor Core</td><td><a href="https://smarter.xin/posts/feaca34d/">阅读</a></td></tr><tr><td>17</td><td>迭代式 MRI 重建</td><td>NUFFT、共轭梯度、医学成像</td><td><a href="https://smarter.xin/posts/fab0715b/">阅读</a></td></tr><tr><td>18</td><td>静电势能图</td><td>N-body 问题、空间分区、cutoff 优化</td><td><a href="https://smarter.xin/posts/d7c6e6a8/">阅读</a></td></tr></tbody></table><h3 id="guan-jian-zhi-shi-dian-2" tabindex="-1" id="🔑-关键知识点-3">🔑 关键知识点</h3><p><strong>深度学习（第 16 章）</strong>：</p><ul><li>卷积是 95%+ 的计算量</li><li>Im2col 把卷积转为 GEMM</li><li>Winograd 减少乘法次数</li><li>cuDNN 自动选择最优算法</li></ul><p><strong>科学计算</strong>：</p><ul><li>迭代算法需要多次 Kernel 启动</li><li>非均匀网格需要特殊处理</li><li>空间分区降低 O(N²) 到 O(N)</li></ul><h3 id="xue-xi-jian-yi-2" tabindex="-1" id="💡-学习建议-3">💡 学习建议</h3><ol><li>如果做深度学习，第 16 章必读</li><li>第 17-18 章展示科学计算的完整流程</li><li>理解如何组合多种技术解决复杂问题</li></ol><hr><h2 id="di-si-bu-fen-gao-ji-zhu-ti-19-22-zhang" tabindex="-1" id="第四部分：高级主题（19-22章）">第四部分：高级主题（19-22章）</h2><p>计算思维方法论、分布式扩展和未来趋势。</p><h3 id="ge-zhang-gai-yao-3" tabindex="-1" id="📖-各章概要-4">📖 各章概要</h3><table><thead><tr><th>章节</th><th>标题</th><th>核心内容</th><th>博客链接</th></tr></thead><tbody><tr><td>19</td><td>并行编程与计算思维</td><td>问题分解、算法选择、性能推理</td><td><a href="https://smarter.xin/posts/46b3d994/">阅读</a></td></tr><tr><td>20</td><td>异构计算集群编程</td><td>MPI+CUDA、多 GPU、Halo 交换</td><td><a href="https://smarter.xin/posts/9506dbb9/">阅读</a></td></tr><tr><td>21</td><td>CUDA 动态并行性</td><td>设备端启动、递归算法、自适应网格</td><td><a href="https://smarter.xin/posts/e519c4bc/">阅读</a></td></tr><tr><td>22</td><td>高级实践与未来演变</td><td>性能方法论、代码可移植性、发展趋势</td><td><a href="https://smarter.xin/posts/a3a140de/">阅读</a></td></tr></tbody></table><h3 id="guan-jian-zhi-shi-dian-3" tabindex="-1" id="🔑-关键知识点-4">🔑 关键知识点</h3><p><strong>计算思维（第 19 章）</strong>：</p><ul><li>识别并行性：数据并行 vs 任务并行</li><li>算法选择：计算复杂度 vs 并行度</li><li>性能推理：Roofline 模型</li></ul><p><strong>分布式扩展（第 20 章）</strong>：</p><ul><li>多 GPU：CUDA-aware MPI、GPUDirect</li><li>通信隐藏：重叠计算和传输</li><li>弱扩展 vs 强扩展</li></ul><p><strong>动态并行（第 21 章）</strong>：</p><ul><li>设备端可以启动子 Kernel</li><li>适合递归和自适应算法</li><li>有同步和开销限制</li></ul><h3 id="xue-xi-jian-yi-3" tabindex="-1" id="💡-学习建议-4">💡 学习建议</h3><ol><li>第 19 章提炼了全书的方法论精华</li><li>如果需要多 GPU/多节点，重点学第 20 章</li><li>第 22 章可作为持续学习的指南</li></ol><hr><h2 id="he-xin-zhi-shi-tu-pu" tabindex="-1" id="核心知识图谱">核心知识图谱</h2><h3 id="nei-cun-you-hua-ji-zhu" tabindex="-1" id="内存优化技术">内存优化技术</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">                    ┌──────────────────────────────────────┐</span><br><span class="line">                    │         GPU 内存层次结构              │</span><br><span class="line">                    ├──────────────────────────────────────┤</span><br><span class="line">                    │  寄存器 (~TB/s) ◀── 最快，数量有限    │</span><br><span class="line">                    │     ↓                                │</span><br><span class="line">                    │  共享内存 (~10 TB/s) ◀── 可编程缓存   │</span><br><span class="line">                    │     ↓                                │</span><br><span class="line">                    │  L2 缓存 (~1 TB/s) ◀── 自动管理       │</span><br><span class="line">                    │     ↓                                │</span><br><span class="line">                    │  全局内存 (~500 GB/s) ◀── 容量大但慢  │</span><br><span class="line">                    └──────────────────────────────────────┘</span><br><span class="line"></span><br><span class="line">优化策略：</span><br><span class="line">├── Tiling（第5章）：数据分块，在共享内存中复用</span><br><span class="line">├── 合并访问（第6章）：连续地址，减少内存事务</span><br><span class="line">├── 寄存器Tiling（第8章）：数据保留在寄存器</span><br><span class="line">└── 私有化（第9章）：避免原子操作的竞争</span><br></pre></td></tr></table></figure><h3 id="bing-xing-suan-fa-xuan-ze" tabindex="-1" id="并行算法选择">并行算法选择</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">问题类型                    推荐算法</span><br><span class="line">───────────────────────────────────────────────</span><br><span class="line">逐元素运算                  Map（直接并行）</span><br><span class="line">全局聚合（求和/最大值）      树形归约（第10章）</span><br><span class="line">前缀依赖计算                Scan（第11章）</span><br><span class="line">有序数据合并                Co-rank归并（第12章）</span><br><span class="line">整数键排序                  基数排序（第13章）</span><br><span class="line">稀疏数据处理                CSR/ELL格式（第14章）</span><br><span class="line">图遍历                      边界推进BFS（第15章）</span><br></pre></td></tr></table></figure><h3 id="xing-neng-diao-you-fang-fa-lun" tabindex="-1" id="性能调优方法论">性能调优方法论</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">1. Profile 先行</span><br><span class="line">   └── 使用 Nsight Compute 定位瓶颈</span><br><span class="line"></span><br><span class="line">2. 判断瓶颈类型</span><br><span class="line">   ├── 内存受限：优化访问模式、增加数据复用</span><br><span class="line">   └── 计算受限：减少指令数、使用快速数学函数</span><br><span class="line"></span><br><span class="line">3. 层次化优化（从上到下收益递减）</span><br><span class="line">   ├── 算法选择</span><br><span class="line">   ├── 数据布局</span><br><span class="line">   ├── 并行策略</span><br><span class="line">   ├── 内存层次利用</span><br><span class="line">   └── 指令级优化</span><br><span class="line"></span><br><span class="line">4. 迭代验证</span><br><span class="line">   └── 保持正确性，逐步优化</span><br></pre></td></tr></table></figure><hr><h2 id="xue-xi-lu-xian-tui-jian" tabindex="-1" id="学习路线推荐">学习路线推荐</h2><h3 id="kuai-su-ru-men-lu-xian-2-zhou" tabindex="-1" id="🚀-快速入门路线（2周）">🚀 快速入门路线（2周）</h3><p>适合：有编程基础，想快速上手 CUDA</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Week 1: 1→2→3→5（基础概念 + Tiling）</span><br><span class="line">Week 2: 10→16（归约 + 深度学习应用）</span><br></pre></td></tr></table></figure><h3 id="xi-tong-xue-xi-lu-xian-2-yue" tabindex="-1" id="📚-系统学习路线（2月）">📚 系统学习路线（2月）</h3><p>适合：想深入掌握 GPU 编程</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Month 1（基础+模式）:</span><br><span class="line">  Week 1-2: 1-6章（打牢基础）</span><br><span class="line">  Week 3-4: 7-11章（核心模式）</span><br><span class="line"></span><br><span class="line">Month 2（进阶+应用）:</span><br><span class="line">  Week 1-2: 12-16章（高级模式+应用）</span><br><span class="line">  Week 3-4: 19-22章（思维+扩展）</span><br></pre></td></tr></table></figure><h3 id="zhuan-xiang-ti-sheng-lu-xian" tabindex="-1" id="🎯-专项提升路线">🎯 专项提升路线</h3><p><strong>深度学习方向</strong>：1→2→5→7→10→16</p><p><strong>科学计算方向</strong>：1→2→5→10→11→14→17→18</p><p><strong>HPC 方向</strong>：1→2→4→5→6→10→20</p><hr><h2 id="xue-xi-zi-yuan" tabindex="-1" id="学习资源">学习资源</h2><h3 id="pei-tao-dai-ma" tabindex="-1" id="配套代码">配套代码</h3><ul><li><strong>GitHub 仓库</strong>：<a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></li><li>包含所有章节练习题解答</li><li>可直接编译运行的 CUDA 代码</li><li>详细的中文注释</li></ul><h3 id="guan-fang-zi-yuan" tabindex="-1" id="官方资源">官方资源</h3><ul><li><a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">CUDA C++ Programming Guide</a></li><li><a href="https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/">CUDA Best Practices Guide</a></li><li><a href="https://docs.nvidia.com/nsight-compute/">Nsight Compute Documentation</a></li></ul><h3 id="xiang-guan-ke-cheng" tabindex="-1" id="相关课程">相关课程</h3><ul><li>UIUC ECE 408（Coursera 可看）</li><li>书籍作者的配套课程</li></ul><hr><h2 id="xie-zai-zui-hou" tabindex="-1" id="写在最后">写在最后</h2><p>PMPP 不是一本可以一次读完的书。第一遍建立框架，第二遍深入细节，每次实战后再回来都会有新的理解。</p><p><strong>学习建议</strong>：</p><ol><li><strong>动手实践</strong>：每章的练习题都需要实际编码，仅阅读难以真正掌握</li><li><strong>使用性能分析工具</strong>：Nsight Compute 和 Nsight Systems 是理解性能的最佳途径</li><li><strong>理解底层原理</strong>：API 会更新，但硬件原理和优化思想具有长期价值</li><li><strong>项目驱动</strong>：在真实项目中应用这些技术才能融会贯通</li></ol><p>GPU 并行编程的重要性日益凸显——深度学习、科学计算、图形渲染、高性能计算都离不开它。掌握 PMPP 的内容，就掌握了进入这些领域的核心技能。</p><p>祝学习进步！</p><hr><p><strong>参考资料：</strong></p><ul><li>Hwu, W., Kirk, D., &amp; El Hajj, I. (2022). <em>Programming Massively Parallel Processors: A Hands-on Approach</em> (4th Edition). Morgan Kaufmann.</li><li><a href="https://docs.nvidia.com/cuda/">NVIDIA CUDA 官方文档</a></li><li><a href="https://developer.nvidia.com/blog/">NVIDIA Developer Blog</a></li></ul><hr><blockquote><p><strong>本文 GitHub 仓库</strong>: <a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></p><p><strong>系列总览</strong>：本文是 PMPP 全书 22 章博客系列的导读索引。</p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;qian-yan&quot; tabindex=&quot;-1&quot; id=&quot;前言&quot;&gt;前言&lt;/h2&gt;
&lt;p&gt;《Programming Massively Parallel Processors: A Hands-on Approach》（简称 PMPP）是 GPU</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/categories/knowledge-sharing/pmpp/"/>
    
    
    <category term="学习路线" scheme="https://smarter.xin/tags/learning-path/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/tags/pmpp/"/>
    
    <category term="全书导读" scheme="https://smarter.xin/tags/book-guide/"/>
    
  </entry>
  
  <entry>
    <title>PMPP-第二十二章：高级实践与未来演变</title>
    <link href="https://smarter.xin/posts/a3a140de/"/>
    <id>https://smarter.xin/posts/a3a140de/</id>
    <published>2026-01-24T08:56:01.000Z</published>
    <updated>2026-03-16T05:46:43.228Z</updated>
    
    <content type="html"><![CDATA[<h2 id="qian-yan" tabindex="-1" id="前言">前言</h2><p>第二十一章介绍了动态并行性。作为全书的最后一章，第二十二章总结了<strong>高级实践（Advanced Practices）<strong>并展望了</strong>未来演变（Future Evolution）</strong>。本章不再引入新的编程技术，而是讨论如何在实际项目中应用前面学到的知识，以及 GPU 计算领域的发展趋势。掌握这些内容，将有助于在快速变化的技术环境中持续成长。</p><blockquote><p><strong>📦 配套资源</strong>：本系列文章配有完整的 <a href="https://github.com/psmarter/PMPP-Learning">GitHub 仓库</a>，包含每章的练习题解答、CUDA 代码实现和详细注释。所有代码都经过测试，可以直接运行。</p></blockquote><h2 id="xing-neng-you-hua-fang-fa-lun" tabindex="-1" id="性能优化方法论">性能优化方法论</h2><h3 id="xing-neng-fen-xi-qu-dong" tabindex="-1" id="性能分析驱动">性能分析驱动</h3><p><strong>不要猜测，要测量</strong>。</p><p>优化前必须先找到瓶颈：</p><ol><li>使用 Nsight Systems 分析整体流程</li><li>使用 Nsight Compute 分析单个 kernel</li><li>根据数据决定优化方向</li></ol><h3 id="roofline-mo-xing-hui-gu" tabindex="-1" id="Roofline-模型回顾">Roofline 模型回顾</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">性能上限 = min(峰值算力, 带宽 × 算术强度)</span><br></pre></td></tr></table></figure><p><strong>算术强度</strong> = FLOP / Byte</p><table><thead><tr><th>算术强度</th><th>瓶颈类型</th><th>优化方向</th></tr></thead><tbody><tr><td>&lt; 10</td><td>内存受限</td><td>提高数据复用、减少访存</td></tr><tr><td>&gt; 10</td><td>计算受限</td><td>优化计算效率、减少指令数</td></tr></tbody></table><h3 id="you-hua-ceng-ci" tabindex="-1" id="优化层次">优化层次</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────┐</span><br><span class="line">│ 第五层：算法选择                         │</span><br><span class="line">│   选择更高效的算法（收益最大）            │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第四层：数据布局                         │</span><br><span class="line">│   SoA vs AoS、对齐、Padding             │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第三层：并行策略                         │</span><br><span class="line">│   任务分解、负载均衡、线程映射            │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第二层：内存层次                         │</span><br><span class="line">│   共享内存、寄存器、缓存利用              │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第一层：指令级                           │</span><br><span class="line">│   循环展开、向量化、快速数学函数          │</span><br><span class="line">└─────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p><strong>从上往下优化</strong>：高层优化的收益通常远大于低层。</p><h2 id="chang-jian-you-hua-ji-zhu-zong-jie" tabindex="-1" id="常见优化技术总结">常见优化技术总结</h2><h3 id="nei-cun-you-hua" tabindex="-1" id="内存优化">内存优化</h3><table><thead><tr><th>技术</th><th>适用场景</th><th>效果</th></tr></thead><tbody><tr><td>合并访问</td><td>全局内存访问</td><td>减少内存事务</td></tr><tr><td>共享内存</td><td>数据复用</td><td>减少全局访问</td></tr><tr><td>常量内存</td><td>只读广播数据</td><td>利用缓存</td></tr><tr><td>纹理内存</td><td>2D 局部性</td><td>硬件插值</td></tr><tr><td>寄存器 Tiling</td><td>矩阵运算</td><td>最大化复用</td></tr></tbody></table><h3 id="ji-suan-you-hua" tabindex="-1" id="计算优化">计算优化</h3><table><thead><tr><th>技术</th><th>适用场景</th><th>效果</th></tr></thead><tbody><tr><td>循环展开</td><td>固定迭代循环</td><td>减少指令开销</td></tr><tr><td>线程粗化</td><td>每线程工作太少</td><td>减少调度开销</td></tr><tr><td>快速数学</td><td>精度要求不高</td><td>减少时钟周期</td></tr><tr><td>Warp Shuffle</td><td>Warp 内通信</td><td>避免共享内存</td></tr></tbody></table><h3 id="bing-xing-du-you-hua" tabindex="-1" id="并行度优化">并行度优化</h3><table><thead><tr><th>技术</th><th>适用场景</th><th>效果</th></tr></thead><tbody><tr><td>增加线程数</td><td>隐藏延迟</td><td>提高占用率</td></tr><tr><td>动态并行</td><td>递归/自适应</td><td>减少 CPU 参与</td></tr><tr><td>CUDA 流</td><td>多任务重叠</td><td>隐藏传输延迟</td></tr></tbody></table><h2 id="dai-ma-ke-yi-zhi-xing" tabindex="-1" id="代码可移植性">代码可移植性</h2><h3 id="kua-gpu-jia-gou" tabindex="-1" id="跨-GPU-架构">跨 GPU 架构</h3><p>不同 GPU 架构有不同特性：</p><table><thead><tr><th>架构</th><th>特点</th></tr></thead><tbody><tr><td>Kepler (sm_35)</td><td>动态并行首次支持</td></tr><tr><td>Maxwell (sm_50)</td><td>改进的共享内存</td></tr><tr><td>Pascal (sm_60)</td><td>统一内存改进</td></tr><tr><td>Volta (sm_70)</td><td>Tensor Core、独立线程调度</td></tr><tr><td>Ampere (sm_80)</td><td>异步拷贝、更大 L2</td></tr><tr><td>Hopper (sm_90)</td><td>Transformer Engine</td></tr></tbody></table><h3 id="bian-xie-ke-yi-zhi-dai-ma" tabindex="-1" id="编写可移植代码">编写可移植代码</h3><p><strong>参数化关键常量</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#if __CUDA_ARCH__ &gt;= 800</span><br><span class="line">    #define SHARED_MEM_SIZE 164 * 1024  // Ampere: 164KB</span><br><span class="line">#elif __CUDA_ARCH__ &gt;= 700</span><br><span class="line">    #define SHARED_MEM_SIZE 96 * 1024   // Volta: 96KB</span><br><span class="line">#else</span><br><span class="line">    #define SHARED_MEM_SIZE 48 * 1024   // 默认: 48KB</span><br><span class="line">#endif</span><br></pre></td></tr></table></figure><p><strong>运行时查询能力</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">cudaDeviceProp prop;</span><br><span class="line">cudaGetDeviceProperties(&amp;prop, 0);</span><br><span class="line"></span><br><span class="line">int blocks_per_sm = prop.maxBlocksPerMultiProcessor;</span><br><span class="line">int shared_mem = prop.sharedMemPerBlock;</span><br><span class="line">int max_threads = prop.maxThreadsPerBlock;</span><br></pre></td></tr></table></figure><h3 id="kua-ping-tai-ji-zhu" tabindex="-1" id="跨平台技术">跨平台技术</h3><table><thead><tr><th>技术</th><th>描述</th><th>优势</th></tr></thead><tbody><tr><td>CUDA</td><td>NVIDIA 专用</td><td>性能最佳</td></tr><tr><td>OpenCL</td><td>跨厂商标准</td><td>可移植性</td></tr><tr><td>SYCL</td><td>C++ 标准化</td><td>现代 C++ 风格</td></tr><tr><td>HIP</td><td>AMD 兼容 CUDA</td><td>易于迁移</td></tr><tr><td>Kokkos</td><td>抽象层</td><td>多后端支持</td></tr></tbody></table><h2 id="diao-shi-yu-yan-zheng" tabindex="-1" id="调试与验证">调试与验证</h2><h3 id="chang-jian-cuo-wu-lei-xing" tabindex="-1" id="常见错误类型">常见错误类型</h3><ol><li><strong>内存错误</strong>：越界访问、未初始化内存</li><li><strong>竞态条件</strong>：同步不当导致数据竞争</li><li><strong>数值误差</strong>：浮点精度、舍入误差</li><li><strong>死锁</strong>：同步原语使用不当</li></ol><h3 id="diao-shi-gong-ju" tabindex="-1" id="调试工具">调试工具</h3><table><thead><tr><th>工具</th><th>用途</th></tr></thead><tbody><tr><td>cuda-memcheck</td><td>内存错误检测</td></tr><tr><td>compute-sanitizer</td><td>新一代错误检测</td></tr><tr><td>Nsight Eclipse</td><td>IDE 集成调试</td></tr><tr><td>Nsight Visual Studio</td><td>Windows 调试</td></tr><tr><td>printf</td><td>简单调试（慎用）</td></tr></tbody></table><h3 id="yan-zheng-ce-lue" tabindex="-1" id="验证策略">验证策略</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">// 1. 保留 CPU 参考实现</span><br><span class="line">void cpu_reference(float *output, float *input, int n);</span><br><span class="line"></span><br><span class="line">// 2. 对比 GPU 结果</span><br><span class="line">bool verify_result(float *gpu, float *cpu, int n, float epsilon) &#123;</span><br><span class="line">    for (int i = 0; i &lt; n; i++) &#123;</span><br><span class="line">        if (fabsf(gpu[i] - cpu[i]) &gt; epsilon) &#123;</span><br><span class="line">            printf(&quot;Mismatch at %d: GPU=%.6f, CPU=%.6f\n&quot;, </span><br><span class="line">                   i, gpu[i], cpu[i]);</span><br><span class="line">            return false;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return true;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 3. 渐进式测试</span><br><span class="line">// 小规模 → 中规模 → 大规模</span><br><span class="line">// 1 Block → 多 Block → 多 SM</span><br></pre></td></tr></table></figure><h2 id="sheng-chan-huan-jing-zui-jia-shi-jian" tabindex="-1" id="生产环境最佳实践">生产环境最佳实践</h2><h3 id="cuo-wu-chu-li" tabindex="-1" id="错误处理">错误处理</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">#define CUDA_CHECK(call) \</span><br><span class="line">    do &#123; \</span><br><span class="line">        cudaError_t err = call; \</span><br><span class="line">        if (err != cudaSuccess) &#123; \</span><br><span class="line">            fprintf(stderr, &quot;CUDA error at %s:%d: %s\n&quot;, \</span><br><span class="line">                    __FILE__, __LINE__, cudaGetErrorString(err)); \</span><br><span class="line">            exit(EXIT_FAILURE); \</span><br><span class="line">        &#125; \</span><br><span class="line">    &#125; while(0)</span><br><span class="line"></span><br><span class="line">// 使用</span><br><span class="line">CUDA_CHECK(cudaMalloc(&amp;d_ptr, size));</span><br><span class="line">CUDA_CHECK(cudaMemcpy(d_ptr, h_ptr, size, cudaMemcpyHostToDevice));</span><br></pre></td></tr></table></figure><h3 id="zi-yuan-guan-li" tabindex="-1" id="资源管理">资源管理</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">// RAII 风格管理 CUDA 资源</span><br><span class="line">class CudaBuffer &#123;</span><br><span class="line">    void *ptr;</span><br><span class="line">    size_t size;</span><br><span class="line">public:</span><br><span class="line">    CudaBuffer(size_t n) : size(n) &#123;</span><br><span class="line">        CUDA_CHECK(cudaMalloc(&amp;ptr, n));</span><br><span class="line">    &#125;</span><br><span class="line">    ~CudaBuffer() &#123;</span><br><span class="line">        cudaFree(ptr);</span><br><span class="line">    &#125;</span><br><span class="line">    void* get() &#123; return ptr; &#125;</span><br><span class="line">    // 禁止拷贝</span><br><span class="line">    CudaBuffer(const CudaBuffer&amp;) = delete;</span><br><span class="line">    CudaBuffer&amp; operator=(const CudaBuffer&amp;) = delete;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="xing-neng-jian-kong" tabindex="-1" id="性能监控">性能监控</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">// 计时</span><br><span class="line">cudaEvent_t start, stop;</span><br><span class="line">cudaEventCreate(&amp;start);</span><br><span class="line">cudaEventCreate(&amp;stop);</span><br><span class="line"></span><br><span class="line">cudaEventRecord(start);</span><br><span class="line">my_kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(args);</span><br><span class="line">cudaEventRecord(stop);</span><br><span class="line"></span><br><span class="line">cudaEventSynchronize(stop);</span><br><span class="line">float milliseconds = 0;</span><br><span class="line">cudaEventElapsedTime(&amp;milliseconds, start, stop);</span><br><span class="line"></span><br><span class="line">printf(&quot;Kernel time: %.3f ms\n&quot;, milliseconds);</span><br><span class="line"></span><br><span class="line">cudaEventDestroy(start);</span><br><span class="line">cudaEventDestroy(stop);</span><br></pre></td></tr></table></figure><h2 id="gpu-ji-suan-sheng-tai-xi-tong" tabindex="-1" id="GPU-计算生态系统">GPU 计算生态系统</h2><h3 id="cuda-ku" tabindex="-1" id="CUDA-库">CUDA 库</h3><table><thead><tr><th>库</th><th>领域</th><th>功能</th></tr></thead><tbody><tr><td>cuBLAS</td><td>线性代数</td><td>矩阵运算</td></tr><tr><td>cuFFT</td><td>信号处理</td><td>傅里叶变换</td></tr><tr><td>cuDNN</td><td>深度学习</td><td>神经网络原语</td></tr><tr><td>cuSPARSE</td><td>稀疏矩阵</td><td>稀疏运算</td></tr><tr><td>cuRAND</td><td>随机数</td><td>伪随机生成</td></tr><tr><td>Thrust</td><td>通用</td><td>STL 风格容器</td></tr><tr><td>CUB</td><td>底层</td><td>原语库</td></tr></tbody></table><h3 id="shi-yong-ku-de-you-shi" tabindex="-1" id="使用库的优势">使用库的优势</h3><ol><li><strong>经过高度优化</strong>：专家团队持续调优</li><li><strong>版本间改进</strong>：自动受益于新架构优化</li><li><strong>减少开发时间</strong>：专注业务逻辑</li><li><strong>减少错误</strong>：经过广泛测试</li></ol><h3 id="he-shi-zi-ji-shi-xian" tabindex="-1" id="何时自己实现">何时自己实现</h3><ul><li>特殊需求库不满足</li><li>库的通用实现不够高效</li><li>学习目的</li></ul><h2 id="wei-lai-qu-shi" tabindex="-1" id="未来趋势">未来趋势</h2><h3 id="ying-jian-yan-jin" tabindex="-1" id="硬件演进">硬件演进</h3><p><strong>更多核心</strong>：</p><ul><li>SM 数量持续增加</li><li>更高的并行度</li><li>对负载均衡要求更高</li></ul><p><strong>专用加速器</strong>：</p><ul><li>Tensor Core（矩阵运算）</li><li>RT Core（光线追踪）</li><li>Transformer Engine（AI）</li></ul><p><strong>内存技术</strong>：</p><ul><li>HBM 带宽持续增长</li><li>更大的 L2 缓存</li><li>统一内存性能改进</li></ul><h3 id="bian-cheng-mo-xing-yan-jin" tabindex="-1" id="编程模型演进">编程模型演进</h3><p><strong>更高抽象</strong>：</p><ul><li>CUDA Graphs：静态任务图</li><li>CUDA Cooperative Groups：灵活同步</li><li>C++ 标准并行：std::execution</li></ul><p><strong>跨平台标准</strong>：</p><ul><li>SYCL 普及</li><li>oneAPI 生态</li><li>可移植性重要性增加</li></ul><h3 id="ying-yong-ling-yu-kuo-zhan" tabindex="-1" id="应用领域扩展">应用领域扩展</h3><p><strong>传统 HPC</strong>：</p><ul><li>气象模拟</li><li>分子动力学</li><li>流体力学</li></ul><p><strong>AI/ML</strong>：</p><ul><li>大语言模型训练</li><li>推理加速</li><li>自动驾驶</li></ul><p><strong>新兴领域</strong>：</p><ul><li>量子计算模拟</li><li>数字孪生</li><li>元宇宙渲染</li></ul><h2 id="chi-xu-xue-xi-zi-yuan" tabindex="-1" id="持续学习资源">持续学习资源</h2><h3 id="guan-fang-zi-yuan" tabindex="-1" id="官方资源">官方资源</h3><ul><li><strong>CUDA 文档</strong>：<a href="https://docs.nvidia.com/cuda/">https://docs.nvidia.com/cuda/</a></li><li><strong>NVIDIA Developer Blog</strong>：技术文章和最佳实践</li><li><strong>GTC 大会</strong>：最新技术发布</li><li><strong>CUDA Zone</strong>：示例代码和教程</li></ul><h3 id="xue-zhu-zi-yuan" tabindex="-1" id="学术资源">学术资源</h3><ul><li><strong>PMPP 教材</strong>：本书是经典参考</li><li><strong>UIUC ECE 408</strong>：配套课程（Coursera 可看）</li><li><strong>Stanford CS 149</strong>：并行计算基础</li><li><strong>论文</strong>：GTC、SC、ICS 会议</li></ul><h3 id="she-qu" tabindex="-1" id="社区">社区</h3><ul><li><strong>Stack Overflow</strong>：cuda 标签</li><li><strong>NVIDIA 开发者论坛</strong>：官方支持</li><li><strong>GitHub</strong>：开源 CUDA 项目</li><li><strong>Reddit r/CUDA</strong>：讨论社区</li></ul><h2 id="quan-shu-hui-gu" tabindex="-1" id="全书回顾">全书回顾</h2><h3 id="ji-chu-pian-1-6-zhang" tabindex="-1" id="基础篇（1-6章）">基础篇（1-6章）</h3><table><thead><tr><th>章节</th><th>主题</th><th>核心概念</th></tr></thead><tbody><tr><td>1</td><td>引言</td><td>异构计算、CUDA 生态</td></tr><tr><td>2</td><td>数据并行</td><td>Thread、Block、Grid</td></tr><tr><td>3</td><td>多维数据</td><td>线程索引、边界检查</td></tr><tr><td>4</td><td>计算架构</td><td>SM、Warp、调度</td></tr><tr><td>5</td><td>内存架构</td><td>层次结构、Tiling</td></tr><tr><td>6</td><td>性能考虑</td><td>合并访问、发散、资源</td></tr></tbody></table><h3 id="mo-shi-pian-7-15-zhang" tabindex="-1" id="模式篇（7-15章）">模式篇（7-15章）</h3><table><thead><tr><th>章节</th><th>主题</th><th>核心模式</th></tr></thead><tbody><tr><td>7</td><td>卷积</td><td>Tiling、Halo</td></tr><tr><td>8</td><td>模板</td><td>缓存、Register Tiling</td></tr><tr><td>9</td><td>直方图</td><td>原子操作、私有化</td></tr><tr><td>10</td><td>归约</td><td>树形归约、Warp 原语</td></tr><tr><td>11</td><td>前缀和</td><td>Scan、Work-Efficient</td></tr><tr><td>12</td><td>归并</td><td>Co-Rank、循环展开</td></tr><tr><td>13</td><td>排序</td><td>基数排序、并行归并</td></tr><tr><td>14</td><td>稀疏矩阵</td><td>CSR/ELL/COO 格式</td></tr><tr><td>15</td><td>图算法</td><td>BFS、边界推进</td></tr></tbody></table><h3 id="ying-yong-pian-16-18-zhang" tabindex="-1" id="应用篇（16-18章）">应用篇（16-18章）</h3><table><thead><tr><th>章节</th><th>主题</th><th>应用领域</th></tr></thead><tbody><tr><td>16</td><td>深度学习</td><td>卷积、池化、全连接</td></tr><tr><td>17</td><td>MRI 重建</td><td>NUFFT、共轭梯度</td></tr><tr><td>18</td><td>静电势能</td><td>N-body、空间分区</td></tr></tbody></table><h3 id="gao-ji-pian-19-22-zhang" tabindex="-1" id="高级篇（19-22章）">高级篇（19-22章）</h3><table><thead><tr><th>章节</th><th>主题</th><th>核心内容</th></tr></thead><tbody><tr><td>19</td><td>计算思维</td><td>方法论、设计原则</td></tr><tr><td>20</td><td>集群编程</td><td>MPI+CUDA、Halo交换</td></tr><tr><td>21</td><td>动态并行</td><td>设备端启动、递归</td></tr><tr><td>22</td><td>高级实践</td><td>最佳实践、未来趋势</td></tr></tbody></table><h2 id="xie-zai-zui-hou" tabindex="-1" id="写在最后">写在最后</h2><h3 id="bing-xing-bian-cheng-xin-fa-shi-tiao" tabindex="-1" id="并行编程心法十条">并行编程心法十条</h3><ol><li><strong>理解硬件</strong>：了解 GPU 架构，扬长避短</li><li><strong>数据为王</strong>：性能通常受限于数据移动</li><li><strong>最大化并行</strong>：暴露足够的并行性隐藏延迟</li><li><strong>最小化同步</strong>：同步是性能杀手</li><li><strong>合并访问</strong>：让内存访问连续</li><li><strong>复用数据</strong>：共享内存是你最好的朋友</li><li><strong>避免发散</strong>：让 Warp 内线程走相同路径</li><li><strong>权衡取舍</strong>：没有银弹，只有适合的解</li><li><strong>Profile 优先</strong>：数据驱动优化，不要猜测</li><li><strong>渐进迭代</strong>：先正确，后优化，持续改进</li></ol><h3 id="cong-xue-xi-dao-shi-jian" tabindex="-1" id="从学习到实践">从学习到实践</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">阶段一：理解基础</span><br><span class="line">  ↓ 多写代码、多做练习</span><br><span class="line">阶段二：掌握模式</span><br><span class="line">  ↓ 在实际项目中应用</span><br><span class="line">阶段三：形成直觉</span><br><span class="line">  ↓ 阅读论文、参与开源</span><br><span class="line">阶段四：持续精进</span><br></pre></td></tr></table></figure><h3 id="jie-yu" tabindex="-1" id="结语">结语</h3><p>这本书带你从 CUDA 入门走到了高级实践。但学习永无止境——GPU 技术快速演进，新架构、新特性不断涌现。</p><p><strong>核心能力</strong>胜过<strong>具体知识</strong>：</p><ul><li>理解并行计算原理 &gt; 记住 API 细节</li><li>掌握优化方法论 &gt; 背诵优化技巧</li><li>培养计算思维 &gt; 复制代码模板</li></ul><p>希望这本书和这个系列博客能帮助你建立扎实的并行计算基础。接下来，不断实践、持续学习，在 GPU 计算的世界里探索更多可能！</p><hr><p><strong>参考资料：</strong></p><ul><li>Hwu, W., Kirk, D., &amp; El Hajj, I. (2022). <em>Programming Massively Parallel Processors: A Hands-on Approach</em> (4th Edition). Morgan Kaufmann.</li><li>NVIDIA. <em>CUDA C++ Programming Guide</em>. <a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">https://docs.nvidia.com/cuda/cuda-c-programming-guide/</a></li><li>NVIDIA. <em>CUDA C++ Best Practices Guide</em>. <a href="https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/">https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/</a></li></ul><hr><blockquote><p><strong>本文 GitHub 仓库</strong>: <a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></p><p><strong>系列完结</strong>：感谢阅读 PMPP 全书 22 章博客系列！</p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;qian-yan&quot; tabindex=&quot;-1&quot; id=&quot;前言&quot;&gt;前言&lt;/h2&gt;
&lt;p&gt;第二十一章介绍了动态并行性。作为全书的最后一章，第二十二章总结了&lt;strong&gt;高级实践（Advanced</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/categories/knowledge-sharing/pmpp/"/>
    
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/tags/pmpp/"/>
    
    <category term="未来趋势" scheme="https://smarter.xin/tags/future-trends/"/>
    
    <category term="最佳实践" scheme="https://smarter.xin/tags/best-practices/"/>
    
  </entry>
  
  <entry>
    <title>PMPP-第二十一章：CUDA动态并行性</title>
    <link href="https://smarter.xin/posts/e519c4bc/"/>
    <id>https://smarter.xin/posts/e519c4bc/</id>
    <published>2026-01-24T08:20:44.000Z</published>
    <updated>2026-03-16T05:46:43.228Z</updated>
    
    <content type="html"><![CDATA[<h2 id="qian-yan" tabindex="-1" id="前言">前言</h2><p>第二十章介绍了多 GPU 集群编程。第二十一章回到单 GPU 的高级特性——<strong>动态并行性（Dynamic Parallelism）</strong>。传统 CUDA 程序中，只有 CPU 能启动核函数；而动态并行性允许<strong>GPU 核函数直接启动子核函数</strong>，无需返回 CPU。这一特性对递归算法、自适应计算、不规则数据结构（如树、图）的处理特别有用。</p><blockquote><p><strong>📦 配套资源</strong>：本系列文章配有完整的 <a href="https://github.com/psmarter/PMPP-Learning">GitHub 仓库</a>，包含每章的练习题解答、CUDA 代码实现和详细注释。所有代码都经过测试，可以直接运行。</p></blockquote><h2 id="shi-yao-shi-dong-tai-bing-xing-xing" tabindex="-1" id="什么是动态并行性">什么是动态并行性</h2><h3 id="chuan-tong-cuda-mo-xing-de-xian-zhi" tabindex="-1" id="传统-CUDA-模型的限制">传统 CUDA 模型的限制</h3><p>传统 CUDA 程序中：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CPU 代码 → 启动 kernel1 → GPU 执行 → 返回 CPU</span><br><span class="line">         → 启动 kernel2 → GPU 执行 → 返回 CPU</span><br><span class="line">         → ...</span><br></pre></td></tr></table></figure><p><strong>问题</strong>：</p><ol><li>每次启动 kernel 都需要 CPU 参与</li><li>递归算法难以表达</li><li>自适应算法需要多次 CPU-GPU 往返</li></ol><h3 id="dong-tai-bing-xing-xing" tabindex="-1" id="动态并行性">动态并行性</h3><p><strong>动态并行性</strong>：允许 GPU kernel 直接启动子 kernel（child kernel）。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CPU 代码 → 启动父 kernel → GPU 执行</span><br><span class="line">                           ├→ 启动子 kernel1 → GPU 执行</span><br><span class="line">                           ├→ 启动子 kernel2 → GPU 执行</span><br><span class="line">                           └→ ...</span><br></pre></td></tr></table></figure><p><strong>优势</strong>：</p><ul><li>减少 CPU-GPU 通信</li><li>自然表达递归算法</li><li>根据数据特性动态调整计算</li></ul><h3 id="ying-jian-yao-qiu" tabindex="-1" id="硬件要求">硬件要求</h3><p>动态并行性需要计算能力 3.5 或更高的 GPU。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 编译时需要特殊选项</span></span><br><span class="line">nvcc -<span class="built_in">arch</span>=sm_35 -rdc=<span class="literal">true</span> my_program.cu -lcudadevrt</span><br></pre></td></tr></table></figure><ul><li><code>-rdc=true</code>：启用可重定位设备代码</li><li><code>-lcudadevrt</code>：链接设备运行时库</li></ul><h2 id="ji-ben-yu-fa" tabindex="-1" id="基本语法">基本语法</h2><h3 id="zai-she-bei-dai-ma-zhong-qi-dong-kernel" tabindex="-1" id="在设备代码中启动-kernel">在设备代码中启动 kernel</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">__global__ void child_kernel(int *data, int n) &#123;</span><br><span class="line">    int idx = threadIdx.x + blockIdx.x * blockDim.x;</span><br><span class="line">    if (idx &lt; n) &#123;</span><br><span class="line">        data[idx] *= 2;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__global__ void parent_kernel(int *data, int n) &#123;</span><br><span class="line">    // 在设备代码中启动子 kernel</span><br><span class="line">    child_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;(data, n);</span><br><span class="line">    </span><br><span class="line">    // 等待子 kernel 完成</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="she-bei-duan-tong-bu" tabindex="-1" id="设备端同步">设备端同步</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 等待当前线程启动的所有子 kernel 完成</span><br><span class="line">cudaDeviceSynchronize();</span><br><span class="line"></span><br><span class="line">// 等待特定流中的操作完成</span><br><span class="line">cudaStreamSynchronize(stream);</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：父 kernel 退出时会隐式等待所有子 kernel 完成。</p><h3 id="she-bei-duan-nei-cun-guan-li" tabindex="-1" id="设备端内存管理">设备端内存管理</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel() &#123;</span><br><span class="line">    float *temp;</span><br><span class="line">    </span><br><span class="line">    // 在设备端分配内存</span><br><span class="line">    cudaMalloc(&amp;temp, 1024 * sizeof(float));</span><br><span class="line">    </span><br><span class="line">    // 使用内存...</span><br><span class="line">    child_kernel&lt;&lt;&lt;1, 256&gt;&gt;&gt;(temp, 1024);</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">    </span><br><span class="line">    // 释放内存</span><br><span class="line">    cudaFree(temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="nei-cun-ke-jian-xing" tabindex="-1" id="内存可见性">内存可见性</h2><h3 id="na-xie-nei-cun-zi-kernel-ke-yi-fang-wen" tabindex="-1" id="哪些内存子-kernel-可以访问">哪些内存子 kernel 可以访问</h3><table><thead><tr><th>内存类型</th><th>子 kernel 可访问</th><th>说明</th></tr></thead><tbody><tr><td>全局内存</td><td>✓</td><td>父子共享</td></tr><tr><td>常量内存</td><td>✓</td><td>编译时确定，共享</td></tr><tr><td>纹理内存</td><td>✓</td><td>父子共享</td></tr><tr><td>共享内存</td><td>✗</td><td>仅限当前 Block</td></tr><tr><td>局部内存</td><td>✗</td><td>仅限当前线程</td></tr></tbody></table><h3 id="nei-cun-yi-zhi-xing" tabindex="-1" id="内存一致性">内存一致性</h3><p>父 kernel 的全局内存写入对子 kernel 可见，需要遵循一定规则：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel(int *data) &#123;</span><br><span class="line">    // 写入全局内存</span><br><span class="line">    data[threadIdx.x] = threadIdx.x * 2;</span><br><span class="line">    </span><br><span class="line">    // 确保写入完成（块内同步）</span><br><span class="line">    __threadfence();</span><br><span class="line">    </span><br><span class="line">    // 启动子 kernel</span><br><span class="line">    if (threadIdx.x == 0) &#123;</span><br><span class="line">        child_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;(data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>关键点</strong>：</p><ul><li>父线程在启动子 kernel 前的写入对子 kernel 可见</li><li>子 kernel 的写入在 <code>cudaDeviceSynchronize()</code> 后对父线程可见</li></ul><h2 id="liu-yu-bing-fa" tabindex="-1" id="流与并发">流与并发</h2><h3 id="mo-ren-liu-xing-wei" tabindex="-1" id="默认流行为">默认流行为</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel() &#123;</span><br><span class="line">    // 这两个 kernel 在同一个 Block 的默认流中顺序执行</span><br><span class="line">    child_kernel1&lt;&lt;&lt;1, 32&gt;&gt;&gt;();</span><br><span class="line">    child_kernel2&lt;&lt;&lt;1, 32&gt;&gt;&gt;();  // 等待 child_kernel1 完成</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>重要</strong>：同一 Block 内的线程共享默认流，因此子 kernel 会串行执行。</p><h3 id="shi-yong-du-li-liu-shi-xian-bing-fa" tabindex="-1" id="使用独立流实现并发">使用独立流实现并发</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel() &#123;</span><br><span class="line">    cudaStream_t stream;</span><br><span class="line">    cudaStreamCreateWithFlags(&amp;stream, cudaStreamNonBlocking);</span><br><span class="line">    </span><br><span class="line">    // 在独立流中启动，可以与其他 kernel 并发</span><br><span class="line">    child_kernel&lt;&lt;&lt;1, 32, 0, stream&gt;&gt;&gt;();</span><br><span class="line">    </span><br><span class="line">    cudaStreamDestroy(stream);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="bing-fa-zi-kernel-shi-li" tabindex="-1" id="并发子-kernel-示例">并发子 kernel 示例</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel(float *data, int n) &#123;</span><br><span class="line">    int chunk_size = n / 4;</span><br><span class="line">    cudaStream_t streams[4];</span><br><span class="line">    </span><br><span class="line">    for (int i = 0; i &lt; 4; i++) &#123;</span><br><span class="line">        cudaStreamCreateWithFlags(&amp;streams[i], cudaStreamNonBlocking);</span><br><span class="line">        child_kernel&lt;&lt;&lt;1, 256, 0, streams[i]&gt;&gt;&gt;(</span><br><span class="line">            data + i * chunk_size, chunk_size);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 等待所有流完成</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">    </span><br><span class="line">    for (int i = 0; i &lt; 4; i++) &#123;</span><br><span class="line">        cudaStreamDestroy(streams[i]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="qi-dong-chi-pei-zhi" tabindex="-1" id="启动池配置">启动池配置</h2><h3 id="shi-yao-shi-qi-dong-chi" tabindex="-1" id="什么是启动池">什么是启动池</h3><p>每次子 kernel 启动需要从<strong>启动池</strong>分配资源。</p><p><strong>两种池</strong>：</p><ol><li><strong>固定大小池</strong>：默认 2048 个槽位，预分配</li><li><strong>虚拟化池</strong>：动态扩展，但性能较低</li></ol><h3 id="pei-zhi-qi-dong-chi" tabindex="-1" id="配置启动池">配置启动池</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 获取当前设备属性</span><br><span class="line">cudaDeviceProp prop;</span><br><span class="line">cudaGetDeviceProperties(&amp;prop, 0);</span><br><span class="line"></span><br><span class="line">// 设置固定池大小</span><br><span class="line">cudaDeviceSetLimit(cudaLimitDevRuntimePendingLaunchCount, 8192);</span><br></pre></td></tr></table></figure><p><strong>建议</strong>：</p><ul><li>如果预期子 kernel 数量超过默认值，增大固定池</li><li>如果子 kernel 数量可预测，设置为该值</li></ul><h2 id="ying-yong-shi-li-bezier-qu-xian-xi-fen" tabindex="-1" id="应用示例：Bezier-曲线细分">应用示例：Bezier 曲线细分</h2><h3 id="wen-ti-miao-shu" tabindex="-1" id="问题描述">问题描述</h3><p>Bezier 曲线由控制点定义。曲线越弯曲，需要越多采样点才能平滑显示。</p><p><strong>自适应细分</strong>：根据曲率决定采样点数量。</p><h3 id="shu-ju-jie-gou" tabindex="-1" id="数据结构">数据结构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#define MAX_TESS_POINTS 32</span><br><span class="line"></span><br><span class="line">struct BezierLine &#123;</span><br><span class="line">    float2 CP[3];                      // 3 个控制点</span><br><span class="line">    float2 vertexPos[MAX_TESS_POINTS]; // 细分后的顶点</span><br><span class="line">    int nVertices;                     // 顶点数量</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="ji-suan-qu-lu" tabindex="-1" id="计算曲率">计算曲率</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">__device__ float computeCurvature(float2 *cp) &#123;</span><br><span class="line">    // 计算首尾连线长度</span><br><span class="line">    float dx = cp[2].x - cp[0].x;</span><br><span class="line">    float dy = cp[2].y - cp[0].y;</span><br><span class="line">    float line_length = sqrtf(dx * dx + dy * dy);</span><br><span class="line">    </span><br><span class="line">    if (line_length &lt; 0.001f) return 0.0f;</span><br><span class="line">    </span><br><span class="line">    // 计算中点到直线的距离（曲率近似）</span><br><span class="line">    float cross = fabsf((cp[1].x - cp[0].x) * dy - </span><br><span class="line">                        (cp[1].y - cp[0].y) * dx);</span><br><span class="line">    return cross / line_length;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zi-kernel-ji-suan-xi-fen-dian" tabindex="-1" id="子-kernel：计算细分点">子 kernel：计算细分点</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">__global__ void computeBezierLine_child(</span><br><span class="line">    int lidx, BezierLine *bLines, int nTessPoints) &#123;</span><br><span class="line">    </span><br><span class="line">    int idx = threadIdx.x + blockDim.x * blockIdx.x;</span><br><span class="line">    if (idx &lt; nTessPoints) &#123;</span><br><span class="line">        // 计算参数 u ∈ [0, 1]</span><br><span class="line">        float u = (float)idx / (float)(nTessPoints - 1);</span><br><span class="line">        float omu = 1.0f - u;</span><br><span class="line">        </span><br><span class="line">        // 二次 Bezier 基函数</span><br><span class="line">        float B[3];</span><br><span class="line">        B[0] = omu * omu;</span><br><span class="line">        B[1] = 2.0f * u * omu;</span><br><span class="line">        B[2] = u * u;</span><br><span class="line">        </span><br><span class="line">        // 计算点位置</span><br><span class="line">        float2 pos = &#123;0, 0&#125;;</span><br><span class="line">        for (int i = 0; i &lt; 3; i++) &#123;</span><br><span class="line">            pos.x += B[i] * bLines[lidx].CP[i].x;</span><br><span class="line">            pos.y += B[i] * bLines[lidx].CP[i].y;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        bLines[lidx].vertexPos[idx] = pos;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="fu-kernel-dong-tai-jue-ding-xi-fen-cheng-du" tabindex="-1" id="父-kernel：动态决定细分程度">父 kernel：动态决定细分程度</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">__global__ void computeBezierLines_parent(</span><br><span class="line">    BezierLine *bLines, int nLines) &#123;</span><br><span class="line">    </span><br><span class="line">    int lidx = threadIdx.x + blockDim.x * blockIdx.x;</span><br><span class="line">    if (lidx &lt; nLines) &#123;</span><br><span class="line">        // 根据曲率计算需要的顶点数</span><br><span class="line">        float curvature = computeCurvature(bLines[lidx].CP);</span><br><span class="line">        int nVertices = min(max((int)(curvature * 16.0f), 4), </span><br><span class="line">                            MAX_TESS_POINTS);</span><br><span class="line">        bLines[lidx].nVertices = nVertices;</span><br><span class="line">        </span><br><span class="line">        // 动态启动子 kernel</span><br><span class="line">        int blocks = (nVertices + 31) / 32;</span><br><span class="line">        computeBezierLine_child&lt;&lt;&lt;blocks, 32&gt;&gt;&gt;(</span><br><span class="line">            lidx, bLines, nVertices);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="dong-tai-vs-jing-tai-dui-bi" tabindex="-1" id="动态-vs-静态对比">动态 vs 静态对比</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">// 静态版本：每条曲线使用固定 Block，循环处理</span><br><span class="line">__global__ void computeBezierLines_static(</span><br><span class="line">    BezierLine *bLines, int nLines) &#123;</span><br><span class="line">    </span><br><span class="line">    int bidx = blockIdx.x;</span><br><span class="line">    if (bidx &lt; nLines) &#123;</span><br><span class="line">        float curvature = computeCurvature(bLines[bidx].CP);</span><br><span class="line">        int nVertices = min(max((int)(curvature * 16.0f), 4), </span><br><span class="line">                            MAX_TESS_POINTS);</span><br><span class="line">        bLines[bidx].nVertices = nVertices;</span><br><span class="line">        </span><br><span class="line">        // 用循环代替子 kernel</span><br><span class="line">        for (int inc = 0; inc &lt; nVertices; inc += blockDim.x) &#123;</span><br><span class="line">            int idx = inc + threadIdx.x;</span><br><span class="line">            if (idx &lt; nVertices) &#123;</span><br><span class="line">                // ... 计算顶点 ...</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>对比</strong>：</p><table><thead><tr><th>方面</th><th>动态并行</th><th>静态版本</th></tr></thead><tbody><tr><td>代码结构</td><td>更自然</td><td>需要手动循环</td></tr><tr><td>资源利用</td><td>子 kernel 精确配置</td><td>可能浪费线程</td></tr><tr><td>启动开销</td><td>较高</td><td>无额外开销</td></tr><tr><td>适用场景</td><td>工作量变化大</td><td>工作量均匀</td></tr></tbody></table><h2 id="ying-yong-shi-li-si-cha-shu-gou-jian" tabindex="-1" id="应用示例：四叉树构建">应用示例：四叉树构建</h2><h3 id="wen-ti-miao-shu-1" tabindex="-1" id="问题描述-2">问题描述</h3><p><strong>四叉树（Quadtree）</strong>：递归地将 2D 空间划分为四个象限，用于空间索引、碰撞检测、图像压缩等。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">初始区域         第一次划分         继续划分...</span><br><span class="line">┌─────────┐     ┌────┬────┐     ┌──┬──┬────┐</span><br><span class="line">│         │     │ TL │ TR │     │  │  │ TR │</span><br><span class="line">│    *    │  →  ├────┼────┤  →  ├──┼──┼────┤</span><br><span class="line">│   **    │     │ BL │ BR │     │  │  │    │</span><br><span class="line">└─────────┘     └────┴────┘     └──┴──┴────┘</span><br></pre></td></tr></table></figure><h3 id="di-gui-suan-fa" tabindex="-1" id="递归算法">递归算法</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">function build_quadtree(node, points):</span><br><span class="line">    if depth &gt; max_depth or len(points) &lt; threshold:</span><br><span class="line">        return  // 停止递归</span><br><span class="line">    </span><br><span class="line">    // 将点分类到四个象限</span><br><span class="line">    for each point in points:</span><br><span class="line">        classify to TL, TR, BL, or BR</span><br><span class="line">    </span><br><span class="line">    // 递归处理每个象限</span><br><span class="line">    build_quadtree(node.TL, TL_points)</span><br><span class="line">    build_quadtree(node.TR, TR_points)</span><br><span class="line">    build_quadtree(node.BL, BL_points)</span><br><span class="line">    build_quadtree(node.BR, BR_points)</span><br></pre></td></tr></table></figure><h3 id="shu-ju-jie-gou-1" tabindex="-1" id="数据结构-2">数据结构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">class Quadtree_node &#123;</span><br><span class="line">    int m_id;                  // 节点 ID</span><br><span class="line">    Bounding_box m_bbox;       // 边界框</span><br><span class="line">    int m_begin, m_end;        // 点范围 [begin, end)</span><br><span class="line">    </span><br><span class="line">public:</span><br><span class="line">    __device__ int num_points() const &#123; return m_end - m_begin; &#125;</span><br><span class="line">    // ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">struct Parameters &#123;</span><br><span class="line">    int point_selector;         // 双缓冲选择器</span><br><span class="line">    int num_nodes_at_this_level; // 当前层节点数</span><br><span class="line">    int depth;                  // 当前深度</span><br><span class="line">    int max_depth;              // 最大深度</span><br><span class="line">    int min_points_per_node;    // 停止阈值</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="he-xin-kernel" tabindex="-1" id="核心-kernel">核心 kernel</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line">__global__ void build_quadtree_kernel(</span><br><span class="line">    Quadtree_node *nodes, Points *points, Parameters params) &#123;</span><br><span class="line">    </span><br><span class="line">    __shared__ int smem[8];  // 每个象限的点数和偏移</span><br><span class="line">    </span><br><span class="line">    Quadtree_node *node = &amp;nodes[blockIdx.x];</span><br><span class="line">    int num_points = node-&gt;num_points();</span><br><span class="line">    </span><br><span class="line">    // 检查停止条件</span><br><span class="line">    if (params.depth &gt;= params.max_depth || </span><br><span class="line">        num_points &lt;= params.min_points_per_node) &#123;</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 计算边界框中心</span><br><span class="line">    float2 center;</span><br><span class="line">    node-&gt;bounding_box().compute_center(&amp;center);</span><br><span class="line">    </span><br><span class="line">    // 统计每个象限的点数</span><br><span class="line">    count_points_in_children(points[params.point_selector], </span><br><span class="line">                              smem, node, center);</span><br><span class="line">    </span><br><span class="line">    // 计算重排偏移</span><br><span class="line">    scan_for_offsets(node-&gt;points_begin(), smem);</span><br><span class="line">    </span><br><span class="line">    // 重排点到双缓冲</span><br><span class="line">    reorder_points(points[(params.point_selector + 1) % 2],</span><br><span class="line">                   points[params.point_selector], smem, node, center);</span><br><span class="line">    </span><br><span class="line">    // 递归启动子 kernel</span><br><span class="line">    if (threadIdx.x == 0) &#123;</span><br><span class="line">        Quadtree_node *children = </span><br><span class="line">            &amp;nodes[params.num_nodes_at_this_level + blockIdx.x * 4];</span><br><span class="line">        </span><br><span class="line">        prepare_children(children, node, smem, center);</span><br><span class="line">        </span><br><span class="line">        Parameters next_params(params, true);  // 更新参数</span><br><span class="line">        </span><br><span class="line">        for (int i = 0; i &lt; 4; i++) &#123;</span><br><span class="line">            if (smem[i] &gt; 0) &#123;  // 只处理非空象限</span><br><span class="line">                build_quadtree_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;(</span><br><span class="line">                    &amp;children[i], points, next_params);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="dian-fen-lei-tong-ji-jie-duan" tabindex="-1" id="点分类（统计阶段）">点分类（统计阶段）</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">__device__ void count_points_in_children(</span><br><span class="line">    const Points &amp;in_points, int *smem, </span><br><span class="line">    int range_begin, int range_end, float2 center) &#123;</span><br><span class="line">    </span><br><span class="line">    // 初始化计数</span><br><span class="line">    if (threadIdx.x &lt; 4) &#123;</span><br><span class="line">        smem[threadIdx.x] = 0;</span><br><span class="line">    &#125;</span><br><span class="line">    __syncthreads();</span><br><span class="line">    </span><br><span class="line">    // 每个线程处理多个点</span><br><span class="line">    for (int iter = range_begin + threadIdx.x; </span><br><span class="line">         iter &lt; range_end; iter += blockDim.x) &#123;</span><br><span class="line">        float2 p = in_points.get_point(iter);</span><br><span class="line">        </span><br><span class="line">        if (p.x &lt; center.x &amp;&amp; p.y &gt;= center.y) &#123;</span><br><span class="line">            atomicAdd(&amp;smem[0], 1);  // 左上</span><br><span class="line">        &#125; else if (p.x &gt;= center.x &amp;&amp; p.y &gt;= center.y) &#123;</span><br><span class="line">            atomicAdd(&amp;smem[1], 1);  // 右上</span><br><span class="line">        &#125; else if (p.x &lt; center.x &amp;&amp; p.y &lt; center.y) &#123;</span><br><span class="line">            atomicAdd(&amp;smem[2], 1);  // 左下</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            atomicAdd(&amp;smem[3], 1);  // 右下</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    __syncthreads();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="shen-du-fen-xi" tabindex="-1" id="深度分析">深度分析</h3><p>假设初始有 64 个均匀分布的点：</p><table><thead><tr><th>深度</th><th>节点数</th><th>每节点点数</th></tr></thead><tbody><tr><td>0</td><td>1</td><td>64</td></tr><tr><td>1</td><td>4</td><td>16</td></tr><tr><td>2</td><td>16</td><td>4</td></tr><tr><td>3</td><td>64</td><td>1</td></tr></tbody></table><p>总共启动 1 + 4 + 16 = <strong>21</strong> 个子 kernel。</p><h2 id="xing-neng-kao-lu" tabindex="-1" id="性能考虑">性能考虑</h2><h3 id="qi-dong-kai-xiao" tabindex="-1" id="启动开销">启动开销</h3><p>子 kernel 启动有开销：</p><ul><li>资源分配</li><li>参数传递</li><li>调度延迟</li></ul><p><strong>建议</strong>：只在工作量足够大时使用动态并行。</p><h3 id="qian-tao-shen-du-xian-zhi" tabindex="-1" id="嵌套深度限制">嵌套深度限制</h3><p>CUDA 限制嵌套深度（通常 24 层）。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">// 检查支持的嵌套深度</span><br><span class="line">int max_depth;</span><br><span class="line">cudaDeviceGetAttribute(&amp;max_depth, </span><br><span class="line">    cudaDevAttrMaxDeviceRuntimeSynchronizationDepth, 0);</span><br></pre></td></tr></table></figure><h3 id="nei-cun-xiao-hao" tabindex="-1" id="内存消耗">内存消耗</h3><p>每层递归消耗栈空间。深度递归可能导致栈溢出。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">// 增大栈大小</span><br><span class="line">cudaDeviceSetLimit(cudaLimitStackSize, 8192);</span><br></pre></td></tr></table></figure><h3 id="he-shi-shi-yong-dong-tai-bing-xing" tabindex="-1" id="何时使用动态并行">何时使用动态并行</h3><p><strong>适合使用</strong>：</p><ul><li>递归算法（树遍历、分治）</li><li>自适应细分（网格细化、LOD）</li><li>不规则数据（稀疏图、不平衡树）</li></ul><p><strong>不适合使用</strong>：</p><ul><li>规则的数据并行（矩阵运算）</li><li>子 kernel 工作量很小</li><li>需要极致性能的场景</li></ul><h2 id="diao-shi-yu-zui-jia-shi-jian" tabindex="-1" id="调试与最佳实践">调试与最佳实践</h2><h3 id="cuo-wu-chu-li" tabindex="-1" id="错误处理">错误处理</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel() &#123;</span><br><span class="line">    cudaError_t err;</span><br><span class="line">    </span><br><span class="line">    child_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;();</span><br><span class="line">    err = cudaGetLastError();</span><br><span class="line">    if (err != cudaSuccess) &#123;</span><br><span class="line">        printf(&quot;Child kernel launch failed: %s\n&quot;, </span><br><span class="line">               cudaGetErrorString(err));</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    err = cudaDeviceSynchronize();</span><br><span class="line">    if (err != cudaSuccess) &#123;</span><br><span class="line">        printf(&quot;Child kernel execution failed: %s\n&quot;, </span><br><span class="line">               cudaGetErrorString(err));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="bi-mian-guo-du-qian-tao" tabindex="-1" id="避免过度嵌套">避免过度嵌套</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">__global__ void recursive_kernel(int depth, int max_depth) &#123;</span><br><span class="line">    if (depth &gt;= max_depth) &#123;</span><br><span class="line">        return;  // 停止递归</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    recursive_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;(depth + 1, max_depth);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="zi-yuan-guan-li" tabindex="-1" id="资源管理">资源管理</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">__global__ void parent_kernel() &#123;</span><br><span class="line">    float *temp;</span><br><span class="line">    cudaMalloc(&amp;temp, size);</span><br><span class="line">    </span><br><span class="line">    // 确保子 kernel 完成后再释放</span><br><span class="line">    child_kernel&lt;&lt;&lt;1, 32&gt;&gt;&gt;(temp);</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">    </span><br><span class="line">    cudaFree(temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="xiao-jie" tabindex="-1" id="小结">小结</h2><p>第二十一章介绍了 CUDA 动态并行性这一高级特性：</p><p><strong>核心概念</strong>：GPU kernel 可以直接启动子 kernel，无需返回 CPU。这使得递归算法和自适应计算可以完全在 GPU 上执行。</p><p><strong>语法要点</strong>：</p><ul><li>在 <code>__global__</code> 函数中使用 <code>&lt;&lt;&lt;...&gt;&gt;&gt;</code> 启动子 kernel</li><li><code>cudaDeviceSynchronize()</code> 等待子 kernel 完成</li><li>可以在设备端使用 <code>cudaMalloc</code>/<code>cudaFree</code></li></ul><p><strong>内存可见性</strong>：</p><ul><li>全局、常量、纹理内存父子共享</li><li>共享内存和局部内存不能传递给子 kernel</li></ul><p><strong>流与并发</strong>：</p><ul><li>同一 Block 的默认流共享，子 kernel 串行执行</li><li>使用非阻塞流实现子 kernel 并发</li></ul><p><strong>应用场景</strong>：</p><ul><li>Bezier 曲线自适应细分：根据曲率动态决定采样密度</li><li>四叉树构建：递归划分空间</li></ul><p><strong>性能考虑</strong>：</p><ul><li>启动开销不可忽视</li><li>深度递归消耗栈空间</li><li>配置启动池大小</li></ul><p>动态并行性让 GPU 编程更加灵活，但要权衡使用——对于规则的数据并行任务，传统方式可能更高效。对于天然递归或自适应的问题，动态并行性是强大的工具。</p><h2 id="xia-yi-bu" tabindex="-1" id="🚀-下一步">🚀 下一步</h2><ul><li>实现 Bezier 曲线的自适应细分，体验动态并行性的优势</li><li>构建一个四叉树或八叉树，学习递归算法的 GPU 实现</li><li>探索其他递归算法：快速排序、归并排序、分治算法</li><li>学习动态并行性的性能调优：启动池配置、嵌套深度控制</li><li>对比动态并行性与传统方法的性能差异，理解适用场景</li><li>研究自适应网格细化（AMR）等高级应用</li></ul><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="📚-参考资料">📚 参考资料</h2><ul><li>PMPP 第四版 Chapter 21</li><li>Hwu, W., Kirk, D., &amp; El Hajj, I. (2022). <em>Programming Massively Parallel Processors: A Hands-on Approach</em> (4th Edition). Morgan Kaufmann.</li><li>NVIDIA. <em>CUDA C++ Programming Guide - Dynamic Parallelism</em>. <a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">https://docs.nvidia.com/cuda/cuda-c-programming-guide/</a></li><li>NVIDIA. <em>CUDA Dynamic Parallelism</em>. <a href="https://developer.nvidia.com/blog/cuda-dynamic-parallelism-api-principles/">https://developer.nvidia.com/blog/cuda-dynamic-parallelism-api-principles/</a></li></ul><p><strong>学习愉快！</strong> 🎓</p><hr><blockquote><p><strong>本文 GitHub 仓库</strong>: <a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;qian-yan&quot; tabindex=&quot;-1&quot; id=&quot;前言&quot;&gt;前言&lt;/h2&gt;
&lt;p&gt;第二十章介绍了多 GPU 集群编程。第二十一章回到单 GPU 的高级特性——&lt;strong&gt;动态并行性（Dynamic Parallelism）&lt;/strong&gt;。传统</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/categories/knowledge-sharing/pmpp/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/tags/pmpp/"/>
    
    <category term="动态并行" scheme="https://smarter.xin/tags/dynamic-parallel/"/>
    
    <category term="递归算法" scheme="https://smarter.xin/tags/recursive-algorithm/"/>
    
    <category term="四叉树" scheme="https://smarter.xin/tags/quadtree/"/>
    
  </entry>
  
  <entry>
    <title>PMPP-第二十章：异构计算集群编程</title>
    <link href="https://smarter.xin/posts/9506dbb9/"/>
    <id>https://smarter.xin/posts/9506dbb9/</id>
    <published>2026-01-24T07:26:44.000Z</published>
    <updated>2026-03-16T05:46:43.228Z</updated>
    
    <content type="html"><![CDATA[<h2 id="qian-yan" tabindex="-1" id="前言">前言</h2><p>第十九章总结了并行编程的思维方法。第二十章将视野扩展到<strong>计算集群（Computing Cluster）</strong>——多台计算机通过高速网络连接，每台计算机可能配备多个 GPU。这是当今超级计算机和数据中心的典型架构。本章讨论如何使用 **MPI（Message Passing Interface，消息传递接口）**与 CUDA 结合，实现跨节点的异构并行计算。掌握这些技术，就能编写可扩展到数千个 GPU 的大规模并行程序。</p><blockquote><p><strong>📦 配套资源</strong>：本系列文章配有完整的 <a href="https://github.com/psmarter/PMPP-Learning">GitHub 仓库</a>，包含每章的练习题解答、CUDA 代码实现和详细注释。所有代码都经过测试，可以直接运行。</p></blockquote><h2 id="yi-gou-ji-suan-ji-qun-jia-gou" tabindex="-1" id="异构计算集群架构">异构计算集群架构</h2><h3 id="shi-yao-shi-yi-gou-ji-qun" tabindex="-1" id="什么是异构集群">什么是异构集群</h3><p><strong>异构集群</strong>：由多个计算节点组成，每个节点包含：</p><ul><li>CPU（主机）</li><li>一个或多个 GPU（加速器）</li><li>本地内存</li><li>网络接口</li></ul><p>节点之间通过高速网络（如 InfiniBand、NVLink）连接。</p><h3 id="dian-xing-jia-gou" tabindex="-1" id="典型架构">典型架构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────────┐</span><br><span class="line">│                    计算集群                              │</span><br><span class="line">├─────────────────┬─────────────────┬─────────────────────┤</span><br><span class="line">│    节点 0       │    节点 1       │    节点 N-1         │</span><br><span class="line">│  ┌─────────┐   │  ┌─────────┐   │  ┌─────────┐        │</span><br><span class="line">│  │  CPU    │   │  │  CPU    │   │  │  CPU    │        │</span><br><span class="line">│  └────┬────┘   │  └────┬────┘   │  └────┬────┘        │</span><br><span class="line">│       │        │       │        │       │             │</span><br><span class="line">│  ┌────┴────┐   │  ┌────┴────┐   │  ┌────┴────┐        │</span><br><span class="line">│  │GPU0│GPU1│   │  │GPU0│GPU1│   │  │GPU0│GPU1│        │</span><br><span class="line">│  └─────────┘   │  └─────────┘   │  └─────────┘        │</span><br><span class="line">└────────┬───────┴────────┬───────┴────────┬────────────┘</span><br><span class="line">         │                │                │</span><br><span class="line">         └────────────────┴────────────────┘</span><br><span class="line">                    高速网络</span><br></pre></td></tr></table></figure><h3 id="bian-cheng-tiao-zhan" tabindex="-1" id="编程挑战">编程挑战</h3><ol><li><strong>分布式内存</strong>：每个节点有独立的内存空间，不能直接访问</li><li><strong>数据通信</strong>：需要显式地在节点间传递数据</li><li><strong>同步协调</strong>：多个进程需要协调工作</li><li><strong>故障容错</strong>：单个节点故障不应导致整个计算崩溃</li></ol><h2 id="mpi-ji-chu" tabindex="-1" id="MPI-基础">MPI 基础</h2><h3 id="shi-yao-shi-mpi" tabindex="-1" id="什么是-MPI">什么是 MPI</h3><p><strong>MPI（Message Passing Interface）</strong>：一种标准化的消息传递编程模型。</p><ul><li>定义了进程间通信的 API</li><li>支持点对点通信和集合通信</li><li>与硬件无关，可移植性好</li></ul><p>常见实现：OpenMPI、MPICH、Intel MPI。</p><h3 id="ji-ben-gai-nian" tabindex="-1" id="基本概念">基本概念</h3><p><strong>进程（Process）</strong>：MPI 程序的基本执行单元。每个进程有：</p><ul><li>唯一的<strong>秩（Rank）</strong>：0 到 N-1</li><li>独立的地址空间</li><li>可以运行在不同的物理节点上</li></ul><p><strong>通信子（Communicator）</strong>：定义参与通信的进程组。</p><ul><li><code>MPI_COMM_WORLD</code>：包含所有进程的默认通信子</li></ul><h3 id="mpi-cheng-xu-gu-jia" tabindex="-1" id="MPI-程序骨架">MPI 程序骨架</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mpi.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="type">int</span> rank, size;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 初始化 MPI</span></span><br><span class="line">    MPI_Init(&amp;argc, &amp;argv);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 获取当前进程的秩</span></span><br><span class="line">    MPI_Comm_rank(MPI_COMM_WORLD, &amp;rank);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 获取进程总数</span></span><br><span class="line">    MPI_Comm_size(MPI_COMM_WORLD, &amp;size);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;进程 %d / %d\n&quot;</span>, rank, size);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 计算和通信...</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 结束 MPI</span></span><br><span class="line">    MPI_Finalize();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>运行方式</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mpirun -np 4 ./my_program   <span class="comment"># 启动 4 个进程</span></span><br></pre></td></tr></table></figure><h2 id="dian-dui-dian-tong-xin" tabindex="-1" id="点对点通信">点对点通信</h2><h3 id="ji-ben-fa-song-he-jie-shou" tabindex="-1" id="基本发送和接收">基本发送和接收</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 发送</span></span><br><span class="line">MPI_Send(</span><br><span class="line">    <span class="type">void</span> *buf,          <span class="comment">// 发送缓冲区</span></span><br><span class="line">    <span class="type">int</span> count,          <span class="comment">// 元素个数</span></span><br><span class="line">    MPI_Datatype dtype, <span class="comment">// 数据类型（MPI_FLOAT, MPI_INT 等）</span></span><br><span class="line">    <span class="type">int</span> dest,           <span class="comment">// 目标进程秩</span></span><br><span class="line">    <span class="type">int</span> tag,            <span class="comment">// 消息标签</span></span><br><span class="line">    MPI_Comm comm       <span class="comment">// 通信子</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收</span></span><br><span class="line">MPI_Recv(</span><br><span class="line">    <span class="type">void</span> *buf,          <span class="comment">// 接收缓冲区</span></span><br><span class="line">    <span class="type">int</span> count,          <span class="comment">// 最大元素个数</span></span><br><span class="line">    MPI_Datatype dtype, <span class="comment">// 数据类型</span></span><br><span class="line">    <span class="type">int</span> source,         <span class="comment">// 源进程秩</span></span><br><span class="line">    <span class="type">int</span> tag,            <span class="comment">// 消息标签</span></span><br><span class="line">    MPI_Comm comm,      <span class="comment">// 通信子</span></span><br><span class="line">    MPI_Status *status  <span class="comment">// 状态信息</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h3 id="zu-sai-yu-fei-zu-sai" tabindex="-1" id="阻塞与非阻塞">阻塞与非阻塞</h3><p><strong>阻塞通信</strong>：函数返回时，操作已完成或缓冲区可安全复用。</p><ul><li><code>MPI_Send</code>：可能阻塞直到接收方准备好（取决于实现）</li><li><code>MPI_Recv</code>：阻塞直到消息到达</li></ul><p><strong>非阻塞通信</strong>：函数立即返回，后续检查完成状态。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">MPI_Request request;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 非阻塞发送</span></span><br><span class="line">MPI_Isend(buf, count, dtype, dest, tag, comm, &amp;request);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 做其他事情...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待完成</span></span><br><span class="line">MPI_Wait(&amp;request, &amp;status);</span><br></pre></td></tr></table></figure><h3 id="send-receive-zu-he" tabindex="-1" id="Send-Receive-组合">Send-Receive 组合</h3><p>避免死锁的常用模式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">MPI_Sendrecv(</span><br><span class="line">    send_buf, send_count, send_type, dest, send_tag,</span><br><span class="line">    recv_buf, recv_count, recv_type, source, recv_tag,</span><br><span class="line">    comm, &amp;status</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>同时发送和接收，系统自动处理顺序。</p><h2 id="mpi-cuda-bian-cheng" tabindex="-1" id="MPI-CUDA-编程">MPI + CUDA 编程</h2><h3 id="ji-ben-ce-lue" tabindex="-1" id="基本策略">基本策略</h3><p>每个 MPI 进程管理一个或多个 GPU：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> rank;</span><br><span class="line">MPI_Comm_rank(MPI_COMM_WORLD, &amp;rank);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 每个进程选择不同的 GPU</span></span><br><span class="line"><span class="type">int</span> num_devices;</span><br><span class="line">cudaGetDeviceCount(&amp;num_devices);</span><br><span class="line">cudaSetDevice(rank % num_devices);</span><br></pre></td></tr></table></figure><h3 id="shu-ju-liu-mo-shi" tabindex="-1" id="数据流模式">数据流模式</h3><p>典型的 MPI + CUDA 计算流程：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. MPI 进程接收输入数据（主机内存）</span><br><span class="line">2. 复制数据到 GPU（cudaMemcpy H2D）</span><br><span class="line">3. GPU 计算（kernel）</span><br><span class="line">4. 复制结果到主机（cudaMemcpy D2H）</span><br><span class="line">5. MPI 进程发送结果</span><br></pre></td></tr></table></figure><h3 id="shi-li-fen-bu-shi-xiang-liang-jia-fa" tabindex="-1" id="示例：分布式向量加法">示例：分布式向量加法</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">distributed_vector_add</span><span class="params">(<span class="type">float</span> *a, <span class="type">float</span> *b, <span class="type">float</span> *c, <span class="type">int</span> n, <span class="type">int</span> rank, <span class="type">int</span> size)</span> &#123;</span><br><span class="line">    <span class="type">int</span> local_n = n / size;</span><br><span class="line">    <span class="type">int</span> start = rank * local_n;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 分配 GPU 内存</span></span><br><span class="line">    <span class="type">float</span> *d_a, *d_b, *d_c;</span><br><span class="line">    cudaMalloc(&amp;d_a, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    cudaMalloc(&amp;d_b, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    cudaMalloc(&amp;d_c, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 复制本地数据到 GPU</span></span><br><span class="line">    cudaMemcpy(d_a, a + start, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>), cudaMemcpyHostToDevice);</span><br><span class="line">    cudaMemcpy(d_b, b + start, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>), cudaMemcpyHostToDevice);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// GPU 计算</span></span><br><span class="line">    <span class="type">int</span> block_size = <span class="number">256</span>;</span><br><span class="line">    <span class="type">int</span> grid_size = (local_n + block_size - <span class="number">1</span>) / block_size;</span><br><span class="line">    vector_add&lt;&lt;&lt;grid_size, block_size&gt;&gt;&gt;(d_a, d_b, d_c, local_n);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 复制结果回主机</span></span><br><span class="line">    cudaMemcpy(c + start, d_c, local_n * <span class="keyword">sizeof</span>(<span class="type">float</span>), cudaMemcpyDeviceToHost);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 收集所有结果到进程 0</span></span><br><span class="line">    MPI_Gather(c + start, local_n, MPI_FLOAT,</span><br><span class="line">               c, local_n, MPI_FLOAT,</span><br><span class="line">               <span class="number">0</span>, MPI_COMM_WORLD);</span><br><span class="line">    </span><br><span class="line">    cudaFree(d_a);</span><br><span class="line">    cudaFree(d_b);</span><br><span class="line">    cudaFree(d_c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="halo-jiao-huan-yu-bian-jie-tong-xin" tabindex="-1" id="Halo-交换与边界通信">Halo 交换与边界通信</h2><h3 id="shi-yao-shi-halo" tabindex="-1" id="什么是-Halo">什么是 Halo</h3><p>在模板计算（stencil）、有限差分等应用中，每个点的计算依赖于邻近点。</p><p>当数据分布在多个进程时，边界点的计算需要<strong>相邻进程的数据</strong>。</p><p><strong>Halo（光晕/幽灵区域）</strong>：存储来自邻居进程的边界数据。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">进程 0 的数据        进程 1 的数据</span><br><span class="line">┌──────────────┐    ┌──────────────┐</span><br><span class="line">│              │    │              │</span><br><span class="line">│   内部区域   │    │   内部区域   │</span><br><span class="line">│              │    │              │</span><br><span class="line">├──────────────┤    ├──────────────┤</span><br><span class="line">│  右边界 →→→  │ ↔  │ ←←← 左 Halo │</span><br><span class="line">│ (发送给 P1)  │    │ (来自 P0)   │</span><br><span class="line">└──────────────┘    └──────────────┘</span><br></pre></td></tr></table></figure><h3 id="3-d-mo-ban-ji-suan-shi-li" tabindex="-1" id="3D-模板计算示例">3D 模板计算示例</h3><p>以 25 点模板为例（每个方向延伸 4 个点）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 计算每个进程需要多少 Halo 点</span></span><br><span class="line"><span class="type">int</span> halo_size = <span class="number">4</span>;  <span class="comment">// 每侧 4 层</span></span><br><span class="line"><span class="type">int</span> num_halo_points = dimx * dimy * halo_size;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分配包含 Halo 的数据</span></span><br><span class="line"><span class="type">int</span> total_z = local_dimz + <span class="number">2</span> * halo_size;</span><br><span class="line"><span class="type">float</span> *data = <span class="built_in">malloc</span>(dimx * dimy * total_z * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br></pre></td></tr></table></figure><h3 id="halo-jiao-huan-shi-xian" tabindex="-1" id="Halo-交换实现">Halo 交换实现</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">exchange_halos</span><span class="params">(<span class="type">float</span> *data, <span class="type">int</span> dimx, <span class="type">int</span> dimy, <span class="type">int</span> dimz,</span></span><br><span class="line"><span class="params">                     <span class="type">int</span> left_neighbor, <span class="type">int</span> right_neighbor)</span> &#123;</span><br><span class="line">    <span class="type">int</span> halo_size = <span class="number">4</span>;</span><br><span class="line">    <span class="type">int</span> num_halo_points = dimx * dimy * halo_size;</span><br><span class="line">    </span><br><span class="line">    <span class="type">float</span> *left_send = data + num_halo_points;  <span class="comment">// 左边界数据</span></span><br><span class="line">    <span class="type">float</span> *right_send = data + dimx * dimy * (dimz - halo_size);  <span class="comment">// 右边界数据</span></span><br><span class="line">    <span class="type">float</span> *left_recv = data;  <span class="comment">// 左 Halo 接收区</span></span><br><span class="line">    <span class="type">float</span> *right_recv = data + dimx * dimy * (dimz + halo_size);  <span class="comment">// 右 Halo 接收区</span></span><br><span class="line">    </span><br><span class="line">    MPI_Status status;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 发送到左邻居，从右邻居接收</span></span><br><span class="line">    MPI_Sendrecv(left_send, num_halo_points, MPI_FLOAT, left_neighbor, <span class="number">0</span>,</span><br><span class="line">                 right_recv, num_halo_points, MPI_FLOAT, right_neighbor, <span class="number">0</span>,</span><br><span class="line">                 MPI_COMM_WORLD, &amp;status);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 发送到右邻居，从左邻居接收</span></span><br><span class="line">    MPI_Sendrecv(right_send, num_halo_points, MPI_FLOAT, right_neighbor, <span class="number">1</span>,</span><br><span class="line">                 left_recv, num_halo_points, MPI_FLOAT, left_neighbor, <span class="number">1</span>,</span><br><span class="line">                 MPI_COMM_WORLD, &amp;status);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="ji-suan-yu-tong-xin-zhong-die" tabindex="-1" id="计算与通信重叠">计算与通信重叠</h2><h3 id="wen-ti" tabindex="-1" id="问题">问题</h3><p>通信需要时间，如果先计算完再通信，GPU 会空闲等待。</p><h3 id="jie-jue-fang-an" tabindex="-1" id="解决方案">解决方案</h3><p><strong>思路</strong>：重叠计算与通信。</p><ol><li>先计算边界区域（通信需要的数据）</li><li>边界计算完成后，开始通信</li><li>同时计算内部区域</li><li>通信完成后，所有计算都完成了</li></ol><h3 id="cuda-liu-shi-xian" tabindex="-1" id="CUDA-流实现">CUDA 流实现</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line">cudaStream_t stream_boundary, stream_internal;</span><br><span class="line">cudaStreamCreate(&amp;stream_boundary);</span><br><span class="line">cudaStreamCreate(&amp;stream_internal);</span><br><span class="line"></span><br><span class="line">// 阶段 1：计算边界（在 stream_boundary 中）</span><br><span class="line">stencil_kernel&lt;&lt;&lt;grid_boundary, block, 0, stream_boundary&gt;&gt;&gt;(</span><br><span class="line">    d_output + left_offset, d_input + left_offset, dimx, dimy, 12);</span><br><span class="line">stencil_kernel&lt;&lt;&lt;grid_boundary, block, 0, stream_boundary&gt;&gt;&gt;(</span><br><span class="line">    d_output + right_offset, d_input + right_offset, dimx, dimy, 12);</span><br><span class="line"></span><br><span class="line">// 阶段 2：同时进行——</span><br><span class="line">// 2a: 计算内部区域（在 stream_internal 中）</span><br><span class="line">stencil_kernel&lt;&lt;&lt;grid_internal, block, 0, stream_internal&gt;&gt;&gt;(</span><br><span class="line">    d_output + internal_offset, d_input + internal_offset, dimx, dimy, dimz - 8);</span><br><span class="line"></span><br><span class="line">// 2b: 复制边界到主机，准备发送</span><br><span class="line">cudaMemcpyAsync(h_left_boundary, d_output + boundary_left,</span><br><span class="line">                num_halo_bytes, cudaMemcpyDeviceToHost, stream_boundary);</span><br><span class="line">cudaMemcpyAsync(h_right_boundary, d_output + boundary_right,</span><br><span class="line">                num_halo_bytes, cudaMemcpyDeviceToHost, stream_boundary);</span><br><span class="line"></span><br><span class="line">// 等待边界复制完成</span><br><span class="line">cudaStreamSynchronize(stream_boundary);</span><br><span class="line"></span><br><span class="line">// 阶段 3：MPI 通信</span><br><span class="line">MPI_Sendrecv(h_left_boundary, num_halo_points, MPI_FLOAT, left_neighbor, 0,</span><br><span class="line">             h_right_halo, num_halo_points, MPI_FLOAT, right_neighbor, 0,</span><br><span class="line">             MPI_COMM_WORLD, &amp;status);</span><br><span class="line">MPI_Sendrecv(h_right_boundary, num_halo_points, MPI_FLOAT, right_neighbor, 1,</span><br><span class="line">             h_left_halo, num_halo_points, MPI_FLOAT, left_neighbor, 1,</span><br><span class="line">             MPI_COMM_WORLD, &amp;status);</span><br><span class="line"></span><br><span class="line">// 复制 Halo 回 GPU</span><br><span class="line">cudaMemcpyAsync(d_output + left_halo_offset, h_left_halo,</span><br><span class="line">                num_halo_bytes, cudaMemcpyHostToDevice, stream_boundary);</span><br><span class="line">cudaMemcpyAsync(d_output + right_halo_offset, h_right_halo,</span><br><span class="line">                num_halo_bytes, cudaMemcpyHostToDevice, stream_boundary);</span><br><span class="line"></span><br><span class="line">// 等待所有操作完成</span><br><span class="line">cudaDeviceSynchronize();</span><br></pre></td></tr></table></figure><h3 id="shi-jian-xian-fen-xi" tabindex="-1" id="时间线分析">时间线分析</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">                    时间 →</span><br><span class="line">stream_boundary:  [边界计算][D2H][       ][H2D]</span><br><span class="line">stream_internal:  [           内部计算          ]</span><br><span class="line">MPI 通信:         [      ][Sendrecv][      ]</span><br><span class="line">                           ↑</span><br><span class="line">                     重叠区域：内部计算</span><br><span class="line">                     与 MPI 通信同时进行</span><br></pre></td></tr></table></figure><h2 id="cuda-aware-mpi" tabindex="-1" id="CUDA-Aware-MPI">CUDA-Aware MPI</h2><h3 id="chuan-tong-fang-shi-de-wen-ti" tabindex="-1" id="传统方式的问题">传统方式的问题</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 传统方式：必须经过主机内存</span></span><br><span class="line">cudaMemcpy(h_buf, d_buf, size, cudaMemcpyDeviceToHost);  <span class="comment">// GPU → CPU</span></span><br><span class="line">MPI_Send(h_buf, count, MPI_FLOAT, dest, tag, comm);       <span class="comment">// CPU → 网络</span></span><br><span class="line"><span class="comment">// 接收端</span></span><br><span class="line">MPI_Recv(h_buf, count, MPI_FLOAT, src, tag, comm, &amp;status);  <span class="comment">// 网络 → CPU  </span></span><br><span class="line">cudaMemcpy(d_buf, h_buf, size, cudaMemcpyHostToDevice);      <span class="comment">// CPU → GPU</span></span><br></pre></td></tr></table></figure><p><strong>问题</strong>：额外的内存拷贝开销。</p><h3 id="cuda-aware-mpi-1" tabindex="-1" id="CUDA-Aware-MPI-2">CUDA-Aware MPI</h3><p><strong>CUDA-Aware MPI</strong>：MPI 实现能直接识别 GPU 指针。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 直接传递 GPU 指针——不需要手动拷贝</span></span><br><span class="line">MPI_Send(d_buf, count, MPI_FLOAT, dest, tag, comm);</span><br><span class="line">MPI_Recv(d_buf, count, MPI_FLOAT, src, tag, comm, &amp;status);</span><br></pre></td></tr></table></figure><p>MPI 库自动处理：</p><ul><li>通过 GPUDirect RDMA 直接 GPU 到 GPU 传输</li><li>如果不支持，自动回退到经过主机的方式</li></ul><h3 id="huan-jing-pei-zhi" tabindex="-1" id="环境配置">环境配置</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 编译时链接 CUDA-Aware MPI</span></span><br><span class="line">mpicc -o my_prog my_prog.c -I<span class="variable">$&#123;CUDA_HOME&#125;</span>/include -L<span class="variable">$&#123;CUDA_HOME&#125;</span>/lib64 -lcudart</span><br><span class="line"></span><br><span class="line"><span class="comment"># 运行前设置</span></span><br><span class="line"><span class="built_in">export</span> UCX_RNDV_SCHEME=cuda</span><br><span class="line"><span class="built_in">export</span> UCX_TLS=rc,cuda_copy,cuda_ipc</span><br></pre></td></tr></table></figure><h3 id="shi-yong-cuda-aware-mpi-gai-xie" tabindex="-1" id="使用-CUDA-Aware-MPI-改写">使用 CUDA-Aware MPI 改写</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 无需 host buffer，直接使用 device buffer</span></span><br><span class="line">MPI_Sendrecv(d_output + boundary_left, num_halo_points, MPI_FLOAT, left_neighbor, <span class="number">0</span>,</span><br><span class="line">             d_output + right_halo_offset, num_halo_points, MPI_FLOAT, right_neighbor, <span class="number">0</span>,</span><br><span class="line">             MPI_COMM_WORLD, &amp;status);</span><br></pre></td></tr></table></figure><p><strong>优势</strong>：</p><ul><li>减少内存拷贝</li><li>更低延迟</li><li>代码更简洁</li></ul><h2 id="shu-ju-fu-wu-qi-mo-shi" tabindex="-1" id="数据服务器模式">数据服务器模式</h2><h3 id="wen-ti-1" tabindex="-1" id="问题-2">问题</h3><p>大规模集群中，I/O 可能成为瓶颈。每个计算节点都从存储读数据会导致争用。</p><h3 id="jie-jue-fang-an-1" tabindex="-1" id="解决方案-2">解决方案</h3><p><strong>数据服务器模式</strong>：一个进程专门负责 I/O，其他进程专门计算。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────┐</span><br><span class="line">│  数据服务器 (Rank N-1)                               │</span><br><span class="line">│  - 读取输入数据                                      │</span><br><span class="line">│  - 分发数据给计算节点                                │</span><br><span class="line">│  - 收集计算结果                                      │</span><br><span class="line">│  - 写入输出                                          │</span><br><span class="line">└──────────────────────────────────────────────────────┘</span><br><span class="line">         ↓ 分发           ↑ 收集</span><br><span class="line">┌─────────┬─────────┬─────────┬─────────┐</span><br><span class="line">│ Rank 0  │ Rank 1  │ Rank 2  │   ...   │</span><br><span class="line">│ 计算    │ 计算    │ 计算    │         │</span><br><span class="line">└─────────┴─────────┴─────────┴─────────┘</span><br></pre></td></tr></table></figure><h3 id="shi-xian" tabindex="-1" id="实现">实现</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">data_server</span><span class="params">(<span class="type">int</span> dimx, <span class="type">int</span> dimy, <span class="type">int</span> dimz, <span class="type">int</span> nreps)</span> &#123;</span><br><span class="line">    <span class="type">int</span> np, num_comp_nodes;</span><br><span class="line">    MPI_Comm_size(MPI_COMM_WORLD, &amp;np);</span><br><span class="line">    num_comp_nodes = np - <span class="number">1</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 分配并初始化数据</span></span><br><span class="line">    <span class="type">float</span> *input = <span class="built_in">malloc</span>(dimx * dimy * dimz * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    <span class="type">float</span> *output = <span class="built_in">malloc</span>(dimx * dimy * dimz * <span class="keyword">sizeof</span>(<span class="type">float</span>));</span><br><span class="line">    initialize_data(input, dimx, dimy, dimz);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 计算每个节点的数据量</span></span><br><span class="line">    <span class="type">int</span> slice_per_node = dimz / num_comp_nodes;</span><br><span class="line">    <span class="type">int</span> halo_size = <span class="number">4</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 分发数据给计算节点</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> p = <span class="number">0</span>; p &lt; num_comp_nodes; p++) &#123;</span><br><span class="line">        <span class="type">int</span> start_z = p * slice_per_node - (p &gt; <span class="number">0</span> ? halo_size : <span class="number">0</span>);</span><br><span class="line">        <span class="type">int</span> num_slices = slice_per_node + (p &gt; <span class="number">0</span> ? halo_size : <span class="number">0</span>) </span><br><span class="line">                                        + (p &lt; num_comp_nodes - <span class="number">1</span> ? halo_size : <span class="number">0</span>);</span><br><span class="line">        <span class="type">int</span> num_points = dimx * dimy * num_slices;</span><br><span class="line">        </span><br><span class="line">        MPI_Send(input + start_z * dimx * dimy, num_points, MPI_FLOAT,</span><br><span class="line">                 p, <span class="number">0</span>, MPI_COMM_WORLD);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 等待计算完成</span></span><br><span class="line">    MPI_Barrier(MPI_COMM_WORLD);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 收集结果</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> p = <span class="number">0</span>; p &lt; num_comp_nodes; p++) &#123;</span><br><span class="line">        <span class="type">int</span> offset = p * slice_per_node * dimx * dimy;</span><br><span class="line">        <span class="type">int</span> num_points = slice_per_node * dimx * dimy;</span><br><span class="line">        </span><br><span class="line">        MPI_Recv(output + offset, num_points, MPI_FLOAT,</span><br><span class="line">                 p, DATA_COLLECT, MPI_COMM_WORLD, MPI_STATUS_IGNORE);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 保存结果</span></span><br><span class="line">    save_output(output, dimx, dimy, dimz);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">free</span>(input);</span><br><span class="line">    <span class="built_in">free</span>(output);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="wan-zheng-shi-li-mpi-cuda-mo-ban-ji-suan" tabindex="-1" id="完整示例：MPI-CUDA-模板计算">完整示例：MPI + CUDA 模板计算</h2><h3 id="cheng-xu-jie-gou" tabindex="-1" id="程序结构">程序结构</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="type">int</span> pid, np;</span><br><span class="line">    </span><br><span class="line">    MPI_Init(&amp;argc, &amp;argv);</span><br><span class="line">    MPI_Comm_rank(MPI_COMM_WORLD, &amp;pid);</span><br><span class="line">    MPI_Comm_size(MPI_COMM_WORLD, &amp;np);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (pid &lt; np - <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">// 计算节点：设置 GPU 并计算</span></span><br><span class="line">        <span class="type">int</span> device = pid % num_devices;</span><br><span class="line">        cudaSetDevice(device);</span><br><span class="line">        compute_node_stencil(dimx, dimy, dimz / (np - <span class="number">1</span>), nreps);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 数据服务器：I/O 和数据分发</span></span><br><span class="line">        data_server(dimx, dimy, dimz, nreps);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    MPI_Finalize();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="ji-suan-jie-dian-shi-xian" tabindex="-1" id="计算节点实现">计算节点实现</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">compute_node_stencil</span><span class="params">(<span class="type">int</span> dimx, <span class="type">int</span> dimy, <span class="type">int</span> dimz, <span class="type">int</span> nreps)</span> &#123;</span><br><span class="line">    <span class="type">int</span> pid, np;</span><br><span class="line">    MPI_Comm_rank(MPI_COMM_WORLD, &amp;pid);</span><br><span class="line">    MPI_Comm_size(MPI_COMM_WORLD, &amp;np);</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> server = np - <span class="number">1</span>;</span><br><span class="line">    <span class="type">int</span> left_neighbor = (pid &gt; <span class="number">0</span>) ? (pid - <span class="number">1</span>) : MPI_PROC_NULL;</span><br><span class="line">    <span class="type">int</span> right_neighbor = (pid &lt; np - <span class="number">2</span>) ? (pid + <span class="number">1</span>) : MPI_PROC_NULL;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 分配内存</span></span><br><span class="line">    <span class="type">int</span> halo = <span class="number">4</span>;</span><br><span class="line">    <span class="type">int</span> total_z = dimz + <span class="number">2</span> * halo;</span><br><span class="line">    <span class="type">size_t</span> num_bytes = dimx * dimy * total_z * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="type">float</span> *h_input = <span class="built_in">malloc</span>(num_bytes);</span><br><span class="line">    <span class="type">float</span> *h_output = <span class="built_in">malloc</span>(num_bytes);</span><br><span class="line">    <span class="type">float</span> *d_input, *d_output;</span><br><span class="line">    cudaMalloc(&amp;d_input, num_bytes);</span><br><span class="line">    cudaMalloc(&amp;d_output, num_bytes);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Halo 缓冲区（固定内存，加速传输）</span></span><br><span class="line">    <span class="type">float</span> *h_left_boundary, *h_right_boundary;</span><br><span class="line">    <span class="type">float</span> *h_left_halo, *h_right_halo;</span><br><span class="line">    <span class="type">size_t</span> halo_bytes = dimx * dimy * halo * <span class="keyword">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">    cudaHostAlloc(&amp;h_left_boundary, halo_bytes, cudaHostAllocDefault);</span><br><span class="line">    cudaHostAlloc(&amp;h_right_boundary, halo_bytes, cudaHostAllocDefault);</span><br><span class="line">    cudaHostAlloc(&amp;h_left_halo, halo_bytes, cudaHostAllocDefault);</span><br><span class="line">    cudaHostAlloc(&amp;h_right_halo, halo_bytes, cudaHostAllocDefault);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 创建 CUDA 流</span></span><br><span class="line">    cudaStream_t stream_boundary, stream_internal;</span><br><span class="line">    cudaStreamCreate(&amp;stream_boundary);</span><br><span class="line">    cudaStreamCreate(&amp;stream_internal);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 从数据服务器接收初始数据</span></span><br><span class="line">    MPI_Status status;</span><br><span class="line">    MPI_Recv(h_input, dimx * dimy * total_z, MPI_FLOAT,</span><br><span class="line">             server, MPI_ANY_TAG, MPI_COMM_WORLD, &amp;status);</span><br><span class="line">    cudaMemcpy(d_input, h_input, num_bytes, cudaMemcpyHostToDevice);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 迭代计算</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> iter = <span class="number">0</span>; iter &lt; nreps; iter++) &#123;</span><br><span class="line">        <span class="comment">// 阶段 1：边界计算</span></span><br><span class="line">        launch_boundary_kernel(d_output, d_input, dimx, dimy, stream_boundary);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 阶段 2：内部计算（与通信重叠）</span></span><br><span class="line">        launch_internal_kernel(d_output, d_input, dimx, dimy, dimz, stream_internal);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 复制边界到主机</span></span><br><span class="line">        copy_boundary_to_host(d_output, h_left_boundary, h_right_boundary,</span><br><span class="line">                               dimx, dimy, halo, stream_boundary);</span><br><span class="line">        cudaStreamSynchronize(stream_boundary);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// Halo 交换</span></span><br><span class="line">        MPI_Sendrecv(h_left_boundary, dimx * dimy * halo, MPI_FLOAT, left_neighbor, iter,</span><br><span class="line">                     h_right_halo, dimx * dimy * halo, MPI_FLOAT, right_neighbor, iter,</span><br><span class="line">                     MPI_COMM_WORLD, &amp;status);</span><br><span class="line">        MPI_Sendrecv(h_right_boundary, dimx * dimy * halo, MPI_FLOAT, right_neighbor, iter,</span><br><span class="line">                     h_left_halo, dimx * dimy * halo, MPI_FLOAT, left_neighbor, iter,</span><br><span class="line">                     MPI_COMM_WORLD, &amp;status);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 复制 Halo 回 GPU</span></span><br><span class="line">        copy_halo_to_device(d_output, h_left_halo, h_right_halo,</span><br><span class="line">                             dimx, dimy, dimz, halo, stream_boundary);</span><br><span class="line">        </span><br><span class="line">        cudaDeviceSynchronize();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 交换输入输出指针</span></span><br><span class="line">        <span class="type">float</span> *temp = d_output;</span><br><span class="line">        d_output = d_input;</span><br><span class="line">        d_input = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 发送结果给数据服务器</span></span><br><span class="line">    cudaMemcpy(h_output, d_input, num_bytes, cudaMemcpyDeviceToHost);</span><br><span class="line">    MPI_Send(h_output + dimx * dimy * halo, dimx * dimy * dimz, MPI_FLOAT,</span><br><span class="line">             server, DATA_COLLECT, MPI_COMM_WORLD);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 清理</span></span><br><span class="line">    <span class="built_in">free</span>(h_input);</span><br><span class="line">    <span class="built_in">free</span>(h_output);</span><br><span class="line">    cudaFreeHost(h_left_boundary);</span><br><span class="line">    cudaFreeHost(h_right_boundary);</span><br><span class="line">    cudaFreeHost(h_left_halo);</span><br><span class="line">    cudaFreeHost(h_right_halo);</span><br><span class="line">    cudaFree(d_input);</span><br><span class="line">    cudaFree(d_output);</span><br><span class="line">    cudaStreamDestroy(stream_boundary);</span><br><span class="line">    cudaStreamDestroy(stream_internal);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="xing-neng-you-hua" tabindex="-1" id="性能优化">性能优化</h2><h3 id="tong-xin-you-hua" tabindex="-1" id="通信优化">通信优化</h3><table><thead><tr><th>技术</th><th>描述</th><th>效果</th></tr></thead><tbody><tr><td>非阻塞通信</td><td>使用 <code>MPI_Isend</code>/<code>MPI_Irecv</code></td><td>重叠通信与计算</td></tr><tr><td>集合通信</td><td>使用 <code>MPI_Allreduce</code> 而非循环 P2P</td><td>利用优化的算法</td></tr><tr><td>CUDA-Aware MPI</td><td>直接传递 GPU 指针</td><td>减少内存拷贝</td></tr><tr><td>固定内存</td><td><code>cudaHostAlloc</code></td><td>加速 H2D/D2H 传输</td></tr></tbody></table><h3 id="fu-zai-jun-heng" tabindex="-1" id="负载均衡">负载均衡</h3><p>确保每个节点的工作量大致相等：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 处理不能整除的情况</span></span><br><span class="line"><span class="type">int</span> base_slices = dimz / num_nodes;</span><br><span class="line"><span class="type">int</span> remainder = dimz % num_nodes;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> p = <span class="number">0</span>; p &lt; num_nodes; p++) &#123;</span><br><span class="line">    <span class="type">int</span> slices = base_slices + (p &lt; remainder ? <span class="number">1</span> : <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 分配 slices 给节点 p</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="ke-kuo-zhan-xing-fen-xi" tabindex="-1" id="可扩展性分析">可扩展性分析</h3><p>对于 25 点模板计算：</p><table><thead><tr><th>节点数</th><th>通信量（每节点）</th><th>计算量（每节点）</th><th>计算/通信比</th></tr></thead><tbody><tr><td>16</td><td>2×64×64×4 = 32K</td><td>64×64×128 = 512K</td><td>16:1</td></tr><tr><td>64</td><td>2×64×64×4 = 32K</td><td>64×64×32 = 128K</td><td>4:1</td></tr><tr><td>256</td><td>2×64×64×4 = 32K</td><td>64×64×8 = 32K</td><td>1:1</td></tr></tbody></table><p><strong>观察</strong>：节点越多，通信开销占比越高。这是<strong>强扩展</strong>的典型特征。</p><h2 id="chang-jian-wen-ti-yu-jie-jue" tabindex="-1" id="常见问题与解决">常见问题与解决</h2><h3 id="si-suo" tabindex="-1" id="死锁">死锁</h3><p><strong>原因</strong>：所有进程都在等待接收，没有进程发送。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 错误示例——会死锁！</span></span><br><span class="line"><span class="keyword">if</span> (rank == <span class="number">0</span>) &#123;</span><br><span class="line">    MPI_Recv(..., <span class="number">1</span>, ...);  <span class="comment">// 等待 rank 1</span></span><br><span class="line">    MPI_Send(..., <span class="number">1</span>, ...);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    MPI_Recv(..., <span class="number">0</span>, ...);  <span class="comment">// 等待 rank 0</span></span><br><span class="line">    MPI_Send(..., <span class="number">0</span>, ...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>解决</strong>：使用 <code>MPI_Sendrecv</code> 或非阻塞通信。</p><h3 id="gpu-nei-cun-bu-zu" tabindex="-1" id="GPU-内存不足">GPU 内存不足</h3><p><strong>原因</strong>：每个节点分配的数据太多。</p><p><strong>解决</strong>：</p><ul><li>增加节点数</li><li>使用统一内存自动管理</li><li>分批处理</li></ul><h3 id="xing-neng-bu-jia" tabindex="-1" id="性能不佳">性能不佳</h3><p><strong>诊断</strong>：使用 Nsight Systems 分析 MPI + CUDA 程序。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nsys profile --trace=cuda,mpi mpirun -np 4 ./my_program</span><br></pre></td></tr></table></figure><p>查看是否有：</p><ul><li>过长的 MPI 等待时间</li><li>未重叠的计算和通信</li><li>GPU 空闲时间</li></ul><h2 id="xiao-jie" tabindex="-1" id="小结">小结</h2><p>第二十章扩展了并行编程的视野，从单 GPU 扩展到多节点集群：</p><p><strong>异构集群架构</strong>：每个节点包含 CPU 和 GPU，节点间通过网络连接。分布式内存模型要求显式通信。</p><p><strong>MPI 基础</strong>：消息传递编程模型。进程通过发送/接收消息通信。点对点通信（Send/Recv）和集合通信（Broadcast/Reduce）。</p><p><strong>MPI + CUDA</strong>：每个 MPI 进程管理一个或多个 GPU。数据在主机内存和 GPU 内存之间传输，在进程间通过 MPI 传输。</p><p><strong>Halo 交换</strong>：模板计算中，边界数据需要与邻居进程交换。使用 Sendrecv 避免死锁。</p><p><strong>计算与通信重叠</strong>：利用 CUDA 流，边界计算完成后立即开始通信，同时进行内部计算。显著减少总执行时间。</p><p><strong>CUDA-Aware MPI</strong>：MPI 库直接接受 GPU 指针，利用 GPUDirect 技术减少内存拷贝。</p><p><strong>数据服务器模式</strong>：一个进程专门负责 I/O，减少存储争用。</p><p>掌握 MPI + CUDA 编程，你就能编写可扩展到数千 GPU 的应用程序——这是当今 AI 训练、科学计算、天气预报等领域的核心技术。</p><h2 id="xia-yi-bu" tabindex="-1" id="🚀-下一步">🚀 下一步</h2><ul><li>搭建一个简单的多节点 GPU 集群环境，配置 MPI 和 CUDA-Aware MPI</li><li>实现一个分布式矩阵乘法，学习数据分割和结果收集</li><li>掌握 Halo 交换模式，实现分布式模板计算（如热传导方程）</li><li>学习集合通信操作：Allreduce、Allgather、Alltoall</li><li>探索性能分析工具：Nsight Systems 分析 MPI + CUDA 程序的性能瓶颈</li><li>了解现代 HPC 框架：NCCL（多 GPU 通信）、Horovod（分布式深度学习）</li></ul><hr><h2 id="can-kao-zi-liao" tabindex="-1" id="📚-参考资料">📚 参考资料</h2><ul><li>PMPP 第四版 Chapter 20</li><li>Hwu, W., Kirk, D., &amp; El Hajj, I. (2022). <em>Programming Massively Parallel Processors: A Hands-on Approach</em> (4th Edition). Morgan Kaufmann.</li><li>MPI Forum. <em>MPI: A Message-Passing Interface Standard</em>. <a href="https://www.mpi-forum.org/">https://www.mpi-forum.org/</a></li><li>NVIDIA. <em>CUDA-Aware MPI</em>. <a href="https://developer.nvidia.com/blog/introduction-cuda-aware-mpi/">https://developer.nvidia.com/blog/introduction-cuda-aware-mpi/</a></li></ul><p><strong>学习愉快！</strong> 🎓</p><hr><blockquote><p><strong>本文 GitHub 仓库</strong>: <a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;qian-yan&quot; tabindex=&quot;-1&quot; id=&quot;前言&quot;&gt;前言&lt;/h2&gt;
&lt;p&gt;第十九章总结了并行编程的思维方法。第二十章将视野扩展到&lt;strong&gt;计算集群（Computing</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/categories/knowledge-sharing/pmpp/"/>
    
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="CUDA流" scheme="https://smarter.xin/tags/cuda-stream/"/>
    
    <category term="MPI" scheme="https://smarter.xin/tags/mpi/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/tags/pmpp/"/>
    
    <category term="集群计算" scheme="https://smarter.xin/tags/cluster-computing/"/>
    
    <category term="分布式计算" scheme="https://smarter.xin/tags/distributed-computing/"/>
    
  </entry>
  
  <entry>
    <title>PMPP-第十九章：并行编程与计算思维</title>
    <link href="https://smarter.xin/posts/46b3d994/"/>
    <id>https://smarter.xin/posts/46b3d994/</id>
    <published>2026-01-24T04:29:59.000Z</published>
    <updated>2026-03-16T05:46:43.229Z</updated>
    
    <content type="html"><![CDATA[<h2 id="qian-yan" tabindex="-1" id="前言">前言</h2><p>经过前十八章的学习，我们掌握了 CUDA 编程的各种技术细节。第十九章是全书的总结与升华，从&quot;术&quot;上升到&quot;道&quot;——<strong>计算思维（Computational Thinking）</strong>。本章不再讲解具体的 API 或优化技巧，而是讨论<strong>如何思考并行问题</strong>、<strong>如何设计并行算法</strong>、<strong>如何权衡各种因素</strong>。这些思维方式将帮助读者应对未来的各种并行计算挑战。</p><blockquote><p><strong>📦 配套资源</strong>：本系列文章配有完整的 <a href="https://github.com/psmarter/PMPP-Learning">GitHub 仓库</a>，包含每章的练习题解答、CUDA 代码实现和详细注释。所有代码都经过测试，可以直接运行。</p></blockquote><h2 id="shi-yao-shi-ji-suan-si-wei" tabindex="-1" id="什么是计算思维">什么是计算思维</h2><h3 id="ding-yi" tabindex="-1" id="定义">定义</h3><p><strong>计算思维</strong>：用计算机科学的基本概念来解决问题、设计系统、理解人类行为。</p><p>——Jeannette Wing, 2006</p><p>在并行计算语境下，计算思维包括：</p><ul><li>识别问题的并行性</li><li>分解任务与数据</li><li>设计高效的通信模式</li><li>权衡各种资源约束</li></ul><h3 id="wei-shi-yao-zhong-yao" tabindex="-1" id="为什么重要">为什么重要</h3><p>硬件在变，但思维方式持久。</p><ul><li>CUDA 可能被其他技术取代</li><li>但并行思维的核心原则不变</li><li>掌握思维方式 &gt; 记住 API</li></ul><h2 id="wen-ti-fen-jie" tabindex="-1" id="问题分解">问题分解</h2><h3 id="ren-wu-bing-xing-vs-shu-ju-bing-xing" tabindex="-1" id="任务并行-vs-数据并行">任务并行 vs 数据并行</h3><p><strong>任务并行（Task Parallelism）</strong>：</p><ul><li>不同的处理器执行不同的任务</li><li>适合异构任务</li><li>例：流水线处理</li></ul><p><strong>数据并行（Data Parallelism）</strong>：</p><ul><li>相同的操作应用到不同的数据</li><li>适合同构任务</li><li>例：向量加法、矩阵乘法</li></ul><p><strong>GPU 偏好数据并行</strong>：</p><ul><li>SIMT 模型天然支持</li><li>数千线程执行相同代码</li><li>分支发散会降低效率</li></ul><h3 id="fen-jie-ce-lue" tabindex="-1" id="分解策略">分解策略</h3><p><strong>输出驱动</strong>：每个线程负责计算一个输出元素</p><ul><li>适合输出位置确定的问题</li><li>例：矩阵乘法、图像滤波</li></ul><p><strong>输入驱动</strong>：每个线程处理一个输入元素</p><ul><li>适合输出需要聚合的问题</li><li>例：直方图、归约</li></ul><p><strong>混合</strong>：根据问题特性灵活选择</p><ul><li>例：稀疏矩阵——每行一个线程或每 Warp 一行</li></ul><h2 id="suan-fa-she-ji-mo-shi" tabindex="-1" id="算法设计模式">算法设计模式</h2><h3 id="chang-jian-bing-xing-mo-shi" tabindex="-1" id="常见并行模式">常见并行模式</h3><p>经过前面章节的学习，你已经见过这些经典模式：</p><table><thead><tr><th>模式</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td>Map</td><td>一对一变换</td><td>ReLU、标量乘法</td></tr><tr><td>Reduce</td><td>多对一聚合</td><td>求和、最大值</td></tr><tr><td>Scan</td><td>前缀操作</td><td>流压缩、基数排序</td></tr><tr><td>Stencil</td><td>邻域模式</td><td>卷积、PDE 求解</td></tr><tr><td>Gather</td><td>间接读取</td><td>查表、稀疏访问</td></tr><tr><td>Scatter</td><td>间接写入</td><td>直方图、排序分桶</td></tr></tbody></table><h3 id="mo-shi-zu-he" tabindex="-1" id="模式组合">模式组合</h3><p>复杂算法通常是基本模式的组合：</p><p><strong>基数排序</strong> = Histogram + Scan + Scatter</p><p><strong>稀疏矩阵-向量乘</strong> = Gather + Reduce</p><p><strong>BFS</strong> = Gather + Scatter + Reduce</p><p>识别这些模式有助于快速设计并行算法。</p><h2 id="xing-neng-you-hua-si-wei" tabindex="-1" id="性能优化思维">性能优化思维</h2><h3 id="ping-jing-fen-xi" tabindex="-1" id="瓶颈分析">瓶颈分析</h3><p>任何程序都有瓶颈，优化要找对方向：</p><p><strong>计算瓶颈</strong>：</p><ul><li>算术操作是限制因素</li><li>优化：减少操作数、使用快速数学函数</li></ul><p><strong>内存瓶颈</strong>：</p><ul><li>数据传输是限制因素</li><li>优化：提高缓存利用、减少访问次数</li></ul><p><strong>延迟瓶颈</strong>：</p><ul><li>等待时间是限制因素</li><li>优化：增加并行度隐藏延迟</li></ul><h3 id="roofline-mo-xing" tabindex="-1" id="Roofline-模型">Roofline 模型</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">性能 = min(峰值算力, 带宽 × 算术强度)</span><br></pre></td></tr></table></figure><p><strong>算术强度</strong> = FLOP / Byte</p><table><thead><tr><th>算术强度</th><th>瓶颈类型</th><th>优化方向</th></tr></thead><tbody><tr><td>&lt; 10</td><td>内存受限</td><td>提高数据复用</td></tr><tr><td>&gt; 10</td><td>计算受限</td><td>优化计算效率</td></tr></tbody></table><h3 id="you-hua-ceng-ci" tabindex="-1" id="优化层次">优化层次</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────┐</span><br><span class="line">│ 第四层：算法层                           │</span><br><span class="line">│   选择更高效的算法                       │</span><br><span class="line">│   减少总操作数                           │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第三层：并行策略层                       │</span><br><span class="line">│   任务分解方式                           │</span><br><span class="line">│   负载均衡                               │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第二层：内存层                           │</span><br><span class="line">│   全局/共享/寄存器的使用                 │</span><br><span class="line">│   访问模式优化                           │</span><br><span class="line">├─────────────────────────────────────────┤</span><br><span class="line">│ 第一层：指令层                           │</span><br><span class="line">│   循环展开                               │</span><br><span class="line">│   指令级并行                             │</span><br><span class="line">└─────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p><strong>从上往下优化</strong>：算法级改进的收益最大。</p><h2 id="quan-heng-yu-jue-ce" tabindex="-1" id="权衡与决策">权衡与决策</h2><h3 id="chang-jian-quan-heng" tabindex="-1" id="常见权衡">常见权衡</h3><p><strong>1. 并行度 vs 冗余计算</strong></p><p>增加并行度可能需要冗余计算：</p><ul><li>Tiling 需要 Halo 区域重叠</li><li>归约需要额外的合并操作</li></ul><p><strong>2. 内存使用 vs 计算效率</strong></p><ul><li>预计算查找表：省计算，费内存</li><li>实时计算：省内存，费计算</li></ul><p><strong>3. 通用性 vs 专用优化</strong></p><ul><li>通用代码易维护</li><li>专用优化性能好</li><li>模板/代码生成可以兼顾</li></ul><h3 id="jue-ce-kuang-jia" tabindex="-1" id="决策框架">决策框架</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">1. 理解问题</span><br><span class="line">   - 输入/输出特性</span><br><span class="line">   - 计算密度</span><br><span class="line">   - 数据依赖</span><br><span class="line"></span><br><span class="line">2. 选择算法</span><br><span class="line">   - 理论复杂度</span><br><span class="line">   - 并行友好度</span><br><span class="line">   - 实现复杂度</span><br><span class="line"></span><br><span class="line">3. 设计并行策略</span><br><span class="line">   - 分解方式</span><br><span class="line">   - 同步需求</span><br><span class="line">   - 通信模式</span><br><span class="line"></span><br><span class="line">4. 实现与优化</span><br><span class="line">   - 先正确，后优化</span><br><span class="line">   - Profile 驱动</span><br><span class="line">   - 迭代改进</span><br></pre></td></tr></table></figure><h2 id="ke-kuo-zhan-xing-si-wei" tabindex="-1" id="可扩展性思维">可扩展性思维</h2><h3 id="qiang-kuo-zhan-yu-ruo-kuo-zhan" tabindex="-1" id="强扩展与弱扩展">强扩展与弱扩展</h3><p><strong>强扩展（Strong Scaling）</strong>：</p><ul><li>问题规模固定</li><li>增加处理器，减少时间</li><li>受 Amdahl 定律限制</li></ul><p><strong>弱扩展（Weak Scaling）</strong>：</p><ul><li>每处理器负载固定</li><li>增加处理器，处理更大问题</li><li>受 Gustafson 定律指导</li></ul><h3 id="amdahl-ding-lu" tabindex="-1" id="Amdahl-定律">Amdahl 定律</h3><p><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mtext>加速比</mtext><mo>=</mo><mfrac><mn>1</mn><mrow><mo stretchy="false">(</mo><mn>1</mn><mo>−</mo><mi>P</mi><mo stretchy="false">)</mo><mo>+</mo><mfrac><mi>P</mi><mi>N</mi></mfrac></mrow></mfrac></mrow><annotation encoding="application/x-tex">加速比 = \frac{1}{(1-P) + \frac{P}{N}}</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord cjk_fallback">加速比</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:2.4288em;vertical-align:-1.1073em;"></span><span class="mord"><span class="mopen nulldelimiter"></span><span class="mfrac"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:1.3214em;"><span style="top:-2.2377em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mopen">(</span><span class="mord">1</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">P</span><span class="mclose">)</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mord"><span class="mopen nulldelimiter"></span><span class="mfrac"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8723em;"><span style="top:-2.655em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight" style="margin-right:0.10903em;">N</span></span></span></span><span style="top:-3.23em;"><span class="pstrut" style="height:3em;"></span><span class="frac-line" style="border-bottom-width:0.04em;"></span></span><span style="top:-3.394em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">P</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.345em;"><span></span></span></span></span></span><span class="mclose nulldelimiter"></span></span></span></span><span style="top:-3.23em;"><span class="pstrut" style="height:3em;"></span><span class="frac-line" style="border-bottom-width:0.04em;"></span></span><span style="top:-3.677em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">1</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:1.1073em;"><span></span></span></span></span></span><span class="mclose nulldelimiter"></span></span></span></span></span></span></p><p>其中 P 是可并行部分比例，N 是处理器数。</p><p><strong>启示</strong>：串行部分决定加速上限。</p><p><strong>例</strong>：95% 可并行，最大加速比 = 20×（无论多少 GPU）</p><h3 id="gustafson-ding-lu" tabindex="-1" id="Gustafson-定律">Gustafson 定律</h3><p><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mtext>加速比</mtext><mo>=</mo><mi>N</mi><mo>−</mo><mo stretchy="false">(</mo><mn>1</mn><mo>−</mo><mi>P</mi><mo stretchy="false">)</mo><mo stretchy="false">(</mo><mi>N</mi><mo>−</mo><mn>1</mn><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex">加速比 = N - (1-P)(N-1)</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord cjk_fallback">加速比</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.7667em;vertical-align:-0.0833em;"></span><span class="mord mathnormal" style="margin-right:0.10903em;">N</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord">1</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.13889em;">P</span><span class="mclose">)</span><span class="mopen">(</span><span class="mord mathnormal" style="margin-right:0.10903em;">N</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord">1</span><span class="mclose">)</span></span></span></span></span></p><p><strong>启示</strong>：增大问题规模可以提高并行效率。</p><p><strong>例</strong>：95% 可并行，100 GPU，加速比 = 95×</p><h2 id="diao-shi-yu-yan-zheng" tabindex="-1" id="调试与验证">调试与验证</h2><h3 id="bing-xing-cheng-xu-diao-shi-tiao-zhan" tabindex="-1" id="并行程序调试挑战">并行程序调试挑战</h3><ul><li><strong>不确定性</strong>：竞态条件导致结果不可重复</li><li><strong>规模</strong>：数千线程难以逐一追踪</li><li><strong>隐蔽性</strong>：边界情况难以触发</li></ul><h3 id="ce-lue" tabindex="-1" id="策略">策略</h3><p><strong>1. 简化问题</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">先在小规模数据上测试</span><br><span class="line">1 个 Block → 多 Block</span><br><span class="line">1 个 Warp → 多 Warp</span><br></pre></td></tr></table></figure><p><strong>2. 串行参考</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 总是保留串行版本用于验证</span></span><br><span class="line"><span class="type">float</span> result_cpu = compute_cpu(data, n);</span><br><span class="line"><span class="type">float</span> result_gpu = compute_gpu(data, n);</span><br><span class="line">assert(<span class="built_in">abs</span>(result_cpu - result_gpu) &lt; epsilon);</span><br></pre></td></tr></table></figure><p><strong>3. 使用工具</strong></p><table><thead><tr><th>工具</th><th>用途</th></tr></thead><tbody><tr><td>cuda-memcheck</td><td>内存错误检测</td></tr><tr><td>Nsight Compute</td><td>性能分析</td></tr><tr><td>Nsight Systems</td><td>系统级分析</td></tr><tr><td>printf</td><td>简单调试（慎用）</td></tr></tbody></table><p><strong>4. 渐进式开发</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1. 实现朴素版本，验证正确性</span><br><span class="line">2. 添加一个优化，验证正确性</span><br><span class="line">3. 重复直到满足性能需求</span><br></pre></td></tr></table></figure><h2 id="kua-ping-tai-si-wei" tabindex="-1" id="跨平台思维">跨平台思维</h2><h3 id="ying-jian-duo-yang-xing" tabindex="-1" id="硬件多样性">硬件多样性</h3><p>不同 GPU 架构有不同特性：</p><table><thead><tr><th>特性</th><th>Kepler</th><th>Pascal</th><th>Volta</th><th>Ampere</th></tr></thead><tbody><tr><td>SM 内存(KB)</td><td>64</td><td>64</td><td>96</td><td>164</td></tr><tr><td>L2 缓存(MB)</td><td>1.5</td><td>4</td><td>6</td><td>40</td></tr><tr><td>Tensor Core</td><td>无</td><td>无</td><td>有</td><td>有</td></tr></tbody></table><p><strong>策略</strong>：</p><ul><li>使用运行时查询能力</li><li>参数化关键常量</li><li>针对目标硬件调优</li></ul><h3 id="ke-yi-zhi-xing" tabindex="-1" id="可移植性">可移植性</h3><p><strong>CUDA 之外</strong>：</p><ul><li>OpenCL：跨厂商</li><li>SYCL：C++ 标准化</li><li>HIP：AMD GPU</li></ul><p><strong>抽象库</strong>：</p><ul><li>Thrust：STL 风格</li><li>CUB：低级原语</li><li>cuBLAS/cuDNN：领域特定</li></ul><h2 id="chi-xu-xue-xi" tabindex="-1" id="持续学习">持续学习</h2><h3 id="ji-zhu-yan-jin" tabindex="-1" id="技术演进">技术演进</h3><p>GPU 技术快速发展：</p><p><strong>过去</strong>：可编程着色器 → CUDA 诞生<br><strong>现在</strong>：Tensor Core、光线追踪<br><strong>未来</strong>：量子模拟？神经形态计算？</p><p><strong>保持学习</strong>：</p><ul><li>阅读最新论文</li><li>关注 GTC 大会</li><li>参与开源项目</li></ul><h3 id="she-qu-zi-yuan" tabindex="-1" id="社区资源">社区资源</h3><ul><li><strong>NVIDIA Developer Blog</strong>：技术文章</li><li><strong>CUDA Zone</strong>：官方资源</li><li><strong>GitHub</strong>：开源项目</li><li><strong>Stack Overflow</strong>：问题解答</li><li><strong>课程</strong>：UIUC、Stanford 公开课</li></ul><h2 id="zong-jie-bing-xing-bian-cheng-xin-fa" tabindex="-1" id="总结：并行编程心法">总结：并行编程心法</h2><h3 id="shi-tiao-yuan-ze" tabindex="-1" id="十条原则">十条原则</h3><ol><li><p><strong>理解硬件</strong>：了解你的 GPU 架构，扬长避短</p></li><li><p><strong>数据为王</strong>：并行程序性能通常受限于数据移动</p></li><li><p><strong>最大化并行</strong>：暴露足够的并行性隐藏延迟</p></li><li><p><strong>最小化同步</strong>：同步是性能杀手</p></li><li><p><strong>合并访问</strong>：让内存访问连续</p></li><li><p><strong>复用数据</strong>：共享内存是你最好的朋友</p></li><li><p><strong>避免发散</strong>：让同一 Warp 的线程走相同路径</p></li><li><p><strong>权衡取舍</strong>：没有银弹，只有适合具体问题的解</p></li><li><p><strong>Profile 优先</strong>：数据驱动优化，不要猜测</p></li><li><p><strong>渐进迭代</strong>：先正确，后优化，持续改进</p></li></ol><h3 id="cong-ji-zhu-dao-si-wei" tabindex="-1" id="从技术到思维">从技术到思维</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">初学者：学习 API 和语法</span><br><span class="line">         ↓</span><br><span class="line">进阶者：掌握优化技巧</span><br><span class="line">         ↓</span><br><span class="line">高手：形成并行思维</span><br><span class="line">         ↓</span><br><span class="line">专家：能设计新算法</span><br></pre></td></tr></table></figure><p>这本书带你走完了前两个阶段，后两个阶段需要通过实践和持续学习来达成。</p><h2 id="xiao-jie" tabindex="-1" id="小结">小结</h2><p>第十九章是全书的升华，总结了并行编程的核心思维：</p><p><strong>计算思维</strong>：用计算的视角看问题——分解、模式匹配、抽象、算法设计。</p><p><strong>问题分解</strong>：任务并行 vs 数据并行，输出驱动 vs 输入驱动。根据问题特性选择。</p><p><strong>设计模式</strong>：Map、Reduce、Scan、Stencil、Gather、Scatter——识别这些模式能快速构建解决方案。</p><p><strong>性能思维</strong>：瓶颈分析、Roofline 模型、分层优化。从算法层开始，逐层向下。</p><p><strong>权衡决策</strong>：没有完美方案，只有最适合当前约束的方案。理解各种权衡才能做出好决策。</p><p><strong>可扩展性</strong>：Amdahl vs Gustafson，强扩展 vs 弱扩展。理解极限有助于设定合理预期。</p><p>CUDA 只是工具，思维方式才是核心竞争力。希望这本书不仅教会你 CUDA 编程，更培养了你的并行计算思维。</p><hr><p><strong>参考资料：</strong></p><ul><li>Hwu, W., Kirk, D., &amp; El Hajj, I. (2022). <em>Programming Massively Parallel Processors: A Hands-on Approach</em> (4th Edition). Morgan Kaufmann.</li><li>Wing, J. M. (2006). <em>Computational Thinking</em>. Communications of the ACM.</li><li>Williams, S., et al. (2009). <em>Roofline: An Insightful Visual Performance Model</em>. Communications of the ACM.</li></ul><hr><blockquote><p><strong>本文 GitHub 仓库</strong>: <a href="https://github.com/psmarter/PMPP-Learning">https://github.com/psmarter/PMPP-Learning</a></p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;qian-yan&quot; tabindex=&quot;-1&quot; id=&quot;前言&quot;&gt;前言&lt;/h2&gt;
&lt;p&gt;经过前十八章的学习，我们掌握了 CUDA</summary>
        
      
    
    
    
    <category term="知识分享" scheme="https://smarter.xin/categories/knowledge-sharing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/categories/knowledge-sharing/pmpp/"/>
    
    
    <category term="性能优化" scheme="https://smarter.xin/tags/performance-optimization/"/>
    
    <category term="CUDA" scheme="https://smarter.xin/tags/cuda/"/>
    
    <category term="GPU编程" scheme="https://smarter.xin/tags/gpu-programming/"/>
    
    <category term="并行计算" scheme="https://smarter.xin/tags/parallel-computing/"/>
    
    <category term="PMPP" scheme="https://smarter.xin/tags/pmpp/"/>
    
    <category term="计算思维" scheme="https://smarter.xin/tags/computational-thinking/"/>
    
    <category term="方法论" scheme="https://smarter.xin/tags/methodology/"/>
    
  </entry>
  
</feed>
